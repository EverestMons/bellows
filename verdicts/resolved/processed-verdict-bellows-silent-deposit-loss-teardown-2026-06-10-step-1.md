verdict: continue
Rule 22(b) substance check PASS — decisive diagnostic. It REFUTED the BACKLOG "auto-staged dirs" hypothesis (zero auto-staging code exists) and pinned the real predicate: a deposit survives teardown iff the agent committed it to the worktree branch; directory is irrelevant. Teardown is a git merge of committed changes only — untracked/staged-only/modified-unstaged files are destroyed at worktree removal while deposit_exists reads green (checks disk) and files_changed=0 (git diff blind to untracked). Reconciled the contradiction correctly (this session's architecture/ deposits survived because the SAs were instructed to self-commit; v1 lab-fix's did not). Fix shapes (a) auto-commit plan-declared deposits + (b) fail-loud gate are sound, Layer-1 mechanical (commits plan-declared paths only, no content judgment), and additive to the hardened teardown family (land-or-raise + Gap 1b/1c/2a confirmed untouched). CEO disposition: SHIP (a)+(b) with a PLANNER ORDERING CORRECTION — (a) auto-commit runs BEFORE gates.check() so deposit_exists evaluates post-commit state, and (b) is the residue net catching declared deposits (a) could not resolve/commit; Bellows auto-commits plan-declared deposit paths via a path-scoped git add (NOT git add -A). Noted boundary (recorded, out of scope for this fix): uncommitted NON-declared changes (e.g., source) are not auto-preserved by (a) — DEV-commit discipline covers it; "make the worktree fully loss-proof" is a candidate follow-on. Design approved as the basis for the implementing executable (scope: bellows.py + gates.py + tests; daemon restart to activate). All gates passed clean; SA self-committed its deposit (self-protection held).
