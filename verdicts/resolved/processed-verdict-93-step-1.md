continue

Planner Rule 22(b) substance review of Step 1 (heaviest strip-only cut) — PASS. Precise, well-documented strip per blueprint + the locked CEO keep-/team decision: 9 team-CRUD routes + 3 helpers (_dashboard_team_card, _tier_base_query, get_worklist_tiers) removed from app.py; 5 routes KEPT-but-stripped (/team strips branch-coverage + worklist-health blocks; /api/cards/team strips worklist_health card; /workload/list + export strip team_id/teams dropdown; /team/<employee_id> strips worklist-tiers + expected-vs-actual). web/team.py: Assignment Rules (172-368) removed, Employee Manager (1-170, kept team_members) retained, unused imports cleaned. workload.py: team_id param + team_memberships subquery removed. Templates: 3 removed (team_manage, _team_report_card, partials/card_worklist_health), 3 stripped (workload_list, team, team_member_profile); dashboard.html nav card kept (per decision). Independent Planner verification: `import app, web.team, engines.workload` all resolve cleanly (the critical check for a heavy app.py strip); all 4 kept route functions present (team_dashboard, workload_list_page, team_member_profile, workload_list_export); all 6 removed routes/helpers gone; web/team.py still references kept team_members 12x (Employee Manager intact); collection = 1733, ZERO errors, exact (1734 - 1 test removed). FROZEN clean (confidence/validator/validate_batch/ingestion untouched; team_members + assigned_user untouched). scope_check clean (no false-flag — inlined test path). 121 targeted tests pass. Release Step 2 (schema: drop team_memberships → teams → employee_assignments → worklist_snapshots, FK order, v12). WATCH for Step 3 QA: only 1 test removed — confirm no team-CRUD-route test now fails (404) in the full suite; reconcile against true prior 1734.
