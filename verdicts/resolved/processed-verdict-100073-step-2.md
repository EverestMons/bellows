continue

continue — all gate rows PASS at the QA checkpoint; Rule 22(b) substance read by the Planner: `lifecycle.mark_step_complete` (lifecycle.py:501) flips only an `awaiting_verdict` row and returns the rowcount; `_consume_verdicts` calls it on both continue branches (bellows.py +6, guarded on `_lc_plan_id`, the count logged); `tools/reconcile_steps.py` LISTs, refuses `--apply` while a plan is in flight, and repairs closed rows only; ten tests read the row; the mutation run's last line `MUTATION: 5 killed, 0 survived, 0 error`; the full suite `2242 passed, 1 skipped`; the receipt's Rule 20 block PASSED. The QA met a flaw in the plan's own Item 2 (a copy of the live DB always carries the running plan, so `--apply` on it is refused by design) by applying on a second copy with #100073 marked closed: 17 rows flipped, rowcount 1 each, LIST-again 0 closed/awaiting, the six halted rows unchanged — the evidence the item wanted. Merged at the QA pause (`c1ba53f`). Post-close acts owed: the daemon restart (bellows.py changed), then the Planner's one `tools/reconcile_steps.py --apply` on the live DB.
