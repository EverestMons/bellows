verdict: continue
Plan 267 Step 1 (DEV — engine seam) verified clean by the Planner. All gates PASS (scope_check, deposit_exists, rule_22_verification; 9 files_changed, exactly the declared scope).

Rule 22(b): all five `query(conn)` extractions match the diag-266 Q2 bodies — each takes an already-open connection, executes the module-level SQL constants, returns structured data, and opens nothing / writes nothing / prints nothing. `run()` behaviour is preserved in every script (same return tuples; receipt and stdout bytes unchanged apart from the intentional Task C marker). `scripts/__init__.py` + `scripts/queries/__init__.py` created and all five `query` imports verified from the repo root. Task C corrected the misnomer `[INCONSISTENT]` → `[NON-MONDAY EFFECTIVE DAY]` with the CEO's domain rationale preserved in-code (timing_rule and effective_day are independent; differing effective days are expected, not defects). Targeted tests 13/13 pass across all five script test files, and files_changed confirms the ONLY test file touched was `tests/test_prior_monday_detail.py` — the single permitted edit. The other four script tests passing UNCHANGED is itself the behaviour-preserving proof.

Accepted residue (not a defect, recorded): the CLI stdout HEADER still reads `N inconsistent (effective_day != monday)`. The DEV flagged this honestly — correcting it needs a second existing-test edit (`test_header_counts`), which this plan's single-edit guard prevented; the guard worked as designed. This does NOT reach the new surface: the Step-2 endpoints render their own summary from `query()`'s structured data and are already instructed to use neutral, non-defect wording. The CLI header wording is a small follow-up, not a blocker.

Proceed to Step 2 (the `/system/queries` route, template, nav, and the Write Receipt action — under the leak rule: rendering must write nothing).
