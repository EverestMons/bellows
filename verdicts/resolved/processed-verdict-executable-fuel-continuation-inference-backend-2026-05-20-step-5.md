verdict: continue
Create invoice-pulse/knowledge/research/LESSONS.md with one entry capturing the multi-pause syntax limitation surfaced this session: Bellows' pause_for_verdict header accepts only a single enum value (after_step_1 | after_qa_step | always); comma-separated lists like 'after_step_1, after_step_4' silently match nothing and produce zero pauses. The Bellows parser warning fires at deposit but is not blocking. Plans needing multiple pauses currently have no syntax — must rely on auto-pause behaviors (QA step auto-detection, gate failures, single after_step_1 for sanity gates). Use this entry as the file's first content. Then complete the remaining Step 5 docs (PROJECT_STATUS, domain glossary) as specified.
