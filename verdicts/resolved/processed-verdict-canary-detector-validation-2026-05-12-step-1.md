verdict: continue
Canary closed with two findings. Finding 1 — the new intermediate-decision detector has a structural bug: `bellows/decisions.py::extract_decision_blocks` reads `event.get("content", [])` but the actual Claude CLI NDJSON stream uses `event.get("message", {}).get("content", [])`. The Step 1 unit tests passed because they used synthetic flat-shape fixtures; production data uses the wrapped shape. The detector returns zero matches on every real plan because no assistant events are ever recognized. Confirmed by parsing the canary's `bellows/logs/20260512-184339-step.json`: 34 assistant events present, but the detector's code path finds 0 because of the structure mismatch. Also confirmed `intermediate_decisions` key absent from the step.json's top-level dict — the detector either ran and returned empty, or didn't run at all (additional surface to investigate during the fix). Finding 2 — the canary design was insufficient: even with the bug corrected, the agent produced only 3 assistant text blocks total during execution and none contained any of the 33 phrases. The "deliberate ambiguity" instruction didn't reliably force phrase-matched narration; the agent committed to a framing choice without narrating it. Future canary designs should engineer scenarios that produce phrase-matched narration with higher probability (e.g., a task with an in-flight error the agent must diagnose and correct). The bug finding is dispositive — the detector needs a fix plan before any further canary is meaningful. Plan moved to Done/ by Planner per disable-auto-close model.
