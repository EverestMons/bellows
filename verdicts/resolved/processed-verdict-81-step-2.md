continue

Planner Rule 22(b) substance review of Step 2 (reactive wiring) — PASS. The single approved FROZEN-core touch is confirmed surgical: validator.py diff is 6 lines (genuine_variance = [r for r in failed if r.get("source") != "none"]; task_code = CarrRevAcc if genuine_variance else None) confined to gate_9_accessorials' any_fail return — no tier resolution, GateResult structure, or other gate logic altered. Mixed variance+missing correctly yields CarrRevAcc. Reactive upsert in action_router _build_fill_gap uses ON CONFLICT DO UPDATE, preserves gap_source='declared', re-opens (is_loaded=0/resolved_at=NULL) on recurrence, guarded by _table_exists + try/except. Auto-resolve in dismiss_filled_gap_actions sets is_loaded=1/resolved_at with is_loaded=0 guard. rates.py surfaces dropped_rows in the import response and upserts base_rates requirements for contracts linked via base_tariff_doc_id. FROZEN clean (confidence.py/validate_batch.py untouched). Two existing Gate-9 CarrRevAcc tests verified to assert genuine-variance only — no weakening needed. scope_check clean: validator.py, action_router.py, rates.py. 302 targeted tests pass, only pre-existing failure. Release Step 3 (gap-dashboard proactive surfacing).
