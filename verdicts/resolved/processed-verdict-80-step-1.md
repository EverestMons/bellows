continue

Planner Rule 22(b) substance review of Step 1 (DEV) — PASS. extraction_provenance TEXT DEFAULT 'parsed' added to all 7 target tables via idempotent _safe_add_column; 4 source-tracking tables left unchanged with llm_assisted reserved; backfill covered by column DEFAULT (no NULLs); 20 write sites tagged parsed/manual across gap_dashboard.py, contracts.py, documents.py; web/rates.py correctly untouched (tariff_rates uses existing source). No site emits llm_assisted (locked decision 2 honored). contract_fuel_table new column independent of existing source (bracket-origin) column. FROZEN boundary clean (confidence.py, validator.py, validate_batch.py, element_invoice_evidence, confidence_state untouched). scope_check clean. 806 targeted tests pass, only pre-existing failures. Two non-blocking semantic-tagging refinements noted for revisit during B/C authoring (legacy *_bulk paste routes tagged 'manual' where 'parsed' is arguable; some out-of-scope UI routes ride the 'parsed' default) — neither affects current behavior (provenance is inert until B/C; B keys only on llm_assisted). Release Step 2 (QA + closeout).
