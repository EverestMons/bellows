continue

Planner Rule 22(b) substance review of Step 2 (validator use-with-warning + persist) — PASS. Verified the validator.py diff directly: 15 insertions / 3 deletions, purely annotate-only. The 3 deletions are the 3 tariff_rates SELECTs gaining ", confirmation_status" (identical FROM/WHERE/logic); all other additions are new data keys — rate_confirmed = <resolved_row>.get("confirmation_status","confirmed") != "unconfirmed" for gates 5/7/8/9, plus the gate-9 aggregate gate9_rate_confirmed = all(r.get("rate_confirmed",True) for resolved lines). No change to tier resolution, expected-value computation, task codes, blocking status, or GateResult structure beyond the additive key. validate_batch.py persists rate_confirmed (1/0) in both persistence paths mirroring the exec-78 rate_source pattern; defaults True for gates that resolve no rate. Provably non-regressive: all current rate rows are confirmed (Step-1 DEFAULT), so rate_confirmed=1 everywhere and validation output is byte-identical except the additive key — confirmed by 644 targeted tests passing, zero regressions. FROZEN clean (confidence.py/element_invoice_evidence/confidence_state untouched; no batch-orchestration change). scope_check clean: engines/validator.py, validate_batch.py. Release Step 3 (full-suite QA + closeout).
