verdict: continue
Rule 22(b) substance check PASS — comprehensive blueprint conforming to the locked Phase 0 coverage map. The 27-site filename/slug consumer census (grep-grounded, file:line per row) is the load-bearing deliverable and found two latent substring-match bugs that become ACTIVE with bare-integer ids: bellows.py:1424 (`plan_slug in pname`) and bellows.py:1521-1538 (Done/halted stale scan) — "142" false-matches inside "1423"; both convert to exact comparison in the same change that ships id naming. All other 25 sites tolerate the rename as-is (verdict regexes, orphan-guard exact set membership, shadow cache, branch naming, Done/halted filing). Mint design sound: id + plans row in one BEGIN IMMEDIATE transaction (no burned ids), one rename not two at the existing claim-rename site (bellows.py:411), crash-window recovery via plans.lifecycle_state + deposit_placeholder_name. Deposit placeholder `<type>-draft-<HHMMSS>.md` validated against is_runnable_plan/validate_at_claim/_parse_plan_header (all filename-agnostic or tolerant). DB write surface mapped to 7 transition boundaries with conforming DDL (pointers only, gate_events core); lifecycle writes are net-new lifecycle.py call sites, not record_run extensions — correctly argued. Commit id-tagging via bootstrap-prompt injection + daemon auto-stage/merge message edits; no rewriting. Dual-format tolerance proven; date-rollover scope_check kill confirmed as convention-level at gates.py:721. CEO disposition: design APPROVED as basis for the implementing executables with the recommended A→B split (A: id-threading + substring fixes + minimal lifecycle.db subset [id_sequence + plans — A's mint requires it, pin this explicitly in A's scope]; B: remaining tables + 7-boundary writes). Planner-side convention changes (draft-placeholder deposits, [id] commit tags, "implements diagnostic N" citation) ride with the same wave incl. PLANNER_TEMPLATE update. Terminal step — route to Done.
