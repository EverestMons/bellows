verdict: continue
Rule 22 deposit verification complete. QA report `bellows/knowledge/qa/deposit-exists-worktree-aware-qa-2026-05-06.md` exists, well-formed, contains 7-row verification table all PASS (6 unit tests + full-suite regression), tests-before/after counts (212→218, 0 regressions), 6 evidence file references, Rule 20 self-check PASSED. BACKLOG #1 close annotation confirmed in `bellows/knowledge/BACKLOG.md` Closed section. Fix shipped: gates.py + bellows.py threaded with wt_path; Strategy 0 worktree-first resolution preserves bellows-self compatibility via `if wt_path != project_path` guard. **Bellows daemon restart required to pick up the new code** — the running daemon is still pre-fix.
