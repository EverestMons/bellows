verdict: continue

Terminal close (Rule 22, deep pass + evidence read). Gate Passed=True; rule_20_self_check PASS (banner byte-exact), rule_22 PASS (table honest — 5 failures disclosed, not hidden), scope_check PASS, no worktree_teardown failure. Substance: merge-ff teardown correct in source (ff→no-ff→abort+raise, legacy detection, branch-cleanup ordering, Gap-1b coherent, no rebase/cherry-pick/lifecycle/retry symbols); 6 scenario tests pass incl. invariant tripwire + legacy; abort-clean confirmed. The 5 suite failures (test_decisions.py x4, test_runner_parser.py x1) are PRE-EXISTING — confirmed failing on baseline bf3b679, in files DEV never touched — NOT regressions. Suite 443/448 with those 5 pre-existing reds. Plan B complete. NOTE: new merge model goes live only on daemon restart + canary; pre-existing red tests filed as separate hygiene item. Closing to Done.
