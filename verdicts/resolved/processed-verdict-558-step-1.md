stop

Honest agent HALT on a real instrument mismatch, no work performed: the agent hashed with BARE shasum (SHA-1 default → 450fcf33...) against the plan's SHA-256 pin (815ce5a4... — verified correct against the live file by the Planner, both algorithms reproduced). The deposit_exists failures are the halt's consequence, not missing work. Recovery per the grammar: corrected re-deposit under the stable slug with the algorithm trap named inline at the pin.
