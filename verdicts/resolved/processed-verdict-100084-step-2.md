continue

Both steps verified at the (b) read. DEV: _step_commit_shas (git rev-list --reverse pre..post in the worktree; [] on empty/equal shas, WARN and [] on failure) and its two call sites after record_gate_events at both step-end regions; record_commits deduped within the plan (the SELECT through steps.plan_id, a repeat inside one call skipped, returns the count); eight tests as drafted (t1-t3 lifecycle, t4-t6 on the git_repo fixture, t7 the continuous two-step run with rows per step and call_count 2, t8 the resumed step under a pre-recorded history); the dev-log pastes the red rows (all three shas under step 2 before the fix) and the green rows after; MUTATION: 5 killed, 0 survived, 0 error. QA: full suite 2290 passed, 2 skipped, redirected; P2's counts re-run on a backup copy (175/59/78-93-4 beside 172/58/76-92-4, the delta being #100083's rows) and the boundary max(plan_id) = 100083 stated as the last merge-step plan, with the sentence that the copy holds none of the new shape; production writes none beyond the two evidence files; Rule 20 PASSED; every gate PASS on both steps. Continue - close. Post-close: both restarts, the per-machine glossary rider at the wrap (boundary 100083 on the mini; the Air's read after its restart), threads done 275 --commit bellows:665b5f4.
