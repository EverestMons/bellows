continue

All gates PASS. Rule 22(b) verified against merged c533f00: the 248-line census SUPERSEDES the Planner's own F1 pin with the decisive mechanism — ingest_lesson_entries matches via _key_heading normalization (src/lessons_forge.py:52-56, shipped by plans 499/500 on 2026-08-21, BEFORE the relabel) — the Planner's hand-rolled dry run compared raw headings where the consumer compares normalized keys, the probe-must-match-representation class measured on the Planner's own probe. Real-function dry run: would_insert 32 / unchanged 313 / would_update 0 / db-only 57, all 715 rows classified with zero unexplained and zero ambiguous; the 57 db-only are the 2026-08-21 reconciliation's removals, all proposals terminal, safely orphaned; the proposal-staling trace shows the update path never fires (0 updates). M-6 Q1 (57-row disposition): under the standing recommended-options directive the Planner adopts the deposit's recommendation — KEEP as historical corpus, zero-risk. No migration executable is needed; the follow-up is the standard 32-entry ingest per the gap table (ids 371-402 predicted, pre-backup mandated, count verifications specified).

Terminal step of 1 — plan closes. The ingest executable authors next from the gap table.
