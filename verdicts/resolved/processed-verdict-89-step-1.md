continue

Planner Rule 22(b) override of a benign scope_check FAIL + deep substance review (largest, most core-adjacent cut) — PASS. The gate flagged 4 test files + web/templates/intelligence.html out-of-scope, but the plan body explicitly scoped "test files that assert on these 8 tables" and "associated templates" — paths just not inlined (same benign pattern as #81/#85/#88). I verified the riskiest claims directly against the Step-1 commit (b6e0e70):

LOAD-BEARING RISKS — both confirmed SAFE: (1) carrier_identity.py removed wholesale — caller-trace + glossary confirm carrier-merge is COSMETIC (groups T-codes in the intelligence dashboard only); core carrier resolution uses direct carrier_code matching in contract_matcher/validator, no carrier_group involvement. (2) circuit_breaker.py removed wholesale — not load-bearing; check_contradictions degraded to {"clear": True} when the table was absent anyway.

ACTION_ROUTER (core-adjacent) — diff verified circuit-breaker/notifications-only: removed the check_contradictions import, carriers_checked cache, cb_status param from _determine_action_type/_route_pricing_failure. The one behavior change — dropping "and cb_status.get('clear', True)" from the DISPUTE_EMAIL gate — is EQUIVALENT to post-table-drop behavior (the breaker returns clear once the table is gone), not a new change. Decision tree (APPROVE/FILL_GAP/DISPUTE_EMAIL/priority scoring) otherwise intact. _check_contract_renewals gutted to a clean no-op stub (callers preserved).

INTEGRITY: FROZEN absent from commit (confidence.py/validator.py/validate_batch.py/element_invoice_evidence). ZERO orphaned imports of the 4 removed modules in non-test code. Import smoke (app, action_router, carrier_profiles, validate_batch) resolves. Collection = 1830, ZERO errors, reconciliation EXACT (1871 - 41 removed = 1830). Good discrimination: kept /api/carriers/<id>/intelligence (activity analytics, different feature) + its card template. Whole modules removed: carrier_identity, location_intelligence, circuit_breaker, web/intelligence (+ template + blueprint + nav). Release Step 2 (schema: drop 8 tables, FK order carrier_group_members before carrier_groups, v10). QA already has the count baseline (1830) + the -v timeout mitigation.
