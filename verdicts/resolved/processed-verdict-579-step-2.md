continue

Delegated continue to Done (clean-gate lane): 7/7 gates, 0 overrides; full suite 1631 passed + 1 skipped = 1632, matching the G6 baseline (this plan adds no tests). THE DETERMINISM PROOF THE 577-578 SEQUENCE WAS MISSING IS NOW ON RECORD, and it covers the exact axis that broke 577: (a) FIVE runs at the primary path, all identical — '2 killed, 0 survived, 0 error' five times; (b) the SECOND-PATH run agrees — and note the two paths are genuinely different environments, the primary being the dispatch worktree /Users/marklehn/Developer/GitHub/bellows/.bellows-worktrees/579 and the second a real git clone at /var/folders/.../tmp.WNgICfguLi/alt. A worktree is precisely where exec-577's mutant diverged from my checkout measurement, so this is not a generic repetition — it is the same axis, now agreeing. (c) The gate_watcher consumer still returns 2 killed / exit 0, so the two added lines caused no regression in the tool's other user. Rule 22(b) verified by the Planner independently: the live-tree hash claim was checked from OUTSIDE the tool — shasum -a 256 returns f7037a1359f1, matching both the receipt's pre/post pair and the tool's own LIVE-TREE UNCHANGED line. WHAT THIS CLOSES: the runner's same-length mutants are now deterministic by construction rather than by luck, because invalidation is forced through CPython's actual (mtime, size) rule instead of depending on whether a run crosses a second boundary. The manifest honestly carries only the two mutants that kill, with a _removed_note recording that both invalidation guards are individually un-mutation-testable precisely because they are jointly sufficient — a redundancy that is a feature, and the second measured case motivating the deferred 'expect: survived' CONTROL-mutant feature. Thread 24's self-application fork is delivered.
