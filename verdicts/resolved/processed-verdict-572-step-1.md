stop

STOP — Planner-found DESIGN DEFECT in the plan I authored; gates are clean (7/7) and the agent built exactly what was specified, so this is my error, not the agent's. THE GUARD'S PREMISE IS FALSE. judge_watch_line treats a pending verdict-request present at arm time as 'PRE-EXISTING — already resolved and awaiting daemon cleanup' (its docstring, from my spec). That is an ASSUMPTION, never checked. A watcher that arms AFTER a step completes but BEFORE the Planner issues a verdict sees a GENUINE, UNRESOLVED pause and silently downgrades it to one log line, then returns None on every later poll while unchanged — a MISSED PAUSE. That is strictly worse than the defect this plan set out to fix, which only produced a spurious log line, and it is the exact failure mode the plan's own MUST-PRESERVE warned about ('the snapshot would swallow every subsequent pause') — I guarded the clear-on-empty case and missed the arm-time case. MEASURED LIVE, on this very plan: my shell watcher armed after 572 step 1 posted verdict-request-572-step-1.md at 08:39:03, recorded it as its snapshot, printed 'arm-time pending ignored', and would have sat 4h without ever reporting a pause that was real and awaiting my verdict; no resolved verdict for 572 existed at that moment. Note the tests could not catch this — all 8 encode the same wrong premise (the synthetic-tests-inherit-the-author's-model law), so a green QA would have certified the defect. THE FIX, for the corrected re-deposit under this stable slug: staleness is KNOWABLE, not assumable — a pending verdict-request-<id>-step-<N>.md is stale IFF a verdict for that plan+step has already been issued, i.e. verdicts/resolved/ contains verdict-<id>-step-<N>.md or processed-verdict-<id>-step-<N>.md (naming confirmed live: processed-verdict-570-step-1/2, -571-step-1/2). Absent that file the pause is GENUINE and must be reported normally even at arm time. Re-deposit will key on the resolved-file check and add the discriminating test this design lacked: arm over an UNRESOLVED pending request and assert a normal awaiting-verdict line.
