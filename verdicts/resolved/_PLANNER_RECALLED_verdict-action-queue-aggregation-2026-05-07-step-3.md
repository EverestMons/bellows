verdict: stop
QA work is sound and verifiable, but the QA report's Rule 20 self-check section does not contain the literal banner string `Rule 20 — QA Self-Check Results` followed by `PASSED — SELF-CHECK PASSED` line as Rule 22(e) requires. Stopping the plan here so a follow-up amendment can fix the banner format. The 8 deposit_exists / rule_20 gate failures from this run are a separate worktree-vs-main path resolution concern (files exist in main, Bellows looked in worktree) — owns to bellows backlog, not to this plan. Plan re-resumes via the amendment executable being deposited next.
