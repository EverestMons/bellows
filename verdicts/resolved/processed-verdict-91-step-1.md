continue

Planner Rule 22(b) substance review of Step 1 (code removal) — PASS. Cleanest cut yet (diag 90 confirmed clean-separable). 4 modules removed wholesale (email_matcher 739, email_parser 219, pst_importer 158, email_triage 958); blueprint dereg in app.py (2 lines: import + register_blueprint); template email_triage.html removed; nav pill removed from disputes_list.html; copilot_prompts comment cleaned (kept the paste_route config key, removed only the trailing comment); 5 test files removed (96 tests: 41+21+24+6+4). Zero-orphan grep clean (no email_matcher/email_parser/pst_importer/email_triage/url_for-email/`/emails` references remain). FROZEN untouched (confidence/validator/validate_batch/action_router/ingestion zero diff). Independent Planner verification: `import app` resolves cleanly after the blueprint dereg; collection = 1734, ZERO errors, reconciliation EXACT (1830 - 96 = 1734). 124 targeted tests pass. scope_check PASSED with no false-flag this time (inlining the test/template/nav paths in the scope line fixed the recurring issue). Release Step 2 (schema: drop email_pro_matches → email_ingestions → pst_emails, v11).
