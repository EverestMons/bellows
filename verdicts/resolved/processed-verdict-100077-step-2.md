continue

continue — all gate rows PASS at the QA checkpoint; Rule 22(b) substance read by the Planner: `hooks/eluvian/wrap_debt_hook.py` gains `_compose_debt_message(checklist)` — the softened header when `[R2/registry]` is in the report, the asserting header otherwise, the checklist and the closing sentence unchanged in both — and `main()` emits it; two tests by path-loading the hook; `MUTATION: 2 killed, 0 survived, 0 error`; the full suite `2264 passed, 1 skipped`; the receipt's Rule 20 block PASSED; QA Item 2 read both envelopes in-process and pasted their first lines. T0, the integration-vs-record pass recorded in the plan. Merged at the QA pause (`e94c617`). The hook is loaded from the repo at each SessionStart, so the next session on this machine reads the new envelope; no restart owed.
