continue

continue — all gate rows PASS at the QA checkpoint; Rule 22(b) substance read by the Planner: `anvil/src/config.py` de-hardcoded exactly as *What this changes* 1 (env-overridable `ANVIL_ROOT`, `PROJECTS_PARENT` from the checkout's parent, the two `SCAN_TARGETS` paths joined to it); `src/what_works.py` (430 lines: `production_defs`, `verified_inbound`, `survived`, `ROLE_WEIGHTS` labelled the Planner's, `rank`, `precision_recall`, `entry`, the two renderers; the writers forbidden by name in its docstring); `build_scratch_db` factored out of the reprice build; `scripts/what_works.py` with the glossary guard as code; twelve tests and the 33-member fixture; `MUTATION: 5 killed, 0 survived, 0 error` on the scratch clone; anvil's suite `274 passed` (P2's 262 plus twelve); the QA reproduced #100072 exactly — `P@33=0.394 R@33=0.394 P@20=0.450 R@20=0.273` at `a7ba78c` against the doc's four at `cc58328` — with the top-5 shift stated (`slug_from_path` in, `parser.py::parse` out); the guard fired `exit 2` on `GLOSSARY.md` with its refusal line; governance shows only the two new research files; the anvil tree carries exactly the six declared paths, uncommitted, `log -1` still `a1910df`; the receipt's Rule 20 block PASSED; the entries doc lists twenty functions with role, signature, callers, docstring and tests. Merged at the QA pause (`76d550b`). Reading notes (not a stop): the entries render a module-level caller as `bellows.py::bellows.py`, and rows 9 and 10 show a caller count with an empty name list — rendering defects for a follow-up thread, not the definition. Post-close acts: the Planner commits the six anvil paths on anvil `main` and the two governance research files (the #100057 shape); 'applied elsewhere' is the CEO's reading of the deposited entries.
