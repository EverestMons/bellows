continue

Planner Rule 22(b) substance review of Step 2 (schema removal) — PASS. All 8 intelligence tables' CREATE TABLE DDL + 14 indexes removed from contract_tables.py. Drop migration: all 8 appended to _drop_dead_tables() with EXPLICIT FK ordering — carrier_group_members listed before carrier_groups (commented), correct for the carrier_group_members.group_id → carrier_groups(id) FK; DROP TABLE IF EXISTS idempotent. CURRENT_SCHEMA_VERSION bumped 9→10, justified. Notable thoroughness: the DEV also cleaned the SCAC migration in database.py — removed intelligence_notifications/circuit_breaker_events/carrier_location_profiles from the _migrate_table_carrier_code list and removed Steps 6-7 (carrier_group_members + carrier_merge_suggestions SCAC migrations) entirely; these would have errored on existing-DB startup trying to migrate dropped tables (same hazard caught in cluster 6). FROZEN clean (confidence.py/validator.py/validate_batch.py zero diff; action_router untouched this step; no rate/validation table). scope_check clean: database.py + contract_tables.py. 103 targeted tests pass. 27 total dead tables now in the drop migration. Release Step 3 (full-suite QA + closeout). REMINDER for QA: run the full suite with -v/--tb=line (NOT silent -q) to avoid the inactivity-monitor false-kill that hit cluster 6; reconcile against the TRUE baseline 1830 (verified by Planner at Step 1) — expect zero further removals in steps 2-3, so collected should remain 1830; ZERO collection errors; verify FK drop order on a simulated existing DB.
