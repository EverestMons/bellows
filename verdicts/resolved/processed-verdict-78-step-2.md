continue

Planner Rule 22(b) substance review of Step 2 (read side) — PASS. All 4 read sites migrated to first-class columns: triangulation.py _find_elements_for_attestation (lane_id column), triangulate() and get_attestation_requests() (column-first + blob-merge, column-wins via `if k not in data`), and reporting.py _get_fuel_accuracy (expected_fsc column, json.loads dropped). Behavior identity preserved: NULL columns on un-backfilled rows fall back to data_json blob; Tier-3 keys (eia_date, gate-9 results[], fix_url) still sourced from blob. Two judgment calls both sound and verified by QA: dropped the expected_amount fallback in reporting (diag-77 gate-8 inventory confirms only expected_fsc is ever produced — expected_amount was dead code), removed now-unused import json. FROZEN confidence boundary honored; app.py untouched (out of scope by design). scope_check clean: only engines/triangulation.py, web/reporting.py. 326 targeted tests pass; only the 2 known pre-existing failures. Release Step 3 (QA: full suite + 6 behavior checks + closeout to Done).
