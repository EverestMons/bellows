continue

Planner Rule 22(b) override of a benign scope_check FAIL + deep substance review (most entangled cut so far) — PASS. The gate flagged 6 test files out-of-scope (test_pattern_learner/exit_interview/variance_analyzer/diagnosis_tracking/batch_optimization/lifecycle), but the plan body explicitly scoped "test files that assert on these tables/modules" — the paths just weren't inlined (same benign pattern as #81/#85). I verified the three riskiest claims directly against the Step-1 commit (cc30796):
(A) FROZEN clean — validate_batch.py is NOT in the commit (only referenced in the commit message); confidence.py/validator.py untouched. The DEV correctly found a coupling diag 83 MISSED: validate_batch.py:32 module-imports evaluate_suggestion_accuracy from pattern_learner. Rather than touch FROZEN validate_batch, it gutted pattern_learner to an 18-line stub keeping that one function as a no-op (validate_batch's call at :177 is try/except-guarded, tolerates the no-op return). Correct conservative call.
(B) variance_analyzer (validation-adjacent) — committed diff removes ONLY _check_repeat_patterns (the pattern_log SELECT) + its confidence-boost call site; scoring/diagnosis logic untouched. _adjust_confidence_by_history degrades gracefully (get_diagnosis_accuracy_for_type now returns None → "not enough data" path). Exactly the enrichment-only removal required.
(C) pattern_learner stub is clean (no functional suggestion_log refs; 2 docstring mentions only). exit_interview confirmed LIVE (callers in profile_ingestion.py/ingest.py) → pattern_log reads/writes stripped, run_pro_exit_interviews no-op'd (callers handle 0), analytics/helpers retained.
backup.py + integrity.py: pattern_log removed from table lists. Dead Code Sweep recency check clean. Vestigial no-op calls remain in live code (validate_batch guarded call, ingest exit-interview call) — inert, acceptable given FROZEN constraints, cleanable in a later core pass. CARRY-FORWARD FOR STEP 3 QA: reconcile collected count against the TRUE prior (1938 after exec 86, not the dev log's estimated 1935); ~64-67 tests removed across the 6 cut-feature test files; require ZERO collection errors and zero kept-test breakage. Release Step 2 (schema removal: drop suggestion_log + pattern_log, schema v9).
