verdict: stop
Step 2 QA correctly identified a BLOCKER that the #146 fix introduced. CONFIRMED by independent check:
- F3 makes contract_fuel_infer return preview start_price=6.000 (corrected floor) for rule_fallback, but contract_fuel_infer_apply's TOCTOU staleness check (web/contracts.py:5988-5989) still compares body start_price (6.000) against re-detected result.start_price (5.999). abs(6.0-5.999)=0.001000000000000334 > 0.001 in IEEE754 -> false 'stale' rejection. Apply flow broken for fallback at most thresholds (only ~3.0 passes by float luck). DEV test test_apply_fallback_preserves_fields used start_price=3.0 so it missed it.
Bellows Rule 20/22 gate FAILs were correct (QA self-check FAILED on this blocker; verification table carries the non-PASS). The materialization offset, preview override, remat tool, and boundary/far-row alignment are all otherwise correct and verified (full suite 1729 pass / 2 pre-existing / 0 regressions; my independent far-row spot-check to $9.999 = all aligned). Only the TOCTOU interaction is broken.
Halting #146. Follow-up executable will fix the TOCTOU check via mill-based integer comparison (abs(round(a*1000)-round(b*1000)) > 1) plus a threshold=5.999 apply-flow test, then re-QA. #146's good commits remain on main; the follow-up completes the intent.
