verdict: stop
Halting due to Phase 3b/3c slug-collision bug: the bellows-session-wrap-2026-05-01 slug has DB step-state from a prior session's plan (verdict request still in pending/ with mtime 16:46:56). When this new plan was dispatched, Phase 3b's _get_last_completed_step queried by slug, found "step 2 complete" from the prior plan, and resumed at Step 3. Phase 3c's plan-hash drift warning fired correctly (shadow=0b32... current=c64d...) but the resume happened anyway. Step 3 then failed deposit_exists because Step 1's session_end evidence and Step 2's documentation edits were never produced (the prior plan's "Step 2" was a different plan). This is a real slug-collision bug to file as BACKLOG. Re-depositing wrap with a unique slug to bypass DB resume.
