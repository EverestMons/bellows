verdict: stop
Step 1 (QA) died to a HARD 5-hour usage cap (five_hour rate_limit_event, overageStatus=rejected, resetsAt=1784183400 = 2026-07-16 01:30; num_turns=11), files_changed=0, no deposit. The 3 gate failures (deposit_exists, rule_20, rule_22) are all downstream of the empty deposit — a cap death, not a QA result. Did NOT auto-park because the QA step used Bash (pytest) -> has_mutating_tool_use=True -> exec-197 fix conservatively declines to park (residual gap: Bash-using steps never auto-park even at files_changed=0).
Nothing to preserve. The actual fix (exec-201 Step 1, CURRENT_SCHEMA_VERSION 16->17) is committed on main (f856b9c) and INDEPENDENTLY VERIFIED by the Planner: on a v16 copy of the real DB, init_db() now creates contract_documents + the contract_document_id/doc_source_scope columns and stamps version 17 (was False before the bump). Halt; re-dispatch the full-suite QA after the cap resets (~01:30).
