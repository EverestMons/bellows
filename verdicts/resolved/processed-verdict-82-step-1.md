continue

Planner Rule 22(b) substance review of Step 1 (schema + confirm route) — PASS. confirmation_status TEXT DEFAULT 'confirmed' added to all 11 rate-bearing tables (7 extraction_provenance tables + 4 source tables) via idempotent _safe_add_column; rate_confirmed INTEGER DEFAULT 1 added to validation_gate_results; schema version 5→6; DEFAULT backfills all existing rows (confirmed / 1), no manual UPDATE needed. Confirm route added in web/contracts.py: bulk POST /contracts/<id>/confirm-rates (optional category/row_ids, all 6 categories if unspecified) + per-row POST /contracts/<id>/confirm-rate/<category>/<row_id>, both flip unconfirmed→confirmed and leave extraction_provenance untouched (status changes, provenance permanent), guarded with _table_exists, fuel_table FK chain handled, calls _mark_validations_stale() to invalidate cached results. JSON-API routes, no template (plan made it optional; producer deferred so no unconfirmed rows to surface yet). FROZEN clean (confidence.py/element_invoice_evidence/confidence_state untouched; validator.py/validate_batch.py correctly deferred to Step 2). scope_check clean: contract_tables.py, database.py, web/contracts.py. 434 targeted tests pass, only pre-existing failure. Release Step 2 (annotate-only validator use-with-warning + rate_confirmed persist).
