verdict: continue
Rule 22 (b) substance — PASS. Diagnostic answers the question and produces a verified filter-rule table the follow-on executable can mechanize directly.

Gate failure was the worktree_teardown_dirty_tree pre-check (sub-variant A — untracked claim-rename `in-progress-diagnostic-dirty-tree-precheck-false-trip-surface-2026-05-28.md` on local main), NOT a verification failure. All mechanical gates passed (deposit_exists, scope_check, rule_22_verification, file_change_audit, etc.). The pre-check fired as designed; this trip is itself the first confirmed reproduction of session 14 and is consistent with the diagnostic's own predictions.

Recovery applied (R2 sub-variant A): committed two lifecycle artifacts on local main (`verdict-pending-diagnostic-dirty-tree-precheck-false-trip-surface-2026-05-28.md` and `verdicts/pending/verdict-request-dirty-tree-precheck-false-trip-surface-2026-05-28-step-1.md`) at commit 8ac9990, pushed to origin/main. Working tree confirmed clean before this verdict.

Substance verification (Rule 22 (b)) — verified by reading the deposit from the worktree at `/Users/marklehn/Developer/GitHub/bellows/.bellows-worktrees/dirty-tree-precheck-false-trip-surface-2026-05-28/knowledge/research/dirty-tree-precheck-false-trip-surface-2026-05-28.md`:
- Section 1 enumerates 8 lifecycle artifact patterns exhaustively against source code, with file:line citations, lifecycle stage, porcelain status code, and persistence. Caught the deletion side of the claim-rename (`executable-*`/`diagnostic-*` D entries) that the BACKLOG entry did not enumerate — more complete than the BACKLOG framing.
- Section 2 classifies each pattern against cherry-pick conflict semantics (case-A modification conflict and case-B untracked-overwrite conflict) with concrete evidence per row. All 8 lifecycle patterns → safe-to-ignore. `PROJECT_STATUS.md`, tracked source files, `knowledge/research/` tracked files, and submodule pointers correctly remain in must-still-block. The conservative default (unmatched → block) preserves the pre-check's original safety property.
- Section 3 delivers a concrete `_LIFECYCLE_IGNORE_RE` regex and `_is_lifecycle_artifact()` predicate ready for the executable to drop in. Handles renamed-file porcelain (`R  old -> new`) by splitting on the arrow.
- Section 4 dispositions BACKLOG #2 (teardown-stranded-worktree cascade) as subsumed by #1 with a session-13 log timestamp chain showing the cascade was entirely caused by the false-trip. Real-source-conflict cascade case correctly preserved as feature, not bug (worktree preservation for manual recovery).
- Section 5 covers Bellows-self mode (covered by the `verdicts/(pending|resolved)/` patterns), submodule pointer drift (block by default — conservative), copy-back interaction (operates post-cherry-pick, no overlap), concurrent plans (no cross-plan trip — filter is path-pattern based, not slug-based), race window (unchanged), and `git status` failure handling (unchanged, fail-open at bellows.py:895-896).
- Section 6 specifies the test surface for the follow-on executable: 4 new tests (~62 LOC) plus 2-3 existing test fixture updates (~6 LOC), with the critical negative test `test_teardown_blocks_on_non_lifecycle_untracked` proving the filter does NOT mask real conflicts. Total ~88 LOC.

Minor note for the follow-on executable: the diagnostic's filter covers both untracked (`??`) and deletion (`D `, ` D`) porcelain codes per the regex table in Section 3, but the session-13 trips were all on `??` codes only. The executable's test surface must explicitly exercise both code categories — Section 3 specifies this but the executable should not narrow the test surface to the observed codes alone.

Terminal step. Daemon moves the plan to Done/ on consumption.
