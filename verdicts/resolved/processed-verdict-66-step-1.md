verdict: continue
Planner Rule 22 (b) substance check PASS. Single-step read-only diagnostic (id 66); both gates passed, substance-only review. Findings at knowledge/research/validation-result-consumers-diagnostic-2026-06-15.md answer the decision question cleanly: bottom line is CLEANER, not BLOCKED-ON — every consumer that needs per-line detail gets it, either from in-memory GateResult dicts on the first run (email_generator via action_router) or by parsing data_json after persistence (triangulation 3 sites, reporting 1 site, app.py 5 sites; 7 json.loads(data_json) sites across 4 files total). No SQL-level JSON extraction exists, so nothing is blocked on queryability today. Strongest evidence and the one concrete functional loss: dispute_gates.expected_value/actual_value are ALWAYS NULL (schema defines them, INSERT at web/disputes.py:232 never populates them), so the regenerate-email path rebuilds gate rows without structured data and degrades to a generic one-liner while the first-run email is rich. variance_analyzer and reporting-mostly use first-class columns; calibration.py is a dead reference (file does not exist). Read-only, no code touched, confidence machine untouched. Close to Done.
