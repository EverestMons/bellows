verdict: stop
The QA gate passed the FORM (Rule 20 self-check PASSED, evidence present, no hedging) but the receipt HONESTLY REPORTS a substantive FAIL, and the Planner confirms it from the committed raw output (`git show HEAD:knowledge/qa/evidence/bellows-restart-lock-fix-2026-08-15/qa-receipt.md`): the full suite is **1049 passed, 4 failed** (baseline 1040, expected 1053) — a −4 regression. A plan cannot be filed to Done with a red suite, so this is stop + a corrected re-deposit (no-redo grammar), NOT continue-to-done.

Verified facts: the 4 regressions are all pre-existing tests that construct a `MagicMock()` orchestrator and never set `_shutting_down`; Step 1's correct new guard `if self.orchestrator._shutting_down:` at `PlanHandler._handle` reads the auto-truthy MagicMock attribute and returns early, so the tests never reach their assertions. Named: `test_handle_parallel_from_watchdog_adds_pending_not_dispatched`, `test_nonparallel_plan_dispatches_immediately_from_handle`, `test_two_parallel_siblings_collected_as_one_group`, `test_seen_uses_slug_not_path`. The PRODUCTION code is correct (the guard is right and its 13 targeted tests pass); only the mock-based tests need `mock_orch._shutting_down = False`. The QA agent correctly reported and did not repair.

Steps 1-2 code is committed and sound; it stays. A corrective plan (DEV: add `_shutting_down = False` to the 4 mocks → QA: full suite green at 1053/0) will re-deposit under the restart-lock-fix lineage and carry this plan's work to Done.
