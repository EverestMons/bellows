continue

Step 2 (QA) of #100101 verified against the plan, from both steps' transcripts (logs/20260914-194754-step.json, logs/20260914-201047-step.json), bellows main (1e7883c, pushed), the receipt, the dev-log, the run file and both steps' gate rows, read by the Planner.

- The fix: `notify_watcher_down` passes `detail_key=f"{machine}:{status}"`, and `mark_machine_live(machine)` clears only the keys whose detail names that machine (`notifier.py`, +11/−4). Two machines stale at once now page once each, and one machine's return no longer re-arms the other's page: thread 331's defect. test_8's one assertion (`"stale"` → `"air:stale"`) is the plan's declared edit; the file's other tests are unedited (numstat 120/1).
- STEP 1: red `6 failed, 14 passed`; the file `20 passed`; the full suite `2369 passed, 2 skipped`, the prediction exact; the mutation run redirected into its deposit, `MUTATION: 3 killed, 0 survived, 0 error`; the dev-log's four headings, P2's cell verbatim through `:3523–3524`; two DEV commits, `ae0df7f` (three files) and `54ca523` (the run file and the dev-log); `$T` removed; the tree clean.
- STEP 2: Item 1 redirected into its deposit, the file's summary `2369 passed, 2 skipped`; Item 2's six PASSED lines and the old-`notifier.py` statement; Item 3 stated; Item 4's receipt carries the numstat over the five files, a three-row Verification table with one-token cells, and the Rule 20 block's stdout; Item 5 one command — the suite-file gates, the step-2 pre-check (`PRECHECK: 0 failure(s)`), `git add` and `git commit` — `1e7883c`. Every gate PASS at both steps, `scope_step` among them.

Recorded, none of it changing what landed:
1. STEP 1 Item 4 was not run as one command. The first chained run piped the suite through `tail -2`, so its one failure (below) gated nothing, and the pre-check ran after it; the suite was then re-run alone (`| tail -3`, 2369 passed), and the commit's own command chained the pre-check with the stage flag, `git add` and `git commit`. The last commit ran as one command, its suite also piped through `tail -3`. Thread 333's class: its binding holds the pre-check link, not the suite link.
2. `tests/test_notifier_server.py::test_server_respond` failed once in STEP 1's first chained suite run and once in STEP 2's first Item 1 run (`1 failed, 2368 passed`), passing alone and on every re-run; it failed the same way in #100100's DEV run tonight, a plan that does not touch `notifier.py` — a flaky test, not this change. STEP 2's evidence file holds the re-run; the receipt reports the clean run and does not mention the first.
3. STEP 2 Item 4: the block's first run printed `FAILED — SELF-CHECK FAILED` on the word `skipped` in the receipt's Item 1 row, which had pasted pytest's own summary where the plan asked for the skips named as `two skips`. The QA rewrote the row in the plan's form and re-ran the block (`PASSED`), and the receipt carries the second run's stdout. The plan asked for a FAILED stdout to go into the receipt and for the step to stop; the receipt carries neither, and the transcript holds both runs.
4. The plan's post-condition says the receipt "closes with the block's own PASSED line"; the block prints `Evidence folder:` and `Files verified:` after it — the plan's wording (thread 341's EXECUTION seat showed it false), not the step's.

Continue: the plan closes. The daemon restart the plan owes (`notifier.py` is daemon-loaded) is the CEO's.
