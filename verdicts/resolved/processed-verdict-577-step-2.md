stop

STOP — and the AGENT WAS RIGHT TO HALT; this is a defect in MY plan, not agent error. The five gate failures (deposit_uncommitted x2, deposit_exists, rule_20_self_check, rule_22_verification) are all downstream of an honest halt: the agent produced no deposits because it stopped, exactly as MUST-PRESERVE line 52 instructs — 'If any of the three shipping mutants does NOT kill on your run, STOP and report. Do not adjust the mutant, the selector, or the tests to obtain a kill.' Its Event-65 narration states the case plainly: drop-bytecode-isolation SURVIVED, expected KILLED. It refused to retune the instrument until it agreed, which is precisely the exec-574 discipline the plan asked for. Commend, do not penalize. THE REAL DEFECT: the drop-bytecode-isolation mutant is NONDETERMINISTIC ACROSS ENVIRONMENTS. Measured by the Planner: 5/5 KILLED on the live checkout; the agent's worktree run returned SURVIVED on identical code. Mechanism, from tools/mutation_check.py:48-49 — the runner builds its child env from a COPY of os.environ, so the OUTER (unmutated) runner exports PYTHONDONTWRITEBYTECODE=1 and the INNER mutated runner does env.pop() to remove it; whether the resulting stale .pyc is then reused depends on same-second mtime granularity and on sys.pycache_prefix's absolute-path mirror, which DIFFERS between the live checkout and a worktree. So the mutant reproduces a defect whose symptom is itself timing-dependent — the same nondeterminism that let exec-574's dev log honestly record '8 passed' against a tree I measured as 1 failed. A mutation runner whose mutants are nondeterministic is worse than one with fewer mutants: it manufactures false alarms and trains readers to ignore red. MY ERROR, precisely: when designing this manifest I explicitly flagged 'M3 might be flaky, let me MEASURE rather than reason' — then measured it only on one machine, in one directory, and treated KILLED as settled. A single-environment measurement cannot establish determinism; that needed a worktree run, which the dispatch itself provided and I did not anticipate. FOR THE CORRECTED RE-DEPOSIT under this stable slug, two options and I recommend the second: (a) drop drop-bytecode-isolation from the manifest and record that the 574 defect is not mutation-testable because its symptom is timing-dependent — cheap, honest, loses coverage of a real defect; (b) REMOVE THE NONDETERMINISM AT SOURCE by having the runner bump the mutated file's mtime explicitly (os.utime with an advanced timestamp) after writing each mutant. That guarantees bytecode invalidation regardless of env vars, pycache_prefix, platform or mtime granularity, makes the env var true defence-in-depth rather than the sole mechanism, and makes this mutant deterministic everywhere. It is a small behavior change to the runner and needs its own tests. The three-mutant manifest, the scoring-arm comment, and the DEV commit af552d3 all stand; only the mutant's reliability is at issue.
