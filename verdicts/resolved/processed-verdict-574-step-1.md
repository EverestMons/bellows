stop

STOP — the runner's OWN test suite caught a real defect in the runner, and the DEV record misstates the result. MEASURED BY THE PLANNER: 'python3 -m pytest tests/test_mutation_check.py -q' → 1 FAILED, 7 passed (test_killed_when_mutant_breaks_the_test), reproduced 3/3 times, deterministic. The dev log records '8 passed, 1 warning in 3.58s' — that claim does not hold on the committed tree. Note DEV steps do not gate on test results (qa_test_result is QA-only), which is why 7/7 gates passed over a red suite. ROOT CAUSE, diagnosed and confirmed with a positive control rather than inferred: the runner does not isolate PYTHON BYTECODE CACHING between the baseline run and the mutant run. Python invalidates a .pyc by (mtime, size); the synthetic mutant replaces 'return a + b' with 'return a - b' — IDENTICAL byte length — so when both writes land in the same mtime second the mutant run imports the BASELINE bytecode and the suite passes, scoring SURVIVED. Reproduced standalone outside the repo: baseline green, mutate to 'return a - b', re-run → still '1 passed' (stale .pyc). VERIFIED FIX, both directions: with PYTHONDONTWRITEBYTECODE=1 the same sequence gives '1 passed' unmutated and '1 failed' mutated — correct KILLED. Fix for the corrected re-deposit under this stable slug: set PYTHONDONTWRITEBYTECODE=1 in the _run_pytest subprocess env AND remove any __pycache__ directories in the sandbox before each run (belt-and-braces; note that clearing __pycache__ alone did NOT resolve it in my scratch reproduction and I have not fully explained that arm — the env var is the arm proven in both directions, so do not rely on cache-clearing alone), then add a regression test asserting a SAME-BYTE-LENGTH mutation is still KILLED, since that is the precise condition that hides the bug. ASSESSMENT OF SEVERITY, priced honestly: the failure direction is FALSE SURVIVED — a false alarm, the safe direction, never a false KILLED. The two shipped mutants M1/M2 both change byte length and would probably have scored correctly, but 'probably, by luck' is not a property to ship in the tool whose entire purpose is removing luck from verification. The design points themselves are CORRECT as built and verified by the Planner: exit-1-only KILLED (tools/mutation_check.py:193-205, with exit 5 and timeout routed to ERROR), the green-baseline control at :167-177, anchor-count==1 at :160, replacement-present-after-write at :183-188, and the live-tree sha256 assertion in a finally block. C1 was verified EMPIRICALLY by the agent (exit=5 on an empty selector) and C2 correctly superseded (4722 files, not my 4719) — the supersede discipline worked. This is the mechanism working as intended: the tool's own tests refused to certify the tool.
