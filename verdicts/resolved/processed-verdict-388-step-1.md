verdict: continue
Rule 22(b) PASS on Planner review of the DEV diff. Confirmed at HEAD: fetch_xml.fetch_single(conn, invoice_id) reuses _fetch_one_xml + _update_marker, not_configured guard on empty FETCH_URL. Route POST /invoices/<invoice_id>/fetch-xml (app.py:1945) — 404 on missing invoice; fetch_single -> if fetched, enrich_invoice_xml + commit; flashes BOTH stages (w1-1: enriched -> success with charge count; parse_failed/no_file -> WARNING "fetched but could NOT be enriched"); clear per-status messages (not_xml -> "No XML available for this invoice (its type may not have XML)", auth_fail, not_found, error); redirects to /invoices/<id>. Button in invoice_detail.html is a CSRF-protected form POST. Batch run_pending / 385 guard / spawn all UNTOUCHED (not_xml skip still present). Proceeding to Step 2 (QA). NOTE (T-3): real proof is pressing Fetch XML on a known-good invoice on the work machine.
