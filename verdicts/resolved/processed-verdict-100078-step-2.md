continue

continue — all gate rows PASS at the QA checkpoint; Rule 22(b) substance read by the Planner: `scripts/plan_lint.py` gains `_is_executable(plan_path, header)` (the basename after a lifecycle prefix, then the legacy `Type:` field; the `Execution:` field deliberately not read) and the `(e)` arm that FAILs an executable parsing zero uppercase `## STEP N` headings regardless of `qa_steps`, with the message the plan named; the `qa_steps` arm, the lowercase WARN and the diagnostic tolerance unchanged; four tests beside the existing (e) tests; `MUTATION: 3 killed, 0 survived, 0 error`; the full suite `2266 passed, 1 skipped`; the receipt's Rule 20 block PASSED; QA Item 2 ran the worktree's lint on the plan's own text (clean) and on a headless copy (the new FAIL, exit 1). Merged at the QA pause (`bf3b004`). Thread 13's four other items stay measured in the plan's P5 for the CEO's disposition.
