# Override — plan 100057 STEP 1 — dev_log_declared_text (one cell, truncated at its second clause)

**Date:** 2026-09-09 | **Ruled by:** CEO ("Override", session d04ebd33) | **Gate:** dev_log_declared_text | **Failure:** `cell absent from section '## Pins re-derived (P1, P3)'` — the P3 value cell (470 characters normalized) is present to character 259 and the remainder is absent: `; later-touched hits: bellows.py::run_plan ×4 (a hub every plan touches), tools/battery_census.py::get_coverage by 53e7146 (planned by 100056), tests/test_notifier_coverage.py::* by f5c82a4 (new tests, not fixes)`.

**What happened.** The DEV pasted the ten-row table half of the pin cell under the declared heading, labelled it "P3 verbatim", and left out the cell's second half. Measured by the Planner through the gate's own normalization (backticks and `*` stripped, whitespace collapsed): 259 of 470 characters present, the missing tail as quoted above. Every other gate passed; the four declared headings are present as full lines.

**Why the override is granted.** The diagnostic's deliverable is complete and unaffected: `governance/knowledge/research/anvil-reprice-2026-09-09.md` answers Q1–Q7 with denominators and sample rules and ends with a seven-point "What this does not establish"; the TSV carries 440 rows; the P1 counts and the P3 table were re-derived and matched the pins exactly; the two anvil scripts were extended with no import of either forbidden writer. The same ruling was given on 100056 for the same class on 2026-09-09 (`override-100056-step-1-dev_log_declared_text.md`).

**What is recorded against it, not excused.** The gate's THIRD live catch in one day (100054 halted on six failures; 100056 overridden on one truncated cell; this one, a second truncated cell). The class — a verbatim contract met by half — is refused at the desk by the parked draft `executable-bellows-dev-side-precheck` (thread 253): the DEV's commit chains `tools/check_deposit.py` before `git add`, running this gate's own function over the deposit. This catch is the evidence that draft was parked to wait for; the ruling on its deposit follows the reading of #100057's doc.

**Files changed in the step:** `knowledge/development/dev-log-anvil-reprice-2026-09-09.md` — one, declared; the four other deposits (the governance doc and TSV, the two anvil scripts) are the Planner's to commit at the wrap.
