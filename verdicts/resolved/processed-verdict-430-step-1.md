verdict: continue
gate_failure = scope_check, adjudicated a BENIGN false positive. The only flagged file is `tests/test_instance_lock.py`, which is precisely the new test file Step 1's `## Scope` declared (`bellows/tests/`) and which fold 13 mandated (the extracted `acquire_instance_lock` test). The scope_check did not match the `bellows/tests/` directory-prefix form against the project-relative path; `bellows.py` and the dev-log matched. No genuinely out-of-scope file was touched (files_changed = bellows.py, tests/test_instance_lock.py, knowledge/dev-logs/restart-lock-fix-step1-2026-08-15.md — all three intended).

Rule 22(b): Planner verified the deliverable from the COMMITTED source (`git show 8066311:bellows.py`, receipt `git show e94d975:...`), not the agent's summary. Confirmed against the post-cold-panel corrected design: (1) the SIGTERM handler is registered INSIDE `Bellows.start()` (signal.signal at bellows.py:2465, after `def start` 2431 and after the `observer` local is created) — the capstone HIGH fix; it stops `observer` and `self.response_server` on both the clean-drain and timeout paths. (2) `acquire_instance_lock(lock_path)` is extracted (bellows.py:55) and opens `os.O_RDWR|os.O_CREAT` (61) — non-truncating, so a failing second starter cannot erase the holder PID (fold 3). (3) `_shutting_down` is instance-bound (2441) with the two access paths named by fold 20: `self.orchestrator._shutting_down` at 1894, `self._shutting_down` at 2041/2052. (4) daemon=True confirmed at 2046/2057; 30s drain, timeout abandons with a WARN (accepted last-resort per fold 10); second signal → sys.exit(1) immediate (fold 4). (5) SIGTERM handled alone; the existing `except KeyboardInterrupt` at 2531 stays intact and the drain loop lets KeyboardInterrupt propagate — exactly one shutdown path (fold 12). The receipt reports 6/6 targeted tests passing incl. `test_second_open_preserves_holder_pid` (observes the open-mode fix); the full-suite raw-output verification is Step 3's QA duty.

Continue to Step 2 (the standalone guarded stop path + dashboard wiring).
