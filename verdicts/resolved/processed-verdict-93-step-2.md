continue

Planner Rule 22(b) substance review of Step 2 (schema removal) — PASS. CREATE DDL removed for all 4 cut tables: worklist_snapshots + employee_assignments (database.py), teams + team_memberships (contract_tables.py). SCAC migration cleaned (employee_assignments removed from _migrate_table_carrier_code). team_members DDL explicitly KEPT intact (the critical kept-table distinction held). Drop migration: 4 tables appended to _drop_dead_tables() FK-ordered — team_memberships before teams, then employee_assignments (child of kept team_members, safe), then worklist_snapshots. CURRENT_SCHEMA_VERSION bumped 11→12, justified. FROZEN clean (confidence/validator/validate_batch/ingestion untouched; team_members + invoices.assigned_user untouched). scope_check clean: database.py + contract_tables.py. 97 targeted tests pass. Release Step 3 (full-suite QA + closeout). REMINDERS for QA: run full suite with -v/--tb=line (avoid inactivity false-kill); reconcile against TRUE prior 1733 (post Step 1); confirm no team-CRUD-route test now fails (404) — only 1 test was removed in Step 1, so verify the full suite has no new failures beyond the 2 known pre-existing; verify the kept /team, /workload/list, /team/<employee_id> routes still render; verify FK drop order on a simulated existing DB.
