verdict: stop

Step 1 gates all PASS (Gate Result failures=[], scope_check/rule_22 clean), but substance check (b) fails: the work list handed to Step 2 is undercounted. The dev-log derives "classify 7 (IDs 117-123)" from `NOT EXISTS (any proposal)`. That query is wrong — the ingestion update path marks an edited entry's prior proposal `stale` (the row persists) rather than deleting it, so any stale-only entry is invisible to it. Two entries are stale-only and owe reclassification: 116 (the 05-29 straggler, re-staled by THIS cycle's update_count=1) and 93 (a 2026-05-27 high-confidence governance_rule lesson on schema-migration discipline, staled by a prior content edit and silently dropped every cycle since). Correct work list is 9: [93, 116, 117, 118, 119, 120, 121, 122, 123].

Continuing is not viable — Step 2's embedded query is the same flawed `NOT EXISTS (any proposal)` form and would reproduce the drop. Root cause: the buggy query is prescribed verbatim by entry 117's codified discipline rule (the over-reporting fix shipped this undercount), copied into every cycle plan, and is in this batch awaiting codification into PLANNER_TEMPLATE. Halting to re-author with a stale-aware helper `get_unclassified_entries(conn)` (`... AND status != 'stale'`) added to lessons_forge.py, so the fix lands at the source before 117 enshrines the wrong query. Step 1 ingest already landed and is idempotent; the re-dispatch re-runs it as a no-op.
