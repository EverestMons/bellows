stop

STOP. Gates 11/11 clean and the three constraints this cycle fought for all held — but the shipped change LOSES validation coverage on 15 registers that were passing, and the two mutants that would have caught it never ran. Both root causes are defects in MY plan text, not in the agent's work.

WHAT HELD, and it is most of it. The collector kwarg landed exactly as specified — `run_check(plan_path, warnings=None)`, all three production callers byte-unchanged, and the arity CONTROL mutant (M5) KILLED, which proves the 40-test guard is real rather than assumed. The status names are `LEGACY_SCHEMA` and `FUTURE_SCHEMA`: neither begins with `CONFORMANT` or `NO_TABLE`, so `judge_register`'s substring classification is safe, and the agent implemented both the below-validator and above-validator arms. M3 (assign FAIL not warn) and M4 (warn printed after verdict) both KILLED. `test_walk_register_lint.py` 31 passed.

⚠️ THE SUITE'S ONE FAILURE IS NOT A REGRESSION — and I nearly misread it. `test_gates_cross_machine_paths.py::test_relative_path_unchanged` fails in the canonical checkout and PASSES from a worktree at the SAME commit (6 passed vs 1 failed, HEAD both times). It is the known CWD-`config.json` trap: the resolver finds the canonical checkout's real `config.json`. My first diagnosis compared the parent commit in a worktree against HEAD in the canonical checkout — two variables changed, and I attributed to the wrong one, while diagnosing a location-dependent failure. Step 1 introduced no regression. Step 2's Item 1 mandates the worktree run, so QA would have measured it correctly.

⛔ STOP REASON 1 — THE VERSION BRANCH EXEMPTS BEFORE IT VALIDATES. Corpus after the change: 91 CONFORMANT / 28 LEGACY_SCHEMA / 25 PRE-SCHEMA / 13 UNCONFORMANT / 1 NO_TABLE, against 106 / — / 25 / 23 / 3 before. The plan predicted TWO files moving out of `NO_TABLE`. **Fifteen previously-CONFORMANT registers are now exempted from validation.** Sampled three of the 28: two carry the EXACT v0.3 column shape (`| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |`) while declaring 0.1 or 0.2 in their header. They would PASS if validated, and are now skipped instead. That is a loosening of the very coverage this plan exists to add. **The correct behaviour: attempt validation regardless of declared version, and report the legacy status only when validation FAILS and the declaration is older — so an old-declaring but conformant register stays CONFORMANT.** ⚠️ **This is my under-specification.** Item 3 said "branch on the declared VALUE, not its presence" and never said validation must still be attempted. The agent implemented the letter of it.

⛔ STOP REASON 2 — DEFECT A'S DISCRIMINATORS ARE UNVERIFIED. `mutation_check`: **3 killed, 0 survived, 2 ERROR**. M1-drop-version-branch and M2-pre-schema-counted-bad both report `anchor matched 0 times (expected 1)` — they never ran. Cause: the manifest declares a single `target: scripts/cycle_check.py`, but M1's anchor lives in `walk_register_lint.py` and M2's in `run_check.py`. `mutation_check` takes ONE target per manifest — **which is tuyere thread 97, open since 2026-09-02, and which I read this morning and did not connect.** So the two mutants covering the plan's PRIMARY defect are unproven, and M1 is precisely the one that would have exposed reason 1. An ERROR is not a survivor, but it is not a kill either: the tool's own docstring says scoring a non-kill as KILLED "would manufacture the false confidence this tool exists to destroy."

⛔ WHY THIS CANNOT BE FIXED IN STEP 2. Step 2's declared Scope is the three QA evidence files. `knowledge/mutants/` and `scripts/` are step-1 scope, so a step-2 agent correcting either would fail `scope_check`. There is no in-plan path to a clean close, which is what makes this a stop rather than a continue-with-conditions.

THE FOLLOW-UP, precisely. A new plan, cloning this one: (1) split the manifest into THREE, one per target file — `mutation_check` supports one target each, so thread 97 is not blocking, only unhelpful; (2) restate Item 3 so validation is ATTEMPTED before any version exemption, with the legacy status reserved for registers that both fail and declare an older schema; (3) add a test pinning the case this missed — a register declaring 0.1 whose table IS v0.3-conformant must remain CONFORMANT; (4) re-measure the corpus and require the CONFORMANT count to hold at 106 or rise, never fall. Everything else in this plan stands and should be carried forward: the collector kwarg, both status names, the judge ruling, and M3/M4/M5.

Step 1's work is sound and stays on main; the halt is about coverage and proof, not about the code being wrong to write.
