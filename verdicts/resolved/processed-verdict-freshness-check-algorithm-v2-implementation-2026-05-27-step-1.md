verdict: stop
Rule 22 (b) substance check FAILED. V2 implementation is faithful to blueprint Section 5 (DEV did its job), but actual output is 37 candidates / 6 entries flagged vs. blueprint's predicted 15 / 4. Blueprint's Section 3 FP-validation only re-traced v1-flagged candidates through v2 rules; did not enumerate NEW candidates v2's lowered PS threshold would surface. Live run shows v2 traded one FP source (title-word noise in BACKLOG Closed) for another (slug-token noise in PROJECT_STATUS). Net signal value roughly equivalent to v1. Second failed shipment of same script in one session — continuing to iterate on the same term-matching approach is the sand-pile dynamic. Halting. V2 script left on disk as halted experiment. Ground-truth catches still pass for all 4 cases. BACKLOG entry to capture session lessons and document the term-matching approach's inherent limit.
