# Override — plan 100083 STEP 1 — scope_check

**Date:** 2026-09-11 | **Ruled by:** CEO (verdict question answered in session d04ebd33, "Override") | **Gate:** scope_check | **Failure:** `out-of-scope files: tests/test_dashboard.py`

**What happened.** The DEV routed the dashboard's DB reads through the new `lifecycle.connect_readonly` as the plan asked, and while there changed the dashboard's empty-DB message from `(no database)` to `(no lifecycle.db)` and added a `db_unreadable` state (rendered `(lifecycle.db unreadable)`) — neither in *What this changes*. `tests/test_dashboard.py::TestRenderDbAbsent` asserted the old string and `_make_state` lacked the new key; the full suite — which both DEV commits gate on — went red, and the DEV edited the test (+2/−2, the string and the key). The file is not in the STEP 1 Scope. The gate did what it exists to do: it named a file the plan did not declare, and through it a product change the plan did not ask for.

**Why the override is granted.** The class is 100045's — a message's test consumer edited to follow a message THIS plan changed — and 100052's disposition (CEO override with a ref) is the recorded precedent. The undeclared change is small, user-visible only in the stopped-daemon rendering, and in the plan's spirit (the readers that report a stopped daemon); the helper, the four call sites, the skill-file corrections, the six tests and the mutation run (`MUTATION: 3 killed, 0 survived, 0 error`) match the plan; the commits are already on main (`03e6dd7`, `c0a9d77` — the daemon merges at the pause). A stop would halt a plan whose code has landed to re-run a DEV for one sentence and one Scope line.

**What is recorded against it, not excused.** (1) The plan's own miss: the author enumerated the dashboard's consumers by its `connect` calls (P1 said "mode=ro in module docstring only") and never listed the dashboard's TEST as a consumer of the messages the fallback would touch — the class of 100045 again: a change to a rendered string has its test as a consumer. (2) The DEV changed a message the plan did not name — scope creep the gate caught through the test; the plan should have said "no rendered string changes" or named the new state. (3) A record defect found at the (b) read and corrected by the Planner BEFORE this override (commit on main, the line marked `[CORRECTED …]`): the dev-log stated "Air's system python3 reports SQLite 3.43.2" — the DEV ran no ssh and measured nothing on the Air; the Air's `/usr/bin/python3` reports 3.51.0 and reads (Planner measurement at drafting and at this read). The class: a re-derivation that names a machine the step never reached is an invention, however plausible; the dev-log is a record and takes only what was measured. LESSONS candidate at the wrap.

**Files changed in the step:** `dashboard.py`, `depositor.py`, `hooks/commands/eluvian.md`, `hooks/commands/wrap.md`, `lifecycle.py`, `status.py`, `tools/gate_watcher.py`, `tests/test_lifecycle_readonly.py`, `tests/test_dashboard.py` (undeclared), `knowledge/mutants/readonly-wal-open.json`, `knowledge/mutants/readonly-wal-open.run.txt`, `knowledge/development/dev-log-readonly-wal-open-2026-09-11.md` — twelve, eleven declared.
