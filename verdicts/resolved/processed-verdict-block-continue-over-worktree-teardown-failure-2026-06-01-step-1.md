verdict: continue

Step 1 (DEV) verified correct on disk; advancing to Step 2 (QA). Pause reason gate_failure with a single FAIL: scope_check flagged tests/test_consume_verdicts.py as out-of-scope. This is a plan-authoring artifact, not a substance problem — the STEP 1 body did not literally name the test file as a deposit target, so scope_check could not authorize the modification. Planner manually performed the scope verification the gate could not: the file contains only the three intended regression tests (test_continue_blocked_on_worktree_teardown_failure_interstep, test_continue_to_done_blocked_on_worktree_teardown_failure_final_step, test_continue_advances_normally_without_teardown_failure at lines 813/887/955; +213 lines) and nothing out of scope.

No worktree_teardown failure is present in the gate result — the trap this plan targets is clear. The fix is confined: bellows.py +18 lines at the top of the continue branch in _consume_verdicts (guard predicate at 1345, ledger action continue-blocked-worktree-teardown at 1348, routes to halted- via notify_plan_halted + break, mirroring the existing halt exit); _teardown_worktree and _create_worktree byte-unchanged. Full suite 437 passed / 5 pre-existing carry-over failures (4x test_decisions + test_run_step_timeout), zero new. Scope_check override authorized. Proceed to Step 2 (QA).
