verdict: continue
Planner Rule 22 (b) substance check PASS. Single-step read-only diagnostic (id 73); both gates passed, substance-only review. The 2 intermediate-decision flags are benign (Event 53 "let me also read the test file" = in-scope due diligence; Event 70 "Complete"). Findings at anvil/knowledge/research/backup-bug-diagnostic-2026-06-16.md answer all 6 questions with file:function:line citations. Root cause: single backup site at src/scanner.py:prune_deleted_file_orphans:126-132 (shutil.copy2 of the full ~1.2GB anvil.db) fires unconditionally whenever a scan finds >=1 orphan file (deleted-from-disk file still in code_chunks) — effectively once per project scan, with no gate/flag/cooldown/dedup; the pairs-seconds-apart come from two projects (invoice-pulse + bellows) sharing one anvil.db, each scan copying it independently (~2.4GB per pipeline run); zero retention/pruning anywhere; DB itself likely bloated (full file content in code_chunks, unbounded chunk_fingerprints/chunk_similarities across cycles, no VACUUM). Recommended fix: keep-last-N retention (~5 lines, highest-impact/lowest-risk) + move the backup to once-per-run instead of per-project; DB-hygiene/VACUUM as a separate lower-priority track. Read-only, no code touched, pipeline not run. Close to Done.
