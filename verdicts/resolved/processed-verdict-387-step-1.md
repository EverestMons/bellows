verdict: continue
Rule 22(b) PASS on Planner review of the DEV diff (fetch_xml.py run_pending). Confirmed at HEAD: the not_xml branch (:320-322) is now a print only ("NO XML for {} -- skipping (xml not available)") with NO pool.shutdown/break -> skip+continue; auth_fail STOP (:315-319) unchanged; the Stop-sentinel check (:324-328) unchanged; last_error is set only for auth_fail/error (:309), NOT not_xml -> a skip-only run leaves no red banner. The failure log still appends not_xml (status != fetched -> failures). URL/invoice_id unchanged. Proceeding to Step 2 (QA). NOTE (T-3): the real proof is a live work-machine batch that runs THROUGH the no-XML invoices (fetched>0, not_xml=N skipped, no done=1 halt).
