verdict: continue
Rule 22(b) PASS on Planner review of the DEV diff (fetch_xml.py). Confirmed at HEAD: added `from ingestion.xml_parser import InvoiceXMLParser` (:26); the naive startswith/<html> guard is REPLACED by `InvoiceXMLParser(xml_string=body).parse()` (try/except-wrapped) gated on an invoice Header (parser.root.tag=="Header" or .find("Header")) — accepts the browser-wrapped HTML-XML billing page, rejects a well-formed non-invoice page (C2/w1-1 preserved). Old <html>/<!doctype reject removed. Raw body_bytes atomic write + status taxonomy unchanged. Tests: test_accepts_browser_preamble_html_xml_page -> fetched; test_rejects_well_formed_xhtml_without_header -> not_xml; test_accepts_pure_xml_with_header -> fetched (regression); 45 passed / 0 failed. Proceeding to Step 2 (QA). NOTE (T-3): the load-bearing proof is the live work-machine fetch (fetched>0).
