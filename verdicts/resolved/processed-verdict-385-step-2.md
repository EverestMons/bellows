verdict: continue
Terminal QA — Rule 22(b) PASS on RAW evidence (full-suite.txt): 2 failed, 2563 passed, 1 warning. The 2 failures are the two CLAUDE.md-known pre-existing failures; 0 regressions; 2563 vs 385-baseline 2560 = +3 (the new guard tests: accept-wrapped / reject-no-header / pure-xml-regression). Gate clean, rule_20_self_check PASS (banner byte-exact). Final step — closing plan 385 to Done. NOTE (T-3): proves the guard accepts/rejects representative bodies; the real proof is the live work-machine fetch (fetched>0 + {id}.xml written, then Enrich populates gates 6-9).
