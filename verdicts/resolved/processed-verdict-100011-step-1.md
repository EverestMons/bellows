stop

STOP at Step 1 (gates 11/11 PASS; DEV commit 6b892a3 on main is byte-identical to the committed builder's outputs and stays). The plan's P6 is FALSE in the canonical checkout and in the daemon's real worktree shape (.bellows-worktrees/<wt> resolves the canonical root by its config.json sentinel, so resolve_projects_parent() = /Users/marklehn/Developer, which HOLDS tuyere/.venv/bin/python): four tests in tests/test_plan_claim.py (TestOffModeNoOp::test_release_off_mode_checkout_unresolvable, TestDecisionTable::test_advisory_checkout_none, TestResolverTwin::test_both_none, TestResolverTwin::test_shim_reads_env_at_call_time) hide the env override and Path.home but not the third candidate's root, so they encoded the pre-fix literal's deadness on this machine and now fail — measured on main at 6b892a3: 6 failed / 1658 passed canonical (the four + the CWD survivor + test_gate_watcher::TestPauseStateSpace, the last a 100009 leftover that fires only while a plan is awaiting_verdict). Every scratch proof (walk 0, three seats, the Planner's worktree replicas) sat where the projects parent held no tuyere, so location changed the resolver's answer unseen. QA's Item 3 would only re-measure this Critical; the fix is a test-scope change outside this plan's declared writes. Follow-up plan (tests neutralize the projects parent; gate_watcher STATE gains awaiting_verdict; 100011's QA items re-run on main with the named set measured in the real shape) authored next by the Planner.
