continue

Planner Rule 22(b) substance review of Step 2 (schema removal) — PASS. CREATE TABLE + index DDL removed for all 4 cut tables: contract_exceptions, activity_note_classifications, forge_export_log (contract_tables.py, + header docstring updated) and calibration_snapshots (database.py). All 4 appended to the _drop_dead_tables() tuple (established exec-72/84/85 pattern; now 17 dead tables total), DROP TABLE IF EXISTS idempotent. CURRENT_SCHEMA_VERSION bumped 7→8, justified. No tables deferred (Step 1 confirmed all fully de-referenced, incl. activity_note_classifications via note_classifier module removal). FROZEN clean (confidence.py/validator.py/validate_batch.py untouched; no rate/validation table). scope_check clean: database.py, contract_tables.py. 103 targeted tests pass. (Minor hygiene note: conftest.py:84 has a comment-only mention of note_classifier — no code impact; QA may clean.) Release Step 3 (QA). REMINDER for QA review: full-suite collected count should drop by ~39 from the prior baseline (35 note_classifier module tests + 4 forge_export_log tests removed in Step 1) — QA must reconcile the drop against those and confirm zero kept tests broke.
