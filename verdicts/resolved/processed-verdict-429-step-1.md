verdict: continue
Clean gate — header_pause (pause_for_verdict: always), 0 gate failures, 1 file changed. Diagnostic, single step; continue closes it to Done. Rule 22(b): the Planner re-verified the deposit from the committed file (`git show HEAD:knowledge/research/bellows-restart-lock-findings-2026-08-15.md`, commit `2b5ab1e`), not the agent's summary.

Measured against source: the 341-line findings doc answers Q1–Q6, each with `file:line` citations and quoted code, plus a Recommended minimal fix. Key claims spot-verified against the code: (1) NO SIGTERM handler exists anywhere in `bellows.py` — the daemon main loop catches only `KeyboardInterrupt` (bellows.py:2442), so SIGTERM propagates as `SystemExit` uncaught; this is the load-bearing finding for the fix. (2) `status.py` already carries `get_daemon_pid` (lsof-based identity) and `probe_daemon` (flock liveness), but both are display-only — wired to no stop/restart path. (3) `dashboard.py` `_do_restart`/`_terminate_child` act only on `self.child`, so an out-of-band incumbent (the PID-3969 repro) is unreachable — matches the Planner's own pre-diagnostic code inspection. The recommendation preserves the hardened guard sequence intact: atomic flock acquire first (success ⇒ stale/free, no kill), then identity guard (lsof + `ps` cmdline), then idle guard (query_in_flight + in-progress scan), refuse as default, re-acquire as arbiter.

Recommended minimal fix scoped at ~55 LOC across `bellows.py` (PID+timestamp in lock + informative error; new SIGTERM drain handler) and `dashboard.py` (`_do_restart` incumbent discovery + guarded kill); `status.py` needs no change. The executable fix will be authored from these findings next.
