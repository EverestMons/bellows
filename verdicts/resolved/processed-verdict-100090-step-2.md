continue

Both steps verified at the (b) read. DEV: _replace_closing_line refuses a closing file whose text does not begin with **Closing:** (the error names its first 40 characters) before any search of the draft; step 4b primes when the draft held a placeholder line at the splice — it re-emits on the spliced draft, re-splices, prints CLOSE: prime OK and hands its output to the commit subject and the register seal. Two deviations from the plan's letter, both accepted: (1) the prime saves a fold baseline before its emit, where the plan said it saves none — the dev-log's measured reason is that without it the prime's emit reads the splice as a fold drift which step 6 then contradicts; it does not call after_step("baseline"), so c4's tamper at the second named baseline still reaches STORED == LIVE, and c4 is green; (2) the placeholder test matches a full `key: <placeholder>` line anywhere in the draft instead of naming "the lines _splice_keys replaces" — _splice_keys replaces every line that starts with `key: ` anywhere, so the lines the test reads are a subset of the lines the splice replaces and the scopes agree. Tests: c12, c13, c13b as drafted, c10/c10b/c11 re-counted, the step-order assertions gained prime; red 5 failed with c13b green before the edit, exactly as predicted; green 15 passed; full suite 2305 passed, 2 skipped; MUTATION: 4 killed, 0 survived, 0 error. QA: the real read-only-probe first close replayed on a clone at 401715f7^ — the pre-merge tool CLOSE: stored==live FAIL, this worktree's tool CLOSE: prime OK, stored==live OK, battery OK, both with --dry-run; Rule 20 PASSED; every gate PASS on both steps. The agent read runs = 11, pid 61599 before the plan and after both suite runs; the canonical evidence tree empty throughout. Continue — close. Post-close: no restart (the tool is Planner-side); the CEO's DRAFTING_CYCLE v2.37 clause; threads done 298 --commit bellows:2f9b9b6.
