continue

Planner Rule 22(b) substance review of Step 1 (schema + seed) — PASS. contract_rate_requirements table created with full diag-79 §6a schema (UNIQUE(contract_id, category), is_required/is_loaded/gap_source/discovered_at/resolved_at, idx on contract_id). Idempotent seed _seed_declared_rate_requirements() reads contract_document_refs.categories, computes is_loaded from sub-table presence, ON CONFLICT DO NOTHING; registered in init_db() after _backfill_data_json_columns, schema version bumped 4→5. Sound judgment calls: DO NOTHING for seed (won't clobber; reactive path will use DO UPDATE per Step 2 flag), base_rates is_loaded JOINs contracts.base_tariff→tariff_rates.tariff_name (tariff_rates has no contract_id FK). FROZEN clean (confidence.py/validator.py/validate_batch.py/element_invoice_evidence untouched). scope_check clean: contract_tables.py + database.py. 417 targeted tests pass, no regressions. Minor note for QA: confirm seeded rows carry gap_source='document_reference'. Release Step 2 (reactive wiring — surgical Gate-9 task_code change + action_router upsert/auto-resolve + rates.py parser-drop surfacing).
