verdict: continue
Findings accepted (delegated authority; clean gates + substance verified by direct read). The crux is CONFIRMED FROZEN-safe: contract-owned parsed rates land as Tier-1 by writing confirmed sub-table rows with source_document_id=NULL / source_document_scope=NULL — the validator's Tier-1 queries (gate 5 validator.py:1660+, gate 7 2087+, gate 8 2522+) select by contract_id only with NO source_document_id filter, so NULL-source rows are contract-direct automatically; Tier-2 override/fallback queries filter by specific doc IDs and never match NULL. No validator change needed.
Wire-in map complete: new contract_documents store (direct contract_id FK, NO bridge row per the separate-store decision); re-point exec-191 upload (file path -> data/contract_docs/<contract_id>/, INSERT contract_documents not carrier_documents, skip contract_document_refs, parser takes contract_documents.id); additive parsed_rate_candidates columns (contract_document_id nullable + doc_source_scope); carrier-page upload untouched; file-serve + re-parse extend with scope=contract. Tier Small-Medium; FROZEN core untouched (additive schema only). Final step (1 of 1) -> move plan 193 to Done/.
