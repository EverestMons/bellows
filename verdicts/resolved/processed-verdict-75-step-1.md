verdict: continue
Planner Rule 22 (b) substance check PASS. Single-step read-only diagnostic (id 75); both gates passed, substance-only review. Findings at knowledge/research/ambiguous-tables-headless-reclassification-2026-06-16.md re-classify the 32 ambiguous tables against the headless boundary with per-table readers/writers. Result corrects an earlier Planner overstatement: headless yields only ~5 net-new cuts, NOT "the bulk." Buckets: KEEP-VALIDATION 0; KEEP-INGESTION 21 (most ambiguous tables feed the ingestion pipeline — resolution_checker/exit_interview wired into ingest.py Steps 7-8, billto_validator per-import, activity_import, copilot extraction as the ingestion mechanism, contract-import audit trails — ingestion stays under headless); CUT-UI 7 (but 2 already dropped in v3, so 5 net-new: carrier_policy_references, carrier_service_points, contract_exceptions, prompt_queue, system_kv); DECISION 4 (action_queue, action_queue_audit, follow_up_config, navisphere_attestations). The 4 DECISION tables collapse to one question — does the headless tool dispatch post-validation actions or stop at producing results — but cutting them requires surgery in validate_batch.py (core), not a clean DROP. Critical drift catch: contract_governing_pubs was reported "no reader found" by diagnostic 67 but a reader (database.py:_ensure_pricing_versions:1275) was added between diagnostics — the codebase moved under the analysis, which makes the planned adversarial verify-sweep non-optional and means even 67's CORE/SEVERABLE list may need re-checking. Also flagged: tariff_library + contract_partner_rates are KEEP-but-dead-write (written, no reader). Read-only, no code touched, confidence machine untouched (confirmed reads zero ambiguous tables). Close to Done.
