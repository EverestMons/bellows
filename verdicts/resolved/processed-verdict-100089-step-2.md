continue

Both steps verified at the (b) read. DEV: connect_readonly now sets ro = None, opens mode=ro and runs PRAGMA schema_version inside the try before returning it; the except arm closes ro first (a failing close swallowed), then applies the unchanged message test — re-raise unless "unable to open database file" with the file present — and the fallback (plain connection, PRAGMA query_only = 1) is unchanged; the docstring names both forms (thread 299). Tests: the _DeferredRefusal stand-in and t7-t9 as drafted; red exactly as the plan predicted (t7 assert False is True on closed, t8 DID NOT RAISE, t9 the refusal inside status.query_in_flight), green 10 passed, 1 skipped in the file; full suite 2302 passed, 2 skipped; MUTATION: 4 killed, 0 survived, 0 error. QA: five fresh copies of a backup of the live DB under /usr/bin/python3 (SQLite 3.43.2) with the opener's own probe statement — 5 of 5 raised at the first query, 0 at connect, so on this machine the deferred form is the form, not an occasional one, and it is the form the probe now catches; the live DB opened once, read-only, for the backup; Rule 20 PASSED; every gate PASS on both steps. The one intermediate block flagged is the QA re-running its own Rule 20 block. The agent read runs = 10, pid 3106 before the plan and after both suite runs; the canonical evidence tree empty throughout. Continue — close. Post-close: both restarts (lifecycle.py is daemon-loaded), threads done 299 --commit bellows:47986e3.
