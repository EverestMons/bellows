continue
Rule 22 passed — Planner-authorized terminal close. Phase 3c plan-hash drift warning shipped: import hashlib at bellows.py:3, 7-LOC warning block at run_plan() lines 248-254 between DB-resume block and is_diagnostic check. Fires when resume_step > 1 AND shadow_text exists AND sha256(shadow) != sha256(plan_text). Stdout warning + Pushover "Bellows — Plan Modified". Warn-and-proceed (no halt). +1 test (test_run_plan_plan_hash_drift_warning), 71/71 in test_bellows.py. Behavioral hash smoke confirms hashlib import. BACKLOG #6 (step state across re-claim) is now structurally complete: Phase 3a (verdict-only resume docs) + Phase 3b (DB-based step state) + Phase 3c (hash drift warning) all shipped. REMINDER: restart Bellows to load.
