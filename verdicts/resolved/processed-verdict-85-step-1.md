continue

Planner Rule 22(b) override of a benign scope_check FAIL + substance review — PASS. The gate flagged tests/test_carrier_rules_tariff_qa.py as out-of-scope, but Step 1's plan body explicitly authorized updating the test that references a removed route ("update it to reflect removal"); the test path just wasn't inlined in the scope line (same benign pattern as #81 Step 3). I verified the test diff directly (177 del / 14 ins): every removed test is cut-related — test_carrier_policy_references_table_exists/_fk_constraint (deleted table), the entire TestPolicyCRUD class (deleted routes), test_policies_page_renders/_shows_existing_data (deleted route), and test_six_card_grid_renders renamed to test_five_card_grid_renders (kept behavior, card count adjusted). NO kept-behavior coverage was removed. Code removal is sound and complete: carrier_policy_references (3 routes + card + template), carrier_service_points (3 routes + card + copilot config + template), prompt_queue (5 CRUD routes + dead fn + template) with the /prompts/text API correctly RETAINED (used by 3 templates), system_kv neutered to no-ops in carrier_identity (genuinely optional perf-marker, not load-bearing — confirmed; full scan always runs now, acceptable since carrier-merge intelligence is itself Cluster 7). 2-week recency check clean for all handlers. Backend-ref/nav cleanup done. FROZEN untouched. CARRY-FORWARD FOR STEP 3 QA: the full-suite collected count will DROP by ~12 (removed cut-tests) — QA must reconcile the new total against the removed tests and confirm zero KEPT tests broke, not merely report "2 pre-existing failures". Release Step 2 (schema removal: DDL + merge cascade + drop migration + schema v7).
