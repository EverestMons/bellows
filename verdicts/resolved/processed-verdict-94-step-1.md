continue

Planner Rule 22(b) substance review — PASS. Final-cluster (Dispute) coupling diagnostic, all 6 items + verdict with file:line evidence and A/B classification. Three focal answers resolved: (Q1) validate_batch.py:550-556 disputes query is inside generate_quality_summary() — a report-writing sub-fn, NOT the core validate→result path; validate_batch.py is NOT in the true FROZEN set (FROZEN = engines/validator.py gates + confidence machine; validate_batch.py was already modified by exec 78/82) — strip to zeros, no FROZEN exception needed. (Q2) action_router only READS disputes, all _table_exists guarded → graceful no-ops; DISPUTE_EMAIL action content uses validation gate data not the disputes table; cutting does not break kept action routing (only FOLLOW_UP generation + existing-dispute routing become no-ops, correctly). (Q3) invoice detail has 3 UNGUARDED dispute SELECTs (app.py:1742,1881) that would OperationalError if the table drops without stripping — MUST strip; degrades gracefully (lifecycle falls through to activity-based classification, View-Dispute button gone). All 3 tables CLEAN-CUT, no HOLD. DECISION-table boundary clarified (action_queue.dispute_id stays as dead soft-ref column; follow_up_config/navisphere_attestations not structurally touched). email_matcher/email_triage dispute_outcomes refs confirmed already gone (cluster 8). FK drop order: dispute_gates → disputes → dispute_outcomes. Accept and close to Done — findings in knowledge/research/cluster10-dispute-coupling-2026-06-18.md. One point worth a quick CEO confirmation before authoring (validate_batch.py was declared off-limits in prior cluster plans): strip-vs-guard the validate_batch reporting query. Surfacing that, then authoring the final executable.
