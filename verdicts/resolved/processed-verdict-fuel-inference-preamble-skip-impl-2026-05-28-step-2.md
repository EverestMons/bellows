verdict: continue
Rule 22 (b) substance — PASS. Rule 25 gate_failure — overridden as false positive.

The scope_check FAIL flags 5 evidence files at `knowledge/qa/evidence/fuel-inference-preamble-row-skip-qa-2026-05-28/*.txt` (engine-preamble-fields.txt, engine-preamble-count.txt, endpoint-gate-logic.txt, unit-test-methods.txt, integration-test-methods.txt) as out-of-scope. These files were explicitly required by the plan body ("Deposit all grep outputs as evidence files at invoice-pulse/knowledge/qa/evidence/fuel-inference-preamble-row-skip-qa-2026-05-28/<check-name>.txt") and the plan's Deposits block named the evidence directory. The sibling file full-suite.txt — created in the same directory by the same agent following the same instruction — passed the scope_check. The matcher asymmetry: full-suite.txt is mentioned by exact filename in the prompt body, while the other 5 are referenced as "one file per grep-based deliverable verification check." The Bellows scope_check matcher appears to require explicit per-filename mention rather than honoring the parent-directory mention in Deposits. This is a Bellows gate-evaluator gap, not a real scope violation, and a new BACKLOG item will be filed for the fix.

Substance check on the QA deliverable: all 8 deliverable verification rows green with file:line citations; all 8 multi-dimensional checks PASS with per-test detail; full-suite went 1935→1944 passing, 2 pre-existing failures unchanged, zero new regressions; all 9 new tests verified by name and line number (TestPreambleRowSkip 6 tests at lines 367-453, TestPreambleEndpointBehavior 3 tests at lines 619-682); Rule 20 self-check PASSED banner present with 6 evidence files cited; apply endpoint no-gate design accepted correctly with reasoning matching DEV's. Engine implementation verified: preamble_rows and preamble_rows_skipped fields at engine lines 43-44, preamble_count detection loop at engine lines 80-83, all 8 return statements populate preamble fields (7 explicit at engine lines 101-256, 1 pre-detection using dataclass defaults at engine line 67), endpoint gate adjusted at web/contracts.py:5778-5799.

Plan ships. Closing the executable to Done/ after this verdict.
