verdict: continue

Step 1 of 2 (DEV) — advance to QA. All gates PASS (scope_check PASS, deposit_exists PASS, rule_22_verification clean; 6 files changed match the declared set exactly; 2 INFORMATIONAL intermediate-decision blocks, both benign — a verification rerun and the pre-edit confirmations). Substance check (b): the fix is correct and confined. runner.py diff is exactly the import line + BELLOWS_ROOT replacement; LOGS_DIR, _write_log(), run_step() byte-unchanged. bellows_root.py is pathlib-only (no bellows/runner/planner/verdict import — cycle safe) and uses _start=None (3.9-safe; Python confirmed 3.9.6). conftest isolate_runner_logs_dir autouse fixture landed adjacent to isolate_verdicts_dir. Suite 448 -> 451, exactly +3 new tests, zero regressions, all 19 test_runner.py pass with the fixture active. Canonical-logs check clean (git status --porcelain logs/ empty) — the conversion did not widen the worktree-write gap; the fixture closed it. Pre-edit Rule 39 verification confirmed all five claims including config.json absent-from-worktree marker behavior. Negative-test integrity deliberately left to QA's dedicated check rather than pre-verified here. Proceed to Step 2 (QA).
