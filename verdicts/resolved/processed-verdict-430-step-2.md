verdict: continue
gate_failure = scope_check, BENIGN (same class as step 1): the only flagged file is `tests/test_stop_path.py`, the new test file Step 2's `## Scope` declared (`bellows/tests/`). files_changed = bellows.py, dashboard.py, tests/test_stop_path.py, knowledge/dev-logs/restart-lock-fix-step2-2026-08-15.md — all four intended. No genuinely out-of-scope file.

Rule 22(b): Planner verified from COMMITTED source (`git show 39c96f6:bellows.py`, `:dashboard.py`, receipt `2d56048`), not the agent summary. The kill path implements the mandated ordered guard sequence with no bypass: `stop_daemon` (bellows.py:155) attempts `acquire_instance_lock` FIRST (164 — success ⇒ no incumbent); discovery `_discover_holder` refuses on 0 PIDs and on `len(pids) > 1` ("ambiguous", 115-116, fold 16); identity via `ps -o command=` (127); idle `_check_idle` refuses ONLY on `row["status"] == "running"` (149) — `awaiting_verdict`/NULL and orphaned `in-progress-*` files do NOT block (fold 14 + fold 7 reconciliation, confirmed in the docstring and receipt §Idle guard); SIGTERM at 187 → SIGKILL at 210 using LOCAL `_STOP_SIGTERM_TIMEOUT=5`/`_STOP_SIGKILL_TIMEOUT=2` (95-96) with NO `import dashboard` (grep clean, fold 15); re-acquire as arbiter at 223 REUSES `acquire_instance_lock` (fold 17/23). Dashboard `_do_restart` (dashboard.py:397) invokes the stop path via `subprocess.run([sys.executable,"bellows.py","stop"])` for a non-child incumbent when the lock is still held after `_terminate_child` — no bellows import, no adopt-and-kill on initial spawn. Entry point = `bellows.py stop`/`restart` CLI args (agent-chosen over a separate module, with rationale). Receipt reports 13/13 targeted tests passing incl. `test_ambiguous_lsof_refuses`, `test_idle_guard_passes_awaiting_verdict_with_orphan`, and `test_reacquire_race_lost_refuses`; full-suite raw-output is Step 3's QA duty.

Continue to Step 3 (QA — full suite + Rule 20 self-check + code-read that no kill bypasses both guards; must NOT touch the live PID 3969).
