verdict: continue

Clean mechanical gate (all rows PASS). Rule 22(b) BY EXECUTION: both helper names count exactly 2 in bellows.py (def + sole call each, the == 2 probes); the config keys documented in config.example.json; my own targeted run reproduces 200 passed (the 189 test_bellows baseline + the new hygiene module). Step commit 05b3fc8. Proceed to Step 2 (QA: full suite foreground + the wiring diff read + the scratch prune rehearsal + the restart boundary).
