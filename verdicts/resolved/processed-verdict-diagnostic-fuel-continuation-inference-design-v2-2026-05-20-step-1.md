verdict: continue
Design approved. Drop per-contract materialization ceiling override (global config only). Cents-as-integer prompt change deferred to executable step. Sample Mac dev DB for real bracket patterns when scoping the build.
