verdict: continue

Scope_check false positive. All "out-of-scope" files are pre-existing working-tree dirt (session-lessons uncommitted append, bellows.py from parallel Plan B, ledger.jsonl from Bellows own bookkeeping). The README was created correctly at verdicts/README.md — verified via direct filesystem check. This is the exact _parse_diff_stat OR-union bug that Plan B fixes.
