stop

QA verified from the merged commit 31e0a58: gates 10/11 with rule_22_verification FAIL on the receipt's row 27, which is the plan's own Item 2.5 rule applied: tools/mutation_check.py on the committed code gives cycle_check M1 and M4 KILLED but M2-drop-negation-stripping and M3-drop-oserror-guard SURVIVED (the Planner's independent run agrees: 2 killed / 2 survived; cycle_yields 1/0; plan_lint 2/0). Everything else is green and stays on main at e088d05: fail-before/pass-after 12/12, corpus canaries 19/19 BAR_MET, the five fixtures as pinned, the (u) probes, full suite 1782 passed exit=0. Item 2.5 says a survivor is a missing test stated as Critical, never a note, so the plan cannot close. Stop; a follow-up plan adds the two discriminating tests (a closure line whose claim token sits inside a negated span, e.g. 'has not met the bar'; an oversized backticked register ref that reaches exists()) and re-runs the kill map.
