continue

continue — all gate rows PASS at the QA checkpoint; Rule 22(b) substance read by the Planner: `tools/lessons_guard.py` gains `_scan(root) -> (freezing, parked_by_class)` with the sidecar branch fail-closed as specified (no sidecar, unreadable JSON, a non-string reason, or a reason not starting `class:` → freezing), `freezing_plans` keeps its signature, `parked_class_holds` added, `pin` and `verify` print `class-held: N`; `hooks/commands/wrap.md` changed on one line naming the case; nine new tests beside the sixteen rows; `MUTATION: 3 killed, 0 survived, 0 error`; the full suite `2262 passed, 1 skipped`; the receipt's Rule 20 block PASSED; QA Item 2 on a copy of the lane's hold files: `lanes: 1  frozen: no  class-held: 1` exit 0, then FROZEN naming only the synthetic `stale-checkout` probe, exit 2. Merged at the QA pause (`dfc4bb0`). Reading note (not a stop): the DEV made three commits, not two — `3ba9461` fixed the m3 mutant (a surviving "park-when-absent" arm) before the mutation run, the subject says so, and the run then killed all three; the plan's two-commit post-condition is the one thing the receipt does not meet, by one honest commit. The guard's live behaviour meets STANDING_CONSTRAINTS v1.1 line 9 (`34725147`), applied by the CEO this morning.
