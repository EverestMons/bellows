verdict: continue
Planner Rule 22 (b) substance check PASS. Single-step read-only diagnostic (id 69); both gates passed, substance-only review. INFORMATIONAL intermediate-decision flag was the agent doing extra due diligence (checking _fix_stale_fk_references drop pattern + test schema assertions) — in-scope, 1 file changed (deposit only). Findings at knowledge/research/schema-migration-mechanics-2026-06-15.md fully answer the mechanics question. Key facts: schema defined via CREATE TABLE IF NOT EXISTS in database.py:_create_tables() + contract_tables.py:create_contract_tables(), run by init_db() (database.py:98). schema_version table gates everything — CURRENT_SCHEMA_VERSION=2 (database.py:21); fast-path at init_db:106-109 SKIPS all migrations when stored version >= current. Migrations are ad-hoc guarded functions called sequentially from init_db(), no numbered registry, no external runner. DROP precedent: only table-REBUILD patterns exist (create_new→copy→drop_old→rename); NO precedent for permanent whole-table drop, but _fix_stale_fk_references() (database.py:1744-1756) has an exact guarded leftover-DROP template to copy. LOAD-BEARING answer (Q4): to remove tables from EXISTING DBs, must (a) add DROP TABLE IF EXISTS to the init_db migration chain AND (b) bump CURRENT_SCHEMA_VERSION 2→3 — without the bump the fast-path skips the drop and live DBs keep the tables. No out-of-band migration mechanism exists. Q5 verdict: removing these tables is a SINGLE DEV-step change (DROP TABLE IF EXISTS, no data migration), all changes atomic in one commit. Read-only, no code touched. Close to Done.
