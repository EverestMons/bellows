continue

Planner Rule 22(b) substance review of Step 2 (schema removal) — PASS. CREATE DDL removed for all 3 dispute tables: disputes + dispute_gates (database.py sections 5/5b), dispute_outcomes + dedup UNIQUE index (contract_tables.py) + docstring entry. Drop migration: 3 tables appended to _drop_dead_tables() FK-ordered — dispute_gates before disputes (correct for the dispute_id FK), then dispute_outcomes. CURRENT_SCHEMA_VERSION bumped 12→13, justified. FROZEN clean: zero diff to engines/validator.py + engines/confidence.py (validate_batch.py's only change remains the Step-1 approved reporting-query strip). action_queue.dispute_id intentionally left as a dead soft-reference column (no FK). scope_check clean: database.py + contract_tables.py. 97 targeted tests pass. Release Step 3 (FINAL full-suite QA + closeout). REMINDERS for QA: run full suite with -v/--tb=line (avoid inactivity false-kill); FROZEN check = validator.py/confidence.py ONLY (validate_batch.py carries the approved reporting diff); reconcile against TRUE prior 1684 (post Step 1); verify FK drop order on a simulated existing DB; the closeout PROJECT_STATUS entry should note this COMPLETES ADR step-3 (all 35 clean-DROP tables cut across clusters 1-10).
