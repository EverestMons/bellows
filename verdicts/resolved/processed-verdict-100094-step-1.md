continue

Step 1 (DEV) verified at the (b) read. Two single-parent commits on tuyere main, pushed: a93777c (tuyere/threads.py, tests/test_tuyere.py, the manifest) and 4402116 (the run file, the dev-log) — the five declared files; 16 gate rows PASS, 0 intermediate blocks. The code matches What this changes item for item: the listing `diff-tree --root --no-commit-id -r --name-only --diff-merges=first-parent`, returning `list_ok` and `list_error`; a failed listing refuses after the not-found and not-pushed refusals and before the component check, with `threads done: could not list the files of REPO SHA: <error>`; a defect's miss refuses naming the component, `REPO SHA`, the first ten files (`… +N more`, or `the commit touched no files`) and the three remedies; a blank `--allow-miss` refuses; a non-blank one records `checks.allow_miss` and appends ` allow-miss: <WHY>` to the direct-edit note — after the note's closing `— review?` rather than directly after the checks, a cosmetic placement that t2 accepts, not corrected; `--allow-miss` on a hit, `n/a` or a non-defect prints `--allow-miss not needed (component <c>, type <type>)` and records nothing; the arms' comment names the refusal; `--allow-miss WHY` sits on the done parser. No PEP 604 union in any signature. Tests: besides the planned `_done_args` parameter, E1 (component `gates` to `threads.py`) and E2 (renamed `test_done_commit_records_component_na`, its miss half and docstring claim removed) are the only existing tests changed — read from the diff's removed lines by enclosing function; t1–t8 added under the planned names. Dev-log: the four headings; P2's cell whole through `needs git 2.31 or later`; 107 before, 7 red with t4 green before the production edits (recorded with why), 115 after; `MUTATION: 9 killed, 0 survived, 0 error`, m1–m9 as planned. Planner checks: tuyere's full suite on the canonical tree after the merge, 115 passed; the QA's merge check repeated, `7 hit`; the agent read runs = 11, pid 61599 at the deposit, at this read and around my suite run; the live thread store matches its pre-deposit baseline (max thread id 309, 228 thread events, 130 intents), so step 1 wrote nothing live, as the plan requires. Continue — step 2 (QA).
