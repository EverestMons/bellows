# Bellows — Project Status
**Last Updated:** 2026-07-01 (Reporting Phase 2 — read-side cycle query module shipped)

## Status: Phase 1 Complete — Live (daemon running, all 10 watched paths active)

## Completed
### Plan 148
Session-limit park + auto-resume shipped 2026-07-09 (plan 148). Runner detects session-limit 429 result events on stdout (distinct from transient stderr 429s) and parses `resets-at` wall-clock time + IANA zone with a 5h fallback. Daemon parks the plan (rename to `parked-*` + `parked_steps` DB table), notifies CEO once (deduped), and auto-resumes at reset time via both `_rescan` (30s interval) and startup scan (survives daemon restart). No-progress safety guard ensures parking only when the step made zero progress (`num_turns <= 1`, `total_cost_usd == 0`, `output_tokens == 0`). 26 new tests, full suite 788 passed. Daemon restart required.

### Plan 141
Claim-dedup guard shipped 2026-07-07 (plan 141), closing the 137/138 double-claim class of bugs. Three fixes implemented: (1) partial unique index on active placeholders in lifecycle.db provides defense-in-depth at the DB level, (2) application-level dedup check in `run_plan` refuses duplicate deposits before minting, routing them to `halted-`, (3) `_invalidate_seen_on_redeposit` guard prevents `_seen` clearance while an active plan exists. Worktree-health probed for plan 140 — finding benign (worktree created and torn down normally). Tangle cruft from 137/138 retired: orphan worktrees removed, stale branches deleted, buggy stash dropped.

### Plan 140
plan_lint qa_steps ↔ step-label cross-check shipped 2026-07-07 (plan 140). WARN-only advisory in `scripts/plan_lint.py` that detects mismatches between the `qa_steps` header field and actual step labels — guards against the plan-133 trap class where a DEV step is silently gated as QA. Six tests cover all specified cases. Full suite 755/755 green.

### Plan 129
Cycle-nudge trigger shipped 2026-07-06 (code-level verified, inert until daemon restart, live canary pending). Implementation covers config block with safe absent-config defaults, evaluator with read-only cross-DB queries (lessons-forge.db MAX(ingested_at) + lifecycle.db closed-plans count), interval gating, post-fire suppression until ingestion advances, and notifier integration via deferred (non-urgent) delivery. 8 new tests, full suite green (749 passed). Closes diagnostic-127 gap assessment row 2.

### Plan 122
FORWARD rows 1/3/14 shipped as tier-2 batch: (A) step-header regex made case-insensitive across `gates.py` (2 sites) and `verdict.py` (1 site) so mixed-case `## Step N` headers extract correctly, (B) `_strip_trailing_parenthetical` helper added and applied at all 6 deposit-path extraction sites in `gates.py` to strip parenthetical qualifiers like `(volunteered)` from paths, (C) mojibake U+FFFD chars replaced with checkmark in QA report `preserve-unlanded-commits-on-stranded-cleanup-2026-06-01.md`. Daemon restart required to load gates.py/verdict.py changes.

### Plan 119
Declared `**Scope:**` block shipped (CEO 2026-07-02 design: norm-shaped changes are authorable scope, not failures). `scope_check` now supports a three-tier pass path: global allowlist, declared scope (exact file or prefix), and text-mention — in that priority order. Strictness against undeclared files is preserved; evidence text extended with `; not in declared **Scope:** block` when a plan has scope declarations. Backward compatibility verified: plans without a Scope block produce byte-identical behavior to pre-change. Lint check (d) prevents dead convention text (present-but-empty blocks). Daemon restart required to load the updated `gates.py` (covers plan 118's fallback too). PLANNER_TEMPLATE codification of the authoring convention is the deliberate follow-up once the mechanism is proven live.

### Plan 118
FORWARD row 9 pre-deposit lint script shipped (`scripts/plan_lint.py`) and `rule_20` receipt fallback in `gates.py` closes the plan-116 false-positive class where plans declaring deposits as inline prose rather than a `**Deposits:**` block caused `rule_20_self_check` gate failures on substantively correct QA deposits. The shared `_extract_agent_declared_deposits` helper eliminates duplicated receipt-parsing logic between `_gate_deposit_exists` and `_gate_rule_20_self_check`. Daemon restart required to load the `gates.py` changes.

### Plan 110
Reporting Phase 2 — read-side cycle query module shipped. New `reporting.py` with `query_cycle_report(db_path, start, end)` returning per-project, per-type plan counts, cost, and turn aggregates over a half-open `[start, end)` window on `closed_at`. Plan-grain `COUNT(DISTINCT p.id)`, step-grain `SUM(cost_usd)`/`SUM(turns)`, LEFT JOIN, read-only DB access (`?mode=ro`), no daemon imports. 8 test methods across 6 classes covering grain separation, hand-computed cost/turn verification, empty range, half-open boundary, grouping, and no-daemon-import static check. Full suite: 718 passed, 0 regressions. Blueprint at `knowledge/decisions/reporting-phase2-cycle-query-blueprint-2026-07-01.md`, QA at `knowledge/qa/reporting-phase2-cycle-query-2026-07-01.md`.

### Plan 64
Daemon-owned ledgers shipped and proven live (plans 41–63). Agents now emit PROJECT_STATUS, prompt feedback, and FORWARD rows via the Output Receipt `### Ledger Updates` channel; the daemon writes them post-merge. The append-only worktree-conflict class is eliminated (FORWARD rows 4/5/13 CLOSED). Prompt feedback lives in the `prompt_feedback` DB table; `agent-prompt-feedback.md` is daemon-generated. Also shipped this arc: FORWARD reconciliation sweep (26), gate-FP cluster fix (27–29), DB-out-of-git policy (30), status CLI + dashboard TUI + restyling (31–33, 59), type-qualified plan ids (36), plan_doc_ref (37/38/40), PLANNER_TEMPLATE v4.68.

### Plan 55
CANARY-PROJSTATUS-153330 — milestone via channel; daemon must append to PROJECT_STATUS.md (multi-turn fix).

- 2026-06-12: **BACKLOG → FORWARD register cutover (Reporting Phase 3, diagnostic 13).** Migrated 20 open-class entries from `knowledge/BACKLOG.md` to `knowledge/FORWARD.md` (diagnostic Section 4 format: 6-column table, 18 deferred-work + 2 ceo-decision-fork). Froze full BACKLOG.md (183,650 bytes, 131 entries) into `knowledge/BACKLOG-ARCHIVE.md` (read-only, byte-identical body). Deleted `knowledge/BACKLOG.md` via `git rm`. QA verified: format conformance, entry conservation (independent 5-class classification), archive integrity (zero diff), trim landed. Rule 20 self-check PASSED. Reference: plan 17, QA at `knowledge/qa/bellows-backlog-cutover-qa-report-2026-06-12.md`.
- 2026-06-10: **Deposit-loss fix — auto-commit declared deposits + fail-loud gate (BACKLOG #1).** Two-layer fix for silent deposit loss at worktree teardown: (a) PRESERVE: `_auto_stage_deposits()` in `bellows.py` auto-commits plan-declared deposits (path-scoped `git add`, never `git add -A`) immediately before `gates.check()` at step completion, so `deposit_exists` evaluates post-commit state and the teardown merge lands them; (b) FAIL-LOUD: `_gate_deposit_exists` in `gates.py` now runs `git status --porcelain` after confirming a deposit exists on disk, failing with `deposit_uncommitted` when the file is not committed. Changes are strictly additive — `_teardown_worktree`, land-or-raise contract, Gap 1b/1c guards, and stranded-cleanup preserve-branch logic are all unchanged. 6 new tests (3 auto-stage + 3 gate). Test suite: 461 passed, zero regressions. Rule 20 self-check PASSED. **DAEMON RESTART REQUIRED** to activate — the fix is in `bellows.py` and `gates.py`, loaded at daemon startup. Reference: `executable-bellows-deposit-loss-fix-2026-06-10`, design at `knowledge/architecture/silent-deposit-loss-teardown-fix-design-2026-06-10.md`, dev log at `knowledge/development/deposit-loss-fix-2026-06-10.md`, QA at `knowledge/qa/deposit-loss-fix-2026-06-10-qa.md`.
- 2026-06-10: **scope_check union-of-all-step-texts fix (closes BACKLOG #2 fully, #9 cross-step partial).** Replaced single-step text extraction in `_gate_scope_check` (gates.py) with a union of all step texts (steps 1 through `step_number`). Files mentioned in any prior step are now authorized at later steps, eliminating false positives from continuous-run cumulative diffs where prior-step files leak into later steps' `files_changed` sets. Purely additive — only widens the authorization haystack, never narrows it. The allowlist checks, directory-mention depth guard, failure-append, and all other clauses are unchanged. Evidence display retains current-step text for diagnostic clarity. 4 new tests: 2 positive (union authorization across steps) + 2 negative (teeth — unmentioned files still rejected, no directory-wide blanket authorization). Test suite: 455 passed, zero regressions. Rule 20 self-check PASSED. **DAEMON RESTART REQUIRED** to activate the fix (gates.py loads at startup). BACKLOG #9 (blueprint delegation) partially addressed: the blueprint file path is now authorized via the union (Step 1 mentions it), but files listed inside the blueprint remain unaddressed (blueprint-follow deferred per CEO disposition — regex-fragility risk). Reference: `executable-bellows-scope-check-fp-fix-impl-2026-06-10`, design at `knowledge/architecture/scope-check-false-positive-fix-design-2026-06-09.md`, dev log at `knowledge/development/scope-check-fp-fix-impl-2026-06-10.md`, QA at `knowledge/qa/scope-check-fp-fix-impl-2026-06-10-qa.md`.
- 2026-06-08: **resolve_bellows_root() helper + runner.py BELLOWS_ROOT conversion (Plan A, the one proven-reachable instance; runner LOGS_DIR test-isolation gap closed).** Created standalone `bellows_root.py` with `resolve_bellows_root()` — a config.json marker walk-up helper modeled on `resolve_governance_root()` in `decisions.py`. Converted `runner.py`'s `BELLOWS_ROOT = Path(__file__).parent.resolve()` to `BELLOWS_ROOT = resolve_bellows_root()`, making the module worktree-safe (resolves to canonical root even when `__file__` is inside `.bellows-worktrees/<wt>/`). Added `isolate_runner_logs_dir` autouse fixture to `tests/conftest.py` to close the load-bearing worktree-write gap: post-conversion, unpatched LOGS_DIR writes from worktree tests would resolve to CANONICAL `logs/` instead of the transient worktree. Three helper unit tests added including a negative worktree-resolution proof (`test_walks_up_to_config` — fails under legacy `Path(__file__).parent`, passes under the helper). The three latent instances (bellows.py, planner.py, verdict.py) remain unconverted per CEO decision — converting them requires their own targeted reachability proof. No daemon restart required for correctness (canonical resolution identical old vs new). Test suite: 451 passed, zero regressions. Rule 20 self-check PASSED. Reference: `executable-bellows-root-helper-runner-conversion-2026-06-08`, dev log at `knowledge/development/bellows-root-helper-runner-conversion-2026-06-08.md`, QA at `knowledge/qa/bellows-root-helper-runner-conversion-2026-06-08.md`.
- 2026-06-05: **_teardown_worktree (b) raise on git-log failure (last worktree-family silent-loss path).** Replaced the bare `except Exception: commit_shas = []` in `_teardown_worktree` step (b) with a land-or-raise contract: raise `WorktreeTeardownError` on git-log exception (chained) or non-zero returncode (with stderr), while preserving the legitimate-empty path (returncode 0, no commits made proceeds normally to copy-back and removal). This closes the last uncovered silent-loss path in the worktree teardown/resume family: a git-log failure previously defaulted `commit_shas` to empty, causing teardown to cherry-pick nothing, record no failure, and remove the worktree — losing un-landed commits with no recorded `worktree_teardown` failure (uncatchable by the Gap-1b continue-block and Gap-1c retry). The fix routes failures into the existing 1b halt / 1c retry path, matching the rest of the function's land-or-raise contract. Three regression tests added: exception-raises, nonzero-rc-raises, empty-commit-list-proceeds. Test suite: 455 passed, 5 carry-over failures, zero new regressions. Rule 20 self-check PASSED. **REMINDER: restart the Bellows daemon** to activate the teardown-(b) raise — the running daemon executed this plan under the pre-edit `_teardown_worktree`; the fail-safe activates on the next teardown after restart. Reference: `executable-teardown-b-raise-on-log-failure-2026-06-05`, dev log at `knowledge/development/teardown-b-raise-on-log-failure-2026-06-05.md`, QA at `knowledge/qa/teardown-b-raise-on-log-failure-2026-06-05.md`.
- 2026-06-04: **scope_check directory-mention authorization (2026-05-28 FP).** Added one depth-guarded trailing-slash ancestor-directory authorization clause to `_gate_scope_check` in `gates.py`. When a step's text names a parent directory with a trailing slash (e.g. `knowledge/qa/evidence/foo-2026-06-04/`) and the directory has at least 2 path segments (>= 1 slash), child files under that directory are accepted as in-scope without requiring individual basename mentions. The depth guard prevents shallow single-segment mentions (e.g. `web/`) from blanket-authorizing everything beneath them. Ancestors are derived from the file's own path via `os.path.dirname`, so only genuine parents can match. Four regression tests added: trailing-slash dir acceptance, placeholder-pattern dir acceptance, shallow-dir depth guard rejection, and unmentioned sibling-dir rejection. All existing clauses, constants, and failure-append unchanged. Test suite: 452 passed, 5 carry-over failures, zero new regressions. Rule 20 self-check PASSED. **REMINDER: restart the Bellows daemon** to activate the scope_check directory-mention clause — the running daemon evaluated this plan under the pre-edit gates.py; the fix activates on the next gate evaluation after restart. Reference: `executable-scope-check-dir-mention-2026-06-04`, dev log at `knowledge/development/scope-check-dir-mention-2026-06-04.md`, QA at `knowledge/qa/scope-check-dir-mention-2026-06-04.md`.
- 2026-06-04: **Invalidate _seen on re-deposit via on_created/on_moved (#7).** Extracted the inline `_seen`-invalidation logic from `PlanHandler.on_modified` into a new shared helper method `_invalidate_seen_on_redeposit(self, path)` and wired all three watcher callbacks (`on_created`, `on_modified`, `on_moved`) to call it before `_handle`. This fixes the bug where a follow-on plan deposited at the same base slug after a Planner-direct close-to-Done (which the daemon never observes) was silently skipped because `on_created`/`on_moved` did not invalidate the stale `_seen` entry. The lifecycle-prefix guard (`in-progress-`, `verdict-pending-`, `halted-`) is preserved verbatim to prevent re-dispatch loops on Bellows-managed renames. `_handle` and the rescan path are untouched. Four regression tests added mirroring the existing `on_modified` pair for create and move paths. Test suite: 448 passed, 5 carry-over failures, zero new regressions. Rule 20 self-check PASSED. **REMINDER: restart the Bellows daemon** to activate the create/move `_seen` invalidation — the running daemon executed this plan with the pre-edit watcher; the fix activates on the next watcher event after restart. Reference: `executable-seen-invalidate-on-created-moved-2026-06-04`, dev log at `knowledge/development/seen-invalidate-on-created-moved-2026-06-04.md`, QA at `knowledge/qa/seen-invalidate-on-created-moved-2026-06-04.md`.
- 2026-06-04: **Re-attempt recoverable dirty-tree teardown on continue-resume (Gap 1c).** Added `_retry_recoverable_teardown()` module-level helper to `bellows.py` that re-attempts a worktree teardown at verdict-consume time when the prior step's failure was the dirty-tree variant only. The helper fires at the top of the `if v == "continue":` branch, immediately before the existing Gap-1b halt guard. It collects worktree_teardown failures, gates on three early-return conditions (no failures, worktree directory missing, non-dirty-tree variant), and on success clears all worktree_teardown failures from `gate_result["failures"]` in place so the normal continue/advance proceeds. On any skip or failure, the failure is left for Gap-1b to halt. This closes the loop Gap 1(b) opened: 1(b) halts safely, 1(c) recovers automatically for the common dirty-tree case where the operator has already committed the stray file by verdict time. Four regression tests added targeting the helper directly. Test suite: 444 passed, 5 carry-over failures, zero new regressions. Rule 20 self-check PASSED. **REMINDER: restart the Bellows daemon** to activate the Gap 1c retry — the running daemon executed this plan with pre-edit `_consume_verdicts`; the helper activates on the next continue-resume after restart. Reference: `executable-reattempt-teardown-on-continue-resume-2026-06-04`, dev log at `knowledge/development/reattempt-teardown-on-continue-resume-2026-06-04.md`, QA at `knowledge/qa/reattempt-teardown-on-continue-resume-2026-06-04.md`.
- 2026-06-01: **Preserve un-landed worktree commits on stranded-cleanup — branch before destroy (Gap 2a).** Added a preserve-before-destroy guard to `_create_worktree` in `bellows.py` that detects un-landed commits on a stranded worktree's detached HEAD and creates a `bellows-preserved/{slug}-{ts}` branch pointing at that HEAD before the existing force-remove/rmtree/prune cleanup runs. Detection predicate: `git merge-base --is-ancestor <wt_head> main` — returncode 0 means already landed (skip), any other outcome (non-zero, exception, unresolvable main) triggers preservation (fail-safe bias to never destroy silently). The existing destroy/recreate-at-HEAD-detach sequence is byte-unchanged — this is a data-safety backstop, not a functional-resume fix. Three regression tests added covering positive preservation, no-spurious-branch specificity, and fail-safe-under-uncertainty. Test suite: 440 passed, 5 carry-over failures, zero new regressions. Rule 20 self-check PASSED. **REMINDER: restart the Bellows daemon** to activate the preserve guard — the running daemon executed this plan with pre-edit code; the guard activates on the next plan dispatched after restart. Reference: `executable-preserve-unlanded-commits-on-stranded-cleanup-2026-06-01`, dev log at `knowledge/development/preserve-unlanded-commits-on-stranded-cleanup-2026-06-01.md`, QA at `knowledge/qa/preserve-unlanded-commits-on-stranded-cleanup-2026-06-01.md`.
- 2026-06-01: **Block continue verdict over uncleared `worktree_teardown` failure — route to `halted-` for manual R2 instead of silent advance (Gap 1b).** Added a guard at the top of the `continue`-verdict branch in `_consume_verdicts` (`bellows.py`) that blocks advancement when the prior step's gate result carries an uncleared `worktree_teardown` failure. Detection predicate: `any(f.get("gate") == "worktree_teardown" for f in gate_result.get("failures", []))`. When the predicate fires, the plan routes to `halted-` (not `Done/`, not `in-progress-`) with a `continue-blocked-worktree-teardown` ledger action, CEO notification, and loud ERROR log — preventing the silent-skip-to-orphaned-commits cascade identified in the 2026-05-31 diagnostic. The guard sits before the final/non-final split, covering both inter-step resume and final-step continue-to-done. Three regression tests added. Test suite: 437 passed, 5 carry-over failures, zero new regressions. Rule 20 self-check PASSED. **REMINDER: restart the Bellows daemon** to activate the guard — the running daemon executed this plan with pre-edit code; the guard activates on the next plan dispatched after restart. Reference: `executable-block-continue-over-worktree-teardown-failure-2026-06-01`, dev log at `knowledge/development/block-continue-over-worktree-teardown-failure-2026-06-01.md`, QA at `knowledge/qa/block-continue-over-worktree-teardown-failure-2026-06-01.md`.
- 2026-05-31: **No-match verdict WARN dedup in `_consume_verdicts` — log once per resolved/ file, clear on leave (BACKLOG 2026-05-21).** Added a module-level `_warned_no_match: set[str] = set()` guard adjacent to the existing `_NOTIFIED_MISPLACED` pattern in `bellows.py`. The no-match `else` branch in `_consume_verdicts` (the site that logs `⚠️ no verdict-pending plan found step {N} — leaving in resolved/ for retry`) now fires only when `fname not in _warned_no_match`, adds `fname` to the set after logging, and clears via `_warned_no_match.discard(fname)` at both processed-move sites: the `if plan_matched:` consumed-move and the `if stale:` stale-move. Without the fix, a single stuck file in `verdicts/resolved/` produced ~2,880 WARN lines/day; with the fix, it produces exactly one. Message text is unchanged. Two regression tests added to `tests/test_consume_verdicts.py`: `test_no_match_warning_logged_once` (proves log-once across two ticks) and `test_no_match_warning_cleared_when_file_leaves_resolved` (proves clear semantics via stale-detection path). Test suite: 5 failed (same carry-over baseline), 434 passed, 0 new regressions. Rule 20 self-check PASSED on 7 evidence files. **REMINDER: restart the Bellows daemon** to activate the no-match WARN dedup — the running daemon executed this plan with pre-edit code; the fix activates on the next plan dispatched after restart. Reference: `executable-no-match-verdict-warning-dedup-2026-05-31`, dev log at `knowledge/development/no-match-verdict-warning-dedup-2026-05-31.md`, QA at `knowledge/qa/no-match-verdict-warning-dedup-2026-05-31.md`.
- 2026-05-29: **Worktree & dirty-tree pre-check hardening — stranded-worktree cleanup (`_create_worktree`), rstrip + leading-space tolerance, `.bellows-worktrees/` lifecycle-ignore (BACKLOG a/f/g).** Batch close of three confirmed-Open BACKLOG items in the worktree-teardown / dirty-tree-pre-check subsystem. (a) `_create_worktree` now detects and cleans stranded worktrees from prior failed dispatches before re-creation, mirroring the `__init__` startup-prune pattern — eliminates the `fatal: '<path>' already exists` loop on retry. (f) Changed `.strip()` to `.rstrip()` on the dirty-tree pre-check's porcelain result to preserve leading status-code space on the first porcelain line, fixing a false-strict filter hole where space-prefixed lifecycle artifacts (` D`, ` M`) were not recognized. (g) Extended `_LIFECYCLE_IGNORE_RE` with `^\.bellows-worktrees(/|$)` to treat worktree directories as lifecycle artifacts, fixing three pre-existing `test_worktree.py` failures and any real-world pre-check trip on the worktrees directory. Four regression tests added with positive and negative controls. Test suite: 432 passed (+7 from baseline: 3 flipped + 4 new), 5 pre-existing failures, zero new regressions. Blocking behavior preserved — filter-negative tests all green. **REMINDER: restart Bellows daemon to activate all three fixes.** Reference: `executable-worktree-precheck-hardening-2026-05-29`, dev log at `knowledge/development/worktree-precheck-hardening-2026-05-29.md`, QA at `knowledge/qa/worktree-precheck-hardening-2026-05-29.md`.
- 2026-05-28: **Lifecycle-artifact filter shipped to dirty-tree pre-check + QA verified + smoke-test confirmed (session 14, closes BACKLOG #1 and #2).** Three-arc ship across one session: diagnostic → DEV substance → QA verification → smoke-test confirmation, with two Planner-direct R2 closures along the way. **Diagnostic (`Done/diagnostic-dirty-tree-precheck-false-trip-surface-2026-05-28.md`):** mapped the false-trip surface, produced filter-rule table (regex + predicate spec, test surface), and explicitly disposition'd BACKLOG #2 (teardown-stranded worktree cascade) as a downstream symptom of #1 rather than a separate failure mode. **DEV (commit `7bb05ae`):** `_LIFECYCLE_IGNORE_RE` regex at `bellows.py` lines 32-51 + `_is_lifecycle_artifact()` predicate at lines 43-51 + pre-check filter integration at lines 885-916. Shipped via R2 sub-variant Planner-direct close because the executable's own teardown tripped the pre-check it was designed to fix (filter not yet loaded in daemon at dispatch time). **QA (commit `3d79151`, halted Planner-direct on teardown cherry-pick conflict — substance shipped):** 3 filter-positive tests (lifecycle artifacts ignored), 4 filter-negative tests (CRITICAL SAFETY — real dirty conditions still block), 1 regex-coverage test, full-suite regression check (zero new failures). All 7 new tests PASS on main at commit `f8edacc`: 429 passed locally, 4 pre-existing failures only (1x test_run_step_timeout + 3x test_worktree.py — see BACKLOG follow-on). **Smoke-test (commit `d4d7b56`):** first fully clean Bellows dispatch this session (no R2 recovery needed) confirms the filter operational in the live daemon. **BACKLOG closures:** #1 (dirty-tree pre-check false-trips on lifecycle artifacts), #2 (failed teardown strands worktree → cascade — subsumed), and the deferred `_seen.discard` QA tests (deferred indefinitely as the precondition is now met but the regression risk is low). **Four new BACKLOG entries filed from this session:** (1) `_LIFECYCLE_IGNORE_RE` false-strict on space-prefixed porcelain codes (safe direction, trivial fix); (2) pre-check trips on test fixtures' `.bellows-worktrees/` directory (3 pre-existing test_worktree.py failures); (3) dispatch-validator `stop_prose` matches "do not proceed" in instructional prose (smoke-test surfaced); (4) vestigial `mv` claim-rename in plan prose — R3 shadow-cache makes it a no-op. **LESSONS entry:** R2 sub-variant Planner-direct close documented as the working recovery shape for "substance shipped, teardown cherry-pick conflicts on lifecycle artifacts" — pattern recurrence count is now 2 (sessions 13 and 14). **Daemon operations note:** session diagnosed and resolved two duplicate `bellows.py` processes (84501 was niced + session-detached, suspected origin: Ctrl-Z then resume losing controlling terminal, not Ctrl-C). PID 84501 killed; PID 86097 (current foreground daemon) confirmed as sole instance for smoke test. Reference: `Done/diagnostic-dirty-tree-precheck-false-trip-surface-2026-05-28.md`, `halted-executable-dirty-tree-precheck-false-trip-filter-2026-05-28.md` (DEV close), `halted-executable-dirty-tree-precheck-filter-qa-2026-05-28.md` (QA close), `Done/executable-smoke-daemon-connectivity-2026-05-28.md` (smoke confirmation), QA at `knowledge/qa/executable-dirty-tree-precheck-filter-qa-2026-05-28.md`, LESSONS.md 2026-05-28 "R2 sub-variant Planner-direct close".
- 2026-05-28: **Worktree teardown dirty-tree pre-check shipped — new `worktree_teardown_dirty_tree` gate (session 12, first daemon-side teardown-hardening ship).** Closes the structural half of the 2026-05-22 BACKLOG entry (option (b), ~20 LOC pre-cherry-pick dirty-tree check); the operational half (R2 recovery shape) was promoted to LESSONS this session. **Diagnostic (`Done/diagnostic-worktree-teardown-dirty-tree-precheck-v2-2026-05-27.md`, SA):** mapped the `_teardown_worktree` cherry-pick call site (bellows.py:856-870), identified the pre-check insertion point (after index.lock cleanup, before the cherry-pick loop), proposed the evidence-string format, enumerated the test surface, worked 5 edge cases (race window, worktree-vs-main cwd, submodule dirty state, Bellows-self, additive-not-replacing), and refuted the BACKLOG ~20 LOC estimate as production-only (~25 prod + ~40 test actual). Two SA corrections accepted: the gate is NOT a new `_pause_reason` enum (flows through the existing `WorktreeTeardownError` → `gate_failure` path with the gate name embedded in the evidence string — no `run_plan` change, smaller blast radius); and the LESSONS pointer is valid because the R2 entry was promoted before dispatch. Findings at `knowledge/research/worktree-teardown-dirty-tree-precheck-surface-2026-05-27.md`. **Executable (DEV commit `6252f8c`, QA commit `d87caa2`):** +41 prod / +107 test. Pre-check runs `git --no-pager status --porcelain` with `cwd=project_path` (main checkout, NOT worktree) between index.lock cleanup and the cherry-pick loop; dirty predicate `returncode==0 and stdout.strip()`; on dirty, raises `WorktreeTeardownError` with an evidence string leading `worktree_teardown_dirty_tree:` containing the dirty-file listing (10-line truncation), both R2 recovery sub-variants (untracked-artifact, dirty-bookkeeping), and a LESSONS.md 2026-05-27 pointer. **fail-open** correctly implemented with non-obvious exception ordering: `except WorktreeTeardownError: raise` precedes the generic `except Exception: log+proceed`, so the intentional dirty-tree raise isn't swallowed by the fail-open catch. 4 new tests (3 spec'd + a fail-open bonus); teardown suite 9 passed (4 new + 5 existing); adjacent suite 122/122, 0.83s. **Step 2 gate failure + Rule 22 (d) override:** QA substance was complete (8/8 checks, independently re-verified by Planner) but the gate fired `rule_20_self_check` because the Planner-authored QA prompt mandated a non-canonical banner string (`RULE 20 SELF-CHECK: PASSED`) instead of the gate-required `Rule 20 — QA Self-Check Results` / `PASSED — SELF-CHECK PASSED` pair — the gate fired accurately on a Planner-authoring defect; overridden with documented reason. **Three Planner-authoring failures this session** (header field-line position causing dispatch-mode rejection + `-v2-` re-deposit; the QA banner; cf. the prior `### Deposits` shape) consolidated into a single LESSONS entry (2026-05-28) with a copy-from-artifact + local-parse-check mitigation. **Operational note:** the executable plan sat unclaimed ~9 min after `move_file` deposit (watcher missed the move event); CEO-initiated daemon restart's startup scan claimed it. **REMINDER: restart Bellows daemon** to load the new `_teardown_worktree` pre-check symbols (the running daemon executes `6252f8c` code on main but the process predates the symbol load). Three new BACKLOG entries filed: Rule 25 routing for the new gate, pre-deposit plan-lint script, periodic `decisions/` rescan self-heal. Reference: `Done/executable-worktree-teardown-dirty-tree-precheck-2026-05-27.md`, dev commit `6252f8c`, QA at `knowledge/qa/executable-worktree-teardown-dirty-tree-precheck-2026-05-27.md`, LESSONS.md 2026-05-27 (R2 recovery) + 2026-05-28 (convention-string discipline).
- 2026-05-27: **Coordinated daemon-side gate-FP fix — 3 BACKLOG entries closed in one ship (ceo_flags null-declaration, rule_22 (c) row-status enumerative-table FPs, hedging-detector domain-term FPs).** First session to close daemon-side BACKLOG entries since they began accumulating — historical pattern of "filed but not shipped" broken. Single diagnostic + single executable, ~3 hours wall-clock from session-open to all-closed. **Diagnostic (`diagnostic-gate-fp-coordinated-shape-2026-05-27`):** joint call-site characterization in `gates.py` evaluated four abstraction-convergence options (single helper, section-scoping primitive, three independent, mixed shape) against actual code; recommended **5c mixed shape** — CEO Flags independent (different gate, different pipeline, different content shape), Rule 22 (c)+(d) coordinated within `_gate_rule_22_verification` (shared iteration context). Two BACKLOG-framing divergences flagged and accepted: (1) "single shared helper" overturned — root-cause SHAPES similar but MECHANISMS distinct (null blindness vs. table-type blindness vs. cell-scope blindness); (2) Rule 22 (c) BACKLOG option (a) "section-scoping analogous to 2026-05-24" wrong direction — existing section-scoping is the CAUSE (too-coarse `## ` header matching), not absent. Findings at `bellows/knowledge/research/gate-fp-coordinated-shape-2026-05-27.md`. **Executable (`executable-gate-fp-coordinated-shape-2026-05-27`, DEV `7e67b0b`, QA `9f8a0d4`):** three coordinated fixes in one ship. (1) `_is_null_flag_declaration(text)` helper at `gates.py:73`, consumed by `_gate_ceo_flags` at `gates.py:247` via filter comprehension; regex `^(none|n/a|no flags?|nothing|clean|no issues)\b` (case-insensitive) handles prose-null pattern. (2) Defer-and-discard pattern at `gates.py:558-611` for Rule 22 (c) row-status: table-scoped state (`current_table_failures: list`, `current_table_has_positive_row: bool`) accumulates failures during iteration; at table-end transitions deferred failures commit only if at least one positive-status row found; `❌` immediate-fire preserved. (3) `_hedging_in_status_vicinity(line, keyword)` helper at `gates.py:94`, consumed by (d) loop at `gates.py:613-629`; splits row by `|`, identifies status-bearing cells, restricts hedging-keyword match to those cells only. Plus section-scoping extension via `in_verification_section_d` tracking (one-line `continue` skip). 6 new regression tests + 2 existing-fixture updates (`test_rule_22_qa_hedging_keyword`, `test_rule_22_qa_both_fail_and_hedging`). Test suite: 133 passed, 0 failed, 0.17s. Rule 20 self-check PASSED. **DEV-vs-spec divergence (accepted):** DEV chose stricter cell-scope than diagnostic spec (only status-bearing cells, not "adjacent cells") — better serves FP-elimination goal. **Daemon restart confirmed 2026-05-27 20:14** (CEO-initiated). **Operational recoveries this session:** (1) R2 worktree-stranded-findings recovery on diagnostic's teardown failure (cherry-pick conflict on agent's own claim-rename in main's untracked tree; recovery shape: copy findings from worktree to main, commit standalone, leave transient lifecycle artifact uncommitted, `b3b9646`). (2) Stale-worktree cleanup before executable could spin up (`git worktree remove --force` on the diagnostic's orphaned worktree; precondition-failure verdict retry then succeeded — first practical use of the 2026-05-24 precondition-failure field). (3) `### Deposits` vs. `**Deposits:**` Planner-authoring failure caught by `_gate_rule_20_self_check` line-441 evidence string (validates the 2026-05-26 evidence-string disambiguation ship — the failure mode now routes Planner correctly); recovery via R2 in-place plan-file edit (safe per `is_runnable_plan` returning False for `verdict-pending-*` prefix and `on_modified` lifecycle-prefix guard, no re-dispatch trigger fired). Commits this session: bellows `b3b9646` (diagnostic findings), `7e67b0b` (DEV three fixes), `9f8a0d4` (QA verification), `d40afb0` (session-wrap: 3 BACKLOG closures + lifecycle); governance `30bdc83`, `3b7fa89` (submodule pointer bumps). Reference: `Done/diagnostic-gate-fp-coordinated-shape-2026-05-27.md`, `Done/executable-gate-fp-coordinated-shape-2026-05-27.md`, QA at `knowledge/qa/executable-gate-fp-coordinated-shape-2026-05-27.md`.
- 2026-05-27: **Leftover-after-ship tooling halted (term-matching approach insufficient).** Two implementation iterations of `scripts/check_backlog_freshness.py` shipped this session per session-9's tooling-not-rule LESSON recommendation, both halted at Rule 22 (b) substance check. V1 (239 LOC, commit `3c377a2`) used 4-source matching with title-word fingerprints; live run flagged 6/6 currently-Open BACKLOG entries with 39 candidates — title-word matching on generic shared vocabulary (`project`, `status`, `diagnostic`, `teardown`, `files`, `deposits`) produced systemic false positives. V2 (254 LOC, commit `141c0c3`) redesigned for high-distinctiveness term extraction (backtick ≥ 5, hyphenated ≥ 12, underscore ≥ 8 with ≥ 2 underscores, executable slugs) with title-word matching dropped; live run still flagged 6/6 entries with 37 candidates — V2 traded one FP source (BACKLOG Closed title-word noise, ELIMINATED) for another (PROJECT_STATUS slug-token noise from generic 6+ char tokens). Blueprint's FP-validation only re-traced v1-flagged candidates, didn't enumerate NEW candidates v2 would surface. **Root limit:** term-matching can't distinguish same-function-different-bug, shared project names, or slug-token substring collisions without semantic understanding. All 4 ground-truth recurrences (set→list, precondition-failure-field, Phase 3b read-side, mcp\_\_vexp\_\_) still caught in v2 trace, but with no entries-flagged improvement. BACKLOG entry filed retiring original problem (`Closed 2026-05-27 (RETIRE — term-matching approach insufficient)`); manual Phase 1.5 grep remains the working solution. V2 script left on disk as reference; daemon doesn't run it. **Three reproducible session patterns captured in BACKLOG entry:** (1) SA dense-content steps time out at ~600-730s with empty agent output (3 occurrences this session) — successful retry used explicit early-output anchors (claim-confirmation BEFORE reads, 1-line acknowledgment after each file read, 1-line marker at start of each section); (2) Bellows `scope_check` FAILs when script's deposit-file date differs from plan's expected-date (script's report uses `date.today()`, ran on 2026-05-27 vs plan expected 2026-05-26); (3) Blueprint hand-traces must enumerate NEW candidates the algorithm would surface, not just re-trace existing v1-flagged ones. Commits this session: `301185c` (scope diagnostic findings), `d2e07a9` (v1 blueprint), `3c377a2` (v1 script), `550517f` (v2 outline), `0d219a1` (v2 blueprint), `141c0c3` (v2 script edit), `285d82d` (session-wrap: BACKLOG closure + 17 lifecycle files). **Daemon NOT restarted** — no Bellows daemon code changed. Reference: `halted-executable-leftover-after-ship-tooling-2026-05-26.md`, `halted-executable-leftover-after-ship-tooling-implementation-2026-05-26.md`, `halted-executable-freshness-check-algorithm-rework-2026-05-26.md`, `halted-executable-freshness-check-algorithm-v2-blueprint-2026-05-27.md`, `halted-executable-freshness-check-algorithm-v2-implementation-2026-05-27.md`, `Done/executable-freshness-check-algorithm-v2-blueprint-retry-2026-05-27.md`, v2 blueprint at `knowledge/research/freshness-check-algorithm-v2-blueprint-2026-05-27.md`, v2 dev log at `knowledge/development/freshness-check-algorithm-v2-implementation-2026-05-27.md`.
- 2026-05-26: **Test-isolation conftest — `tests/conftest.py` added with autouse fixture redirecting `verdict.VERDICTS_DIR` to tmpdir.** Closes the 2 leaking tests (`test_bellows.py::test_apply_defensive_header_defaults_propagates_to_reparsed_header` and `test_consume_verdicts.py::test_dispatch_starts_fresh_when_db_has_orphan_slug_rows`) that wrote orphan `verdict-request-*` files to production `verdicts/pending/` during test runs. Function-scoped autouse fixture patches `verdict.VERDICTS_DIR` to `tmp_path / "verdicts"` — no production code changes, no test modifications required. Full suite: 411 passed, 5 known carry-overs, 0 regressions. Load-bearing verification: zero orphan files in `verdicts/pending/` after full-suite run. Rule 20 self-check PASSED. Reference: `executable-bellows-test-isolation-conftest-2026-05-26`, SA diagnostic at `knowledge/research/bellows-test-isolation-conftest-fixture-shape-2026-05-26.md`, dev log at `knowledge/development/bellows-test-isolation-conftest-2026-05-26.md`, QA at `knowledge/qa/executable-bellows-test-isolation-conftest-2026-05-26.md`.
- 2026-05-26: **Bellows hardening batch — items 1, 3, 4 (evidence string disambiguation, _seen on_modified invalidation, defensive default re-parse propagation).** Three independent hardening fixes for confirmed-Open BACKLOG items. Item 1: disambiguated the `_gate_rule_20_self_check` evidence string at `gates.py:441` — the no-md-paths branch now emits `"deposits block declares no .md paths..."` instead of the byte-identical string shared with the banner-absent branch at line 464, enabling distinct Planner routing at verdict-read time. Item 3: added `_seen` invalidation with lifecycle-prefix guard to `PlanHandler.on_modified` — corrected re-deposits at the same filename after rejection now trigger re-dispatch instead of being silently skipped; the guard (`in-progress-`, `verdict-pending-`, `halted-`) prevents re-dispatch loops on Bellows's own lifecycle renames. Item 4: added a second `_apply_defensive_header_defaults(header, total_steps)` call after `header = gate_result.get("plan_header", {})` reassignment at `bellows.py:499` — the re-parsed header now inherits the defensive default before being passed to `header_says_pause()` consumers. 4 regression tests added (3 new + 1 existing test fix). Full suite: 411 passed, 5 known carry-overs, 0 regressions. Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load all three fixes. Reference: `executable-bellows-hardening-batch-items-1-3-4-2026-05-26`, dev log at `knowledge/development/bellows-hardening-batch-items-1-3-4-2026-05-26.md`, QA at `knowledge/qa/executable-bellows-hardening-batch-items-1-3-4-2026-05-26.md`.
- 2026-05-26: **Fix F isinstance guard removal + test fixture conformance.** Removed the two defensive `isinstance(f, dict)` guards at `bellows.py:495` and `bellows.py:587` (added during the verdict-ledger-gate-result-preservation plan as contract-pollution defense) after updating the sole non-conformant test fixture in `test_run_plan_inprogress_entry_renames_to_verdict_pending` from string-list `["scope_check"]` to production dict shape `[{"gate": "scope_check", "evidence": "..."}]`. The 2026-05-21 Block 1 / Block 2 symmetric isinstance pattern at lines 509 and 600 is preserved (structurally distinct — `all()` predicates for rule_22 classification, not `.join()` log expressions). All test fixtures now conform to the production `list[dict]` contract. Full suite: 407 passed, 5 pre-existing carry-over failures, 0 regressions. Rule 20 self-check PASSED. Reference: `executable-fix-f-guard-removal-2026-05-26`, dev log at `knowledge/development/fix-f-guard-removal-2026-05-26.md`, QA at `knowledge/qa/executable-fix-f-guard-removal-2026-05-26.md`.
- 2026-05-26: **Verdict ledger gate_result preservation via JSON metadata line (E.4) + terminal log expansion (Fix F).** Commit `fe8f45e`. +11 production LOC across `verdict.py` (+1 line: `**Gate Result JSON:**` metadata field in verdict request f-string) and `bellows.py` (+10 lines: `gate_result_from_request` init, 4-line JSON parse block in `_consume_verdicts` metadata loop, `or`-fallback replacing unconditional empty dict at line 1234, expanded terminal log at two sites with failure gate names and `files_changed` count). +5 new tests covering the write path, all three consume paths (continue-to-done, continue-resume, backward-compat fallback), and expanded log format. Full suite: 407 passed, 5 pre-existing failures, 0 regressions. SA diagnostic at `knowledge/research/verdict-ledger-gate-result-preservation-2026-05-26.md`, dev log at `knowledge/development/verdict-ledger-gate-result-preservation-2026-05-26.md`, QA at `knowledge/qa/executable-verdict-ledger-gate-result-preservation-2026-05-26.md`.
- 2026-05-26: **`_parse_diff_stat` stat column widened to prevent path truncation — one-parameter fix (`--stat` → `--stat=300`).** Changed `git diff --stat` to `git diff --stat=300` at bellows.py:730, preventing git's default ~80-column path truncation that caused false-positive `scope_check` gate trips on two plans this session (the Rule 21 contract-change plan and the verdict-ledger-gate-result-preservation diagnostic). Root cause: truncated paths with `...` prefix were fed to `_gate_scope_check` verbatim, failing the raw-substring text-mention predicate. No change to function signature, return shape, or any other code path. 4/4 targeted tests pass, integration verification confirms full untruncated paths. Rule 20 self-check PASSED. Reference: `executable-parse-diff-stat-truncation-fix-2026-05-26`, dev log at `knowledge/development/parse-diff-stat-truncation-fix-2026-05-26.md`, QA at `knowledge/qa/executable-parse-diff-stat-truncation-fix-2026-05-26.md`.
- 2026-05-26: **PLANNER_TEMPLATE.md bumped to v4.51 with Rule 21 contract-change carve-out.** When a plan changes a function's return type, parameter types, or semantic contract, `targeted` scope justification must consider where the function is TESTED, not just where it lives. Pre-flight `grep -rn "<function_name>" tests/` is now mandatory; if the function appears in >1 test file, scope MUST be `full-suite`. Empirical evidence: 2026-05-25 `_extract_plan_required_deposits` set→list plan declared `targeted` on production-code locality, passed 126/126, and four hours later an unrelated full-suite run surfaced 6 regressions in `tests/test_rule_26_deposit_parser.py` — a test file outside the targeted bucket that tested the changed function's return-type contract. New Lessons row appended. **Plan halted at Step 1 scope_check** (false positive — see follow-on diagnostic), but Edits A/B/C all landed at the governance root before halt; the v4.51 governance change is live. Step 2 QA never ran; verify edits via direct file read at next session start. Reference: `executable-planner-template-rule-21-contract-change-2026-05-26` (in `decisions/` as `halted-*`), governance-root commit `bc0f51e`, dev log at `knowledge/development/planner-template-rule-21-contract-change-2026-05-26.md`.
- 2026-05-26: **scope_check post-fix behavior characterized — DESIGN-INTENT-AUDIT-NEEDED.** SA diagnostic reframed the CEO context: plan-file renames do NOT trip scope_check (architecturally impossible — plan files are untracked, claim rename happens in main repo not worktree, `SCOPE_ALLOWLIST_PREFIXES` already covers `in-progress-*`). The Rule 21 plan's scope_check trip was caused by the **dev log deposit** (`knowledge/development/planner-template-rule-21-contract-change-2026-05-26.md`) failing scope_check's raw-substring text-mention predicate. Three load-bearing findings: (1) the predicate is `fpath in step_text or basename in step_text` — no structural parsing of `**Deposits:**` blocks; (2) lifecycle prefixes already handled (no gap); (3) `knowledge/development/` deposits from every Rule 8 DOC step are a systemic scope_check risk going forward. Four fix-shape options proposed (allowlist `knowledge/`, structurally parse Deposits block, slug-substring exemption, governance-only). Next session: pristine-plan-text follow-up diagnostic to determine path-form gap vs. predicate failure. No source code changes (characterization only). Reference: `diagnostic-scope-check-post-fix-behavior-2026-05-26` (in `Done/`), commit `e940f5d`, findings at `knowledge/research/scope-check-post-fix-behavior-2026-05-26.md`.
- 2026-05-26: **Parallel-SHA teardown population audit complete — CLOSE-SUPERSEDED.** Audit of 34 post-v4.47 plans (21 with code changes) across the 5-day window 2026-05-21 → 2026-05-26 confirmed zero parallel-SHA reproductions. `main` = `origin/main` = `1709c9c` at audit time; zero merge commits, zero cherry-pick commits, zero clusters of content-identical commits with distinct SHAs. Two false-positive candidates investigated and ruled out (the 50-commit QA retry loop on the file-change-audit-fix v2 plan; the qa_steps message-reuse). Disposition: the BACKLOG reference to parallel-SHA teardown divergence was a LESSONS-only pattern, not a tracked BACKLOG entry — no closure edit needed. v4.47 (agent no-push prohibition) closed the root cause empirically. Adjacent BACKLOG entries (teardown push silent failure 2026-05-24, cherry-pick conflicts on shared bookkeeping files 2026-05-22) remain Open as separate issues — they are conflict-driven, not parallel-SHA. Reference: `diagnostic-parallel-sha-population-audit-2026-05-26` (in `Done/`), commit `abc0241`, findings at `knowledge/research/parallel-sha-population-audit-2026-05-26.md`.
- 2026-05-25: **`tests/test_rule_26_deposit_parser.py` updated — 6 set-literal membership assertions wrapped in `set(...)`** to accommodate `_extract_plan_required_deposits` now returning `list` (per 2026-05-25 set→list plan); closes the carry-over regression detected during today's `file_change_audit` fix full-suite run. 9/9 tests PASSED, Rule 20 self-check PASSED. Reference: `executable-test-rule-26-set-to-list-followup-2026-05-25`, QA at `knowledge/qa/executable-test-rule-26-set-to-list-followup-2026-05-25.md`.
- 2026-05-25: **`_capture_git_diff` and `_parse_diff_stat` rewritten to use HEAD SHA + commit-range diff** — new mechanism captures BOTH committed and uncommitted changes since step start (single `git diff --stat <pre_sha> -- .` command); function names and signatures preserved (32 existing mock-patch sites in test suite continue working unchanged); 2 call sites updated to pass `wt_path` instead of `project_path`; 12 obsolete unit tests deleted, 6 new tests added (regression test for committed-change detection, uncommitted-change edge case, SHA capture, empty-SHA short-circuit, `../` path filtering, non-git-repo handling); closes BACKLOG 2026-05-21 `file_change_audit` false-negative AND the cascading silent bypass of `_gate_scope_check` (empty `files_changed` caused scope_check to short-circuit on every code-edit step). Test suite: 407 collected, 396 passed, 11 pre-existing carry-over failures, 0 new regressions. Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load the rewritten diff-capture functions. Reference: `executable-file-change-audit-fix-2026-05-25`, dev log at `knowledge/development/file-change-audit-fix-2026-05-25.md`, QA at `knowledge/qa/executable-file-change-audit-fix-2026-05-25.md`.
- 2026-05-25: **`_extract_plan_required_deposits` and `_filter_transient_paths` converted from `set` to `list`** (preserving authoring order via `re.finditer` walk order + `dict.fromkeys` dedup on the legacy prose-matching path); `md_paths[0]` selection in `_gate_rule_20_self_check` and `_gate_rule_22_verification` is now deterministic; convention: QA report is the first `.md` in the `**Deposits:**` block; 1 new regression test added (`test_extract_plan_required_deposits_preserves_block_order`); stale comments updated at 2 `patch.object` sites in `test_gates.py`. Test suite: 126 collected in test_gates.py, 126 passed, 0 regressions. Rule 20 self-check PASSED. Closes 2026-05-24 BACKLOG `_extract_plan_required_deposits` capability entry. **REMINDER: restart Bellows daemon** to load the updated return types. Reference: `executable-extract-plan-required-deposits-set-to-list-2026-05-25`, dev log at `knowledge/development/extract-plan-required-deposits-set-to-list-2026-05-25.md`, QA at `knowledge/qa/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md`.
- 2026-05-25: **`READ_CLASS_TOOLS` extended with 5 read-class vexp tools** (`get_context_capsule`, `get_session_context`, `get_skeleton`, `index_status`, `search_memory`); `save_observation` deliberately excluded as write-class. Closes 2026-05-22 BACKLOG entry on MCP tool denials. 6 new regression tests (5 positive exemption + 1 critical negative for `save_observation`). Test suite: 412 collected, 407 passed, 5 pre-existing carry-over failures, 0 new regressions. Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load the new exemptions. Reference: `executable-mcp-read-class-tools-extension-2026-05-25`, dev log at `knowledge/development/mcp-read-class-tools-extension-2026-05-25.md`, QA at `knowledge/qa/executable-mcp-read-class-tools-extension-2026-05-25.md`.
- 2026-05-25: **PLANNER_TEMPLATE.md bumped to v4.50 with `qa_steps` header field documented in three places** (header example, definitional paragraph after Execution map, Gate 6 table row). Pairs with this morning's code half (`executable-qa-steps-header-field-code-2026-05-25`) to close the 2026-05-22 BACKLOG entry on `qa_step_detection` leak (10.1% misclassification rate). No daemon restart required — governance-only edit. Lessons Learned row documents the pattern: when shipping mechanization that creates a new authoritative data source, the governance edit obligating the Planner to write the new field must ship in the same or immediate-next plan. QA verified 5/5 deliverables, 2/2 structural compliance checks, Rule 20 self-check PASSED. Reference: `executable-qa-steps-governance-2026-05-25`, QA at `knowledge/qa/executable-qa-steps-governance-2026-05-25.md`.
- 2026-05-25: **`qa_steps` header field added to `_gate_is_qa_step` as authoritative QA-step detection source.** Replaced the naked substring check (`"qa" in match.group(0).lower()`) with a primary path that parses the `qa_steps` plan-header field (comma-separated integers or YAML list), falling back to keyword detection when the field is absent or malformed. Closes the 2026-05-22 BACKLOG entry on `qa_step_detection` leak (10.1% / 14 of 139 QA steps misclassified per the diagnostic population audit). Handles three input forms: string (`"2"`), comma-separated string (`"1,3"`), and YAML list (`[2, 4]`). Malformed values emit `logger.warning` and fall through to keyword fallback. 7 new regression tests including leak vector closure test for "Invoice Security & Testing Analyst" header pattern. Test suite: 406 collected, 401 passed, 5 pre-existing carry-over failures, 0 new regressions. Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load the new `_gate_is_qa_step` behavior. Reference: `executable-qa-steps-header-field-code-2026-05-25`, dev log at `knowledge/development/qa-steps-header-field-2026-05-25.md`, QA at `knowledge/qa/executable-qa-steps-header-field-code-2026-05-25.md`.
- 2026-05-25: **Documentation update to BELLOWS_DEVELOPER.md — bash-fallback pattern for `.claude/settings.local.json` edits.** Added a new paragraph to the "Project-Specific Procedure" section of `agents/BELLOWS_DEVELOPER.md` documenting that the Edit tool is denied on `.claude/settings.local.json` when running in a worktree (path-scope restriction) and instructing agents to use Bash with a `python3 -c` script instead. Includes canonical pattern code example with absolute path. Closes the BACKLOG entry from 2026-05-21 (moved to Closed section as of 2026-05-22). No code changes — documentation-only. QA verified 5/5 deliverables. Rule 20 self-check PASSED. Reference: `executable-settings-local-bash-fallback-doc-2026-05-22`, QA at `knowledge/qa/executable-settings-local-bash-fallback-doc-2026-05-22.md`.
- 2026-05-25: **Gate file-scoping fixes (Shape 6C + 7A) — closes BACKLOG items #6 (rule_22 table false-positive) and #7 (rule_20 wrong file).** Two disjoint gate-function fixes in `gates.py`, both characterized by the 2026-05-24 gate-file-scoping diagnostic at `knowledge/architecture/gate-file-scoping-2026-05-24.md`. Item #6 (Shape 6C) fixes `_gate_rule_22_verification`'s (c) check by scoping table inspection to sections whose `##` header contains "verification" (eliminating false-positives from non-verification tables like failure-classification tables) and broadening status-token acceptance from checkmark-only to include text tokens (PASS, OK, done, complete, verified) via the existing `_is_positive_status_row()` helper. Item #7 (Shape 7A) fixes `_gate_rule_20_self_check` by replacing the iteration over all `.md` deposit paths with a single-file check on `md_paths[0]` (the QA report), eliminating false-positives when a non-QA deposit contains incidental banner text. 5 new regression tests + 4 existing fixture updates. Test suite: 399 collected, 394 passed, 5 pre-existing carry-over failures, 0 new regressions. Rule 20 self-check PASSED. Shape 7B (structural-context banner check) deferred as feature-shaped hardening. **REMINDER: restart Bellows daemon** to load the new gate behaviors. Reference: `executable-gate-file-scoping-2026-05-24`, diagnostic at `knowledge/architecture/gate-file-scoping-2026-05-24.md`, dev log at `knowledge/development/gate-file-scoping-2026-05-24.md`, QA at `knowledge/qa/executable-gate-file-scoping-2026-05-24.md`.
- 2026-05-25: **Precondition-failure verdict-request field — closes BACKLOG item #5 (step-counter loop).** Added an explicit `**Precondition Failure:**` boolean field to the verdict-request format, implementing Shape E from the 2026-05-24 daemon-restart-state-divergence diagnostic (`knowledge/architecture/daemon-restart-state-divergence-2026-05-24.md`, Section D root cause, Section F18 Shape E design). The producer side (`run_plan` worktree-creation failure path at bellows.py:443) writes `precondition_failure=True`; all other pause sites retain the default `False`. The consumer side (`_consume_verdicts` at bellows.py:1160-1248) reads the field and conditionally computes `resume_step`: `step_number` for retry on precondition failure, `step_number + 1` for advance otherwise. This eliminates the step-counter loop where `_consume_verdicts` advanced past a step that never ran due to a precondition gate failure (e.g., worktree creation failure), causing subsequent step re-runs to encounter the parallel-SHA pattern and loop. Historical verdict-request files lacking the field default to `False` via parser initialization, preserving backward compatibility. Four regression tests added (3 consumer-side, 1 producer-side). Test suite: 394 collected, 389 passed, 5 pre-existing carry-over failures, 0 new regressions. Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load the precondition-failure handling. Reference: `executable-precondition-failure-field-2026-05-24`, diagnostic at `knowledge/architecture/daemon-restart-state-divergence-2026-05-24.md`, dev log at `knowledge/development/precondition-failure-field-2026-05-24.md`, QA at `knowledge/qa/executable-precondition-failure-field-2026-05-24.md`.
- 2026-05-25: **Rename-first ordering at all 4 pause sites — closes RV-1 boundary (BACKLOG items #2, #3).** Reordered all four pause sites in `bellows.py` to rename plan files from `in-progress-*` to `verdict-pending-*` BEFORE posting verdict-requests, closing the RV-1 boundary identified in the 2026-05-24 daemon-restart-state-divergence diagnostic (`knowledge/architecture/daemon-restart-state-divergence-2026-05-24.md`, Section E16). The root cause was that `verdict.post_verdict_request()` and the plan rename were non-atomic operations with the rename happening AFTER the verdict-post; a daemon restart between these operations left the verdict-request file in `verdicts/pending/` while the plan remained named `in-progress-*`, preventing `_consume_verdicts` from matching (its predicate requires the `verdict-pending-` prefix). Four sites fixed: (1) worktree-creation failure ~line 437, (2) intermediate-step gate_failure ~line 519, (3) final-step gate_failure ~line 611, (4) auto-close-failure ~line 642. Four regression tests added asserting rename-first ordering at each site. Test suite: 390 collected, 385 passed, 5 pre-existing carry-over failures, 0 new regressions. Rule 20 self-check PASSED. BACKLOG item #5 (step-counter loop) is explicitly out of scope. **REMINDER: restart Bellows daemon** to load the rename-first ordering. Reference: `executable-rename-first-ordering-2026-05-24`, diagnostic at `knowledge/architecture/daemon-restart-state-divergence-2026-05-24.md`, dev log at `knowledge/development/rename-first-ordering-2026-05-24.md`, QA at `knowledge/qa/executable-rename-first-ordering-2026-05-24.md`.
- 2026-05-24: **Removed pre-scan `processed-` prefix rename from `_consume_verdicts` (Shape 1c) — eliminates P0 multi-step pause-cycle loop class.** Removed the pre-scan block at bellows.py:1129-1162 that normalized write-time `processed-verdict-*` files to canonical `verdict-*` form, the associated `_prescan_orphan_logged` module-level set declaration, and all seven pre-scan test functions from `tests/test_consume_verdicts.py` (-38 LOC production, -391 LOC tests). The pre-scan was originally shipped in commits `3c9344f` (2026-05-21) and `d1855ba` (2026-05-22) but the 2026-05-24 diagnostic findings at `knowledge/architecture/processed-prefix-reconsumption-and-rename-skip-2026-05-24.md` (Section B) identified that it causes a P0 infinite loop on multi-step plans that pause for verdict between steps — a worse bug than the one it prevented. PLANNER_TEMPLATE v4.49 Rule 25 now prohibits the `processed-` prefix at write time, making the pre-scan unnecessary. The legitimate post-consumption `processed-` rename sites (bellows.py:1252, 1275) are preserved. This plan shipped after three prior Planner-side prompt errors halted earlier attempts (incomplete test enumeration, `cd bellows` path error, worktree-state assumption error). Test suite: 386 collected, 381 passed, 5 pre-existing failures, 0 regressions. Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load the pre-scan removal. Reference: `executable-remove-pre-scan-processed-rename-v2-2026-05-24`, diagnostic at `knowledge/architecture/processed-prefix-reconsumption-and-rename-skip-2026-05-24.md`, dev log at `knowledge/development/remove-pre-scan-processed-rename-v2-2026-05-24.md`, QA at `knowledge/qa/executable-remove-pre-scan-processed-rename-v2-2026-05-24.md`.
- 2026-05-22: **Bellows pre-scan orphan guard — bellows.py:1131 skips rename when no paired verdict-pending plan exists; eliminates 8,823 daily rename events and 10,827 stale-verdict events; 8 existing orphans migrated to processed-* form.** The 2026-05-22 pre-scan orphan WARN flood diagnostic identified that the pre-scan loop at bellows.py:1131-1139 unconditionally renamed every `processed-verdict-*.md` file to canonical `verdict-*.md` form, regardless of whether a paired `verdict-pending-*` plan still existed in any watched decisions/ directory. This produced ~400 file moves per cycle across 313 stale plan slugs with zero legitimate rename cases observed. The fix adds an orphan-check guard that extracts the plan slug from each `processed-verdict-*` filename and scans all watched `decisions/` directories for an active `verdict-pending-*` match before allowing the rename. Files with no paired plan are left in `processed-*` form with a one-shot INFO log (deduplicated by daemon lifetime via `_prescan_orphan_logged` set at bellows.py:33). A one-shot migration cleaned up 9 existing canonical orphans in `verdicts/resolved/`. 4 regression tests added. **REMINDER: restart Bellows daemon** to load the new guard code. Reference: `executable-pre-scan-orphan-guard-2026-05-22`, diagnostic at `knowledge/research/pre-scan-orphan-warn-flood-2026-05-22.md`, dev log at `knowledge/development/pre-scan-orphan-guard-2026-05-22.md`, QA at `knowledge/qa/executable-pre-scan-orphan-guard-2026-05-22.md`.
- 2026-05-21: **PLANNER_TEMPLATE v4.48 shipped — Rule 25 codification of Planner Rule 22 routing post-verdict-enrichment.** Three additive edits to PLANNER_TEMPLATE.md: (A) new "Mechanized check routing (post-verdict-enrichment)" paragraph appended after the existing Rule 25 "Rule 22 routing on auto-proceed codes" paragraph — codifies that when both `_gate_rule_22_verification` (PASS) and `_gate_rule_20_self_check` (PASS) appear in a verdict request, the Planner performs Rule 22 (b) substance check ONLY, skipping the mechanical checks (a), (c), (d), (e) that Bellows has already verified via the two gates shipped in the 2026-05-21 verdict-enrichment plan; (B) version bump 4.47 to 4.48; (C) new Lessons Learned row documenting the pattern: when shipping mechanization of a Planner-side discipline, the governance text must be updated in the same or next plan to reflect what the Planner stops doing. QA verified 8/8 deliverables, 3/3 structural compliance checks, Rule 20 self-check PASSED. Reference: `executable-planner-template-rule-25-codification-2026-05-21`, dev log at `knowledge/development/planner-template-v4-48-rule-25-codification-2026-05-21.md`, QA at `knowledge/qa/executable-planner-template-rule-25-codification-2026-05-21.md`.
- 2026-05-21: **Bellows tier-1 batch — gitignore config.json + narrow git allowlist + warn on unrecognized pause_for_verdict (3 BACKLOG items closed).** Three independent surgical edits batched into one plan. (A) Appended `config.json` to `.gitignore` — the file contains Pushover `app_key` and `user_key` and was untracked but not gitignored, risking accidental secret commit on `git add -A`. File was never tracked in HEAD so no `git rm --cached` needed. (B) Replaced the overly-broad `Bash(git:*)` auto-approval in `.claude/settings.local.json` with 11 explicit non-destructive git subcommand entries (`git add`, `git commit`, `git status`, `git log`, `git diff`, `git show`, `git ls-files`, `git checkout`, `git branch`, `git config`, `git rev-parse`). Destructive commands (`git push`, `git reset --hard`, `git push --force`, `git rebase`, `git filter-repo`, `git worktree`) now require manual approval — closes the root cause of parallel-SHA divergence from agent-side push. (C) Added WARN log for unrecognized non-empty `pause_for_verdict` values in `header_says_pause` (bellows.py:314-315), guarded by `if pv:` to avoid warning on empty-string default. Test suite: 116 passed, 0 regressions. Rule 20 self-check PASSED. Reference: `executable-bellows-tier-1-batch-2026-05-21`, dev log at `knowledge/development/bellows-tier-1-batch-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-tier-1-batch-2026-05-21.md`.
- 2026-05-21: **Bellows expected-keys warning narrowed to pause_for_verdict only (bellows.py:416-419).** Replaced the 4-line `expected_keys`/`missing_keys` warning block at bellows.py:416-419 with a targeted 2-line `pause_for_verdict` check, implementing Shape C from the 2026-05-21 expected-keys-shape-choice diagnostic (`knowledge/decisions/Done/diagnostic-bellows-expected-keys-shape-choice-2026-05-21.md`, findings at `knowledge/research/bellows-expected-keys-shape-choice-2026-05-21.md`). The old warning fired on any missing key from a 5-key set (project, date, author, total_steps, pause_for_verdict), generating false positives on cosmetic metadata keys that the Planner template does not reliably emit. The new warning fires only when `pause_for_verdict` is absent — the sole safety-critical key that controls intermediate-step pause behavior. The existing sparse-header warning at bellows.py:382-383 (which handles Case 3 via `_apply_defensive_header_defaults`) is preserved unchanged. Test assertion updated in `test_warning_multi_step_plan_without_pause_for_verdict` to reflect that the narrowed warning correctly does NOT fire when the defensive default has already inserted the key (Case 3). Test suite: 116 passed, 0 regressions. Rule 20 self-check PASSED. Authority: 2026-05-21 expected-keys-warning diagnostic and 2026-05-21 expected-keys-shape-choice diagnostic. Reference: `executable-bellows-expected-keys-narrow-2026-05-21`, dev log at `knowledge/development/bellows-expected-keys-narrow-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-expected-keys-narrow-2026-05-21.md`.
- 2026-05-21: **Bellows isinstance asymmetry fix — bellows.py:594 now mirrors Block 1 defensive pattern.** Applied symmetric `isinstance(f, dict)` guard at bellows.py:594 (Block 2) to mirror the existing defensive pattern at line 505 (Block 1). Both blocks were introduced in commit `4bd1c84` (2026-05-21 verdict-enrichment); the asymmetry was identified as an authorial oversight per the 2026-05-21 isinstance-asymmetry diagnostic findings (Section 4, Gap Assessment row (a)). The fix adds `isinstance(f, dict) and` before `f.get("gate")` and changes direct subscript `f["gate"]` to `.get("gate")`, ensuring a graceful `gate_failure` pause instead of a `TypeError` crash on any hypothetical non-dict entry in `gate_result["failures"]`. Purely defensive — no behavioral change on dict inputs. Test suite: 116 passed in test_bellows.py, 0 regressions. Rule 20 self-check PASSED. Reference: `executable-bellows-isinstance-asymmetry-fix-2026-05-21`, dev log at `knowledge/development/bellows-isinstance-asymmetry-fix-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-isinstance-asymmetry-fix-2026-05-21.md`.
- 2026-05-21: **PLANNER_TEMPLATE v4.47 shipped — explicit agent no-push rule + Rule 25 routing-count fix.** Two additive edits to PLANNER_TEMPLATE.md: (1) Rule 8 mirror paragraph and Rule 23(c) now explicitly prohibit agents from running `git push` during step execution — the Planner handles all push activity at session-wrap. This closes the root cause of parallel-SHA divergence identified by the 2026-05-21 teardown-git-operations diagnostic and validated by the verdict-enrichment plan (4 commits, zero divergence). Rule 31 (submodule pointer bump) and Procedure 3 (git filter-repo) push references remain unchanged as Planner-side and operator-side respectively. (2) Rule 25 preamble count corrected from "two" to "three" auto-proceed codes, reflecting the `header_pause` row added by the 2026-05-13 Lessons Forge ratification. New Lessons row documents BACKLOG-entries-written-from-memory pattern (pre-write contradiction scan caught stale "remove" framing before ship). Version bumped to 4.47. QA verified 7/7 deliverables, 3/3 structural compliance checks, Rule 20 self-check PASSED. Reference: `executable-planner-template-no-push-and-routing-count-2026-05-21`, dev log at `knowledge/development/planner-template-v4-47-no-push-and-routing-count-2026-05-21.md`, QA at `knowledge/qa/executable-planner-template-no-push-and-routing-count-2026-05-21.md`.
- 2026-05-21: **Priority 3 carry-over audit closed — BACKLOG +3, NEXT_SESSION Priority 3 section retired.** Five-item carry-over audit from the 2026-05-19 baton completed via SA diagnostic (`diagnostic-priority-3-carryover-audit-v2-2026-05-21`). Dispositions: 1 UNRESOLVABLE (stale-redirect grep audit — no referent found in codebase), 1 STALE (verdict prose directives — superseded by the same session's verdict-enrichment work), 3 LIVE-MINOR promoted to BACKLOG (pause_for_verdict unvalidated enum, Deposits parenthetical qualifier stripping, no-match verdict warning rate-limit dedup). NEXT_SESSION.md Priority 3 section replaced with a one-line closure note. No source code changes — markdown-only. Reference: `Done/executable-priority-3-audit-closeout-2026-05-21.md`, QA at `knowledge/qa/priority-3-audit-closeout-2026-05-21.md`.
- 2026-05-21: **Verdict-request enrichment with Rule 22 mechanical check gate shipped.** Moved Rule 22 (a)+(c)+(d) mechanical checks from Planner into Bellows via new `_gate_rule_22_verification` gate in gates.py (~65 LOC), enriched verdict requests with a Verification Results table (10 gates + 1 informational row) and Planner-Only Checks Remaining section in verdict.py (~55 LOC), and added pause-reason discrimination (`rule_22_check_failed` vs `gate_failure`) in bellows.py. 14 new tests (8 gate + 6 verdict), test suite 386 collected / 385 passed / 0 regressions. QA verified 9/9 deliverables, 4/4 structural compliance checks, Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load `_gate_rule_22_verification` and the verdict-enrichment code — the running daemon executed this plan with pre-edit code; new gate activates on first plan dispatched after restart. Reference: `executable-bellows-verdict-enrichment-2026-05-21`, dev log at `knowledge/development/verdict-enrichment-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-verdict-enrichment-2026-05-21.md`.
- 2026-05-21: **Teardown git operations diagnostic — parallel-SHA divergence mechanism characterized.** Single SA diagnostic (`knowledge/research/teardown-git-operations-mapping-2026-05-21.md`) mapped the complete git operation timeline from agent dispatch through teardown through session-end. Refuted the baton's stated hypothesis (Bellows pushes from worktree) with grep evidence: Bellows codebase contains zero `git push` calls across all 10 Python modules. Identified the actual mechanism as dual-path commit delivery: (a) the dispatched agent runs `git push origin main` from inside the worktree per Planner-authored plan step prose (auto-approved by overly-broad `Bash(git:*)` permission), reaching origin with original SHAs; (b) `_teardown_worktree` (bellows.py:757) cherry-picks the same commits onto local main, producing content-identical commits with different SHAs (different parent chain + different committer date). Six CVBs HIGH-confidence verified. Classification: architectural mismatch, NOT a Bellows code bug — teardown was designed for a world where the agent does not push. Q6 recommendation for Priority 1 verdict-enrichment (229 LOC, 5 agent commits): proceed with operational reconciliation at session end, or remove `git push` from plan step prose to eliminate divergence at source. Concurrency stress test result: two plans dispatched simultaneously (governance executable + bellows diagnostic) interleaved cleanly through Bellows queue with no contention, deadlock, or missed verdict consumption. Daemon's expected-keys check emits a misleading warning for single-line pipe-separated plan headers (`pause_for_verdict` parsed correctly but other keys flagged missing); not a defect but noise worth tightening. Plan: `Done/diagnostic-teardown-git-operations-mapping-2026-05-21.md`.
- 2026-05-27: **Verdict-enrichment surface diagnostic complete — informs next session's executable.** Single SA diagnostic mapping the six-question surface required to ship the verdict-request enrichment roadmap (`/Users/marklehn/Developer/GitHub/roadmap-bellows-verdict-enrichment-2026-05-27.md`). Findings: (Q1) `post_verdict_request` signature at verdict.py:95 already receives `gate_result` — no signature change needed; (Q2) all 4 call sites in bellows.py (lines 439-440, 518, 606, 635-636) already have gate-result data in scope; (Q3) intermediate_decisions list composes cleanly as table row; (Q4) all 9 existing gates use append-on-failure / silent-on-pass pattern, recommended approach (ii) post-hoc inference in verdict.py (~30 LOC helper, zero gate refactoring); (Q5) `_gate_rule_20_self_check` does no structured parse — independent open recommended for new gate (~5 LOC vs 15 LOC shared cache); (Q6) `rule_22_check_failed` Pause Reason Code routed at both bellows.py:504 and 590 via `all(f["gate"] == "rule_22_verification" for f in failures)`. Path-form authoring confirmed: project-prefixed relative remains canonical post-normalization-fix. Net LOC estimate: ~229 across gates.py (~53), verdict.py (~50), bellows.py (~6), test_gates.py (~60), test_verdict.py (~60). Operational note: diagnostic experienced a worktree teardown 'empty cherry-pick' failure; investigation showed the agent's commit DID push directly to origin from inside the worktree (origin SHA `7f0c6f2`), but local main never received the cherry-pick. Local was reset via `git fetch origin && git reset --hard origin/main` after confirming all 4 of today's commits had content-identical parallel SHAs between local and origin. Two LESSONS entries captured. Diagnostic file: `knowledge/research/verdict-enrichment-surface-2026-05-27.md`. Plan: `Done/diagnostic-verdict-enrichment-surface-2026-05-27.md`.
- 2026-05-27: **deposit_exists path-form normalization — closes shop_next_session Thread 3 (deposit_exists normalization fix).** Two-component fix for abs-vs-rel path-form mismatch in `_gate_deposit_exists` (RC3, per diagnostic `deposit-exists-path-form-normalization-2026-05-27.md`). **Component A:** Added `_normalize_deposit_path(path, project_path)` helper (gates.py:216-236) that reduces absolute, project-prefixed, and bare-relative paths to canonical project-relative form. Updated 3 call sites in `_gate_deposit_exists` (lines 295, 303, 310) to normalize before set membership checks. **Component B:** Added absolute-path-to-worktree remap branch in `_resolve_deposit_path` Strategy 0 (gates.py:254-261) — strips `project_path` prefix from absolute paths before joining with `wt_path`, fixing the `os.path.join(wt_path, absolute_path)` bypass. Files changed: `gates.py` (~25 LOC production), `tests/test_gates.py` (+6 tests: `test_normalize_deposit_path_abs_to_rel`, `test_normalize_deposit_path_prefixed_to_rel`, `test_normalize_deposit_path_already_rel`, `test_gate_deposit_exists_cross_form_abs_vs_rel`, `test_gate_deposit_exists_actually_missing`, `test_resolve_deposit_path_absolute_worktree_remap`). Test suite: 372 total (delta +6), 367 passed, 5 pre-existing failures, 0 new failures. Rule 20 self-check PASSED. Commits: `336e4fb` (DEV). **REMINDER: restart Bellows daemon** to load the new gate code. Reference: `executable-deposit-exists-path-form-normalization-2026-05-27`, diagnostic at `knowledge/research/deposit-exists-path-form-normalization-2026-05-27.md`, dev log at `knowledge/development/deposit-exists-path-form-normalization-2026-05-27.md`, QA at `knowledge/qa/deposit-exists-path-form-normalization-2026-05-27.md`.
- 2026-05-27: **Disable Claude Code autoupdater — closes BACKLOG entry `Set DISABLE_AUTOUPDATER=1 in the Bellows daemon environment`.** Added `os.environ.setdefault("DISABLE_AUTOUPDATER", "1")` to both `bellows.py` (line 21) and `runner.py` (line 18) so every `claude -p` subprocess inherits the flag, preventing background upgrades from breaking prompt-cache continuity mid-plan. `setdefault` semantics respect explicit operator overrides. Two new tests in `tests/test_runner.py`: `test_runner_sets_disable_autoupdater_env_var`, `test_runner_respects_explicit_disable_autoupdater_override`. Manual upgrade cadence documented in `CLAUDE.md` (`## Claude Code upgrade cadence (manual)`). Test suite: 366 total (delta +2), 361 passed, 5 pre-existing failures, 0 new failures. Rule 20 self-check PASSED. Commits: `93b74fb` (DEV). **REMINDER: restart Bellows daemon** to load the env-var mutation into the running process. Reference: `executable-disable-autoupdater-2026-05-27`, dev log at `knowledge/development/disable-autoupdater-2026-05-27.md`, QA at `knowledge/qa/disable-autoupdater-2026-05-27.md`.
- 2026-05-20: **Planner Contract Validators V1+V2+V3.** Three new validators (verdict filename format, pause_for_verdict enum, header field types) shipped per diagnostic-planner-authored-contract-validation-2026-05-20. Completes 5/8 → 8/8 of the Planner-authored contract surface identified in the 2026-05-12 architectural audit. ~125 LOC. CEO restart of Bellows daemon required for live effect.
- 2026-05-20: **Bash gate GUARDRAILS exemption shipped — closes shop_backlog `guardrails-vs-bash-gate-contradiction-git-locks`.** Added `GUARDRAILS_BASH_EXEMPTION_PATTERN` compiled regex constant and Bash exemption filter to `_gate_no_permission_denials` in `gates.py`. Bash permission denials matching the GUARDRAILS-prescribed `rm -f .git/index.lock` lock-cleanup pattern are now treated as non-blocking (exempt from `gate_failure`), eliminating three documented false-positive fires (2026-04-17, 2026-05-06, 2026-05-21). Architecturally parallel to the existing `READ_CLASS_TOOLS` exemption. Files changed: `gates.py` (lines 37-48 constant, lines 198-203 filter), `tests/test_gates.py` (+2 tests: `test_no_permission_denials_exempts_guardrails_lock_cleanup`, `test_no_permission_denials_still_blocks_other_bash_denials`). Test suite: 93 in test_gates.py (91 pre-existing + 2 new), 349 full suite (344 passed, 5 pre-existing failures). Commits: `a430b14` (DEV). Reference: `executable-bash-gate-guardrails-exemption-2026-05-20`, diagnostic at `knowledge/research/bash-gate-vs-guardrails-diagnostic-2026-05-20.md`, QA at `knowledge/qa/bash-gate-guardrails-exemption-2026-05-20.md`.
- 2026-05-19: **Dispatch-mode validator shipped — mechanical enforcement layer for PLANNER_TEMPLATE Rule 35.** New `bellows/validators.py` module with `validate_at_claim()` invoked at `bellows.py:362-372` after header parse and before claim move. Three checks per `shop_backlog` entry: (c) missing `**Dispatch Mode:**` field → reject (halts plan to `halted-` prefix); (a) `manual_bootstrap` mode deposited into a Bellows-watched directory → warn with Pushover dedup; (b) STOP-prose regexes (`STOP\.`, `wait for confirmation`, `do not proceed`, case-insensitive) detected in step prose under `dispatch_mode: bellows` → warn with Pushover dedup. Exclusion scoping: backticks, fenced code blocks, `**Deposits:**` blocks. 13 unit tests in `tests/test_validators.py`, all pass. Full suite: 336 passed, 5 pre-existing failures (4 test_decisions.py worktree path, 1 test_run_step_timeout), 0 regressions. Commit `37edd40`. **QA recovery required** — prior QA shipped Rule 20 block as source without executing it; recovery plan `executable-dispatch-mode-validator-qa-recovery-2026-05-19` re-executed the block, captured stdout under `**Output:**` heading, gate passed. **Two LESSONS entries captured at governance root:** (1) `qa-discipline`, `rule-20` — third recurrence in 11 days of QA agents shipping the Rule 20 block in non-executable forms; mitigation proposed at the canonical block file and gate level. (2) `planner-discipline`, `plan-shape` — Planner-authored plan tripped its own validator on a quoted example in Step 1 rationale; disposition accepted (validator working as designed, quotation marks deliberately not in exclusion scoping); pattern-validator plans need authoring-time self-scan against the not-yet-shipped validator. **REMINDER: restart Bellows daemon to load validator code.** Reference: `Done/executable-bellows-dispatch-mode-validator-2026-05-19.md`, `Done/executable-dispatch-mode-validator-qa-recovery-2026-05-19.md`, QA at `knowledge/qa/dispatch-mode-validator-2026-05-19.md`.
- 2026-05-20: **Governance dispatch infrastructure — Plan A complete.** Added `governance/` as the tenth Bellows-watched project. Three governance specialist files authored at `governance/agents/`: DOCUMENTATION_AGENT.md (shop-meta prose authoring), SYSTEMS_ANALYST.md (governance structural architecture), QA_AGENT.md (governance-prose verification and registry consistency). COMPANY.md reconciled to three-list project registry (Active Projects 11 rows, Bellows-watched 10 projects, Shop infrastructure 4 projects) plus Shop-level vs Project-level labeling section. Version bumped to 2.6. All five verifications passed; Rule 20 self-check PASSED. Plan B (governance dispatch first-use — Options 1+3 governance edits) is now unblocked. References: `knowledge/research/governance-watched-project-addition-2026-05-20.md`, `knowledge/qa/governance-dispatch-infrastructure-qa-2026-05-20.md`.
- 2026-05-17: **Hygiene sweep — `auto_close` type-safety + Rule 20 bold-tolerance shipped via QA-recovery flow.** Two Bellows-side fixes landed despite Planner-induced mid-dispatch crash on the first attempt. **F4 (`auto_close` type-safety):** `str()` wrap added at `bellows.py:458` to coerce `header.get("auto_close")` before `.lower()` — tolerates YAML-frontmatter Python bool from `pyyaml.safe_load`. Audit of 5 files (`bellows.py`, `gates.py`, `verdict.py`, `parser.py`, `runner.py`) found 9 other `.lower()`-after-`header.get()` sites, all cleared with documented dispositions (regex match groups, no-lower paths). Two regression tests added (`test_auto_close_yaml_bool_does_not_crash`, `test_auto_close_yaml_bool_false`). Commit `9e79e4d`. **F3 (Rule 20 bold-tolerance):** `_gate_rule_20_self_check` regex at `gates.py:374` extended from `r'^\s*PASSED\s+—\s+SELF-CHECK\s+PASSED'` to `r'^\s*\*{0,2}\s*PASSED\s+—\s+SELF-CHECK\s+PASSED'` — tolerates 0–2 leading asterisks on the banner PASSED line (e.g., `**PASSED — SELF-CHECK PASSED**` from QA agents that bold-wrap the banner). Two regression tests added. 91 gate tests pass. Commit `f130573`. **Incident:** original dispatch crashed all 3 plans (F1+F3+F4) with `AttributeError: 'bool' object has no attribute 'lower'` because Planner authored YAML frontmatter `auto_close: false` in plans that hadn't yet shipped the fix. Recovery: live source patched correctly (agent worktree pushes reached origin), local main reset to `origin/main` after cherry-pick divergence, 3 stranded worktrees torn down, 3 fresh QA-recovery plans authored with bold-Markdown headers (deliberately bypassing the fixed code path), Bellows daemon restarted. All four post-restart dispatches paused cleanly at `auto_close_disabled`, verdicts processed, plans closed to `Done/`. **Bonus:** Bellows restart picked up the leftover Track D `executable-frontmatter-canary-2026-05-20` plan and dispatched it successfully — first real-world canary for the auto_close fix on YAML frontmatter, with `Gate Result Passed: True`. **LESSONS entry shipped:** governance-root LESSONS.md appended with `Plans that fix Bellows bugs must not exercise the very bug they're fixing` (planner-discipline tag), capturing the captured-but-not-internalized failure mode and the bug-aware-plan-format-selection mitigation. References: `Done/executable-auto-close-type-safety-qa-recovery-2026-05-17.md`, `Done/executable-rule-20-bold-tolerance-qa-recovery-2026-05-17.md`, `Done/executable-frontmatter-canary-2026-05-20.md`, QA reports at `knowledge/qa/2026-05-17-*.md`. Halted originals retained as untracked audit evidence: `halted-executable-auto-close-type-safety-2026-05-17.md`, `halted-executable-rule-20-bold-tolerance-2026-05-17.md`.
- 2026-05-19: **Gate 2c — `_staging_*` deposit filter + tolerant Rule 20 banner matching shipped (strikes 4 & 5).** Two false-positive gate fixes: (1) `_filter_transient_paths()` helper filters `_staging_*` basenames from `_extract_plan_required_deposits()` at all three extraction paths — prevents transient atomic-deposit filenames mentioned in step prose from tripping `deposit_exists`. (2) `_gate_rule_20_self_check` banner match replaced with `re.search(re.MULTILINE)` pattern tolerating leading whitespace, decoration lines (`===`), fenced-code indentation, and shell-prompt prefixes. 10 new tests, full suite green (84 in test_gates.py). Commits: `30e395c` (fix), `ae67035` (dev log). QA: 7/7 checks PASS at `knowledge/qa/gate-2c-qa-2026-05-19.md`. **REMINDER: restart Bellows daemon to load fix.** Reference: `executable-gate-2c-bellows-gate-fixes-2026-05-19`.
- 2026-05-15: **Post-relocation restart canary passed end-to-end; Q2/Fix-b empirically closed.** Bellows daemon restarted after yesterday's hardcoded-path fixes (`planner.py:13` GOVERNANCE_ROOT, `verdict.py:60-75` path-strip blocks). Boot clean (8 watched projects loaded, no FileNotFoundError on PLANNER_TEMPLATE.md, no path-strip warnings). Single-step canary `executable-post-relocation-restart-canary-2026-05-15` deposited via atomic staging pattern; full lifecycle verified: detected → RUNNING → started → all 8 gates passed (`passed=True, failures=0`) → header_pause → CEO verdict → continue-to-done → worktree torn down. Agent's captured `cwd` = `/Users/marklehn/Developer/GitHub/bellows/.bellows-worktrees/post-relocation-restart-canary-2026-05-15`, decisively confirming worktree creation works in the new submodule arrangement — closes NEXT_SESSION's Q2/Fix-b ("monorepo-worktree-at-governance-root" reconsideration after submodule conversion). Two minor cosmetic flags surfaced for later attention: (a) boot banner reported stale module SHAs (`4e0a755` for planner.py instead of `66927a9`); code on disk is correct, only the display lags. (b) `⚠ could not detect main branch, falling back to 'main'` warning emitted during worktree creation — falls back correctly, but root cause unknown. Commits: bellows `611ac4a` (canary lifecycle: plan to Done/, agent deposit, processed-verdict), governance root `2938ad9` (submodule pointer bump). Reference: `Done/executable-post-relocation-restart-canary-2026-05-15.md`, deposit at `knowledge/documentation/post-relocation-restart-canary-2026-05-15.md`.
- 2026-05-14: **Governance root recovered from iCloud `dataless` eviction; bellows recovered as side-effect.** Session opened on a recovery handoff describing bellows `.git` corruption. Four read-only diagnostics established the actual failure mode: 1,061 of 4,879 loose objects (21.7%) in the governance root carried the macOS `dataless` file flag — iCloud had evicted their data while they lived under `~/Desktop/GitHub/`, and the data was never re-materialized after the 2026-05-14 relocation to `~/Developer/GitHub/`. Bellows has no `.git` of its own (it's a tracked subdirectory inside the governance root), so the governance root's health is bellows's health. All 8 independent project repos confirmed clean. Recovery via clone-and-swap from `https://github.com/EverestMons/eluvian-governance.git`: cloned bare to `/tmp/`, swapped into place, damaged `.git` preserved as `.git-DAMAGED-2026-05-14`. Post-swap verification: `fsck --full --strict` exit 0 (was exit 128 mmap-timeout), 0 dataless files (was 1,061), 4,950 reachable objects fully traversable, `.git` size 4.0M packed (was 25M unpacked loose). New commit `0cdf52f` on governance root: re-points `bellows/config.json` `watched_projects` paths from `Desktop/GitHub` to `Developer/GitHub`. **NOT PUSHED** — push deferred to next session. **12 unpushed local commits' history was lost** in the swap; CEO explicitly accepted (file content survives in working tree). 53 working-tree changes remain uncommitted for next-session review. Diagnostics: `Done/diagnostic-9-repo-fsck-triage-2026-05-14.md`, `Done/diagnostic-governance-root-refsck-2026-05-14.md`, `Done/diagnostic-governance-root-connectivity-probe-2026-05-14.md`, `Done/diagnostic-dataless-flag-sweep-2026-05-14.md`. Executable: `Done/executable-governance-root-git-swap-2026-05-14.md`. Receipts: `invoice-pulse/knowledge/research/governance-root-git-swap-receipt-2026-05-14.md`, `governance-root-recovery-verification-2026-05-14.md`. **REMINDER: Bellows daemon needs manual restart** to pick up the re-pointed config (paths now match the new GitHub root location).
- 2026-05-13: **Runner retry-on-transient-failure shipped (Lessons Forge proposal 4).** Added one-shot retry to `run_step` in `runner.py` for transient `claude -p` failures. When the subprocess exits non-zero and stderr contains known transient indicators (401/Unauthorized/authentication/429/rate limit/too many requests), retries once after a 5-second delay using a private `_retry_attempted` kwarg guard. No retry on other error paths (timeout, spawn failure, no_result_event, parse_error). Two regression tests added: `test_run_step_retries_on_transient_401`, `test_run_step_does_not_retry_on_non_transient_error`. Test suite: 308 passed, 1 pre-existing failure (`test_run_step_timeout`), 0 regressions. Rule 20 self-check PASSED. Commit: `36693a5`. **REMINDER: restart Bellows daemon** to load retry logic. Reference: `executable-runner-retry-transient-failure-2026-05-13`, QA at `knowledge/qa/runner-retry-transient-failure-qa-2026-05-13.md`.
- 2026-05-13: **PLANNER_TEMPLATE v4.38 → v4.39 — Source D plan-write-time re-read discipline.** Implements Lessons Forge accepted proposal #1. New paragraph appended to Phase 1.5 Source D (line 81) mandating that the Planner re-read the 5 most recent `planner-discipline`-tagged LESSONS entries before authoring any plan involving housekeeping operations (PROJECT_STATUS edits, verdict deposits, Done/ moves, governance edits, commit orderings). Addresses the captured-but-not-internalized failure mode where session-start lesson reads faded under task-specific cognitive load — third reproduction of the `pending/` vs `resolved/` verdict-directory error within three days was the direct evidence. New Lessons Learned table row at line 1232. No code changes — governance-only edit. Governance-root commit: `2afaf8d`. Rule 20 self-check PASSED. Reference: `executable-plan-write-time-lessons-reread-2026-05-13`, QA at `knowledge/qa/plan-write-time-lessons-reread-qa-2026-05-13.md`.
- 2026-05-12: **Verdict content schema validator shipped.** Transforms the silent-skip failure mode in `check_verdict()` (verdict.py:188-219) into a loud failure: when a verdict file exists, is non-empty, but the first line doesn't match the expected `continue`/`stop` pattern, emits a WARN log to stderr with the actual first line and expected format, and posts a Pushover notification deduped per (filename, failure-type) for the daemon lifetime via `_notify_malformed_verdict()`. Observability: `VERDICT_PARSE_LOG_VERBOSE` constant gates DEBUG-level per-scan outcome logging (`not_found`, `empty`, `malformed`, `parsed_<verdict>`). Return contract preserved — always returns `{"found": False}` for malformed files. Pushover failures swallowed via try/except to never break verdict checking. 7 new tests added to `tests/test_verdict.py`. Test suite: 306 passed, 1 pre-existing failure (`test_run_step_timeout`). Rule 20 self-check PASSED. Commit: `db57921`. **REMINDER: restart Bellows daemon to load fix.** Reference: `parallel-1-executable-verdict-content-validator-2026-05-12`, QA at `knowledge/qa/verdict-content-validator-qa-2026-05-12.md`.
- 2026-05-12: **Verdict directory validator for misplaced verdict files.** New `_scan_misplaced_verdicts()` method on `Bellows` class scans `verdicts/pending/` each tick for verdict response files (`verdict-*.md` but NOT `verdict-request-*.md`) that belong in `verdicts/resolved/`. Logs WARN with full filepath and expected location; sends Pushover notification deduped per `(filename, "misplaced_directory")` for daemon lifetime. Integrated into `_consume_verdicts()` before the resolved-directory consumption loop. Module-level `_NOTIFIED_MISPLACED` dedup set and `MISPLACED_VERDICT_SCAN_VERBOSE` debug toggle. 7 new tests in `tests/test_misplaced_verdicts.py`. Test suite: 305 passed, 2 failed (pre-existing `test_run_step_timeout` + environment-specific `test_server_respond` port collision). Rule 20 self-check PASSED. **Commit:** `30964f7`. **REMINDER: restart Bellows daemon** to load directory validator. Reference: `parallel-1-executable-verdict-directory-validator-2026-05-12`, QA at `knowledge/qa/verdict-directory-validator-qa-2026-05-12.md`.
- 2026-05-12: **Detector NDJSON shape fix + fixture rewrite + module fingerprint observability.** Fixed `decisions.py::extract_decision_blocks()` NDJSON parse path from `event.get("content", [])` to `event.get("message", {}).get("content", [])` to match real Claude CLI wrapped event shape. Rewrote 4 test fixtures in `test_decisions.py` to use the production-accurate `"message": {"content": [...]}` wrapping. Added `decisions.py` as 6th module in `bellows.py::_module_fingerprints()` for startup banner and heartbeat observability. QA behavioral verification: detector correctly found 6 phrase-matched decision blocks in production step.json (`logs/20260512-103456-step.json`) and 0 in canary negative control (`logs/20260512-184339-step.json`). Test suite: 292 passed, 1 pre-existing failure (`test_run_step_timeout`). Rule 20 self-check PASSED. Commits: `13571fe` (Step 1 code fix), Step 2 QA commit pending. Reference: `executable-detector-shape-fix-2026-05-12`, QA at `knowledge/qa/detector-shape-fix-qa-2026-05-12.md`.
- 2026-05-12: **Intermediate decision detector + Vexp exemption shipped.** New `decisions.py` module with phrase-grep detector (`load_phrases()` + `extract_decision_blocks()`) surfaces intermediate agent decisions in verdict requests via new `## Intermediate Decisions Detected` section. 33-phrase list at governance root (`INTERMEDIATE_DECISION_PHRASES.md`). Runner integration at `runner.py:257` feeds NDJSON stream through detector. Vexp code-exploration tool `mcp__vexp__run_pipeline` added to `READ_CLASS_TOOLS` exemption in `gates.py` to close gate-exemption gap from 2026-05-12 design diagnostic. QA verified: 6/6 S-class ground-truth blocks matched, 0/5 T-class false positives, verdict rendering 7/7 checks, Vexp exemption 2/2 checks. Test suite: 292 passed, 1 pre-existing failure (`test_run_step_timeout`). Commits: `08fa9e8` (detector), `5fac495` (Vexp exemption). Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load new detector code. Reference: `executable-intermediate-decision-detector-2026-05-12`, QA at `knowledge/qa/intermediate-decision-detector-qa-2026-05-12.md`.
- 2026-05-12: **BACKLOG closure — bellows-self exposure closed as won't-fix.** SA disposition diagnostic at `bellows/knowledge/architecture/bellows-self-exposure-disposition-2026-05-12.md` audited 7-day window (2026-05-06 → 2026-05-12, 47 ledger entries + all resolved verdicts) and found zero scope_check failures of any class vs. 4 bellows-self reproductions in the prior 5-day window. Procedural mitigations are fully effective. Option (b) governance-root-worktree fix: revised to 50–70 production LOC / 80–120 with tests, 9 call sites, negative ROI against zero observed harm. Options (c) and commit-message slug scoping ruled out (privilege requirements / structurally inapplicable). Closed as won't-fix with revisit trigger documented. No code change. Open BACKLOG count: 1 → 0. Reference: `Done/diagnostic-bellows-self-exposure-disposition-2026-05-12.md`, `knowledge/architecture/bellows-self-exposure-disposition-2026-05-12.md`, `Done/executable-bellows-self-exposure-wontfix-close-2026-05-12.md`.
- 2026-05-12: **BACKLOG hygiene close — 2026-04-19 plan-fixing-bug-X entry moved to Closed (partial supersession by v4.38).** Hygiene-only close per CEO Option B 2026-05-12: PLANNER_TEMPLATE v4.38 (governance-root commit `4e54c02`, 2026-05-11) covers the parser-fix subset of the pattern via the Restart Discipline paragraph (line 882) and Lessons row (line 1227); the general close-path pattern remains procedurally documented in the original BACKLOG entry's mitigation text. No code changes. No daemon restart required. Open BACKLOG count: 2 → 1. **Commit:** `57620b0` (BACKLOG + dev log together). Reference: `Done/executable-backlog-2026-04-19-plan-fixing-bug-x-hygiene-close-2026-05-12.md`, QA at `knowledge/qa/backlog-2026-04-19-plan-fixing-bug-x-hygiene-close-qa-2026-05-12.md`.
- 2026-05-12 (session wrap): **2 BACKLOG closures, 9 plans shipped across PATH-001 closure + terminal/notification redesign.** Plans shipped: 9 (4 diagnostics + 1 design + 4 executables). All in Done/. BACKLOG closures: 2 — PATH-001 Rule 20 self-check (hygiene close, structurally fixed by 2026-05-10 single-source migration) and terminal output redesign + notification audit (shipped via 2 implementation plans following design diagnostic). Code commits: `d742f88` (PATH-001 hygiene close), `b11ecc4` (Plan 1 — terminal output + capture), `07a87ad` (Plan 2 — notification coalescing). Test suite: 268/269 (unchanged; 1 pre-existing `test_run_step_timeout` failure). Code changes: ~323 LOC across `bellows.py`, `runner.py`, `notifier.py`, config files, test file. Open backlog: 2 items remain (both accepted-constraint / documented-pattern entries). Daemon restarted twice during session (after Plan 1, after Plan 2). Reference: `Done/diagnostic-path-001-rule-20-staleness-audit-2026-05-11.md`, `Done/diagnostic-path-001-hygiene-close-edit-surface-2026-05-11.md`, `Done/executable-path-001-hygiene-close-2026-05-11.md`, `Done/diagnostic-terminal-and-notification-surface-audit-2026-05-11.md`, `Done/diagnostic-terminal-notification-capture-design-2026-05-11.md`, `Done/executable-terminal-capture-2026-05-12.md`, `Done/executable-notification-coalescing-2026-05-12.md`, `Done/diagnostic-terminal-notification-backlog-close-edit-surface-2026-05-12.md`, `Done/executable-terminal-notification-backlog-close-2026-05-12.md`.
- 2026-05-11: **Daemon code-version observability shipped.** New `_module_fingerprints()` helper in `bellows.py` returns a dict mapping five core modules (`bellows.py`, `gates.py`, `verdict.py`, `parser.py`, `runner.py`) to their fingerprint string (`git:<short-sha>` when available, `mtime:<timestamp>` fallback, `"unknown"` on error). Fingerprints surfaced at daemon startup (in the banner block after the existing `Source:` line) and every 10th heartbeat tick (~10 minutes at 60s cadence) via `MODULE_FINGERPRINT_HEARTBEAT_INTERVAL = 10` module-level constant. Helper never raises — per-module try/except ensures a single broken module never crashes startup or heartbeat. Three regression tests added: `test_module_fingerprints_returns_all_five_modules`, `test_module_fingerprints_handles_git_failure`, `test_module_fingerprints_fallback_to_unknown_on_unexpected_error`. **Test suite:** 104 passed in test_bellows.py (delta +3 new tests), 0 failures. **Commits:** `6411054` (fix), `fd0efd7` (tests), `bef1947` (dev log). **BACKLOG closure:** closes the `2026-05-11: daemon code-version observability` entry. **REMINDER: restart Bellows daemon** to load the new observability code. Reference: `executable-daemon-version-observability-2026-05-11`, QA at `knowledge/qa/daemon-version-observability-qa-2026-05-11.md`.
- 2026-05-11: **PLANNER_TEMPLATE v4.38 shipped — parser self-trip warning + session-wrap hygiene.** Four anchored edits to `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`: (1) version bump 4.37 → 4.38, (2) new paragraph in Restart Discipline subsection documenting the structural inevitability of gate failures when Bellows-side fix-plans modify plan-text parsers (`_extract_step_text`, `_gate_is_qa_step`, `extract_total_steps`, `_extract_step_text_from_plan`, `strip_fenced_code_blocks`) — the running daemon executes pre-fix code through the fix-plan's lifecycle, so the QA-step gate trip is expected and the CEO override is correct, (3) fourth bullet in Session Wrap subsection codifying `git add -A` commit pattern for accumulated Bellows lifecycle artifacts (plan-file renames, verdict-request consumption, Done/ moves) that are shutil.move operations with no associated git commit, (4) two Lessons Learned rows: Row A documenting the parser self-trip pattern with both 2026-05-11 reproductions (commits `4d57fd3` and `0fab609`), Row B documenting the session-wrap hygiene gap (cleanup commit `8cadade` captured 213 accumulated files). **No code changes.** Governance-root commit `4e54c02`, dev log commit `f83e5be`. QA verified 7/7 deliverables, Rule 20 self-check PASSED. Reference: `executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11`, QA at `knowledge/qa/planner-template-parser-self-trip-and-session-wrap-qa-2026-05-11.md`.
- 2026-05-11: **`PlanHandler._seen` retry cache refactored to slug-keyed ownership on `Bellows`.** Moved `_seen` from `PlanHandler.__init__` to `Bellows.__init__`; re-keyed all 8 access sites from path to slug via `verdict.slug_from_path()`; added `bellows=None` parameter to `run_plan` and passed `bellows=self` from `_run_tracked`; added `discard()` calls at 3 lifecycle terminal events (auto-close line 529, continue-to-done line 1030, halt line 1051). Plans that reach Done/ or halted/ now clear their slug from `_seen`, enabling re-dispatch on retry workflows without a daemon restart. Dispatch-window guard (race-condition protection during 2-second stagger) preserved intact — `_seen.add()` calls remain in `PlanHandler._handle` at all original sites. No new threading primitives; GIL atomicity on `set.add`/`set.discard` is sufficient. **LOC delta:** production +16/−12 (bellows.py), tests +139/−4 (test_bellows.py). **Test suite:** 101 passed, 0 failed (delta +4 new tests). New tests: `test_seen_uses_slug_not_path`, `test_seen_dispatch_window_guard_holds`, `test_seen_cleared_on_continue_to_done`, `test_seen_cleared_on_halt`. **Commits:** `63dd6ed` (fix), `130649b` (tests), `e889186` (dev log). **BACKLOG closure:** closes the `2026-05-06: PlanHandler._seen retry cache` entry. **REMINDER: restart Bellows daemon** to load the refactored code. Reference: `executable-plan-handler-seen-slug-refactor-2026-05-11`, diagnostic at `knowledge/research/plan-handler-seen-retry-cache-2026-05-11.md`, QA at `knowledge/qa/plan-handler-seen-slug-refactor-qa-2026-05-11.md`.
- 2026-05-11: **Line-start anchor fix for `## STEP N` parsers shipped.** Complementary to the fence-strip fix (commit `4d57fd3`), this fix addresses the INLINE-code variant of the parser-vs-structure bug class. Diagnostic at `knowledge/research/rule-20-gate-false-positive-root-cause-2026-05-11.md` (Q5) identified 3 parsers using `## STEP N` regex without `^` line-start anchor: `_extract_step_text` (gates.py:260), `_extract_step_text_from_plan` (verdict.py:40), `_gate_is_qa_step` (gates.py:353). Without the anchor, `re.search` matched inline single-backtick `` `## STEP N` `` references in plan prose before the real header, causing `_gate_rule_20_self_check` false positives. Fix: added `^` anchor to all 3 patterns (both main pattern and lookahead where applicable) + `re.MULTILINE` flag. Aligns with the companion `extract_total_steps` at bellows.py:109-111 which already used `^` + `re.MULTILINE` correctly. **LOC delta:** production 0 net (6 lines modified in-place across 2 files), tests +47 (3 new tests). **Test suite:** 92 passed in targeted files (test_gates.py + test_verdict.py), delta +3 new tests, 0 regressions. Behavioral regression REPL confirmed all 3 parsers correctly skip inline references. Combined fenced+inline REPL confirmed both fix classes coexist correctly. Rule 20 self-check PASSED. Commit `0fab609`. **REMINDER: restart Bellows daemon** to load the new parser code. Reference: `executable-step-header-line-anchor-2026-05-11`, diagnostic at `knowledge/research/rule-20-gate-false-positive-root-cause-2026-05-11.md`, QA at `knowledge/qa/step-header-line-anchor-qa-2026-05-11.md`.
- 2026-05-11: **Fence-strip plan-text parsers fix shipped.** Diagnostic at `bellows/knowledge/research/extract-total-steps-fence-class-audit-2026-05-11.md` confirmed 4 BUG-CONFIRMED parsers (`extract_total_steps`, `_extract_step_text`, `_gate_is_qa_step`, `_extract_step_text_from_plan`) matched `## STEP N` patterns inside fenced code blocks, causing spurious step overcounts, wrong step-text extraction, QA-step misclassification, and corrupted verdict-request deposit paths. Fix: new `strip_fenced_code_blocks()` utility (duplicated in `bellows.py`, `gates.py`, `verdict.py` for circular-import avoidance) applied at entry of all 4 parsers. Regex `re.sub(r"^```[^\n]*\n.*?^```[^\n]*$", "", text, flags=re.MULTILINE | re.DOTALL)` strips triple-backtick fenced blocks before header parsing. 5 new tests added (2 in `test_bellows.py`, 2 in `test_gates.py`, 1 in `test_verdict.py`). **LOC delta:** production +34, tests +97, net +131. **Test suite:** 186 passed across targeted files (delta +5 new tests), 0 regressions. REPL behavioral regression checks confirmed all 4 parsers return correct values post-fix. Rule 20 self-check PASSED. Commit `4d57fd3`. Closes the BACKLOG entry from 2026-05-10 (`extract_total_steps` overcount from in-fence headers). **REMINDER: restart Bellows daemon** to load the new parser code. Reference: `executable-fence-strip-plan-text-parsers-2026-05-11`, diagnostic at `knowledge/research/extract-total-steps-fence-class-audit-2026-05-11.md`, QA at `knowledge/qa/fence-strip-plan-text-parsers-qa-2026-05-11.md`.
- 2026-05-11 (session wrap): **`deposit_exists` Cause 5 closure, PLANNER_TEMPLATE v4.37, BACKLOG hygiene.** Session arc: revisited the open BACKLOG #1 `deposit_exists` false-positive item with a fresh population audit → found Cause 1 (worktree path resolution) already closed 2026-05-06, identified a new Cause 5 (plan-agent evidence path convention mismatch) responsible for 18 gate-failure lines across 3 verdict requests on 2026-05-07/08 → verified governance fix shape via a tiny canary diagnostic → shipped governance edit → closed BACKLOG hygienically. Two diagnostics shipped: (1) `diagnostic-deposit-exists-false-positive-audit-2026-05-11` enumerated 14 reproductions across 8 verdict requests, classified each by candidate cause, identified Cause 5 as the dominant unresolved class (findings at `bellows/knowledge/research/deposit-exists-false-positive-audit-2026-05-11.md`); (2) `diagnostic-rule-26-directory-bullet-canary-2026-05-11` verified the current gate (`gates.py:183-212`) accepts directory-only `**Deposits:**` bullets via `os.path.isfile() or os.path.isdir()` at every strategy, confirmed the 2026-04-19 Lessons entry was made stale by commit `e609ad3` (findings at `knowledge/research/rule-26-directory-bullet-canary-2026-05-11.md`). One governance executable: `executable-rule-26-evidence-path-fix-2026-05-11` shipped PLANNER_TEMPLATE v4.37 via governance-root commit `75904fd`: Rule 26 evidence-file guidance tightened from permissive ("do NOT need individual bullets") to mandatory ("MUST be represented by the evidence directory as a single bullet; Do NOT list individual evidence files"); three Lessons rows appended (stale-lesson retraction of 2026-04-19 directory-prohibition, Cause 5 capture with meta-lesson on mandatory governance language, diagnostic-before-executable discipline win on the canary catch). One BACKLOG hygiene executable: `executable-backlog-hygiene-cause-5-and-daemon-logging-2026-05-11` closed Cause 5 RC2 (citing audit + Rule 26 v4.37 + commit `75904fd`) and opened a new low-priority Open entry for daemon code-version observability gap (no mechanism to detect code-vs-runtime drift; fix shape proposed: startup log + heartbeat surfacing mtime/SHA of core modules). One LESSONS.md append at governance root capturing the plan-drift meta-lesson: agent adapted byte-exact anchors on initiative when PLANNER_TEMPLATE.md advanced v4.34 → v4.36 between deposit and dispatch; adaptations succeeded but the pattern is risky; four mitigation options enumerated, deferred to next observation before designing the fix. **Code changes:** none (pure governance + BACKLOG hygiene). **Test suite:** no delta (no code changes this session). **Commits:** governance-root `75904fd` (PLANNER_TEMPLATE v4.37) + LESSONS.md append (uncommitted); bellows `60c56e9` (BACKLOG.md) + `2a80b3c` (BACKLOG dev log) + QA-step bellows commits (BACKLOG QA report + evidence directory). **Discipline observations:** (a) Pre-executable scan-for-contradictions caught the stale 2026-04-19 Lesson before the governance edit shipped — followed the 2026-04-20 diagnostic-before-executable pattern; (b) Hypothesis-from-memory caught at BACKLOG hygiene step — nearly authored a Cause 5 "Open" entry before reading the BACKLOG file revealed RC2 was already there since 2026-05-10, one `read_text_file` saved an unnecessary entry; (c) Agent-adapts-on-drift pattern observed — captured in LESSONS.md, no Planner-side mitigation yet shipped. **Open BACKLOG items** (post-session): 9 items — 7 carried forward + 2 new (daemon code-version observability gap, multi-line bold header `extract_total_steps` overcount carried from 2026-05-10). **No daemon restart required** — no Python code changes this session.
- 2026-05-10 (late evening): **BACKLOG `2026-05-10: Multi-line bold plan headers bypass _parse_plan_header Strategy 2` closed same-day.** Diagnostic + executable shipped end-to-end. Diagnostic at `bellows/knowledge/research/header-parser-multiline-bold-gap-2026-05-10.md` empirically confirmed root cause via REPL fixtures, traced call chain through `bellows.py:247-350`, audited 30-day population (3 affected plans all in Done/), surfaced Shape (g) defensive default that the Planner had not considered. CEO selected combination (a)+(e)+(g) per chat. Executable shipped via `executable-header-parser-multiline-fix-2026-05-10` at commit `491aab9`: (a) `gates.py::_parse_plan_header` Strategy 2 now collects all consecutive bold-Markdown lines and joins with ` | `, (g) extracted `_apply_defensive_header_defaults()` helper that sets `pause_for_verdict = after_step_1` for sparse multi-step headers, (e.2) replaced single-purpose `pause_for_verdict` warning with richer missing-keys observability message. **LOC delta:** production +46, tests +113, net +159. **Test suite:** 253 passed (delta +7), 1 pre-existing failure (`test_run_step_timeout`) unchanged, 0 new failures. QA verified 13/13 properties including end-to-end test (re-parsing the earlier-today incident plan now correctly returns `pause_for_verdict=after_step_1`). Rule 20 self-check PASSED using the new single-source canonical pattern from `RULE_20_SELF_CHECK_BLOCK.md`. **Dispatch anomaly captured as separate BACKLOG entry:** Bellows ran 4 dispatches instead of 2 because `extract_total_steps()` counted `## STEP N` patterns inside test-fixture string literals in plan prose. Cosmetic only; real Step 1 and Step 2 deposits both shipped correctly. Same shape as today's Rule 20 banner substitution (parser sees literal-looking pattern in prose and treats it as structural). Recommended fix: strip fenced code blocks from plan_text before counting. **REMINDER: restart Bellows daemon** to load the new parser code (running daemon is still on pre-fix code; cosmetic only since the new behavior is strict superset of old). Reference: `bellows/knowledge/decisions/Done/executable-header-parser-multiline-fix-2026-05-10.md`, diagnostic at `Done/diagnostic-header-parser-multiline-bold-2026-05-10.md`, findings at `knowledge/research/header-parser-multiline-bold-gap-2026-05-10.md`, QA at `knowledge/qa/header-parser-multiline-fix-qa-2026-05-10.md`.

- 2026-05-10 (late evening): **BACKLOG `2026-05-10: Rule 20 self-check block sourcing` closed same-day via single-source migration.** Diagnostic + executable shipped end-to-end in one session. Diagnostic at `bellows/knowledge/research/rule-20-block-sourcing-migration-surface-2026-05-10.md` mapped: canonical block was 60+ Python LOC in PLANNER_TEMPLATE.md Rule 20; bellows QA agent file is `BELLOWS_QA.md` (not `qa.md`); invoice-pulse QA agent file is `INVOICE_SECURITY_TESTING_ANALYST.md` with zero prior Rule 20 references; 24 plans across 2 projects had Rule 20 references in last 7 days with ~30% non-canonical banner drift rate. CEO selected migration shape (d.2): single-source template file at governance root. Executable shipped via `executable-rule-20-single-source-2026-05-10`. New file `RULE_20_SELF_CHECK_BLOCK.md` (122 LOC) created at `/Users/marklehn/Desktop/GitHub/` as the ONLY location publishing the canonical Python block — enforces single-source invariant by design. PLANNER_TEMPLATE.md Rule 20 rewritten (−51 LOC net) to reference the canonical file; version bumped 4.35 → 4.36; Lessons Learned entry added documenting migration and ~30% drift rate. `bellows/agents/BELLOWS_QA.md` and `invoice-pulse/agents/INVOICE_SECURITY_TESTING_ANALYST.md` updated with reference paragraphs (no inlining). **Three commits across three repos:** governance `a109e47`, bellows `b05dc42`, invoice-pulse `02702201`. **Structural success criterion met:** QA Step 2 was the first plan to use the new pattern — ran the canonical block from `RULE_20_SELF_CHECK_BLOCK.md` using only plan prompt + canonical file reference, no inlined block, gate-passed cleanly with `Rule 20 — QA Self-Check Results` banner. QA verified 14/14 properties at `knowledge/qa/rule-20-single-source-qa-2026-05-10.md` including byte-exact banner survival, no-inline-block invariant across all 4 files, test suite unchanged (246 passed, 1 pre-existing failure). The Planner-side substitution failure mode is eliminated by design — the Planner never authors the block, so paraphrasing is impossible. Header parse worked correctly this session (single-line pipe-format header on plan; multi-line bold header bug from earlier captured as separate open BACKLOG item). **No daemon restart required** — changes are governance-layer documents, no Python code affected. Reference: `bellows/knowledge/decisions/Done/executable-rule-20-single-source-2026-05-10.md`, diagnostic at `Done/diagnostic-rule-20-block-sourcing-2026-05-10.md`.

- 2026-05-10 (late evening): **BACKLOG `2026-05-01: test_startup_sweep_removes_done_plan_orphans inline-replicated sweep logic` closed.** Shipped via `executable-startup-sweep-extract-2026-05-10` (DEV commit `783eea7`, Variant (i) per CEO). New `_perform_startup_sweep(self) -> list[str]` extracted onto `Bellows` class, placed between `_consume_verdicts` and `start`; `start()` calls the method and retains the existing print loop. Test `test_startup_sweep_removes_done_plan_orphans` migrated to exercise production code directly via `b._perform_startup_sweep()` — stale NOTE comment removed, both existing assertions preserved. LOC delta: bellows.py +13, test_consume_verdicts.py −27, net −14. Test suite: 246 passed, 1 pre-existing failure (`test_run_step_timeout`), 0 new failures. QA verified 13/13 properties at `bellows/knowledge/qa/startup-sweep-extract-qa-2026-05-10.md`. **Step 2 gate failure (`rule_20_self_check`) overridden via continue verdict after Rule 22 verification** — Planner authoring substitution failure mode: my plan substituted non-canonical banner `"RULE 20 SELF-CHECK"` for canonical PLANNER_TEMPLATE banner `"Rule 20 — QA Self-Check Results"`. Gate did its job; substantive verification (13/13) was complete on first deposit. **Two new BACKLOG items added from this session:** (1) multi-line bold plan headers bypass `_parse_plan_header` Strategy 2 — every plan I've authored with the multi-line bold header style silently auto-advances through `pause_for_verdict` declarations because the parser reads only the first non-empty bold line; (2) Rule 20 self-check block in plans should reference canonical PLANNER_TEMPLATE rather than be re-authored — best fix shape is migrating the canonical block into `bellows/agents/qa.md` so QA agents own it. **No daemon restart required for this fix** — the refactor is module-level Python; running daemon is on pre-refactor code, but the production sweep behavior is byte-identical (no behavior change) so the running daemon continues to work correctly. Restart at next convenient time to load the cleaner code. Reference: `bellows/knowledge/decisions/Done/executable-startup-sweep-extract-2026-05-10.md`, dev log at `knowledge/development/startup-sweep-extract-2026-05-10.md`, QA at `knowledge/qa/startup-sweep-extract-qa-2026-05-10.md`, verdict at `bellows/verdicts/resolved/verdict-startup-sweep-extract-2026-05-10-step-2.md`.
- 2026-05-10 (evening session wrap): **Session wrap — 4 backlog closures, 1 SA verification diagnostic.** Plans shipped: 1 SA verification diagnostic (`diagnostic-teardown-cherry-pick-audit-2026-05-10` — overturned cherry-pick fragility BACKLOG hypothesis; confirmed `_teardown_worktree` has been multi-SHA from day 1; Failure 1 stale lock already shipped at commit `8eac4c3`). All in Done/. BACKLOG closures: 4 — (1) initial hygiene sweep — 17 strikethrough entries relocated to Closed (commit `8ac259f`), (2) 2026-05-07 `_teardown_worktree` cherry-pick fragility — superseded via SA diagnostic (commit `5b3f830`), (3) 2026-05-09 S3 Bug C halted-* stale check — stale entry, fix already shipped same-day (commit `80d18ea`), (4) 2026-05-06 stranded plan/verdict files — surgical close (commits `1d45adc` + invoice-pulse `17639d72`). Test suite: no delta (no code changes this session). Code changes: none. Operational: cross-repo pushes succeeded after pull-rebase on invoice-pulse (Windows-side auto-commits required stash+rebase+pop). Open backlog: 7 items remain (PlanHandler._seen retry cache, session-wrap hygiene gap rewritten, bellows-self parallel exposure accepted constraint, startup_sweep test refactor, terminal output redesign, plan-fixing-bug-X pattern, PATH-001 Rule 20 governance pass).
- 2026-05-10 (session wrap): **Session wrap — 6 backlog closures + PLANNER_TEMPLATE v4.35 + config tightening.** Plans shipped: 10 (4 diagnostics, 5 executables, 1 session-wrap canary). All in Done/. Backlog closures: 6 — (1) 2026-05-07 deposit_exists/rule_20_self_check RC1 code fix + RC2 governance deferral, (2) 2026-05-07 cherry-pick fragility (stale lock + single-SHA), (3) 2026-05-08 plan filename not flipped (superseded — upstream bugs fixed 2026-05-08), (4) 2026-05-08 S3 retry loop Done/ target (superseded — Done/ stale-check present since 2026-04-24), (5) 2026-05-09 S3 Bug C halted-* stale check, (6) 2026-05-06 inactivity timeout (config-only, threshold mismatch not code defect). Test suite: 242 → 246 passing (+4 new regression tests). Pre-existing `test_run_step_timeout` failure unchanged throughout. Code changes: gates.py ~5 LOC (rule_20_self_check wt_path threading), bellows.py ~30 LOC (stale-lock detection + orphan cleanup + halted-* stale check), config.json 1 line (step_inactivity_timeout_seconds 1800 → 600), PLANNER_TEMPLATE.md 4 anchored edits (v4.34 → v4.35, Phase 1.5 Source D). Governance: PLANNER_TEMPLATE v4.35 Phase 1.5 Source D mandates LESSONS.md reading. Operational: Bellows daemon restarted twice (load S3 Bug C fix; load config change). Discipline: 4 consecutive QA reports with verbatim Rule 20 template passed gate without override. Open backlog: 6 items remain (PlanHandler._seen retry cache, bellows-self parallel exposure accepted constraint, terminal output redesign, plan-fixing-bug-X pattern, PATH-001 Rule 20 governance pass, test_startup_sweep inline logic).
- 2026-05-10 (session wrap): **BACKLOG `2026-05-07 deposit_exists / rule_20_self_check gate path-resolution gap` Root Cause 1 closed.** Diagnostic-then-executable session. Diagnostic at `bellows/knowledge/research/gate-path-resolution-post-teardown-2026-05-10.md` overturned the BACKLOG hypothesis — gates.check() runs BEFORE _teardown_worktree (bellows.py:333 vs 362), not after. Two real root causes identified: **RC1 (HIGH confidence)** — `_gate_rule_20_self_check` (gates.py:273) was missed by the 2026-05-06 worktree-aware fix; the function did not accept or thread `wt_path` to `_resolve_deposit_path`, so Strategy 0 (worktree-first) was never attempted on rule_20_self_check evaluation. **RC2 (MEDIUM confidence)** — legacy prose regex in `_extract_plan_required_deposits` captures unresolvable abbreviated paths from plan prose (e.g., `evidence/foo.txt` when actual location is `knowledge/qa/evidence/<slug>/foo.txt`); no resolution strategy can map short→long. RC1 fix shipped via `executable-rule-20-self-check-wt-path-2026-05-10`: 3 surgical edits to `gates.py` (signature line 273, internal `_resolve_deposit_path` call line 292, caller in `gates.check()` line 105) + 1 regression test `test_rule_20_self_check_resolves_via_worktree_path` in `tests/test_gates.py:638`. Test count: 62/62 gates suite passing (delta +1 new test, 0 regressions). Full suite: 242 passed, 1 pre-existing `test_run_step_timeout` failure unchanged. Behavioral spot-check confirmed end-to-end: file in worktree + empty project_path + `wt_path` passed → zero `rule_20_self_check` failures. RC2 deferred to governance enforcement (Rule 26 `**Deposits:**` blocks already required; the action-queue-aggregation plan that exhibited it predates strict enforcement). Cross-reference: BACKLOG `2026-05-07 _teardown_worktree cherry-pick fragility` is structurally distinct — that produces `worktree_teardown` gate failures, not `deposit_exists`/`rule_20_self_check`. **REMINDER: restart Bellows daemon to load fix into running daemon.** Reference: `bellows/knowledge/decisions/Done/diagnostic-gate-path-resolution-post-teardown-2026-05-10.md`, `Done/executable-rule-20-self-check-wt-path-2026-05-10.md`, dev log at `knowledge/development/rule-20-self-check-wt-path-2026-05-10.md`, QA at `knowledge/qa/rule-20-self-check-wt-path-qa-2026-05-10.md`.
- 2026-05-09 (session wrap): **S3 verdict-resolved retry loop session complete.** Bugs A and B fixed (commits `dc0bdd7`, `5136326`); post-restart canary auto-processed 13 of 14 historical bare-format stranded files (one residual is Bug C, captured to BACKLOG). Re-QA correction shipped for Rule 20 banner format via `executable-s3-fix-qa-rule-20-banner-redeposit-2026-05-09`. Three plans closed to `Done/`.
- 2026-05-09: **S3 verdict-resolved retry loop fix shipped.** Two-bug fix per `executable-s3-verdict-resolved-retry-loop-fix-2026-05-09`: (A) `check_verdict()` regex at `verdict.py:157` extended from `r"^verdict:\s*(continue|stop)$"` to `r"^(?:verdict:\s*)?(continue|stop)$"` — accepts both spec-compliant `verdict: continue` and bare `continue`/`stop` formats, unblocking 14 stranded bare-format verdict files. (B) `bellows.py:883` prefix exclusion `if fname.startswith("verdict-request-"):` added to `_consume_verdicts` file listing — prevents 2 misnamed `verdict-request-*` files from entering the verdict consumption pipeline with corrupted slugs. +5 tests (4 in `test_verdict.py`, 1 in `test_consume_verdicts.py`). Test count: 241 passing (up from 236), 1 pre-existing failure (`test_run_step_timeout`). Commits: `dc0bdd7` (fix), `5136326` (feedback). Canary pending CEO restart. QA: `knowledge/qa/s3-verdict-fix-qa-2026-05-09.md`, Rule 20 self-check PASSED.
- 2026-05-08 (session wrap): **Bellows reliability triple-ship — three executable plans + two diagnostics.** (1) `executable-bellows-qa-prefix-and-skip-logging-2026-05-08` — added `qa-` to dispatch regex whitelist at `bellows.py:709` + advisory log line for files that fail prefix filter (with `_seen` dedup and roadmap exemption). Fixes silent-skip of QA-only Planner plans. (2) `executable-step2-auto-advance-fix-2026-05-08` — PLANNER_TEMPLATE updated to emit `pause_for_verdict: after_step_1` in standard pipe-format header (Fix A) + advisory warning when multi-step plan dispatches without `pause_for_verdict` header (Fix B). (3) `executable-pipe-header-parser-and-comprehensive-qa-2026-05-08` — extended `_parse_plan_header()` in `gates.py` to parse pipe-separated bold-Markdown headers, restoring functional integrity to Fix A. Comprehensive QA verified all three changes work together end-to-end. **Test count: 236 passing (up from 223 baseline), 1 pre-existing failure (`test_run_step_timeout`).** Two diagnostics shipped: `diagnostic-plan-pickup-failure-2026-05-08` (root-caused `qa-` prefix not in dispatch regex) and `diagnostic-step2-auto-advance-2026-05-08` (root-caused header-based pause decision; only 4 of 631 historical multi-step plans declared `pause_for_verdict`). **Cross-repo change:** PLANNER_TEMPLATE.md (governance root at `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`) was updated to teach `pause_for_verdict: after_step_1` by default — committed in bellows but landing at governance root. Three new BACKLOG entries added (verdict-resolved retry loop, plan filename not flipped on pause, deposit_exists absolute path re-affirmation). Reference Done/ plan files: `executable-bellows-qa-prefix-and-skip-logging-2026-05-08.md`, `executable-step2-auto-advance-fix-2026-05-08.md`, `executable-pipe-header-parser-and-comprehensive-qa-2026-05-08.md`.
- 2026-05-06 (cross-project, Planner-direct): **BACKLOG hygiene wrap from invoice-pulse session.** Single Planner-direct edit to `bellows/knowledge/BACKLOG.md` (commit `d9e3cf1`) capturing a symptom-only observation: `deposit_exists` gate reports plan-required deposits as missing when files exist on disk. Three reproductions in one session (2026-05-06) on `diagnostic-backlog-hygiene-audit-2026-05-06` Step 1 and `executable-backlog-hygiene-edits-2026-05-06` Step 2 (twice — file deposit + directory deposit). Distinct from BACKLOG #11 (closed 2026-04-30, directory paths via `os.path.isfile()`-only check), `_seen` cache, and prose-embedded path false positives (closed 2026-04-28 + 2026-05-05). Error format: `plan-required deposit missing (not declared by agent): <path>`. Both plans had Rule 26-compliant `**Deposits:**` blocks declaring the missing paths; deposits existed and read cleanly via direct tool call seconds later. Four candidate root causes listed (timing race / path resolution drift / stale snapshot / agent-receipt parser regression), all unverified. Operational impact: each occurrence requires CEO/Planner Rule 22 override. Next action: focused diagnostic when pattern recurs OR 4th reproduction lands. **No bellows code changes this session.** No daemon restart required.
- 2026-05-05 (session-wrap, Planner-direct): **Hot threads session — Failure 2, OP-001, Failure 3 all resolved.** Five diagnostics + two executables + one governance bump shipped. **Failure 2 (Rule 20 fabrication)** — two-layer fix per `executable-rule-20-self-check-gate-2026-05-05` (gates.py) and `executable-rule-22e-rule-20-tightening-2026-05-05` (PLANNER_TEMPLATE v4.34); diagnostic findings in `invoice-pulse/knowledge/research/rule-20-fabrication-detection-findings-2026-05-05.md`; gate addition surface mapped in `knowledge/research/rule-20-gate-addition-surface-findings-2026-05-05.md`. **OP-001 (worktree teardown)** — discovered already resolved 2026-05-03/04; current-state diagnostic at `knowledge/research/worktree-teardown-current-state-findings-2026-05-05.md`. CEO memory updated to reflect resolved status. **Failure 3 (move-to-Done vs gate ordering)** — fully resolved across both modes: Mode A via population audit (`knowledge/research/failure-3-population-audit-findings-2026-05-05.md`, 0 violations since v4.30 hardening), Mode B via static-analysis diagnostic (`knowledge/research/failure-3-mode-b-static-analysis-findings-2026-05-05.md`, structurally impossible — every Done/ path flows through `gates.check()` first). Both BACKLOG entries marked resolved in invoice-pulse BACKLOG. One new lower-priority BACKLOG entry: possible Mode A reproduction during this session (plan landed in Done/ via unexplained path; investigation deferred unless recurs). **Test state at session end:** 205 pass / 1 pre-existing failure (`test_run_step_timeout`), 0 regressions — baseline captured by the rule-20-self-check-gate QA suite. Per Rule 21 cumulative-ledger guidance, no separate session-end run since no production code shipped after that QA. **No Bellows daemon restart required** — the only production code change (rule-20 self-check gate) shipped via Bellows-self plan, which means the running daemon dispatched it but loaded the new code via subsequent rescan; CEO can verify via canary if desired.
- 2026-05-05 — PLANNER_TEMPLATE v4.34: Rule 22 (e) tightened to require literal Rule 20 banner string and PASSED line in QA reports. Pairs with Bellows-side `_gate_rule_20_self_check` (also shipped 2026-05-05) for two-layer detection of fabrication and omission.
- 2026-05-05: **Rule 20 self-check verification gate added to gates.py.** Catches QA fabrication (banner without PASSED) and omission (no banner) on QA steps. `_resolve_deposit_path` refactored to return path-or-None for new gate's file-read access. 46 test_gates.py tests pass (38 existing + 8 new). Full suite: 1 pre-existing failure (`test_run_step_timeout`) → same 1 failure post-change (0 regressions). Reference: `executable-rule-20-self-check-gate-2026-05-05`.
- 2026-05-05 (session close): **Bellows reliability — deposit-parser agent-receipt fix shipped end-to-end.** Session arc: stability assessment for invoice-pulse work (verdict: stable enough; sequential single-plan dispatches recommended initially) → diagnostic of deposit-parser prose-failure (overturned BACKLOG hypothesis — root cause was agent-receipt parser at `gates.py:157`, NOT plan-text parser as BACKLOG entry assumed) → 1-LOC regex extraction fix + 4 unit tests + BACKLOG Open→Closed amendment → daemon restart loaded new code (Source SHA `06aa938`, 11 orphaned verdict requests cleaned in startup sweep). Memory drift identified: `userMemories` "On the horizon" still cites OP-001 worktree teardown (closed 2026-05-03/04) and "deposit_exists newly-created-files false positive (new, 2026-05-03)" (same root cause class as today's fix, also closed). To clean at next session start. Two BACKLOG items remain priority for next session: PATH-001 Rule 20 self-check (governance-layer) and pre-existing invoice-pulse Rule 20 fabrication fix.
- 2026-05-05: **Deposit-parser agent-receipt false-positive fix shipped + BACKLOG closure.** Diagnostic at `bellows/knowledge/research/deposit-parser-prose-failure-diagnosis-2026-05-05.md` traced the `deposit_exists` false-positive at `gates.py:157` to a `.strip("`")` call that only stripped boundary backticks, mangling agent-receipt paths formatted with the standard Output Receipt template. Fix replaced the line with regex extraction (`re.match` against a backtick-quoted span) plus fallback to legacy strip behavior for lines without backticks. +4 unit tests in `test_gates.py`. Full pytest suite passed; behavioral smoke confirmed the fix end-to-end. BACKLOG entry `2026-05-05: deposit-parser captures backtick-wrapped paths from step prose` closed in the same plan. REMINDER: restart Bellows daemon to load fix. Reference: `executable-deposit-parser-agent-receipt-fix-2026-05-05`.
- 2026-05-05: **Session wrap deposits shipped.** PROJECT_STATUS.md header bumped to 2026-05-05, NEXT_SESSION.md fully refreshed with three carry-forward priority options (BACKLOG #2 test refactor, KNOWLEDGE_INDEX.md creation, deposit-parser false-positive fix), new BACKLOG Open entry added for deposit-parser false-positive (backtick-wrapped prose paths captured as literal deposit paths). QA verified all 4 deliverables, Rule 20 self-check PASSED. Reference: `executable-session-wrap-2026-05-05`.
- 2026-05-05: **BACKLOG #1 closed via population audit; bellows-self constraint opened as known-tradeoff entry; PLANNER_TEMPLATE bumped to v4.33.** BACKLOG entry `2026-04-30: scope_check diff-collision from concurrent activity` closed after population audit at `knowledge/research/backlog-1-reproduction-audit-2026-05-05.md` confirmed zero post-fix reproductions on real-`.git` projects. Audit window 2026-04-30 through 2026-05-05: 7 scope_check failures total — 3 pre-fix (before commit `36b2bba` daemon restart), 4 bellows-self exposure (worktree intentionally skipped per detect-and-skip commit `06aa938`), 0 real-`.git` post-fix gap. New known-constraint Open entry captures the bellows-self parallel/concurrent activity exposure vector with mitigation guidance. PLANNER_TEMPLATE v4.33 adds two Lessons Learned entries: (1) verify shipped state before designing follow-up — hypothesis-from-memory vs state-from-code pattern; (2) population audits as a cheap, high-leverage diagnostic technique for recurring failures with shipped fixes. Reference: `executable-backlog-1-close-and-lessons-2026-05-05`.
- 2026-05-04: **BACKLOG hygiene: scope_check diff-collision entry broadened (external-vector reproduction).** BACKLOG entry for scope_check diff-collision broadened to capture the external/CEO vector reproduced today during executable-close-monorepo-worktree-backlog-2026-05-04 Step 1. Same structural class as parallel-sibling collision — working-tree-shared file-state observation cannot isolate per-step changes from concurrent activity in the same project subtree. Fix-shape candidates unchanged. References: executable-backlog-addendum-scope-check-external-vector-2026-05-04.md.
- 2026-05-04: **BACKLOG hygiene: monorepo-worktree-at-governance-root closed.** BACKLOG entry for the monorepo-worktree fix moved from Open to Closed. Fix shipped earlier 2026-05-04 via executable-monorepo-worktree-fix-2026-05-04 (Option A: detect-and-skip in _create_worktree); canary validated. Entry was stale at time of session-open scan. References: executable-close-monorepo-worktree-backlog-2026-05-04.md.
- 2026-05-04: **Monorepo worktree fix shipped (Option A: detect-and-skip).** When `project_path` has no project-local `.git` (bellows-self today, living inside governance-root), `_create_worktree` now returns `project_path` directly — agent runs in-place without worktree isolation. `_teardown_worktree` detects this via `wt_path == project_path` sentinel and becomes a no-op (zero subprocess calls). Files changed: `bellows.py` (+9 lines: detection at L528, no-op at L562), `tests/test_bellows.py` (+51 lines: 3 new tests). Tests: 193 passed / 1 pre-existing failure (`test_run_step_timeout`). 2026-05-03 type-fix (commit `0f2059f`) preserved intact at lines 340, 405, 433 — remains the safety net for cherry-pick failures on other projects. Cross-project regression confirmed: projects with `.git` proceed through normal worktree path unchanged. Commit `06aa938`. BACKLOG entry `2026-05-04: monorepo-worktree-at-governance-root` captures full context (commit `4c056db`). REMINDER: restart Bellows daemon to load fix.
- 2026-05-03 (final): **Worktree teardown type-mismatch fix shipped + verified + canary-dispatched.** Diagnostic at `knowledge/research/worktree-teardown-bug-diagnosis-2026-05-03.md` (deposited via forge-hosted plan since bellows-self dispatch was unsafe pre-fix; Planner-recovered the stranded plan via verdict-continue ceremony after the diagnostic itself silently wedged Bellows mid-teardown — bug bit during diagnosis, recovered via Rule 25 terminal-step path). Diagnostic surfaced four type-contract-violation sites in `bellows.py` (3 enumerated by agent + 1 caught by Planner Rule 22 verification): line 286 in `_create_worktree` failure handler (`worktree_creation_failed`) + lines 340/405/433 in `_teardown_worktree` paths (`worktree_teardown_failed`). All four appended plain strings to `gate_result["failures"]`, but `verdict.py::post_verdict_request` iterates as dicts (`f['gate']`, `f['evidence']`) — mismatch crashed teardown silently with `string indices must be integers`. Fix at commit `272fbe4` (`/Users/marklehn/Desktop/GitHub/bellows/`): all 4 sites changed to `{"gate": "worktree_creation"|"worktree_teardown", "evidence": str(e)}` dict format. New regression test `test_post_verdict_request_handles_worktree_teardown_failure_dict_format` in `tests/test_verdict.py` exercises the contract end-to-end. Pre-existing test `test_run_plan_pauses_on_cherry_pick_conflict` in `tests/test_bellows.py` updated to match new dict format (5th deliverable not anticipated by Planner — surfaced by DEV agent during fix). 87 targeted tests pass. QA Step 2 verified all 6 deliverables, Rule 20 self-check PASSED, evidence at `knowledge/qa/evidence/worktree-teardown-type-mismatch-fix-2026-05-03/` (commits `0f2059f` for QA report). **This wrap plan is the canary** — first bellows-self dispatch post-fix; if it reaches Done/ cleanly, the fix is verified end-to-end in production. **Open downstream items:** (1) monorepo-worktree-at-governance-root structural fix (Q2/Fix-b from diagnosis) — bellows still creates worktrees from governance-root's `.git` because bellows has no own `.git`; type fix prevents the crash but the governance-root-worktree behavior persists (~20-30 LOC structural fix or repo-split decision needed); (2) `origin/HEAD` setup on 3 watched projects without it (Fix-c) — one-line CEO action `git remote set-head origin --auto`; (3) ~14 stranded `verdict-*.md` files in `bellows/verdicts/resolved/` from prior sessions awaiting cleanup audit. **Lessons captured at `/Users/marklehn/Desktop/GitHub/LESSONS.md`:** (a) diagnostic site enumeration must include test-suite grep, not just production code; (b) plans modifying daemon code must include explicit "restart daemon" step; (c) "commit landed" QA checks should cite SHA from dev log, not HEAD-anchor; (d) stranded resolved-verdicts accumulation pattern; (e) silent-wedge pattern when teardown crashes — exception doesn't propagate to notification path. Reference: fix plan at `bellows/knowledge/research/fix-plan-worktree-teardown-type-mismatch-2026-05-03.md` (status header marks SHIPPED).
- 2026-05-03 (later): **First worktree teardown crash discovered + recovered.** Session goal was to close the BACKLOG `2026-05-03: multi-step plan step-count regression` entry (already fixed by earlier `9786e87` corrective fix; entry was stale). Authored `executable-close-2026-05-03-step-count-regression-2026-05-03` as a markdown-only BACKLOG-close plan with DEV → QA structure per Position A. Plan dispatched cleanly; agent completed all five phases successfully INSIDE the worktree (BACKLOG.md edits applied correctly, dev log written, commit `44ab370` landed). Then `_teardown_worktree` raised `string indices must be integers` after `⚠ could not detect main branch...falling back to 'main'`. Plan stranded at `in-progress-`, worktree stranded at `bellows/.bellows-worktrees/<slug>/`, no verdict request posted, no ledger entry. **First real-plan exposure of the worktree code shipped earlier today (commit `36b2bba`).** Two structural issues identified: (1) literal Python error in `_teardown_worktree` (subprocess result indexed as dict instead of attribute, or status-porcelain line treated as dict); (2) worktree was created at governance-root level (`.git` pointer goes to `/Users/marklehn/Desktop/GitHub/.git/worktrees/<slug>`, not bellows-local) — same monorepo issue BACKLOG #4 addressed for `_capture_git_diff`. Bellows is the only watched project without its own `.git`, so the bug is likely bellows-self-only. **Manual recovery completed:** committed Planner direct edits to BACKLOG.md (worktree-teardown-crash entry added at top of Open) and `agent-prompt-feedback.md` (new Patterns Identified section + OP-001 pattern); cherry-picked agent's worktree commit `44ab370` onto main checkout (became `b20e2e6`); removed stale Open entry that the cherry-pick didn't catch (commit `f7eae84`); moved plan to `Done/`; `git worktree remove --force` cleaned the stranded worktree; cleared `.bellows-cache/` shadow files. **Operational state:** Bellows daemon STOPPED. OP-001 pattern in feedback log forbids dispatching bellows-self plans until teardown is fixed. Memory entry 18 captures the constraint. **Next session:** focused diagnostic of `_teardown_worktree` authored as a non-bellows-project plan (forge or invoice-pulse) — reads `bellows.py::_teardown_worktree` source, identifies the literal `string indices must be integers` source via Python REPL test against today's stranded-then-removed worktree analog, and characterizes the monorepo-vs-standalone-repo divergence. After diagnosis, fix executable (also non-bellows) ships the teardown fix and Bellows restarts. Reference: BACKLOG.md 2026-05-03 worktree-teardown-crash entry; agent dev log preserved in conversation history.
- 2026-05-03: Plan 2 of BACKLOG #1 (parallel-plan scope_check collision) shipped — worktree test suite. 6 new unit tests in test_bellows.py exercise run_plan's worktree call points (creation before pre-diff, wt_path passed to capture and runner, teardown after final gate, strict-pause on creation failure, pause on cherry-pick conflict, startup prune in __init__). 7 new integration tests in test_worktree.py exercise REAL git operations (worktree creates with tracked files, isolation from main checkout dirty files, teardown removes directory, cherry-pick on teardown, copy-back uncommitted files, abort-on-conflict leaves worktree alive, retry-once-on-creation-failure). Full suite passes — previous baseline + 13 new tests, with 1 pre-existing unrelated failure. BACKLOG #1 implementation + tests now both shipped. NEXT: post-restart canary diagnostic to verify behavioral isolation in live dispatch (separate plan to author).
- 2026-05-03: Plan 1 of BACKLOG #1 (parallel-plan scope_check collision) shipped — per-plan git worktree implementation. Two new helpers in bellows.py: `_create_worktree(project_path, slug)` (with retry-once-then-strict-pause-via-gate_failure-verdict on creation failure) and `_teardown_worktree(project_path, wt_path, slug)` (cherry-pick + uncommitted file copy-back + abort-on-conflict). 6 cwd swaps in `run_plan` route capture and runner calls through worktree path. Startup `git worktree prune` hook added to `Bellows.__init__` for stale-registration cleanup after crashes. `.bellows-worktrees/` added to `bellows/.gitignore`. Targeted test regression matches baseline. Plan 2 (tests) staged at `_staging-executable-bellows-worktree-tests-2026-05-03.md` for post-restart dispatch. REMINDER: restart Bellows daemon to load worktree code; behavioral verification ships via Plan 2 + post-restart canary.
- 2026-05-03: Multi-step diagnostic parser undercount fixed via corrective narrow override. Full session arc: (1) BACKLOG #1 (parallel-plan scope_check diff-collision) investigated via three sequential diagnostics — implementation surface, candidate designs (D1-D8), cost-coverage recommendation — all closed cleanly via Rule 22. CEO locked in worktree-based fix design: in-tree `<project>/.bellows-worktrees/<slug>/`, always-worktree, detached HEAD with cherry-pick, copy-back uncommitted, halt-on-cherry-pick-conflict, persist-on-shutdown, strict-pause-on-creation-failure, CEO confirmation between Plan 1 (impl) and Plan 2 (tests), canary authored now / dispatched post-restart. (2) Side-quest: Bellows reported `plan has 1 steps` for multi-step diagnostics. Diagnostic identified hardcoded `is_diagnostic` overrides at two sites in `bellows.py` setting `total_steps = 1` for any diagnostic-prefixed plan. (3) First parser fix attempt (`executable-remove-is-diagnostic-step-override`) removed both overrides wholesale — broke 3 tests in test_bellows.py covering Phase 8.1 semantics for header-less diagnostics (override was load-bearing). Plan halted via stop verdict. (4) Phase-skip investigation diagnostic confirmed agent did NOT skip phases — agent ran Phase 1 (edit), Phase 2 (edit), Phase 3 (pytest, found 3 failures), stopped per plan's explicit stop-on-failure instruction. Agent behaved correctly; the test failures revealed a real semantics issue. (5) Corrective plan `executable-corrective-narrow-is-diagnostic-override` shipped: narrow override (`if total_steps == 0 and is_diagnostic: total_steps = 1`) at both `run_plan` and `_consume_verdicts` sites preserves Phase 8.1 pause-or-close semantics for header-less diagnostics while letting multi-step diagnostics count headers correctly. Commits `9786e87` (fix) + `799f908` (dev log). 66/66 tests pass, behavioral spot-check 0/3/1 confirms semantics preserved. QA Step 1 gate-tripped on parallel-collision (23 archived verdict-request files in `bellows/verdicts/pending/archived/`) — known BACKLOG #1 false positive class. QA Step 2 passed cleanly (4/4 deliverables, Rule 20 self-check PASSED). Plan moved to Done/ via Filesystem:move_file per Rule 25 terminal-step resolution. REMINDER: restart Bellows daemon to load — pre-fix code still active in running daemon.
- 2026-05-01 (later): Removed Phase 3b/3c DB step-state-resume logic per F3 recommendation in `phase-3b-mechanism-and-cost-benefit-2026-05-01.md`. Dead code that guarded the unsupported manual-rename resume path; verdict-resume path passes resume_step explicitly and never used the DB-query path. Eliminates the slug-collision phantom-resume bug class entirely. plan_slug column preserved for analytics. Net ~20 LOC removed. CEO restart required.
- 2026-05-01 (session): Closed BACKLOG #3 (cleanup_verdicts_for_slug call-site gap → diagnosed as slug normalization mismatch, fixed via cleanup_slug + lookup_slug + Done/-loop removal in startup sweep, +3 regression tests; commit `bc09bb5`). Raised step_inactivity_timeout_seconds 300→1800 (operational unblock for SA cross-referencing tasks). Halted one diagnostic plan that was killed by the old timeout before re-running successfully under the new threshold. Halted original session-wrap plan due to Phase 3b slug-collision bug; re-shipped as -v2.
- 2026-05-01 (later session): Two additional plans shipped after the session-wrap-v2 close. (1) `diagnostic-phase-3b-mechanism-and-cost-benefit-2026-05-01` — investigated the phantom-resume bug observed during the original session-wrap dispatch. SA characterized DB state with literal SQL evidence (rows 554-556 from prior session triggered phantom resume of fresh wrap deposit). Q6 cost-benefit recommended F3 (remove Phase 3b/3c) over patch (F1) and simplify (F2) — Phase 3b/3c guarded the manual-rename resume path that's unsupported per Rule 25, while the supported verdict-resume path passes resume_step explicitly without consulting the DB. (2) `executable-remove-phase-3b-3c-2026-05-01` — implemented F3: removed `_get_last_completed_step` (L175-188), DB-resume guard (L243-247), Phase 3c hash-drift block (L249-254), `import hashlib`. Deleted 5 Phase 3b/3c canary tests; added regression test `test_dispatch_starts_fresh_when_db_has_orphan_slug_rows` exercising production `run_plan` directly. Net ~30 LOC removed from bellows.py. Eliminates phantom-resume bug class structurally. plan_slug column preserved for analytics. Test count: 180→176 passing (5 deleted + 1 added) + 1 known pre-existing failure. CEO restart required to load.
- 2026-05-01: Closed BACKLOG #3 (cleanup_verdicts_for_slug call-site gap). Diagnostic identified the actual root cause as slug normalization mismatch (NOT missing call sites as BACKLOG entry hypothesized): Planner-side verdict filenames include plan-type prefix while verdict-request filenames use stripped slug; cleanup glob never matched. Fix normalizes cleanup_slug via verdict.slug_from_path(original_name) at three call sites in _consume_verdicts, normalizes pending-req-file lookup slug, and removes Done/ exclusion from startup sweep. +3 regression tests. CEO restart required to load.
- 2026-05-01: Raised `step_inactivity_timeout_seconds` 300 → 1800 (operational unblock for SA cross-referencing tasks; wall-clock cap unchanged at 2400s). CEO restart required.
- 2026-05-01: PLANNER_TEMPLATE v4.31 shipped (plan: executable-lessons-verdict-format-and-stranded-plans-2026-05-01). Three Lessons rows appended codifying today's failure modes: (1) verdict format mismatch — Rule 25 documented `continue\n{reason}` while `bellows/verdicts/README.md` requires `verdict:` prefix on line 1 per regex `^verdict:\s*(continue|stop)$`; meta-lesson on validating governance prescriptions against system READMEs. (2) Continue verdict semantics — `verdict: continue` advances step N→N+1, does NOT close the plan; close requires terminal-step continue OR `verdict: stop`. (3) Stranded plans audit — 5 plans across 3 projects silently stranded over 2-3 weeks before today's session caught them; "stranded plans audit" promoted to routine Planner discipline. Commit 3030402 governance-root.
- 2026-05-01: PLANNER_TEMPLATE v4.30 shipped (plan: executable-rule-25-verdict-content-spec-fix-2026-05-01). Rule 25 verdict content spec corrected at 3 occurrences in PLANNER_TEMPLATE.md (Rule 25 terminal-step resolution item 3 line 709, Bellows Execution Model verdict cycle line 861, Manual Execution Model resume protocol line 973). All three now reference `verdict: continue\n{reason}` matching Bellows's parser regex. Commits b705e48 (governance-root) + f3c414f (bellows dev log).
- 2026-05-01: Verdict format mismatch root-caused and patched mid-session. 13 verdict files stranded in `bellows/verdicts/resolved/` (8 from prior sessions + 5 from today's first close attempts) because all 13 were missing the `verdict:` prefix that `bellows/verdicts/README.md` requires. Empirical test: prepending `verdict: ` to one stranded resolved file caused immediate consumption on next 30s scan. Patched all 13 via shell loop. All consumed cleanly as `processed-*`. Underlying governance bug (PLANNER_TEMPLATE describing wrong content spec) fixed via v4.30 plan above.
- 2026-05-01: Pending/ archive operation. Audited 47 verdict-request files in `bellows/verdicts/pending/` against `*/knowledge/decisions/Done/` across all watched projects: 41 archive-safe (slug present in some Done/), 3 stale halted-plan requests, 3 truly active. Archived 44 files (41 + 3) to `bellows/verdicts/pending/archived/` (mirrors decisions/Done/ pattern). 0 truly-orphan files. Pending/ now contains only the 3 active verdicts.
- 2026-05-01: Stranded plan recovered. `verdict-pending-executable-planner-template-lessons-step-numbering-2026-04-23` (9 days old) had Step 1 work complete (lesson row live in PLANNER_TEMPLATE.md line 1235) but Step 2 never ran due to BACKLOG #2 pre-fix gate behavior. Closed via fresh QA executable `executable-close-stranded-lessons-step-numbering-2026-05-01` which adapted the original Step 2's checks to current state. Bellows ran the close-plan cleanly; Rule 22 verification PASSED. Original 2026-04-23 plan moved to Done/.
- 2026-05-01: Accidental Step 2 dispatch incident on the 2026-04-23 stranded plan during recovery. Planner deposited `verdict: continue` thinking it would close the plan, but a non-terminal continue advances step N→N+1. Bellows correctly executed the original Step 2 (which had been authored 9 days ago and was internally inconsistent with current state); Step 2 gate-failed on session-noise scope_check (34 files modified during today's heavy session activity). Recovered via `verdict: stop`. Plan ended in Done/ rather than `halted-` (acceptable outcome since the work is verified by the fresh close-plan above). Lesson captured in v4.31 Row 2.
- 2026-05-01: BACKLOG `2026-04-17: activity-based timeout` closed via executable-close-activity-timeout-backlog-2026-05-01. Diagnostic at knowledge/research/activity-based-timeout-diagnosis-2026-05-01.md confirmed the inactivity-based timeout was already shipped 2026-04-17 (QA at knowledge/qa/activity-timeout-qa-2026-04-17.md). Empirical event-cadence analysis on 20 longest post-stream-JSON logs justified tightening step_inactivity_timeout_seconds from 2400s to 300s. Pure config change, no code modified. Bellows daemon restarted post-close (PID 41471, source SHA `7e1d157`); new config now live.
- 2026-05-01 (session observations): Two items surfaced during today's session that are not blocking but should be tracked. (1) **`test_run_step_timeout` no longer in suite.** PROJECT_STATUS entries from 2026-04-24 through 2026-04-28 cite this as a known pre-existing failure ("149/150" or "168/169" with 1 unrelated). Today's targeted run showed 85 collected, 85 passing — the test is gone. Either deleted, renamed, or skipped at some point in the last week. PROJECT_STATUS notes are out of sync with actual test suite. Worth a brief audit at next session start. (2) **Lessons Learned candidate for PLANNER_TEMPLATE.** Today's diagnostic on activity-based timeout produced a "this work was already done" finding. Phase 1.5 of the Context-Loading Protocol asks the Planner to read recent QA reports, but the Planner did not search the QA folder for filenames matching the BACKLOG topic before scoping the diagnostic. `activity-timeout-qa-2026-04-17.md` was sitting in the QA folder. A 30-second filename grep would have flagged the BACKLOG entry as already-shipped. Proposed rule (next governance pass): "Before scoping work on a BACKLOG item, grep the project's `knowledge/qa/` folder for filenames containing the BACKLOG topic — stale entries closed in code but not in BACKLOG are a real failure mode." Not making the edit now; capturing here for the next governance sweep.
- 2026-05-01: Added 2026-05-01 Lessons Learned entries to PLANNER_TEMPLATE.md v4.29: (1) diagnostic Rule 22 verification needs end-to-end thought-experiment; (2) test names that imply a property must encode it as assertions. Both lessons derived from the failed snapshot-fix close attempt.
- 2026-05-01: Reverted commit `9b20d94` (file-checksum snapshot fix) after Rule 22 verification identified structural insufficiency. Re-opened BACKLOG entry `2026-04-30: parallel-plan scope_check diff-collision` with annotation capturing the failed-close analysis. Refined fix-shape candidates: (d) per-plan worktree, (b) plan-slug commit-message scoping with convention adoption, (e) process-filtered file-touch tracking. Working-tree-shared approaches structurally cannot solve the bug.
- ~~2026-05-01: Closed BACKLOG `2026-04-30: parallel-plan scope_check diff-collision`. Replaced `_capture_git_diff` (working-tree observation, contaminated by sibling commits) with `_snapshot_file_state` (per-step file-checksum snapshot, immune to sibling contamination). Diagnosis at `knowledge/research/parallel-plan-scope-check-collision-diagnosis-2026-05-01.md`. +6 unit tests. The `parallel-N-` pattern is no longer yellow-flagged for committing plans. REMINDER: restart Bellows daemon to load.~~ **(REVERTED 2026-05-01 — see above)**
- 2026-05-01: Closed 2026-04-18 BACKLOG entry (Planner↔Bellows integration protocol underdefined) via hygiene close. Audit at knowledge/research/bellows-integration-section-audit-2026-05-01.md confirmed v4.29 PLANNER_TEMPLATE.md Bellows Execution Model section accurate. Captured new low-priority BACKLOG item: parallel-N- dispatch mechanics not documented in Bellows section.
- 2026-04-30: BACKLOG #11 closed — deposit_exists gate accepts directory paths. Fix: _resolve_deposit_path in gates.py now accepts paths that exist as either files or directories. Closes the false-positive gate_failure on every Rule 26 plan that declares an evidence directory in its **Deposits:** block. +5 unit tests in test_gates.py. Reference: parallel-1-executable-deposit-exists-directory-paths-2026-04-30. REMINDER: restart Bellows daemon to load fix.
- 2026-04-30: BACKLOG #12 closed — ledger pause_reason_code schema enhancement. Fix: log_to_ledger() in verdict.py now accepts and persists a pause_reason_code field; bellows.py call sites updated to pass the appropriate code per call site context. Closes the schema gap surfaced by the 2026-04-30 verdict mechanization distribution audit. +3 unit tests. Reference: parallel-1-executable-ledger-pause-reason-code-2026-04-30. REMINDER: restart Bellows daemon to load fix.
- 2026-04-30: BACKLOG hygiene sweep — three carried items reverified against live code and closed: (1) BACKLOG 2026-04-23 no_permission_denials cross-project entry — superseded by BACKLOG #2 fix shipped 2026-04-28; (2) BACKLOG 2026-04-18 step-state-lost-across-re-claim — superseded by Phase 3b/3c shipped 2026-04-28/30; (3) Phase 8 QA paperwork (carried since 2026-04-16) — closed as functionally-verified-by-downstream-use. CEO decision: write retroactive QA paperwork would be ceremony without value two weeks out; functional verification by 11+ dependent plans (Phase 8.1, resume-from-correct-step, agent self-request, claim-at-entry, parallel defer, scope_check fix, cross-project verdict scoping, et al, all closed cleanly) has effectively replaced it. Reference: executable-backlog-hygiene-sweep-2026-04-30.
- 2026-04-24: Reliability bugs 1-3 shipped (plan: executable-bellows-reliability-bugs-1-2-3-2026-04-24, commit `c7f69f3`). Three fixes to `bellows.py`: (1) Bug 1 — `_consume_verdicts` `plan_matched` boolean gate prevents unconditional verdict-to-processed move; stale verdicts (plan already in Done/) moved to processed with warning; unmatched verdicts left in resolved/ for retry. (2) Bug 2 — `base_filename` canonicalization strips lifecycle prefix (`in-progress-`, `verdict-pending-`, `halted-`) before path construction at 5 sites in `run_plan()`, eliminating double-prefix (`in-progress-in-progress-*`) and embedded-prefix (`verdict-pending-in-progress-*`) rename failures. (3) Bug 3 — `extract_total_steps` regex tightened from `r"^## STEP"` to `r"^## STEP\s+\d+"` with `re.IGNORECASE`; mixed-case plans now counted correctly; case-mismatch warning logged; prose H2 headers (e.g., `## Step-by-step`) excluded by `\s+\d+` requirement. +10 unit tests (150 total, 149 pass, 1 pre-existing `test_run_step_timeout` unrelated). QA: 4/4 deliverables, 3/3 smoke tests PASS, Rule 20 self-check PASSED. REMINDER: restart Bellows daemon to load fixes.
- 2026-04-24: Disable auto-close shipped (plan: executable-disable-auto-close-2026-04-24). Four coordinated changes: (1) `bellows.py` L272 — auto_close default inverted from bifurcated (`true` for executable, `false` for diagnostic) to universal `false`; all terminal steps now pause for Planner verdict by default. (2) PLANNER_TEMPLATE Rule 8 — removed agent move-to-Done from final-step ordering; agents now STOP after commit. (3) PLANNER_TEMPLATE Rule 23 — rationale rewritten: Planner performs move-to-Done after Rule 22 verification, not the agent. (4) PLANNER_TEMPLATE Rule 25 — terminal-step resolution routing added with `Filesystem:move_file` tool call pattern. Also created `knowledge/verdict-log.md` (pause-telemetry observation surface, 8-column schema). +2 tests (140 total, 139 pass, 1 pre-existing). Resolves Failure 3 root cause (auto-close fires before gate check). REMINDER: restart Bellows to load.
- 2026-04-23: Orchestrator-support current-state diagnostic shipped — findings at `knowledge/research/orchestrator-support-current-state-2026-04-23.md`. Full audit of dispatch state machine (7 states, 10 transitions), gate evaluation (8 gates, all independent, post-completion per-step), agent dispatch mechanics (`claude -p` subprocess, process-exit detection with inactivity/wall-clock timeouts, JSON+parser structured output, no real-time step progress), and extension points (no plan-to-plan references exist today, `**Depends on:**` is documentation-only, `parallel-N-*` shares only the group prefix string with no lifecycle coupling, child-spawn mechanically works but coordination does not). Recommendation: new `orchestrator.py` module as smallest viable surface for orchestrator support. Informed ADR-003 design.
- 2026-04-23: ADR-003 deposited (`governance/adr/ADR-003-orchestrator-plan-pattern.md`) — orchestrator plan pattern Phase 1 design-ready. Bellows-side implementation work named: new `orchestrator.py` module, extended `is_runnable_plan` regex for `orchestrator-*` prefix, dispatch routing branch in `PlanHandler._handle`, startup-time scan for `in-progress-orchestrator-*` watchdog thread restart. Three existing BACKLOG items identified as complementary prerequisites (step state across re-claim, verdict mechanization, Planner↔Bellows protocol). Backlog entry added: `no_permission_denials` gate trips on successful cross-repo diagnostics (gate precision issue, does not block work).
- 2026-04-13: Repo scaffolded. Stub files created. Knowledge structure initialized.
- 2026-04-13: runner.py and parser.py implemented and tested.
- 2026-04-13: planner.py implemented and tested.
- 2026-04-13: notifier.py and server.py implemented and tested.
- 2026-04-13: bellows.py orchestrator implemented and tested. Phase 1 build complete.
- 2026-04-13: First autonomous end-to-end run confirmed. Pushover notification delivered.
- 2026-04-14: planner.py rewritten to use claude -p subprocess — no API key required.
- 2026-04-14: Full loop confirmed — Plan → Bellows → Claude Code → Planner judgment → auto-continue → Done → Pushover.
- 2026-04-14: Bellows ran its own improvement plan (planner-claude-code) autonomously.
- 2026-04-14: Phase 2A complete — threading, diagnostic fix, periodic rescan, terminal output.
- 2026-04-14: Phase 2B complete — parallel groups, queue drain notification.
- 2026-04-14: is_runnable_plan parallel-prefix fix shipped. Parallel plans now flow through all 5 call sites (_handle, collect_group, _check_queue_drain, _rescan, startup scan).
- 2026-04-14: Reliability fixes A+B shipped. Bug A: stranded plan detection added to run_plan (checks inprogress_path gone + Done/ file present before emitting ✅ DONE). Bug B: _rescan no longer clears _seen entries, preventing double-dispatch during agent bootup window. Parallel group thread starts now staggered by 2s, matching handle_new_plan. REMINDER: restart Bellows process manually to load the fix — running process still on old code.
- 2026-04-14: Source SHA visibility at startup shipped (Bug C mitigation). _source_sha() helper added; startup banner now prints `Source: bellows.py @ <sha>`. Does not fix Python import semantics — still requires manual restart — but makes staleness immediately visible on boot. REMINDER: restart Bellows manually to see the new banner.
- 2026-04-18: Bellows architecture audit complete — findings at `knowledge/research/bellows-architecture-audit-2026-04-18.md` (20,461 bytes, four sections). Key findings: (1) Bellows is pure Layer 1 infrastructure — the only LLM-invoking module (`planner.py:consult()`) is dead code since Phase 7, imported but never called. Zero SDK imports in active codepaths. `anthropic` in requirements.txt is a dead dependency. (2) Plan file truncation root cause identified as Planner-side write-race, NOT Bellows code and NOT agent Edit tool use. Shadow cache evidence proved file was already truncated at first-read time (1,418 bytes matched truncated size before agent was dispatched). Shadow cache does not protect against write-race — it preserves whatever content exists at claim time. (3) Bellows never writes plan file content — all lifecycle operations are `shutil.move()` renames. (4) State persistence: single `runs` table (302 rows) with operational metadata only, no plan content stored in DB. Fix deployed Planner-side via PLANNER_TEMPLATE v4.23 Rule 24 (Atomic Plan Deposit). Bellows-side hardening (debounce file-creation events, clean dead planner.py module, fix shadow cache design) queued to backlog.
- 2026-04-15: Phase 4 reliability fixes shipped (commit `0a405e5`). Four fixes: (1) parser.py — replaced unreliable text-scan with stop_reason-based status inference (28/30 production logs were recording "Unknown"). (2) bellows.py — escalation timeout `return` changed to `break` so strand check runs on halted plans (silent strands now get STRANDED notification). (3) planner.py — retry on 401 auth failure with 5s delay, fallback to "continue" on transient failures instead of escalating. (4) bellows.py + planner.py — planner consultation logging to stdout and JSONL file. 26/26 tests pass. REMINDER: restart Bellows manually to load fixes.
- 2026-04-16: Phase 6 (runner.py observability + configurable timeout). Five fixes: configurable subprocess timeout (default 600s), cost reported as None on timeout, log files written on all code paths, stderr captured, JSONDecodeError handled. ~30 lines runner.py + tests. Resolves Plan 2b dispatch failure mode (300s timeout was killing complex Bellows steps mid-execution). REMINDER: restart Bellows manually to load fixes.
- 2026-04-16: Phase 7 — Validation gates replace AI planner consultation. New gates.py module with 8 gates: 5 pass/fail checks (receipt_status, ceo_flags, errors, permission_denials, deposit_exists), 1 always-verdict trigger (qa_step_detection), 1 activity recorder (file_change_audit via git diff), 1 scope validator (scope_check comparing actual changes against plan). New verdict.py module for async verdict queue (request-queue/resolved folders + ledger). bellows.py now runs gates after each step instead of spawning claude -p for planner consultation. Verdict requests posted when gates fail or QA step completes. planner.py no longer called. 55/55 tests pass. REMINDER: restart Bellows manually to load Phase 7.
- 2026-04-16: Phase 7 Polish — Parser now accepts 4 additional CEO flag formats (H2 `## CEO Flag`, H2/H3 `CEO Flag(s)`, bold `**CEO Flag:**`, paragraph content under flag headings) in addition to the original H3 `### Flags for CEO` + bulleted format. Bellows auto-closes clean diagnostics that pass all gates (is_diagnostic + passed + not is_qa_step → move to Done + ledger entry + Pushover). First real verdict-layer test (2026-04-16) surfaced both issues — polish plan fixes them before the next test. 61/61 tests pass (55 existing + 6 new). REMINDER: restart Bellows manually to load polish fixes.
- 2026-04-16: Phase 8 — Verdict layer redesign. Gate 2 transformed from prose-based parser (unreliable, agent-output dependent) to mechanical checks: plan-header `pause_for_verdict` YAML field + agent-authored verdict-request files. Polish regex patterns removed from parser.py (safety-net only: `### Flags for CEO` + bulleted items). gates.py gains `_parse_plan_header()` (YAML frontmatter extraction) and `_verdict_requested()` (request-file detection). bellows.py gains `header_says_pause()` helper. Diagnostic default flipped: pause for verdict by default, `auto_close: true` in header opts into auto-close. Final-step `continue` verdict now moves plan to Done instead of re-dispatching Step 1. 64/64 tests pass (-3 polish tests deleted, +1 replacement, +5 new). QA Step 2 caught by Gate 8 scope_check on dirty working tree (4 unrelated uncommitted files) — plan manually moved to Done after Rule 22 verification confirmed Phase 8 code shipped correctly. REMINDER: restart Bellows manually to load Phase 8.
- 2026-04-16: `_parse_diff_stat` audit diagnostic shipped. Confirms OR-semantics bug: function unions `pre_diff` and `post_diff` filenames instead of computing diff-of-diffs. Files already dirty before a step appear as "changed during this step" (false positives). Recommended fix: Option B — build `{filename: stat_line}` dicts for pre/post, report files whose stat line differs. Not yet implemented. Findings at `knowledge/research/bellows-parse-diff-stat-audit-2026-04-16.md`. Diagnostic stranded (Phase 8 final-step block missing pause logic for clean single-step diagnostics with `auto_close=false`) — plan manually moved to Done.
- 2026-04-16: Phase 8.1 — Final-step gate check extended with 3 additional pause conditions (`verdict_requested`, `header_says_pause`, `not effective_auto_close`) to match while-loop logic. Fixes stranding of clean single-step diagnostics with `auto_close=false` (the new Phase 8 default). Discovered when `_parse_diff_stat` audit stranded 2026-04-16. 66/66 tests pass (64 + 2 new). REMINDER: restart Bellows manually to load Phase 8.1.
- 2026-04-16: Resume-from-correct-step fix — mid-plan verdict continue now dispatches Step N+1 instead of restarting from Step 1. `resume_step` parameter threaded through `run_plan` → `_run_tracked` → `handle_new_plan`; `_consume_verdicts` passes `resume_step=step_number+1` on continue verdict for non-final steps. Stale verdict-request files in `verdicts/pending/` now deleted after consumption (all verdict types). 81/81 tests pass (+4 new). REMINDER: restart Bellows manually to load fix.
- 2026-04-16: Agent self-request verdict mechanism: agents can output `VERDICT_REQUESTED: <reason>` to trigger a pause. Dead filesystem-scanning code in gates.py removed (`VERDICT_REQUEST_DIR` constant and `_verdict_requested()` function). Parser extracts the signal from result_text via regex. `verdict_requested` key added to all error-path return dicts in runner.py. `plan_path` parameter removed from `gates.check()` (no longer used). 85/85 tests pass (+4 new).
- 2026-04-16: Notifier timeout fix: `requests.post` in `notifier.push` now uses `timeout=(5, 10)` — eliminates indefinite main-thread freeze from stalled Pushover TCP connections. Heartbeat print added to main loop (every 60s) as a canary. Unreachable stranded-check block removed from `run_plan` (dead code — logical complement conditions guaranteed earlier `return`). 88/88 tests pass (+3 new). REMINDER: restart Bellows to load changes.
- 2026-04-16: Scope_check false positives eliminated. `verdicts/ledger.jsonl` untracked and gitignored (operational runtime data, not source — matches treatment of `bellows.db`). `SCOPE_ALLOWLIST_PREFIXES = ("in-progress-", "verdict-pending-", "halted-")` added to gates.py — plan lifecycle renames skipped by scope_check without requiring exact basename listing. 92/92 tests pass (+4 new prefix tests).
- 2026-04-16: Cross-project verdict scoping fix. `_consume_verdicts` now reads `verdicts/pending/verdict-request-{slug}-step-{N}.md` to extract the `**Plan:**` field and scopes the plan search to that project's decisions directory — prevents a verdict from matching the same slug in a different watched project. Falls back to all watched_projects if the request file is missing. `break` added after processing a match to prevent double-consumption within a directory. 95/95 tests pass (+3 new).
- 2026-04-16: Claim-at-entry fix — `run_plan` now moves the plan file to `in-progress-` before calling `runner.run_step`, eliminating the orphan-original duplicate-dispatch class of bugs. If Bellows restarts between dispatch and agent claim, the original no longer exists in `decisions/` so startup rescan cannot re-dispatch it. Idempotent guard (`if not plan_filename.startswith("in-progress-")`) handles resume paths. 99/99 tests pass (+4 new).
- 2026-04-16: Parallel group deferred dispatch fix — watchdog `on_created` no longer dispatches parallel groups immediately. Instead, a `_pending_groups` dict records first-seen timestamp per group prefix; `_rescan` dispatches groups after a 5-second settle window, giving siblings time to land. `from_rescan=False` parameter added to `_handle` to distinguish watchdog vs. rescan call paths. Non-parallel plans unaffected (still dispatch immediately). 104/104 tests pass (+5 new). REMINDER: restart Bellows to load fix.
- 2026-04-18: Plan A verdict schema additions (Project, Pause Reason Code, Gate Result Passed) plus Total Steps non-None enforcement with legacy-file tolerance. Four new fields in every verdict request file. Unblocks Planner Rule 22 verdict-routing (Plan B for Deposit field still to come).
- 2026-04-18: Plan B verdict schema — added `Deposit:` field to verdict request files via `extract_primary_deposit()` parser. Handles 95%+ of corpus formats (clusters A/B/C/D/F/G/H) plus absolute-path normalization plus ALL-CAPS Write variant via re.IGNORECASE. QA fix: INLINE_DEPOSIT_RE regex updated to handle zero-noun-phrase "deposit as" pattern (cluster G). Completes Planner Rule 22 unblock alongside Plan A.
- 2026-04-19: Rule 26 deposit parser scope fix — closes BACKLOG #6 (deposit-path parser false positives). Three changes: (1) factored `_extract_step_text` helper replacing duplicated inline regex in `_gate_deposit_exists` and `_gate_scope_check`, (2) taught `_extract_plan_required_deposits` to prefer declared `**Deposits:**` block (Rule 26 convention) over legacy prose-matching regexes, (3) fixed scoping bug in `post_verdict_request` where `extract_primary_deposit` scanned the entire plan instead of the current step. QA added 6 targeted tests covering block preference, legacy fallback, none-bullet, code-fence false-positive regression, scoping bug regression, and step-text extraction. 110/110 tests (99 passed, 11 pre-existing runner failures).
- 2026-04-19: `extract_primary_deposit` now recognizes Rule 26 `**Deposits:**` blocks — returns first `.md` path from declared block, block is authoritative (no fallback to legacy regexes when present), 6 targeted tests added.
- 2026-04-19 — Created initial Bellows specialist roster (DEV, QA, SA, Documentation Analyst) under `bellows/agents/`. Unblocks all downstream Bellows backlog work.
- 2026-04-19 — Verdict schema refresh diagnostic confirms all 5 proposed schema additions (Project, Pause Reason Code, Gate Result Passed, Deposit, Total Steps with None guard) already shipped via Plan A + Plan B on 2026-04-18. BACKLOG item #3 closed. No further schema work needed; verdict file format is ready for downstream Rule 25 mechanization and verdict lifecycle coupling work.
- 2026-04-19 — Filesystem watcher reliability fix shipped. PlanHandler now overrides on_moved to catch rename events (BACKLOG #4 closed). Root cause: macOS FSEvents fires on_moved — not on_created — for same-directory renames; default no-op on_moved was silently dropping events. 3-line handler added + 2 unit tests. REMINDER: restart Bellows to load.
- 2026-04-19 — Fixed on_moved re-run cascade via `_handle` subdirectory guard. Guard at start of `PlanHandler._handle()` checks `path_parent not in watched_projects` and returns early — prevents Bellows from re-claiming plans the Planner moves to `decisions/Done/`. Updated 7 existing test fixtures; added 3 new tests (`test_on_moved_dispatches_for_top_level_dest`, `test_on_moved_rejects_subdirectory_dest`, `test_on_moved_dispatches_same_directory_rename`). 42/42 tests pass. Commit `fb1d5f3`. REMINDER: restart Bellows to load guard fix.
- 2026-04-19 — Verdict lifecycle coupling shipped (BACKLOG #9 closed). New `_cleanup_verdicts_for_slug` helper sweeps stranded verdict-request files on three terminal-state transitions (auto-close, continue-to-done, halt) and on Bellows startup. Four call sites + 4 unit tests. REMINDER: restart Bellows to load startup sweep.
- 2026-04-19 — BACKLOG append: 3 new items captured from plan-mutation-source diagnostic session. Items: (1) scope_check false-positive over too-wide git range, (2) deposit parser gap for Rule 26 block form, (3) single-step diagnostic generates verdict request on successful completion. Source document: `knowledge/research/plan-mutation-source-2026-04-19.md`.

- 2026-04-19 — R3 variant (c) shadow cache prompt shipped (BACKLOG #6/#7 closed). All four prompt construction sites in `bellows.py` (diagnostic, resume, fresh, continuation) migrated from mutable `{plan_path}`/`{inprogress_path}` to read-only shadow cache path (`.bellows-cache/*.pristine`). Plan-mutation risk eliminated — agents no longer learn the mutable plan file path. Two code commits (`6b085b8`, `5297a06`), test count 113 → 118 (+5 net-new shadow path assertion tests), 11 pre-existing failures unchanged. QA: 5/5 checks passed, Rule 20 self-check passed. REMINDER: restart Bellows daemon before variant (c) takes effect — next plan runs under pre-fix code.

- 2026-04-28: BACKLOG #4 closed — scope_check false-positive over too-wide git range. Fix: scoped `_capture_git_diff` to project subtree via `--relative -- .`. Universally applicable across nested and standalone repos. Reference: `executable-scope-check-monorepo-fix-2026-04-28`. REMINDER: restart Bellows to load the fix.
- 2026-04-28: Closed BACKLOG item: deposit_exists false-positive prose-path — confirmed structural fix holds via canary 2026-04-28. Reference: `executable-deposit-exists-prose-canary-2026-04-28`.
- 2026-04-28: BACKLOG #2 closed — no_permission_denials read-class tool taxonomy fix. Added `READ_CLASS_TOOLS = {"Grep", "Glob", "Read"}` constant to `gates.py`; modified `_gate_no_permission_denials` to filter read-class denials while preserving write-class blocking. 100% of observed gate false positives were Grep/Glob against cross-project paths — agents route around via bash, complete work, but the gate fired anyway. Defensive handling: string-form denials, missing/None `tool_name`, and unknown tools all default to blocking (allowlist semantics). 7 new edge-case tests, 29 total in test_gates.py. Reference: `executable-no-permission-denials-read-class-fix-2026-04-28`. REMINDER: restart Bellows daemon to load the new gate code.
- 2026-04-28: BACKLOG #6 Phase 3b closed — DB-based step state recovery + plan_slug column. Five changes: (1) `verdict.py` — renamed `_slug_from_path` → public `slug_from_path` and updated all 5 call sites (1 internal + 4 in bellows.py). (2) `bellows.py` — added `plan_slug TEXT` column to all 3 DDL/migration sites (migrate_db DDL, migrate_db additions dict, record_run DDL). (3) `bellows.py` — wired `plan_slug: str` parameter through `record_run()` signature, INSERT statement, and all 4 call sites; slug derived via `verdict.slug_from_path(base_filename)` at top of `run_plan()`. (4) `bellows.py` — new `_get_last_completed_step(db_path, plan_slug)` helper using exact `WHERE plan_slug = ?` match (not LIKE). (5) `bellows.py` — DB resume logic in `run_plan()` fires when `resume_step is None` and shadow cache exists, querying DB for last completed step. 5 new tests (test_record_run_stores_plan_slug, test_get_last_completed_step_returns_max, test_get_last_completed_step_no_rows, test_get_last_completed_step_excludes_non_complete, test_get_last_completed_step_exact_slug_match). 168/169 pass, 1 pre-existing `test_run_step_timeout` unrelated. Phase 3c (plan-hash drift warning) deferred. Reference: `executable-step-state-resume-phase-3b-2026-04-28`. REMINDER: restart Bellows daemon to load the new code.
- 2026-04-28: BACKLOG #4 Phase 3a closed — verdict-only resume documentation shipped. New "Resume Protocol (Verdict-Only)" subsection added to PLANNER_TEMPLATE.md under Execution Model (v4.28). Documents that manual `verdict-pending-*` → `executable-*` rename bypasses `_consume_verdicts()` and re-dispatches Step 1; supported resume is verdict-only via `bellows/verdicts/resolved/`. Phase 3b (DB-based step state recovery) and Phase 3c (plan-hash drift warning) deferred to a separate session per design at `bellows/knowledge/architecture/step-state-resume-design-2026-04-28.md`. Reference: `executable-verdict-only-resume-docs-2026-04-28`.
- 2026-04-28: BACKLOG #5 closed — DEPOSITS_BLOCK_RE blank-line tolerance fix. Regex in `verdict.py` (constant) and `gates.py` (inline) updated to insert `(?:[> ]*\n)*` between header and bullet group, allowing blank quoted lines (`>\n`) between `**Deposits:**` header and first bullet. 6 new tests (3 in `test_extract_primary_deposit_blocks.py`, 3 in `test_rule_26_deposit_parser.py`) lock positive + negative properties. Behavioral check against real plan (`diagnostic-permission-prompt-substrate-2026-04-23`) confirmed extraction now returns expected path. 157 total tests, 156 pass, 1 pre-existing unrelated failure. Reference: `executable-deposits-block-regex-blank-line-2026-04-28`. REMINDER: restart Bellows to load the fix.
- 2026-04-30: Phase 3b restart canary — confirmed daemon loaded new DDL + record_run signature + _get_last_completed_step helper. Live DB has plan_slug column; canary's run row populated; helper returns 1.
- 2026-04-30: Phase 3c plan-hash drift warning shipped (BACKLOG #6 fully closed). Added `import hashlib` + 7-LOC warning block to `run_plan()` in `bellows.py` between DB resume block and is_diagnostic check. Fires when resume_step > 1 AND shadow_text present AND sha256(shadow) != sha256(plan_text). Stdout warning + Pushover notification "Bellows — Plan Modified". Warn-and-proceed (no halt). +1 unit test (`test_run_plan_plan_hash_drift_warning`), 71 total in test_bellows.py. REMINDER: restart Bellows to load.
- 2026-04-30: PLANNER_TEMPLATE v4.29 — added "Bellows Execution Model (Layer 1 Autonomous Dispatch)" section providing architectural overview of three-layer model, plan lifecycle states, the 8 gates, verdict cycle, disable-auto-close model, atomic deposit, Planner discipline cross-references, what Bellows does NOT do, and restart discipline. Existing "Execution Model" section renamed to "Manual Execution Model (RUN EXE / RUN DIAG / Bootstrap)". Closes 2026-04-18 BACKLOG entry "Planner↔Bellows integration protocol underdefined" — was deferred pending step-state-persistence (BACKLOG #6 Phase 3b/3c closed today) and watcher-reliability (BACKLOG #4 closed 2026-04-28).
- 2026-04-30: Verdict mechanization SA diagnostic shipped — findings at `knowledge/research/verdict-mechanization-distribution-audit-2026-04-30.md` (23,660 bytes). Distribution audit of 56 ledger entries (2026-04-16 to 2026-04-28): 25% auto-close (already automated), 36% `auto_close_disabled` clean (mechanization-eligible), 34% `gate_failure` (all spurious — fixed via gate precision shipped 2026-04-28), 5% `qa_checkpoint`. **0 real Planner findings** in 42 paused verdicts (CI [0%, 7%]). Scope analysis evaluated three mechanization tiers (a/b/c) with LOC, modules, failure modes. **Recommendation: don't mechanize, close BACKLOG verdict mechanization entry.** Reasoning: highest-friction class is gate_failure, mechanization addresses none of it; gate precision fixes already shipped 2026-04-28 dropped predicted spurious-failure rate from 34% → ~7%; auto_close_disabled pauses are lowest-friction (~3-7 min/day savings); scope (a) introduces silent false-positive auto-resolution risk; scopes (b)/(c) violate Layer 1 mechanical-only invariant from PLANNER_TEMPLATE v4.29. Revisit triggers: paused verdicts >5/day sustained, new high-friction routine class emerges, or Rule 22 checks consistently >5 min. Ship CEO accepted; closeout in BACKLOG.

- 2026-04-23: Stream-JSON minimal switch shipped. Bellows now invokes claude -p with --output-format stream-json --verbose; runner.py extracts the terminal result event from the NDJSON stream; parser.py and gates.py untouched (identical field schema). Resume compatibility verified. Full NDJSON stream preserved in raw_output log for future corpus/telemetry work. CEO must restart Bellows to load. Commit: d8eced1.

- 2026-04-23: Stream-JSON post-restart smoke test confirmed live deployment. Two-step smoke-test plan dispatched post-restart: Step 1 wrote `hello from step 1` to a scratch file, Step 2 read and appended `goodbye from step 2`. Both step logs (`20260423-211911-step.json`, `20260423-211937-step.json`) show `raw_output` as NDJSON (system/init + multiple assistant/user events + terminal result), compared to pre-restart baseline (`20260423-210526-step.json`) which was a single JSON object — qualitative difference confirms stream-json is active. Session_id identical across both steps (`d2838d35-36a2-41d0-a97d-0c747857cc30`), confirming Bellows's --resume path captures session_id from NDJSON terminal result event. Both steps `is_error=false`, `stop_reason=end_turn`. Plan auto-closed cleanly with zero gate trips — first plan of the session to close end-to-end without verdict intervention.

- 2026-04-23: Stream-JSON feasibility diagnostic shipped — findings at `knowledge/research/stream-json-feasibility-2026-04-23.md`. Confirmed stream-json emits structured tool-call events (5 distinct event types: system/init, assistant, user, rate_limit_event, result) with tool names + arguments visible in real time. Blast radius assessed: minimal switch is day-scale (1 file, ~30 lines, parser.py + gates.py untouched due to identical field schema); full per-event gating is week-scale (3 files, 2 gates reclassified as BECOMES PER-EVENT, 6 STAYS POST-HOC). Scope_check git-range false positive incidentally fixable by per-event mode. Used to justify ship-minimal-now, defer-per-event-gating sequencing.

- 2026-04-23: Permission prompt substrate diagnostic shipped — findings at `knowledge/research/permission-prompt-substrate-2026-04-23.md`. Answered CEO question of whether Bellows steamrolls permission checks during execution. Confirmed: Bellows passes `--allowedTools Read,Edit,Write,Bash` + `claude -p` non-interactive mode; permission prompts don't fire because they can't by design, not because of suppression or auto-approval. Real observability gap identified as tool-call visibility, not permissions — stream-json addresses it. This diagnostic directly motivated the stream-json minimal switch work that followed.

- 2026-04-22: scope_check project-path filter shipped (BACKLOG #2 point fix, commit `fd9053f`). Added `project_path` filter to `_parse_diff_stat` in `bellows.py` — files outside the plan's project directory (paths with `../` components from `git diff --stat`) are now excluded from `files_changed` before scope_check runs. Eliminates false positives from sibling projects and repo root files. 6 new tests added to `tests/test_bellows.py`, 52/52 pass. REMINDER: restart Bellows to load fix.

## Pending (next session)
- **Restart Bellows daemon FIRST** before any worktree work — multi-step parser fix (commits `9786e87` + `799f908`) is committed but pre-fix code is still running. Module-level code; no hot-reload.
- **BACKLOG #1 worktree implementation** — design fully locked (see 2026-05-03 entry above). Plan 1 = impl (8 sites in `bellows.py`: 2 helper functions `_create_worktree`/`_teardown_worktree`, `run_plan` lifecycle integration, 4 cwd swaps, 2 `runner.run_step` cwd swaps, teardown insertion, startup `git worktree prune` hook in `Bellows.__init__`, plus `.bellows-worktrees/` in `bellows/.gitignore`). Plan 2 = tests (~13: 6 in test_bellows.py + 7 in new tests/test_worktree.py). CEO confirmation gate between Plan 1 and Plan 2.
- **Canary plan dispatch after worktree ships.** Two parallel-99- plans designed in D8: Plan X writes `canary-X-marker.txt`, sleeps 30s, commits; Plan Y writes `canary-Y-marker.txt`, commits immediately. Falsification: under unfixed code, Y's `files_changed` includes X's marker; under worktree fix, files are isolated.
- **23 archived verdict-request files in `bellows/verdicts/pending/archived/`** contributing to scope_check noise on every plan. Pre-existing dirty git state. Worktree fix solves this structurally; worth noting until then.
- **Three Lessons candidates for PLANNER_TEMPLATE governance pass** (route through future executable governance plan with QA per Rule 8 — do NOT direct-edit during session-wrap):
  1. **Test fixtures are part of the audit surface.** When auditing "is this code load-bearing?", grep tests/ in addition to production artifacts. Today's audit caught production patterns but missed test fixtures locking in Phase 8.1 override semantics.
  2. **`git status` and `git log` are source of truth for working-tree/commit state.** Don't infer "commit landed" from "edit visible in file." Today the broken parser fix's edit was visible all afternoon as uncommitted; Planner called Phase 4 (commit) "clearly executed" based on seeing the edit, not on git log.
  3. **"Stop and report" instructions need explicit deposit paths.** First parser fix's stop-on-failure documented to conversation text rather than a file deposit. Future plans: specify "write failure details to `knowledge/development/<slug>-failure-log.md` BEFORE stopping" — Bellows can't see conversation text.
- Audit `test_run_step_timeout` disappearance — was cited as known pre-existing failure across 2026-04-24 to 2026-04-28 PROJECT_STATUS entries; today's targeted run shows it gone. Determine whether deleted, renamed, or skipped. Update older PROJECT_STATUS entries if appropriate (or add a closing note rather than rewriting history).
- Lessons Learned candidate for next PLANNER_TEMPLATE governance pass: "Before scoping work on a BACKLOG item, grep `knowledge/qa/` for filenames containing the topic — stale entries closed in code but not in BACKLOG are a real failure mode."
- `_parse_diff_stat` fix (Option B diff-of-diffs) — diagnostic findings ready, fix is well-scoped.

## Blocked
- None


## 2026-04-16 — Bellows Verdict + Resume Session

**Plans shipped:**
- `verdicts/README.md` — verdict file format documentation
- `_parse_diff_stat` Option B — diff-of-diffs replaces OR-union, fixes false-positive scope flags
- Verdict request `pause_reason` — five pause conditions threaded through verdict requests
- Resume path fix — mid-plan verdict continue dispatches Step N+1 instead of restarting Step 1
- Pending file cleanup — `verdicts/pending/` files deleted after consumption

**QA status:** Resume path fix got full QA (4/4 targeted, 81/81 suite). Other three plans Planner-verified only (scope_check loop prevented formal QA execution — the exact bug the resume fix addresses).

**Open items:**
- Restart Bellows to pick up all code changes
- Agent self-request mechanism in gates.py non-functional (filename + slug mismatch) — backlog
- bellows/agents/ specialist files don't exist — backlog


## 2026-04-16 — Bellows Reliability + Forge Evaluation Infrastructure

**Duration:** ~4 hours | **Projects:** Bellows, Forge

### Bellows — Plans Shipped (11 total)

| Plan | What | Tests | QA |
|---|---|---|---|
| Verdict README | `verdicts/README.md` documenting format + 5 pause conditions | — | Planner-verified |
| `_parse_diff_stat` Option B | OR-union → diff-of-diffs, eliminates false-positive scope flags | +tests | Planner-verified |
| Verdict request `pause_reason` | 5 pause conditions threaded through verdict requests | +tests | Planner-verified |
| Resume path + pending cleanup | Mid-plan verdict continue dispatches Step N+1; pending files cleaned | +4 | 85/85 ✅ |
| Agent self-request verdict | Text marker in parsed output replaces broken file-based mechanism | +4 | 88/88 ✅ |
| Notifier timeout + heartbeat + dead code | `requests.post` timeout=(5,10), 60s heartbeat, unreachable code removed | +3 | 88→92 ✅ |
| Scope_check infra fix | `ledger.jsonl` gitignored, prefix allowlist for plan file renames | +4 | 92→96* ✅ |
| Agent self-request integration test | End-to-end validation of VERDICT_REQUESTED signal path | — | ✅ Validated |
| Cross-project verdict scoping | `_consume_verdicts` scoped by Plan field from pending file + break | +3 | 95* ✅ |
| Parallel group defer | Deferred dispatch from on_created to rescan with 5s settle window | +5 | 104 ✅ |
| Claim-at-entry | Bellows claims plan at run_plan entry, eliminates orphan duplicates | +4 | 99* ✅ |

*Test counts vary due to concurrent parallel execution — final suite is 104.

**Bellows test suite: 64 → 104 (+40 tests)**

### Forge — Plans Shipped (8 total)

| Plan | What | Tests |
|---|---|---|
| Evaluation annotation (valid=0) | 57 rubber-stamped 2026-03-21 rows invalidated | +3 → 129 |
| Budget diagnostic correction | Stale "same 10 chunks" claim postscripted | — |
| Retrieval fix (Approach B) | chunk_type filter to qa_report + feedback_entry | 129 |
| Approach A viability diagnostic | Found: Approach A won't fix 0% — root cause is variant-scoped retrieval | — |
| Test case generation design diagnostic | Full blueprint: schema, prompt, regen trigger | — |
| Test case infrastructure | Schema + helpers + branching + evaluate_variant | +8 → 137 |
| Test case gen + Batch 2 eval | 28 scenarios generated for 6 variants, all 100% would_prevent | 137 |
| Exp120 sweep fix | Stale experiment closed + 24h auto-sweep added | +3 → 140 |
| Forge cycle #11 | 2,289 files / 12,109 chunks / 136 experiments / 100 rule_mappings | — |
| Cycle template diagnostic | Verified correct function refs + experiment 120 root cause | — |

**Forge test suite: 126 → 140 (+14 tests)**

### Key Architecture Decisions

- **Approach A (describes_failure column) rejected** — doesn't fix the 0% evaluation problem; root cause is variant-scoped retrieval, not chunk classification
- **Test case evaluation adopted** — lab auto-generates failure scenarios per variant; evaluations run against those instead of origin chunks; regen trigger deferred until a variant scores poorly
- **Bellows claim-at-entry** — Bellows owns the file claim, not the agent; eliminates orphan-original duplicate dispatch class
- **Parallel defer to rescan** — parallel groups collected on rescan tick with 5s settle, not on per-file watchdog events

### Lessons Logged

- Lesson 8 in `bellows/knowledge/research/session-lessons-2026-04-16.md` — Planner multi-chunk writes race against Bellows watcher; staging pattern established

### Remaining Backlog

- Regen trigger logic for test cases (parked until a variant scores poorly)
- Bellows restart needed to pick up all code changes from this session
- PLANNER_TEMPLATE cycle plan Step 3 uses wrong function refs (Planner knowledge fix, not code)


## 2026-04-19 — Bellows `_handle` Subdirectory Guard + Session Cleanup

**Duration:** ~4 hours | **Projects:** Bellows + governance root (PLANNER_TEMPLATE.md)

### Session Arc

Session started as a build session for BACKLOG #9 (verdict lifecycle coupling). Diagnostic for #9 completed cleanly, findings deposited. When the Planner moved the completed diagnostic to `Done/`, Bellows re-claimed and re-executed it from inside Done/ — a regression introduced by the morning's BACKLOG #4 `on_moved` handler fix. Session scope pivoted to diagnose and fix the regression before returning to #9.

### Plans Shipped (1)

| Plan | What | Tests | QA |
|---|---|---|---|
| `_handle` subdirectory guard | Added `if path_parent not in watched_projects: return` at start of `PlanHandler._handle()`. Fixes BACKLOG #4 regression; closes on_moved / on_created / on_modified subdirectory vulnerability with one edit. | +3 new, 1 updated | 42/42 ✅ |

**Commit:** `fb1d5f3` | **Live-tested:** 8 move-to-Done operations during session housekeeping, no re-claim cascades observed

### Diagnostics Shipped (3)

| Diagnostic | Purpose | Deposit |
|---|---|---|
| verdict-lifecycle-coupling | BACKLOG #9 investigation — characterize terminal-state rename paths, slug extraction, race windows, edge cases | `knowledge/research/verdict-lifecycle-coupling-2026-04-19.md` |
| bellows-rerun-cascade | Characterize the 18:14:32 regression trigger | `knowledge/research/bellows-rerun-cascade-2026-04-19.md` |
| on-moved-fix-shape | Design the exact guard condition + test surface | `knowledge/research/on-moved-fix-shape-2026-04-19.md` |

### Housekeeping Sweep

After restart with the guard loaded, 8 stranded plans swept from `verdict-pending-*` state to `Done/`:
- 2 completed diagnostics from tonight (rerun-cascade, on-moved-fix-shape)
- 5 stranded diagnostics from prior sessions (all had findings files already deposited — housekeeping-only, no re-execution needed)
- 1 mis-named file renamed inside `Done/` (verdict-lifecycle-coupling had wrong prefix due to the regression capture)

### Governance Updates

- PLANNER_TEMPLATE.md — added Lessons Learned entry (v4.25) on verifying agent hypotheses against unchecked artifacts before designing follow-up work.
- `bellows/knowledge/BACKLOG.md` — added Closed item for `_handle` guard regression; annotated BACKLOG #9 with diagnostic-complete status + deferral reasoning.
- `bellows/knowledge/research/agent-prompt-feedback.md` — seeded Patterns Identified section with three patterns (PATH-001, TOOL-001, DIAG-001).

### Key Architecture Decisions

- **Guard placement in `_handle`, not `on_moved`** — single location covers all three watchdog handlers (on_moved, on_created, on_modified) instead of repeating the guard per method. Structural fix per COMPANY.md Quality Standards.
- **BACKLOG #9 deferred (Option 2b)** — diagnostic complete and executable shape designed, but acute pain is now resolved by the `_handle` guard. Remaining impact is cosmetic stranded-file accumulation, not re-run cascades. Queued for next session.

### Lessons Logged

- PLANNER_TEMPLATE v4.25 Lessons Learned: "Verify agent hypotheses against unchecked artifacts before designing follow-up work" — the Strategy A vs Strategy B orphan-recovery debate was premised on an SA hypothesis (plans need re-run) that one `list_directory` check would have disproved (findings already existed). Rule 22 applied inward.
- BACKLOG.md Closed-item meta-lesson: "Future watchdog handler fixes should enumerate every legitimate dispatch path AND every wrong dispatch path the fix must continue to block, then test both." The original BACKLOG #4 fix tested same-directory renames but not dest-in-subdirectory moves.

### Remaining Backlog (next session)

- BACKLOG #9 executable (verdict lifecycle coupling sweep) — diagnostic complete, shape designed, ready to write
- Session cost tally: 4 diagnostics + 1 executable ≈ ~$5 across all runs (including $1.07 unintended re-run from the BACKLOG #4 regression before it was identified)
