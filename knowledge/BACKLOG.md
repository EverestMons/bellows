# bellows — Backlog

**How to use:** same convention as invoice-pulse. New items at top, move to Closed when done.

---

## Open



- **Added 2026-05-31:** Worktree teardown→resume regression — **full diagnosis + Opus-class fix blueprint at `bellows/knowledge/research/worktree-teardown-resume-regression-2026-05-31.md`.** Unifies the 2026-05-30 item (immediately below) and the 2026-05-22 dirty-tree-teardown item as ONE failure family. **Key correction to the 2026-05-30 framing: this fires on a NORMAL continue-resume, NOT restart-only.** Confirmed live this session: a stray uncommitted non-lifecycle file on local `main` trips `_teardown_worktree` step (b2) dirty-tree pre-check at the Step-1 pause → cherry-pick skipped, worktree left alive; the `worktree_teardown` failure appends to `gate_result` AFTER the `gates step N: passed=True` log line (so it is INVISIBLE in that log line — must read the verdict-request Gate Result JSON / ledger); a continue-over-failure then advances, and `_create_worktree`'s unconditional stranded-cleanup (commit `93b18ec`) removes the still-live worktree and recreates at main `HEAD`, orphaning Step 1's commits as dangling. Inverse confirmed (clean `main` → teardown succeeds → resume correct). Three fixable gaps with sequencing in the findings doc: (1) continue silently advances past a failed teardown; (2) stranded-cleanup orphans un-landed commits; (3) dirty-tree teardown ergonomics. Reproduction: `executable-no-match-verdict-warning-dedup-2026-05-31` (now in `Done/`); ledger step-1 `worktree_teardown_dirty_tree`; recovered dangling chain `858ab10`. **Status 2026-06-01:** Gap 1(b) (continue silently advances past a failed teardown) SHIPPED — see Closed 2026-06-01. **Remaining in this family:** Gap 1(c) re-attempt teardown on continue-resume; Gap 2 preserve un-landed commits on stranded-cleanup (the dangling-commit escalation / data-at-risk); Gap 3 dirty-tree teardown auto-stash ergonomics. Recommended next cut by severity: Gap 2 (prevents the orphaned-commit escalation that the Gap 1(b) guard only halts rather than recovers).

- **Added 2026-05-30 (session 18):** Worktree re-creation on resume checks out main `HEAD`, not the step's branch — regresses the next step's tree after a daemon restart during a verdict pause. `bellows.py:825` runs `git worktree add <path> HEAD --detach`; `bellows.py:805-818` removes a "stranded" worktree then re-creates it the same way. Reproduced on `executable-isolated-fuel-sample-prompt-key-2026-05-29`: Step 1 (DEV) completed in-worktree, committed `9b26411` on branch `feat/isolated-fuel-sample-prompt-key`, pushed to origin, paused for verdict at 19:38. A session restart at 23:35 orphaned the live worktree. On continue-verdict resume at 00:33, Bellows found the stranded worktree, removed it, and re-created it detached at main `HEAD` (`209b628`) — which lacks Step 1's commit (that lived only on the pushed branch). Step 2 (QA) ran against the regressed base tree, correctly found no dev log / no new key, stopped with an accurate blocker, and gates failed `deposit_exists`/`rule_20_self_check`/`rule_22_verification` (`files_changed=0`). A compounding `worktree_teardown` gate failure also fired (dirty local `main` would have blocked the teardown cherry-pick). No work lost — safe on the origin branch. **Root cause:** the worktree-per-step model assumes each step's commit reaches `main` (via teardown cherry-pick) before the next step's worktree is created from `HEAD`. That holds for uninterrupted dispatch (Step N continues in Step N-1's still-live worktree) but breaks when a restart between steps forces re-creation from `HEAD` while the intermediate commit is still only on the unmerged branch. **Fix shape options:** (a) on re-create, if a `feat/<slug>` branch exists (local or origin), check it out instead of `HEAD --detach`, preserving the step's accumulated commits; (b) skip the stranded-worktree removal when the worktree HEAD is ahead of `main` (unmerged commits present) — reattach rather than destroy; (c) on resume, detect unmerged prior-step commits and cherry-pick them into the freshly-created worktree before running the next step. **Effort estimate:** medium — (a) needs a branch-exists probe + checkout path + a regression test simulating restart-between-steps; (b) needs an ahead-of-`main` check before the destructive remove. **Severity:** high for multi-step plans interrupted by a restart during a verdict pause — silently regresses the next step to a base tree and produces empty-deposit gate failures that look like agent failure but are environmental. Single-dispatch multi-step plans (no restart) are unaffected. **Recovery used this session (Planner-direct close):** committed dirty `main` housekeeping (`e5657bd`), cherry-picked `9b26411` onto `main` (`080024c`), Planner re-ran suite + Rule 22 checks against `main`, issued continue-to-done. **Reference:** `bellows-2026-05-29.log` 18:56 dispatch → 19:38 step-1 pause → 23:35 session restart → 00:33 resume "stranded worktree found ... removing before re-creation"; `verdicts/pending/verdict-request-isolated-fuel-sample-prompt-key-2026-05-29-step-2.md` (gate JSON incl. `worktree_teardown`); invoice-pulse `main` commits `e5657bd` + `080024c`.

- **Added 2026-05-29 (session 17):** `scope_check` gate cannot evaluate plans that delegate file lists to a referenced blueprint. Reproduced on `executable-fuel-timing-rule-extraction-hardening-2026-05-28` step 2 (DEV). The Step 2 prompt said "Implement per the blueprint deposited in Step 1" rather than inlining target file paths. When DEV completed having modified 8 files exactly as the blueprint specified, scope_check flagged every file as out-of-scope: `out-of-scope files: copilot_prompts.py, engines/validator.py, tests/test_contract_fuel_import.py, tests/test_copilot_fuel_combined.py, tests/test_validator.py, web/gap_dashboard.py, web/templates/contract_fuel.html, web/utils.py`. The gate's `plan step context` capture was the Step 2 header line plus the first ~80 characters of the body — the blueprint reference is captured as text but the gate has no logic to follow the reference and read the actual file list from the blueprint document. Gate failure was a false positive; Planner's Rule 22 (b) substance check confirmed alignment against the blueprint and issued continue. **Distinction from the 2026-05-28 entry on multi-file evidence in named directories:** that entry covers QA-step evidence files referenced by parent directory. This covers DEV-step file lists referenced by a deposited blueprint markdown document at a different path. Both stem from the same root cause: scope_check requires explicit per-filename mention in the step body. **Fix shape options:** (a) teach scope_check to follow blueprint references — when the step body contains `Read .../blueprint*.md` or similar reference, the gate should `view` the referenced file and extract its file list (more flexible but requires careful regex for the reference pattern and a structural assumption about blueprint format); (b) update PLANNER_TEMPLATE to require an inline `**Target Files:**` block in DEV steps even when a blueprint exists (template change only, duplicates file paths from blueprint into step body solely to satisfy scope_check; redundant and drift-prone but simpler). **Effort estimate:** (a) small-medium — pattern-match for blueprint reference + view-and-extract logic + 2-3 regression tests; (b) trivial — template edit, but does not actually fix the gate logic. **Severity:** moderate. Every SA→DEV→QA executable using the blueprint-delegation pattern will hit this. The Planner override via substance check is viable but each repetition costs ~5 minutes walking the DEV-modified files against blueprint sections. Discipline rule documented in LESSONS.md 2026-05-29 "Bellows scope_check gate cannot evaluate plans that delegate file lists to a referenced blueprint". **Reference:** verdict request at `bellows/verdicts/pending/verdict-request-fuel-timing-rule-extraction-hardening-2026-05-28-step-2.md`; blueprint at `invoice-pulse/knowledge/development/fuel-timing-rule-extraction-hardening-blueprint-2026-05-28.md`; dev log at `invoice-pulse/knowledge/development/fuel-timing-rule-extraction-hardening-dev-log-2026-05-28.md`; override verdict at `bellows/verdicts/resolved/verdict-fuel-timing-rule-extraction-hardening-2026-05-28-step-2.md`.

- **Added 2026-05-29 (session 17):** `stop_prose` dispatch-validator matches PLANNER_TEMPLATE-prescribed step-control language. Reproduced on `executable-fuel-timing-rule-extraction-hardening-2026-05-28` at claim time: two WARN entries fired against `STOP\.` matching the `**STOP. Do NOT proceed to Step N. Do NOT move the plan to Done. Wait for CEO verdict before continuing.**` block that PLANNER_TEMPLATE's example step structure (lines ~368-389 of the template) explicitly prescribes for Bellows-mode multi-step executables. **Distinction from the 2026-05-28 (session 14) entry on `do not proceed` substring matching:** that entry covers `do not proceed` matching inside Rule 20 evidence-files instructional prose (a recovery-instruction line). This covers `STOP.` matching the canonical end-of-step Wait-for-CEO-verdict block — different pattern from the same detector, different source (PLANNER_TEMPLATE prescription vs Rule 20 evidence prose), but same false-positive class. **Why the template prescribes the prose:** historical pattern from manual_bootstrap dispatch where agents needed explicit step-boundary instruction. Under Bellows dispatch, the daemon controls step pauses via `pause_for_verdict: always` header, making the prose advisory rather than mechanical. The validator flagged it because the prose now duplicates a mechanism the daemon owns. **Two fix shape options:** (a) update PLANNER_TEMPLATE to remove the `**STOP. Do NOT proceed...**` block from Bellows-mode step structure, since `pause_for_verdict: always` in the header is the mechanical authority — agents can read the header. Risk: agents may behave less predictably without explicit prose anchoring. (b) Relax the validator pattern to exclude the canonical step-boundary phrasing — e.g. ignore `STOP\.` when followed by `Do NOT proceed to Step` (the specific phrasing only used at step boundaries, not inside Rule 20 or other instructional prose). **Effort estimate:** (a) template edit + version bump + sweep any in-flight plans authored with old form; (b) regex anchoring change + 2-3 regression tests covering canonical step-boundary preservation. **Severity:** low — informational warning, not blocking. Two WARN entries per multi-step Bellows-mode executable; visual noise at claim time. **Disposition recommendation:** (b) is the right structural fix — keeps the PLANNER_TEMPLATE prescription intact while teaching the validator that step-boundary STOP language is not a substantive scope-control violation. **Reference:** bellows-2026-05-28.log 21:54:07 dispatch-validator WARN entries; LESSONS.md 2026-05-29 "Bellows scope_check gate cannot evaluate plans that delegate file lists to a referenced blueprint" tangentially references the dispatch-validator family.

- **Added 2026-05-28 (session 16):** `scope_check` gate matcher false-positive on plan-required evidence files that aren't named by exact filename. Reproduced this session on `executable-fuel-inference-preamble-skip-impl-2026-05-28` step 2 (QA). The plan body said "Deposit all grep outputs as evidence files at `invoice-pulse/knowledge/qa/evidence/fuel-inference-preamble-row-skip-qa-2026-05-28/<check-name>.txt`" and the Deposits block named the parent directory. The agent created 6 evidence files in that directory: `full-suite.txt` plus 5 grep-based check files (`engine-preamble-fields.txt`, `engine-preamble-count.txt`, `endpoint-gate-logic.txt`, `unit-test-methods.txt`, `integration-test-methods.txt`). `full-suite.txt` is mentioned by exact filename in the prompt body and passed scope_check. The other 5 are only referenced collectively ("one file per grep-based deliverable verification check") and were flagged out-of-scope. All 6 sit in the same directory, were created by the same agent following the same instruction. **Matcher gap:** scope_check appears to require explicit per-filename mention in the prompt body and does not honor parent-directory mentions in the Deposits block as authorization for child files. **Likely fix shape:** when the plan's Deposits block lists a directory path (trailing-slash or "evidence directory" suffix), accept any file written under that directory as in-scope. Alternative: also accept files matching the pattern `<check-name>.txt` when the plan body uses an angle-bracket placeholder near a directory path. **Effort estimate:** small — pattern-match additions to the scope_check gate evaluator, with 2-3 regression tests covering (a) exact-filename match (current behavior), (b) directory-mention authorization, (c) placeholder-pattern authorization. **Severity:** moderate. Every QA step that produces multi-file evidence in a named directory will hit this. The Planner override is viable but each repetition costs CEO attention and the override pattern bypasses scope_check entirely for the affected step. **Reference:** verdict request at `bellows/verdicts/pending/verdict-request-fuel-inference-preamble-skip-impl-2026-05-28-step-2.md`; QA report at `invoice-pulse/knowledge/qa/fuel-inference-preamble-row-skip-qa-2026-05-28.md`; override verdict at `bellows/verdicts/resolved/verdict-fuel-inference-preamble-skip-impl-2026-05-28-step-2.md`.

- **Added 2026-05-28 (session 15):** `_seen` slug cache not invalidated when plan re-deposited at same slug after successful completion + Done/ move. Reproduced this session: diagnostic `diagnostic-fuel-inference-preamble-row-skip-2026-05-28.md` ran cleanly through Bellows (claim → dispatch → step 1 complete → verdict pause → CEO verdict continue → Planner-direct move to Done/). Within minutes I deposited the follow-on executable at `executable-fuel-inference-preamble-row-skip-2026-05-28.md` (same base slug after prefix stripping by `slug_from_path`). Bellows logged `heartbeat: idle` for 4+ minutes and never detected the new file. Recovery: renamed the executable to `executable-fuel-inference-preamble-skip-impl-2026-05-28.md` (distinct slug) — picked up on the next scan tick. **Distinction from the 2026-05-26 close (`_seen` invalidation on `on_modified`):** that fix invalidates `_seen` when an existing file is modified. The current case is a NEW file deposited at the same slug after the prior file moved to `Done/`. The watcher's `on_created` fires for the new file but apparently does not invalidate `_seen` either, OR the slug-keyed entry was never cleared when the prior plan transitioned to `Done/`. **Likely fix shape:** clear `_seen[slug]` at the Done/ transition site (Planner-direct move via `Filesystem:move_file` won't trigger this, but the daemon's own move-to-Done after verdict consumption is the canonical clear point). Alternative: add `on_created` invalidation symmetric to the `on_modified` invalidation already in place, with the same lifecycle-prefix exclusion guard. **Effort estimate:** small — single-site `_seen.discard()` at the verdict-resolution → Done/ move path, OR mirror the `on_modified` invalidation into `on_created`. 1-2 regression tests for the "same-slug re-deposit after Done/" scenario. **Severity:** the diagnostic→executable handoff is a very common workflow shape (literally every diagnostic that produces an actionable executable hits this slug-collision since `slug_from_path` strips both prefixes). Manual rename is a viable workaround but each repetition costs Planner attention. **Pattern note:** echoes the 2026-05-27 LESSON on the dispatch-mode validator rejection path (the only `run_plan` early-exit that didn't `_seen.discard`). Both are slug-cleanup-gap defects; the rejection-path one was fixed by `commit 0322124`. This is the same family on the success-path. Reference: bellows terminal log 2026-05-28 16:30-16:38 (idle heartbeats after diagnostic close + executable deposit); recovery rename to `executable-fuel-inference-preamble-skip-impl-2026-05-28.md`; LESSONS 2026-05-27 entry on `_seen.discard()` on rejection-path; `Closed 2026-05-26: Bellows path-keyed rejection cache — _seen invalidation on on_modified` for the prior `on_modified` fix.



- **Added 2026-05-28 (session 14):** Dispatch-validator `stop_prose` detector matches substring `do not proceed` inside instructional prose, producing false-positive WARN at claim time. Reproduced on the smoke-test plan deposit at 14:23:24 (`bellows-2026-05-28.log`): the dispatch-validator emitted `dispatch-validator: stop_prose — STOP-prose detected in step body: 'do not proceed' matched in line: > Each evidence file captures the relevant pytest output (one per pytest invocat...` — but the line is from the Rule 20 evidence-files instruction (`If the block prints FAILED, do not proceed with closure.`), not a step-control directive. Warning only, did not halt dispatch. **Impact:** Bellows operational noise at every plan that contains Rule 20 prose. **Fix shape:** tighten the detector to require either (a) `do not proceed` not following an `If` predicate, OR (b) the pattern to appear outside markdown-quote-prefixed (`>`) instructional prose, OR (c) the pattern to start a sentence or be at top-level of a step body. **Effort estimate:** small — single regex anchoring change + 2-3 regression tests covering Rule 20 prose preservation. **Disposition recommendation:** defer until a second occurrence creates ambiguity in CEO triage. Reference: `bellows-2026-05-28.log` 14:23:24 line; dispatch-validator implementation TBD (locate at next sweep).



- **Added 2026-05-28:** Pre-deposit plan-lint script for strict-convention strings (Planner-authoring mechanization). Three Planner-authoring failures in session 12 shared one root cause: strict machine-checked convention strings (plan header field-line shape, QA Rule 20 banner, `**Deposits:**` block format) authored from memory rather than copied from a known-good artifact, each caught downstream by a gate/validator at the cost of a recovery cycle. **Fix shape:** a pre-deposit lint script (run by Planner before the atomic move into `decisions/`, or as a Bellows intake pre-check) that (a) parse-checks the header via `gates._parse_plan_header` and asserts `dispatch_mode` extracts to `bellows`|`manual_bootstrap`, (b) if the plan has a QA step, greps the QA-step prompt for the canonical `Rule 20 — QA Self-Check Results` / `PASSED — SELF-CHECK PASSED` banner pair, (c) confirms each step's deposits block uses `**Deposits:**` bold-colon form. **Effort estimate:** small-to-medium — ~40-60 LOC standalone script reusing existing `gates.py` parsers; the harder question is where it runs (Planner-side manual invocation vs. Bellows intake gate). A Bellows intake gate would catch the header/deposits failures at claim time (the dispatch-mode validator already does part of this); the QA-banner check can only run at plan-author time since the banner lives in the prompt, not the deposit. **Disposition recommendation:** consider the copy-from-artifact + local-parse-check discipline (now in LESSONS 2026-05-28) sufficient until a fourth occurrence; if it recurs, build the script. Reference: LESSONS.md 2026-05-28 "Strict Bellows convention strings must be copied from a known-good artifact".

- **Added 2026-05-27:** Verdict-response filename prefix tolerance vs. README convention. `bellows/verdicts/README.md` specifies the verdict-response filename strips the leading plan-slug prefix (`in-progress-`, `verdict-pending-`, `executable-`, `diagnostic-`): example `diagnostic-foo-bar-2026-04-16.md` → `verdict-foo-bar-2026-04-16-step-1.md`. Reproduced 2026-05-27 (continuation session, gate 2d housekeeping): authored two verdict files with prefix NOT stripped — `verdict-diagnostic-gate-2d-mapping-v2-2026-05-27-step-1.md` and `verdict-executable-gate-2d-status-advancement-2026-05-27-step-1.md`. Both consumed correctly by Bellows; plans auto-moved to `Done/` on continue-verdict consumption; no `unmatched verdict` errors in daemon log. **Hypotheses:** (1) Bellows matching is suffix-based on `-step-N` + plan-slug substring, tolerating extra prefix segments; (2) the verdict consumer normalizes both sides before comparison and incidentally strips multiple prefix segments. **Risk of observed leniency:** a future plan with a slug legitimately starting with `executable-` or `diagnostic-` (e.g. `diagnostic-bellows-redesign-2026-XX-XX.md`) could create verdict-filename collisions in the same step range. **Fix shape options:** (a) tighten the verdict consumer to enforce the README spec strictly — any non-matching filename is logged `unmatched verdict` and ignored; forces Planner authoring discipline. (b) update the README to document the observed tolerance and underlying matching logic — removes documentation drift without behavior change. **Effort estimate:** small — investigation is ~30 min reading `_consume_verdicts` and the matching predicate; fix is either ~10 LOC tightening or a README rewrite. **Disposition recommendation:** investigate the matching logic first, THEN choose. Reference: LESSONS.md 2026-05-27 entry on verdict-response filename prefix tolerance; verdict files in `resolved/processed-verdict-diagnostic-gate-2d-mapping-v2-2026-05-27-step-1.md` and `resolved/processed-verdict-executable-gate-2d-status-advancement-2026-05-27-step-1.md`.

- **Added 2026-05-27:** `lessons-forge.db` tracked-but-gitignored disposition. The `lessons-forge.db` file is tracked in lessons-forge git (committed since at least the 2026-05-27 cycle ingest, commit `ae08cf7`) BUT `*.db` is also present in `.gitignore`. Behavior inconsistent across sessions: the 2026-05-19 gate 2d status advancement did NOT commit the DB (followed plan-as-written "the file on disk is the state of record"); the 2026-05-27 continuation-session gate 2d advancement DID commit the DB (in lessons-forge commit `d1627253...`, after CEO routing decision). **Disposition needed (one of):** (a) commit the DB on every state change — current de-facto behavior in this session; cost is binary diff churn in commits; (b) un-track the DB and treat disk file as state of record — requires `git rm --cached lessons-forge.db`, a one-time commit removing the file from go-forward history, and an operational decision about how fresh clones bootstrap their DB (one-time seed + cycle re-runs to catch up? distributed via separate seed file?). **Effort estimate:** small (a) — no change, just document the convention. Small-to-medium (b) — un-track is one commit, but bootstrap-for-fresh-clones design is a multi-decision question (seed-file vs ingest-re-run vs cloud-backup). **Disposition recommendation:** not blocking next session; surface as a decision before the next gate 2d cycle or schema migration. Reference: lessons-forge `.gitignore` line `*.db`; lessons-forge git history showing committed DB across recent state changes; LESSONS.md 2026-05-27 entry referencing this BACKLOG.

- **[RETIRED 2026-05-31 → moved to Closed; premise overturned, the renormalization site is absent in current code, removed by `c2aeef4`.]** **Added 2026-05-27:** Orphan-guard renormalization fires on wrong step. `_consume_verdicts` pre-scan renames `processed-verdict-*` files back to canonical `verdict-*` when no paired `verdict-pending-*` plan exists for the slug (the orphan-guard added 2026-05-21 in commit `dc0bdd7` / executable-consume-verdicts-prefix-fix-2026-05-21). The guard's predicate checks slug match but does NOT check step-number match. When a paired `verdict-pending-*` plan exists at step N and a consumed `processed-verdict-*-step-(N-1).md` is also present in resolved/, the orphan-guard incorrectly renormalizes the step-(N-1) consumed verdict back to canonical, the main loop reads it as a fresh continue verdict, re-dispatches step (N-1+1) = step N, agent runs (idempotent so produces same deposit), gates pass, pauses at step N, ~2 minutes later the cycle repeats. Reproduced on `executable-deferred-validation-status-card-2026-05-22` — verdict loop repeated until Planner manually moved the step-1 processed verdict to `bellows/verdicts/pending/archived/`. **Fix shape:** orphan-guard at the renormalization site should also check `step_number == current_plan_step` before renaming. Mechanism analogous to LESSON 2026-05-22 "Pre-scan-style fix-ups must disambiguate filename-overloaded lifecycle states" — same disambiguator-missing root cause, different lifecycle case (with-paired-plan-but-different-step rather than no-paired-plan). **Operational workaround in use:** Planner manually moves prior `processed-verdict-*-step-(N-1).md` to `bellows/verdicts/pending/archived/` before issuing step N verdict; not codified as governance (per CEO decision 2026-05-27 — file as Bellows BACKLOG entry rather than create a temporary Planner workaround rule). **Effort estimate:** small — single-line predicate strengthening at the orphan-guard site + 1-2 regression tests covering the with-paired-plan-but-different-step case. Reference: LESSONS.md 2026-05-22 entry on filename-overloaded lifecycle states; LESSONS.md 2026-05-27 entry on this proposal source (lesson_entries.id=81); Lessons Forge proposal 86 (rejected — fix routed here instead).

- **Added 2026-05-22:** Worktree teardown cherry-pick conflict on dirty `PROJECT_STATUS.md` (sequential-Planner-edit variant). The existing 2026-05-22 entry on parallel-diagnostic teardown conflicts covers the case where two agents both modify `PROJECT_STATUS.md` in their respective worktrees. THIS variant: agent commits a `PROJECT_STATUS.md` update in its worktree, but the local main branch's working tree has uncommitted Planner-side edits to the same file. Teardown's cherry-pick onto local main aborts with `Your local changes to the following files would be overwritten by merge: PROJECT_STATUS.md`. The worktree branch already pushed direct to origin; the local main never receives the QA commit; the deposit file (QA report) doesn't exist on local filesystem despite Bellows' `deposit_exists` gate passing (because that gate checks the worktree, not local main post-teardown). Observed 2026-05-22 on `executable-fuel-bracket-floor-only-phase-a-2026-05-22` Step 3. **Recovery applied:** Planner committed the dirty local edits (`b274d1d`), cherry-picked the QA worktree commit (`b142e4a` → local `42d571f`), pushed origin, then issued continue verdict so Bellows' next teardown attempt would succeed. **Resolution shape (options):** (a) teardown auto-stashes local changes before cherry-pick, then unstashes (handles the common case but loses changes on conflict during unstash); (b) teardown detects dirty working tree before attempting cherry-pick and produces a clearer pause-for-CEO with explicit recovery instructions, rather than the cryptic cherry-pick conflict surfaced today; (c) Planner discipline rule — NEVER leave uncommitted PROJECT_STATUS.md edits between session checkpoints (operational mitigation today, no Bellows code change required). **Effort estimate:** option (b) is small (~20 LOC pre-cherry-pick dirty-tree check), option (a) is medium with risk, option (c) is free but discipline-dependent. **Operational impact:** Planner-side recovery surgery required on every occurrence; QA deposits are written but not visible to subsequent plans until manual cherry-pick lands. Reference: this session's verdict-request `verdict-request-fuel-bracket-floor-only-phase-a-2026-05-22-step-3.md` with worktree_teardown gate failure.

- **Added 2026-05-22:** Parallel-diagnostic cherry-pick conflicts on shared bookkeeping files at teardown. Two diagnostics (`diagnostic-background-job-infrastructure-inventory-2026-05-22` and `diagnostic-match-lane-query-pattern-anatomy-2026-05-22`) dispatched in parallel against invoice-pulse. Each ran in its own worktree. Each appended a session entry to `PROJECT_STATUS.md` and a feedback entry to `knowledge/research/agent-prompt-feedback.md`. Teardown serializes worktree merge-back via cherry-pick onto local main. First diagnostic cherry-picked cleanly (commit `8513e17`); second hit cherry-pick conflict on both shared files at the same byte ranges (commit `a3426f0` aborted). Bellows surfaced via `worktree_teardown` gate failure even though all substantive gates passed. Deposit file was net-new but never landed on main because cherry-pick aborted as a unit. Recovery required manually extracting deposit content from the worktree branch and re-applying additive PROJECT_STATUS and feedback log changes via Python script. **Why this is structural:** `PROJECT_STATUS.md` and `agent-prompt-feedback.md` are project-wide append-only bookkeeping files that EVERY plan touches. Any two plans running in parallel within a project conflict at teardown regardless of source-code overlap. **Resolution shape:** detect cherry-pick conflict on known append-only files (`PROJECT_STATUS.md`, `agent-prompt-feedback.md`) at teardown time, and resolve by appending both worktree additions onto main rather than aborting. Could be a teardown-stage `git mergetool`-style auto-resolution or a Python diff merger using the conflict markers. **Operational mitigation today (now in `/Users/marklehn/Developer/GitHub/LESSONS.md` 2026-05-22 `planner-discipline` tag):** Planner serializes same-project plans by default; "parallel" CEO instructions interpreted as between-projects unless explicitly overridden. **Effort estimate:** medium — touches teardown code path, needs careful conflict-marker parsing and idempotency, regression tests on multi-plan scenarios. **Disposition recommendation:** defer until second occurrence demonstrates the LESSONS-based discipline alone is insufficient. Reference: LESSONS.md 2026-05-22 "Parallel diagnostics against the same project conflict at teardown on shared bookkeeping files" entry.

- **Added 2026-05-21:** Bellows status UI — replace terminal-only output as the primary observability surface. Idea from CEO during 2026-05-21 session: the terminal log requires scrolling and parsing to understand what's currently happening; a UI that displays per-plan step status at a glance (which plans are in-flight, paused, awaiting verdict, by step) would be a meaningfully better daily-use experience. Terminal log remains available as a secondary view. Open design questions before scoping: (a) deployment shape — web (Flask already in use, browser tab) vs Tauri desktop app vs macOS menu-bar widget vs ncurses TUI in same terminal; (b) data source — read directly from `bellows.db` runs table + filesystem state (decisions/*, verdicts/pending/*, verdicts/resolved/*) vs Bellows daemon exposing a status endpoint; (c) update mechanism — polling (1-5s refresh) vs websocket vs server-sent events vs filesystem watch on the UI side; (d) scope of v1 — just per-plan status table, or also include step-level progress, gate failure highlights, log tail per plan, click-through to deposit files. Likely 1–3 days of work depending on shape choices. Worth a separate planning session — not snap-decided. Reference: CEO chat 2026-05-21 ~22:30 session-wrap.

- **Added 2026-05-21:** Deposits parser does not strip parenthetical qualifiers (Priority 3 audit). `_extract_plan_required_deposits` at `gates.py:380` and `_gate_deposit_exists` agent-declared path parser at `gates.py:323` both use backtick-only regex (`r'`([^`]+)`'`) — parenthetical text outside backticks is harmlessly ignored, but parenthetical text inside backticks (e.g., `` `knowledge/foo.md (volunteered)` ``) is captured into the path string and causes a clear `deposit_exists` gate trip (recoverable via Rule 22 override). Rule 26 governance prevents the Planner from authoring the problematic pattern; no documented incident exists. **Disposition recommendation:** small — add `re.sub(r'\s*\([^)]*\)\s*$', '', path)` strip at both call sites if a Planner-authored parenthetical incident ever occurs. Defer until then. Reference: `Done/diagnostic-priority-3-carryover-audit-v2-2026-05-21.md`, SA findings at `logs/20260521-093802-step.json`.

- **[SHIPPED 2026-05-31 → moved to Closed.]** **Added 2026-05-21:** No-match verdict warning rate-limit (Priority 3 audit). `_consume_verdicts` at `bellows.py:1280` emits `⚠️ no verdict-pending plan found step {N} — leaving in resolved/ for retry` every 30 seconds (rescan_interval) per unresolved verdict file in `resolved/`. Without dedup, a single stuck file produces ~2,880 warning lines per day. Adjacent stale-verdict warning at `bellows.py:1278` is self-limiting (file moved to processed on first detection). The S3 Bug A fix (commit `dc0bdd7`, exclusion filter for `verdict-request-*` files) eliminated the primary source of stuck files, making accumulation rare. **Disposition recommendation:** small — add `_warned_no_match: set` dedup guard (log once per file, clear on match or startup). Low priority; no documented flooding incident. Reference: `Done/diagnostic-priority-3-carryover-audit-v2-2026-05-21.md`, SA findings at `logs/20260521-093802-step.json`.

- **Added 2026-05-13:** `_extract_step_text` regex case-sensitivity — canary plan header convention secondary defect. Originating context: Lessons Forge proposal 8 (entry 8, ratified into PLANNER_TEMPLATE v4.40 Rule 26 as the canary header convention). The governance rule documents the symptom — mixed-case `## Step 1` headers silently bypass the dispatch path because Bellows's `_extract_step_text` regex matches only uppercase `## STEP N`. The Bellows-side fix would be a regex flag change (`re.IGNORECASE` on the relevant pattern), but: (a) the governance rule already prevents the failure mode at plan-write time, so the Bellows fix is now a quality-of-life improvement rather than a load-bearing defect, (b) loosening regex case-sensitivity may introduce unintended matches in plan prose that mentions `step 1` lowercased, requiring scope analysis before shipping. **Disposition recommendation:** defer until a second occurrence demonstrates the governance rule alone is insufficient, or until a plan-authoring session lands a lowercase header through human error despite the rule. **Effort estimate:** small if scope analysis confirms no prose collision risk (1-line regex flag change + 2-3 tests); medium if collision risk requires regex anchoring or fence-stripping. Reference: PLANNER_TEMPLATE v4.40 Rule 26 canary header convention paragraph (governance-root commit c2c7ad3); Lessons Forge proposal 8 reasoning.


---

## Closed

- **Closed 2026-06-01:** Worktree teardown→resume regression — Gap 1(b) (continue silently advances past a failed teardown). Shipped via `Done/executable-block-continue-over-worktree-teardown-failure-2026-06-01.md` (DEV+QA on default Opus). Added an 18-line guard at the TOP of the `if v == "continue":` branch in `_consume_verdicts` (`bellows.py`), before the final/non-final split, so it covers BOTH inter-step resume AND final-step continue-to-done. Predicate: `any(f.get("gate") == "worktree_teardown" for f in gate_result.get("failures", []))` against the verdict-request's parsed Gate Result JSON; "uncleared" = present in `failures`. On trip: loud `_log("ERROR", ...)`, `verdict.log_to_ledger(..., "continue-blocked-worktree-teardown", ...)`, `shutil.move` plan to `halted-` (NOT advance, NOT `Done/`), `_cleanup_verdicts_for_slug` + `_seen.discard` + `_delete_shadow`, `notifier.notify_plan_halted`, then `break` (existing `plan_matched` block moves the verdict to `processed-`). Recovery is manual R2 — out of scope by design. 3 regression tests (inter-step block, final-step continue-to-done block, no-false-trip clean continue). Commits on local `main`: `8b2f952` (fix) + `2652ce1` + `20cfe4d` (QA) + `c991a6a`. **Daemon restart required to activate** (change is in `_consume_verdicts`). `_teardown_worktree`/`_create_worktree` byte-unchanged. Full suite 437 passed / 5 pre-existing carry-over / zero new. **scope_check note:** the DEV step paused on a false-positive scope_check FAIL because the plan body did not literally name `tests/test_consume_verdicts.py` as a deposit target (Planner over-applied the "don't name test paths from memory" rule, which is about assertions not deposit targets) — Planner verified the file held only the 3 intended tests and issued a documented override continue. **A/B baseline (organic Opus):** DEV 31 turns / ~349s / $1.38; QA 47 turns / ~283s / $1.67 — DEV slower in wall than last session's Sonnet clean run (286s) and above the Opus median (184s), but a heavier task (3 tests + lifecycle routing vs the dedup's single edit), so not like-for-like; routing-speed thesis stays under-evidenced. **Remaining family (still Open):** Gap 1(c), Gap 2 (preserve un-landed commits — recommended next), Gap 3.

- **Closed 2026-05-31:** No-match verdict WARN rate-limit (originally 2026-05-21). Shipped via `Done/executable-no-match-verdict-warning-dedup-2026-05-31.md` (DEV+QA on Sonnet — first Opus→Sonnet routing A/B). Added module-level `_warned_no_match: set[str]` (mirrors `_NOTIFIED_MISPLACED`); the no-match `else` branch in `_consume_verdicts` now logs only when `fname not in _warned_no_match` and adds it; `.discard(fname)` at both processed-move sites (matched + stale) clears on leave; module-level scope makes startup-clear automatic. 2 regression tests (`test_no_match_warning_logged_once`, `test_no_match_warning_cleared_when_file_leaves_resolved`). Commits on local `main`: `e38b958` (fix) + `a004270`/`7edb969`/`3663e1a`. **Daemon restart required to activate** (change is in `_consume_verdicts`). **A/B note:** Sonnet ran above the Opus all-step median on both DEV runs (37 turns/286s clean; 41/348s first attempt) — leaning against the routing-speed thesis; like-for-like Opus baseline deferred to the next mechanical item. **Incident note:** the first attempt cascaded via the worktree teardown→resume regression (see Open 2026-05-31 worktree entry); the clean re-run shipped it after clearing a stray untracked file from the repo root.

- **Closed 2026-05-31 (RETIRE — structurally resolved):** Orphan-guard renormalization fires on wrong step (originally 2026-05-27). The targeted site — `_consume_verdicts` pre-scan renaming `processed-verdict-*` back to canonical `verdict-*` — does not exist in current code. Verified 2026-05-31 by direct read of `bellows.py` (`_consume_verdicts`, `_scan_misplaced_verdicts`, `_perform_startup_sweep`) and all of `verdict.py`: the consume loop selects only `verdict-*` and skips `verdict-request-*`; terminal state `processed-verdict-*` cannot re-match; nothing renames processed→canonical. Removed by the 2026-05-24 P0 pre-scan removal (commit `c2aeef4`, Shape 1c). The 2026-05-27 entry was authored from the 2026-05-22 incident symptom + rejected Lessons Forge proposal 86, after `c2aeef4` landed, without re-checking the code. Not reproducible on current code. **No code changes.** Same "authored from mental model, not current code" class as the 2026-05-26 retirement cluster.

- **Closed 2026-05-29 (session 19):** `_create_worktree` stranded-worktree cleanup (was Open 2026-05-29 session 18, item a). Shipped via `Done/executable-worktree-precheck-hardening-2026-05-29.md` (DEV `93b18ec`, QA `77417a6`). `_create_worktree` now detects a pre-existing `wt_path` and cleans it (`git worktree remove --force` → `shutil.rmtree(ignore_errors=True)` → `git worktree prune`, WARN logged) before `git worktree add`, mirroring the `__init__` startup-prune. 2 regression tests (stranded dir, stranded registered worktree). Daemon restarted to activate. Notes: this plan's own teardowns ran clean (no stranded-worktree cascade), and the (n) orphan-guard workaround (archive the step-1 processed verdict before the terminal verdict) was exercised live at close with no replay.

- **Closed 2026-05-29 (session 19):** `_LIFECYCLE_IGNORE_RE` `strip()`→`rstrip()` for space-prefixed porcelain codes (was Open 2026-05-28 session 14, item f). Shipped in the same executable. Pre-check now uses `.rstrip()` (bellows.py:906-907) preserving the leading status-code space; `_is_lifecycle_artifact` extracts via `porcelain_line[3:]` (handles the rename form), so the rstrip alone closes the bug. 1 regression test (space-prefixed lifecycle line + negative control).

- **Closed 2026-05-29 (session 19):** Pre-check trips on `.bellows-worktrees/` (3 `test_worktree.py` failures) (was Open 2026-05-28 session 14, item g). Shipped in the same executable via the **filter-extend** route (CEO-locked, NOT the fixture-gitignore route): `_LIFECYCLE_IGNORE_RE` gained `^\.bellows-worktrees(/|$)`. The 3 prior failures (`test_teardown_removes_worktree_directory`, `test_teardown_cherry_picks_commits`, `test_teardown_copies_uncommitted_files`) now pass; 1 regression test + negative control (`bellows-worktrees-imposter` not matched). Full suite 425→432, zero new regressions, blocking behavior preserved (`test_teardown_aborts_on_cherry_pick_conflict` green).

- **Closed 2026-05-29 (session 19):** Vestigial `mv` claim-rename instruction in plan prose (was Open 2026-05-28 session 14, item i). Shipped via PLANNER_TEMPLATE v4.57 (governance `c416f0c`): removed the claim-rename from the canonical step template AND the SA-blueprint liveness template, replaced with the liveness-anchor message (Bellows owns the `executable-`→`in-progress-` claim; manual_bootstrap needs no rename). Verified by dogfood — the worktree-precheck executable's DEV step produced **0** intermediate-decision phrase-matches.

- **Closed 2026-05-29 (session 19):** PLANNER_TEMPLATE Rule 25 routing entry for `worktree_teardown_dirty_tree` (was Open 2026-05-28, item j). Shipped via v4.57 (governance `c416f0c`): added a gate_failure evidence-string discrimination note under Rule 25 distinguishing `worktree_teardown_dirty_tree:` (routine → commit + re-verdict) from `worktree_teardown:` content conflict (R2 cherry-pick surgery). Implemented as a sub-note under `gate_failure`, not a new Pause Reason Code row (both flow through `gate_failure`).

- **Closed 2026-05-28 (session 14):** Dirty-tree pre-check false-trips on Bellows's own lifecycle artifacts (originally 2026-05-28 session 13). Shipped via diagnostic + executable + QA arc this session. **Diagnostic (`Done/diagnostic-dirty-tree-precheck-false-trip-surface-2026-05-28.md`):** mapped the false-trip surface, produced filter-rule table at `knowledge/research/dirty-tree-precheck-false-trip-surface-2026-05-28.md` Section 3 (regex + predicate spec) and Section 6 (test surface). Explicitly disposition'd BACKLOG #2 (teardown-stranded worktree cascade) as a downstream symptom of #1 — not a separate failure mode. **Executable (`halted-executable-dirty-tree-precheck-false-trip-filter-2026-05-28.md` — halted Planner-direct after DEV substance landed via R2 recovery, QA step never ran due to filter not yet being loaded in the running daemon).** DEV commit `7bb05ae`: `feat: add lifecycle-artifact filter to dirty-tree pre-check`. Added `_LIFECYCLE_IGNORE_RE` (regex covering `in-progress-*`, `verdict-pending-*`, `halted-*`, `processed-verdict-*`, `verdict-request-*`, `executable-*`, `diagnostic-*` in `knowledge/decisions/`, `knowledge/decisions/Done/`, `verdicts/pending/`, `verdicts/resolved/`) at `bellows.py` lines 32-51 and `_is_lifecycle_artifact()` predicate at lines 43-51. Modified pre-check at lines 885-916 to filter lifecycle artifacts before raising. **QA (`Done/executable-dirty-tree-precheck-filter-qa-2026-05-28.md` — halted Planner-direct, substance shipped at commit `3d79151`).** 3 filter-positive tests (lifecycle artifacts ignored), 4 filter-negative tests (CRITICAL SAFETY — real dirty conditions still block), 1 regex coverage test, regression check (425 passed, zero new failures). Rule 20 self-check PASSED. **Confirmed by local pytest run at commit `f8edacc`:** 429 passed, 4 pre-existing failures only (1x test_run_step_timeout + 3x test_worktree.py — see BACKLOG follow-on). **Smoke-test verification at commit `d4d7b56`:** first fully clean dispatch (no R2 recovery needed) confirms filter operational in live daemon. **Follow-on BACKLOG entries:** (1) `rstrip()` instead of `strip()` for space-prefixed porcelain codes — false-strict edge case, safe direction; (2) `.bellows-worktrees/` pre-check trip on test fixtures — directory-prefix pattern outside filter coverage. Reference: `Done/diagnostic-dirty-tree-precheck-false-trip-surface-2026-05-28.md`, `halted-executable-dirty-tree-precheck-false-trip-filter-2026-05-28.md`, `Done/executable-dirty-tree-precheck-filter-qa-2026-05-28.md`, QA at `knowledge/qa/executable-dirty-tree-precheck-filter-qa-2026-05-28.md`, smoke test at `Done/executable-smoke-daemon-connectivity-2026-05-28.md`.

- **Closed 2026-05-28 (session 14, subsumed by parent fix):** Failed teardown strands the worktree, blocking the next step's worktree creation (cascade) (originally 2026-05-28 session 13). Diagnostic Section 4 (Done/diagnostic-dirty-tree-precheck-false-trip-surface-2026-05-28.md) verified the cascade as downstream symptom of the dirty-tree false-trip — not a separate failure mode. With the lifecycle filter live at commit `7bb05ae`, teardown completes normally on lifecycle artifacts and the worktree is removed at the end of step 1; step 2's `_create_worktree` succeeds because there is no stranded worktree at the target path. Smoke-test dispatch at `d4d7b56` confirmed: single worktree, no orphans, teardown clean. **No separate fix required.** Reference: same as parent entry above; smoke-test plan in `Done/executable-smoke-daemon-connectivity-2026-05-28.md`.

- **Closed 2026-05-28 (session 14, deferred — superseded by smoke-test confirmation):** QA regression tests for the `_seen.discard`-on-rejection fix (originally 2026-05-28 session 13). The original deferral was conditional on the dirty-tree pre-check false-trip fix landing first — that landed this session at commit `7bb05ae`. The fix itself (commit `0322124`, dispatch-mode-validator rejection-path `_seen.discard`) remains shipped-but-untested-in-suite. However, the rationale for filing as Open ("deferred until the dirty-tree precondition is closed so the QA plan's own teardown doesn't loop") is no longer load-bearing — the smoke test dispatch this session demonstrated the lifecycle filter operates correctly in the live daemon. The 2-LOC change mirrors the line-668 auto-close precedent exactly, so behavioral regression risk on the `_seen.discard` fix is low. **Disposition:** closing as deferred indefinitely. The regression test surface from `knowledge/research/rescan-miss-disposition-2026-05-28.md` Section 4 (slug-cleanup-on-rejection + cross-plan-type slug collision) remains valid and could be picked up if a future session opens the dispatch-mode-validator code path — but is not standalone-justified given low risk and the working live evidence. **Revisit trigger:** future regression observed at the rejection path. Reference: original Open entry text (now removed); `knowledge/research/rescan-miss-disposition-2026-05-28.md` Section 4 (test surface preserved); commit `0322124` (the fix).

- **Closed 2026-05-28 (session 13, RE-FRAMED — premise overturned, root cause fixed):** Watcher misses `move_file`-deposited plans — periodic `decisions/` rescan as self-heal (originally Added 2026-05-28 session 12). **The entry's premise was wrong on every count.** Disposition diagnostic `Done/diagnostic-rescan-miss-disposition-2026-05-28.md` (findings: `knowledge/research/rescan-miss-disposition-2026-05-28.md`) established: (1) the periodic `decisions/` rescan the entry proposed adding ALREADY EXISTS — `_rescan` (bellows.py:1156) runs every 30s on the heartbeat loop (bellows.py:1462-1464) and its general-sweep block (bellows.py:1172-1177) scans every watched `decisions/` for all `is_runnable_plan` files and calls `_handle(..., from_rescan=True)`, present since 2026-05-15. The proposed fix was dead code. (2) The entry's "_seen-aware filter" suggestion was ironic — `_seen`-awareness is what CAUSED the miss, not what was missing. **Actual root cause (hypothesis (b), confirmed against the session-12 log):** the dispatch-mode validator rejection path (bellows.py:386-394) was the ONLY early-exit in `run_plan` that did not call `bellows._seen.discard()`. A v1 diagnostic rejected for a missing Dispatch Mode header stranded its slug in `_seen`; because `slug_from_path` strips both `diagnostic-`/`executable-` prefixes, the follow-on executable sharing the base name inherited the stranded slug and was silently skipped at the `_handle` `_seen` guard (bellows.py:1046) on every rescan tick until the manual restart cleared the in-memory set. **Fix shipped this session:** commit `0322124` adds `if bellows is not None: bellows._seen.discard(verdict.slug_from_path(plan_path))` to the rejection path, mirroring the line-668 auto-close precedent. Planner-verified by direct code read. **QA regression tests deferred** — see Open entry "QA regression tests for the `_seen.discard`-on-rejection fix" (the executable was closed Planner-direct after the dirty-tree pre-check trapped it in a recovery loop). **Pattern note:** this is another recurrence of "BACKLOG entries authored from current-state observation without scanning the code that explains them" — the entry was authored from the session-12 symptom (plan unclaimed ~9 min) without checking that the rescan it proposed already existed. Caught by the diagnostic-first discipline (Rule 10). Reference: `Done/diagnostic-rescan-miss-disposition-2026-05-28.md`, `knowledge/research/rescan-miss-disposition-2026-05-28.md`, `Done/executable-seen-discard-rejection-path-2026-05-28.md`, commit `0322124`.

- **Closed 2026-05-27:** `ceo_flags` gate false positive on null-declaration prose content (originally 2026-05-27). Shipped via `executable-gate-fp-coordinated-shape-2026-05-27` following diagnostic `diagnostic-gate-fp-coordinated-shape-2026-05-27` (findings at `bellows/knowledge/research/gate-fp-coordinated-shape-2026-05-27.md`). **Fix shape chosen:** Option (a) — null-token allowlist via new `_is_null_flag_declaration(text: str) -> bool` helper at `gates.py:73`, consumed by `_gate_ceo_flags` at `gates.py:247` via filter comprehension. Regex `^(none|n/a|no flags?|nothing|clean|no issues)\b` (case-insensitive) handles prose-null pattern with word-boundary tolerance for trailing explanatory content. 3 new regression tests. **Daemon restart confirmed 2026-05-27 20:14** (CEO-initiated). Reference: `Done/diagnostic-gate-fp-coordinated-shape-2026-05-27.md`, `Done/executable-gate-fp-coordinated-shape-2026-05-27.md`, QA at `knowledge/qa/executable-gate-fp-coordinated-shape-2026-05-27.md`.

- **Closed 2026-05-27:** `rule_22_verification` (c) sub-check false positives on enumerative tables in QA reports (originally 2026-05-27). Shipped via `executable-gate-fp-coordinated-shape-2026-05-27`. **Fix shape chosen:** Option (b) status-cell heuristic via defer-and-discard pattern at `gates.py:558-611`. Table-scoped state (`current_table_failures: list`, `current_table_has_positive_row: bool`) accumulates failures during table iteration; at table-end (3 transition points plus final-flush) deferred failures commit only if the table had at least one positive-status row. Enumerative tables discard all deferred failures. The `❌` immediate-fire branch preserved unchanged. **Diagnostic-vs-BACKLOG divergence:** BACKLOG suggested option (a) section-scoping refinement; diagnostic identified that existing 2026-05-24 section-scoping IS the cause (too-coarse `## ` header matching), not absent. Option (b) is content-intrinsic and self-certifying. **Daemon restart confirmed 2026-05-27 20:14**. Reference: same as above.

- **Closed 2026-05-27:** Hedging-detector false positives on domain terminology in QA verification tables (originally 2026-05-27; root pattern observed 2026-05-22 on `executable-deferred-validation-status-card-2026-05-22` Step 4). Shipped via `executable-gate-fp-coordinated-shape-2026-05-27`. **Fix shape chosen:** Option (c) cell-scope match via new `_hedging_in_status_vicinity(line: str, keyword: str) -> bool` helper at `gates.py:94`; the (d) loop at `gates.py:613-629` replaces whole-row `if kw in lower:` with cell-scoped match (helper splits row by `|`, identifies status-bearing cells via positive-status token matching, restricts keyword search to status cells only). Plus section-scoping extension via `in_verification_section_d` tracking. **DEV-vs-spec divergence (accepted):** DEV chose stricter cell-scope than diagnostic spec — match only in cells containing positive-status tokens, not 'adjacent cells'. Reasoning: adjacent description cells would re-introduce the FP. **Diagnostic-vs-BACKLOG divergence:** BACKLOG suggested option (a) word-boundary + context; diagnostic identified word-boundary matching doesn't close the FP. Two existing-test fixtures updated (`test_rule_22_qa_hedging_keyword`, `test_rule_22_qa_both_fail_and_hedging`); 133 tests pass. **Daemon restart confirmed 2026-05-27 20:14**. Reference: same as above.

- **Closed 2026-05-27 (RETIRE — term-matching approach insufficient):** Leftover-after-ship automated detection (originally surfaced 2026-05-26 as 5th recurrence of "leftover after ship" pattern, never formally captured in BACKLOG.md before close — backdated). Goal was to automate the manual catch the Planner runs at Phase 1.5 (grep BACKLOG Open vs Closed + recent commits + recent PROJECT_STATUS for already-shipped entries). Two implementation attempts shipped in one session, both halted at Rule 22 (b) substance check. **v1** (commit landed `scripts/check_backlog_freshness.py` 239 LOC, halted at QA): 4-source matching (BACKLOG Open fingerprint, BACKLOG Closed duplicate, PROJECT_STATUS Completed slug, git log closure-signal), 4 ground-truth recurrences all caught, but live run flagged 6/6 currently-Open entries as `investigate-as-shipped` with 39 candidates — title-word matching on generic shared vocabulary (`project`, `status`, `diagnostic`, `teardown`, `files`, `deposits`) produced systemic false positives. SA's blueprint hand-traced FP analysis was wrong: verified fingerprint overlap in the abstract but did not compute the cross-product against all Closed entries and PS slugs. **v2** (script edited in place to 254 LOC, halted at DEV review): redesigned for high-distinctiveness term extraction (backtick identifiers ≥ 5, hyphenated compounds ≥ 12, underscore identifiers ≥ 8 with ≥ 2 underscores, executable slugs) with title-word matching dropped. Live run: 4 ground-truth still caught, but output traded one FP source (BACKLOG Closed title-word noise) for another (PROJECT_STATUS slug-token noise from generic 6+ char tokens `bellows`, `planner`, `template`, `governance`). Net candidates 39 → 37, entries flagged still 6/6. Blueprint's Section 3 FP-validation only re-traced v1-flagged candidates through v2 rules — did not enumerate NEW candidates v2's lowered PS threshold would surface. SA's own "Flags for Next Step" called this risk out explicitly; Planner underweighted it. **Root limit:** term-matching is the wrong approach. Entries sharing high-distinctiveness identifiers (same function name across different bugs, shared project names, slug-token substring collisions) are inherently indistinguishable to term matching without semantic understanding of whether two entries describe the same bug. Tightening thresholds further regresses ground-truth catches. The manual Phase 1.5 grep is the working solution. **v2 script left on disk** as halted experiment at `scripts/check_backlog_freshness.py` — runs cleanly, catches all 4 ground-truth recurrences in trace, ~60% candidate-count reduction vs v1 but no improvement in entries-flagged rate. **Revisit trigger:** capability-addition framing only — if a semantic-comparison primitive becomes available (LLM-based same-bug detection, embeddings for entry similarity), reconsider. Pure term-matching iterations should not be reopened. **Session lessons:** (1) plan-write-time SA dense-content steps consistently timed out at ~600-730s with empty agent output (3 occurrences this session: 1100w SA blueprint, 700w SA blueprint, 600w SA blueprint-fill); successful SA steps had explicit early-output instructions like "post a 3-5 line plan-of-attack before drafting" plus per-file-read acknowledgments — the verbose acknowledgments keep the inactivity timer warm. Pattern is reproducible. (2) Bellows scope_check FAILs when the script's deposit-file date differs from the plan's expected-date because the script uses today's date (`backlog-freshness-check-<DATE>.md`) — second-day runs of a script that overwrites a same-date report appear as new-file creations. (3) Blueprint hand-traces validating "what to catch" without stress-testing "what to exclude" miss the asymmetry that drives false positives in production runs; SA Section 3 should always enumerate NEW candidates the algorithm would surface, not just re-trace existing ones. **No code changes shipped to mainline** — script is uncommitted/halted, can be reverted via `git checkout bellows/scripts/check_backlog_freshness.py` or left for reference. Reference: `halted-executable-leftover-after-ship-tooling-2026-05-26.md`, `halted-executable-leftover-after-ship-tooling-implementation-2026-05-26.md`, `halted-executable-freshness-check-algorithm-rework-2026-05-26.md`, `halted-executable-freshness-check-algorithm-v2-blueprint-2026-05-27.md`, `halted-executable-freshness-check-algorithm-v2-implementation-2026-05-27.md`, `Done/executable-freshness-check-algorithm-v2-blueprint-retry-2026-05-27.md`, blueprint at `knowledge/research/freshness-check-algorithm-v2-blueprint-2026-05-27.md`, v1 dev log at `knowledge/development/leftover-after-ship-tooling-implementation-2026-05-26.md`, v2 dev log at `knowledge/development/freshness-check-algorithm-v2-implementation-2026-05-27.md`.

- **Closed 2026-05-26 (defer pending demonstrated need):** `WebSearch` and `WebFetch` not in Bellows-dispatched agents' `--allowedTools` list (originally 2026-05-22). Phase-1.5 audit verified the prior `permission-denial-history-audit-2026-05-22.md` findings against current `parsed.permission_denials` data: structured WebSearch/WebFetch denial events total **3 in 30 days**, all in a single log file (`logs/20260522-104929-step.json`) — the `diagnostic-claude-settings-permission-gap-2026-05-22` Step 1 incident the BACKLOG entry already cited. **No further incidents** in the 4 days since, including this session. The 2 SA queries and 1 WebFetch URL the SA attempted were all for Anthropic Claude Code docs; the diagnostic's substantive findings were derivable from prior Bellows research files (the audit explicitly confirmed this). The BACKLOG entry's own framing of question (b) — "the deeper question is whether the Planner should be authoring SA-investigation plans without web access in the first place" — answers itself by the data: the Planner's existing pattern of pre-loading external docs into research files before SA dispatch is sufficient. **Disposition:** defer. **Revisit trigger:** second incident demonstrating the pre-load workaround is insufficient (specifically: an SA step where the substantive finding genuinely required live web access AND the Planner had no reasonable way to pre-fetch the relevant content). At that point re-open with capability-addition framing including (a)/(b) decision: uniform vs role-conditional allowance. **No code changes shipped today** — BACKLOG hygiene closure only. **Methodology lesson surfaced this session (Forge candidate):** initial log-mining attempt used `grep WebSearch logs/*.json` which matched 676 files because `WebSearch` appears in agents' tool-registry text echoed back in `raw_output`, producing a ~22× false-positive count. Correct extraction is via `json.load()` + scan of `parsed.permission_denials` array for `tool_name in ("WebSearch", "WebFetch")`. Generalizable: when grepping agent step logs for denial events, parse JSON structure; don't rely on string matching against `raw_output`. Reference: `bellows/knowledge/research/permission-denial-history-audit-2026-05-22.md` (gate-level inventory + bucket (d) events 8-9).

- **Closed 2026-05-26 (RETIRE — structurally resolved):** MCP tool denials (`mcp__vexp__run_pipeline`, `mcp__vexp__get_context_capsule`) not on `READ_CLASS_TOOLS` exemption list (originally 2026-05-22). Retired as structurally resolved by `executable-mcp-read-class-tools-extension-2026-05-25` (DEV commit `9473cf7`, shipped 2026-05-25 — before session 6 of 2026-05-26). The fix extended `READ_CLASS_TOOLS` at `gates.py:35-43` from 4 entries (`Grep`, `Glob`, `Read`, `mcp__vexp__run_pipeline`) to 9 entries by adding `mcp__vexp__get_context_capsule`, `mcp__vexp__get_session_context`, `mcp__vexp__get_skeleton`, `mcp__vexp__index_status`, `mcp__vexp__search_memory` — covers both tools the BACKLOG entry named PLUS three additional vexp read-class tools. Deliberately excludes `mcp__vexp__save_observation` (write-class). 6 regression tests landed in `tests/test_gates.py` (5 positive + 1 critical negative for save_observation). Commit message literally reads "closes BACKLOG mcp_tool_denials." **No code changes shipped today** — BACKLOG hygiene closure only. The entry was authored 2026-05-22 and the fix landed 2026-05-25, but the Open entry was not moved to Closed at that time; it propagated through three session batons (sessions 5, 6, 7 of 2026-05-26) as a live horizon item before catching at session-8 Phase 1.5. **Pattern note:** fourth recurrence in three days of LESSONS 2026-05-26 "BACKLOG entries authored from current-state grep without scanning Closed history can misframe already-evaluated work." Previous catches: Item 2 (set→list) caught by session-7 freshness diagnostic; precondition-failure-field duplicate retired same session; Phase 3b read-side retired same session. Today's catch fired at the very first grep — `READ_CLASS_TOOLS` already contained the named tools. Cost of grep: ~5 seconds. Cost of not grepping: would have authored a diagnostic + executable to ship code that already exists. Discipline rule remains: pre-write grep of Closed section AND current code state before treating any baton-carried entry as live. Reference: `bellows/knowledge/decisions/Done/executable-mcp-read-class-tools-extension-2026-05-25.md`, `bellows/knowledge/development/mcp-read-class-tools-extension-2026-05-25.md` (dev log), `gates.py:35-43` (current READ_CLASS_TOOLS).

- **Closed 2026-05-26:** Test-isolation orphan pattern — Bellows tests had no `conftest.py` and `verdict.post_verdict_request()` was unmocked, so tests writing through dispatch paths created real `verdict-request-*` files in production `verdicts/pending/` (originally 2026-05-26). Shipped via `executable-bellows-test-isolation-conftest-2026-05-26` (DEV commit `fde737b`, QA commit `8b1fed4`) following diagnostic `diagnostic-bellows-test-isolation-conftest-2026-05-26` (commit `54fb8e1`). **Fix shape chosen:** Option (a) from the original entry — `tests/conftest.py` with a function-scoped autouse fixture (not session-scoped as the entry recommended; SA Deliverable C corrected this because pytest's `monkeypatch` is inherently function-scoped, and the performance cost is negligible). Body: `monkeypatch.setattr(verdict, "VERDICTS_DIR", tmp_path / "verdicts")` — patches the module-level constant that all three verdict-write paths (`post_verdict_request` at line 180, `check_verdict` at line 283, `log_to_ledger` at lines 317–318) read at call time. No production code change required — `VERDICTS_DIR` constant already existed at `verdict.py:14`. **Leakers identified by SA (Deliverable B):** exactly 2, both dispatch-spawn vector — `test_bellows.py::test_apply_defensive_header_defaults_propagates_to_reparsed_header` and `test_consume_verdicts.py::test_dispatch_starts_fresh_when_db_has_orphan_slug_rows`. Both fall to the same mechanism (sparse `gates.check()` mock + `_apply_defensive_header_defaults` injecting `pause_for_verdict="after_step_1"` + un-mocked `verdict.post_verdict_request`). 37 other `run_plan()` callers audited and confirmed safe (28 mock explicitly, 8 single-step auto-close, 1 dense-header). **Load-bearing QA verification:** after full-suite run (411 passed, 5 known carry-overs, 0 regressions) `ls verdicts/pending/ | grep -v "^archived$"` returned empty. Specific-test reproductions of both leakers also produced zero orphans. **Opt-out mechanism: none required** — existing tests in `test_verdict.py` that exercise real `VERDICTS_DIR` write behavior use their own `patch.object(verdict, "VERDICTS_DIR", ...)` inside `with` blocks, which override the conftest fixture for their scope. **Pattern note (Forge candidate):** SA "Total LOC: 7" label in Deliverable C labeled non-blank code lines; the actual prescribed code block is 9 lines (comment + 2 PEP 8 blank lines). Planner copied the 7-line claim verbatim into DEV/QA prompts. QA correctly resolved the mismatch as a counting-convention difference and marked deliverable verification ✅. Worth filing as a Forge observation: "count what you paste, not what the upstream artifact labels." Reference: `Done/diagnostic-bellows-test-isolation-conftest-2026-05-26.md` (SA blueprint), `Done/executable-bellows-test-isolation-conftest-2026-05-26.md` (executable), `knowledge/research/bellows-test-isolation-conftest-fixture-shape-2026-05-26.md` (full audit), `knowledge/qa/executable-bellows-test-isolation-conftest-2026-05-26.md` (QA verification + 4 evidence files).

- **Closed 2026-05-26:** `_gate_rule_20_self_check` ambiguous evidence string at `gates.py:441` (originally 2026-05-26). Shipped via `executable-bellows-hardening-batch-items-1-3-4-2026-05-26` (DEV+QA, commit `af8e7f3`). Single string change at `gates.py:441` from `"no QA deposit contains Rule 20 self-check banner"` to `"deposits block declares no .md paths (check **Deposits:** block format — must be multi-line bullets)"`. Line 464 (banner-not-in-content branch) preserved unchanged. The two failure modes now route Planner to distinct discipline-rules at verdict-read time: line 441 = Planner-authoring failure (malformed `**Deposits:**` block), line 464 = QA-agent discipline failure (banner absent or paraphrased). New regression test `test_rule_20_self_check_distinguishes_no_md_paths_from_missing_banner` covers both branches and asserts the evidence strings differ. Test suite: 411 passed / 5 known carry-overs / 0 regressions. Rule 20 self-check PASSED with 12 evidence files. Reference: `Done/executable-bellows-hardening-batch-items-1-3-4-2026-05-26.md`, QA at `knowledge/qa/executable-bellows-hardening-batch-items-1-3-4-2026-05-26.md`, freshness audit at `knowledge/research/bellows-hardening-batch-freshness-2026-05-26.md`. **Daemon restart required** to load the new evidence string.

- **Closed 2026-05-26:** Bellows path-keyed rejection cache — `_seen` invalidation on `on_modified` (originally 2026-05-22). Shipped via `executable-bellows-hardening-batch-items-1-3-4-2026-05-26` (DEV+QA, commit `af8e7f3`). SA freshness audit re-characterized the BACKLOG's framing: the "rejection cache" was not a dedicated `dispatch_mode_validator` cache but the existing `_seen` set in `PlanHandler` at `bellows.py:1047`, keyed by slug via `verdict.slug_from_path()`. Without invalidation, any rejected plan re-deposited at the same filename was silently skipped on subsequent scans because the slug remained in `_seen`. Fix: `PlanHandler.on_modified` at `bellows.py:1033-1044` now invalidates `_seen` entries on file modification when the file is NOT prefixed with a Bellows-managed lifecycle prefix (`in-progress-`, `verdict-pending-`, `halted-`) — the exclusion guard prevents re-dispatch loops on Bellows's own lifecycle renames. Two new regression tests: `test_on_modified_invalidates_seen_for_runnable_plan` (verifies invalidation) and `test_on_modified_preserves_seen_for_lifecycle_renames` (verifies the guard fires for all three lifecycle prefixes). QA verified lifecycle-prefix list completeness against 4 sources (BELLOWS_DEVELOPER.md, BELLOWS_QA.md, gates.py `SCOPE_ALLOWLIST_PREFIXES`, PROJECT_STATUS.md) — no fourth prefix exists. Reference: same as Item 1 above; SA mechanism re-characterization at `knowledge/research/bellows-hardening-batch-freshness-2026-05-26.md` Item 3 row. **Daemon restart required** to load the new invalidation logic.

- **Closed 2026-05-26:** `_apply_defensive_header_defaults` ineffective at runtime (originally 2026-05-21). Shipped via `executable-bellows-hardening-batch-items-1-3-4-2026-05-26` (DEV+QA, commit `af8e7f3`). Line numbers shifted from BACKLOG-cited values (381/494/502/590) to current values (382/498/506/596) due to intervening 2026-05-2x commits (rename-first ordering, precondition-failure field, Fix F guard removal, deposit-exists normalization); structural claim was fully intact. Fix: added `_apply_defensive_header_defaults(header, total_steps)` call at `bellows.py:499`, immediately after the `header = gate_result.get("plan_header", {})` reassignment at line 498. The re-parsed header now inherits the defensive default before being passed to `header_says_pause()` consumers at lines 506 (intermediate steps) and 596 (final step). Call symmetry with the pre-gate call at line 382 verified by QA. New regression test `test_apply_defensive_header_defaults_propagates_to_reparsed_header` constructs a 2-step plan with sparse header, mocks `gates.check` to return sparse `plan_header`, spies on `header_says_pause` to capture the header, and asserts the captured header contains the defensive default. Existing test `test_run_plan_continuation_prompt_uses_shadow_path` updated to use non-sparse `plan_header` because it was previously passing due to the bug (sparse re-parsed header not getting the default, allowing auto-advance to reach the shadow-path assertion); QA accepted the test fix as a correct behavioral consequence. Reference: same as Item 1 above; SA line-number verification at `knowledge/research/bellows-hardening-batch-freshness-2026-05-26.md` Item 4 row + Q5 verification block. **Daemon restart required** to load the defensive-default propagation.

- **Closed 2026-05-26 (RETIRE — structurally resolved):** `_extract_plan_required_deposits()` returns a `set` making `md_paths[0]` hash-dependent (originally 2026-05-24). Retired as structurally resolved by `executable-extract-plan-required-deposits-set-to-list-2026-05-25` (DEV+QA, shipped 2026-05-25 — before session 6 of 2026-05-26). The fix chose **Option (a)** from this BACKLOG entry: `_extract_plan_required_deposits` and `_filter_transient_paths` now return ordered lists via `paths = []` + `paths.append(...)` + `dict.fromkeys(paths)` dedup, preserving authoring order. The QA report at `bellows/knowledge/qa/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md` confirms (a) the function returns a list, (b) docstring codifies the convention "the QA report is the first `.md` entry in the block," (c) both `md_paths[0]` consumers preserved unchanged — `_gate_rule_20_self_check` at `gates.py:450` (renumbered from 432) and `_gate_rule_22_verification` at `gates.py:505` (renumbered from 493) — and now deterministic. The BACKLOG entry mentioned option (b) (path-suffix matching) as "most robust," but option (a) was chosen as sufficient and lower-LOC; the deterministic-order convention plus single-`.md`-deposit-per-QA-step operational discipline together close the failure mode the entry described. The companion plan `executable-test-rule-26-set-to-list-followup-2026-05-25` (DEV commit `bc1ecd9`) updated 6 set-literal assertions in `tests/test_rule_26_deposit_parser.py` to `set(result) == {...}` for compatibility with the new list return type. **No code changes shipped today** — BACKLOG hygiene closure only. The entry was authored 2026-05-24 and the fix landed 2026-05-25, but the Open entry was not moved to Closed at that time; it propagated through two session batons (session 5 baton 2026-05-26, session 6 baton 2026-05-26) as a live horizon item before catching at session-7 Phase 1.5. **Pattern note:** third recurrence in three days of LESSONS 2026-05-26 "BACKLOG entries authored from current-state grep without scanning Closed history can misframe already-evaluated work" — here the failure mode was symmetric (entry was authored at the time of discovery, but ship-time closure was skipped). Discipline rule remains: pre-write grep of Closed section before treating any baton-carried entry as live. Reference: `bellows/knowledge/decisions/Done/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md`, QA at `bellows/knowledge/qa/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md`, follow-up at `bellows/knowledge/decisions/Done/executable-test-rule-26-set-to-list-followup-2026-05-25.md`.

- **Closed 2026-05-26 (RETIRE — duplicate):** Step-counter loop after precondition-failure verdict (originally 2026-05-21). Duplicate of Closed entry below (`Closed 2026-05-24: Step-counter loop after precondition-failure verdict`) — shipped 2026-05-24 via `executable-precondition-failure-field-2026-05-24` (DEV commit `0a90e26`). The Open entry was a leftover that wasn't cleaned up when the fix shipped. The Closed entry below documents the implementation in full (Shape E precondition-failure verdict-request field, 4 regression tests, 0 regressions, daemon restart confirmed). **No code changes shipped today** — hygiene closure only. **Pattern note:** fourth retirement today in the 2026-05-2x BACKLOG-misframing cluster. Three were premise-wrong (verdict-enrichment roadmap, Phase 3b read-side, teardown push silent failure); this one is a leftover-after-ship duplicate. LESSONS 2026-05-22 "BACKLOG entries authored from session-handoff batons can become stale within the same session" covers this duplicate-after-ship subclass; today's LESSON on current-state-grep covers the premise-wrong subclass. Both subclasses share the same upstream cause: BACKLOG hygiene not running at the right cadence.

- **Closed 2026-05-26 (RETIRE — structurally resolved):** Step 2 (final-step) gate_failure pause does NOT rename in-progress-* to verdict-pending-* (originally 2026-05-22). Retired as structurally resolved by `executable-rename-first-ordering-2026-05-24` (DEV commit `10b5fc3`) — see Closed entry below (`Closed 2026-05-24: Daemon-restart recovery shape AND Step 2 final-step gate_failure rename-skip`). The 2026-05-24 fix explicitly names final-step gate_failure as one of the four pause sites reordered (rename BEFORE verdict-post). The Open entry was a leftover that wasn't cleaned up when the rename-first fix shipped — the 2026-05-24 Closed entry explicitly cites this as one of the two items it closes ("Daemon-restart recovery shape (BACKLOG item #2, originally 2026-05-23) AND Step 2 final-step gate_failure rename-skip (BACKLOG item #3, originally 2026-05-22)"), but the Open entry was not moved to Closed at that time. **No code changes shipped today** — hygiene closure only.

- **Closed 2026-05-26 (RETIRE — structurally resolved):** Daemon-restart recovery shape — in-progress-* plans with verdict in resolved/ not auto-matched (originally 2026-05-23). Retired as structurally resolved by `executable-rename-first-ordering-2026-05-24` (DEV commit `10b5fc3`). The 2026-05-24 fix makes the `in-progress-*` + verdict-in-pending state structurally impossible by ordering rename BEFORE verdict-request post; this also prevents the 2026-05-23 entry's scenario (`in-progress-*` + verdict-in-resolved, which can only arise if the verdict was authored AFTER a daemon restart that found the plan still in `in-progress-*`). Post-2026-05-24, plans transition `in-progress-*` → `verdict-pending-*` atomically with verdict-request post, so daemon-restart cannot find a plan in `in-progress-*` with a paired verdict in either pending/ or resolved/. **Critical note on the entry's documented recovery procedure:** the entry's "manual rename `in-progress-*` → `verdict-pending-*`" recovery was explicitly contraindicated by the parallel 2026-05-22 entry on Step 2 final-step rename-skip (see Closed entry above) — manual rename triggers the watcher's on_moved handler and re-dispatches the plan, compounding the failure. The recovery procedure was already known to be unsafe at the time the 2026-05-23 entry was filed; the entry should have referenced the correct recovery (issue `verdict: stop`, then Planner-direct `Filesystem:move_file` to Done/). **No code changes shipped today** — hygiene closure only. The 2026-05-24 Closed entry already cites this as one of the two items it closes (item #2), but the Open entry was not moved to Closed at that time.

- **Closed 2026-05-26:** Fix F `isinstance(f, dict)` guard at `bellows.py:495` and `bellows.py:587` (originally 2026-05-26). Shipped via `executable-fix-f-guard-removal-2026-05-26` (DEV+QA). Three changes: (1) test fixture `test_run_plan_inprogress_entry_renames_to_verdict_pending` in `tests/test_bellows.py` updated from string-list `["scope_check"]` to production dict shape `[{"gate": "scope_check", "evidence": "test fixture for scope_check failure"}]`; (2) Fix F `isinstance(f, dict)` guard removed from `.join()` log expression at `bellows.py:495`; (3) same guard removed at `bellows.py:587`. The 2026-05-21 isinstance symmetry pattern in the `all()` pause-reason predicate at lines 509 and 600 was correctly preserved as structurally distinct from the Fix F guards. Pre-flight scope check audited all 4 test files referencing `gate_result["failures"]` (`test_bellows.py`, `test_verdict.py`, `test_consume_verdicts.py`, `test_rule_26_deposit_parser.py`); only the one fixture was non-conformant. Production contract `list[dict]` with shape `{"gate": str, "evidence": str}` is now uniformly consistent across `bellows.py`, `gates.py`, `verdict.py`. Test suite: 407 passed / 5 known carry-overs (4 worktree-context `test_decisions.py` + 1 long-standing `test_run_step_timeout`) / 0 regressions. Rule 20 self-check PASSED. **REMINDER: restart Bellows daemon** to load the simplified discriminator. Reference: `Done/executable-fix-f-guard-removal-2026-05-26.md`, dev log at `knowledge/development/fix-f-guard-removal-2026-05-26.md`, QA at `knowledge/qa/executable-fix-f-guard-removal-2026-05-26.md`.

- **Closed 2026-05-26 (RETIRE — premise wrong):** Teardown push silently fails on long-running plans (originally 2026-05-24). Retired as won't-fix per disposition diagnostic `Done/diagnostic-teardown-push-silent-failure-2026-05-26.md` (findings at `knowledge/research/teardown-push-silent-failure-disposition-2026-05-26.md`). The BACKLOG entry's premise — "teardown should be pushing commits to origin" — was the Planner's mental model at authoring time, not the actual code. Fresh grep confirms zero `git push` calls anywhere in the bellows source (unchanged from the 2026-05-21 teardown-git-operations diagnostic finding). `_teardown_worktree` mapped line-by-line: 5 git subprocess calls (symbolic-ref, log, cherry-pick, cherry-pick --abort, worktree remove), all local, zero network operations, zero origin interactions. The 2026-05-24 50-commit local-only accumulation is fully explained by teardown cherry-picking onto local main during 25 P0 loop iterations — the loop is the cause (already Closed), not push absence. **Architecture clarification:** pre-v4.47, agents pushed from plan prose instructions (the mechanism identified by the 2026-05-21 teardown-git-operations diagnostic); post-v4.47, agents are prohibited from pushing; the Planner/CEO session-wrap push has always been the path for commits reaching origin. Bellows-the-daemon has never pushed. No operational gap: session-wrap push is documented and functioning. **Revisit trigger:** if a future design decision introduces a push call into `_teardown_worktree` (e.g., to reduce commit delivery latency between session-wraps), file a new BACKLOG entry with capability-addition framing including error capture, logging, and notification. **No code changes shipped.** **Pattern note:** this is the third 2026-05-24-era BACKLOG entry retired today (after verdict-enrichment roadmap stale-claim and Phase 3b step-state-resume misframing) for the same root cause — entry authored from current-state observation without scanning the architectural shifts that explain it. LESSONS 2026-05-26 "BACKLOG entries authored from current-state grep without scanning Closed history can misframe already-evaluated work" covers the pattern. Reference: `Done/diagnostic-teardown-push-silent-failure-2026-05-26.md`, findings at `knowledge/research/teardown-push-silent-failure-disposition-2026-05-26.md`.

- **Closed 2026-05-26 (RETIRE):** Phase 3b step-state-resume (DB read-side) (originally 2026-05-24). Retired as won't-fix per disposition diagnostic `Done/diagnostic-phase-3b-read-side-step-state-resume-2026-05-26.md` (findings at `knowledge/research/phase-3b-read-side-disposition-2026-05-26.md`). **Critical historical context surfaced by SA:** the read-side was previously shipped 2026-04-28 (`_get_last_completed_step(db_path, plan_slug)` helper + `run_plan()` query integration) and deliberately removed 2026-05-01 per the Phase 3b/3c cost-benefit diagnostic, which confirmed the read-side guarded a manual-rename resume path unsupported by Rule 25 — the supported verdict-resume path passes `resume_step` explicitly without consulting the DB. The 2026-05-24 BACKLOG entry's "half-implemented" framing was imprecise; the accurate description is "fully implemented, evaluated, removed as dead code, write-side retained as benign." **Use-case survey result:** zero concrete use cases found across 4 hardening areas (daemon-restart recovery, cross-validation gates, observability surfaces, 12 named Open BACKLOG items). RV-1 boundary (items #2/#3) closed 2026-05-24 via rename-first ordering; step-counter loop (item #5) closed 2026-05-24 via precondition-failure verdict-request field — neither requires or would have benefited from DB state. **Retirement terms:** `plan_slug` column on `runs` table retained as benign write-only state (one TEXT column per INSERT, no indexes, no constraints, no behavioral risk). **Revisit trigger:** re-evaluate the read-side if a future Bellows hardening item lands that requires cross-validation of step completion against on-disk state, AND filename-prefix state plus verdict-request metadata fields are insufficient for that cross-validation. The 2026-05-21 Status UI BACKLOG entry's potential use of `bellows.db` is for general run-history display, not the step-state-resume cross-validation Phase 3b targeted — if that entry advances, evaluate then whether step-state queries are needed. **Column-disposition at future migration:** if a future schema migration touches the `runs` table, evaluate the `plan_slug` column — retain if a read-side use case has emerged, remove otherwise (updating `record_run` INSERT to drop the column). **No code changes shipped.** Reference: `Done/diagnostic-phase-3b-read-side-step-state-resume-2026-05-26.md`, findings at `knowledge/research/phase-3b-read-side-disposition-2026-05-26.md`.

- **Closed 2026-05-25:** `file_change_audit` false-negative on committed changes (originally 2026-05-21). Shipped via `executable-file-change-audit-fix-2026-05-25` (DEV commit `950436c`). Root cause confirmed by diagnostic H1: `_capture_git_diff` used `git diff --stat` (working-tree-vs-index), blind to committed changes. Fix rewrites `_capture_git_diff` to return HEAD SHA via `git rev-parse HEAD` and `_parse_diff_stat` to run `git diff --stat <pre_sha> -- .` internally — captures both committed and uncommitted changes. 2 call sites updated (`project_path` → `wt_path`). 12 old tests deleted, 6 new tests added. Closes cascading silent bypass of `_gate_scope_check`. Reference: `executable-file-change-audit-fix-2026-05-25`, diagnostic at `knowledge/research/file-change-audit-false-negative-2026-05-25.md`, QA at `knowledge/qa/executable-file-change-audit-fix-2026-05-25.md`.

- **Closed 2026-05-24:** `rule_22_verification` (c) gate false-positive on QA report verification tables AND `rule_20_self_check` gate fires on wrong file (both originally 2026-05-22). Shipped together via `executable-gate-file-scoping-2026-05-24` (DEV commit `56f94f0`). The 2026-05-24 gate-file-scoping diagnostic at `bellows/knowledge/architecture/gate-file-scoping-2026-05-24.md` characterized both as content-scoping bugs (not path-resolution bugs like the 2026-05-10 fix) and proved them INDEPENDENT — different code regions, different failure mechanisms. Shape 6C applied to `_gate_rule_22_verification` (c) check: added section-scoping (only inspect tables under `## headers` containing "verification") + broadened status-token check via `_is_positive_status_row()` (accepts ✅, PASS, OK, done, complete, verified with bounded cell equality). Shape 7A applied to `_gate_rule_20_self_check`: replaced the `for dep_path in md_paths:` loop with single-file check on `md_paths[0]` (matching Rule 22's pattern). 5 new regression tests + 4 existing test fixture updates (DEV added `## Deliverable Verification` headers to legacy tests for section-scoping compatibility). 399 tests, 394 passing, 5 carry-over fails, 0 new regressions. The QA report on this very plan self-validated through the new section-scoped (c) check using its own `## Deliverable Verification` header. Two BACKLOG hypotheses overturned by the diagnostic: (1) the "120-char truncation before status column" theory for item #6 was wrong — the (c) check reads full rows; truncation is in evidence output only. (2) The "iterates files-modified-this-step" theory for item #7 was wrong — gate uses `_extract_plan_required_deposits` against plan step text, not `files_changed`. **One new BACKLOG item discovered:** `_extract_plan_required_deposits` returns a set, making `md_paths[0]` hash-dependent (currently works because QA steps have one .md deposit). Reference: `Done/diagnostic-gate-file-scoping-2026-05-24.md`, `Done/executable-gate-file-scoping-2026-05-24.md`, QA at `bellows/knowledge/qa/executable-gate-file-scoping-2026-05-24.md`.

- **Closed 2026-05-24:** Step-counter loop after precondition-failure verdict (originally 2026-05-21). Shipped via `executable-precondition-failure-field-2026-05-24` (DEV commit `0a90e26`). Independent fix from items #2/#3 per the 2026-05-24 daemon-restart-state-divergence diagnostic Section E16. Shape E (Precondition Failure verdict-request field) implemented at 5 edit sites: `verdict.py` signature gained `precondition_failure: bool = False` parameter; `verdict.py` content template added `**Precondition Failure:** true/false` field after Pause Reason Code; `bellows.py:438` (worktree-creation failure call site) passes `precondition_failure=True`; `bellows.py` parser at `_consume_verdicts` added `precondition_failure_from_request = False` init + parser line; `bellows.py:1230-1237` replaced unconditional `resume_step=step_number + 1` with conditional dispatch (retry same step on precondition failure, advance otherwise). 4 new regression tests cover backward-compat (field absent → advance), explicit false (advance), explicit true (retry), and producer-side field writing. 394 tests, 389 passing, 5 carry-over fails, 0 new regressions. Backward compatibility preserved: parser defaults to `False` when field absent, historical verdict-request files unchanged in behavior. Reference: `Done/executable-precondition-failure-field-2026-05-24.md`, QA at `bellows/knowledge/qa/executable-precondition-failure-field-2026-05-24.md`.

- **Closed 2026-05-24:** Daemon-restart recovery shape (BACKLOG item #2, originally 2026-05-23) AND Step 2 final-step gate_failure rename-skip (BACKLOG item #3, originally 2026-05-22). Both closed by single fix per the 2026-05-24 daemon-restart-state-divergence diagnostic Section E16, which classified them as two observations of the same RV-1 boundary (non-atomic verdict-post → plan-rename gap, restart between ops drops the rename). Shipped via `executable-rename-first-ordering-2026-05-24` (DEV commit `10b5fc3`). Shape A (rename-first ordering): reordered all 4 pause sites (worktree-creation failure at bellows.py:437-444, intermediate-step gate_failure at bellows.py:519-528, final-step gate_failure at bellows.py:610-617, auto-close-failure at bellows.py:639-644) so the `in-progress-*` → `verdict-pending-*` rename happens BEFORE `verdict.post_verdict_request()` rather than after. If daemon dies in the gap: plan is already `verdict-pending-*` (safe) or no rename happened yet (also safe — same state as if pause never happened). The dangerous middle state (plan `in-progress-*` with verdict-request in `pending/`) is no longer producible. 4 new regression tests (one per pause site) verify ordering via monkeypatched `shutil.move` and `verdict.post_verdict_request` call-order capture. 390 tests, 385 passing, 5 carry-over fails, 0 new regressions. Reference: `Done/diagnostic-daemon-restart-state-divergence-2026-05-24.md` (joint characterization of items #2, #3, #5), `Done/executable-rename-first-ordering-2026-05-24.md` (the RV-1 closure), QA at `bellows/knowledge/qa/executable-rename-first-ordering-2026-05-24.md`.

- **Closed 2026-05-24:** Write-time `processed-` prefix normalization re-consumes already-processed verdicts as fresh verdicts, causing infinite step-replay loops (originally 2026-05-22, CRITICAL/P0). Shipped via `executable-remove-pre-scan-processed-rename-v2-2026-05-24` (commit `c2aeef4` on origin/main). Resolution shape 1c (full removal): the pre-scan block at the former `bellows.py:1129-1162` was removed entirely along with the `_prescan_orphan_logged: set` module-level declaration at the former `bellows.py:33` and all seven `test_pre_scan_*` test functions in `tests/test_consume_verdicts.py`. Net change: -38 LOC in `bellows.py`, -391 LOC in tests. Rationale: PLANNER_TEMPLATE v4.49 Rule 25 (shipped 2026-05-21) prohibits the `processed-` prefix at write time, making the pre-scan a belt-and-suspenders safety net that had created a worse bug than it prevented. Governance rule is the authoritative fix; the pre-scan was now negative-LOC ratable. Two legitimate post-consumption rename sites in `bellows.py` preserved (the Bellows-side marker that a verdict has been consumed). Path traveled to ship: 3 halted predecessor plans (incomplete test enumeration / `cd bellows` path errors / worktree-state assumption errors) all in `Done/`, plus a 4th halted run on the v2 fix itself during which the P0 loop fired against the very plan removing it — recovered via daemon stop, Planner-side `git reset --hard` of 50 duplicate commits from 25 loop iterations, push to origin, manual plan close-out. The full session post-mortem is captured at `bellows/knowledge/research/agent-prompt-feedback.md` "2026-05-24 — remove-pre-scan-processed-rename-v2 (Planner-side post-mortem, post-QA)". Reference: `Done/diagnostic-processed-prefix-reconsumption-and-rename-skip-2026-05-24.md` for root-cause findings (Section A/B identifies the pre-scan; Section F Q16 recommends Shape 1c), `Done/executable-remove-pre-scan-processed-rename-v2-2026-05-24.md` for the implementation plan, QA report at `bellows/knowledge/qa/executable-remove-pre-scan-processed-rename-v2-2026-05-24.md`.

- **Closed 2026-05-24:** Pre-scan orphan-guard INFO logs flood terminal at every daemon restart (originally 2026-05-22). Incidentally closed by the pre-scan removal above. The orphan-guard's `_prescan_orphan_logged` set and its INFO logs were part of the pre-scan block that was removed entirely in Shape 1c. With the pre-scan gone, there is no orphan dedup state to flood from. Net effect: zero startup INFO lines from this source, regardless of how many `processed-verdict-*` files exist in `verdicts/resolved/`. The future-detection signal that the orphan-guard provided (a new orphan appearing post-fix) is also gone, but PLANNER_TEMPLATE v4.49 Rule 25 prevents the only mechanism that could produce a new orphan. Reference: same as above — `Done/executable-remove-pre-scan-processed-rename-v2-2026-05-24.md`.

- **Closed 2026-05-22:** `qa_step_detection` gate does not recognize "Invoice Security & Testing Analyst" as a QA agent (originally 2026-05-21). Diagnostic shipped via `Done/diagnostic-qa-step-detection-audit-2026-05-22.md`. Three SA steps: (Step 1) mechanism characterization confirmed the gate is a naked substring check `"qa" in match.group(0).lower()` at `gates.py:543-549` against the `## STEP N` header line — not a keyword set or role registry; (Step 2) 30-day population audit across 10 watched projects found 14/139 QA steps leaked (10.1% overall; 61.9% in invoice-pulse alone); (Step 3) proposed `qa_steps` plan-header field (comma-separated integers, e.g., `qa_steps: 2,4`) with keyword fallback for legacy plans. **Defer rationale was invalidated by Rule 25 codification.** The original entry's "Planner-side substance check on Rule 20 is the existing fallback and works" rationale assumed manual fallback would catch the leak — but PLANNER_TEMPLATE v4.48 Rule 25 (shipped same day as entry filing) routes the Planner to substance check (b) ONLY when both `rule_22_verification` and `rule_20_self_check` show PASS, actively suppressing the manual fallback. The vacuous `PASS | N/A (not a QA step)` produced by misclassified steps now passes through Rule 25's gate trust without further review. Meta-lesson captured at governance root `LESSONS.md` 2026-05-22 "BACKLOG defers grounded in Planner-side fallback can be silently invalidated by gate mechanization". **Follow-on executable queued:** `executable-qa-steps-header-field-2026-05-XX.md` (likely SA → DEV → QA per fix-shape proposal). Reference: `Done/diagnostic-qa-step-detection-audit-2026-05-22.md`, findings at `knowledge/research/qa-step-detection-mechanism-2026-05-22.md`, `knowledge/research/qa-step-detection-population-audit-2026-05-22.md`, `knowledge/research/qa-step-detection-fix-shape-2026-05-22.md`.

- **Closed 2026-05-22:** Agent `Edit` tool denied on `.claude/settings.local.json` during the 2026-05-21 tier-1 batch plan (originally 2026-05-21). Diagnostic shipped via `Done/diagnostic-claude-settings-permission-gap-2026-05-22.md`. Three SA steps: (Step 1) mechanism characterization classified the denial as pattern (ii) file-path-specific — Edit denied because `.claude/settings.local.json` lives outside any worktree's `cwd` and Claude Code enforces path-scope restrictions on file-access tools (Read/Edit/Write/Grep/Glob), while Bash escapes via command-prefix-based permissions. Self-edit paradox surfaced: the file cannot grant itself Edit permission. (Step 2) 30-day audit found bucket (a) at 1 incident/30 days — low recurrence, high predictability (every plan editing this file will hit the denial when run in a worktree). (Step 3) recommended **Shape 1 + supplementary instruction**: document the bash-fallback pattern in BELLOWS_DEVELOPER.md AND template plan-prompts that target this file to instruct "use Bash (python3 -c), not Edit" — the supplementary instruction prevents the Edit attempt entirely, so the denial never fires and no Rule 22 override is needed. Shape 2 (gate filter) was rejected as disproportionate to recurrence; Shape 3 (CEO-only edits) was rejected for introducing CEO-availability dependency. **Follow-on executable queued:** `executable-settings-local-bash-fallback-doc-2026-05-XX.md` (Documentation Analyst → QA, doc-only, no code changes). Reference: `Done/diagnostic-claude-settings-permission-gap-2026-05-22.md`, findings at `knowledge/research/claude-settings-permission-gap-2026-05-22.md`, `knowledge/research/permission-denial-history-audit-2026-05-22.md`, `knowledge/research/claude-settings-permission-gap-fix-shape-2026-05-22.md`.

- **Closed 2026-05-22 (hygiene):** Bellows expected-keys warning misleading on single-line headers (originally 2026-05-21). Stale entry — fix already shipped same-day via `executable-bellows-expected-keys-narrow-2026-05-21` (commit `e2301f7`). Open entry was authored from the NEXT_SESSION baton before the fix landed and was never reconciled when the same-day Closed entry was written. Verified 2026-05-22 by direct read of `bellows.py`: the old 4-line warning block at lines 416-419 is gone; current code at line ~512 uses the narrowed predicate `if total_steps > 1 and "pause_for_verdict" not in header:` with a focused warning message, matching the Shape C design from `diagnostic-bellows-expected-keys-shape-choice-2026-05-21`. No code change required — this is bookkeeping only. **Meta-lesson (third occurrence of pattern):** BACKLOG entries authored from session-handoff batons can become stale within the same session if the corrective work ships before the baton is reconciled. LESSONS.md captures this pattern (2026-05-03, 2026-05-10); a discipline check during session-wrap to scan Open entries against same-day Done/ activity would close the gap. Reference: existing Closed entry `Closed 2026-05-21: Bellows expected-keys warning misleading on single-line headers` for the actual fix details.

- **Closed 2026-05-22 (hygiene):** `isinstance(f, dict)` discriminator asymmetry between bellows.py pause-reason blocks (originally 2026-05-21). Stale entry — fix already shipped same-day via `executable-bellows-isinstance-asymmetry-fix-2026-05-21` (commit `6fdda11`). Same authoring pattern as the expected-keys entry above. Verified 2026-05-22 by direct read of `bellows.py`: both pause-reason blocks now use the identical symmetric guard `if all(isinstance(f, dict) and f.get("gate") == "rule_22_verification" for f in gate_result["failures"]):` — the while-loop pause site (line ~677) and the final-step pause site (line ~735) are byte-equivalent on the discriminator check. No code change required — bookkeeping only. Reference: existing Closed entry `Closed 2026-05-21: isinstance(f, dict) discriminator asymmetry` for the actual fix details.

- **Closed 2026-05-22:** Pre-scan rename produces orphaned `verdict-*` files when source `processed-verdict-*` file has no paired plan (originally 2026-05-21). Shipped via `executable-pre-scan-orphan-guard-2026-05-22`. Resolution shape (1) from the original entry — extend the pre-scan to skip rename when no paired `verdict-pending-*` plan exists. Orphan-check guard added at bellows.py:1135-1155, fires BEFORE the existing collision guard. Plan slug extracted via regex, scanned against `self.config["watched_projects"]` for `verdict-pending-*` match. INFO log with daemon-lifetime dedup via `_prescan_orphan_logged` set at line 33. One-shot migration cleaned 9 canonical orphans. 4 regression tests added. Eliminates ~8,823 daily rename events and ~10,827 stale-verdict events across 313 stale plan slugs. Reference: `Done/executable-pre-scan-orphan-guard-2026-05-22.md`, diagnostic at `knowledge/research/pre-scan-orphan-warn-flood-2026-05-22.md`, dev log at `knowledge/development/pre-scan-orphan-guard-2026-05-22.md`, QA at `knowledge/qa/executable-pre-scan-orphan-guard-2026-05-22.md`. **Daemon restart required** to load the new guard code.

- **Closed 2026-05-21:** `_consume_verdicts` did not drain two valid `resolved/` verdict files across multiple daemon cycles AND a clean restart (originally 2026-05-21, this session). Root cause confirmed and structurally fixed. Diagnostic at `bellows/knowledge/architecture/consume-verdicts-drain-failure-2026-05-21.md` traced both reproduction failures to H2 (post-consumption rename collision), not H1 (slug pairing mismatch). The discovery filter at `bellows.py:1128` (`if not fname.startswith("verdict-")`) silently drops files named `processed-verdict-*` because they don't start with literal `verdict-` — they are structurally indistinguishable from already-consumed verdicts. The Planner had been copying the post-consumption filename pattern from format-source-of-truth examples in `resolved/`. Shipped via `executable-consume-verdicts-prefix-fix-2026-05-21` (Option B — pre-scan rename, self-healing): new pre-scan pass in `_consume_verdicts` detects `processed-verdict-*.md` files and renames them to canonical `verdict-*.md` before the main filter loop, with a collision guard that WARN-skips when both forms exist. 3 new regression tests in `bellows/tests/test_consume_verdicts.py`. Empirically validated by the daemon restart during Step 2 QA — the new pre-scan code renamed ~400 historical `processed-verdict-*` files in a single rescan cycle, all consumed cleanly as stale-verdict-for-Done plans. Governance companion fix shipped in PLANNER_TEMPLATE v4.49: new "Verdict response file write-time filename" paragraph in Rule 25 prohibits the `processed-` prefix at write time; new Lesson row captures the contract-mismatch pattern (third occurrence) and the meta-lesson that lifecycle-state filenames in format-source-of-truth examples leak post-consumption naming back to write time. Pre-scan is belt-and-suspenders; discipline rule is authoritative. **Daemon restart required** to load the new code (already happened during Step 2 QA, observable in the bellows session log at 21:31:34 with hundreds of `normalized write-time processed- prefix:` WARN lines). Reference: `Done/diagnostic-consume-verdicts-drain-2026-05-21.md`, `Done/executable-consume-verdicts-prefix-fix-2026-05-21.md`, QA at `bellows/knowledge/qa/consume-verdicts-prefix-fix-qa-2026-05-21.md`, governance edit at `PLANNER_TEMPLATE.md` v4.49 Rule 25 "Verdict response file write-time filename" + Lessons row.

- **Closed 2026-05-21:** PLANNER_TEMPLATE.md Rule 25 routing codification — mechanized vs Planner-only check delineation (originally 2026-05-21, this session). Shipped via `executable-planner-template-rule-25-codification-2026-05-21` (v4.48). Three edits landed verbatim per QA: (A) new "Mechanized check routing (post-verdict-enrichment)" paragraph appended after the existing Rule 25 "Rule 22 routing on auto-proceed codes" paragraph at line 685, codifying that `_gate_rule_22_verification` covers Rule 22 (a)+(c)+(d) and `_gate_rule_20_self_check` covers (e), so when both gates PASS the Planner performs (b) substance check only — not (a)/(c)/(d)/(e) which Bellows has already verified. Gate-FAIL or non-auto-proceed Pause Reason Codes still halt-and-report. (b) explicitly preserved as Planner-only because it requires substantive judgment no gate can mechanize; (B) version bump 4.47 → 4.48 with zero residual 4.47 references; (C) new Lessons row capturing the discipline lesson that surfaced this session via CEO question "why is planner still doing rule 22 a-e?" Rule 22 (a)-(e) canonical definitions at lines 604-609 preserved unchanged — the new paragraph clarifies *routing*, not *definitions*. Pre-write contradiction scan completed (keywords: mechanically, (a)-(e), rule_22_verification, rule_20_self_check) — no contradictions found. QA shipped 8/8 deliverables verified, 3/3 structural compliance checks PASS, Rule 20 self-check PASSED with 11 evidence files. Reference: `Done/executable-planner-template-rule-25-codification-2026-05-21.md`, QA at `knowledge/qa/executable-planner-template-rule-25-codification-2026-05-21.md`.

- **Closed 2026-05-21:** Tier-1 batch — three small BACKLOG items shipped together via `executable-bellows-tier-1-batch-2026-05-21`. **(A) `bellows/config.json` not in `.gitignore`** (originally 2026-05-21): one-line append to `.gitignore`, file was never tracked in HEAD so no `git rm --cached` needed. Pushover `app_key`/`user_key` and tailscale IP in the file are no longer at risk of accidental commit. **(B) `Bash(git:*)` auto-approval too broad** (originally 2026-05-21): `.claude/settings.local.json` allowlist narrowed from broad `Bash(git:*)` to 11 explicit non-destructive subcommands (`git add`, `git commit`, `git status`, `git log`, `git diff`, `git show`, `git ls-files`, `git checkout`, `git branch`, `git config`, `git rev-parse`). Destructive commands now require manual approval: `git push`, `git reset --hard`, `git push --force`, `git push --force-with-lease`, `git rebase`, `git filter-repo`, `git worktree`. **(C) `pause_for_verdict` unvalidated enum** (Priority 3 audit, originally 2026-05-21): `header_says_pause` at `bellows.py:305-315` now emits a WARN for non-empty unrecognized values (typos, `never`, etc.) before returning False. The `if pv:` guard avoids noise on the empty-string "no directive" case which the defensive default handles. Step 1 hit `gate_failure` from a `permission_denials` block on `Edit` calls to `.claude/settings.local.json`; resolved via Rule 22 override after Planner verified all three edits landed correctly on disk. New BACKLOG entry queued for the underlying permission-shape governance gap. QA verified 9/9 deliverables, 116/116 pytest pass, Rule 20 self-check PASSED with 11 evidence files. Reference: `Done/executable-bellows-tier-1-batch-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-tier-1-batch-2026-05-21.md`. **Daemon restart required** to load Item C (the new WARN in `header_says_pause`).

- **Closed 2026-05-21:** PLANNER_TEMPLATE.md no-push instruction in Rule 23(c) + Rule 8 mirror, AND Rule 25 routing-table preamble stale count (both originally 2026-05-21, this session). Shipped together via `executable-planner-template-no-push-and-routing-count-2026-05-21` (v4.47). Five edits landed verbatim per QA verification: (A) Rule 8 mirror paragraph appended with explicit `**Agents do NOT run \`git push\` during step execution.**` sentence and the parallel-SHA divergence root-cause citation; (B) Rule 23(c) paragraph appended with the same prohibition plus Rule 31/Procedure 3 carve-outs (Planner-side submodule push and operator-side filter-repo push unaffected); (C) Rule 25 preamble "two" → "three" reflecting the `header_pause` row added by 2026-05-13 Lessons Forge proposal 35; (D) version header 4.46 → 4.47; (E) new Lessons row capturing the BACKLOG-entries-written-from-memory discipline lesson (third occurrence of pre-write contradiction scan catching a stale rule before ship). Pre-write contradiction scan verified Rule 31 + Procedure 3 push references remain unchanged. QA shipped 116/116 pytest pass, structural diff matches plan, Rule 20 self-check PASSED. Reference: `Done/executable-planner-template-no-push-and-routing-count-2026-05-21.md`, QA at `knowledge/qa/executable-planner-template-no-push-and-routing-count-2026-05-21.md`.

- **Closed 2026-05-21:** `isinstance(f, dict)` discriminator asymmetry between bellows.py pause-reason blocks (originally 2026-05-21, this session). Shipped via `executable-bellows-isinstance-asymmetry-fix-2026-05-21` (commit `6fdda11`). Block 2 at bellows.py:594 now uses the identical defensive pattern as Block 1 at bellows.py:505 — `if all(isinstance(f, dict) and f.get("gate") == "rule_22_verification" for f in gate_result["failures"]):`. 1-line edit, -1/+1 diff, no behavioral change today (all upstream sites produce dicts per the 21-site enumeration in the diagnostic). Defensive against future refactors — the 2026-05-03 commit `272fbe4` precedent showed this exact bug class (string-to-dict refactor) has already occurred once in this code area. Reference: `Done/diagnostic-bellows-isinstance-asymmetry-2026-05-21.md` (findings at `knowledge/research/bellows-isinstance-asymmetry-2026-05-21.md`), `Done/executable-bellows-isinstance-asymmetry-fix-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-isinstance-asymmetry-fix-2026-05-21.md`. **Daemon restart required** to load the new symmetric guard.

- **Closed 2026-05-21:** Bellows expected-keys warning misleading on single-line headers (originally 2026-05-21, this session). Shipped via `executable-bellows-expected-keys-narrow-2026-05-21` (commit `e2301f7`). Resolution shape (1) from the first diagnostic — narrow the warning. Two diagnostics ran: (1) `diagnostic-bellows-expected-keys-warning-2026-05-21` characterized the 5 keys: `pause_for_verdict` is the only safety-critical key; `project`/`date`/`author`/`total_steps` are cosmetic (zero runtime consumers). 30-plan sample showed the warning fires on ~93% of multi-step plans due to template gaps in cosmetic fields. (2) `diagnostic-bellows-expected-keys-shape-choice-2026-05-21` resolved among three candidate implementation shapes — Shapes A (return flag from defensive default) and B (snapshot original keys) were proven redundant via code-trace (the dict's own state encodes the Case 3/4 distinction after defensive default fires); Shape C (replace 4-line block with 2-line targeted predicate) ships. bellows.py:416-419 replaced with `if total_steps > 1 and "pause_for_verdict" not in header:` and a focused warning message. -4/+2 line delta, zero behavioral change for pause routing. Existing sparse-header warning at bellows.py:382-383 preserved unchanged as Case 3 reporter. Test `test_warning_multi_step_plan_without_pause_for_verdict` (test_bellows.py:2569) assertion updated to reflect Case 3 semantics (defensive default fires → narrowed warning correctly does NOT fire). 116/116 pytest pass. **Side-finding flagged as separate open BACKLOG entry:** defensive default at bellows.py:381 is ineffective for runtime pause behavior because `header` is reassigned at bellows.py:494 from `gates.check()` which re-parses without applying the default. Reference: `Done/diagnostic-bellows-expected-keys-warning-2026-05-21.md`, `Done/diagnostic-bellows-expected-keys-shape-choice-2026-05-21.md`, `Done/executable-bellows-expected-keys-narrow-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-expected-keys-narrow-2026-05-21.md`. **Daemon restart required** to load the new warning.

- **Closed 2026-05-21 (retired):** Pre-existing `test_decisions.py` failures (4 tests) (originally surfaced 2026-05-27 by the disable-autoupdater QA). Retired — not a real failure mode. Independently verified during 2026-05-21 verdict-enrichment QA: all 18 `test_decisions.py` tests PASS when pytest runs from the main repo. The 4 failures were worktree-context artifacts (missing `INTERMEDIATE_DECISION_PHRASES.md` in the worktree because the file was not on the worktree branch tip at QA-run time). The 2026-05-21 verdict-enrichment QA report documents this in its Test Execution section. No code change required. Reference: `Done/executable-bellows-verdict-enrichment-2026-05-21.md`, QA at `knowledge/qa/executable-bellows-verdict-enrichment-2026-05-21.md`.

- **Closed 2026-05-27:** Set `DISABLE_AUTOUPDATER=1` in the Bellows daemon environment (originally 2026-05-20). Shipped via `Done/executable-disable-autoupdater-2026-05-27.md`. Implementation: `os.environ.setdefault("DISABLE_AUTOUPDATER", "1")` added near top of both `bellows/runner.py` and `bellows/bellows.py` with `setdefault` semantics so explicit operator overrides are respected. Two unit tests added in `tests/test_runner.py` (`test_runner_sets_disable_autoupdater_env_var`, `test_runner_respects_explicit_disable_autoupdater_override`). Operational doc note added to `bellows/CLAUDE.md` in a new `## Claude Code upgrade cadence (manual)` section: operator runs `claude --version` + `npm install -g @anthropic-ai/claude-code` manually at session-wrap or weekly. Test suite: 364 → 366, zero new failures. Daemon restarted to load. Reference: `Done/executable-disable-autoupdater-2026-05-27.md`, QA at `knowledge/qa/disable-autoupdater-2026-05-27.md`.

- **Closed 2026-05-27:** `_gate_deposit_exists` path-form normalization (originally surfaced 2026-05-23 via shop_next_session.md Thread 3 — never formally captured in BACKLOG.md before close, so this entry is backdated). Shipped via `Done/executable-deposit-exists-path-form-normalization-2026-05-27.md`. Root cause per diagnostic `knowledge/research/deposit-exists-path-form-normalization-2026-05-27.md`: abs-vs-rel path-form mismatch in `_gate_deposit_exists` membership check; raw string comparison failed when plan declared absolute paths and agent declared project-prefixed relative paths. Two components shipped together: (A) new `_normalize_deposit_path` helper in `gates.py` and normalization at three call sites in `_gate_deposit_exists` (lines 295, 303, 310); (B) absolute-path remap added to `_resolve_deposit_path` Strategy 0 (lines 254-258) so `os.path.join(wt_path, abs_path)` doesn't silently return the absolute path unchanged. 6 new unit tests in `tests/test_gates.py`. Order of operations confirmed unchanged: `gates.check()` at bellows.py:485 BEFORE `_teardown_worktree` at bellows.py:514 (the shop baton's gate-vs-teardown race hypothesis was overturned by the diagnostic — second time this hypothesis has been raised and disproven; LESSONS 2026-05-27 captures the discipline rule). Daemon restarted to load. Reference: `Done/diagnostic-deposit-exists-path-form-normalization-2026-05-27.md`, `Done/executable-deposit-exists-path-form-normalization-2026-05-27.md`, QA at `knowledge/qa/deposit-exists-path-form-normalization-2026-05-27.md`.

- **Closed 2026-05-12:** bellows-self parallel/concurrent activity exposure (originally 2026-05-05). Closed as won't-fix per disposition diagnostic at `bellows/knowledge/architecture/bellows-self-exposure-disposition-2026-05-12.md`. SA audit (2026-05-06 → 2026-05-12, 7 days, 47 ledger entries + all resolved verdicts) found **zero scope_check failures of any class** — vs. 4 bellows-self reproductions in the prior 5-day window (2026-05-01 → 2026-05-05). The procedural mitigations from the original entry (avoid CEO mv/cleanup operations in bellows/ during active bellows-self plans; avoid parallel-N- dispatch for bellows-self plans that commit during DEV; Rule 22 override via continue verdict on demonstrable false trips) are fully effective. Option (b) governance-root-worktree with subdirectory cwd remains the only technically complete fix — revised LOC estimate: 50–70 production / 80–120 with tests; 9 subprocess/path call sites require adaptation; one new risk surface from lock detection code (commit `8eac4c3`, 2026-05-10); agent-confusion risk from subdirectory cwd unchanged. Alternatives evaluated: option (c) process-filtered file-touch tracking requires elevated privileges (root for fs_usage, SIP-off for dtrace) and is platform-locked — non-starter; commit-message slug scoping is structurally inapplicable to the bellows-self case (no worktree means no plan-specific commits to scope against, and uncommitted dirty files from concurrent activity produce no commits at all). The exposure is exclusively a scope_check false-positive — never produces data loss, incorrect agent behavior, or missed defects — and the Planner-side Rule 22 override is cheap. Negative ROI on shipping. **Revisit trigger:** if bellows-self scope_check false positives resume (increased concurrent dispatch, automated lifecycle operations in bellows/, workflow change that makes manual mitigation impractical), re-open as a new BACKLOG entry with updated reproduction count and reference this disposition as prior art. **No code change.** Reference: `bellows/knowledge/architecture/bellows-self-exposure-disposition-2026-05-12.md` (SA findings), `bellows/knowledge/decisions/Done/diagnostic-bellows-self-exposure-disposition-2026-05-12.md` (disposition diagnostic plan), original BACKLOG entry 2026-05-05.

- **Closed 2026-05-12 (hygiene):** Plan-fixing-bug-X tripped bug X during own close (originally 2026-04-19). Partial supersession by PLANNER_TEMPLATE v4.38 (governance-root commit `4e54c02`, shipped 2026-05-11). The Restart Discipline subsection (line 882) and Lessons Learned row at line 1227 document the structural inevitability of QA-step gate trips on Bellows-side fix-plans that modify plan-text parsers — naming the 5 affected parsers (`_extract_step_text`, `_gate_is_qa_step`, `extract_total_steps`, `_extract_step_text_from_plan`, `strip_fenced_code_blocks`) and the correct CEO override pattern. **Scope note:** v4.38's structural documentation covers the parser-fix subset of the pattern, not the full general class. The original 2026-04-19 example (`executable-verdict-lifecycle-coupling-2026-04-19`) was a verdict-lifecycle fix, not a parser fix — that general class (any Bellows-side code change targeting a close-path bug) remains procedurally documented in the original BACKLOG entry's own mitigation text (code-level-only QA, accept manual cleanup as one-time cost, post-fix validation via separate dependent plan after restart). The original entry's "May warrant promotion to a PLANNER_TEMPLATE Lessons Learned entry at next governance pass" disposition is satisfied for the parser-fix subset; the general pattern continues to be procedurally documented in this BACKLOG's Closed section as historical reference. No code change. Reference: PLANNER_TEMPLATE.md v4.38 Restart Discipline subsection (line 882), Lessons Learned row 2026-05-11 (line 1227), governance-root commit `4e54c02`.

- **Closed 2026-05-12:** Terminal output redesign + notification audit (originally 2026-04-19). Shipped as two implementation plans following the 2026-05-11 design diagnostic. Plan 1 (`Done/executable-terminal-capture-2026-05-12.md`, commit `b11ecc4`): added `_log()` helper with 5-level severity taxonomy (EVENT/INFO/WARN/ERROR/PAUSE), migrated 63 print() calls across `bellows.py`/`runner.py`/`notifier.py`, configured Python logging with RotatingFileHandler to `logs/terminal/bellows-YYYY-MM-DD.log` (14-day retention), heartbeat redesigned to 300s/state-bearing/suppression-during-activity, runner heartbeats now carry plan slug + step number, log rotation 14d terminal / 30d step JSON / 10MB planner-consultation. Plan 2 (`Done/executable-notification-coalescing-2026-05-12.md`, commit `07a87ad`): deleted 2 dead-code notification functions (`notify_escalation`, `notify_complete`), added 4 new named functions (`notify_plan_complete`, `notify_plan_halted`, `notify_plan_skipped`, `notify_queue_empty`), implemented urgency-gated coalescing buffer with 30s timer (verdict/failure push immediately, completions/halted/skipped/queue-empty buffer into a digest), migrated all 5 direct `notifier.push()` calls in `bellows.py` to named functions (0 remain), added `notifications` config block to `config.json` and `config.example.json` with backward-compatible defaults, Pushover priority/sound mapping per event type. CEO locked 5 design decisions on 2026-05-12: slug-tag-only plan grouping (no separator lines), combined heartbeat policy (300s state-bearing + 120s suppression), urgency-gated coalescing with 30s digest, 14d terminal log retention, 30d step JSON retention. Tests 268/269 pass (1 pre-existing `test_run_step_timeout` failure unrelated). Minor implementation deviation: `notify_plan_skipped` per-event priority -1 in design, but coalesced digest uses priority 0 since digests may contain higher-priority events. Reference: `Done/diagnostic-terminal-and-notification-surface-audit-2026-05-11.md`, `Done/diagnostic-terminal-notification-capture-design-2026-05-11.md`, `Done/executable-terminal-capture-2026-05-12.md`, `Done/executable-notification-coalescing-2026-05-12.md`.

- **Closed 2026-05-11 (hygiene):** PATH-001 recurrence in Rule 20 self-check (originally 2026-04-19). Stale entry — structurally fixed by the 2026-05-10 Rule 20 single-source migration (governance commit `a109e47`, bellows commit `b05dc42`). Diagnostic at `bellows/knowledge/research/path-001-rule-20-staleness-audit-2026-05-11.md` confirmed: (a) the current canonical block at `/Users/marklehn/Desktop/GitHub/RULE_20_SELF_CHECK_BLOCK.md` uses placeholder-enforced absolute paths (`<absolute-path-to-qa-report.md>`, `<absolute-path-to-evidence-dir>/`) with no `bellows/` prefix anywhere; (b) the Planner no longer authors the Rule 20 block in plans, eliminating the original failure surface; (c) population audit of 17 post-migration QA reports (2026-05-10 and 2026-05-11) showed Rule 20 self-check PASSED in 17/17 with zero path-resolution failures. The companion pattern entry in `agent-prompt-feedback.md` (PATH-001 at line ~399) was updated to CLOSED in the same plan. Reference: `Done/diagnostic-path-001-rule-20-staleness-audit-2026-05-11.md`, `Done/diagnostic-path-001-hygiene-close-edit-surface-2026-05-11.md`, `Done/executable-rule-20-single-source-2026-05-10.md`.

- **Closed 2026-05-11 (hygiene):** Bellows-side parser fix-plans trip own bug (originally 2026-05-11). Recommended fix (governance combination 1+4) shipped via PLANNER_TEMPLATE v4.38 (`executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11`, governance-root commit `4e54c02`). The new paragraph in the Restart Discipline subsection documents the structural inevitability of the QA-step gate trip on Bellows-side parser fix-plans, names the 5 affected parsers (`_extract_step_text`, `_gate_is_qa_step`, `extract_total_steps`, `_extract_step_text_from_plan`, `strip_fenced_code_blocks`), describes the correct CEO override pattern, and references the bait-laden canary verification post-restart. Lessons row also appended capturing both today's reproductions (commits `4d57fd3` and `0fab609`) and the meta-lesson that fix-plan correctness depending on code that does not load until after plan completion makes the gate trip a known cost rather than a defect. No code fix shipped — governance-only per the recommended combination. Reference: `Done/executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11.md`, PLANNER_TEMPLATE v4.38 Restart Discipline subsection.

- **Closed 2026-05-11 (hygiene):** Session-wrap hygiene gap — plan lifecycle file moves accumulate as untracked changes (originally 2026-05-06, REWRITTEN 2026-05-10). Recommended resolution (session-wrap discipline) shipped via PLANNER_TEMPLATE v4.38 Session Wrap subsection (`executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11`, governance-root commit `4e54c02`). New fourth bullet codifies the `git add -A bellows/knowledge/decisions/Done/ bellows/verdicts/` commit pattern and notes the cross-project applicability. Today's cleanup commit `8cadade` captured 213 accumulated lifecycle files across 2-3 weeks as a one-time recovery; the new session-wrap discipline prevents recurrence. Lessons row also appended capturing the today's recovery commit and the meta-lesson that infrastructure operations producing filesystem state changes need explicit commit discipline rather than implicit accumulation. No code fix shipped — governance-only per the recommended resolution path. Reference: `Done/executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11.md`, PLANNER_TEMPLATE v4.38 Session Wrap subsection, today's cleanup commit `8cadade`.

- **Closed 2026-05-11:** Daemon code-version observability gap (originally 2026-05-11). Shipped via `executable-daemon-version-observability-2026-05-11` (commits `6411054` fix, `fd0efd7` tests, `0199aaa` dev log). New `_module_fingerprints()` helper in `bellows.py` returns a dict mapping each of the 5 core modules (`bellows.py`, `gates.py`, `verdict.py`, `parser.py`, `runner.py`) to a fingerprint string: `git:<short-sha>` when git is available, `mtime:<timestamp>` fallback, `"unknown"` ultimate fallback on unexpected error (helper never raises). Startup banner now emits one `Module: <filename> @ <fingerprint>` line per module after the existing source SHA banner. Heartbeat loop surfaces the same block every `MODULE_FINGERPRINT_HEARTBEAT_INTERVAL = 10` ticks (~every 10 minutes at the current 60s cadence). 3 new regression tests added (returns-all-five, git-failure-fallback, ultimate-unknown-fallback); test count 101 → 104. No new threading primitives, no shell=True, no config.json bloat (interval is a module-level constant). **REMINDER:** daemon restart required to load the new observability code — the first time the fingerprint logging actually appears is the restart that loads it. Reference: `Done/executable-daemon-version-observability-2026-05-11.md`, QA at `knowledge/qa/daemon-version-observability-qa-2026-05-11.md`.

- **Closed 2026-05-11:** `PlanHandler._seen` retry cache (originally 2026-05-06). Shipped via `executable-plan-handler-seen-slug-refactor-2026-05-11` (commits `63dd6ed` fix, `130649b` tests, `e889186` dev log). Diagnostic at `bellows/knowledge/research/plan-handler-seen-retry-cache-2026-05-11.md` characterized the lifecycle (3 terminal-event call sites: continue-to-done at line 1027, halt at line 1047, auto-close at line 527), confirmed the dispatch-window race is real and load-bearing, recommended Option (B) on Layer 1 invariant grounds. Fix shipped Option (B) with slug-only keying: moved `_seen` from `PlanHandler.__init__` to `Bellows.__init__`; re-keyed all 8 access sites from path to slug via `verdict.slug_from_path()`; added `bellows=None` parameter to `run_plan` plumbed via `_run_tracked`; added `_seen.discard()` calls at all 3 lifecycle terminal events. Plans reaching Done/ or halted-/ now clear their slug from `_seen`, enabling re-dispatch on retry workflows without a daemon restart. Dispatch-window guard preserved (verified by `test_seen_dispatch_window_guard_holds`). 4 new regression tests added; test count 97 → 101. No new threading primitives. **REMINDER:** daemon restart required to load refactored code. Reference: `Done/executable-plan-handler-seen-slug-refactor-2026-05-11.md`, `Done/diagnostic-plan-handler-seen-retry-cache-2026-05-11.md`, QA at `knowledge/qa/plan-handler-seen-slug-refactor-qa-2026-05-11.md`.

- **Closed 2026-05-11 (hygiene):** `extract_total_steps()` fence-strip (originally 2026-05-10). Stale entry — fix shipped 2026-05-11 via `executable-fence-strip-plan-text-parsers-2026-05-11` (commit `4d57fd3`). Diagnostic at `bellows/knowledge/research/extract-total-steps-fence-class-audit-2026-05-11.md` confirmed 4 BUG-CONFIRMED parsers (`extract_total_steps`, `_extract_step_text`, `_gate_is_qa_step`, `_extract_step_text_from_plan`) all needed fence-strip; fix shipped recommendation (a) — new `strip_fenced_code_blocks()` utility applied at entry of all 4 parsers, duplicated across `bellows.py`, `gates.py`, `verdict.py` for circular-import avoidance. +5 regression tests. The 2026-05-11 line-anchor fix (commit `0fab609`) addressed the complementary inline-code variant. Both fixes loaded into running daemon via 2026-05-11 restart. BACKLOG entry was never moved to Closed at the time of fix-ship; this entry closes the lifecycle. Reference: PROJECT_STATUS 2026-05-11 entries, `Done/executable-fence-strip-plan-text-parsers-2026-05-11.md`, `Done/executable-step-header-line-anchor-2026-05-11.md`.

- **Closed 2026-05-11:** `deposit_exists` Cause 5 — plan-agent evidence path convention mismatch. Closure of the 2026-05-10 entry's RC2 ("legacy prose regex in `_extract_plan_required_deposits` captures unresolvable abbreviated paths — Resolution path: ensure all plan steps use explicit `**Deposits:**` blocks per Rule 26"). Today's audit (`bellows/knowledge/research/deposit-exists-false-positive-audit-2026-05-11.md`) empirically confirmed RC2 as the actual root cause for 18 gate-failure lines across 3 verdict requests on 2026-05-07/08 (action-queue-aggregation, action-queue-limit-and-contract-name, bellows-qa-prefix-and-skip-logging). Mechanism: plans declared bare `evidence/foo.txt` paths in `**Deposits:**` blocks while agents created files at `knowledge/qa/evidence/<slug>/foo.txt` — none of `_resolve_deposit_path()`'s four strategies could bridge the structural gap (Strategy 0 tries `wt_path/evidence/foo.txt`, Strategy 2 tries `project_path/evidence/foo.txt`, neither exists). Follow-up canary diagnostic (`Done/diagnostic-rule-26-directory-bullet-canary-2026-05-11.md`) verified the current gate accepts directory-only `**Deposits:**` bullets via `os.path.isfile() or os.path.isdir()` at every strategy (`gates.py:183-212`), authorizing the Path A resolution. Governance fix shipped via `Done/executable-rule-26-evidence-path-fix-2026-05-11.md` (PLANNER_TEMPLATE v4.37, governance-root commit `75904fd`): Rule 26 tightened from permissive ("evidence files do NOT need individual bullets") to mandatory ("evidence files MUST be represented by the evidence directory as a single bullet; Do NOT list individual evidence files"). No Bellows code change required — the gate already accepted directory bullets correctly; the failure was in plan-authoring convention. Three Lessons rows added at governance root (2026-05-11): stale-lesson retraction of 2026-04-19 directory-prohibition (made stale by commit `e609ad3`), Cause 5 capture with meta-lesson on mandatory governance language, and diagnostic-before-executable discipline win on the canary catch. Reference: `Done/diagnostic-deposit-exists-false-positive-audit-2026-05-11.md`, `Done/diagnostic-rule-26-directory-bullet-canary-2026-05-11.md`, `Done/executable-rule-26-evidence-path-fix-2026-05-11.md`.

- **Closed 2026-05-10:** Multi-line bold header parser gap (originally 2026-05-10 same-day). Diagnostic + executable shipped end-to-end. Diagnostic at `bellows/knowledge/research/header-parser-multiline-bold-gap-2026-05-10.md` empirically confirmed via REPL fixtures (multi-line bold returned `{'project': 'bellows'}` while pipe format returned 6 keys), traced auto-advance mechanism through `bellows.py:247-350`, audited 30-day plan population (3 affected plans all in Done/, zero in-flight risk), surfaced Shape (g) defensive default as belt-and-suspenders complement that the Planner had not considered. CEO selected combination (a)+(e)+(g) per 2026-05-10 chat. Executable shipped via `executable-header-parser-multiline-fix-2026-05-10` at commit `491aab9`: (a) extended `gates.py::_parse_plan_header` Strategy 2 to collect all consecutive bold-Markdown lines and join with ` | ` (~10 LOC + 5 tests), (g) extracted `_apply_defensive_header_defaults()` helper in `bellows.py` that sets `pause_for_verdict = after_step_1` when `total_steps > 1` and `len(header) < 3` (~10 LOC + 2 tests), (e.2) replaced single-purpose `pause_for_verdict` warning with richer missing-keys observability message surfacing ALL missing expected keys. LOC delta: production +46, tests +113, net +159. Test suite: 253 passed (delta +7), 1 pre-existing failure (`test_run_step_timeout`) unchanged, 0 new failures. QA verified 14/13 properties including critical end-to-end test #13 (re-parsing the earlier-today incident plan now correctly returns `pause_for_verdict=after_step_1`, confirming fix prevents original failure). Rule 20 self-check PASSED using new single-source canonical pattern from `RULE_20_SELF_CHECK_BLOCK.md`. **Cosmetic anomaly during dispatch:** Bellows ran 4 dispatches instead of 2 because `extract_total_steps()` counted `## STEP N` patterns inside test-fixture string literals embedded in plan prose. Captured as separate new open BACKLOG entry. No substantive harm — real Step 1 and Step 2 deposits both shipped correctly. Reference: `bellows/knowledge/decisions/Done/executable-header-parser-multiline-fix-2026-05-10.md`, diagnostic at `Done/diagnostic-header-parser-multiline-bold-2026-05-10.md`, findings at `knowledge/research/header-parser-multiline-bold-gap-2026-05-10.md`.

- **Closed 2026-05-10:** Rule 20 self-check block sourcing migration (originally 2026-05-10, same-day). Shipped via `executable-rule-20-single-source-2026-05-10` (Steps 1+2) using migration shape (d.2) — single-source template file at governance root. New file `RULE_20_SELF_CHECK_BLOCK.md` (122 LOC) created at `/Users/marklehn/Desktop/GitHub/` as the only location publishing the canonical Python block. PLANNER_TEMPLATE.md Rule 20 rewritten (−51 LOC net) to reference the canonical file instead of inlining the block; version bumped 4.35 → 4.36; Lessons Learned entry added. `bellows/agents/BELLOWS_QA.md` and `invoice-pulse/agents/INVOICE_SECURITY_TESTING_ANALYST.md` updated with reference paragraphs (no inlining). Three commits across three repos: governance `a109e47`, bellows `b05dc42`, invoice-pulse `02702201`. **Structural success criterion met:** QA Step 2 was the first plan to use the new pattern — ran the canonical block from `RULE_20_SELF_CHECK_BLOCK.md` using only plan prompt + canonical file reference, no inlined block. QA verified 14/14 properties at `bellows/knowledge/qa/rule-20-single-source-qa-2026-05-10.md` including byte-exact banner survival, no-inline-block invariant across all 4 governance-tree files, and test suite unchanged (246 passed, 1 pre-existing failure). The Planner-side substitution failure mode is eliminated by design — the Planner never authors the block, so paraphrasing is impossible. Reference: `bellows/knowledge/decisions/Done/executable-rule-20-single-source-2026-05-10.md`, diagnostic at `Done/diagnostic-rule-20-block-sourcing-2026-05-10.md`, findings at `knowledge/research/rule-20-block-sourcing-migration-surface-2026-05-10.md`.

- **Closed 2026-05-10:** `test_startup_sweep_removes_done_plan_orphans` inline-replicated sweep logic (originally 2026-05-01). Shipped via `executable-startup-sweep-extract-2026-05-10` (DEV commit `783eea7`). `_perform_startup_sweep(self) -> list[str]` extracted onto `Bellows`, called from `start()` (which retains print loop). Test now exercises production directly via `b._perform_startup_sweep()`. Variant (i) chosen (return list + prints stay in caller) per CEO 2026-05-10. LOC delta: bellows.py +13, test_consume_verdicts.py −27, net −14. Test suite: 246 passed, 1 pre-existing failure (`test_run_step_timeout`), 0 new failures. QA verified 13/13 properties (`bellows/knowledge/qa/startup-sweep-extract-qa-2026-05-10.md`). Step 2 tripped `rule_20_self_check` gate due to Planner banner substitution; overridden via continue verdict after Rule 22 verification (gate failure captured as separate open BACKLOG item 2026-05-10 above). Reference: `bellows/knowledge/decisions/Done/executable-startup-sweep-extract-2026-05-10.md`.

- **Closed 2026-05-10:** Stranded plan/verdict files in invoice-pulse `knowledge/decisions/` (originally 2026-05-06). (a) `in-progress-diagnostic-continuation-rule-rounding-2026-05-06.md` was a 128-byte 5-line stub (header only, no body) — moved to `invoice-pulse/knowledge/decisions/Done/archived-stub-in-progress-diagnostic-continuation-rule-rounding-2026-05-06.md`. Canonical content preserved at `Done/parallel-1-diagnostic-continuation-rule-rounding-2026-05-06.md` (9274 bytes, 188 lines). (b) `verdict-pending-diagnostic-lanes-csrf-fix-verification-2026-05-06.md` was already cleaned up before this session (not present in `decisions/`); canonical at `Done/diagnostic-lanes-csrf-fix-verification-2026-05-06.md`. Surgical scope — 5 other stranded files observed in `decisions/` during cleanup left in place (staging file, halted plan from 2026-04-20, SA blueprint, two verdict-pending plans). Audit those separately if priority warrants.

- **Closed 2026-05-10 (hygiene):** S3 Bug C — stale-verdict check does not search `halted-*` plans (originally 2026-05-09). Stale entry — fix already shipped same-day. Commit `db78919` ("fix(verdicts): stale-verdict check recognizes halted-* plans") added parallel halted-* detection in `_consume_verdicts` stale-check branch at `bellows.py:1045-1048` (`if dname.startswith("halted-") and plan_slug in dname: stale = True`). +1 regression test in `tests/test_consume_verdicts.py`. QA verification at commit `0c90626`. Fix verified live in current code. BACKLOG entry was authored before close — same pattern as 2026-05-03 step-count regression (entry authored from symptom observation without scanning Done/ for same-day corrective activity, per LESSONS.md 2026-05-03 entry).

- **Closed 2026-05-10:** `_teardown_worktree` cherry-pick fragility (originally 2026-05-07). SA diagnostic at `bellows/knowledge/architecture/teardown-cherry-pick-audit-2026-05-10.md` overturned the BACKLOG hypothesis — the code has been multi-SHA from day 1 (commit `36b2bba`, 2026-05-03). `_teardown_worktree` collects all non-main commits via `git log HEAD --not main` at `bellows.py:909-913` and iterates cherry-picks at `bellows.py:935-948` — there is no single-SHA code path. The 2026-05-07 terminal log showing one cherry-pick command was the first iteration of the loop timing out on stale `.git/index.lock`; subsequent iterations never ran because the entire teardown was killed. Stranded-commit population audit (22 invoice-pulse plans, 2026-05-03 → 2026-05-10): 0 cases of partial cherry-pick (Class C), 2 cases of teardown-never-completed (Class D — the two stranded commits accepted as loss 2026-05-10). The actual Failure 1 (stale lock) was closed by commit `8eac4c3` (2026-05-10) which adds lock detection at `bellows.py:925-932` before the cherry-pick loop. No further code change required. Reference: `bellows/knowledge/decisions/Done/diagnostic-teardown-cherry-pick-audit-2026-05-10.md`.

- **Closed 2026-05-10:** S3 — verdict-resolved retry loop when target plan in Done/ (originally 2026-05-08). Stale entry — structurally fixed since 2026-04-24. Diagnostic at `bellows/knowledge/research/s3-done-stale-check-verification-2026-05-10.md` overturned the BACKLOG hypothesis. The Done/ stale-check at `bellows.py:1033-1046` (within `_consume_verdicts` no-match branch) was introduced 2026-04-24 in commit `c7f69f3` as part of the same atomic change that created the `plan_matched` conditional logic that enables retry-loop behavior — the check has never been absent while the retry-loop was possible. The 2026-05-08 symptoms were caused by **S3 Bug A** (verdict-request-* files in resolved/ misparsed as legitimate verdicts via the verdict filename regex, producing bogus slugs like `request-foo` that match nothing in Done/), fixed 2026-05-09 in commit `dc0bdd7` with the `if fname.startswith("verdict-request-"): continue` exclusion filter at the top of `_consume_verdicts`. Empirical confirmation: zero stuck verdict files currently in `bellows/verdicts/resolved/`. Reference: `bellows/knowledge/decisions/Done/diagnostic-s3-done-stale-check-verification-2026-05-10.md`.

- **Closed 2026-05-10:** Plan filename not flipped from in-progress- to verdict-pending- on pause (originally 2026-05-08). Stale entry — structurally fixed by upstream bugs. Diagnostic at `bellows/knowledge/research/in-progress-rename-verification-2026-05-10.md` overturned the BACKLOG hypothesis (rename bug). The rename code at all 4 sites in `run_plan()` (bellows.py:291-293, 371-373, 456-458, 483-485) has been structurally correct since commit `c7f69f3` (2026-04-24, which introduced `base_filename` canonicalization). The 2026-05-08 observations were symptoms of two distinct upstream bugs, both fixed 2026-05-08: (1) `qa-` prefix not in `is_runnable_plan()` dispatch whitelist (commit `e5188fa`); (2) `_parse_plan_header()` only read YAML frontmatter, not pipe-separated headers (commit `afc8523`). Empirical confirmation: 2026-05-10 session ran 5 plans with 7 total pause events; all 7 verdict files reached `processed-` prefix in `verdicts/resolved/`, which structurally proves correct rename at every pause event. Reference: `bellows/knowledge/decisions/Done/diagnostic-in-progress-rename-verification-2026-05-10.md`.

- **Closed 2026-05-10:** deposit_exists / rule_20_self_check gate path-resolution gap recurred on action queue aggregation session (originally 2026-05-07). RC1 fixed in code (pending Bellows restart); RC2 deferred to governance. Diagnostic at `bellows/knowledge/research/gate-path-resolution-post-teardown-2026-05-10.md` overturned the BACKLOG hypothesis (post-teardown timing) — gates.check() at bellows.py:333 runs BEFORE _teardown_worktree at bellows.py:362. Two distinct root causes: **RC1 (HIGH confidence)** — `_gate_rule_20_self_check` was missed by the 2026-05-06 worktree-aware fix. Fixed via `executable-rule-20-self-check-wt-path-2026-05-10`: 3 surgical edits to gates.py (signature, internal call, caller) + 1 regression test. Gates suite: 62/62 passing. **RC2 (MEDIUM confidence)** — legacy prose regex in `_extract_plan_required_deposits` captures unresolvable abbreviated paths. Resolution path: ensure all plan steps use explicit `**Deposits:**` blocks per Rule 26 — governance-only, no code change required. Reference: `bellows/knowledge/decisions/Done/diagnostic-gate-path-resolution-post-teardown-2026-05-10.md`, `Done/executable-rule-20-self-check-wt-path-2026-05-10.md`.

- **Closed 2026-05-10:** Inactivity timeout does not fire on hung runner (originally 2026-05-06, REPRODUCED). Config tightened, mechanism was correct all along. Diagnostic at `bellows/knowledge/research/inactivity-timeout-investigation-2026-05-10.md` disproved all three hypothesized bugs (config-load, stdout deadlock, canary reset) via code trace and 684-log corpus analysis showing 36 successful historical timeout kills. Root cause: threshold mismatch, not code defect. The configured 1800s threshold exceeded the 910s inactivity gap observed at manual-kill time on 2026-05-06; the kill check `age >= 1800` evaluated `910 >= 1800 → False`. The 300s → 1800s threshold change between 2026-05-01 and 2026-05-06 created a 30-minute dead zone where hung processes can't be auto-killed. Fix shipped: config-only change `step_inactivity_timeout_seconds: 1800 → 600` (2x headroom over observed 304s max inter-event gap). Zero code changes. Reference: `bellows/knowledge/decisions/Done/diagnostic-inactivity-timeout-investigation-2026-05-10.md`.

- **Closed 2026-05-05:** BACKLOG `2026-05-01: Bellows section omits parallel-N- dispatch mechanics`. Shipped via `parallel-1-executable-planner-template-parallel-group-dispatch-subsection-2026-05-05.md`. PLANNER_TEMPLATE.md bumped to v4.32, new `### Parallel Group Dispatch` subsection added covering filename prefix convention, `_pending_groups` mechanics + 5-second settle window, concurrency model, working-tree shared state with cross-reference to BACKLOG #1, and practical implication. Two-commit split per Rule 8 governance-root pattern: governance commit `bb00f87`, bellows dev-log commit `2702f28`. Reference: `bellows/knowledge/decisions/Done/parallel-1-executable-planner-template-parallel-group-dispatch-subsection-2026-05-05.md`.


- **Closed 2026-05-06:** BACKLOG #1 (`deposit_exists` gate reports plan-required deposits as missing when files exist on disk). Root cause confirmed as path resolution drift (worktree-vs-project_path mismatch) via diagnostic at `bellows/knowledge/research/deposit-exists-false-positive-root-cause-2026-05-06.md`. Fix shipped via executable-deposit-exists-worktree-aware-2026-05-06: `_resolve_deposit_path` now accepts `wt_path` and tries worktree-first resolution; `gates.check()` and `_gate_deposit_exists` thread `wt_path` through; both `gates.check()` call sites in `bellows.py` pass `wt_path`. Strategy 0 guard `if wt_path != project_path` preserves bellows-self compatibility. 6 new unit tests; 0 regressions.

- **Closed 2026-05-05:** BACKLOG `2026-05-05: deposit-parser captures backtick-wrapped paths from step prose`. Diagnostic at `bellows/knowledge/research/deposit-parser-prose-failure-diagnosis-2026-05-05.md` overturned the original entry's hypothesis — root cause was in agent-receipt parser at `gates.py:157` inside `_gate_deposit_exists` (not plan-text parser as the entry hypothesized). Fix: regex extraction at gates.py:157 plus fallback to legacy `.strip("`")` for lines without backticks. +4 unit tests in `test_gates.py`. Reference: `executable-deposit-parser-agent-receipt-fix-2026-05-05`, diagnostic findings file.

- **Closed 2026-05-05:** BACKLOG `2026-04-30: scope_check diff-collision from concurrent activity (REOPENED 2026-05-01 after failed close attempt; external-vector reproduction added 2026-05-04)`. Closed via population audit at `bellows/knowledge/research/backlog-1-reproduction-audit-2026-05-05.md` after delta-check at `bellows/knowledge/research/worktree-delta-check-2026-05-05.md` confirmed the per-plan git worktree fix shipped in commit `36b2bba` on 2026-05-03 with detect-and-skip for monorepo-bellows added in commit `06aa938` on 2026-05-04. Audit window 2026-04-30 through 2026-05-05 enumerated 7 scope_check failures: 3 classified as (b) pre-fix, 4 classified as (a) bellows-self exposure, 0 classified as (c) real-`.git` post-fix gap. Zero real-`.git` projects tripped scope_check in the audit window. The remaining bellows-self exposure vector is captured as a separate known-constraint Open entry — it is an accepted tradeoff from the 2026-05-04 close, not an unfixed bug. Reference plans: `Done/diagnostic-worktree-delta-2026-05-05.md`, `Done/diagnostic-backlog-1-reproduction-audit-2026-05-05.md`.

- **Closed 2026-05-04:** `2026-05-04: monorepo-worktree-at-governance-root structural fix`. Shipped via `executable-monorepo-worktree-fix-2026-05-04.md` (Option A: detect-and-skip). Implementation: `_create_worktree` checks `os.path.exists(os.path.join(project_path, ".git"))` at the top of the function — when False, logs and returns `project_path` directly, skipping all worktree creation. `_teardown_worktree` becomes a no-op via sentinel comparison. Bellows-self plans now run in-place against the live working tree without isolation; all 7 projects with their own `.git` are unaffected. 3 new unit tests added; 90/90 targeted tests passing. Validated via `diagnostic-monorepo-worktree-fix-canary-2026-05-04.md`. Tradeoff acknowledged: bellows-self plans no longer get worktree isolation. Reference: `executable-monorepo-worktree-fix-2026-05-04.md`.

- **Closed 2026-05-03:** worktree teardown crash. Diagnostic at `knowledge/research/worktree-teardown-bug-diagnosis-2026-05-03.md` identified 4 type-contract-violation sites in `bellows.py`. Type-mismatch fix at commit `272fbe4` changed `gate_result["failures"]` entries from plain strings to dicts matching `verdict.py::post_verdict_request` contract. New regression test in `tests/test_verdict.py`. 87 targeted tests pass. QA verified at commit `0f2059f`.

- **Closed 2026-05-03:** BACKLOG `2026-05-03: multi-step plan step-count regression`. Root cause was an unconditional `if is_diagnostic: total_steps = 1` override at two sites in `bellows.py` that forced all diagnostic plans to single-step. The narrow-override re-fix (`executable-corrective-narrow-is-diagnostic-override-2026-05-03`, commit `9786e87`) replaced the unconditional override with `if total_steps == 0 and is_diagnostic: total_steps = 1` at both sites, preserving Phase 8.1 single-step fallback for header-less diagnostics while letting multi-step diagnostics count their headers correctly. QA verification: behavioral spot-check confirmed `extract_total_steps()` returns 3 for the exact 3-step plan that the BACKLOG entry cited as broken; 66 passed, 0 failed.

- **Closed 2026-05-01:** Phase 3b/3c DB step-state-resume slug-collision. Diagnostic at `knowledge/research/phase-3b-mechanism-and-cost-benefit-2026-05-01.md` confirmed phantom-resume mechanism. Q6 cost-benefit recommended F3 (remove dead code) — Phase 3b/3c guards a manual-rename resume path that's unsupported per Rule 25, and the supported verdict-resume path passes resume_step explicitly without consulting the DB. Reference: `executable-remove-phase-3b-3c-2026-05-01`.

- **Closed 2026-05-01:** BACKLOG #3 (`_cleanup_verdicts_for_slug` call-site gap). Diagnostic at `knowledge/research/cleanup-verdicts-call-site-gap-2026-05-01.md` identified the actual root cause as slug normalization mismatch. Fix: (1) compute `cleanup_slug = verdict.slug_from_path(original_name)` after plan matching, (2) use `cleanup_slug` at 3 call sites, (3) normalize `lookup_slug` at the pre-match verdict-request-file lookup, (4) remove Done/ slug loop from startup sweep. +3 regression tests. Commit `bc09bb5`. Reference: `executable-cleanup-slug-normalization-2026-05-01`.

- **Closed 2026-05-01:** BACKLOG `2026-04-17: activity-based timeout`. The inactivity-based timeout was already shipped 2026-04-17 via `executable-activity-timeout-2026-04-17`. BACKLOG entry was stale — never closed when the work landed. Diagnostic at `bellows/knowledge/research/activity-based-timeout-diagnosis-2026-05-01.md` audited current state, ran empirical event-cadence analysis (P50=4.0s, P90=16.8s, P99=119.5s, longest run=743s, zero timeout kills in 100-log corpus), and recommended tightening the inactivity threshold from 2400s to 300s. Config tightened: `step_inactivity_timeout_seconds: 300` added to `bellows/config.json`.

- **Closed 2026-05-01 (hygiene):** "2026-04-18: Planner↔Bellows integration protocol underdefined." All three blocking dependencies resolved. Bellows Execution Model section added to PLANNER_TEMPLATE.md v4.29 (2026-04-30). Rules 22–26 codify the Planner-side discipline this entry was waiting to specify. Audit findings at `bellows/knowledge/research/bellows-integration-section-audit-2026-05-01.md` confirm v4.29 section is accurate against live code. Reference: `diagnostic-bellows-integration-section-audit-2026-05-01.md`.

- **Closed 2026-04-30:** BACKLOG #11 (deposit_exists gate rejects directory paths in Rule 26 **Deposits:** blocks). Fix: _resolve_deposit_path in gates.py extended to accept paths that exist as either files (os.path.isfile) or directories (os.path.isdir) at any of three resolution strategies. +5 unit tests. Reference: parallel-1-executable-deposit-exists-directory-paths-2026-04-30.

- **Closed 2026-04-30:** BACKLOG #12 (ledger.jsonl schema gap — pause_reason_code not persisted). Fix: log_to_ledger() extended with optional pause_reason_code parameter; field added to JSONL entry dict; all bellows.py call sites updated to pass appropriate codes. +3 unit tests. Reference: parallel-1-executable-ledger-pause-reason-code-2026-04-30.

- **Closed 2026-04-30:** verdict mechanization (BACKLOG `2026-04-18: verdict mechanization`). Distribution audit shipped via `diagnostic-verdict-mechanization-distribution-audit-2026-04-30`. Audited 56 ledger entries: 25% auto-close, 36% `auto_close_disabled` clean, 34% `gate_failure` (all spurious — fixed via gate precision shipped 2026-04-28), 5% `qa_checkpoint`. **0 of 42 paused verdicts** had a real Planner finding. Three mechanization scopes evaluated; recommendation: don't mechanize. Reasoning: highest-friction class is gate_failure (now fixed); auto_close_disabled is already lowest-friction; removing the safety net for marginal time savings is wrong tradeoff. Revisit triggers documented in findings file. CEO accepted recommendation.

- **Closed 2026-04-30 (hygiene):** BACKLOG `2026-04-23: no_permission_denials gate trips on successful diagnostics`. Superseded by BACKLOG #2 close (2026-04-28) which added `READ_CLASS_TOOLS = {"Grep", "Glob", "Read"}` to gates.py and modified `_gate_no_permission_denials` to filter read-class denials.

- **Closed 2026-04-30 (hygiene):** BACKLOG `2026-04-18: step state lost across re-claim`. Superseded by BACKLOG #6 Phase 3b (DB-based step state recovery, shipped 2026-04-28: `plan_slug` column added to `runs` table; `_get_last_completed_step(db_path, plan_slug)` helper added; `run_plan()` now queries DB for last completed step when `resume_step is None` and shadow cache exists) and Phase 3c (plan-hash drift warning, shipped 2026-04-30).

- **Closed 2026-04-28 (hygiene):** auto-chain-despite-STOP behavior (originally 2026-04-19) re-classified as intentional under the disable-auto-close model (shipped 2026-04-24). Under disable-auto-close, every plan's terminal step pauses unconditionally for CEO verdict regardless of whether intermediate steps auto-chained. The intermediate auto-chain is the intended behavior; the terminal pause is the safety gate. STOP instructions in step prompts remain useful as belt-and-suspenders. No code change needed; the BACKLOG entry was reasoning about a model that no longer applies.

- **Closed 2026-04-28 (hygiene):** Planner verdict-folder-polling (originally 2026-04-18) shipped as PLANNER_TEMPLATE Rule 25 (v4.24, codified 2026-04-18). Rule 25 mandates that every conversation turn with an active session-plan, the Planner scans `bellows/verdicts/pending/`, reads matching verdict requests, routes on `Pause Reason Code`, and either auto-proceeds to Rule 22 verification or stops-and-reports.

- **Closed 2026-04-28:** BACKLOG #5 (deposit parser gap for Rule 26 block form, originally 2026-04-19) closed by `executable-deposits-block-regex-blank-line-2026-04-28` (commits `fbb0aeb` + `6bedb82`). The actual gap was NOT what the BACKLOG entry described — `extract_primary_deposit()` already reads the `**Deposits:**` block. The real gap was a regex bug: `DEPOSITS_BLOCK_RE` failed to match when blank blockquote lines `>\n` sat between the `**Deposits:**` header and the first bullet. Fix: inserted `(?:[> ]*\n)*` between the header-newline and the bullet group in both copies of the regex. 6 new tests added.

- **Closed 2026-04-28:** BACKLOG #4 (scope_check false-positive over too-wide git range, originally 2026-04-19) closed by `executable-scope-check-monorepo-fix-2026-04-28` (commit `8db0adc`). Diagnostic at `bellows/knowledge/research/scope-check-monorepo-diagnosis-2026-04-28.md` revealed bellows is the only watched project without its own `.git`. Fix: added `--relative -- .` to the git diff argv. Universally applicable across nested and standalone repos. New unit test `test_capture_git_diff_uses_relative_pathspec` locks the argv shape.

- **Closed 2026-04-28:** deposit_exists prose-path false positive (originally 2026-04-23) confirmed resolved via live canary `executable-deposit-exists-prose-canary-2026-04-28`. Step 1 verdict request showed `Deposit: bellows/knowledge/research/deposit-exists-prose-canary-findings-2026-04-28.md` (single declared path, prose distractors correctly ignored), confirming `_extract_plan_required_deposits()` block-scoping holds live post-Bellows-restart.

- **Closed 2026-04-24:** Reliability bugs 1, 2, 3 (originally 2026-04-24) shipped via `executable-bellows-reliability-bugs-1-2-3-2026-04-24.md`. Commit `c7f69f3`. (1) `_consume_verdicts` unconditional `shutil.move` to `processed-*` — fixed with `plan_matched` boolean gate + stale-verdict Done/ check. (2) Pause-time rename wrong-path — fixed by introducing `base_filename` canonicalization at 5 path-construction sites in `run_plan()`. (3) `extract_total_steps()` case-sensitive regex — fixed with `re.IGNORECASE` and tightened to require `\s+\d+` after `## STEP`. 10 new unit tests added.

- **Closed 2026-04-24:** Failure 3 ordering diagnostic + disable-auto-close fix shipped. Diagnostic confirmed that 5 of 6 blocking gates are post-hoc (auto-close race is structural). Fix shipped via `executable-disable-auto-close-2026-04-24.md`: bellows.py default inverted from bifurcated to universal `"false"`; PLANNER_TEMPLATE.md v4.26 → v4.27 with 7 anchored edits to Rules 8, 23, 25 removing agent-owned move-to-Done and adding Planner-owned terminal-step resolution. Auto-close is now default-false for all plans — terminal steps always produce a verdict request, Planner performs the Done/ move after Rule 22 verification.

- **Closed 2026-04-24:** QA-checkpoint-decorative pause (originally 2026-04-19) — same root cause as Failure 3 (agent housekeeping races Bellows gate evaluation). Structurally resolved by disable-auto-close: agents no longer perform move-to-Done, so Bellows's gate evaluation no longer races against anything.

- **Closed 2026-04-24:** single-step diagnostic generates verdict request on success (originally 2026-04-19) — now intended behavior. Per the disable-auto-close model shipped 2026-04-24, every terminal step produces a verdict request (including single-step diagnostics). Planner resolves via Rule 22 verification and performs the Done/ move.

- **Closed 2026-04-19:** Plan file truncation v2 (BACKLOG #6) — resolved by R3 variant (c) shadow cache prompt (executable-r3-shadow-cache-prompt-2026-04-19). All four prompt construction sites now reference `.bellows-cache/*.pristine` instead of the mutable `in-progress-*` path. Agents never learn the mutable plan file path, eliminating the truncation vector. Commit range: 6b085b8..5297a06.

- **Closed 2026-04-19:** Agent rewrites plan files during execution (BACKLOG #7) — resolved by same R3 variant (c) as BACKLOG #6. Shadow cache path is read-only by design. Commit range: 6b085b8..5297a06.

- **Closed 2026-04-19:** Shipped via executable-verdict-lifecycle-coupling-2026-04-19. _cleanup_verdicts_for_slug helper + 4 terminal-state call sites + one-time startup sweep + 4 unit tests. Commit range: c5c7742..cf2c68a.

- **Closed 2026-04-19:** Resolved as side-effect of verdict lifecycle coupling. The startup sweep cleans the stranded files #1 produces.

- **Closed 2026-04-19:** `_handle` subdirectory dispatch regression (introduced by same-day BACKLOG #4 `on_moved` fix). The `on_moved` handler did not filter on `dest_path` parent directory — so when the Planner moved a completed plan to `decisions/Done/`, the handler dispatched it for re-execution from inside Done/. Fix: guard placed in `_handle()` to reject any dispatch whose `path_parent` is not in `watched_projects`. Single location covers on_moved, on_created, on_modified simultaneously. Executable: `executable-handle-subdirectory-guard-2026-04-19.md` — commit `fb1d5f3`, 42 tests passing.

- **Closed 2026-04-19:** filesystem watcher reliability (BACKLOG #4) — PlanHandler now overrides on_moved. Root cause was that macOS FSEvents fires on_moved (not on_created) for same-directory renames, and PlanHandler inherited the default no-op on_moved from FileSystemEventHandler. Fix adds a 3-line on_moved override that calls _handle(event.dest_path). Two new unit tests. Executable: `executable-watcher-on-moved-handler-2026-04-19.md`.

- **Closed 2026-04-19:** verdict request file schema diagnostic + all 5 proposed schema additions are complete. Schema diagnostic ran 2026-04-18. All 5 proposed additions (Project, Pause Reason Code, Gate Result Passed, Deposit, Total Steps with None guard) shipped via `executable-bellows-verdict-schema-plan-a-2026-04-18` and `executable-bellows-verdict-schema-plan-b-2026-04-18`. Test coverage: 24 tests across `test_verdict.py`, `test_extract_primary_deposit_blocks.py`, `test_rule_26_deposit_parser.py`.

- **Closed 2026-04-19:** deposit-path parser false positives (originally 2026-04-18, BACKLOG #6) resolved by executable-rule-26-deposit-parser-scope-2026-04-19 (`_extract_plan_required_deposits` now prefers declared `**Deposits:**` block over legacy prose regexes) + executable-extract-primary-deposit-block-aware-2026-04-19 (`extract_primary_deposit` now reads block form for verdict file `Deposit:` field). 12 tests added total. Smoke-tested end-to-end 2026-04-19. Three failure signatures resolved: bracketed-template paths inside embedded specialist content; test-fixture strings embedded inside QA-step prompt text; literal placeholder paths inside code-fence example blocks.
