{
  "plan": "100040",
  "target": "scripts/cycle_check.py",
  "description": "Kill map for the in-cycle battery (run_battery, run_check wiring, emit_manifest).",
  "mutants": [
    {
      "id": "M1",
      "description": "Drop the plan_lint downgrade arm — replace `b['plan_lint'] not in ('N/A', '0_FAIL')` with `False`.",
      "killed_by": "test_plan_lint_fail_downgrades_bar_met",
      "test_number": 2
    },
    {
      "id": "M2",
      "description": "Apply the downgrade on CONTINUE too — remove the `if v == 'BAR_MET':` guard and always check for downgrade.",
      "killed_by": "test_continue_plan_lint_fail_no_downgrade_warn",
      "test_number": 14
    },
    {
      "id": "M3",
      "description": "Treat VACUOUS as DRIFT — return 'DRIFT' for rc=2 stdout starting 'FOLD-CHECK VACUOUS'.",
      "killed_by": "test_fold_check_vacuous_does_not_downgrade",
      "test_number": 4
    },
    {
      "id": "M4",
      "description": "Match a different token for VACUOUS — use 'VACUOUS:' instead of 'FOLD-CHECK VACUOUS'.",
      "killed_by": "test_fold_check_vacuous_does_not_downgrade",
      "test_number": 4
    },
    {
      "id": "M5",
      "description": "Skip the manifest fold_baseline: resolution — always use beside-the-plan file, ignoring the manifest field.",
      "killed_by": "test_baseline_resolution_order",
      "test_number": 10
    },
    {
      "id": "M6",
      "description": "Run the battery on an ESCALATE arm — add run_battery() call in _escalate().",
      "killed_by": "test_escalate_no_tool_launches",
      "test_number": 7
    },
    {
      "id": "M7",
      "description": "Map rc!=0 back to N/A in the emitter — restore the old `'PASS' if rc == 0 else 'N/A'` in the validation string.",
      "killed_by": "test_emit_manifest_fold_check_values",
      "test_number": 8
    },
    {
      "id": "M8",
      "description": "Give the emitter its own battery copy — restore private subprocess calls in emit_manifest instead of using run_battery.",
      "killed_by": "test_one_implementation",
      "test_number": 9
    },
    {
      "id": "M9",
      "description": "Emit one combined WARN when both tools fire — join into a single WARN line instead of two separate ones.",
      "killed_by": "test_fold_check_drift_both_fire_two_warns (both-fire assertion)",
      "test_number": 3
    }
  ],
  "status": "0 ERROR required — all nine mutants killed"
}
