# QA Report — plan_lint §4 refinements: last-lens-line status (189/N5) + T0 regex fix (190/N6)

**Plan:** 277 | **Step:** 2 (QA) | **Date:** 2026-07-25

## Step-1 Dev-Log Review

Output Receipt confirmed **Complete** in `knowledge/development/plan-lint-189-190-dev-2026-07-25.md`. Two edits to `scripts/plan_lint.py` (190 regex `$`→`\b` at line 165; 189 last-lens-line primary + closing fallback at lines 196–221), one fixture edit to `tests/test_plan_lint.py` (`_fold_closing_warns` ACID line `w1 dry.` → `w1 1 folded.`), and 12 new tests. All warn-first (exit 0 preserved).

## Verification Table

| # | Claim | Result | Evidence |
|---|---|---|---|
| 1 | **Warn-first preserved** — plan_lint on a tier-less plan prints `WARN: no cycle_tier declared` + exit 0; on a T1 plan missing ACID prints `WARN: … missing lens(es): ACID` + exit 0. The `(f)` checks can never block a deposit. | ✅ PASS | Tier-less: `WARN: no cycle_tier declared (DRAFTING_CYCLE.md §1/§3)`, exit 0. T1 missing ACID: `WARN: Drafting Cycle block missing lens(es): ACID (DRAFTING_CYCLE.md §3)`, exit 0. Both WARN via bare `print()`, never append to `results`, never set `all_passed=False`. |
| 2 | **190 fixed** — T0 plan with collapsed form `**cycle_tier:** T0 (no trigger); …` emits NO cycle_tier WARN + exit 0. Regex is `^T([012])\b`. | ✅ PASS | Collapsed T0: zero `cycle_tier`/`not recognized` output, exit 0. `grep -n 'T.*012' scripts/plan_lint.py` → line 165: `ct_match = re.match(r'^T([012])\b', cycle_tier_raw)`. |
| 3 | **189 primary reads the last lens line** — plan whose ACID last segment is `dry` (with fold-mentioning closing prose) → NO fold WARN; plan whose ACID last segment is `folded` → fold WARN fires. Both exit 0. | ✅ PASS | Dry ACID + fold-closing prose: zero `fold as last event` output, exit 0 (primary reads `dry` in the ACID lens line → no WARN). Folded ACID: `WARN: Drafting Cycle closing indicates fold as last event, not a dry lens pass (DRAFTING_CYCLE.md §2)`, exit 0. |
| 4 | **189 fallback intact** — legacy block with no structured lens lines + fold-closing line → fallback fires the WARN (back-compat), exit 0. | ✅ PASS | Legacy block (no lens-line regex matches, fold-closing prose): `WARN: Drafting Cycle closing indicates fold as last event, not a dry lens pass (DRAFTING_CYCLE.md §2)`, exit 0. Fallback branch at lines 213–220 exercised. |
| 5 | **Compliant real plan clean** — plan_lint on a current compliant T2 plan emits NO drafting-cycle WARN. | ✅ PASS | Compliant T2 (real 270 block): zero `fold`/`cycle_tier`/`Drafting Cycle`/`Closing` output, exit 0. |
| 6 | **No crash on degenerate input** — empty/malformed `## Drafting Cycle` block WARNs or passes, never raises. | ✅ PASS | (a) Empty block: WARNs for missing lenses + no Closing line, no traceback, exit 0. (b) Statusless block: same, exit 0. (c) Lens lines with placeholder status: no crash, no false fold-WARN, exit 0. |
| 7 | **Existing behaviour intact** — every prior plan_lint test passes; the ONLY test edit was `_fold_closing_warns`'s fixture (Task C), intent preserved. | ✅ PASS | `git diff cc0777c~1..cc0777c -- tests/test_plan_lint.py` confirms: single fixture line changed (`- ACID: w1 dry.` → `- ACID: w1 1 folded.`); assertions at lines 475-476 unchanged (`"fold"` and `"dry lens pass"` substrings). 12 new tests added after line 476. All 33 plan_lint tests pass (21 existing + 12 new). |
| 8 | **Scope** — changes limited to `scripts/plan_lint.py` + `tests/test_plan_lint.py`; no other module. | ✅ PASS | `git diff --stat cc0777c~1..cc0777c`: `scripts/plan_lint.py` (+32 -8), `tests/test_plan_lint.py` (+277 -1), `knowledge/development/…` (+180). No other bellows module touched. The docs regeneration commit (698f408) only touches `knowledge/research/agent-prompt-feedback.md` (+5). |
| 9 | **Full suite** — all tests pass, no regressions. | ✅ PASS | `python3 -m pytest tests/ --tb=short -q`: **825 passed**, 1 warning in 20.63s. Baseline: 813 (from plan 271 QA, `--collect-only` verified on parent commit). Delta: +12 new tests (4 real-log + 2 synthetic dry/fold + 1 legacy fallback + 3 degenerate + 1 T0 collapsed + 1 compliant T2 re-verify). No new failure. See `evidence/plan-lint-189-190-2026-07-25/full-suite.txt`. |

## Rule 20 Self-Check

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
Evidence folder: /Users/marklehn/Developer/GitHub/bellows/.bellows-worktrees/277/knowledge/qa/evidence/plan-lint-189-190-2026-07-25/
Files verified: 2
```

### Ledger Updates

#### Project Status

- **plan_lint §4 refined** — 189/N5 reads the last lens line's status (closing-line fallback for legacy logs), 190/N6 T0 regex loosened to `^T([012])\b`; warn-first preserved; Gate 2 Plan B complete, Plan A (doc codification + status advancement) unblocked.

#### Prompt Feedback

- The dev-log stated "13 new tests added" but the actual count is 12 new test functions (the `_fold_closing_warns` edit was a fixture change to an existing test, not a new test). Minor dev-log inaccuracy — the tests themselves are correct and comprehensive.
- The CB1 safe rule (fold-token AND no-dry) correctly prevents false fold-WARNs on all 4 real-log embedded fixtures (271/274/275/diag-276), which contain the `→ dry. N folds cohere` pattern that would have false-WARNed under the diagnostic's naive `;`-split approach.
- The `\b` regex fix for 190 is clean — confirmed a single occurrence at line 165, accepts bare T0/T1/T2 + collapsed T0 form, rejects T3/T0X.
