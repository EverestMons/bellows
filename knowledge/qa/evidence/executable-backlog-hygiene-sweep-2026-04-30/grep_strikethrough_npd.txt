23:- ~~2026-04-23: `no_permission_denials` gate trips on successful diagnostics — observed on diagnostic-three-forges-current-state-2026-04-23. Gate fired with 4 Grep tool denials against `/Users/marklehn/Desktop/GitHub/anvil` (Q2(e) entrypoint check). Agent worked around the denials (likely via alternate shell grep), completed all three questions, deposited findings at `forge/knowledge/research/three-forges-current-state-2026-04-23.md`, and self-reported Status: Complete in the Output Receipt. The gate detects a process signal (tool-was-denied) rather than a result signal (task-was-incomplete) — a successful agent that routes around denials still trips the gate. This is the same failure class documented in BACKLOG #6's closed entry under "2026-04-18 third observation" but on a different role: Forge Developer was hitting denials on read-only cross-repo paths (anvil/, governance/) that are outside forge/'s native permission profile. Fix shape options: (a) exempt read-only discovery tools (Grep, find) from the gate when the declared deposit exists and passes content checks, (b) scope the gate to tools whose denial actually blocks execution (write tools, git push, etc.), (c) expand agent permission profiles to cover common cross-repo audit paths for diagnostic plans specifically. Classify as gate precision, not reliability — findings were produced correctly, verdict request was a false positive. Does NOT block work but adds CEO/Planner overhead on every cross-repo diagnostic. **2026-04-23 additional reproduction:** executable-lessons-forge-phase1-schema-ingest-2026-04-23 Step 1 tripped on `Glob` against `/Users/marklehn/Desktop/GitHub` (cross-project root), and executable-adr-002-amendment-classification-external-2026-04-23 Step 2 (QA) tripped on 5 `Grep` calls against `/Users/marklehn/Desktop/GitHub/governance/adr/ADR-002-lessons-forge-design.md`. Both plans' work was completed correctly (agents routed around via bash). Lesson captured in LESSONS.md 2026-04-23: every Claude Code native tool (Read, Grep, Glob, Edit, Write) trips scope_check when invoked on cross-project paths; only bash commands are immune. Planner-side workaround is effective (prompts now instruct agents to use bash exclusively for cross-project access) but the gate still produces noise any time an agent reaches for a native tool out of habit. Fix (a) or (b) from above would eliminate the noise; (c) would too but is harder to scope. **[CLOSED 2026-04-30 — superseded by BACKLOG #2 fix shipped 2026-04-28; READ_CLASS_TOOLS = {"Grep", "Glob", "Read"} in gates.py exempts read-class denials entirely. Verified live in code.]**~~
