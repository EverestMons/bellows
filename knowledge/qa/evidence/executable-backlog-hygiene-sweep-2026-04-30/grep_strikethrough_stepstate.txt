35:- ~~2026-04-18: step state lost across re-claim — when a plan file is renamed from `verdict-pending-*` back to `executable-*` and Bellows re-claims it, step state does not persist. Observed today: `executable-create-forge-developer-specialist-2026-04-18.md` had Step 1 complete and was renamed to pending for resume; on re-claim Bellows reported `plan has 2 steps, ▶ started` and began Step 1 execution from scratch. The run was saved only by the agent's defensive check detecting existing artifacts and reporting "already complete" ($0.18 in wasted API cost). Relying on agent idempotency is unsafe — code-change plans with non-idempotent effects (schema migrations, non-idempotent commits, mutable state updates) would corrupt state on re-run. Fix options: (a) persist step state in `bellows.db` keyed by plan slug, (b) encode completed-step count in filename metadata (e.g., `verdict-pending-[slug]-step-N.md` → resume at N+1 when renamed to `executable-*`), (c) require all plans to declare idempotency and Bellows refuses to re-claim non-idempotent plans post-first-step. Blocks accurate PLANNER_TEMPLATE documentation of Bellows integration — the "correct" resume protocol cannot be documented until this is resolved. **[CLOSED 2026-04-30 — superseded by Phase 3b (DB-based step state recovery, plan_slug column + _get_last_completed_step helper, shipped 2026-04-28) and Phase 3c (plan-hash drift warning, shipped 2026-04-30). The original 2026-04-18 entry's fix option (a) was implemented as recommended.]**~~
