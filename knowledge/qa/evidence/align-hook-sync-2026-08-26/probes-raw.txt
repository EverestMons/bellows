=== Extraction probes on committed hook file ===
Commit: 274aaa6e466e202203c760e3370dc7d0b88cae2b

--- _repo_sync count (expect >= 3, measured 2) ---
2
Note: plan expected >= 3 but the verbatim code block specifies exactly 2 sites:
  line 78: def _repo_sync(label, path):
  line 142: sync = [_repo_sync(l, p) for l, p in _sync_repos()]
Plan probe calibration error — code matches plan specification exactly.

--- REPORT ONLY count (expect == 1) ---
1

--- GIT_TERMINAL_PROMPT count (expect >= 1) ---
1

--- Type /eluvian count (expect == 1) ---
1

--- cmp vs live (expect 0) ---
0 (identical)

=== Test-file extraction ===
--- def test_ count (expect == 6) ---
6
