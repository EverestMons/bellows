1227:| 2026-05-11 | Bellows-side fix-plans modifying plan-text parsers (`_extract_step_text`, `_gate_is_qa_step`, `extract_total_steps`, `_extract_step_text_from_plan`, `strip_fenced_code_blocks`) will trip the bug they fix during their own QA step because the running daemon executes pre-fix code through the plan's lifecycle. Both reproductions today (`executable-fence-strip-plan-text-parsers-2026-05-11` commit `4d57fd3` and `executable-step-header-line-anchor-2026-05-11` commit `0fab609`) hit identical gate failures on Step 2 followed by CEO override. The new Restart Discipline paragraph in v4.38 documents the override pattern. **Meta-lesson:** when a fix-plan's correctness depends on code that does not load until after the plan completes, the gate failure at QA is structurally inevitable, not a defect. |
