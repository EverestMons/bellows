Row Numbering + Defaults Verification
=======================================
Date: 2026-06-13 | Plan: 45 | Step: 2 (QA)

Command: python3 -m pytest tests/test_bellows.py -k "TestApplyLedgerUpdatesForward" -v

tests/test_bellows.py::TestApplyLedgerUpdatesForward::test_writes_correctly_numbered_row PASSED
tests/test_bellows.py::TestApplyLedgerUpdatesForward::test_row_number_handles_withdrawn_closed_rows PASSED

Verified behaviors:
1. Next row number = max(all existing | <n> | rows) + 1:
   - test_writes_correctly_numbered_row: fixture with rows 1-3, new row gets #4
   - test_row_number_handles_withdrawn_closed_rows: fixture with rows 1,2,3,5 (gap at 4, mixed statuses incl. withdrawn/closed), new row gets #6 (max=5, +1=6)

2. Today's date applied:
   - test_writes_correctly_numbered_row: asserts Added column contains today's date (datetime.now().strftime("%Y-%m-%d"))

3. Defaults applied when not specified by agent:
   - Type: "deferred-work" (verified in test_writes_correctly_numbered_row)
   - Plan-id link: "—" (verified in test_writes_correctly_numbered_row)
   - Status: "open" (verified in test_writes_correctly_numbered_row)

4. Row format: 6-column table row | # | Added | Item | Type | Plan-id link | Status |

Code verification (bellows.py:1221-1232):
- row_numbers = [int(m) for m in re.findall(r"^\|\s*(\d+)\s*\|", content, re.MULTILINE)]
- next_num = max(row_numbers) + 1 if row_numbers else 1
- Counts ALL rows regardless of status (open/closed/withdrawn) — gap-safe
