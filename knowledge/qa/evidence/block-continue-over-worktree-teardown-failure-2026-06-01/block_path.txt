Block path — when worktree_teardown predicate fires (bellows.py lines 1345-1358)

When the guard predicate is TRUE, the block path performs:

1. _log("ERROR", ...) — loud error stating continue was rejected, commits not landed, manual R2 required (line 1346)
2. verdict.log_to_ledger(..., "continue-blocked-worktree-teardown", ...) — ledger action with step number and R2 recovery instruction (lines 1347-1350)
3. halted_name = f"halted-{original_name}" — builds halted filename (line 1351)
4. halted_path = os.path.join(decisions_path, halted_name) — builds halted path (line 1352)
5. _cleanup_verdicts_for_slug(cleanup_slug) — removes pending verdict-request file (line 1353)
6. self._seen.discard(cleanup_slug) — clears slug from seen set (line 1354)
7. shutil.move(full_plan_path, halted_path) — moves plan to halted- (NOT Done/, NOT in-progress-) (line 1355)
8. _delete_shadow(original_name) — removes shadow cache entry (line 1356)
9. notifier.notify_plan_halted(original_name) — CEO notification via Pushover (line 1357)
10. break — exits inner pname loop (line 1358)

Routing: halted-{original_name} (confirmed NOT Done/, NOT in-progress-)
Ledger action: "continue-blocked-worktree-teardown"
Skip mechanism: break exits inner loop; plan_matched=True already set so outer loop also breaks; verdict file moved to processed-{fname} by existing plan_matched block at line 1419.
CEO notification: notifier.notify_plan_halted (same as existing stop/halt exit at line 1414)
