Pattern diff check — git diff HEAD~1 -- validators.py
Run by Bellows QA, Step 2, 2026-06-12

$ git diff HEAD~1 -- validators.py

diff --git a/validators.py b/validators.py
index a0d16db..0309637 100644
--- a/validators.py
+++ b/validators.py
@@ -10,9 +10,8 @@ from typing import Optional


 STOP_PROSE_PATTERNS = [
-    re.compile(r"STOP\.", re.IGNORECASE),
-    re.compile(r"wait for confirmation", re.IGNORECASE),
-    re.compile(r"do not proceed", re.IGNORECASE),
+    re.compile(r"^\s*(?:>\s*)*STOP\.", re.IGNORECASE),
+    re.compile(r"^\s*(?:>\s*)*(?:do )?not proceed", re.IGNORECASE),
 ]

Verification:
- Pattern 0 (STOP.): NARROWED to line-start imperative form with optional blockquote prefix — CORRECT
- Pattern 1 (wait for confirmation): REMOVED — CORRECT (zero activations ever)
- Pattern 2 (do not proceed): NARROWED to line-start imperative form, renumbered to Pattern 1 — CORRECT
- No other changes in validators.py — CONFIRMED
- Exactly 2 narrowed regexes + 1 removal, nothing else — CONFIRMED
