=== DELIVERABLE VERIFICATION GREP OUTPUTS ===

--- 1. bellows.py L272 default inversion ---
272:        effective_auto_close = header.get("auto_close", "false").lower() == "true"

--- 2. test_bellows.py new test functions ---
289:def test_executable_no_header_defaults_to_verdict():
341:def test_executable_explicit_auto_close_true_still_closes():

--- 3. PLANNER_TEMPLATE Rule 8 post-edit (three things, STOP directive) ---
451:**PROJECT_STATUS.md update instruction (include in final QA step):** `**Final:** Update PROJECT_STATUS.md — add a completed milestone entry summarizing all changes from this plan (features added, files changed, tests added). Commit with descriptive message. **STOP.** Do NOT move this plan to Done/. Per the disable-auto-close model, the Planner performs the terminal move after Rule 22 verification passes.`

--- 4. PLANNER_TEMPLATE Rule 23 post-edit (Planner performs move-to-Done) ---
453:**Per Rule 23 (housekeeping discipline), the order is feedback append → final commit.** The final commit is the absolute last agent operation. The Planner performs the move-to-Done after Rule 22 verification passes — this is a Planner responsibility, not an agent responsibility. If feedback append fails, the plan correctly strands and the failure is visible. If commit fails, same thing. Separating the agent's final commit from the Planner's move-to-Done eliminates the Failure 3 ordering issue where agents moved plans to Done/ before Bellows evaluated gates, producing stranded verdict requests for already-shipped work.
655:**(c) Ordering: feedback → commit.** The final commit is the absolute last agent operation. The Planner performs the move-to-Done after Rule 22 verification passes — this is a Planner responsibility, not an agent responsibility. If feedback append fails, the plan correctly strands and the failure is visible. If commit fails, same thing. The agent never moves the plan to Done; the Planner does, via `Filesystem:move_file`, after verifying the deposited files pass Rule 22's (a)–(e) checks.

--- 5. PLANNER_TEMPLATE Rule 25 post-edit (Filesystem:move_file) ---
455:**Diagnostic plans use a different closeout pattern — see Rule 22.** As of v4.19, diagnostic plans are single-step (no Step 2 consolidation). The Planner reads the deposited findings file via direct tool call after the Investigation Agent reports Complete, performs the Rule 22 (a)–(e) verification checks, and if all pass, the Planner directly moves the diagnostic file to Done via `Filesystem:move_file` and updates `PROJECT_STATUS.md` via a follow-up Documentation Agent prompt. There is no in-Claude-Code consolidation step for diagnostic plans. This eliminates the recurring failure mode where a Step 2 consolidation gets skipped by the agent — there is no Step 2 to skip, and the housekeeping is performed by the Planner inside the verified pass. Rule 8 (this rule) continues to govern executable plans only: the final QA step in every executable does deliverable verification → QA deposit → PROJECT_STATUS.md update → feedback append → final commit, in that order (per Rule 23). The agent does NOT move the plan to Done. The Planner performs the terminal move after Rule 22 verification passes — the same pattern diagnostic plans already follow.
641:**Diagnostic plans become single-step.** With Rule 22 in place, diagnostic plans no longer need a Step 2 consolidation step. The structure becomes: Step 1 = Investigation Agent investigates, deposits findings, reports completion in conversation. Then the Planner (in the Project conversation, NOT in Claude Code) reads the deposited findings file, performs the (a)-(e) checks above, and if all pass, the Planner does the housekeeping itself via direct `Filesystem:move_file` and updates `PROJECT_STATUS.md` via deposit (when allowed) or via a follow-up Documentation Agent prompt (when not). This removes the recurring failure mode where Step 2 gets skipped — there is no Step 2 to skip, and the housekeeping happens via the Planner's verified pass.
655:**(c) Ordering: feedback → commit.** The final commit is the absolute last agent operation. The Planner performs the move-to-Done after Rule 22 verification passes — this is a Planner responsibility, not an agent responsibility. If feedback append fails, the plan correctly strands and the failure is visible. If commit fails, same thing. The agent never moves the plan to Done; the Planner does, via `Filesystem:move_file`, after verifying the deposited files pass Rule 22's (a)–(e) checks.
667:**(a) Write-to-temp + move.** Write the complete plan to a path OUTSIDE the watched directory (e.g., `/tmp/`, `~/Desktop/GitHub/<project>/`, or any non-`knowledge/decisions/` path), then use `Filesystem:move_file` to move it into `knowledge/decisions/`. Bellows sees one event — the rename — and reads a complete file. This is the most robust option and the recommended default for any plan larger than ~5 KB or any plan that would otherwise require more than one write call.
708:2. If Rule 22 passes: move the plan file from `[project]/knowledge/decisions/verdict-pending-[slug].md` (or `in-progress-[slug].md`) to `[project]/knowledge/decisions/Done/[slug].md` via `Filesystem:move_file`, stripping the lifecycle prefix (`in-progress-`, `verdict-pending-`). Tool call pattern: `Filesystem:move_file(source: "[project]/knowledge/decisions/verdict-pending-executable-[name].md", destination: "[project]/knowledge/decisions/Done/executable-[name].md")`.
1095:| 2026-04-07 | First session with restored filesystem access produced a near-miss data loss event. The Planner saw a directory listing showing 11 stranded `in-progress-` files in `knowledge/decisions/Done/`, AND saw their corresponding non-prefixed `executable-` files also present in Done. The Planner failed to ask "why do both versions exist?" and assumed the in-progress versions were canonical and needed renaming. Fired off six `Filesystem:move_file` calls that succeeded, then panicked on a "STOP — I made a mistake" message claiming six legitimate Done files had been overwritten. The CEO ran `git status` which revealed no overwrites had occurred — the move tool had simply removed the source files without touching the existing destinations. Lesson: `git status` is the safety net for any Planner operation on tracked files and should be consulted BEFORE destructive operations, not after. Added to Guardrails: when the Planner is about to perform a destructive operation (move, rename, overwrite) on a file in a git-tracked directory, it must first check whether the destination already exists via `get_file_info`. If the destination exists, the Planner stops and reports rather than proceeding. The Planner does not have shell or git tool access — it cannot run `git status` itself — so it must reason carefully about destination state from directory listings before acting. The cleanup was completed successfully via a one-line `rm` command run by the CEO for the remaining 5 stranded files (Planner cannot delete files; the right escape hatch is a CEO shell command). |

--- 6. verdict-log.md exists with schema ---
# Verdict Resolution Log

Planner appends to this log on every verdict resolution — observation surface for future auto-approval patterns.

| Plan Slug | Step | Pause Reason | Gates Passed | Gates Failed | Rule 22 Result | Planner Decision | Timestamp |
|---|---|---|---|---|---|---|---|

--- 7. governance_edits_applied.txt exists ---
      57 knowledge/qa/evidence/executable-disable-auto-close-2026-04-24/governance_edits_applied.txt

--- 8. pytest_dev_pre_commit.txt exists ---
     167 knowledge/qa/evidence/executable-disable-auto-close-2026-04-24/pytest_dev_pre_commit.txt
