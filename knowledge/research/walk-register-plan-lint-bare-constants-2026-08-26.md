# Walk register — `plan-lint-bare-constants-2026-08-26` (bellows)

**schema_version:** `0.3`

**Plan:** `bellows/knowledge/decisions/drafts/executable-plan-lint-bare-constants.md`
**Tier:** T1 (Small — one new WARN check in plan_lint + tests + a corpus fire-rate prototype; class shop-infra). **Panel: none.**
**Opened:** 2026-08-26

---

## Walk 0 — context pin (REAL, measured 2026-08-26)

1. **The funnel's contract honored structurally:** the check ships WARN-FIRST (never FAIL) and its DEV step IS the prototype-against-real-pre-fold-states stage — a measured fire-rate run over the newest 60 Done plans, recorded raw, with tuning only inside the declared marker set.
2. **The measured class (the audit's item-4 family, Rule 73's signal):** predicted-number tally ~10 — six pre-session + the 545 census, 549's -k count, 550's pre-existing occurrence, 554's `_repo_sync >= 3`. The 554 instance is the DESIGN CASE: an ad-hoc POST-probe line outside the Numbers table, no local clause → a QA hard-fail on a correct state. The global Numbers-banner deliberately does NOT satisfy the check (stated v0 decision): the banner absorbed the table misses, but the gap being closed is the unclothed ad-hoc line.
3. **Target pins:** `scripts/plan_lint.py` 782 lines; letter-prefixed WARN convention (`(q)` is the last taken — the new check is `(r)`); `lint(plan_path)` at L178; Done corpus 694 plans across the three lanes (prototype sample: newest 60 by mtime, declared).
4. **id prediction:** 561.

⚠️ Walk 0 carries no fold rows. Walks 1–2 appended AFTER the draft exists, from real passes.

---

## Walk 1 — five-lens sequential walk (post-draft, real)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| — | 1 | all five | — | — | DRY on all five — the irony check (this plan's own constants clause-clothed), the regex traced against real non-target shapes, warn-first proven three ways incl. live in QA Item 2, the funnel's stages at their sites, the >600 CEO-priced STOP. | — | No folds. |

**Walk 1 total: 0 findings.**

---

## Walk 2 — five-lens sequential walk (confirming pass, real)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| — | 2 | all five | — | — | DRY — confirming pass. | — | No folds. |

**Walk 2 total: 0 findings — all five lenses dry. BAR MET; T1, no panel escalation.**
