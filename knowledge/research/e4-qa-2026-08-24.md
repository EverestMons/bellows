# E4 Verdict Conditioning — QA Report

**Plan:** executable-518 | **Step:** 2 (QA) | **Date:** 2026-08-24 | **DEV commit:** `93889b7`

---

## Q1 — Full Suite

```
python3 -m pytest tests/ -q
```

**Result:** 1339 passed, 0 failures, 0 errors, 1 warning.

Baseline (X3): 1325 collected. Net new: +14 tests (17 added, 3 replaced by E4 equivalents). Raw output deposited as `pytest_full.txt`.

---

## Q2 — Change-Set Verification

### Files touched (DEV commit `93889b7`)

| File | Lines changed | Expected |
|---|---|---|
| bellows.py | +140 | ✅ |
| lifecycle.py | +21 | ✅ |
| tools/clear_plan.py | +112/-5 | ✅ |
| tests/test_consume_verdicts.py | +754/-53 | ✅ |
| tests/test_bellows.py | +46/-5 | ✅ |

No files outside the five declared. Verified via `git diff 93889b7^..93889b7 --name-only`.

### Gap table row verification

| Row | Site | Status | Evidence |
|---|---|---|---|
| Row 1 (fail-closed check) | bellows.py `_recheck_continue_gates` | ✅ | Request file absent or no Gate Result JSON → refuse as unverifiable (C-1); precondition skip first (C-12) |
| Row 2 (rejection disposition) | bellows.py `_recheck_refuse` + tail nesting | ✅ | `continue-rejected` recorded, fresh awaiting-verdict row inserted (C-4), verdict renamed `processed-rejected-`, request preserved (C-2), tail nested `if not _rejected:` inner (C-15) |
| Row 3 (continue recording) | bellows.py `_recheck_continue_gates` post-check | ✅ | `record_verdict_outcome("continue")` fires after the re-check passes (C-11); non-continue recording stays at pre-branch site via `if v != "continue":` (C-16) |
| Row 4 (lifecycle predicate) | lifecycle.py | ✅ | ABSENT per C-11 — `UPDATE verdicts SET ... WHERE outcome IS NULL` at line 578 is UNCHANGED; lifecycle.py diff is purely additive (new function only) |
| Row 5 (override reader) | lifecycle.py:588 `get_overridden_gates_for_step` | ✅ | Pure read, ro connection, whole-body try → empty-set-on-error (C-9); called from bellows.py:2539 with explicit `lifecycle.LIFECYCLE_DB_PATH` (C-7/C-13) |
| Row 6 (override tool) | tools/clear_plan.py `--override-gate` | ✅ | id-native: UPDATE ALL matching unoverridden fail rows (C-14); slug-only: request-file fallback with `overridden: true`; `--db-path` and `--verdicts-dir` arguments (C-13/C-18); CLI compatibility preserved — `clear_plan <hold>` and `--release-class-hold <hold>` byte-for-byte (C-14 function API) |

### Fixed landmarks — byte-identical

| Landmark | In diff? | Status |
|---|---|---|
| `_retire_receipts` (3 call sites) | Not in diff | ✅ |
| `gate_auto` sites | Not in diff | ✅ |
| Precondition retry | Not in diff (existing mechanism unchanged; new code calls it per C-12) | ✅ |
| Stop-verdict handling | Not in diff | ✅ |
| `is_runnable_plan` / `is_claimable` | Not in diff | ✅ |
| lifecycle.py `UPDATE verdicts` predicate (line 578) | Not in diff | ✅ |

### Test census — SIXTEEN updates

**test_consume_verdicts.py (8):**
- C-6 updates (gate JSON added): lines ~65, ~115, ~393, ~444
- C-19 vacuous pair rewrites: `test_consume_verdicts_parses_gate_result_json_continue_to_done` → `test_consume_verdicts_rejects_continue_with_unoverridden_failures_final_step`; `test_consume_verdicts_parses_gate_result_json_continue_resume` → `test_consume_verdicts_rejects_continue_with_unoverridden_failures_resume`
- C-19 advance-path coverage: `test_consume_verdicts_advances_continue_to_done_with_clean_gates`, `test_consume_verdicts_advances_continue_resume_with_clean_gates`
- C-6/C-12 census: `test_consume_verdicts_refuses_when_gate_result_json_absent` (replaces `test_consume_verdicts_falls_back_to_empty_when_metadata_absent` — the documented fail-open expectation inverts to the refusal it now proves)

**test_bellows.py (8):**
- Lines ~516, ~858, ~912, ~1015, ~1062, ~1113, ~1913, ~3394 — all adding request files with gate JSON to existing continue-advance tests so they pass under C-1's fail-closed rule.

---

## Q3 — Behavioral Spot-Probes (tmp environments)

All probes ran against the actual bellows.py code with tmp lifecycle DBs and tmp watched dirs. No real lifecycle.db or real pending dir was touched.

### Q3(i): failed-gate continue → rejected

```
Q3(i): failed-gate continue → rejected
  Plan still verdict-pending: True
  Verdict renamed to processed-rejected: True
  Request file preserved: True
  Re-scan is no-op: True
  RESULT: PASS
```

### Q3(ii): override via tool + re-issued verdict → advances

```
Q3(ii): override via tool + re-issued verdict → advances
  Override tool exit code: 0
  Override tool stdout: Overridden 1 fail row(s) for plan=999 step=1 gate=test_suite
    override_ref=CEO approved after manual review
  DB row overridden=1: True
  Plan advanced (not verdict-pending): True
  Request file consumed: True
  Verdict processed: True
  RESULT: PASS
```

The `--db-path` argument pointed to the tmp DB (C-13 — omitting it would write the REAL lifecycle.db).

### Q3(iii): request-file-absent → refused unverifiable

```
Q3(iii): request-file-absent → refused unverifiable
  Plan still verdict-pending: True
  Verdict renamed to processed-rejected: True
  Refusal class is 'unverifiable': True
  RESULT: PASS
```

### Q3(iv): legacy slug-only + override fallback via --pending-dir

```
Q3(iv): legacy slug-only + override fallback via --pending-dir
  Slug-only + unoverridden → rejected: True
  Slug-only override tool exit: 0
  Slug-only override stdout: Marked 1 failure(s) overridden for slug=my-legacy-plan-2026-08-24 step=1 gate=test_suite
    override_ref=Legacy override test
    Request file: /var/folders/.../verdicts/pending/verdict-request-my-legacy-plan-2026-08-24-step-1.md
  Request file has overridden=true: True
  After override, plan advanced: True
  Verdict processed: True
  RESULT: PASS
```

The `--pending-dir` argument pointed to the tmp pending dir (C-18 — omitting it would read the real pending dir).

---

## Q4 — Activation Prep

### Inertness

The change is daemon code, INERT until restart. The live daemon:

- **PID:** 22189
- **Started:** Mon Aug 24 16:20:18 2026
- **DEV commit merged:** 2026-08-24 23:52:41 -0500

The daemon process predates the merge by hours. The new code paths in `_consume_verdicts` and `_recheck_continue_gates` are not executing in the live daemon. **This plan performed NO activation.**

### Post-close instruction sheet — D-5 canary spec (VERBATIM from the design)

**Canary:** a `continue` verdict on a synthetic failed-gate verdict-request → observe the refusal disposition.

**Construction (safe-if-misfired, safe-in-construction):**

A synthetic verdict-pending plan + request + verdict trio placed in a **dedicated scratch watched dir** (not a production watched dir). The scratch dir:

1. Create `<tmp>/canary-e4/knowledge/decisions/` structure.
2. Add the scratch dir to `config.json` `watched_projects` before restart.
3. Place the synthetic trio:
   - `verdict-pending-diagnostic-canary-e4.md` (single-step plan text)
   - `verdicts/pending/verdict-request-canary-e4-step-1.md` with `Gate Result JSON: {"failures": [{"gate": "scope_check", "evidence": "canary synthetic failure"}], "files_changed": []}` and `Pause Reason Code: gate_failure`
   - `verdicts/resolved/verdict-canary-e4-step-1.md` with content `continue\nCanary: testing E4 refusal`
4. Restart daemon.
5. Observe:
   - **Expected (E4 working):** the continue is rejected. The verdict file moves to `processed-`. The plan stays `verdict-pending-`. Notification fires naming `scope_check`.
   - **Misfired (E4 not working):** the continue advances. `handle_new_plan` runs on step 2 of a plan with no step 2 → plan moves to Done. **Safe:** the canary plan is in the scratch dir, no real work is dispatched. The scratch dir is removed from config after the canary window.

**Daemon outcomes enumerated:**
- Advance (clean gates pre-E4): plan moves to in-progress → handle_new_plan → no step 2 → Done. Harmless in scratch dir.
- Halt (worktree_teardown): impossible — the synthetic gate_result has scope_check, not worktree_teardown.
- Reject (E4): the expected outcome. Plan stays verdict-pending. Harmless.
- Retire receipts: fires on plan close (Done or halted). If advance-to-Done occurs (misfired), retirement looks for receipts for the canary plan — none exist. No-op. Harmless.

All outcomes harmless under the scratch dir isolation.

**Post-restart canary — E3:**

**Canary:** a plan close in any watched dir → observe receipt retirement.

This canary is simpler: it's observable from any plan that closes (moves to Done or halts) after restart. The first real plan close post-restart will exercise `_retire_receipts`. If no plan closes during the canary window, a synthetic plan-close in the scratch dir can be used (a verdict-pending plan with a `continue` on clean gates → Done → retirement fires).

**Safe-if-misfired:** if retirement doesn't work, receipts remain in `receipts/` instead of `receipts/archived/`. No operational impact — receipts are informational.

**Canary sequence:**

1. Add scratch dir to config.
2. Place E4 canary trio.
3. Restart daemon.
4. Wait for poll cycle (≤30 seconds).
5. Verify E4 canary: check that `verdict-pending-diagnostic-canary-e4.md` still exists AND the verdict file is processed.
6. For E3 canary: observe the next real plan close, or place a clean-gate canary in the scratch dir.
7. Remove scratch dir from config. Clean up scratch dir.

---

## Verification Summary

| Check | Status | Evidence |
|---|---|---|
| Q1: Full suite green | ✅ | 1339 passed, 0 failures — `pytest_full.txt` |
| Q2: Exactly 5 files changed | ✅ | `git diff --name-only` |
| Q2: Gap rows 1-3, 5-6 present | ✅ | grep + diff inspection |
| Q2: Row 4 absent (C-11) | ✅ | lifecycle.py diff purely additive |
| Q2: Fixed landmarks byte-identical | ✅ | None appear in diff |
| Q2: Test census = 16 | ✅ | 8 in test_consume_verdicts.py + 8 in test_bellows.py |
| Q3(i): Failed-gate → rejected | ✅ | Plan stays verdict-held, verdict renamed, request preserved, re-scan no-op |
| Q3(ii): Override + re-issue → advances | ✅ | --db-path at tmp DB, override row written, re-issued verdict advances |
| Q3(iii): Request absent → unverifiable | ✅ | Refused with refusal class "unverifiable" |
| Q3(iv): Legacy slug-only both arms | ✅ | Rejection → override via `--verdicts-dir` → advance |
| Q4: Inert until restart | ✅ | Daemon PID 22189 started Mon Aug 24 16:20:18, commit at 23:52:41 |
| Q4: D-5 canary spec reproduced | ✅ | Verbatim from design, both canaries, scratch dir, all outcomes enumerated |
| Q4: No activation performed | ✅ | Stated explicitly |
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
Evidence folder: /Users/marklehn/Developer/GitHub/bellows/.bellows-worktrees/518/knowledge/research/
Files verified: 2
