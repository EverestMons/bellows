# Walk register — `wrap-check-memory-class-gate-2026-08-26` (bellows)

**schema_version:** `0.3`

**Plan:** `bellows/knowledge/decisions/drafts/executable-wrap-check-memory-class-gate.md`
**Tier:** T1 (Small — the [4/memory] arm gains the class-frontmatter FAIL + two advisory WARNs + tests; class shop-infra). **Panel: none.**
**Opened:** 2026-08-26

---

## Walk 0 — context pin (REAL, measured 2026-08-26)

1. **The audit's item 1, CEO-batched:** no NEW/MODIFIED memory entry without `class: mechanize|codify|keep|stale` (keep requires a stated impossibility). BLOCKING. Orphan + size-cap ship WARN-first (funnel law) — printed, never failed.
2. **Prototyped against the LIVE memory dir this authoring:** 137 md files; ORPHANS today = 0 (a fire means real drift); committed-classless = 134 (EXEMPT — only uncommitted files checked, backfill gradual-by-touch, the first post-ship wrap does not detonate); MEMORY.md 103 lines vs the 140 cap.
3. **Anchors:** wrap_check.py 456 lines; `m_dirty = porcelain(MEMORY)` count-1; porcelain returns raw `XY path` lines — rename lines carry ` -> ` and the parser takes the NEW path; the hook runs in the STOP path under system python — the new checks are pure file reads, no subprocess.
4. **Self-application stated:** this session's own next memories must carry `class:` — the gate binds its builder first.
5. **id prediction:** 562.

⚠️ Walk 0 carries no fold rows. Walks 1–2 appended AFTER the draft exists, from real passes.

---

## Walk 1 — five-lens sequential walk (post-draft, real)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| M1 | 1 | 1 Weak spots | 1.2 | — | Test 5's orphan fixture was state-ambiguous — an uncommitted classless orphan trips BOTH the class FAIL and the WARN, so the test would not isolate the advisory. | `an entry absent from the index → the orphan WARN prints` | Folded: pinned COMMITTED, isolating the WARN by construction. |

**Walk 1 total: one finding, folded.** (Other lenses dry — insertion coexistence traced; stop-path purity; exempt baseline measured 134; the sibling (r) check finds nothing to flag in this plan's own text.)

---

## Walk 2 — five-lens sequential walk (confirming pass, real)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| — | 2 | all five | — | — | DRY — the six test states re-traced, one behavior each. | — | No folds. |

**Walk 2 total: 0 findings — all five lenses dry. BAR MET; T1, no panel escalation.**
