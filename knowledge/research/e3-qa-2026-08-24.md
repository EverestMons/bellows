# E3 Deposit Watcher Receipts — QA Report

**Date:** 2026-08-24 | **Plan:** executable-516 | **Step:** 2 (QA) | **Agent:** fresh QA, re-measured

---

## Q1 — Full Suite

```
python3 -m pytest tests/ -q
1325 passed, 1 warning in 45.09s
```

- **Baseline:** 1288 collected (known_failures 0)
- **New tests:** 37 (test_deposit_receipt.py: 11, test_wrap_receipts.py: 26)
- **Total:** 1325 collected
- **Failures/errors:** 0
- **Status:** GREEN — zero failures, zero errors. The urllib3 warning is unrelated (LibreSSL version notice).

Raw output deposited: `knowledge/research/pytest_full.txt`

---

## Q2 — Change-Set vs Design Gap Table (as corrected)

### Gap table row verification

| Row | Description | Site | Status |
|-----|-------------|------|--------|
| 1 | Receipt store (`receipts/`) | `receipts/` dir with README.md | ✅ |
| 2 | Writer tool (`tools/deposit_receipt.py`) | `tools/deposit_receipt.py` | ✅ |
| 3-8 | `[2r/receipts]` wrap-check step (blocking, warning, SKIP arms) | `hooks/eluvian/wrap_check.py` — 14 occurrences of `[2r/receipts]` | ✅ |
| 9 | Session-id argv plumbing | `wrap_stop_hook.py` (12 refs), `wrap_debt_hook.py` (7 refs) | ✅ |
| 10 | ELUVIAN_PATH.md Stage 5 line (root-repo doc) | ABSENT — `git diff HEAD~1 --name-only` shows no files outside bellows | ✅ |
| 11 | Retirement in bellows.py (C-3, 4 terminal sites) | `_retire_receipts` — 5 refs in bellows.py (1 def + 4 call sites) | ✅ |
| 12 | Portability-census row (root-repo doc) | ABSENT — no files outside bellows changed | ✅ |
| Panel | Clear-tool class-release arm (S1-4) | `tools/clear_plan.py` — cycle_check + plan_lint gates + `cleared_by='clear_tool'` clearance write | ✅ |

### Existing wrap_check group message literals preserved

| Literal | Old count | New count | Preserved |
|---------|-----------|-----------|-----------|
| `[1/project]` | 1 | 1 | ✅ |
| `[2/bellows]` | 2 | 3 | ✅ |
| `[3/root]` | 3 | 3 | ✅ |
| `[3b/lessons]` | 1 | 1 | ✅ |
| `[4/memory]` | 2 | 2 | ✅ |
| `wrap_check: OK` | 1 | 1 | ✅ |

The `[2/bellows]` increase from 2→3 is expected: the S3-9 porcelain pathspec addition (`porcelain(BELLOWS, "receipts")`) for uncommitted-receipt enforcement.

No old group message strings appear in the diff's removed lines — `git diff HEAD~1 -- hooks/eluvian/wrap_check.py | grep '^-' | grep -E '[bracket-literals]'` returned empty.

### is_runnable_plan / claim path

`git diff HEAD~1 -- bellows.py | grep -E 'is_runnable_plan|def.*claim'` — empty. Untouched.

---

## Q3 — Behavioral Spot-Probes (tmp environments)

All probes run in isolated tmp dirs with tmp lifecycle.db instances. No real `receipts/` or `lifecycle.db` touched.

### Probe 1: Full ritual sim (receipt + matching clearance → pass)

- Wrote receipt `receipt-executable-test-plan-test-session-abc123-8d84dee2f651.json`
- Added matching clearance row (`content_hash` equality)
- Clearance match: YES
- **Result: PASS** — own-session receipt matched clearance

### Probe 2: Delete receipt → warning arm only (no failure)

- Added clearance, wrote NO receipt
- Clearances in 24h lookback: 1; receipts: 0; unmatched: 1
- **Result: WARNING only** — no blocking failure (C-1 warning arm, anti-foreign-block)

### Probe 3: Corrupt hash + backdate armed_at past grace → blocking arm fires

- Wrote receipt with wrong hash (`deadbeef...`), `armed_at` backdated 15 minutes
- Added clearance with the real hash
- Clearance match for corrupted hash: NO
- `armed_at` age: 15.0 minutes (past 10-min grace: True)
- **Result: BLOCKING FAILURE** — own-session receipt matches no clearance, past grace window

### Probe 4: Foreign-session receipt-less clearance → warning only

- Wrote receipt from `foreign-session-xyz789`, added matching clearance
- Checked from `our-session-abc456` perspective: 0 own receipts, 1 foreign
- **Result: No blocking failure** — foreign receipts irrelevant to blocking arm

---

## Q4 — Activation Scoping

### (a) Hook half — LIVE AT TEARDOWN-MERGE (worktree proof)

The hook half (wrap_check.py, wrap_stop_hook.py, wrap_debt_hook.py changes) goes live at teardown-merge, which happens AFTER this QA step. Proof is from the WORKTREE:

**No-argv run (`python3 hooks/eluvian/wrap_check.py`):**
```
[2r/receipts] SKIPPED (blocking arm) — no session_id provided; receipt check requires session context.
[2r/receipts] WARNING: 2 cleared deposit(s) in the last 24h without a receipt — arm a watcher and write a receipt at every deposit.
```

**With-argv run (`python3 hooks/eluvian/wrap_check.py "test-session-id-..."`):**
```
[2r/receipts] OK — no deposits in this session (session test-session-id-1787615098).
[2r/receipts] WARNING: 2 cleared deposit(s) in the last 24h without a receipt — arm a watcher and write a receipt at every deposit.
```

The merged-tree proof is the depositing session's own next wrap. Its clearances (515/516) are receipt-less — the warning arm fires, never a block. The A2 blast-radius test (`test_blast_radius_clearances_present_receipts_empty`) is the pre-proof: it asserts zero `[2r/receipts]` failures when clearances exist but receipts are empty with a session id given.

### (b) Daemon half — INERT until restart

The retirement code (`_retire_receipts` in bellows.py) is inert until the daemon restarts. The running daemon predates this change.

- **Daemon PID:** 22189
- **Start time:** Mon Aug 24 16:20:18 2026
- The DEV commit (`757c757`) was made AFTER the daemon started — the running process has the OLD bellows.py in memory.

### (c) Warning arm visibility boundary

On a clean wrap (rc 0), both hook consumers (wrap_stop_hook.py, wrap_debt_hook.py) discard stdout, so warnings reach an operator only via:
- Manual `wrap_check.py` runs
- A wrap that fails on OTHER grounds (the stdout is then visible in the failure output)

This is accepted under the attestation ceiling. The `receipts/README.md` states this explicitly (line 49: "On a clean wrap (rc 0), both hook consumers (stop hook, debt hook) discard").

---

## Verification Table

| Check | Status | Evidence |
|-------|--------|----------|
| Q1: Full suite green | ✅ | 1325 passed, 0 failures, 0 errors — `pytest_full.txt` deposited |
| Q1: Test count growth | ✅ | 1288 → 1325 (+37: 11 + 26 new test functions) |
| Q2: Gap rows 1-9 present | ✅ | Each row verified at its site with grep counts |
| Q2: Gap row 11 present (C-3 anchors) | ✅ | `_retire_receipts` — 5 refs (1 def + 4 terminal sites) |
| Q2: Panel row present (S1-4 clear-tool) | ✅ | cycle_check + plan_lint gates + `cleared_by='clear_tool'` write verified |
| Q2: Rows 10, 12 absent | ✅ | `git diff HEAD~1 --name-only` — all 9 files inside bellows repo |
| Q2: Existing group literals preserved | ✅ | All 6 bracket literals at or above old counts; no removals in diff |
| Q2: is_runnable_plan/claim untouched | ✅ | grep on diff returned empty |
| Q3: Receipt + clearance → pass | ✅ | Probe 1: own receipt matched clearance, no failure |
| Q3: No receipt → warning only | ✅ | Probe 2: unmatched clearance in 24h → WARNING, no blocking failure |
| Q3: Corrupt hash + backdate → block | ✅ | Probe 3: own receipt, wrong hash, past grace → BLOCKING FAILURE |
| Q3: Foreign receipt-less → warning | ✅ | Probe 4: foreign session receipts irrelevant to blocking arm |
| Q4a: Hook half worktree proof | ✅ | No-argv SKIP + with-argv OK pasted; blast-radius test is pre-proof |
| Q4b: Daemon half inert | ✅ | PID 22189, started Mon Aug 24 16:20:18 2026 — predates DEV commit |
| Q4c: Warning visibility boundary | ✅ | README states it; clean-wrap stdout discarded by both hook consumers |

---

## Rule 20 — QA Self-Check Results

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
Evidence folder: /Users/marklehn/Developer/GitHub/bellows/.bellows-worktrees/516/knowledge/research/
Files verified: 2
```
