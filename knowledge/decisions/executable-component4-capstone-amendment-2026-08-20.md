# governance — DRAFTING_CYCLE.md §2 auto-advance cadence (capstone, component 4) [+ paired memory rewrite]
**Date:** 2026-08-20 | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** none (governance doctrine edit; cycle_check already IS the gate — no code/gate change) | **Execution:** Step 1 (GOVERNANCE, in-place) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

## Context

Component 4 (the CAPSTONE) — the arc's final piece: amend `DRAFTING_CYCLE.md` §2 to add the **auto-advance cadence** (the self-driving walk loop), per the finalized design in `bellows/knowledge/research/component4-capstone-design-2026-08-20.md` (diagnostic-482, T-7 — build from Q3's clause + gap table, do not re-derive). Governance IN-PLACE dispatch ([[governance-in-place-dispatch]]): absolute operands, triple-pin (pre-edit sha `9972ec9`), `git add <abspath>` before pathspec commit, `git rev-parse` in the commit compound. **Structural clone of 2a (`executable-476`)** — the same §2/§3 governance-edit class; 2a's panel-hardened machinery applies (git diff HEAD not bare; assert the change LANDED; History-row conventions; describe-don't-quote gate tokens).

**CEO-authorized direct amendment — declared §6 deviation** (v1.5/1.6/1.7/2.1/2.12 precedent): CEO-routed proposal, CEO-formalized capstone (§11), not corpus-path.

**⚠️ TWO SURFACES, atomic (design Q3 / D2):** (1) the §2 cadence clause in `DRAFTING_CYCLE.md` (this STEP); (2) a rewrite of three Planner working-agreement memories ([[drafting-cycle-one-pass-per-turn]], [[no-fabricated-drafting-cycle]], [[drafting-walk-phases-separated-by-turn]]) from "stop each turn / await continue" to the auto-advance cadence. Surface (2) is the **paired PLANNER act** executed at the SAME close (the memory is the Planner's own working-agreement; the Planner keeps it, as all session memory edits have been Planner-direct) — declared here so the pair is atomic, with the rewrite text PINNED (walk-2 V2, same describe→drift lesson) from 482 Q3: **`drafting-cycle-one-pass-per-turn`** → the per-walk CEO directive is replaced by auto-advance on `cycle_check=CONTINUE` + substrate-present + no direction finding; sequential-fold + per-lens commit unchanged; points at the §2 cadence clause as canonical. **`no-fabricated-drafting-cycle`** → auto-advance is NOT fabrication WHEN the committed substrate is present (a `walk_register_lint`-clean committed register + a per-walk commit per walk + a `fold_check` baseline — verified directly, walk-3 W3); the fabrication prohibition is retained and mechanized by that substrate check, gating BOTH auto-advance AND auto-close (panel-DISC-3); manual (CEO-confirmed) advance AND close when the substrate is absent. **`drafting-walk-phases-separated-by-turn`** → the fold-order control is unchanged; the turn boundary (already superseded) is now formally the auto-advance cadence. ⚠️ **No live-behavior contradiction window:** both surfaces say "auto-advance AND auto-close ONLY when the committed substrate is present (directly verified), else manual" — and current scratchpad practice IS substrate-absent → manual, so nothing flips until the substrate is adopted. The memory rewrite is CEO-authorized (capstone is CEO-formalized), not Planner-unilateral (design D3).

**What the clause adds (design Q3):** auto-advance from walk 2 on `cycle_check=CONTINUE` + substrate-present + no §2.0 direction finding; `BAR_MET`→ mandatory closing-record re-read → close + `--emit-manifest`; `ESCALATE:*`→ resumable CEO pause. **The substrate-presence precondition is HARD and gates BOTH auto-advance AND auto-close (panel-DISC-1)** — without the committed substrate the anti-fabrication machinery is not running → fall back to manual (CEO-confirmed) for advance AND close. It ADDS a cadence clause; it re-mandates NOTHING (the substrate is already §2.7/§3 doctrine — I1) and needs NO gate change (cycle_check already emits the verdicts — §6 discharged).

## Drafting Cycle
**Tier:** T2 — triggers: T-6 (governance surface: §2 cadence doctrine), T-8 (novel: first auto-advance cadence). Cold panel owed at close (§2.6, likely full form — a hand-anchored doctrine edit that RELAXES the anti-fabrication cadence for every future cycle; the highest-stakes doctrine change of the arc). cycle_check dogfooded per walk.
**Walk 0 (context pin) — real measurements:**
1. Newest same-class plan (clone-diff target): `executable-476` (2a, DC v2.11→2.12, 2026-08-20) — same class: CEO-routed §2/§3 governance in-place amendment. 2a's cold panel caught: History-row conventions (Inheritors + §6 discharge + no-batch), `git diff HEAD` not bare, the "assert the change LANDED" post-condition, reflexive gate-token avoidance — ALL apply here (clone-drift risk is highest at a proven clone).
2. Target: `DRAFTING_CYCLE.md` sha **`9972ec9`** (v2.12). §2 insertion: between L38 (opening "Walk the lenses **in order, one pass per lens per walk.**…") and L40 (doneness bar "**The cycle is DONE when…**") — the cadence region, before the bar. Version line L5 (`2.12 (2026-08-19)` → `2.13`). History section newest-first (2.12 row at top).
3. ⚠️ The substrate is ALREADY §2.7:160/§3:210/§2.6:133 doctrine — the clause NOTES it as a precondition, does NOT re-mandate it (design I1). No `plan_lint`/`gates.py` change (cycle_check is the gate — §6 discharged).
4. ⚠️ Memory repo is a SEPARATE git repo (`/Users/marklehn/.claude/projects/-Users-marklehn-Developer-GitHub/memory/`) — surface (2) commits there; surface (1) at the GitHub root. Cross-repo atomic pair.
5. cycle_check dogfooded per walk; T2 → cold panel at close.
**Direction verdict (after walk 1): PROCEED.** The angle — add the §2 auto-advance clause (substrate-gated) + the paired memory rewrite — is right and CEO-formalized. No forcing finding.
**Walks:** 5 (walk 1–5). Instruction yield 5, 4, 2, 1, 0 → 5→4→2→1→0 (cycle_check-computed, reconciled). §2 BAR MET at walk 5 (fully dry). Cold panel owed (T2, full form).
- Weak spots:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 1 / record 0; w3 1 folded — instruction 1 / record 0; w4 1 folded — instruction 1 / record 0; w5 dry.
  - W1: clone-drift from 2a — the STEP must carry 2a's proven machinery, VERIFIED against live data not recalled ([[read-the-cited-rule-dont-recall]]): triple-pin sha `9972ec9`; `git diff HEAD` (not bare `git diff`); assert the clause LANDED; History row with Inheritors + §6 discharge + no-batch. Folded into the STEP.
  - W2 (w2): the §2 clause was DESCRIBED, not PINNED — 2a's DISC-7 (describe→drift; two agents write different doctrine). Folded: the exact §2 clause text is now pinned verbatim in the STEP.
  - W3 (w3, ⚠️ corrects a 482 gap): the precondition "asserts all PASS, never N/A" is NOT observable — VERIFIED live: cycle_check prints only the verdict (a register-less plan and a substrate-bearing plan both return `BAR_MET`); no `--assert` flag. Unenforceable → self-attestation fallback (the exact thing the substrate removes). Folded: the clause reframes substrate-presence to a DIRECT Planner check (walk_register_lint-clean committed register + per-walk commits + fold_check baseline) using EXISTING tools — no cycle_check change ("no new code" preserved); cycle_check's asserts then guard a present-but-broken substrate.
  - W4 (w4): the STEP's land-assert grepped `Auto-advance` — which the History row also carries → wouldn't prove the §2 CLAUSE (the doctrine paragraph) itself landed (the 2a F4 lesson: assert the specific change, not an incidental match). Folded: the land-assert now greps a clause-UNIQUE phrase (`governs advancing BETWEEN walks` == 1).
- Destruction:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 1 / record 0; w3 dry; w4 dry; w5 dry.
  - D1: this RELAXES the anti-fabrication cadence for EVERY future cycle — the highest-stakes doctrine change of the arc. Folded: the clause makes auto-advance AND auto-close (DISC-1) HARD-conditional on the substrate (verified by the direct check — register reference line + per-walk commits + fold_check baseline — W3) with an explicit manual fallback; §2.7 subtractive-cut discipline — the clause ADDS, removes no live §2 rule (verify via git diff HEAD that the bar/sequential-fold/per-lens-commit rules are intact).
  - D2 (w2): the clause sits adjacent to L38's "one pass per lens per walk" (within-walk lens ordering) — it must not conflate that with between-walk auto-advance. Folded: the pinned clause opens "The rule above governs lens order WITHIN a walk; this governs advancing BETWEEN walks."
- Vulnerabilities:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 1 / record 0; w3 dry; w4 dry; w5 dry.
  - V1: the added §2 clause + this plan's own DC block must not introduce a gate-matching string nor trip cycle_check step-8 (reflexive, §3 L220) — the doctrine clause naming cycle_check verdicts is fine (§2 prose, not a record block); this plan's own DC block avoids the bare uppercase closure tokens (2a V1 lesson).
  - V2 (w2): the paired MEMORY rewrite text was described, not pinned — same describe→drift risk on the second surface. Folded: the 3 slugs' rewrite text pinned in Context (from 482 Q3).
  - w3: dry.
- Integration-record:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 1 / record 0; w3 1 folded — instruction 1 / record 0; w4 1 folded — instruction 0 / record 1; w5 dry.
  - I1: the memory rewrite is the SECOND surface — the STEP (doctrine) + the paired Planner memory act declared atomic; the substrate-precondition means no live contradiction window (both say "manual when substrate absent"; current practice is manual). Folded into Context.
  - I2 (w2): the clause's `BAR_MET` must reference the §2 doneness bar (L40) as what it MECHANIZES — not a parallel/competing close definition; and note the closing re-read may re-open. Folded into the pinned clause ("On the doneness bar being met (cycle_check BAR_MET)… which may itself re-open the walk").
  - I3 (w3): W3's reframe must propagate to the pinned `no-fabricated-drafting-cycle` memory text (which said "asserts all PASS") — aligned to the direct substrate check (register + per-walk commits + baseline). Folded into Context.
  - I4 (w4, record-DECAY): the Vulnerabilities summary line lagged w3 (sub-bullet added, tally unswept). Swept it (+w3/w4); all five lens lines now carry w1–w4 and reconcile with cycle_check's {5,4,2,1}.
- ACID:  w1 1 folded — instruction 1 / record 0; w2 dry; w3 dry; w4 dry; w5 dry.
  - A1: the clause reconciles with existing §2 states — the doneness bar (closes), §2.0 RE-DRAFT (ends, outside auto-advance), §2.6 cold panel (auto-convened), §2.7 sequential-fold + per-lens commits (the substrate) — auto-advance governs BETWEEN walks only; no existing rule overridden. Folded: the clause is cadence-scoped, explicitly leaving the bar/fold rules unchanged.
  - w2: dry — with the pinned clause referencing the bar (I2) + the cadence/lens-ordering distinction (D2), the §2 states form one consistent contract.
**Conflicts:** none cross-lens.
**Walk 5 STATUS:** FULLY dry (instruction 0 / record 0) across all five lenses — **§2 BAR MET.** No fold at all (the w4 decay sweep left the record clean); convergence: clone machinery (w1) → pin both surfaces (w2) → the enforceability gap (w3) → polish (w4) → dry (w5).
**Closing-record re-read (§2.7, walk 5):** tallies + yield series reconcile with cycle_check's computed {5,4,2,1,0}; all five lens lines carry w1–w5; the pinned §2 clause + the pinned memory text are internally consistent (both gate on direct substrate-presence, not asserts-all-PASS); no closing prose over-claims. Dry.
**cycle_check (dogfood) per walk:** w1 `CONTINUE` · w2 `CONTINUE` · w3 `CONTINUE` · w4 `CONTINUE` · w5 `BAR_MET` (exit 0 — fully dry, earned close recognized).
**Warm close:** 5 walks (bar met w5, fully dry; yield 5→4→2→1→0); structural clone of 2a, machinery carried + verified; the enforceability gap (w3) that 482 glossed closed in the warm cycle.
**Cold panel (§2.6, FULL form — the arc's highest-stakes doctrine edit). Meter opened at convene. Diagnostic-482 was the design scout.**
- **Seat DISCOVERY (weak-spots + destruction; evidence-attack + clone-diff) — RUN, 89.2k tokens.** 3 findings (1 HIGH · 1 MED · 1 LOW), author-verified. **DISC-1 (HIGH — the one that matters):** the substrate precondition guarded ONLY the `CONTINUE`/auto-advance branch, NOT `BAR_MET`/auto-close — a fabricated register-less cycle reaches a dry walk → `BAR_MET` (verified: cycle_check.py:406 treats N/A identically to PASS; `/tmp/fab.md` → BAR_MET) → the clause routed it straight to auto-close + emit-manifest → depositor, bypassing the substrate guard on the exact path that reaches deposit. Inherited faithfully from 482 Q6 (whose "single invariant" scoped the precondition to auto-ADVANCE and never re-imposed it on close). **The whole warm chain missed it** — 482 + 5 warm walks + walk-3's W3 all watched auto-advance. Folded: the HARD precondition now gates BOTH auto-advance AND auto-close (both surfaces); a substrate-less BAR_MET → manual CEO-confirmed close, never auto (manual close stays available as today). **DISC-3 (MED):** the memory rewrite mirrored the gap — aligned. **DISC-2 (LOW, record):** Context prose still repeated the retired "asserts all PASS" phrasing — swept. Verified clean: sha `9972ec9`, §2 anchors count-1 + placed, all of 2a's hardened guards carried, the §2 insertion additive (no live rule damaged), the W3 reframe buildable.
- **Seat EXECUTION (vulnerabilities + execution brief) — RUN, 73.5k tokens.** Rehearsed the §2 edit end-to-end on a scratch git copy + ran the substrate-presence machinery. **VERDICT: the §2 edit applies cleanly AND the DISC-1 substrate check is enforceable** — 3 PASS + 1 MED. EXEC-1 (PASS): scratch rehearsal diff = {version, clause, History}, no §2 rule removed, land-assert `governs advancing BETWEEN walks`==1. EXEC-2 (PASS, load-bearing): a fabricated no-git plan → `BAR_MET` (cycle_check.py:406 N/A==PASS confirmed), but the DIRECT check (grep the reference line + committed file + `.foldcheck.json`) cleanly separates absent from present — the DISC-1 fix is runnable with existing tools. EXEC-3 (PASS): cross-repo register → N/A → manual (correct). **EXEC-4 (MED):** the clause named the substrate artifact WRONG — a `## Walk register` heading vs the real bold walk-register reference token (the one cycle_check's `WALK_REGISTER_RE` reads, verified) → a Planner grepping the heading would find none on a real substrate plan → invert the check. Folded: corrected to the reference-line token. (⚠️ described, not quoted, here — the literal is a gate token cycle_check parses inside a DC block, the V1 reflexive trap.)
- **Seat CAPSTONE (integration + ACID + system brief; runs last on the fold set) — RUN, 54.8k tokens.** Cross-checked the panel's OWN folds. **1 finding (0 HIGH/MED · 1 LOW), fold set COHERENT.** Verified: DISC-1's close branch is fully guarded (the mandatory closing re-read precedes the substrate branch, so both auto- and manual-close get it; no BAR_MET sub-path auto-closes without the substrate check); DISC-1 × the 3 memory rewrites all gate BOTH advance AND close (zero advance-only residue); EXEC-4's token matches `WALK_REGISTER_RE` (cycle_check.py:31); reflexive gate-token hygiene clean (0 of the bold walk-register / closing markers or the uppercase closure tokens in the DC block); yield 5→4→2→1→0 recomputes + `--emit-manifest` empirically returns `yields: 5, 4, 2, 1, 0 · walks: 5 · BAR_MET`. **CAP-1 (LOW, record):** a stale "(asserts all PASS, never N/A)" parenthetical in the D1 fold-note — swept (aligned to the direct-check + both-branches framing).
**Cold panel COMPLETE (full form, 3 seats + the 482 design-scout). Meter: DISCOVERY 89.2k + EXECUTION 73.5k + CAPSTONE 54.8k ≈ 217k tokens.** The panel earned its cost decisively — DISC-1 (HIGH) caught the auto-CLOSE fabrication leak (the substrate guard was on auto-advance only; the close path — the one that deposits — was open), a gap 482 + all 5 warm walks + walk-3's own W3 missed; EXEC-4 caught the wrong substrate-token that would have inverted the check. Both on the arc's highest-stakes edit, exactly where the panel matters most.
**Bar re-met post-panel:** all panel folds applied; the CAPSTONE read the fold set coherent (0 HIGH/MED), so the walk re-reaches the bar on the folded plan.
**Closing-record re-read (§2.7, final):** yield 5→4→2→1→0 reconciles with cycle_check's computed counts; all lens lines carry w1–w5; the pinned §2 clause + the 3 memory rewrites consistently gate both advance and close; the substrate token matches cycle_check's regex; no gate token in the DC block; CAP-1 swept; no closing prose over-claims. Dry.
**Closing:** 5 warm walks (bar met w5, fully dry) + full cold panel (3 seats, ~217k tokens, DISC-1 the auto-close-leak HIGH); the enforceability gap (w3) + the close-leak (DISC-1) + the wrong-token (EXEC-4) all closed; closing-record re-reads run (warm + post-panel), dry; cycle done — deposit exactly once. Class: register-writing → **HOLD-FOR-CEO** at deposit; the paired memory rewrite is the Planner's act at the same close.

---
---

## STEP 1 — GOVERNANCE DOCTRINE EDITOR (in-place)

---

> **Identity:** You amend `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` in place (governance root, top-level `.git`, NO worktree). Read `bellows/knowledge/research/component4-capstone-design-2026-08-20.md` (diagnostic-482) FIRST — its Q3 clause text + gap table are the spec; implement, do not re-derive. Apply ONLY the §2 cadence clause + its §6-mandated bookkeeping (version bump + History row); touch no other rule. ⚠️ Re-verify every line citation by CONTENT (lines shift).
>
> **⚠️ Triple-pin:** assert `git -C /Users/marklehn/Developer/GitHub rev-parse HEAD:DRAFTING_CYCLE.md` == `9972ec9…` AND `git -C /Users/marklehn/Developer/GitHub status --porcelain DRAFTING_CYCLE.md` empty. Else HALT.
>
> **Amendment — §2 auto-advance cadence clause. PIN the text verbatim (panel-DISC-7 clone lesson from 2a: describe→drift; DOCTRINE text is pinned, not paraphrased — walk-2 W2).** Insert a new paragraph into §2 immediately AFTER the opening paragraph (assert `Walk the lenses **in order, one pass per lens per walk.**` occurs exactly ONCE) and BEFORE the doneness-bar paragraph (`The cycle is DONE when a full walk`). The replacement doctrine paragraph is EXACTLY:
>   **Cadence — the self-driving walk loop (auto-advance between walks).** The rule above governs lens order WITHIN a walk; this governs advancing BETWEEN walks. After a walk's final per-lens commit, run `cycle_check` and act on its verdict. On `CONTINUE`, auto-run the next walk — provided (a) the committed substrate is present and (b) no direction-class finding (§2.0) has surfaced. **Substrate-presence is verified DIRECTLY — a committed `**Walk register:**` reference line (the exact token cycle_check's assert #2 reads) pointing to a `walk-register-<slug>.md` file that `walk_register_lint` validates, a per-walk commit per walk, and a `fold_check` baseline — NOT inferred from the `cycle_check` verdict, which reports only CONTINUE/BAR_MET/ESCALATE and does not distinguish a PASS assert from an N/A one (a register-less plan and a substrate-bearing plan both return `BAR_MET`). Once the substrate is present, `cycle_check`'s asserts guard a present-but-broken substrate (`ESCALATE:assert-fail:2/3`, `uncommitted-walk`); when it is absent the checks return N/A and auto-advance is unavailable.** On the doneness bar being met (`cycle_check` `BAR_MET`), run the mandatory closing-record re-read (§2.7) — which may itself re-open the walk — then, **provided the substrate is present, auto-close and emit the manifest; a substrate-less `BAR_MET` is indistinguishable from a fabricated close (cycle_check treats an N/A assert identically to PASS), so without the substrate the close is MANUAL and CEO-confirmed, never auto.** On an escalation (`ESCALATE`), PAUSE for the CEO; a one-word resume runs the next walk. Auto-advance applies from walk 2 onward — walk 0 (context pin), walk 1, and the §2.0 direction verdict remain the manual entry gate; a RE-DRAFT ends the cycle before auto-advance begins. **The substrate-presence precondition is HARD and gates BOTH auto-advance AND auto-close (panel-DISC-1): when the committed register is absent or the direct substrate check does not pass, the loop falls back to manual — advancing between walks awaits CEO direction AND the close is CEO-confirmed, never auto — because a substrate-less verdict (CONTINUE or BAR_MET alike) cannot distinguish a genuine walk from a fabricated one, since cycle_check treats an N/A assert as PASS. Manual close remains available (as it is today); only AUTO-close requires the substrate.** The loop is `cycle_check`-gated for the mechanical signals (plateau, rising yield, assert failures) and Planner-gated for the semantic ones (§2.0 direction findings, §2.8 oscillation). This clause ADDS cadence only: the doneness bar below, the sequential-fold rule (§2.7), the per-lens commits (§2.7), and the closing-record re-read (§2.7) are unchanged.
>   ⚠️ Do NOT use the `[[memory-slug]]` link syntax in doctrine (that is memory-file syntax); the anti-fabrication reference is prose, as above.
>
> **Bookkeeping (§6, clone 2a's row conventions):** bump the Version line (L5) `2.12 (2026-08-19)` → `2.13 (<dispatch date>)`. PREPEND (History is newest-first) a `2.13` row above the `2.12` row: units changed (§2 cadence clause), the changelog ordinal, the declared §6 deviation with the CEO-routed/no-DB-flip note, the §6 coordinate-doctrine-and-gate discharge (cycle_check already the gate — no new gate code, §4 in lockstep), the paired cross-repo memory rewrite (named), and an `Inheritors:` clause (every future cycle once the committed substrate is present; the depositor consumes the emitted manifest).
>
> **Verification (clone 2a's hardened checks):** `git -C /Users/marklehn/Developer/GitHub diff HEAD -- DRAFTING_CYCLE.md` (NOT bare `git diff`) shows ONLY the expected changes — the §2 cadence paragraph, the Version-line bump, the History row — and NO removal of any §2 rule (the bar / sequential-fold / per-lens-commit / closing-re-read text intact). ⚠️ **Assert the CLAUSE (not the History row) LANDED (walk-4 W4, the F4 lesson):** grep a phrase UNIQUE to the §2 clause — `grep -Fc 'governs advancing BETWEEN walks' DRAFTING_CYCLE.md` == 1 post-edit (a bare `Auto-advance` grep would also match the History row → wouldn't prove the doctrine paragraph itself landed). Doctrine-integrity: markdown well-formed, headings intact (fence-aware). NO plan_lint/cycle_check run gates this — neither reads `DRAFTING_CYCLE.md` (v2.2 measurement); the §3↔§4 lockstep is unaffected (§2-only edit). Confirm `plan_lint` on a control Done plan is still 0 FAIL (the doctrine edit broke nothing).
>
> **Commit (governance root):** `cd /Users/marklehn/Developer/GitHub && git rev-parse --show-toplevel` (assert == the GitHub root), `git add /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, commit `docs(drafting-cycle): §2 auto-advance cadence — the self-driving walk loop, DC v2.13 (component 4 capstone) [<id>]`.
>
> **⚠️ Paired memory rewrite (surface 2) is the PLANNER's act at close, NOT this STEP** — declared for atomicity. Do NOT edit the memory repo from this governance STEP.
>
> **Deposits:**
> - `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (in-place, governance root — absolute operand)
