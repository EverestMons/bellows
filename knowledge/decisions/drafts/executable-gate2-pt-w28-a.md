# bellows — executable: DOCTRINE — Gate 2, cycle W=28 PT tranche one: proposals 418, 419, 430, 434 onto PLANNER_TEMPLATE.md v4.97 by a committed builder (Rules 103–106), then the four flip `implemented`

**Date:** 2026-09-02 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full (the bellows suite is UNCHANGED by this plan and is run once by QA so the test gate reads a real summary line — `1676 passed, 1 skipped` in a worktree, which holds no `config.json` — never a pre-declared override) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 2

**auto_close:** false

**Slug:** `gate2-pt-w28-a-2026-09-02`

**Depends on:** the CEO, 2026-09-02 ("Proceed as recommended" — Gate 2 starts with one tranche of four: 418, 419, 430, 434, the verification-discipline kin of today's own findings); tuyere thread 76 (the twelve accepted, PT v4.97); the non-author Gate 1 packet `gate1-packet-2026-09-01.md` (session 352aa9ee, the non-author; rows 418/419/430/434 routed `codify` with their homes); `Done/executable-540.md` in governance (route-57 PT tranche B — the clone origin and the newest same-class plan: doctrine prose onto `PLANNER_TEMPLATE.md` by a committed builder, then a scoped CAS flip in the lessons DB); the builder `governance/knowledge/decisions/drafts/build-gate2-pt-w28-a-2026-09-02.py` (committed `d1c6727`; digest below). Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w28-a-2026-09-02.md`.

**Tier computed, not judged (§1):** **T-6 fires** — `PLANNER_TEMPLATE.md` is the template → **T2**. T-2 fires (four canonical rows flip; caps at T1). T-1 fires (two repos, plus the DB). T-8 fires (a clone by kind of 540). **Panel form — the magnitude proxy, measured at walk 0 (all three must hold): (i) structure-clone** — the parent is 540 (closed 2026-08-25, the newest PT tranche), and both walk-0 clone-diffs ran (against 540's final text and its register); **(ii) mechanized edits** — every template mutation flows through the committed builder, whose four anchors are count-1-asserted BY EXECUTION and whose nineteen post-conditions are asserted on the output before any write (proven at walk 0 and again after the scout's folds: a scratch build, three refusals, a byte-identical rebuild, a 35-added / 2-removed numstat); the flip is one CAS statement under a structural pre-check; **(iii) scripted probes** — every post-condition token measured 0 against the pre-edit file, the retired tokens 1. **→ SMALL form: the walk-0 scout, then EXECUTION, then CAPSTONE.** The CEO may call the full form.

## Why this exists

Gate 1 (the Air's non-author packet, 2026-09-01) routed twelve of cycle W=28's proposals `codify` onto `PLANNER_TEMPLATE.md` — thread 76 carries them all. The CEO chose to start with the four that concern verification and QA discipline, the closest kin of what this day's own panels found: **418** (entry 410) — a test written by the author inherits the author's model; move the oracle outside it (tier-2 state-space suites for every detector); **419** (entry 411) — earnability is not discrimination; enumerate the plausible WRONG fixes and report the kill map; **430** (entry 422) — a detector's fire count is a RATIO, fired / evaluated / skipped; **434** (entry 426) — an environment variable is a process-tree property; test the dispatch environment, not the shell. **Dedup posture (the packet's evidence rows, re-verified against live v4.96 at authoring):** 418 PARTIAL, not absent as the packet said (`cross product`, `force-classif` → 0; `state space` → 1 — Rule 14's truth-table-in-code clause, proposal 355, codified by the parent 540: the enumeration there verifies ARM COVERAGE of a branch structure; Rule 103 makes the enumeration the ORACLE for every detector and says so in its first sentence); 419 PARTIAL (Rules 93 and 73 pair mandates with observers; `kill map` → 0); 430 PARTIAL (Rules 92 and 82 are population rules; `fired / evaluated / skipped`, `denominator` → 0); 434 ABSENT (`ELUVIAN_WRAP_ROOT`, `process tree`, `environment variable` → 0; Restart Discipline covers stale code, not the daemon's environment). Homes: all four as new Orchestration Plan Rules **103, 104, 105, 106** (after Rule 102, before the Lifecycle DB Read Protocol) — the packet's home for 419 is the Rule 93 / Rule 73 family (Orchestration rules), not the Plan Authoring Checklist the classifier's parenthetical suggested; Rule 106 opens by naming that kinship. (The v0 had put 419 in the checklist as item 34 in the Rules' register — the scout's SC-4.)

## What this plan does

**In the governance checkout, by absolute path (`GOV=/Users/marklehn/Developer/eluvian-governance`), four anchored edits to `PLANNER_TEMPLATE.md` by the committed builder (never by hand):**
- **E1** — four new rule blocks (103, 104, 105, 106), each ending in its `*Source: …*` line and a `---`, inserted before the `## Lifecycle DB Read Protocol (Planner)` heading (anchor: that heading with its preceding `---`, count 1).
- **E3** — `**Version:** 4.96` → `**Version:** 4.97`. **E4** — `**Last Updated:** 2026-08-26 (v4.96)` → `**Last Updated:** 2026-09-02 (v4.97)`. **E5** — the History row inserted directly under the `| Date | Lesson |` header (the older rows survive below it). (E2 was the v0's checklist item; removed at the scout's SC-4.)

The builder reads the file as bytes, refuses CRLF, refuses an already-built input, asserts each anchor's count is 1, applies the four edits, asserts nineteen post-conditions on the OUTPUT (the builder's `POST` list: the four new headings once each; `fired / evaluated / skipped` 3 and `kill map` 3 — heading, body, History row; `Report the kill map, not the pass count` 1; the four `codified 2026-09-02 (Gate 2, cycle W=28 PT tranche one)` source lines; the v4.97 tokens 1 and the v4.96 tokens 0; the History row 1; the two section headings still once each; `### 34. ` still 1; the Rule 14 and Rule 55 cross-references once each), asserts the line delta is exactly the blocks' own (33) and only then writes — to a SCRATCH path it refuses to let be the live file or anything under the governance root. Apply = copy the proven scratch output over the live file, then re-measure the live file.

**In the lessons DB (`/Users/marklehn/Developer/forge_lessons/lessons-forge.db`, the mini's one live copy), AFTER the doctrine commit:** one scoped CAS flip — `id IN (418,419,430,434) AND status='accepted' AND route='codify'` → `implemented`, `status_updated_by='ceo'`, a fresh UTC stamp — under a structural pre-check (a TEMP-TABLE `CHECK(x=4)` that aborts the transaction before the UPDATE if the four are not all `accepted|codify`), with the capture of every pre-existing row's `(id, status, route, category, status_updated_by, status_updated_at)` to a SCRATCH file BEFORE the UPDATE (copied into the worktree's evidence dir afterwards — never written into the canonical checkout: an untracked file there at a path the branch commits is exactly what the worktree teardown's dirty-tree check and git's `--ff-only` merge refuse, scout SC-1) and the sentinels by name: `PRE_F=4`, `CHANGES_F=4`, `EXCL_F=4` (the four rows now `implemented` with a stamp that is NOT the vintage `2026-09-01T22:03:28Z` — all twelve accepted share that one stamp, so EXCL_F is a one-value exclusion as in 540), `ACC_POST=8`, `IMPL_POST=326`. The flip SQL is a committed file in governance; a backup of the DB precedes it.

**In the bellows worktree:** the dev log and the QA evidence.

## What this plan does NOT do

- Does not touch the other eight accepted proposals (415, 417, 421, 422, 425, 431, 435, 437 — tranche two, thread 76), the twenty-five W=29 proposals, DRAFTING_CYCLE.md, COMPANY.md, the sketch, code, or threads. Does not push (the Planner pushes after the pause). Does not close thread 76.
- ⚠️ **Runs AFTER `forge-cycle-w29` closes and BEFORE W=29's Gate 1 packet is applied** (W=29's M5 pins every pre-existing proposal's status, so this flip landing first would HALT it; Gate 1's flips landing first move P6's `accepted` count) and after plan B closes (unrelated files, but the release order is the CEO's sentence: bootstrap → A → B → W=29 → this).

## MUST-PRESERVE

- ⚠️ **TWO REPOSITORIES AND A DB, ONE STEP.** Governance by absolute path, `git -C "$GOV"` never `cd`; porcelain EMPTY and sha = P1 before touching `PLANNER_TEMPLATE.md`; commit by explicit pathspec; no push. The DB by absolute path; `sqlite3` with `-bail`; a backup (`pre-gate2-pt-w28-a-<HHMMSS>.db` at the forge root, untracked) BEFORE the flip.
- **The builder is the only editor of the template.** Its committed blob digest must equal the on-disk file (P4) before it runs; it runs scratch→scratch; the live file is replaced only by `cp` of a scratch output whose post-conditions the builder already asserted, and the live file is then re-measured (Task C).
- **Doctrine before DB (540's C6):** the flip runs only after the governance commit has landed; a HALT between leaves v4.97 committed-unpushed and the flip owed — the RE-ENTRY arm in A0 resumes at the flip.
- ⛔ **Never run any `checkout --` on an A0 halt except the ONE recipe named there** (a pre-commit apply that died); a pin mismatch otherwise means the file carries changes that are not this plan's.
- **`known_failures: 0`.** From the worktree under the canonical venv the suite is `1676 passed, 1 skipped` (a worktree holds no `config.json`). Any failure is a HALT/Critical.

## Numbers discipline — the pins DEV re-derives (measured 2026-09-02 by the Planner)

| pin | what | value | how |
|---|---|---|---|
| P1 | **`PT_SHA`**, lines, bytes — pre-edit (v4.96) | `c471d3afee3f9094`; 2390 lines; 449,584 bytes | `shasum -a 256 "$GOV/PLANNER_TEMPLATE.md" \| cut -c1-16`; `wc -l`; `wc -c` |
| P2 | **`ANCHORS`** — the builder's four | each count 1: `## Lifecycle DB Read Protocol (Planner)` (with its preceding `---`) · `**Version:** 4.96` · `**Last Updated:** 2026-08-26 (v4.96)` · the History header pair | `/usr/bin/grep -cF` (the header pair by the builder's own assertion) |
| P3 | **`TOKENS`** 0 before | `### 103. ` · `### 104. ` · `### 105. ` · `### 106. ` · `kill map` · `fired / evaluated / skipped` · `v4.97` — each 0; `### 34. ` 1 (Rule 34, unchanged by this plan) | `/usr/bin/grep -cF` |
| P4 | **`BUILDER`** — on-disk digest = the blob at its OWN last commit | `f1f77c8b9f3426cb` (governance `ad81afa`) | `shasum -a 256 <builder> \| cut -c1-16`; `BC=$(git -C "$GOV" log -1 --format=%H -- governance/knowledge/decisions/drafts/build-gate2-pt-w28-a-2026-09-02.py); git -C "$GOV" show "$BC:governance/knowledge/decisions/drafts/build-gate2-pt-w28-a-2026-09-02.py" \| shasum -a 256 \| cut -c1-16` |
| P5 | **`DRY_RUN`** — the success line, numstat, sizes | `BUILT: <out> edits=4 blocks=4 lines+33 bytes+9607 post=19/19`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" <out>` → `35	2` (exit 1 is the expected differing state — read the numbers, never the exit); `wc -l <out>` → 2423; `wc -c <out>` → 459,191 | as stated, scratch-only |
| P6 | **`FLIP_PRE`** — the four rows | each `accepted\|codify\|planner\|2026-09-01T22:03:28Z`; `SELECT COUNT(*) … WHERE status='accepted'` → 12; `implemented` → 322; all twelve accepted share the stamp `2026-09-01T22:03:28Z` (`SELECT DISTINCT status_updated_at … WHERE status='accepted'` → one value) | `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db` |
| P7 | **`SUITE`** — from the worktree under the canonical venv | `1676 passed, 1 skipped`, exit 0 | `BPY -m pytest tests -q -p no:cacheprovider` |
| P8 | **`W29_CLOSED`** (precondition) | `forge-cycle-w29` is Done: `ls /Users/marklehn/Developer/forge_lessons/knowledge/decisions/ \| /usr/bin/grep -cF -- 'forge-cycle-w29'` → 0 (a zero-match count prints 0 and exits 1 — read the number) (no `hold-`/`in-progress-`/`verdict-pending-` file) AND `SELECT COUNT(*) FROM lesson_entries` → 458 | as stated |

## Drafting Cycle

**Tier:** T2 — T-6 (the template), T-2 (four canonical rows, capped), T-1, T-8 fire. **Panel: SMALL form** (the magnitude proxy measured at walk 0, all three conditions holding) — the walk-0 scout, then EXECUTION, then CAPSTONE; the CEO may call the full form.

**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w28-a-2026-09-02.md

**Walk 0 (context pin, measured):** the template's sha, lines and bytes; the five anchors counted with their preceding separators; every new token counted 0 and the retired tokens 1 against the pre-edit file; the four proposals' rows and the one vintage stamp all twelve accepted share; the packet's dedup rows re-verified token by token against live v4.96 (one `state space` hit found at line 547 and classified as a different claim); the builder executed scratch-to-scratch — the success line, three refusals, a 35/2 numstat, a byte-identical rebuild, the block positions — twice, before and after the scout's folds — and its on-disk digest against its committed blob; the flip SQL executed on a scratch copy of the live DB — the five sentinels by name, the 441-line capture, the fresh read-back, and the already-flipped re-run aborting at the CHECK; the clone-diff against 540's final text in three passes (540 has no committed register; its traps are its own A0 arms); the consumer dry-run (§2.0) — class assigner `shop-infra`, extractor per step with the receipt first.

**Direction verdict (after walk 1): PROCEED.** Tested: the premise (the packet's routes and homes, re-verified against the live text), the mechanism (a committed builder whose anchors and post-conditions execute; a CAS flip under a structural pre-check, rehearsed), the scope (four of twelve; the eight named as tranche two; W=29 first by release order).

**Walks:**
- Weak spots:          w1 2 folded — instruction 2 / record 0 (the P8 probe's grep was bare and without `-F` — the shim hazard the linter itself named; the already-flipped re-run's evidence stated as the CHECK message, never a piped exit code — the rehearsal's own `echo $?` read `tail`'s exit, not sqlite's)
- Destruction:         w1 dry — five edits by the builder, the live file replaced only by a proven scratch output; the flip scoped to four ids under a `CHECK(x=4)` that aborts before the UPDATE; the DB backed up first; the eight remaining accepted rows asserted unchanged by id
- Vulnerabilities:     w1 dry — literal scratch paths; `git -C` never `cd`; the `.output` path absolute because sqlite dot-commands take no variables (the canonical evidence dir, then copied into the worktree — stated); the builder refuses a live or in-root output; the A0 ladder's one `checkout` names `HEAD`
- Integration-record:  w1 dry — the manifest is the emitter's, spliced at the freeze; the class the assigner measured; the block placed above the first step (the doctrine's geometry, measured on plan B today)
- ACID:                w1 dry — doctrine commit, then the flip, then the SQL commit, then the dev-log commit; every arm of A0 names what a death between them leaves and how re-entry resumes
- **Walk 1 total: 2 findings, 2 folded — instruction 2 / record 0; 0 of 2 fold-introduced.**

**Cold panel (SMALL form):** the scout dispatched at walk 0 — its report, EXECUTION and CAPSTONE are entered here as they land.

**Status (after walk 1): the seats not yet reported — the record above is as of walk 1's fold round.**

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the bellows Developer. ⚠️ Governance runs IN PLACE at `$GOV` (every operand ABSOLUTE); commits there by explicit pathspec; ONE action per compound.
>
> ⛔ **A0 — roots, the precondition, then the branch ladder (catch-all LAST):** `cd "$(git rev-parse --show-toplevel)" && [ -f bellows.py ] && [ -d tests ] && echo TREE_OK` — HALT unless TREE_OK; `GOV=/Users/marklehn/Developer/eluvian-governance; PT="$GOV/PLANNER_TEMPLATE.md"; B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w28-a-2026-09-02.py"; DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db; [ -f "$PT" ] && [ -f "$B" ] && [ -f "$DB" ] && echo GOV_OK` — HALT unless GOV_OK. Re-derive `GOV`, `PT`, `B`, `DB` in every compound. `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python`. **P8 → both values, else HALT: `forge-cycle-w29 has not closed — this flip would halt it`.** And P6's `accepted` → 12, else HALT: `the accepted set moved — W=29's Gate 1 (outside any plan) has been applied; re-pin P6 and re-deposit` (this plan runs after W=29 closes and BEFORE its Gate 1 packet is applied on the mini — the window the CEO's release order gives it). Then the ladder — **(1)** P1's sha matches; **(2)** `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw28a-flip.sql` (state it; EMPTY on FRESH); **(3)** `git -C "$GOV" log -1 --format=%s -- PLANNER_TEMPLATE.md` — does the subject carry `gate2-pt-w28-a`?; **(4)** `sqlite3 -readonly "$DB" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (418,419,430,434) ORDER BY id;"`.
> - **FRESH** = (1) match AND (2) empty AND (3) no AND (4) all four `accepted|codify|2026-09-01T22:03:28Z` → A1.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND (4) all four still `accepted|codify` at that stamp → verify Task C on the live file (A3's counts), then A4 (the flip) and A5 only. If (2) shows `g2ptw28a-flip.sql` untracked or modified with the flip NOT applied, that is this arm too (the SQL was written and the run died before it) — A4 rewrites the file from the exact text, which is idempotent.
> - **RE-ENTRY (complete)** = (3) yes AND (4) all four `implemented|codify` with every stamp ≠ `2026-09-01T22:03:28Z` → verify Task C; then the two idempotent tails of A4: the SQL file — if `git -C "$GOV" log -1 --format=%s -- governance/knowledge/development/g2ptw28a-flip.sql` does not carry `gate2-pt-w28-a`, write it from the exact text and commit it; the capture — if `/tmp/g2ptw28a/flip-capture.txt` exists copy it into the worktree, else reconstruct the worktree copy from the backup DB (`sqlite3 -readonly <backup> "<the capture SELECT>"` → the same 441 lines, labelled `RECONSTRUCTED (post-commit re-entry)` in the dev log); then A4's read-back (no flip) and A5 with the dev log marked `re-derived on re-entry`.
> - **NONE-MATCH** = anything else → HALT quoting every measurement. The ONE recipe: (1) mismatch + (3) no + (4) all four `accepted|codify` at the stamp = a pre-commit apply that died → `git -C "$GOV" checkout HEAD -- PLANNER_TEMPLATE.md` (`HEAD`, not the index — a death between `add` and `commit` leaves v4.97 STAGED), re-assert P1, then A1. Every other shape HALTs untouched.
>
> ⛔ **A1 — re-derive P1–P6; state each; a mismatch is a HALT quoting both.** (P4 with the builder's OWN commit, never `HEAD:`.)
>
> **A2 — dry-run, scratch→scratch (LITERAL paths, no variables across compounds):** `rm -rf /tmp/g2ptw28a; mkdir -p /tmp/g2ptw28a; python3 "$B" "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw28a/PT-out.md; echo "builder_exit=$?"` → P5's `BUILT:` line verbatim, `builder_exit=0`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw28a/PT-out.md` → `35	2`; `wc -l /tmp/g2ptw28a/PT-out.md` → 2423; `wc -c` → 459191. Refusals (each `BUILDER REFUSED`, nonzero; quote each): `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/PLANNER_TEMPLATE.md"` (out == in); `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/x.md"` (under the governance root); `python3 "$B" /tmp/g2ptw28a/PT-out.md /tmp/g2ptw28a/PT-out2.md` (already built).
>
> **A3 — apply and measure the LIVE file (Task C), then commit:** `cp /tmp/g2ptw28a/PT-out.md "$GOV/PLANNER_TEMPLATE.md"`; then from the live file, counts read never exit codes (`/usr/bin/grep -cF --`): `### 103. Move the test ORACLE outside the author's model` 1 · `### 104. A detector's fire count is a RATIO` 1 · `### 105. An environment variable is a property of a PROCESS TREE` 1 · `### 106. Earnability is not discrimination` 1 · `### 34. ` 1 (Rule 34 only — the v0's checklist item is gone) · `Report the kill map, not the pass count` 1 · `fired / evaluated / skipped` 3 · `codified 2026-09-02 (Gate 2, cycle W=28 PT tranche one)` 4 · `Extends Rule 14's truth-table-in-code clause` 1 · `Kin of Rule 55's positive-control clause` 1 · `**Version:** 4.97` 1 · `**Version:** 4.96` 0 · `**Last Updated:** 2026-09-02 (v4.97)` 1 · `**Last Updated:** 2026-08-26 (v4.96)` 0 · `| 2026-09-02 | v4.97: Gate 2, cycle W=28 PT tranche one` 1 · `| 2026-08-26 | v4.96: Gate 2, cycle W=3` 1 (the old row survives); `wc -l` 2423; `cmp /tmp/g2ptw28a/PT-out.md "$GOV/PLANNER_TEMPLATE.md"` silent; `git -C "$GOV" diff --stat -- PLANNER_TEMPLATE.md` → 35 insertions, 2 deletions. Any failing → FRESH: `git -C "$GOV" checkout -- PLANNER_TEMPLATE.md` + HALT; RE-ENTRY: HALT, no restore. Commit: `git -C "$GOV" add PLANNER_TEMPLATE.md && git -C "$GOV" commit -m "[<id from your plan filename>] PLANNER_TEMPLATE v4.97: Gate 2 W=28 PT tranche one — Rules 103–105 (418, 430, 434), Checklist item 34 (419)" -- PLANNER_TEMPLATE.md`; `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` → that commit. Do NOT push.
>
> **A4 — the flip (doctrine before DB):** backup first, by sqlite's own copy — the DB is WAL-journaled, so a filesystem `cp` can lag a non-checkpointed write while sizes match (scout SC-13; 540's form): `sqlite3 "$DB" ".backup /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w28-a-$(date +%H%M%S).db"`; then on the backup, read-only: `PRAGMA integrity_check` → `ok`; `SELECT COUNT(*) FROM lesson_proposals` → equal to the live count (state both); `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (418,419,430,434) AND status='accepted'` → 4 (`BK=4`). Write `$GOV/governance/knowledge/development/g2ptw28a-flip.sql` with a QUOTED heredoc, EXACTLY:
> ```
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=4));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (418,419,430,434) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output /tmp/g2ptw28a/flip-capture.txt
> SELECT COALESCE(id,'')||'|'||COALESCE(status,'')||'|'||COALESCE(route,'')||'|'||COALESCE(category,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 441 ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (418,419,430,434) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (418,419,430,434) AND status='implemented' AND status_updated_at <> '2026-09-01T22:03:28Z';
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> ⚠️ The `.output` path is the LITERAL scratch dir A2 created (`sqlite3` dot-commands take no variables) — never a path inside the canonical bellows checkout: an untracked file there at a path this branch commits makes the worktree teardown's dirty-tree check raise and git's `--ff-only` merge abort ("untracked working tree files would be overwritten"). Run: `[ -d /tmp/g2ptw28a ] || mkdir -p /tmp/g2ptw28a; sqlite3 -bail "$DB" < "$GOV/governance/knowledge/development/g2ptw28a-flip.sql"` → the five sentinel lines BY NAME: `PRE_F=4`, `CHANGES_F=4`, `EXCL_F=4`, `ACC_POST=8`, `IMPL_POST=326` (a `CHECK` abort prints a line beginning `Runtime error near line 3: CHECK constraint failed: x=4` and writes nothing — HALT on that MESSAGE; never read the exit code through a pipe, it is the last command's). **Read-back on a FRESH read-only connection** (durability is proven post-commit, never by in-transaction sentinels): the four rows `implemented|codify` with stamps ≠ the vintage; `accepted` 8; `implemented` 326; the capture file has 441 lines. Copy the capture into the worktree: `mkdir -p knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02 && cp /tmp/g2ptw28a/flip-capture.txt knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/flip-capture.txt` (A5 commits it; QA verifies it). Commit the SQL in governance: `git -C "$GOV" add governance/knowledge/development/g2ptw28a-flip.sql && git -C "$GOV" commit -m "[<id>] gate2-pt-w28-a: the flip SQL (418, 419, 430, 434 → implemented)" -- governance/knowledge/development/g2ptw28a-flip.sql`.
>
> **A5 — dev log + commit by explicit pathspec.** `knowledge/development/dev-log-gate2-pt-w28-a-2026-09-02.md`: the A0 determination, A1's pins, A2's lines and the three refusals, A3's counts and the governance commit hash, A4's backup lines, sentinels, read-back and the SQL commit hash. `git add knowledge/development/dev-log-gate2-pt-w28-a-2026-09-02.md knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/flip-capture.txt && git commit -m "[<id>] gate2-pt-w28-a: PT v4.97 + flip landed (dev log, flip capture)" -- knowledge/development/dev-log-gate2-pt-w28-a-2026-09-02.md knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/flip-capture.txt`. `git status --short` → empty. STOP.
>
> **Deposits:**
> - `knowledge/development/dev-log-gate2-pt-w28-a-2026-09-02.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw28a-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/flip-capture.txt`
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-pt-w28-a-2026-09-02.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw28a-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/flip-capture.txt`

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message.** You are the bellows QA agent. `cd "$(git rev-parse --show-toplevel)"`; `GOV=/Users/marklehn/Developer/eluvian-governance`; `B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w28-a-2026-09-02.py"`; `DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db`; `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python` — re-derive per compound.
>
> **(A) Rule 20 self-check** — the canonical block at the path the dispatcher's mandate names (this machine's `RULE_20_SELF_CHECK_BLOCK.md`; quote the path you were handed). Run with:
> - `plan_slug`: `gate2-pt-w28-a-2026-09-02`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02"`
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-pt-w28-a.txt", "flip-capture.txt"]` (the capture committed by DEV in A5; QA verifies it is the committed file — `git log -1 --format=%s -- knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/flip-capture.txt` carries the plan's id)
>
> **(B) Items — raw output appended to `probes-raw.txt`:**
> - **Item 1 — the live template:** `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` (the `[<id>]` commit); every Task C count re-run (sixteen probes); `wc -l` 2423; `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw28a-flip.sql` → EMPTY.
> - **Item 2 — the builder, by a second pair of hands (byte-identity):** `rm -rf /tmp/g2ptw28a-qa; mkdir -p /tmp/g2ptw28a-qa; C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show "$C^:PLANNER_TEMPLATE.md" > /tmp/g2ptw28a-qa/PT-pre.md; python3 "$B" /tmp/g2ptw28a-qa/PT-pre.md /tmp/g2ptw28a-qa/PT-out.md; echo "builder_exit=$?"; cmp /tmp/g2ptw28a-qa/PT-out.md "$GOV/PLANNER_TEMPLATE.md" && echo BYTE_IDENTICAL` → the `BUILT:` line, `builder_exit=0`, `BYTE_IDENTICAL`; P4 with the builder's own commit; the three refusals reproduced.
> - **Item 3 — the flip, read back on a FRESH read-only connection:** the four rows `implemented|codify|ceo|<stamp ≠ 2026-09-01T22:03:28Z>`; `accepted` 8 — and the eight by id (415, 417, 421, 422, 425, 431, 435, 437), each still `accepted|codify|planner|2026-09-01T22:03:28Z`; `implemented` 326; `wc -l knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/flip-capture.txt` → 441; the capture's four rows for 418/419/430/434 read `accepted|codify` (the pre-flip state, captured before the UPDATE).
> - **Item 4 — the full-suite file:** `"$BPY" -m pytest tests -q -p no:cacheprovider > knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/full-suite-gate2-pt-w28-a.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/full-suite-gate2-pt-w28-a.txt` → `1676 passed, 1 skipped`, `exit=0`.
>
> **(C) The report** `qa-receipt.md`: the verification table (status cells carry the glyph only — the `rule_22_verification` gate substring-matches hedging words in positive rows), the follow-ups (tranche two; the Planner pushes governance), the Rule 20 stdout APPENDED. Commit: `git add knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/ && git commit -m "[<id>] QA: PT v4.97 rebuilt byte-identical from the pre-edit blob; flip read back (4 implemented, 8 accepted remain)" -- knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/`. STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/full-suite-gate2-pt-w28-a.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w28-a-2026-09-02/full-suite-gate2-pt-w28-a.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
