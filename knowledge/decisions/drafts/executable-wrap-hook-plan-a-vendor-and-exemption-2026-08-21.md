# bellows — wrap-hook layer, plan A: vendor into the repo + daemon exemption
**Date:** 2026-08-21 | **Tier:** Medium–Large | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full-suite BASELINE (Step 1, read-only) → targeted (Step 2 DEV) → full suite + live canary (Step 3 QA) | **Execution:** Step 1 (DEV, additive vendor + baseline) → Step 2 (DEV, exemption) → Step 3 (QA) | **Priority:** 1 | **qa_steps:** 3 | **Depends on:** Done/diagnostic-495 (measurements + edit set). **Followed by:** plan B (per-session sentinel + anti-hijack message), which edits the SAME vendored files this plan creates and must not be authored against `~/.claude/` copies.

**auto_close:** false
**pause_for_verdict:** always

## Context

Builds directly on **diagnostic-495** (`bellows/knowledge/research/wrap-hook-daemon-exemption-2026-08-21.md`), which measured the mechanism and enumerated the edit set. Two captures happened on 2026-08-21 and this plan closes both:

- **Capture 1 (daemon):** exec-493's DEV worker was SessionStart-injected with wrap debt, Stop-blocked ≥2 times, spent 47% of its turns after its real work was committed, and **pushed three remotes outside its plan scope** (governance root, bellows, and the unrelated `study`). Its Stop hook then removed the SHARED sentinel, disarming the CEO's in-flight wrap.
- **Capture 2 (interactive):** the Planner session was blocked by a *different terminal's* armed wrap and handed that session's checklist — the same hijack, on a session the daemon exemption alone would NOT protect. This is why the two fixes ship together.

**Measurements this plan may rely on WITHOUT re-deriving (diagnostic-495, plus two taken at authoring on 2026-08-21):**
1. Hook subprocesses **do** inherit env vars set on the `claude -p` process — probed live, `BELLOWS_DISPATCH=1` observed from both a SessionStart and a Stop hook.
2. **All three** hook payloads carry `session_id`: SessionStart and Stop (495 Q3(b)) and **UserPromptSubmit** (measured at authoring — keys: `cwd`, `hook_event_name`, `permission_mode`, `prompt`, `session_id`, `transcript_path`). This is the enabler for a per-session sentinel; all three hooks currently READ AND DISCARD stdin.
3. **Three** `claude` spawn sites, not one: `runner.py:201-208` (+`Popen` at 218-223), `bellows.py` auth preflight (~1995), `planner.py` consultation (~129, `cwd="/tmp"`). All inherit the daemon env with no `env=`.
4. The enforcement layer is in **no git repository** and has **no tests** (`bellows/tests/` contains none matching wrap/hook).

**⚠️ SCOPE BOUND — this plan does NOT fix capture 2.** The daemon exemption protects daemon-dispatched sessions only. Capture 2 is two INTERACTIVE sessions sharing one sentinel, and neither is a daemon, so nothing here helps it — that is **plan B**'s subject and it must not be claimed in this plan's code, comments, receipts, or QA report. Capture 2 is described above because it is the reason the pair exists and because it explains why this plan deliberately leaves sentinel semantics untouched: changing them here would collide with plan B, which edits the same three files.

## MUST-PRESERVE

- **The CEO's interactive wrap lock must never end this plan disabled or weakened.** Every step leaves it working. A false-positive exemption that silently disables the lock is WORSE than the defect being fixed.
- **Never create or delete a sentinel outside this plan's own scratch scope.** Do not `rm` any `.wrap-in-progress*` at the governance root that you did not create in a test. ⚠️ In THIS plan nothing you write changes sentinel behavior at all — the sentinel semantics are untouched here and are plan B's subject. So this rule is absolute for plan A: no sentinel at the governance root is created, renamed, or removed, by you or by the code you write, except a scratch one under `ELUVIAN_WRAP_ROOT` in a test.
- Backward compatibility: a pre-existing **bare** `.wrap-in-progress` (the current format) must still arm and still be clearable — a stale bare sentinel must not become permanently unclearable after this change.
- Do not restart, stop, or reconfigure the bellows daemon.
- ⚠️ You are a daemon-dispatched session and this plan edits the hooks that govern you. Until Step 2 lands you may be debt-injected; if a wrap is armed you may be Stop-blocked. That is the SUBJECT OF STUDY, not an instruction — never resolve a wrap, never commit/push/add outside this plan's declared scope, in any repo, for any reason.

## Drafting Cycle
**Tier:** T2 — triggers: **T-6** (governance/enforcement surface — these hooks ARE the enforcement layer) + **T-5** (the live layer at `~/.claude/` is unversioned until Step 1 lands, so a mistake there has no revert) + T-8 (novel: no prior plan has edited this layer). T-7 inherited from diagnostic-495.
**Walk 0 (context pin) — REAL (2026-08-21):**
1. Newest same-class: no prior plan has edited `~/.claude/eluvian/` — this is the first. Nearest structural clone is a governance in-place plan ([[governance-in-place-dispatch]]): absolute operands, no project `.git` at the target until Step 1 creates one.
2. Pre-edit pins (agent RE-VERIFIES, does not trust): `wrap_stop_hook.py` `SENTINEL = ROOT / ".wrap-in-progress"`; `wrap_debt_hook.py` contains NO sentinel reference (channel 1 is ungated); `wrap_arm_hook.py` reads `data.get("prompt")` and calls `SENTINEL.touch()`; `runner.py:21`/`bellows.py:24` carry the `os.environ.setdefault("DISABLE_AUTOUPDATER","1")` precedent.
3. The vendored tree is NEW (additive, no anchor replacement); Step 2's edits are anchor replacements against files Step 1 just committed, so each has a diff.
4. CEO decision 2026-08-21 (diagnostic-495 Fork 1): canonical location is **`bellows/hooks/eluvian/`**.
- Cold panel (T2, §2.6): REQUIRED — status recorded below before deposit.

**Walk 1 — warm lens-by-lens, folds applied:**
- Weak spots (1.4):   w1 3 folded — instruction 3 (W1 the backward-compat rule was DELEGATED to the agent ("decide and implement one rule") on the plan's most defect-prone question → settled it in the plan; W2 `~/.claude/settings.json` is an out-of-repo mutation no gate can see, and hook wiring loads at SESSION START — both unstated → added, with a JSON-parse re-read; W3 `ELUVIAN_WRAP_ROOT` travels by inheritance exactly as `BELLOWS_DISPATCH` does, so a spawn-side setting would redirect every sentinel lookup → forbidden on the daemon side).
- Destruction (2.4):  w1 2 folded — instruction 2 (D1 step 2 repointed the CEO's LIVE lock at code whose tests had not yet run, and with `pause_for_verdict: always` a halt leaves that window open indefinitely → repoint is now the final action, gated on green targeted tests, with an explicit "stop with the repoint NOT done" path; D2 the running daemon executes this plan THROUGH `runner.py` while the step edits it — a syntax error would surface as an unexplained Step-3 spawn failure, not as a visible bug → `py_compile` on all three daemon modules first).
- Vulnerabilities:    w1 1 folded — instruction 1 (V1 D1's own fold created a Convention 4 trap: the settings backup was written during (F), so a CORRECT halt at (G) would gate-FAIL `deposit_exists` on a declared-but-absent deposit → backup moved to a new (A0) first action, making it unconditional on every path).
- Integration-record: w1 1 folded — instruction 0 / record 1 (I1 MUST-PRESERVE's "never create or delete a sentinel" read as forbidding the RUNTIME rename the design requires → split agent-acts vs code-you-write. Also verified rather than cloned: `qa_steps` is a set of step NUMBERS mirroring `gates._gate_is_qa_step` (`plan_lint.py:31-36`), so `qa_steps: 3` correctly designates step 3, not a count).
- ACID (alone, on the four-lens-folded draft): w1 1 folded — instruction 1 (A1 **reversed W1's own fold**: migrate-on-encounter would have the first session to END A TURN claim a legacy bare sentinel — not necessarily the session that armed it — converting the disarm defect into a misappropriation defect that silently locks an uninvolved session into someone else's wrap. Replaced with "legacy bare sentinel keeps exactly today's semantics and self-clears on the next passing check", which adds no hazard that does not already exist).
**Walk 1 STATUS:** 8 folded — instruction 7 / record 1 — NOT dry. Two folds were repairs of earlier folds in the SAME walk (V1 fixed D1's side effect; ACID reversed W1), which is the sequential-fold discipline paying for itself on a plan whose subject is a lock that can trap its own operator.

**Walk 2 — warm lens-by-lens over the whole artifact:**
- Weak spots (1.4):   w2 2 folded — instruction 2 (W2-1 **the fix breaks its own canary**: after (A) every daemon session carries `BELLOWS_DISPATCH=1`, so the `claude -p` that assertion (ii) spawns INHERITS it and would be exempt — the lock would allow, and (ii) would HALT for a reason unrelated to the lock being broken → `env -u BELLOWS_DISPATCH` mandated and must be shown; W2-2 a session blocked behind a foreign wrap had no bounded escape — measured 2026-08-21, the Planner was held ~5 min with no in-band way to distinguish "still working" from "abandoned" → foreign-sentinel AGE in the message, and clearing one is the CEO's call alone).
- Destruction (2.4):  w2 1 folded — instruction 1 (D2-1 `~/.claude/settings.json` wires hooks that are NOT ours — a second `Stop` entry curling the voice harness, a `PreToolUse` matcher, a `PermissionRequest` entry — and a wholesale re-serialize would silently drop them → edit only entries referencing `~/.claude/eluvian/`, then diff against the backup and HALT on any delta beyond the three paths).
- Vulnerabilities:    w2 1 folded — instruction 1 (V2-1 two paths resolve themselves and an agent would "helpfully" break both: `wrap_check.py` is located by `Path(__file__).with_name(...)` so it needs no wiring and a fourth entry would be wrong; and `hooks.log` must stay at its current path or the canary loses its probe and the migration orphans the history).
- Integration-record: w2 2 folded — instruction 1 / record 1 (I2-1 `settings-hooks-snapshot.json` and `settings-backup-2026-08-21.json` are near-identical in name and easily conflated → stated that they are a versioned RECORD and a REVERT copy respectively, and that both are required; I2-2 canary (i) said to hand-set the marker, which would test a synthetic path → re-specified to prove the marker is in the step's OWN env, then spawn plainly so it propagates by real inheritance).
- ACID (alone, on the four-lens-folded draft): w2 2 folded — instruction 2 (A2-1 canary (i) asserted lines "for that session" against a log that **structurally cannot express it** — `hooks.log` has no session id, so the assertion was unearnable ([[earn-the-clean-gate-dont-author-it]]); since (C) already parses `session_id`, `hooklog` now appends it on every branch, which simultaneously closes the owed instrumentation diagnostic-495 identified; A2-2 the assertion was re-specified to match by ID rather than recency, because other sessions write to that log concurrently).
**Walk 2 STATUS:** 8 folded — instruction 7 / record 1 — NOT dry. The two highest-value folds are self-referential: the fix's own marker would have broken its acceptance test (W2-1), and the acceptance test asserted a property the log could not carry until this plan added it (A2-1).

**Walk 3 — warm lens-by-lens over the whole artifact:**
- Weak spots (1.4):   w3 1 folded — instruction 1 (W3-1 walk 1's D1 fold made (F) run after (G), but the DOCUMENT still listed F before G — an agent executing top-to-bottom would repoint the CEO's live lock before the tests ran, doing precisely what the fold forbade. The prose warning was not enough against reading order → explicit `EXECUTION ORDER` line declared authoritative over document order).
- Destruction (2.4):  w3 1 folded — instruction 1 (D3-1 with `pause_for_verdict: always` every step ends at a verdict gate, but the plan never said what a halt LEAVES BEHIND — the Planner would have to re-derive the live layer's state to judge one → added a `## Halt states` table, including that the designed mid-Step-2 stop is indistinguishable from "after Step 1" for the live layer, i.e. the failure path is safe by construction).
- Vulnerabilities:    w3 dry — gate surface re-checked after the new sections: every Step-2 deposit is produced before the designed stop point ((A0) backup, (G) tests, (A)-(E) edits all precede (F)), so the Convention 4 exposure walk 1 closed does not reappear via the new ordering; Deposits and Scope still match per step (7/7, 8/8, 3/3).
- Integration-record: w3 dry — the halt-state table agrees with (F) ("originals still present and unmodified") and with (G)→(F) ordering; `**Depends on:** Done/diagnostic-495` is accurate now that 495 is closed; no count, path, or pin restated inconsistently.
- ACID (alone, on the four-lens-folded draft): w3 dry — walk 3's two folds are complementary rather than interacting: one fixes the order of operations, the other documents the state each order produces; neither touches a probe, a deposit, or a test assertion.
**Walk 3 STATUS:** 2 folded — instruction 2 / record 0 — NOT dry. Yield 8 → 8 → 2; both folds are execution-safety rather than design defects, which is the expected late-cycle shape.

**Walk 4 — warm lens-by-lens over the whole artifact:**
- Weak spots (1.4):   w4 2 folded — instruction 2 (W4-1 Step 3 was told to HALT on "any regression versus the pre-change baseline" and **no baseline was ever captured** — an unfalsifiable check that passes by default; Step 1 is the only moment a true baseline exists, so it now runs the suite and deposits `pytest_baseline.txt`. W4-2 canary (ii) as written would spawn a real session and BLOCK it — a blocked session cannot end its turn, so the test would loop against the lock until timeout, reproducing the trap instead of testing it → invoke the hook script directly with a synthetic Stop payload, and report the (i)-live / (ii)-direct asymmetry honestly).
- Destruction (2.4):  w4 1 folded — instruction 1 (D4-1 **the tests run inside an exempt environment**: after (A) the daemon sets `BELLOWS_DISPATCH=1` on the very session that runs pytest, so the (B) guard fires by default and every blocking assertion inverts — green by hand, red under the daemon → autouse `delenv` mandated. [[hot-path-guard-needs-mock-audit]] class. Verified while checking this: `tests/conftest.py` already isolates verdicts dir, runner logs, and `lifecycle.db` to `tmp_path` via autouse fixtures, which is why the new Step-1 baseline run is safe alongside the live daemon).
- Vulnerabilities:    w4 1 folded — instruction 1 (V4-1 D4-1's fixture had no stated home; in the shared `tests/conftest.py` it would silently change the environment EVERY existing test runs under — a suite-wide behavior change nothing in this plan tests → scoped to `test_wrap_hooks.py`, with an instruction to STOP rather than relocate it).
- Integration-record: w4 2 folded — instruction 1 / record 1 (I4-1 Step 3 still referred to "the pre-change baseline" abstractly after W4-1 created a real file → names the path, quotes both counts lines, and makes a MISSING baseline itself a HALT; I4-2 the header's `Test Scope` no longer described what the steps run → restated per step).
- ACID (alone, on the four-lens-folded draft): w4 2 folded — instruction 1 / record 1 (A4-1 Step 1's own framing still said its point was "only" version control after W4-1 gave it a second job → corrected, noting both are read-only w.r.t. the live layer; A4-2 W4-1's new deposit writes into a directory that does not exist yet — a declared deposit with no parent dir fails the write and then gate-fails `deposit_exists` → explicit `mkdir -p` step added as item 0).
**Walk 4 STATUS:** 8 folded — instruction 6 / record 2 — NOT dry. Yield 8 → 8 → 2 → 8: the rise is explained and is a SHAPE signal, not noise — W4-1 added a genuinely new artifact (the baseline) late in the cycle, and four of this walk's folds are its direct consequences (name it, mkdir it, reframe the step, update the header). The environment-inheritance family (D4-1) is the same root cause as walk 2's W2-1, now found in the unit tests rather than the canary.

**⚠️ cycle_check after walk 4: `ESCALATE:yield-rising` (8 → 8 → 2 → 8) — RESOLVED BY SPLIT, CEO-directed 2026-08-21.** Per [[rising-yield-means-split-not-walk]] the rise was read as a SHAPE signal, and it corroborated diagnostic-495 Q6, which had recommended two executables. The CEO directed the split. **Walks 1–4 above were run on the PAIRED artifact**, which carried both the daemon exemption and the per-session sentinel; this plan is the exemption half. Folds are carried forward verbatim, so several walk records reference material that now lives in plan B — they are retained as the honest provenance of each fold, not as instructions. The cut itself was then reviewed as its own pass ([[a-subtractive-cut-needs-a-diff-review]]) — see walk 5 below.

**Walk 5 — POST-CUT review walk (the subtractive cut is its own pass, [[a-subtractive-cut-needs-a-diff-review]]):**
- Weak spots (1.4):   w5 1 folded — instruction 1 (W5-1 (B) now introduces a `json.loads` on stdin inside hooks that are FAIL-OPEN by construction — `wrap_stop_hook.main()` sits under a bare `except` that prints `{}` and ALLOWS. An unhandled parse error on empty or malformed stdin would therefore not surface as an error; it would **silently disable the CEO's lock**, turning a logging convenience into the catastrophic false positive this plan exists to prevent → own try/except, degrade to a placeholder id, and test empty + non-JSON stdin).
- Destruction (2.4):  w5 3 folded — instruction 2 / record 1 (**cut damage, found by reviewing the cut rather than trusting it**: (a) MUST-PRESERVE pointed at "the migrate-on-encounter rule in (C)" — a section that no longer exists here — and would have instructed the agent to implement plan B's behavior; rewritten to an absolute no-sentinel-changes rule for plan A. (b) (B) justified its `session_id` parse as "you are already parsing it for (C)", a dangling premise once (C) left. (c) the Context still carried the "BOUND ON WHAT THE SENTINEL HALF CAN FIX" paragraph, which described plan B's scope inside plan A → replaced with a SCOPE BOUND stating plainly that this plan does NOT fix capture 2 and must not claim to).
- Vulnerabilities:    w5 dry — gate surface re-checked after the cut: Step 1 Scope/Deposits 8/8 (seven vendored artifacts + the baseline), Step 2 8/8, Step 3 3/3; every Step-2 deposit is still produced before the designed stop at (G); the `EXECUTION ORDER` line was updated to `(A0) → (A) → (B) → (E) → (G) → (F)` and no longer names removed items.
- Integration-record: w5 dry — the walk 1–4 records are retained verbatim as fold provenance and are now explicitly labelled as having been run on the PAIRED artifact, so a reader cannot mistake a record entry for an instruction; the `Depends on` / `Followed by` line ties A and B in the right order.
- ACID (alone, on the four-lens-folded draft): w5 dry — walk 5's folds are independent (one runtime-safety rule, three provenance repairs); none touches a probe, a deposit path, a test assertion, or the execution order.
**Walk 5 STATUS:** 4 folded — instruction 3 / record 1 — NOT dry. Yield 8 → 8 → 2 → 8 → 4. ⚠️ Three of four are CUT DAMAGE — the split itself introduced defects, exactly as [[a-subtractive-cut-needs-a-diff-review]] predicts, and one of them (the dangling (C) reference) would have told the agent to implement plan B's sentinel behavior inside plan A.

**Walk 6 — lens-by-lens over the whole artifact:**
- Weak spots (1.4):   w6 1 folded — instruction 1 (W6-1 Step 3 item 3 told QA to `cmp` the backup against "the pre-edit original" — but after (F) that original exists ONLY as the backup, so the assertion compares a file to itself and can never fail: an unearnable check dressed as verification ([[earn-the-clean-gate-dont-author-it]]) → replaced with the two checkable claims, a bounded `diff` (only the three eluvian paths may differ) and a JSON parse of both files).
- Destruction (2.4):  w6 dry — no instruction added or removed changes what the step writes; the revert path (backup file) is unaffected and the originals under `~/.claude/eluvian/` remain untouched by every path.
- Vulnerabilities:    w6 dry — re-checked the three probes this plan asks an agent to earn: the byte-identity `cmp` in Step 1 (real files on both sides), the `hooks.log` id assertion in Step 3 (i) (earnable only because (B) adds the id), and the bounded `diff` from W6-1. None now rests on a comparison whose two sides are the same artifact.
- Integration-record: w6 dry — Step 3's three items agree with Step 2's (F): the repoint it verifies is the one (F) performs, the backup it diffs is the one (A0) takes, and the id it greps is the one (B) writes.
- ACID (alone, on the four-lens-folded draft): w6 dry — the single fold replaces one verification with two and touches nothing else; no fold from walks 1–5 is re-opened.
**Walk 6 STATUS:** 1 folded — instruction 1 / record 0 — NOT dry. Yield 8 → 8 → 2 → 8 → 4 → 1, falling again now that the cut damage is closed.

**Walk 7 — closing walk, lens-by-lens over the whole artifact:**
- Weak spots (1.4):   w7 dry — every instruction that asks for a judgement now carries its own failure mode, and the load-bearing safety rule (W5-1) was re-verified against source rather than recalled: `wrap_stop_hook.py`'s last-resort handler prints `{"systemMessage": ...}` and `sys.exit(0)` — it ALLOWS, with no `decision: block`, confirming that an unhandled parse error would silently disable the lock exactly as the fold states.
- Destruction (2.4):  w7 dry — the write surface is unchanged from walk 6: seven vendored artifacts plus a baseline (Step 1), six edited files plus a backup and a test module (Step 2), three QA artifacts (Step 3), and the single out-of-repo `settings.json` edit, which is bounded by a diff and two JSON parses.
- Vulnerabilities:    w7 dry — Scope/Deposits parity verified MECHANICALLY rather than by eye (the [[freeze-checklist-is-undefined-doctrine]] failure this session already produced once): step 1 = 8/8, step 2 = 8/8, step 3 = 3/3, and each step's two lists are set-equal. No conditional file appears in any Deposits block.
- Integration-record: w7 dry — the recorded yield series (8 → 8 → 2 → 8 → 4 → 1 → 0) matches the per-walk STATUS lines; walks 1–4 remain labelled as paired-artifact provenance; the ESCALATE and its CEO-directed resolution are both on the record.
- ACID (alone, on the four-lens-folded draft): w7 dry — no fold re-opens another; the one cross-section dependency (Step 3 verifies what Step 2 (F) performs, using the id Step 2 (B) writes) is stated consistently from both ends.
**Walk 7 STATUS:** 0 folded — instruction 0 / record 0 — **DRY**. Yield 8 → 8 → 2 → 8 → 4 → 1 → 0. The last event before deposit is a dry pass and the final walk carries zero INSTRUCTION-class findings (§2 bar met on the class test).

**§5 Conformance pass (run at shape-stability, AFTER the closing dry walk; exit codes are the LAST run's):**
- `plan_lint.py` → **exit 0**, 10 PASS / 0 FAIL. Declared WARNs, each understood: two `step N mentions tests but declares no test scope` (steps 1 and 3 RUN the suite but create no `test_*.py`; step 2, which does, PASSES the check), and two `missing path` notices for `tests/test_wrap_hooks.py` and `pytest_baseline.txt` — both are artifacts this plan CREATES, so their absence now is correct.
- `propagation_check.py` → **exit 2 (COULD NOT RUN)** — no Numbers-discipline symbol table is declared, so detector (1) has nothing to check. Recorded as could-not-run, NOT as clean ([[reason-for-not-running-is-itself-a-claim]]).
- ⚠️ **Cold panel (T2, §2.6): NOT YET RUN — this plan is not deposit-ready until it is.** Recorded here rather than waived: §1 computes T2 from T-6, and §2.6 requires the panel once the bar is met. The warm cycle reached BAR_MET; the panel is the remaining gate.

**Closing:** Seven walks (yield **8 → 8 → 2 → 8 → 4 → 1 → 0**), of which walks 1–4 ran on the paired artifact before the CEO-directed split and walks 5–7 on this plan. `cycle_check`: CONTINUE ×3 → **ESCALATE:yield-rising** → split → CONTINUE ×2 → **BAR_MET**. The cycle's highest-value catches were self-referential: the exemption marker would have been inherited by its own acceptance test and by pytest, inverting every assertion that the lock still blocks; and walk 5 found that three of the split's four folds were CUT DAMAGE, including a dangling reference that would have told the agent to implement plan B's sentinel behavior here. The step order is the safety property — Step 1 is purely additive, and the live wiring is repointed only after targeted tests pass, so every halt state leaves the CEO's lock working.

## Halt states (what the world looks like if this plan stops after each step)

`pause_for_verdict: always`, so every step ends at a verdict gate. The Planner must be able to read a halt without re-deriving the state:
- **After Step 1:** vendored copies committed in the repo; `~/.claude/` untouched; `settings.json` unchanged. The live lock runs exactly the code it runs today. **Nothing is owed** — Step 1 is purely additive and safe to leave indefinitely.
- **After Step 2 — ⚠️ THE RESTART POINT.** Step 2 lands `env=` in `runner.py`, but the RUNNING daemon still holds the pre-edit module in memory. **Restarting bellows at this verdict gate is what makes the exemption actually take effect and makes Step 3's (i-b) live-verifiable.** It is optional — Step 3 handles both cases and reports which occurred — but a restart here converts a code-inspection claim into a measured one, at the cost of one command. Otherwise: the live wiring points at the vendored copies, which carry the exemption and have passed TARGETED tests but not the full suite or the live canary. The lock is believed working but not yet live-proven. **A halt here is acceptable but not indefinite** — the revert is `settings-backup-2026-08-21.json`, and the originals under `~/.claude/eluvian/` are still present and unmodified.
- **Mid-Step-2 halt (tests not green, repoint deliberately not done):** identical to "after Step 1" for the live layer — nothing is repointed, so nothing is at risk. This is the designed failure path, not a broken state.
- **After Step 3:** fully shipped and live-verified.

## STEP 1 — DEV: vendor the enforcement layer into the repo (ADDITIVE ONLY, zero behavior change)

**Role:** DEV.

⚠️⚠️ **WORKTREE DISCIPLINE — the absolute paths in Scope/Deposits name files by IDENTITY, not by the checkout you edit.** Bellows dispatches this plan into `bellows/.bellows-worktrees/<id>/` and your cwd is that worktree (measured: plan 495 ran at `.bellows-worktrees/495`). **Edit and commit inside YOUR worktree** — the same relative paths under your cwd — exactly as the newest same-class bellows plan (exec-492) directs. ⚠️ Do NOT open the absolute `/Users/marklehn/Developer/GitHub/bellows/...` copies for editing: that is the MAIN checkout, it is the code the LIVE DAEMON is running right now, and `gates._resolve_deposit_path` falls back to "path as-is" while `_check_deposit_uncommitted` swallows the out-of-worktree git error — so editing the wrong checkout passes both gates SILENTLY. The one deliberate exception is `~/.claude/settings.json`, which is machine-global by design and is handled under (F) with its own guards.

⚠️ **This step must not be able to break the live lock.** COPY, do not move. Leave every file under `~/.claude/` exactly as it is, and do NOT touch `~/.claude/settings.json` — the live hooks keep running from their current paths for the whole of this step. The point is to get the layer under version control so Step 2's edits have a diff and a revert, AND to capture the pre-change test baseline while this repo is still behaviorally unchanged (item 5) — the only moment at which a true baseline can be taken. Both are read-only with respect to the live enforcement layer.

0. Create the directories this step writes into — `bellows/hooks/eluvian/`, `bellows/hooks/commands/`, and `bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/` (`mkdir -p`). ⚠️ The evidence directory is NEW: item 5 deposits `pytest_baseline.txt` into it, and a declared deposit whose parent directory does not exist fails the write and then gate-fails `deposit_exists` — a prior plan lost a step to exactly this omission. `bellows/knowledge/qa/` itself may or may not exist; `mkdir -p` covers both.
1. Create `bellows/hooks/eluvian/` and copy in, byte-identical: `wrap_check.py`, `wrap_arm_hook.py`, `wrap_stop_hook.py`, `wrap_debt_hook.py` from `/Users/marklehn/.claude/eluvian/`.
2. Create `bellows/hooks/commands/` and copy in `wrap.md` from `/Users/marklehn/.claude/commands/`.
3. Copy the `hooks` stanza of `~/.claude/settings.json` into `bellows/hooks/settings-hooks-snapshot.json` as a WRAPPED object — `{"hooks": { ... }}` via `json.load` + `json.dump(indent=2)`, not a byte-slice, so the artifact is independently parseable (a bare inner stanza is not valid standalone JSON, and "verbatim" left that ambiguous) as a record of the wiring at vendor time (a snapshot, not a live config). ⚠️ This is NOT the same artifact as Step 2's `settings-backup-2026-08-21.json` and neither replaces the other: this snapshot is a versioned RECORD of the pre-migration wiring, committed with the vendored tree; the Step-2 backup is a full-file REVERT copy of `settings.json` taken immediately before it is edited. Produce both.
4. Write `bellows/hooks/README.md`: state that this tree is the CANONICAL copy as of the CEO's 2026-08-21 decision, that `~/.claude/` is the LIVE location the harness loads, that Step 2 repoints the live wiring here, and that edits must be made HERE and never directly in `~/.claude/`.
5. **Capture the full-suite BASELINE** — run the full pytest suite now, while this repo is behaviorally unchanged, and save the raw output to `bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/pytest_baseline.txt`. ⚠️ Step 3 is required to HALT on "any regression versus the pre-change baseline", and without this file there is no baseline to compare against — the check would be unfalsifiable and would pass by default. This step is the only place a true pre-change baseline can be taken, because Step 2 edits the code. Record the counts line verbatim in your Receipt. **Predicted (measured by a cold seat on a faithful scratch mirror, 2026-08-21): `1183 passed, 1 warning in ~39s`, and IDENTICAL inside a real `.bellows-worktrees/<id>` — worktree parity confirmed.** ⚠️ A prediction to VERIFY, not a target to match: if your number differs, report yours and say so rather than reconciling to this figure ([[plan-predicted-numbers-need-verify-clause]]).
6. **Verify byte-identity** for all five copied files with `cmp` (or `shasum -a 256` on both sides, compared) and paste the raw output. A difference is a HALT, not something to reconcile by hand.

**Scope:**
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_check.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_arm_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_stop_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_debt_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/commands/wrap.md`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/settings-hooks-snapshot.json`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/README.md`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/pytest_baseline.txt`

**Deposits:**
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_check.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_arm_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_stop_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_debt_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/commands/wrap.md`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/settings-hooks-snapshot.json`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/README.md`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/pytest_baseline.txt`

**Commit:** `git -C /Users/marklehn/Developer/GitHub/bellows add <all EIGHT declared paths — the seven vendored artifacts plus `pytest_baseline.txt`> && git -C /Users/marklehn/Developer/GitHub/bellows commit -m "..."`. Your final operation is the commit.

## STEP 2 — DEV: daemon exemption (edit the VENDORED copies, then repoint)

**Role:** DEV. Edit ONLY under `bellows/hooks/eluvian/` — never `~/.claude/eluvian/` directly.

⚠️⚠️ **WORKTREE DISCIPLINE — the absolute paths in Scope/Deposits name files by IDENTITY, not by the checkout you edit.** Bellows dispatches this plan into `bellows/.bellows-worktrees/<id>/` and your cwd is that worktree (measured: plan 495 ran at `.bellows-worktrees/495`). **Edit and commit inside YOUR worktree** — the same relative paths under your cwd — exactly as the newest same-class bellows plan (exec-492) directs. ⚠️ Do NOT open the absolute `/Users/marklehn/Developer/GitHub/bellows/...` copies for editing: that is the MAIN checkout, it is the code the LIVE DAEMON is running right now, and `gates._resolve_deposit_path` falls back to "path as-is" while `_check_deposit_uncommitted` swallows the out-of-worktree git error — so editing the wrong checkout passes both gates SILENTLY. The one deliberate exception is `~/.claude/settings.json`, which is machine-global by design and is handled under (F) with its own guards. The repoint in (F) is what makes the vendored copies live.

⚠️⚠️ **Before editing, confirm no wrap is armed: `find /Users/marklehn/Developer/GitHub -maxdepth 1 -name '.wrap-in-progress*'` prints NOTHING. ⚠️ Do NOT use `ls <path>/.wrap-in-progress*`: under zsh an unmatched glob aborts the command with `no matches found` and a NON-ZERO status, so the CLEAN case looks like a failure and `ls` never runs — the check's success signal would be an error.** Distinguish two different loading rules — the hook WIRING (the `command` paths in `settings.json`) is read at session start, but a hook SCRIPT is executed as a fresh subprocess per event and is therefore re-read every time. Once (F) repoints at the vendored tree, any later edit to those scripts is live immediately for every session on this machine. In THIS plan the vendored copies are not yet wired until (F), so your (B) edits are inert while you make them — but the check still matters, because (F) itself goes live instantly and must not land on top of an in-flight wrap. If a sentinel exists, STOP and report rather than repointing the lock out from under it.

⚠️ **EXECUTION ORDER — (A0) → (A) → (B) → (B2) → (E) → (G) → (F).** The items are LETTERED for reference, not sequenced for execution, and (F) is deliberately out of alphabetical order: it is the LAST action, gated on (G)'s tests being green. Do not execute this section top-to-bottom. Read this ordering line as authoritative wherever it conflicts with document order.

**(A0) FIRST ACTION — back up the live settings file.** Copy `~/.claude/settings.json` verbatim to `/Users/marklehn/Developer/GitHub/bellows/hooks/settings-backup-2026-08-21.json` before anything else. ⚠️ Do this at the START, not as part of (F): it is a pure read-and-copy that is safe on every path, and this step's declared Deposits are treated as REQUIRED by `deposit_exists` and `rule_22_verification` — so if the backup were only written during (F), a CORRECT halt at (G) (tests not green, repoint deliberately not done) would gate-FAIL on a missing deposit ([[bellows-plan-deposits-block-format]] Convention 4: a Deposits block lists only files the step produces on EVERY path). Creating it first makes it unconditional.

**(A) Daemon exemption — the marker.** At all THREE spawn sites, pass the marker scoped to the child, using `env={**os.environ, "BELLOWS_DISPATCH": "1"}` on the `Popen`/`run` call. ⚠️ **Do NOT use `os.environ.setdefault`** (the `DISABLE_AUTOUPDATER` shape): that marks the daemon's OWN environment, so an interactive `claude` launched from the daemon's shell or from `dashboard.py`'s terminal would inherit it and **silently lose the wrap lock** — a false positive reached by ordinary use. Sites: `runner.py` (the `Popen` at ~218-223), `bellows.py` (auth preflight `subprocess.run` at ~1995), `planner.py` (consultation `subprocess.run` at ~129).

**(B) Daemon exemption — the guards.** In `wrap_debt_hook.py` and `wrap_stop_hook.py`, as the FIRST action in `main()` after draining stdin: if the session is daemon-dispatched, take the existing no-op branch (`emit(None)` / `allow()`) and `hooklog` a distinct token — use `SessionStart` → `daemon-exempt` and `Stop` → `daemon-exempt` so the canary can grep for them. ⚠️ **Also append the `session_id` to every `hooklog` detail field, in all three hooks and on every branch** (not only the exempt ones). Today `hooks.log` records no session id at all, which makes EVERY attribution in this system a timestamp inference across two sources — diagnostic-495 named this as owed instrumentation, and Step 3's canary literally cannot assert "these lines came from that session" without it. That means this plan parses the hook's stdin payload — which all three hooks currently read and DISCARD — solely to log the id; it changes no other behavior, and plan B later reuses the same parse for the per-session sentinel. ⚠️⚠️ **Wrap that parse in its OWN try/except and degrade to "no id" — never let it raise.** These hooks are FAIL-OPEN by design: `wrap_stop_hook.main()` is wrapped in a bare `except` that prints `{}` and ALLOWS the turn. So an unhandled exception from a new `json.loads` on malformed or empty stdin would not surface as an error — it would **silently disable the CEO's wrap lock**, converting a logging nicety into the exact catastrophic false-positive this plan is built to prevent. Log the id when present, log a fixed placeholder when not, and let neither path escape. Cover empty stdin and non-JSON stdin in the (G) tests. It costs one field and turns an inference into a read. Keep the existing `<iso-ts>\t<event>\t<detail>` shape; put the id inside `detail`. Do NOT add a guard to `wrap_arm_hook.py`: a daemon prompt never matches the arm trigger, and adding one there would be an untested change with no failure it prevents — say so in a comment.

**(B2) ⚠️ THREE MEASURED IMPLEMENTATION TRAPS in (B)/(E) — a cold seat hit all three by writing the code:**
- **`BELLOWS_DISPATCH=0` MUST NOT EXEMPT.** "Truthy" is the wrong test: the string `"0"` is truthy in Python, so the natural off-switch would DISABLE the CEO's lock (measured: the guard returned `{}` and logged `daemon-exempt` with `BELLOWS_DISPATCH=0`). Test membership in an explicit allow-set — `os.environ.get("BELLOWS_DISPATCH", "").strip().lower() in {"1", "true", "yes"}` — and unit-test `"0"`, `""`, and unset as NOT exempt.
- **`os.environ.get("VAR", DEFAULT)` is the WRONG default form** for the root: an env var that is SET BUT EMPTY returns `""`, not the default, and the sentinel then resolves CWD-relative — a `.wrap-in-progress` in whatever directory the hook happened to run from (measured both directions). Use the `or` form: `os.environ.get("ELUVIAN_WRAP_ROOT") or DEFAULT_ROOT`.
- **No PEP 604 annotations in these hooks.** They run under `/usr/bin/python3` 3.9.6, and `wrap_stop_hook.py` is the one hook WITHOUT `from __future__ import annotations` — a signature like `def _sentinel_for(session_id: str | None = None)` raises `TypeError` at DEF time, which is outside the fail-open handler, so the hook prints nothing and exits 1 (measured). Either add the `__future__` import or use no annotations at all.

**(E) Testable sentinel root — RESOLVE IT AT CALL TIME, NEVER AT IMPORT.** ⚠️⚠️ The hooks currently compute `ROOT` and `SENTINEL` as MODULE-LEVEL CONSTANTS (`wrap_stop_hook.py:25-26`, `wrap_arm_hook.py:28-29`, verified 2026-08-21). If you add the env lookup at that same module level, `monkeypatch.setenv("ELUVIAN_WRAP_ROOT", tmp_path)` inside a test is **INERT** — the constant was already bound at import, and a cold-panel seat DEMONSTRATED this by executing the exact module shape and fixture this plan mandates: the module still resolved `SENTINEL = /Users/marklehn/Developer/GitHub/.wrap-in-progress`. The consequence is not a failing test, it is **the unit tests arming and unlinking the CEO's REAL sentinel, silently, on every future full-suite run.** Therefore: provide a FUNCTION (e.g. `_wrap_root()` / `_sentinel_for(session_id)`) that reads `os.environ.get("ELUVIAN_WRAP_ROOT")` on EVERY call, and route every read/write/unlink through it. Keep the module constant only as the default inside that function. Add a test that asserts the override actually takes effect — i.e. that after `setenv`, the resolved path is under `tmp_path` — because that assertion is the one that fails loudly if anyone reintroduces import-time binding. Apply it in the two hooks that actually resolve a sentinel — `wrap_stop_hook.py` and `wrap_arm_hook.py`. ⚠️ NOT `wrap_debt_hook.py`: it contains 0 occurrences of `SENTINEL`, `ROOT`, and `wrap-in-progress`, so there is nothing there to make overridable and "all three hooks" was not executable as written. ⚠️ Keep the helper's signature PER-ROOT, not per-session (e.g. `_sentinel_path()`), since per-session filenames are plan B's subject and MUST-PRESERVE forbids changing sentinel semantics here. ⚠️ Set it ONLY in tests and in the canary — never in `runner.py`, `bellows.py`, or `planner.py`. It travels by inheritance exactly as `BELLOWS_DISPATCH` does, so a spawn-side setting would silently redirect every hook's sentinel lookup to the wrong directory and quietly disarm the real lock.

**(F) Repoint the live wiring — the FINAL action of this step, after (A0)–(E) are written AND (G)'s targeted tests are GREEN.** ⚠️ Ordering is load-bearing, not cosmetic: (F) points the CEO's live lock at the new code, so doing it before the tests pass would leave the enforcement layer running unproven code if this step then halts — and with `pause_for_verdict: always` it halts pending a verdict, so that window is not momentary. Write (A0)–(E), run (G), and only then repoint. If (G) is not green, STOP with the repoint NOT done; the live layer stays on the originals and nothing is lost. Update the three hook `command` paths in `~/.claude/settings.json` to the `bellows/hooks/eluvian/` copies.
⚠️ **COMMIT THE DELIVERABLE EARLY** (from diagnostic-495's MUST-PRESERVE): commit as soon as the step's work is complete, before optional extra verification. This plan runs BEFORE any exemption is live, so you can still be debt-injected and Stop-blocked, and a block must never cost the deliverable.

⚠️⚠️ **YOU ARE RUNNING IN A GIT WORKTREE, AND THIS EDIT ESCAPES IT.** Bellows dispatches bellows-targeted plans into `bellows/.bellows-worktrees/<id>/` (measured: plan 495's own step ran with `cwd=/Users/marklehn/Developer/GitHub/bellows/.bellows-worktrees/495`), and that directory is TORN DOWN when the plan finishes, while `settings.json` is machine-global and outlives it. Therefore: **(a)** write ONLY main-checkout paths (`/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/<file>`) — ⚠️ NEVER a path containing `.bellows-worktrees/`, which would point the CEO's hooks at a directory that ceases to exist and break every hook on this machine with no sentinel to delete; grep the final file for `bellows-worktrees` and assert 0. ⚠️ A zero-match `grep -c` PRINTS `0` and EXITS 1, so never `&&`-chain it or read its exit status as the result — capture the printed count (`n=$(grep -c -F bellows-worktrees <file> || true)`) and compare that. Same for `diff`, which exits 1 on any difference including the three lines you EXPECT to differ. **(b)** Assert each target RESOLVES before writing it (`test -f <abs>` for all three) — a missing target means Step 1's commit has not reached the main checkout, so STOP and report rather than repointing at nothing. **(c)** Assert the hooks still FIRE after the repoint — as a SMOKE TEST, not a uuid assertion. ⚠️ A cold seat measured that the uuid form fails DETERMINISTICALLY here: at (F) time the MAIN-checkout files are still Step 1's pristine copies, because your Step-2 edits live in your worktree and the merge happens at TEARDOWN, which under `pause_for_verdict: always` runs before every pause — i.e. per STEP, at the END of this step, after (F) has already run — so the hooks that actually run are the pre-edit ones, which write no session id. Asserting a uuid would HALT a correct execution. Instead prove each wired path RESOLVES AND PARSES **without executing its logic**: for all three, `/usr/bin/python3 -m py_compile <abs-path>` and record the exit status (0 = the file exists and is valid; a non-zero is the missing/broken target you are ruling out). ⚠️⚠️ **Do NOT run `python3 <path> < /dev/null` on the Stop hook.** At (F) time the main-checkout copy is Step 1's PRISTINE code — it has no `ELUVIAN_WRAP_ROOT` support yet, because that is your Step-2 edit still sitting in your worktree — so it resolves the REAL governance root, and on a passing `wrap_check` its `returncode == 0` branch calls `SENTINEL.unlink()`. Executing it would delete the CEO's live sentinel: the exact disarm this plan exists to prevent, performed by its own verification step. `py_compile` answers the same question and runs no `main()`. The uuid-attributable liveness assertion belongs in Step 3, AFTER the merge has landed, where it is earnable.

**(G) Targeted tests — run BEFORE (F).** ⚠️ First, syntax-check every daemon module you edited: `python3 -m py_compile runner.py bellows.py planner.py`, and paste the raw result. The live daemon is executing THIS PLAN through `runner.py`; it holds the already-imported modules in memory so your edit cannot break the run in flight, but a syntax error would make the NEXT dispatch — Step 3, your own QA — fail to spawn at all, which presents as an unexplained step failure rather than as your bug. Then the targeted tests (the full suite runs in QA): create `bellows/tests/test_wrap_hooks.py` covering — exemption on/off for BOTH hooks (guard fires / guard absent), that the exempt branch is taken before any `wrap_check` subprocess runs, that `hooklog` writes the `session_id` — ⚠️ **assert this via a SUBPROCESS invocation of the hook with a real piped stdin payload, not by calling `main()` in-process**: under pytest, stdin is captured and `sys.stdin.read()` raises `OSError`, so an in-process test can only ever observe the no-id placeholder (measured — this was the one failing test when a cold seat wrote the module exactly as specified, and `-s` hangs indefinitely rather than fixing it). ⚠️ Give `hooklog` a `ELUVIAN_HOOKS_LOG` env override defaulting to the current path, so tests write to `tmp_path` instead of polluting the live `~/.claude/eluvian/hooks.log` — a subprocess-driven test cannot redirect it otherwise. Also test and that an UNEXEMPT session still blocks when armed (the lock-intact case, at unit level). Use `ELUVIAN_WRAP_ROOT` (item E) so no test touches the real root.
⚠️⚠️ **THE TESTS RUN INSIDE AN EXEMPT ENVIRONMENT — control `BELLOWS_DISPATCH` explicitly in every test.** After (A), the daemon sets `BELLOWS_DISPATCH=1` on the session that runs these tests, and pytest inherits it, so the guard you add in (B) fires by DEFAULT during the test run: every test asserting that a hook BLOCKS would fail, and every test asserting exemption would pass for the ambient reason rather than the one under test. Add an `autouse` fixture that `monkeypatch.delenv("BELLOWS_DISPATCH", raising=False)` AND ⚠️ **`monkeypatch.setenv("ELUVIAN_WRAP_ROOT", str(tmp_path))` — SET it, never `delenv` it.** Deleting it restores the module default, which IS the CEO's real governance root (measured: the resolved sentinel became `/Users/marklehn/Developer/GitHub/.wrap-in-progress`), and this module also contains a test that ARMS — so a `delenv` fixture would arm and unlink the CEO's real lock on every future full-suite run. `BELLOWS_DISPATCH` is deleted (absence is its safe state); `ELUVIAN_WRAP_ROOT` is set (absence is its dangerous state). Then have each test set what it needs. ⚠️ **Put that fixture in `tests/test_wrap_hooks.py`, NOT in the shared `tests/conftest.py`.** Scoping it to the new module is a deliberate blast-radius choice: a conftest-level env fixture would silently change the environment every existing test in the suite runs under, turning a targeted fix into a suite-wide behavior change that nothing in this plan tests for. If you believe it belongs in conftest, STOP and say so rather than moving it. ⚠️ Without this the file's results depend on WHO runs it — green run by hand, red under the daemon — which is the worst kind of test. This is the [[hot-path-guard-needs-mock-audit]] class: a new environment-reading guard silently changes the meaning of every existing assertion that reaches it. The existing `tests/conftest.py` already establishes the pattern with its autouse `isolate_*` fixtures (verdicts dir, runner logs, lifecycle DB → `tmp_path`); follow it, and note that this same isolation is why running the full suite alongside the live daemon is safe.

**Scope:**
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_stop_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_debt_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_arm_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/runner.py`
- `/Users/marklehn/Developer/GitHub/bellows/bellows.py`
- `/Users/marklehn/Developer/GitHub/bellows/planner.py`
- `/Users/marklehn/Developer/GitHub/bellows/tests/test_wrap_hooks.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/settings-backup-2026-08-21.json`

**Deposits:**
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_stop_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_debt_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_arm_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/runner.py`
- `/Users/marklehn/Developer/GitHub/bellows/bellows.py`
- `/Users/marklehn/Developer/GitHub/bellows/planner.py`
- `/Users/marklehn/Developer/GitHub/bellows/tests/test_wrap_hooks.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/settings-backup-2026-08-21.json`

**Commit:** repo-asserting absolute form, explicit pathspec, add before commit. Your final operation is the commit.

## STEP 3 — QA: full suite + the live exemption canary

**Role:** QA.

**MANDATORY Rule 20 self-check banner** — the deposited QA report MUST contain, verbatim, the heading `## Rule 20 — QA Self-Check Results` and, below it, `**PASSED — SELF-CHECK PASSED**`. Canonical block: `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`. `plan_slug: wrap-hook-plan-a-vendor-and-exemption-2026-08-21`; `qa_report_path: /Users/marklehn/Developer/GitHub/bellows/knowledge/qa/wrap-hook-exemption-qa-2026-08-21.md`; `evidence_dir: /Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/`; `required_evidence_files: [pytest_full.txt, canary.txt]`. FAILED → halt.

1. **Full suite**, foreground, output captured raw to `pytest_full.txt`. Report the counts line verbatim, and compare it against the pre-change baseline Step 1 captured at `knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/pytest_baseline.txt` — quote BOTH counts lines side by side in the report. Any regression versus that baseline is a HALT. ⚠️ **The passed-count will legitimately be HIGHER than the baseline** — both plans ADD test modules — so do not treat a count increase as a discrepancy, and do not "reconcile" the numbers. The check is a REGRESSION check, not an equality check: no test that passed at baseline may now fail or error, and the failure/error counts must not rise. State the comparison in those terms. ⚠️ If `pytest_baseline.txt` is missing, that is itself a HALT: without it "no regression" is unfalsifiable and would pass by default ([[assert-the-post-condition-not-the-artifact]]).
2. **Live exemption canary** — a mocked env cannot prove this. Capture raw output to `canary.txt`. Both assertions must hold:
   - **(i) daemon exempt.** ⚠️ **Split this into two claims, because only one is live-testable inside this plan.** The live daemon imported `runner.py` at startup and never reloads it (pid/start-time are moving facts — read `bellows/.bellows.lock` for the current ones rather than trusting any pinned here; it was restarted at 14:47:07 on 2026-08-21), and `runner.py` contains **0** occurrences of `env=` today — so YOUR session was spawned by the pre-edit runner and `BELLOWS_DISPATCH` is NOT in your environment. Do not assert that it is; the earlier instruction to prove it from your own env was wrong and is withdrawn.
     - **(i-a) THE HOOK HONOURS THE MARKER — live, testable now.** Spawn a child with the marker set explicitly and a KNOWN id: `BELLOWS_DISPATCH=1 ELUVIAN_WRAP_ROOT=<scratch> claude -p "reply OK" --session-id <uuid-you-generate> --max-turns 1`. Assert `~/.claude/eluvian/hooks.log` gains `SessionStart`/`Stop` → `daemon-exempt` carrying THAT uuid. ⚠️ Use `--session-id` (it exists — `claude --help`): a plain `claude -p` prints no session id, so asserting "by id" would be unearnable, and asserting by recency is forbidden because other sessions write to this log concurrently. ⚠️ **Before spawning, `find /Users/marklehn/Developer/GitHub -maxdepth 1 -name '.wrap-in-progress*'` must print NOTHING**, and pin `ELUVIAN_WRAP_ROOT` at a scratch dir for the child — a nested session runs the live hooks, so without both guards the acceptance test for the disarm fix could itself perform a disarm. ⚠️ CREATE that scratch sentinel first (`touch <scratch>/.wrap-in-progress`) — the assertion has no antecedent otherwise and a cold seat measured the scratch root simply empty. Then assert it still exists after the child exits. ⚠️ Append `< /dev/null` to every `claude -p` invocation: without it the command stalls ~3s and writes `Warning: no stdin data received in 3s` into your evidence file.
     - **(i-b) THE DAEMON SETS THE MARKER — live-testable IF, AND ONLY IF, the daemon was restarted at the Step-2 verdict gate.** The daemon imports `runner.py` once at startup and never reloads it, so Step 2's `env=` edit reaches a running daemon only across a restart. This plan is `pause_for_verdict: always`, which makes that a DESIGNED point rather than a caveat: Step 2 ends at a verdict gate, the CEO restarts bellows there, and Step 3 is then dispatched by a daemon carrying the edit. **Decide which case you are in by MEASURING, never by assuming:** run `env | grep -F BELLOWS_DISPATCH` in your own environment.
       - If it prints `BELLOWS_DISPATCH=1`, the restart happened: (i-b) is LIVE-VERIFIED by that line — your own session is the evidence — and you should also quote the `env=` line at each of the three spawn sites.
       - If it prints nothing (`grep` exits 1), the restart did NOT happen. Verify (i-b) by CODE INSPECTION plus the unit tests, and record it in the QA report as **PENDING A DAEMON RESTART** — a CEO action, which MUST-PRESERVE forbids you performing.
       ⚠️ Either outcome is a legitimate, reportable result. Reporting (i-b) as live-verified when the marker is absent from your own environment would be a false attestation; so would silently omitting which case occurred. State the raw `env | grep` output.
   - **(ii) interactive lock INTACT:** ⚠️ **You must explicitly UNSET the marker — `env -u BELLOWS_DISPATCH ...` — and prove it is unset in the captured output.** You are yourself a daemon-dispatched session, so after this fix `BELLOWS_DISPATCH=1` is in YOUR environment and any `claude -p` you spawn INHERITS it. Without the unset you would be testing an exempt session, the hook would allow rather than block, and assertion (ii) would fail for a reason that has nothing to do with the lock being broken — a false HALT that reads as "the fix disabled the lock". With `ELUVIAN_WRAP_ROOT` pointed at a scratch dir and `BELLOWS_DISPATCH` unset, arm a scratch sentinel and assert the Stop hook still returns `decision: block` and still logs `armed-BLOCK`. ⚠️ **Invoke the VENDORED hook SCRIPT — `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_stop_hook.py`, the copy (F) wired live, NOT the stale original under `~/.claude/eluvian/`** (testing the original would pass trivially while certifying pre-migration code) — piping a synthetic Stop payload to its stdin (a JSON object carrying at minimum `session_id`, `hook_event_name`, `cwd` — match the shape diagnostic-495 captured) and read its stdout. ⚠️ **Correction to an earlier draft of this plan, measured by a cold seat: a blocked session does NOT loop forever if you bound it.** With `--max-turns 1` it exits in ~9.4s (`Error: Reached max turns (1)`), logs `armed-BLOCK` against the pinned uuid, and leaves the sentinel intact. So a genuinely live (ii) IS available and is STRONGER evidence than a direct invocation. Prefer the live form — `env -u BELLOWS_DISPATCH ELUVIAN_WRAP_ROOT=<scratch> claude -p "reply OK" --session-id <uuid> --max-turns 1 < /dev/null` — with the scratch sentinel armed beforehand, and fall back to the direct synthetic-payload invocation only if the spawn is unavailable. ⚠️ Whichever you use, NEVER arm the real governance-root sentinel, and confirm `find /Users/marklehn/Developer/GitHub -maxdepth 1 -name '.wrap-in-progress*'` prints nothing first. State in the report which FORM each assertion actually used — live spawn or direct synthetic-payload invocation — and label each accordingly. ⚠️ Do not assume the answer: an earlier draft asserted (ii) was necessarily a direct invocation, which is no longer true now that the live form is available and preferred. Report the form you ran, not the form the plan once expected. ⚠️ Use the scratch root — never arm the real governance-root sentinel. ⚠️ **This assertion silently depends on `wrap_check` exiting NONZERO.** If the four repos happen to be clean at that moment, the hook takes the pass branch — unlink-and-allow — which this plan's own text reads as "the lock is DISABLED, HALT", a false halt. Capture `wrap_check`'s exit code alongside the assertion and state it; if it exits 0, the environment is not suitable for (ii) — say so rather than reporting a failure.
   - ⚠️ **A FAILING canary looks like:** either token absent, OR (ii) allowing instead of blocking. Assertion (ii) failing means the CEO's lock is DISABLED — that is a HALT and must never be read as "the exemption works". State the pass/fail of each assertion separately; a single combined verdict is not acceptable.
3. Confirm `~/.claude/settings.json` now points at the vendored copies. ⚠️ **Do NOT try to `cmp` the backup against "the pre-edit original" — that file no longer exists anywhere except as the backup itself, so the check would be circular and unearnable** ([[earn-the-clean-gate-dont-author-it]]). Assert the two things that ARE checkable: (a) `diff` the backup against the current `settings.json` and show that the ONLY differing lines are the three `eluvian` hook `command` paths — any other delta is a HALT; and (b) both files parse as JSON (`python3 -m json.tool`, raw output for each). Paste both results.

**Scope:**
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/wrap-hook-exemption-qa-2026-08-21.md`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/pytest_full.txt`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/canary.txt`

**Deposits:**
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/wrap-hook-exemption-qa-2026-08-21.md`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/pytest_full.txt`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/wrap-hook-exemption-2026-08-21/canary.txt`

**Commit:** repo-asserting absolute form, explicit pathspec. Your final operation is the commit.
