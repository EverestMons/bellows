# bellows — executable: DISCHARGE THREAD 125'S RULE 102 ENUMERATION — three sites carry thread 116's void headline, and the divergence they describe no longer exists (threads 125 / 122 / 116)

**Date:** 2026-09-06 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** manual_bootstrap | **cycle_tier:** T1 | **Test Scope:** none (record correction only — no code path is edited; the corrected claims are re-derived by a probe deposited as `.txt` evidence, which `qa_test_result` cannot certify: the pre-declared benign gate failure below) | **Execution:** Step 1 (RECORD) | **qa_steps:** none | **pause_for_verdict:** always | **known_failures:** 0

**auto_close:** false

---

## ⛔ WHY THIS IS A PLAN AND NOT AN EDIT

**Rule 102, quoted from `PLANNER_TEMPLATE.md:1419`:**

> A record error observed mid-batch (a doctrine sentence contradicting its source, a stale count, a wrong path form) gets a DECLARATION at the observing plan's open_forks and a corpus entry at the wrap — **never an in-passing patch, which would bypass the non-author gate.** Enumerate every site with its observing plan and finding id; **one fix plan may discharge many sites, and the enumeration is its scope.** Measured: six PT record errors carried by declaration across the route-57 batches, discharged by a single enumerated plan (552) — four corrected, **two closed as verified dissolutions with derivations so they are never re-routed.**

⛔ **The non-author clause binds THIS author specifically.** The claim these sites carry was falsified partly by `775cb9e` — a fix I authored on 2026-09-06. Patching the sites in passing would have me grading my own correction, which is the exact bypass Rule 102 names. The enumeration below is thread 125's, unchanged; this plan does not widen it.

⚠️ **`manual_bootstrap`, and why.** No gate refuses this and no override is recorded — the plan edits records, not code, and the daemon never scans `drafts/`. ⛔ **Cost, stated as a choice:** no `Done/` record and no lifecycle plan id. A later citation uses this file's path.

## How to Run This Plan

```
Read the plan at bellows/knowledge/decisions/drafts/executable-rule102-116-headline.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further and do NOT move the plan to Done.
```

## What this decides

**Nothing new.** It discharges an enumeration already fixed by thread 125 and records two dissolutions with their derivations. ⛔ It does not re-open what `775cb9e` decided, does not touch `gates.py` or `plan_lint.py`, and does not widen the enumeration.

## Why this exists

**THE FALSIFIED CLAIM.** Thread 116: *"NEITHER parser handles `qa_steps: [2]`."* Thread 122 measured that the `plan_lint` half is FALSE — `_parse_qa_steps('[2]') -> {2}`; it strips brackets. 116 was closed SUPERSEDED 2026-09-04. Three sites still assert its headline as live.

⭐ **AND THE SUBJECT HAS SINCE DISSOLVED.** `775cb9e` (2026-09-06) made `gates.parse_qa_steps` the single canonical reader and had `plan_lint._parse_qa_steps` delegate to it. Measured before: **5 of 12** spelling×title combinations diverged. Measured after: **0 of 12.** ⛔ **So two of the three sites are not merely wrong — the thing they describe no longer exists**, which is Rule 102's "verified dissolution" outcome and requires a derivation so they are never re-routed.

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | the enumeration | **3 sites**, thread 125, derived mechanically by grep over both repos | thread 125's body |
| P2 | the void headline | *"NEITHER parser handles `qa_steps: [2]`"* — false as to `plan_lint` | `gates.parse_qa_steps('[2]') -> {2}` |
| P3 | ⛔ **the dissolution** | divergence **5/12 → 0/12** at `775cb9e` | the Item 2 probe |
| P4 | 116's status | `superseded`, 2026-09-04 13:09 | `tuyere.threads show 116` |
| P5 | thread 122 consequence #2 | the `u-qa-predicate-align` banner still states `plan_lint._parse_qa_steps -> set()` as measured fact | read the banner |

## The sites

⛔ **Thread 125's enumeration, verbatim in scope. Site 4 is 122's consequence #2, which 125 deliberately left with 122 so it is not discharged twice — it is folded here because one plan may discharge many sites and splitting it would re-create the double-discharge risk 125 was avoiding.**

| # | site | disposition |
|---|---|---|
| 1 | `bellows/knowledge/decisions/Done/executable-100037.md:16` | **CORRECT** — the entanglement claim |
| 2 | `bellows/knowledge/decisions/Done/executable-100037.md:36` (pin P8) | **CORRECT** — the falsified measurement |
| 3 | `eluvian-governance/shop_next_session.md:257` | **VERIFIED DISSOLUTION** — a baton one-liner |
| 4 | `bellows/knowledge/decisions/drafts/executable-u-qa-predicate-align.md` banner | **CORRECT** — 122 consequence #2 |

⚠️ **Sites 1 and 2 are one SHIPPED plan that contradicts itself** — its P4a cites diagnostic 100036 correctly while P8 and line 16 carry 116's void headline. A reader landing on P8 gets the wrong account with no signal to look at P4a. ⛔ **Corrections are APPENDED as dated notes beside the original text, never rewritten away** — the 100037 record must keep what it shipped.

## What this does NOT do

- ⛔ Does not edit `gates.py`, `plan_lint.py`, or any test.
- ⛔ Does not re-derive thread 102's 74-of-861 counts (122 consequence #3). ⚠️ **Thread 125 states plainly that 100036 never measured check (u) at all, so its Q7 does not void those counts.** That gap keeps its own thread; folding it here would widen the enumeration Rule 102 fixes as the scope.
- ⛔ Does not close threads 122 or 125 — closing happens at the keyboard.
- Does not touch thread 114 or ruling 119.

## Drafting Cycle

**Tier:** T1 — T-6 does NOT fire (edits no doctrine, gate or specialist contract; `shop_next_session.md` is the baton, not doctrine). T-1 fires: two repositories. T-8 not fired: clone by kind of the enumerated-discharge plan 552 that Rule 102 cites as its own precedent.
**Walk register:** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-rule102-116-headline-2026-09-06.md`
**Walks:**
⛔ **v0 — no lens has walked it and no §2.0 direction verdict has been issued.** The five lens lines are added at walk 1, each carrying its own `wN N folded — instruction N / record N` token; ⚠️ a restructuring fold is declared in THIS BLOCK, not only in the register.

**Closing:** ⛔ **NOT CLOSED — v0.**

## Cycle Manifest

*(to be EMITTED at BAR_MET — ⛔ this placeholder must not survive the freeze.)*

---
---

## STEP 1 — RECORD

---

> **FIRST — before any reads or work: post a short visible message to chat (1–2 sentences) confirming you are starting this plan and stating your immediate next action.**
>
> **Scope:**
> - `bellows/knowledge/decisions/Done/executable-100037.md`
> - `bellows/knowledge/decisions/drafts/executable-u-qa-predicate-align.md`
> - `/Users/marklehn/Developer/eluvian-governance/shop_next_session.md`
> - `bellows/knowledge/qa/evidence/rule102-116-headline-2026-09-06/derivation.txt`
>
> **Item 0 — ROOTS.** `GOV=/Users/marklehn/Developer/eluvian-governance`; `test -f "$GOV/GLOSSARY.md"` before proceeding. ⚠️ Hardcoded to this machine; FAILS CLOSED on the Air, which is the safe direction. ⛔ Governance by absolute path with `git -C "$GOV"`, committed by EXPLICIT PATHSPEC; **bellows LAST**.
>
> **Item 1 — RE-DERIVE THE DISSOLUTION BEFORE CORRECTING ANYTHING, and deposit it as `.txt`.** ⛔ A dissolution asserted without a derivation is re-routed by the next reader — Rule 102's own precedent kept derivations for exactly this. Run both readers over every corpus spelling × both step titles and record the divergence count. **Expected: 0 of 12.** ⚠️ **POSITIVE CONTROL, or the zero is unproven:** re-run the same probe against `git show 6998c30:gates.py` (the commit before `775cb9e`) and confirm it reports **5 of 12**. An empty result must be proven, never assumed.
>
> **Item 2 — HALT CONDITION.** ⛔ If Item 1's probe does not reproduce 5/12 before and 0/12 after, the dissolution is not established, sites 1–3 are ordinary corrections rather than dissolutions, and this plan must be re-derived before any edit.
>
> **Item 3 — sites 1 and 2**, `Done/executable-100037.md`. ⛔ **APPEND a dated correction note beside each; do not rewrite the shipped text.** Each note states: the claim as shipped, that thread 116 was superseded by 122 on 2026-09-04, the corrected statement (`plan_lint` parses the bracketed form; `gates` did not), and that `775cb9e` dissolved the divergence entirely. ⚠️ P4a already cites 100036 correctly — the note must point a P8 reader at P4a, which is the signal 125 says is missing.
>
> **Item 4 — site 4**, the `u-qa-predicate-align` banner. Its top line already withdrew the correctness objection on 2026-09-04; the body below still states `plan_lint._parse_qa_steps -> set()` as measured fact. ⛔ **Re-derive it, do not delete it** — 122 consequence #2 asks for re-derivation. ⚠️ The plan's OTHER objection (six `cycle_check` escalations pushed through without a CEO pause) **stands untouched and is sufficient on its own**; this plan must not read as clearing it.
>
> **Item 5 — site 3**, `$GOV/shop_next_session.md:257`. A one-line baton entry. Correct it in place with the dissolution named, since the baton is a live working record rather than a shipped artifact.
>
> **Item 6 — commit.** ⛔ **TWO commits: governance by explicit pathspec FIRST, bellows LAST.** Record `numstat`.
>
> ⚠️ **Pre-declared benign gate failure.** `test_scope` is `none` and this step deposits a `.txt`, so `_gate_qa_test_result` finds no pytest summary and FAILs. ⛔ **Under `manual_bootstrap` no gate actually runs and there is no plan id to override** — stated so a later reader does not attempt the override act (thread 154).
>
> **Deposits:**
> - `bellows/knowledge/decisions/Done/executable-100037.md`
> - `bellows/knowledge/decisions/drafts/executable-u-qa-predicate-align.md`
> - `/Users/marklehn/Developer/eluvian-governance/shop_next_session.md`
> - `bellows/knowledge/qa/evidence/rule102-116-headline-2026-09-06/derivation.txt`
>
> **Post-conditions:** all three of thread 125's sites discharged, plus 122's consequence #2 · the dissolution re-derived with a firing positive control on the pre-fix commit · corrections APPENDED, never rewritten · P8 readers pointed at P4a · the `u-qa-predicate-align` deposit objection left standing · ⛔ **no code edited and the enumeration not widened.**
>
> **STOP. Do NOT proceed further. Do NOT move the plan to Done. Wait for CEO confirmation.**
