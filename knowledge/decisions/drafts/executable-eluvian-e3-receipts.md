# bellows — E3: deposit watcher receipts — receipt store, writer tool, `[2r/receipts]` wrap-check step, session-id plumbing, retirement

**Date:** 2026-08-24 | **Project:** bellows | **Tier:** Medium | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full suite (bellows) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **known_failures:** 0 | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** always

**Depends on:** `knowledge/research/e3-deposit-receipts-design-2026-08-24.md` — sha256 `3fda1d98012679cb4c39cd9a296e4428249736f33f6292284ba764ef35054d9e` — **the DESIGN (diagnostic-515), consumed T-7: every mechanism below is specified there with file:line, and this plan BINDS it — the C-corrections below govern where they and the design differ, the 513 precedent.** Also: `governance/knowledge/research/eluvian-path-audit-2026-08-24.md` §E3 (attestation boundary), `eluvian-path-rulings-2026-08-24.md`. Precedent: `executable-513` (the E2 build from design-511 — same two-step shape; its cold panel found 46/16 HIGH, which is why this plan's freeze convenes the full panel).

## Why this exists

Audit bypass (f): no artifact proves a watcher was armed after a deposit (S9). The design settles how; this plan builds it: a slug-keyed receipt file written by the Planner at deposit time, a writer tool that attests (never claims liveness), a `[2r/receipts]` group in `wrap_check.py`, the session-id argv plumbing the stop/debt hooks owe it, and receipt retirement at plan close. ⚠️ **The wrap lock is LIVE Tier-3 enforcement, and the hook half of this change ACTIVATES AT TEARDOWN-MERGE — the first wrap it judges is the depositing session's own.** Containment is designed in: the step blocks only on attributable facts, degrades to named SKIP/WARNING everywhere else, and never leans on the outer FAIL-OPEN.

## Design corrections (build-time, verified against the merged tree — these govern)

- **C-1 — the D-3 window anchor is VACUOUS; corrected semantics.** The design anchors gap detection on "clearances whose `cleared_at` falls within the session window (from the wrap sentinel's creation time to now)" — but the sentinel is created when `/wrap` is typed, and every deposit precedes it by construction (measured this session: clearance 16:46:37, wrap hours later). That window can never contain a deposit clearance; the gap arm as designed detects nothing. **Corrected `[2r/receipts]` semantics:** (a) **BLOCKING arm — attributable only:** every receipt in `receipts/` with `session_id == argv id` must match a clearance row OR a `hold-*.hold.json` sidecar. ⚠️ **The clearance match is the step's OWN query — `SELECT 1 FROM clearances WHERE content_hash = ?` with NO `consumed_at` filter: a consumed clearance (= claimed plan) still proves the deposit. `lifecycle.has_clearance` (lifecycle.py:217-222) is the WRONG reuse — it filters `consumed_at IS NULL` for the admission gate, and cloning it here false-blocks every wrap whose deposits were claimed (they all are, within seconds).** The sidecar match predicate: the receipt's `slug` equals the sidecar filename minus its `hold-` prefix and `.hold.json` suffix; an own-session receipt matching nothing is a **failure** (`[2r/receipts] N receipt(s) from this session match no clearance or hold — stale, mistyped, or abandoned deposit. If the deposit was deliberately abandoned, remove the receipt file — that is the sanctioned disarm.`) — **with a pending-evaluation grace: a matchless own receipt whose `armed_at` is younger than 10 minutes prints a pending note instead of failing** (the ordering contract writes the receipt BEFORE the depositor evaluates, so a wrap racing a fresh deposit would otherwise false-block — the trap class again). (b) **WARNING arm — global, non-blocking:** clearances with `cleared_at` in a trailing 24h lookback — **consumed and unconsumed rows alike: consumption means the daemon claimed the plan, which is still a deposit that needed arming; a `WHERE consumed_at IS NULL` filter here would exclude nearly every real deposit** — that match NO receipt from ANY session print `[2r/receipts] WARNING: N cleared deposit(s) in the last 24h without a receipt — arm a watcher and write a receipt at every deposit.` — never a failure (anti-foreign-block, design D-4). (c) The honest boundary, restated in the step's own output when the warning arm fires: a receipt-less deposit cannot be attributed or blocked; the narrowing is ritual + visibility, per the audit's armed-and-attested ceiling. **The boundary includes malformation (S3-8): a field-corrupted own receipt degrades to a WARNING, so corrupting a receipt is effectively a second disarm path — equivalent in effect to the sanctioned deletion, and always visible in the step's output; the README states both.**
- **C-2 — the wrap_check env posture moved under the plan's feet.** Design D-5's "the file remains structurally incapable of env reads (no `import os`)" is superseded by origin commit `75cc1b4` (merged at `246d89c`): `wrap_check.py` NOW imports os (:32) and resolves `ROOT`/`MEMORY` via `ELUVIAN_WRAP_ROOT`/`ELUVIAN_WRAP_MEMORY`. The design's sequencing claim held exactly as written: `RECEIPTS = BELLOWS / "receipts"` derives from `BELLOWS` ← `ROOT` and inherits the override automatically. **This plan follows the file's NEW idiom, adds NO new env variables, and derives every new path from the existing constants.**
- **C-3 — the retirement anchor.** Design gap row 11 guessed "bellows.py :1201-1208 or equivalent". Measured close transitions: `lifecycle.mark_plan_state(plan_id, "closed", …)` at **bellows.py:1260 and :2582**; halted transitions exist in the same bookkeeping class. Retirement is ONE helper `_retire_receipts(slug)` (move `receipts/receipt-<slug>-*.json` → `receipts/archived/`), called from the close/halt bookkeeping at both sites, **fail-toward-WARN** — a receipts error must never break a plan close. Anchor by the `mark_plan_state` calls, never by line number.

## Numbers discipline

⚠️ **Measured 2026-08-24 against bellows `246d89c` (the merge that includes 75cc1b4); RE-DERIVE each — yours supersede and you say so.**

| id | pin | value | probe |
|---|---|---|---|
| X1 | design doc sha256 | `3fda1d98012679cb4c39cd9a296e4428249736f33f6292284ba764ef35054d9e` | HALT on mismatch — the spec moved |
| X2 | target blob SHA-1s BEFORE | wrap_check.py `37c453ba7173…`, wrap_stop_hook.py `0564bc6811a9…`, wrap_debt_hook.py `75e769438342…`, bellows.py `3d162ac2b082…` | `git hash-object` in YOUR worktree; HALT on mismatch — wrap_check.py is SHARED SUBSTRATE and a drift means another arc landed |
| X3 | **`T`** — tests collected BEFORE | **1288** | `python3 -m pytest tests/ --collect-only -q` tail; suite GREEN at baseline (known_failures 0) |
| X4 | failability proofs (all ABSENT) | `receipts/` **absent**; `tools/deposit_receipt.py` **absent**; `tests/test_deposit_receipt.py`, `tests/test_wrap_receipts.py` **absent** | positive control, same instrument: `tools/clear_plan.py` **present** |
| X5 | wrap_check.py post-merge posture | `import os` count **1** (:32); `sys.argv` count **0** | the argv gap is still real (G2's session-id half); the env half of the design's G2 is superseded per C-2 |
| X6 | `mark_plan_state` terminal sites | closed :1260, :2582 · halted :2549, :2611 · (**:893 is `in_progress` — EXCLUDE**) | `grep -n -F 'mark_plan_state' bellows.py` — five sites total; retirement hooks the four TERMINAL ones; anchored by the call + state literal, not the number |
| X7 | wrap test surfaces | **20 + 28** | `grep -cE '^def test\|^    def test'` test_wrap_hooks.py / test_wrap_sentinel.py — must pass unchanged |

## Drafting Cycle
**Tier:** T2 computed — the wrap lock is live-guard enforcement (T-5/T-6-adjacent); T-7 fires (builds from the 515 design). **Cold panel: MANDATED at the freeze (full form, four seats)** — the E-family build precedent (E2: 46/16 HIGH, non-decaying) plus the hook half's activate-at-merge blast radius.
**Walk register:** `governance/knowledge/research/walk-register-executable-eluvian-e3.md`
**Walks:** walk 0 pinned; walks 1–n OWED — five lenses each, sequential, v2.13 auto-advance, cycle_check branched after each walk. This line is rewritten at the close from the register's actual rows, never ahead of them.
**Direction verdict (after walk 1):** owed.
**Conformance (§5):** owed per lens; recorded at the close from actual runs.
**Closing:** owed. The deposit travels the lane: ready- staging → depositor gates → **expected HOLD `shop-infra` (writes bellows code) — by construction, not a failure** → release ONLY via `tools/clear_plan.py` (manual rename is INERT under the live flip) → re-evaluation → clear → claim. ⚠️ This will be the clear tool's first LIVE happy-path release.

## Cycle Manifest
tier: T2
target: hooks/eluvian/wrap_check.py
class: shop-infra
reads: /Users/marklehn/Developer/GitHub/bellows/knowledge/research/e3-deposit-receipts-design-2026-08-24.md, /Users/marklehn/Developer/GitHub/bellows/config.json, /Users/marklehn/Developer/GitHub/governance/knowledge/research/eluvian-path-audit-2026-08-24.md, /Users/marklehn/Developer/GitHub/governance/knowledge/research/eluvian-path-rulings-2026-08-24.md
writes: hooks/eluvian/wrap_check.py, hooks/eluvian/wrap_stop_hook.py, hooks/eluvian/wrap_debt_hook.py, tools/deposit_receipt.py, tools/clear_plan.py, bellows.py, receipts/README.md, tests/test_deposit_receipt.py, tests/test_wrap_receipts.py, knowledge/research/e3-qa-2026-08-24.md, knowledge/research/pytest_full.txt
open_forks: (1) a depositor-side `no_receipt` HOLD would make the receipt structural at admission — a NEW ruling beyond the audit's E3 scope (wrap_check as reader); listed for the CEO, not built; (2) design gap rows 10 (ELUVIAN_PATH.md Stage 5 line) and 12 (portability-census row) are ROOT-repo doc edits routed to a small follow-up plan, not this dispatch; (3) E5 consumes the session-id interface built here; (4) ⚠️ CEO-SURFACED: the panel measured that E2 shipped with NO terminating release for `class:shop-infra` holds (depositor holds unconditionally; the tool's rename-to-ready loops back; the DDL's `cleared_by='clear_tool'` arm is dead code) — this plan repairs it per fork 2's letter (tool re-runs cycle_check + plan_lint, writes the clearance as the human's deliberate act), and the repair's bootstrap requires ONE declared manual patch of tools/clear_plan.py in the live tree before this deposit can itself be released, superseded by this plan's authored version — the 513 "final sanctioned bypass, retired by the plan it ships" shape; (5) warning-arm visibility boundary: on a CLEAN wrap (rc 0) both hook consumers discard stdout, so warnings surface only in manual wrap_check runs and failing wraps — accepted under the attestation ceiling, stated in Q4 and README
walks: 0
yields: none
validation: pending
coherence: N/A
N/A

## MUST-PRESERVE

- ⚠️⚠️ **ACTIVATION IS SPLIT, and the hook half activates AT MERGE.** wrap_check/stop/debt changes go live for every subsequent wrap the moment teardown merges — including the wrap that closes the depositing session, whose own deposits (clearances 515/516) HAVE NO RECEIPTS (the tool did not exist when they were made). Under C-1 those produce at most the WARNING arm, never a block. **QA proves this blast-radius arm explicitly.** The daemon half (retirement in bellows.py) stays inert until the next restart.
- ⚠️⚠️ **THE `[2r/receipts]` STEP NEVER LEANS ON THE OUTER FAIL-OPEN.** `check()` has no try of its own; an unhandled exception exits 0 and silently allows the ENTIRE wrap (design D-3, wrap_check.py main try). Every error class — malformed receipt JSON, unreadable receipts dir, unreadable lifecycle.db — is handled inside the step to a named message or printed SKIP. Test each arm.
- ⚠️⚠️ **BLOCK ONLY ON ATTRIBUTABLE FACTS.** Own-session receipts matching nothing → failure. Everything unattributable → warning or SKIP (C-1; anti-foreign-block, design D-4).
- ⚠️ **Attestation says "armed", never "alive"** — tool output, receipt JSON `watcher` field, and the embedded `attestation_boundary` sentence (design D-2). Any liveness claim is a defect.
- ⚠️ **Receipts key by SLUG** (`receipt-<slug>-<session_id>.json`); session id shape `[A-Za-z0-9-]+`; hash = sha256 over raw `read_bytes` (design G4) — cross-checkable against `clearances.content_hash`.
- ⚠️ **wrap_check.py is SHARED SUBSTRATE** (portability arc + E5): follow the post-75cc1b4 idiom, derive `RECEIPTS = BELLOWS / "receipts"`, add no new env variables, keep every existing group's text byte-identical except the additions.
- ⚠️ **Worktree + test isolation:** all writes project-relative in YOUR cwd; every test uses tmp dirs AND tmp DBs — never the real `receipts/`, never the real `lifecycle.db`, never a real watched `decisions/` path (a claimable-looking file in a live watched dir summons the RUNNING daemon mid-test).
- ⚠️ **EVERY DATE IS A FIXED LITERAL.** **`grep` is ugrep: `-F` for literals.**
- ⚠️ **DEV runs TARGETED tests only; the full suite belongs to QA.**

## STEP 1 — DEV: receipts substrate, wrap-check step, plumbing, retirement

**Role:** DEV. `<id>` from your plan filename.

**A0 — preconditions.** Assert X1–X7 (X1/X2 HALT on mismatch; X6 relocates by anchor). Three-way start: pins as stated → proceed; the substrate already present (receipts/ + tool + `[2r/receipts]` in wrap_check) → ALREADY APPLIED no-op success; else partial → STOP with inventory.

**A1 — implement, the design as corrected governing:**
- `receipts/README.md` (creates the directory in git): states the contract — slug-keyed receipt files, the attestation boundary verbatim, retirement to `archived/` on plan close, **and how the depositing session obtains its own session id** (it is the id the wrap hooks receive as `session_id` — the same UUID that names the session's scratchpad directory; a WRONG id does not trap: the blocking arm never sees the receipt and the deposit surfaces in the warning arm instead).
- `tools/deposit_receipt.py` (design D-2): args plan-path + session-id; derives slug (strip `ready-`/`.md`) and hash (raw `read_bytes`); validations — **the plan file may live at ANY path (the ritual runs the tool against the PRE-STAGING file — see the ordering contract below); location is INFORMATIONAL ONLY, never a refusal or a WARN-vs-refusal fork: the tool prints the resolved path and a note when it lies outside every watched project tree, and proceeds either way** (S3-2 measured the prior two-reading contradiction, aggravated by bellows' drafts dir prefix-matching its watched decisions dir; the hash is content-derived and the slug filename-derived, so the location carries no contract), session id shape-valid, no existing receipt for same slug+hash **in the ACTIVE `receipts/` dir only — `archived/` is excluded from the duplicate check** (a legitimate re-deposit after a close must not be refused by its predecessor's retired receipt); refuse loudly; a NEW hash under the same slug writes a NEW receipt; writes `receipts/receipt-<slug>-<session_id>-<hash12>.json` — **the first 12 hex of the content hash in the NAME (S3-3: a slug+session-only name lets a same-session corrective re-deposit silently OVERWRITE the prior attestation; with the hash in the name, the duplicate check is plain file-existence and overwrite is impossible by construction)** — creating `receipts/` on demand with the design's exact content shape incl. `attestation_boundary`; prints `Receipt written: <abs path> — watcher armed (not a liveness claim)`; all refusals to stderr with the design's messages; resolves the bellows root from `__file__`, never a hardcoded absolute path. ⚠️ **The ordering contract, HARDENED by S2-2: the receipt is written BEFORE the plan exists under ANY `ready-` or claimable name.** The depositor evaluates `ready-` files event-driven within seconds, and auto-clearing classes are cleared, claimed, and renamed id-canonical before a post-staging tool run could see them — receipt-after-staging reliably attests only HELD plans, the S9 gap wearing a new name. The README documents the ritual: (1) tool against the DRAFT-path file (same bytes that will be staged — hash equality is the proof), (2) then stage as `ready-<slug>.md`.
- `hooks/eluvian/wrap_check.py`: `check(session_id: str | None = None)`; `main()` parses `sys.argv[1]` (empty → None); new `[2r/receipts]` group appended after step 4, implementing **C-1's corrected semantics** — blocking arm (own receipts vs clearances/hold-sidecars), warning arm (24h lookback, any-session), SKIP arms — **scoped to the BLOCKING arm only: with no session id, the blocking arm SKIPs but the WARNING arm still runs (it is session-independent by construction; a whole-group SKIP would make warnings unreachable in exactly the manual runs Q4(c) names as their surface — S3-1)** — plus `receipts/ absent or unreadable` and `lifecycle.db unreadable` (each with its named printed line), every arm's errors handled in-step **including FIELD validation: a syntactically-valid receipt JSON with missing or unparseable fields (`armed_at` above all) is a per-receipt WARNING skip, never a raised `ValueError`/`TypeError` reaching the outer catch. Timestamp convention pinned: NAIVE LOCAL `datetime.now().isoformat()` — the writer stamps it, the grace comparison parses it, both naive; an aware-vs-naive comparison raises and must be impossible by construction.** ⚠️ **Channel discipline: ONLY the blocking arm appends to the `fails` list; warnings and SKIP notes `print()` directly from the step and never enter `fails`** — a warning that lands in `fails` blocks the wrap and violates C-1. **Hold-sidecar location:** mirror `project_done_dirs()`'s existing idiom — `ROOT.glob("*/knowledge/decisions/*.hold.json")` (bellows is a direct child of ROOT, so its decisions dir is already covered) — never a config.json read (wrap_check has none today; keep it that way). **The warning arm consults BOTH `receipts/` and `receipts/archived/`** — a retired receipt still proves its deposit was armed, and ignoring archived/ makes every closed-before-wrap plan a false warning. lifecycle.db opened read-only (`sqlite3.connect(f"file:{path}?mode=ro", uri=True)`). **Commit-discipline mechanism (S3-9): the existing `[2/bellows]` group gains a second porcelain pathspec — `porcelain(BELLOWS, "receipts")` — so uncommitted receipts fail the wrap exactly as uncommitted verdicts do; this is the enforcement the design's "committed at wrap step [2]" sentence lacked.**
- `hooks/eluvian/wrap_stop_hook.py` :207-210 → `[sys.executable, str(CHECK), session_id or ""]`.
- `hooks/eluvian/wrap_debt_hook.py` :82-85 (the `subprocess.run` call) → same argv addition, **with the sentinel normalized: `_parse_session_id` returns the literal `"unknown"` (never empty, :55-65), which is shape-valid and would masquerade as a real id — pass `"" if session_id == "unknown" else session_id` — and shape-validated before passing (the debt hook, unlike the stop hook, validates nothing today — S2-8; add the stop hook's `[A-Za-z0-9-]+` check so the E5-inherited channel carries only validated ids)** (design D-3 SessionStart arm: a new session's id matches no deposits — the step reports OK/SKIP, never debt).
- `tools/clear_plan.py` — **the S1-4 repair: the class-hold release arm.** Panel-measured E2 residual: depositor.py holds `shop-infra` UNCONDITIONALLY (the `if assigned_class == "shop-infra": self._hold(...)` branch), and the tool's rename-to-ready re-entry loops straight back into the same hold — under the live flip NO shop-infra plan can ever dispatch, and the DDL's `cleared_by IN ('depositor','clear_tool')` arm is written by NOTHING. The repair honors fork 2's letter ("the gated clear tool, which re-runs depositor gates"): the release arm is an EXPLICIT FLAG — `--release-class-hold` — because the hold-reason string is fragile: a daemon restart rewrites every sidecar's reason to `held_pending_ceo_release` (depositor.py:225-227, full-JSON rewrite, S2-4), so keying on `class:shop-infra` alone strands restart-survivors. With the flag, the tool — run by a human as the deliberate release act fork 4 demands — re-runs `scripts/cycle_check.py` (BAR_MET required) and `scripts/plan_lint.py` (**0 NON-BENIGN FAIL — the depositor's own benign filter, depositor.py:494-503; a bare 0-FAIL bar is STRICTER than the gate it re-runs and re-strands benign-FAIL plans, S2-3**) as subprocesses against the held file, then writes the clearance via `lifecycle.write_clearance(basename, hash, class, cleared_by='clear_tool')` — **the class value read from the held plan's own Cycle Manifest `class:` line (the same line the depositor cross-checks); absent or unparseable → refuse, never guess (S3-6)** — — ⚠️ **the stored plan_path is the BASENAME claimable name: every consumer keys on basename (bellows.py:861, :2059, the mint-time consume), and an absolute path makes `is_claimable` False → silent re-HOLD `no_clearance` (S2-5)** — renames `hold-` → the bare claimable name AND removes the `.hold.json` sidecar (both existing paths do, clear_plan.py:35; an orphaned sidecar keeps satisfying `[2r]`'s sidecar-match forever, S2-6); any gate failure → refuse loudly, file left held. All other hold reasons keep the existing rename-to-ready re-entry unchanged. Residuals, stated: (i) the tool does not re-run the depositor's collision/disk checks — the daemon's claim-time re-check still gates hash and clearance; (ii) the clearance INSERT is the ONE sanctioned out-of-daemon lifecycle.db write — human-invoked, single-row, its own short connection — the `cleared_by='clear_tool'` enum arm was built for exactly this and is dead code today; both documented in the tool's output and README.
- `bellows.py`: `_retire_receipts(plan_id)` per **C-3** — ⚠️ **the SLUG is NOT in scope at the terminal sites: the claim rebinds every name to id-canonical (`in-progress-<type>-<id>.md`, bellows.py:887-900), so the helper takes the PLAN ID and looks up `plans.deposit_placeholder_name` from lifecycle.db, strips the `.md` suffix, globs `receipts/receipt-<that-slug>-*.json` **and then filters candidates by JSON `slug` EQUALITY — the glob alone over-matches prefix-extending slugs (S3-4, executed: closing `executable-foo` archived `executable-foo-2`'s receipt — silent de-attestation of an in-flight sibling)** (S2-1 — a filename-derived glob retires nothing, forever, hidden by the WARN). Moves matches → `receipts/archived/`, whole-body try to WARN log; called at the four X6 terminal sites. ⚠️ **The receipts path derives from the daemon's own root resolution (the same mechanism bellows.py already uses for its repo-local paths), never from cwd** — the daemon's cwd is not contractually the repo root.

**A2 — targeted tests:**
- `tests/test_deposit_receipt.py`: write round-trip (all fields, boundary sentence present); refusal arms — missing plan, unwatched dir, invalid/missing session id, duplicate slug+hash; new-hash-new-receipt; receipts dir auto-created.
- `tests/test_wrap_receipts.py`: `[2r/receipts]` pass (own receipts all matched); blocking arm fires on an own-session receipt matching nothing **whose `armed_at` is aged past the grace window; the pending-evaluation grace — an identical matchless receipt with fresh `armed_at` produces a note, not a failure**; warning arm on a receipt-less clearance (non-blocking — `check()` returns no failure for it); SKIP arms (no session id / no dir / unreadable DB); malformed receipt JSON → warning, no exception; **anti-foreign-block: a foreign session's receipt-less state never adds a failure**; **the blast-radius arm: clearances present + receipts empty + session id given → warnings only, zero failures**; `_retire_receipts` retires by the plans-row `deposit_placeholder_name` lookup (a tmp DB row with an id-canonical filename on disk proves the S2-1 path), moves matching files, ignores missing, never raises; **the clear-tool release arm (tmp tree + tmp DB): `--release-class-hold` with passing gates → clearance row `cleared_by='clear_tool'` storing the BASENAME + bare rename + sidecar REMOVED — and the same flag works on a `held_pending_ceo_release` sidecar (the restart-rewrite case, S2-4); a failing gate → refused, still held, sidecar intact; NO flag → old rename-to-ready path byte-unchanged; the lint bar test: a benign-only-FAIL plan passes the arm (S2-3)**; the tool accepts a draft-path (non-watched-dir) plan file, printing the location note (S3-2's corrected contract); **the release-arm test fixture uses BULLETED lens lines — cycle_check silently ignores non-bulleted Drafting Cycle lens lines and returns CONTINUE (S3-10, measured), the exact trap the fixture author will otherwise hit**; the retirement test includes the prefix-extending-slug sibling (S3-4: `executable-foo` close must NOT archive `executable-foo-2`'s receipt); the overwrite test: same slug+session, new hash → SECOND receipt file, first intact (S3-3). ⚠️ ALL in tmp dirs/DBs.

**A3 — verify before committing:** new tests green (paste raw); `py_compile` **every .py file this step changes — six source (wrap_check, wrap_stop_hook, wrap_debt_hook, deposit_receipt, clear_plan, bellows.py) plus the two test files (S3-7)**; run `python3 hooks/eluvian/wrap_check.py` (no argv) from the worktree and paste the `[2r/receipts]` line — it must print the SKIP note (read the CHANNEL: other groups may legitimately fail mid-session; you are asserting only the new line's presence and form).

**A4 — commit** (worktree): `git add receipts/README.md tools/deposit_receipt.py tools/clear_plan.py hooks/eluvian/wrap_check.py hooks/eluvian/wrap_stop_hook.py hooks/eluvian/wrap_debt_hook.py bellows.py tests/test_deposit_receipt.py tests/test_wrap_receipts.py && git commit -m "[<id>] E3 receipts: store+tool+[2r] wrap-check step+argv plumbing+retirement+clear-tool class release (hook half live at merge; daemon half inert until restart)"`

⚠️ **IF ANY A3 CHECK FAILS: no commit, no revert, no retry — leave the worktree as evidence, report, raise `### Flags for CEO`.**

**Deposits:**
- `/Users/marklehn/Developer/GitHub/bellows/receipts/README.md`
- `/Users/marklehn/Developer/GitHub/bellows/tools/deposit_receipt.py`
- `/Users/marklehn/Developer/GitHub/bellows/tools/clear_plan.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_check.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_stop_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_debt_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/bellows.py`
- `/Users/marklehn/Developer/GitHub/bellows/tests/test_deposit_receipt.py`
- `/Users/marklehn/Developer/GitHub/bellows/tests/test_wrap_receipts.py`

**Scope:**
- `/Users/marklehn/Developer/GitHub/bellows/receipts/README.md`
- `/Users/marklehn/Developer/GitHub/bellows/tools/deposit_receipt.py`
- `/Users/marklehn/Developer/GitHub/bellows/tools/clear_plan.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_check.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_stop_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/hooks/eluvian/wrap_debt_hook.py`
- `/Users/marklehn/Developer/GitHub/bellows/bellows.py`
- `/Users/marklehn/Developer/GitHub/bellows/tests/test_deposit_receipt.py`
- `/Users/marklehn/Developer/GitHub/bellows/tests/test_wrap_receipts.py`

## STEP 2 — QA

**Role:** QA. ⚠️ Fresh agent: re-measure; the DEV report is not evidence.

**Q1 — full suite.** `python3 -m pytest tests/ -q`; deposit RAW output as `pytest_full.txt`. Baseline **1288 collected, green, known_failures 0**; the count grows by the two new files; assert zero failures/errors, report the total. The hot-path-guard lesson applies: the argv change touches every wrap invocation — name and diagnose every failure before classification.
**Q2 — change-set vs the design's gap table AS CORRECTED:** rows 1–9 and 11 present at their sites (C-3's anchors for row 11, X6's four terminal sites); **plus the panel-added row: the clear-tool class-release arm in tools/clear_plan.py (S1-4), present with both gates subprocess-run and the clearance write;** **rows 10 and 12 verifiably ABSENT — `git diff` shows NO edit to any file outside the bellows repo** (both are root-repo doc edits routed to a follow-up plan, open_forks (2)); every existing wrap_check group's message literal survives unchanged — `grep -cF` each of the six existing bracket literals (`[1/project]`, `[2/bellows]`, `[3/root]`, `[3b/lessons]`, `[4/memory]`, and the `wrap_check: OK` line) in the new file and assert each count ≥ its old value (the signature and argv additions are expected `-`/`+` lines; the assertion is on the message strings, not on a diff-free file); `is_runnable_plan`/claim path untouched.
**Q3 — behavioral spot-probes on tmp environments:** the full ritual sim — tmp plan file → tool writes receipt → tmp lifecycle DB with a matching clearance → `wrap_check`-equivalent step invocation with the session id → pass; delete the receipt → warning arm only (no failure); corrupt the receipt's hash **AND back-date its `armed_at` past the grace window (S3-5, executed: a fresh corrupted receipt reads as `pending`, rc 0 — the probe cannot fire inside the grace)** → blocking arm fires; foreign-session receipt-less clearance → warning only.
**Q4 — activation scoping, stated honestly:** (a) hook half goes LIVE AT TEARDOWN-MERGE, which happens AFTER this QA step — so QA's proof is from the WORKTREE: paste the no-argv run's `[2r/receipts]` SKIP line and a with-argv run's output from YOUR tree, and state plainly that the merged-tree proof is the depositing session's own next wrap (its clearances 515/516 are receipt-less → the warning arm, never a block — the A2 blast-radius test is the pre-proof); (b) daemon half (retirement) INERT until restart — the running daemon predates this change; state the daemon PID and its `ps -o lstart=` start time; (c) the warning arm's VISIBILITY boundary stated plainly (S1-3): on a clean wrap (rc 0) both hook consumers discard stdout, so warnings reach an operator only via manual `wrap_check.py` runs or a wrap that fails on other grounds — accepted under the attestation ceiling, and the README says so.

> **QA SELF-CHECK — Rule 20.** Post the block from `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` verbatim, under the banner **`Rule 20 — QA Self-Check Results`**, and close with **`PASSED — SELF-CHECK PASSED`** only if every check genuinely passed. ⚠️ Both literals are matched by `plan_lint` check (c) and neither may be paraphrased. Rule 20 block + Q1–Q4 results in the `.md`; raw suite output in `pytest_full.txt` — the two QA gates scan DIFFERENT extensions.

**Deposits:**
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/research/e3-qa-2026-08-24.md`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/research/pytest_full.txt`

**Scope:**
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/research/e3-qa-2026-08-24.md`
- `/Users/marklehn/Developer/GitHub/bellows/knowledge/research/pytest_full.txt`

**Commit:** `git add knowledge/research/e3-qa-2026-08-24.md knowledge/research/pytest_full.txt && git commit -m "[<id>] qa: E3 receipts — full suite + evidence"`

## Deposit ritual

Stage as `ready-executable-eluvian-e3-receipts.md`. **Expected depositor outcome under the LIVE flip: HOLD `class:shop-infra` — by construction, not a failure.** ⚠️ The panel measured that the SHIPPED clear tool cannot terminate a class hold (open fork 4) — release therefore requires the BOOTSTRAP, declared loudly: the depositing session applies the class-release arm to `tools/clear_plan.py` in the live tree as a ONE-TIME manual patch (small, non-daemon CLI code, superseded byte-for-byte-in-intent by this plan's authored version — the 513 "final sanctioned bypass, retired by the very plan it releases" shape), then runs `python3 tools/clear_plan.py --release-class-hold <hold-file>`: the tool re-runs cycle_check (BAR_MET) + plan_lint (0 NON-benign FAIL, the depositor's own filter), writes the clearance `cleared_by='clear_tool'` with the BASENAME claimable name, renames to the bare claimable name, removes the sidecar, and the daemon's claim-time re-check does the rest. Manual rename remains INERT. The depositing session arms a slug-keyed watcher BEFORE staging, per the very contract this plan builds.
