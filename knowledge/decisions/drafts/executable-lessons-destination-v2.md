# bellows — executable: DOCTRINE v2 — the lessons-destination ruling reaches its four carriers, with a PER-SURFACE re-entry discriminator, a rooted corpus guard, and the 2026-08-18 test retired by an appended correcting entry (thread 91, second rebuild — authored FROM a 52-finding cold panel)

**Date:** 2026-09-04 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** targeted (`tests/test_wrap_sentinel.py` — ⛔ `wrap.md`'s CONTENT **is** read by `TestWrapMdClauses`; the predecessor asserted no code path reads these files and was falsified twice) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0

**auto_close:** false

**Depends on:** tuyere thread 91 (the ruling of 2026-09-02, confirmed 2026-09-03). ⛔ **Authored from `drafts/executable-lessons-destination-doctrine.md`, which took a RE-DRAFT verdict on 2026-09-04 — read it as EVIDENCE, never as a clone origin.** Its cold panel (DISCOVERY 18 · EXECUTION 16 · CAPSTONE 18 = **52 findings**) is this plan's input, recorded in `governance/knowledge/research/walk-register-lessons-destination-doctrine-2026-09-03.md`. Machinery parent: `Done/executable-100027.md`; newest same-class: `Done/executable-100026.md`.

## ⛔ What the panel proved, and what this v0 does differently

| # | the predecessor's defect, MEASURED | what v2 does |
|---|---|---|
| 1 | **The A0 ladder discriminated the GOVERNANCE half only.** All three arms read `PLANNER_TEMPLATE.md`; Items 5–6 edited bellows files with no discriminator, and **both of Item 5's anchors were consumed by its own edit**. Re-run on an already-edited copy it produced a doubled, truncated 220-char sentence in a LIVE-AT-WRITE file, with the gate co-tenant intact so `[3b/lessons]` still passed (S2-15) | **A per-surface re-entry table — one row per edited file, each with a POST-STATE token the edit CREATES** (never one it consumes) |
| 2 | **The RE-ENTRY arm was unreachable** — it tested for a plan id no Item wrote; the plan's own half-landed state reported NONE-MATCH, *"a foreign editor is live"*, which is the predecessor failure the ladder existed to fix (S2-3). And a fourth state existed: arm 2 inferred "governance landed" from the template alone and skipped the `LESSONS.md` append — reachable through the plan's OWN guard-(a) HOLD (S3-8/S3-9) | Each row's token is **written by a named Item**, and `LESSONS.md` gets its own row so its append cannot be skipped by inference |
| 3 | **`_assign_class` fell back to `app-feature`, which AUTO-CLEARS** — a four-file two-repo doctrine edit would have dispatched with no human release act (S1-5) | Deposits carry all four doctrine paths; **P-CLASS states the runnable recipe including `project_root`, whose default is the dangerous answer** (S2-10) |
| 4 | **guard (a) enumerated ONE of TWELVE `decisions/` lanes**, missing `forge_lessons` — the repo that INGESTS `LESSONS.md` and is daemon-watched (S2-11) | The guard is **rooted and enumerates all twelve**, naming the three watched |
| 5 | **`pending` is a Rule 20 hedging keyword** — a compliant QA receipt FAILED the canonical self-check, and Step 2 forbids closure on FAILED (S2-9) | QA **DESCRIBES** the marker, never quotes it |
| 6 | **Five pin commands could not resolve from the DEV cwd**, and `git blame` failed with **rc=0** so an `&&` chain read silence as success (S2-8) | Every pin command is `$GOV`-rooted; **each asserts non-empty OUTPUT, never exit status** |
| 7 | **The Cycle Log carried no per-lens lines**, so `cycle_check` could not see a declared restructuring fold and returned the WRONG escalation (S3-3) | The lens block is present from v0, and **restructuring is declared in the BODY** where the tool reads it |

## Why this exists

**The ruling.** CEO 2026-09-02: lessons go to the corpus unless they cannot be codified. Confirmed 2026-09-03, explicitly SUPERSEDING the 2026-08-18 test.

⚠️ **The licensing evidence STRENGTHENED during the failed cycle.** On 2026-09-03 a Planner routed a working-pattern lesson to memory, following `wrap.md`, a day after the ruling. On **2026-09-04 a different session followed the RULING instead** and sent all three of its classes to the corpus. ⛔ **Compliance is now AUTHOR-dependent rather than DOCUMENT-dependent — worse than consistent non-compliance, because the corpus acquires both behaviours and no reader can tell which is correct.**

| # | pin | value | how to re-derive (⛔ every command `$GOV`-rooted; assert non-empty OUTPUT, never `rc`) |
|---|---|---|---|
| P1 | carrier 1 — the template | `PLANNER_TEMPLATE.md:2094`, Session Wrap step 7 | `grep -nF -- "memory repo" "$GOV/PLANNER_TEMPLATE.md"` |
| P2 | ⛔ **`memory repo` sits TWICE on line 2094** | col **2183** (historical narrative — DO NOT TOUCH) and col **2576** (`…go to the Planner's memory repo. Three guards:` — **the live routing rule, which MUST change**), plus `:2267`/`:2272` History rows. ⚠️ `grep -c` counts LINES and hides the second site | a column scan of line 2094, never `grep -c` |
| P3 | ⛔ **`three destinations`: 3 OCCURRENCES on 2 lines** | `THREE destinations` col 128 and `three destinations` col 3705, both on `:2094`; plus the `:2267` History row. Line length **3975** | a column scan |
| P4 | carrier 2 — the ritual, **spanning TWO lines** | the sentence STARTS on `wrap.md:68` (which ends `…stamp the DB's status. Planner`) and continues on `:69`. `:69` is 92 chars, fragment at col 3 | `grep -nF -- "working-pattern" hooks/commands/wrap.md`, then read `:68` |
| P5 | ⛔ **`:69`'s GATE-LOAD-BEARING co-tenant** | after the sentence, `:69` continues `**Then add a line to \`shop_next_session.md\`:**` — the instruction introducing the `Lessons-swept:` line that `wrap_check`'s `[3b/lessons]` (`wrap_check.py:289–320`) BLOCKS THE WRAP on | read `:69` whole |
| P6 | carrier 3 — the wiring line | `eluvian.md:31`, 112 chars; `Planner-personal` begins at col **23** — ⛔ that is the EDIT BOUNDARY (`working patterns` at col 40 is only a locator) | a column scan |
| P7 | ⛔ **`:31`'s second clause is a SEPARATE live invariant** | `on a path to hardcoding — systems must not RELY on it` — not about destinations; nothing here supersedes it | read the bullet whole |
| P8 | carrier 4 — forge-ingested | `LESSONS.md:4447`, 300 chars, fragment at col 209, inside the 2026-08-19 entry marked `[status: implemented]` | `sed -n '4447p' "$GOV/LESSONS.md"` |
| P9 | ⛔ **the gate enforces NOTHING here — inertness, not weakness** | `MEMORY` is not a git repo on this machine, so `porcelain(MEMORY)` returns `[]`, the `[4/memory]` loop body is unreachable, and `check()` returns **0 fails** over 12 entries carrying no `class:`, 9 modified after the gate shipped. ⛔ **Do NOT cite the `class: banana` demonstration** — constructed, and the corpus does not produce it (thread 114 re-derivation, 2026-09-04) | import `wrap_check`, call `porcelain(MEMORY)` and `check()` |
| P10 | ⛔ **`pending` is a Rule 20 HEDGING KEYWORD** | `RULE_20_SELF_CHECK_BLOCK.md:58`, beside `inferred`, `assumed`, `not run`. A receipt QUOTING it fails the self-check | `grep -nF -- "hedging_keywords" "$GOV/RULE_20_SELF_CHECK_BLOCK.md"` |
| P11 | ⛔ **TWELVE `decisions/` lanes, THREE daemon-watched** | `find /Users/marklehn/Developer -maxdepth 5 -type d -name decisions` → 12; `config.json` `watched_projects` → bellows, tuyere, **`forge_lessons`** (the repo that ingests `LESSONS.md`) | the `find` and the config read |
| P12 | ⛔ **`LESSONS.md` has THREE writers, and the third runs INSIDE the verdict window** | this machine, another machine (measured: two sha moves on 2026-09-04 by two sessions), and `wrap.md:66`'s `project_status_markers.py --apply`. ⛔ **Do not pin its line count** — it moved twice in one afternoon | `git log --format="%ad %s" -- "$GOV/LESSONS.md"` |
| P13 | the class recipe, RUNNABLE | ⛔ `_parse_plan` / `_assign_class` are **instance methods on `class Depositor`**, not module attributes. `_assign_class(self, writes, project_root="")` — **the default returns `app-feature`, the auto-clearing class.** Print `w` before trusting any answer; the mis-order failure returns `app-feature` too | instantiate a `Depositor`, then `w,r,dc = d._parse_plan(text)`; `d._assign_class(w, "/Users/marklehn/Developer/bellows")` |
| P14 | ⛔ **`wrap.md`'s CONTENT is read** | `tests/test_wrap_sentinel.py::TestWrapMdClauses` asserts on its clauses. The predecessor claimed no code path reads these files and was falsified twice | `grep -rlF -- "wrap.md" tests/` |
| P15 | ⛔ **`git -C ""` means the CURRENT DIRECTORY** | not "no repo": rc=128 outside a repo, but **a REAL SHA from the governance checkout**. THE root guard is `test -n "${GOV:-}"` then `test -f "$GOV/PLANNER_TEMPLATE.md"` — executed, it fires | run it from three cwds |
| P16 | version + last writer | PT is **v4.98** → this ships **v4.99**. ⛔ Test the `**Version:**` LINE — the History table is append-only so `v4.98` survives the bump | `grep -m1 -nF -- "**Version:**" "$GOV/PLANNER_TEMPLATE.md"` |
| P17 | in-flight | re-derive at execution | `sqlite3 "file:$PWD/lifecycle.db?mode=ro" …` |

## MUST-PRESERVE

- ⛔ **`wrap.md:69`'s co-tenant survives byte-identical** from `**Then add a line…**` onward (P5).
- ⛔ **`eluvian.md:31`'s second clause survives** (P7).
- ⛔ **`LESSONS.md:4447`, its `[status: implemented]` marker, body and baton clause stay byte-identical.** The supersede is an APPEND; `numstat` shows lines added and **zero deleted**.
- ⛔ **No QA receipt row quotes `pending`** (P10) — describe the marker.
- ⛔ **Deposits keep all four doctrine paths** — trimming re-opens the `app-feature` auto-clear.
- ⛔ **No landed text carries a Rule 20 hedging keyword.**
- ⛔ **Every re-entry token is one an Item WRITES, never one an edit consumes.**

## What this does NOT do

- ⛔ It does not touch `wrap_check.py` or any code. The gate's six defects are thread 114.
- ⛔ It does not fix the `[3b/lessons]`-vs-*never-rewrite-another-machine's-block* contradiction, or the three-writer `LESSONS.md` window. Those are `drafts/diagnostic-multi-machine-wrap.md`, which prices them and decides nothing.
- **It does not backfill `class:` onto any memory entry**, and does not change the memory frontmatter schema.
- **It does not adjudicate the BATON's status** as a destination — read as orthogonal, stated for the CEO to correct.

## Drafting Cycle

**Tier:** **T2 — computed.** T-6 fires (the template, two ritual documents, the doctrine corpus); T-1 fires (two repositories). ⚠️ **A split by surface was PRICED AND REJECTED:** all three halves are T-6 → T2, so a split buys no tier reduction, and DC §1 warns in terms — *"role-class count and raw file count BOTH fail to predict walk depth. Size is not the lever the data supports; tier is."* The predecessor's failure was **dropped parent machinery plus fold accumulation**, not breadth.
**Walk register:** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-lessons-destination-v2-2026-09-04.md`
**Panel:** FULL form owed (Fork C) — T2, and RE-DRAFT-adjacent by construction.
**Walks:**
⛔ **No walk has run, so there are no per-pass rows yet.** The five lens lines are added at walk 1, each carrying its own `wN N folded — instruction N / record N` token; ⚠️ **a restructuring fold is declared in THIS BLOCK, not only in the register** — `cycle_check` reads the body and the predecessor's restructuring declaration, recorded only in its register, produced the WRONG escalation (S3-3).
- Cold panel — not convened; the predecessor's 52 findings are INPUT to v0, not credit against this cycle's obligation.

**Closing:** ⛔ **NOT CLOSED — v0, no lens has walked it, no direction verdict issued.**

## Cycle Manifest

*(to be EMITTED at BAR_MET — ⛔ this placeholder must not survive the freeze; an unemitted manifest reclassified plan 100031 and dispatched it past its class hold)*

## STEP 1 — DEV

> **Scope:**
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/LESSONS.md`
> - `hooks/commands/wrap.md`
> - `hooks/commands/eluvian.md`
> - `knowledge/development/dev-log-lessons-destination-v2-2026-09-04.md`
>
> **Item 0 — ROOTS, same invocation as every use.**
> ```
> GOV=/Users/marklehn/Developer/eluvian-governance
> BPY=/Users/marklehn/Developer/bellows/.venv/bin/python
> test -n "${GOV:-}" || { echo "GOV UNSET"; exit 1; }
> test -f "$GOV/PLANNER_TEMPLATE.md" || { echo "GOV WRONG"; exit 1; }
> ```
> ⚠️ **`GOV` is hardcoded to THIS machine's layout (walk 1, v2-4).** The shop MacBook Air keeps governance under `~/Developer/GitHub` (threads 89, 113), so this block FAILS CLOSED there — the safe direction, and the plan is `shop-infra` so it holds for a human release on the machine that releases it. ⛔ **Stated, not fixed:** resolving the root as `bellows_root.py` does would make it portable, but that changes how the plan finds its own operands and belongs in a plan that can test it on both machines.
> ⛔ **This block is THE root guard (P15).** Do not substitute an emptiness test on `git -C "$GOV" log` — `git -C ""` resolves to the current directory and returns a real sha from the governance checkout.
>
> **Item 1 — the PER-SURFACE RE-ENTRY TABLE. Run it BEFORE any edit; it replaces the predecessor's single ladder.**
>
> | surface | POST-STATE token (written by an Item, never consumed by an edit) | if ABSENT | if PRESENT |
> |---|---|---|---|
> | `PLANNER_TEMPLATE.md` | `**Version:** 4.99` **AND** a History row carrying this plan's id | apply Items 2–3 | skip; verify |
> | `wrap.md` | `grep -cF -- "cannot be codified goes to the memory repo" wrap.md` ≥ 1 | apply Item 5 | skip; verify |
> | `eluvian.md` | `grep -cF -- "cannot be codified" hooks/commands/eluvian.md` ≥ 1 | apply Item 6 | skip; verify |
> | `LESSONS.md` | an entry dated 2026-09-04 naming `LESSONS.md:4447` | apply Item 4 | skip; verify |
>
> ⛔ **Each row is INDEPENDENT — a surface's state is never inferred from another's** (S3-8: the predecessor inferred the corpus append from the template and could skip it). ⛔ **Any row whose token is PRESENT while its file is dirty → HALT and request a verdict.** ⛔ **Mixed states are NORMAL, not foreign edits** — that misreading is what made the predecessor's own half-landed state report as an intruder.
>
> **Item 2 — re-derive P1–P17 and HALT only on P9's failure** (if `[4/memory]` no longer derives its candidate set from `porcelain(MEMORY)`, thread 114 has landed and P9 must be rewritten). Every other mismatch is a finding.
>
> **Item 3 — `GATE_BASELINE` FIRST.** Run `plan_lint` and `cycle_check` against a NAMED shipped plan and record the verdicts verbatim. ⛔ Do NOT name `gates.check` — its signature is `gates.check(parsed, plan_text, step_number, project_path, …)` and it raises `TypeError` against a plan path.
>
> **Item 4 — `PLANNER_TEMPLATE.md` step 7.** Demote memory from a routine destination to the named exception. ⛔ **BOTH sites on line 2094** (P2 col 2576 is the live rule; col 2183 is narrative — leave it) and **both `three destinations` sites** (P3). ⛔ Do not edit the History rows at `:2267`/`:2272`. ⛔ Fix the dead glossary path inside the sentence you rewrite — resolve the root as `bellows_root.py` does, **never by expanding `$ELUVIAN_WRAP_ROOT`**, which that file states the daemon's environment does not carry. ⚠️ **Cite thread 91's admission test** (*"identity, working preferences, pointers"*, with the rider convention) — that is what may ENTER memory; P8's audit vocabulary is a RETIREMENT test and is not a substitute.
>
> **Item 5 — the History row, v4.99**, citing the ruling and stating the supersede. ⛔ **The row MUST carry this plan's id** — Item 1's first row discriminates on it.
>
> **Item 6 — the `LESSONS.md` append.** ⛔ **Guard, rooted, over all TWELVE lanes (P11)** for `in-progress-*`, `ready-*`, `hold-*`, `verdict-pending-*`; if any resident plan pins `LESSONS.md`, HOLD and record it. ⛔ **Take `shasum -a 256 "$GOV/LESSONS.md"` at the guard AND again immediately before writing** — three writers (P12), one of them the wrap ritual itself. On a difference, re-read the tail and re-derive `:4447`. ⛔ **Do NOT edit `:4447`; APPEND** a dated entry naming it by file and line and quoting the superseded bullet, with `[status: pending]` as the only status value alongside normal `[tag:]` markers, and a closing `**Tag:**` line.
>
> **Item 7 — `wrap.md:68–69`** (P4). Edit the SPAN including its separator whitespace; ⛔ everything from `**Then add a line…**` onward stays byte-identical (P5).
> ⛔ **The replacement MUST contain the literal `cannot be codified goes to the memory repo` — Item 1's re-entry row for this file tests for exactly that string (walk 1, v2-1).** Without it the row tests a token no Item writes, which is verbatim the S2-3 defect this v0 exists to correct.
>
> **Item 8 — `eluvian.md:31`** from col 23 (P6); ⛔ the second clause survives (P7).
> ⛔ **The replacement MUST contain the literal `cannot be codified` — Item 1's re-entry row for this file tests for it (walk 1, v2-1).**
>
> ⚠️ **DECLARED, Items 7–8: these two files are LIVE AT WRITE, not at merge.** `~/.claude/commands/wrap.md` and `eluvian.md` are symlinks INTO THIS WORKING TREE, so an edit takes effect for every subsequent session the moment it is saved — before any commit and before Step 2 verifies it. With `pause_for_verdict: always` the Step 1 → Step 2 window is unbounded wall-clock. ⛔ **Harm bounded and accepted** (the new text IS the intended doctrine), but stated so a clone editing a surface where the intermediate state IS harmful does not inherit an inertness assumption. ⚠️ **This declaration was folded at the predecessor's walk 1 and then DROPPED when this v0 was authored — restored at walk 1 (v2-3).**
>
> **Item 9 — dev-log**, recording all four surfaces' prior text verbatim, the `GATE_BASELINE` verdicts, and BOTH commit shas.
>
> **Item 10 — commit.** ⛔ Governance by absolute path with `git -C "$GOV"`, EXPLICIT PATHSPEC (this plan's register lives there and will be dirty); **bellows LAST**. `numstat` is TWO commits.
>
> **Deposits:**
> - `knowledge/development/dev-log-lessons-destination-v2-2026-09-04.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/LESSONS.md`
> - `hooks/commands/wrap.md`
> - `hooks/commands/eluvian.md`
>
> ⛔ **All four doctrine paths are listed deliberately.** With only the dev-log, `_parse_plan`'s fallback yields `writes=1` under `knowledge/` and `_assign_class` returns **`app-feature`, which AUTO-CLEARS** (S1-5). Verify with P13's runnable recipe after any edit to this block.
>
> **Post-conditions:** all four carriers re-read and none routes working-pattern lessons to memory by default, verified by grepping the CLAIM in several phrasings; the appended entry present and **the named line VERIFIED to still contain the quoted bullet** (`sed -n '4447p'`); the 2026-08-19 entry byte-identical and the previous last entry intact; PT at v4.99 with a History row carrying this plan's id; `wrap.md`'s co-tenant and `eluvian.md`'s second clause byte-identical; both commit shas recorded.

## STEP 2 — QA

> ⚠️ **Pre-declared benign gate failure.** `test_scope` is targeted but the probe evidence deposits as `.txt`; if `_gate_qa_test_result` finds no parseable pytest summary it FAILs. Expected and named here. The override act is `tools/clear_plan.py --override-gate <id> 2 qa_test_result --ref <committed path>`; ⛔ **commit the justification BEFORE the override** — it is write-once and uncorrectable.
>
> **Item 1 — the four surfaces quoted side by side**, post-change. A reader must not derive a different destination from any one of them.
> **Item 2 — the supersede is legible:** the appended entry, `:4447` unchanged (sha before/after), the previous last entry intact, `numstat` **zero deleted**. ⛔ **DESCRIBE the `[status:]` marker; never quote `pending`** (P10) — a quoted hedging keyword FAILS the Rule 20 self-check and Step 2 forbids closure on FAILED.
> **Item 3 — no-regression:** `plan_lint` and `cycle_check` against the SAME shipped plan named in Item 3, before/after identical. ⛔ Run `tests/test_wrap_sentinel.py` — `wrap.md`'s content IS read (P14).
> **Item 4 — the re-entry table is REAL:** re-run Item 1's four probes against the landed state; every token PRESENT, no HALT.
> **Item 5 — hygiene + receipt:** `numstat` vs BOTH commits; `reflog -n 4` **in both repos, each named**; per-item table; the Rule 20 self-check inside a Verification-headed section (the 556 placement law).
>
> **Deposits:**
> - `knowledge/qa/evidence/lessons-destination-v2-2026-09-04/qa-receipt.md`
> - `knowledge/qa/evidence/lessons-destination-v2-2026-09-04/probes-raw.txt`
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root, with:
> - `plan_slug`: `lessons-destination-v2-2026-09-04`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/lessons-destination-v2-2026-09-04/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/lessons-destination-v2-2026-09-04"`
> - `required_evidence_files`: `["probes-raw.txt"]`
>
> Include the literal stdout of the block in the QA report. Banner, byte-exact, inside the receipt's VERIFICATION section:
>
> ```
> Rule 20 — QA Self-Check Results
> ================================
> PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
> ```
> ⚠️ **Both lines are byte-exact, em-dash (— U+2014) included** — copied from `RULE_20_SELF_CHECK_BLOCK.md:105,114`, not authored. `plan_lint` (c) matches the literals and `gates.py::_gate_rule_20_self_check` enforces them.
>
> ⛔ **Step 2 does not close on a FAILED self-check.** ⚠️ And per P10, no row of the per-item table may QUOTE `pending` — describe the marker.
