# bellows — CYCLE_CHECK READS THE FOLD BASELINE WHERE THE MANIFEST DECLARES IT, AND A `deposit(` COMMIT IS NOT WALK HISTORY: `check_assert_3` resolves the baseline through `resolve_fold_baseline` — the ONE resolver, the manifest's `fold_baseline:` line first, beside the plan only when undeclared, a declared baseline that does not resolve never falling back to a sibling file — and `check_assert_2`'s walk-commit pattern drops `deposit\(`, so a governance-drafted plan held in a project lane is judged on its own record and the release gate no longer keys on the deposit commit's subject (thread 368)

**Date:** 2026-09-15 | **Project:** bellows | **Tier:** Small (two sites in one checker; three tests appended to its test file; one mutation manifest; one dev-log) | **Dispatch Mode:** bellows | **Test Scope:** full-suite (Rule 21 — `scripts/cycle_check.py` is imported by the release tool and the close and launched by the lens tools, and seven test files read it; `pytest tests/ -q`) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 368 | **cycle_tier:** T1

**auto_close:** false

**Post-close:** no daemon restart — `scripts/cycle_check.py` runs per invocation: `tools/clear_plan.py` imports it at each release (`:111`), `lens_commit`, `close_cycle`, `lens_order_check` and `substrate_check` import or launch it at their start, and no daemon code path holds it loaded; the change is live at the merge for every tool. Two bound follow-ups, the Planner's under the CEO's delegation of 2026-09-15. (1) The doctrine, applied at the same sitting as the merge: the GLOSSARY's `identity tie (fold_baseline)` entry (`:953–954`), which gives `lens_order_check` as the reader of a deposited copy's drafting log through `fold_baseline:`, gains an addendum beneath it, the file being append-only: "Since bellows #<id> (thread 368): `cycle_check`'s third assertion — a fold happened, so its baseline exists — reads the baseline through the same pointer, by the ONE resolver (`resolve_fold_baseline`: the manifest's `fold_baseline:` line first, beside the plan only when undeclared, a declared baseline that does not resolve never taken from a sibling file), and a `deposit(` commit in a lane is not walk history: the release gate judges a held copy on the record its manifest names, wherever the copy sits." (2) The release the gate refused on 2026-09-15 — Gate 2 tranche one, `knowledge/decisions/hold-executable-gate2-pt-w30-a.md` — re-run after the merge with `tools/clear_plan.py … --release-class-hold`, its `cycle_check` then reading `BAR_MET` (QA Item 3 measures it read-only first); the three sibling baseline copies placed beside that hold on 2026-09-15 (`99d577e3`) are then surplus and removed by the Planner in the wrap's chore commit, never by this plan.

**Depends on:** thread 368 (filed 2026-09-15 ~14:20 at the release of Gate 2 tranche one, session d04ebd33): `tools/clear_plan.py --release-class-hold` refused the held tranche with `cycle_check gate: ESCALATE:assert-fail:3` while the byte-identical closed draft in governance read `BAR_MET`, and the two other held plans with folds in their Cycle Logs (threads 321 and 341, committed as `chore`) read `BAR_MET` from the same lane; the CEO's ruling of the same afternoon ("Option a for the tranche" — release by override), which no tool could execute: `--override-gate` writes an override for a claimed plan's step gate in the lifecycle DB, and the class-release path's `cycle_check` check has no override switch, so the honest route is the checker's fix. Clone origin by kind: #100106 (`knowledge/decisions/Done/executable-100106.md`, Done 2026-09-15, T1 — one helper changed by inode, tests appended after the file's last, a three-mutant manifest, the two-commit DEV with the manifest riding the first, the mutation run redirected, the QA's receipt with the Rule 20 block run); the newest same-class writers of this checker, #100042 (the in-cycle battery, `faee8941`, T1) and #100053 (the closure claim with no walk data ESCALATES, `54e8b850`/`99d24c1a`, thread 213), both at T1. Threads 362 and 363 (the pre-check's presence-only read; the suite gate's first summary-shaped `.txt`) are the same family — a gate's evidence read from somewhere other than where the plan names it — and stay their own plans.

**Tier computed (§1):** **T1** — T-6 (a gate edited) is read against the shipped precedent: #100042 and #100053 changed this checker at T1, and this plan changes two sites of it, the second by deletion; §1's if-unsure rule is answered by the precedent, and the CEO may call T2 (open fork 1). T-3 fires: the checker runs on both machines under the mini's 3.12.14 venv and the Air's 3.9.6, with no filesystem or platform dependence in the two sites — the resolver's four steps are the ones every tool already runs on both layouts (thread 153). T-1, T-2, T-4, T-5, T-7 and T-8 do not fire: one file's two sites, no data, revertible by one commit, a structure-for-structure clone of #100106's form.

## CEO Context

When you release a held plan, the release tool re-runs the drafting-cycle checker on the held file and refuses unless the cycle reads complete. Tranche one of Gate 2 read complete in governance, where it was drafted, and refused in bellows' lane, where it was held — the same bytes. The checker looks for the cycle's fold baseline BESIDE whichever file it is handed, and only bothers when the file's own git history shows "walk" commits — and it counts a commit whose subject starts `deposit(` as one. I committed that hold with a `deposit(` subject; the two holds committed as `chore` passed. So today's gate judges a held copy by how its deposit was worded and where it sits, not by the record its manifest names — a `fold_baseline:` line every closed plan carries, which the checker's own resolver already reads for the battery and the lens tools but not for this assertion. This plan makes the assertion use that resolver, and stops counting a `deposit(` commit as walk history. Nothing restarts; the change is live at the merge for the release tool. The class is shop-infra, so it holds for your release; after the merge a GLOSSARY addendum, quoted in Post-close, is my apply act under your delegation, and then tranche one's release is re-run through the fixed gate.

## What this changes

1. **`scripts/cycle_check.py` — `check_assert_3(parsed, plan_path, git_has_context)`** (:369–379, "Fold happened — baseline exists. Degrades with assert #2."): the sibling lookup — `baseline = plan_path.parent / f".{plan_path.name}.foldcheck.json"` / `if not baseline.exists():` (:376–377) — becomes `baseline_path, _declared = resolve_fold_baseline(plan_path)` / `if baseline_path is None:`, the function already defined at :818 and used by `run_battery` (:872), `lens_order_check` (:196) and `substrate_check` (:106): the manifest's `fold_baseline:` line first, through `_resolve_register_ref` (absolute; the plan's git root; the governance root; its parent — the shop root), beside the plan only when the manifest declares none, and a declared baseline that does not resolve returning `None` — never a sibling file. The two answers are unchanged: `FAIL` when `git_has_context`, else `N/A`; `PASS` when the baseline resolves; `N/A` when no walk folded. A three-line comment names thread 368; the docstring's first line says "where the manifest declares it".
2. **`scripts/cycle_check.py` — `check_assert_2`'s walk-commit pattern** (:356): `re.compile(r"drafting\(|\[draft\]|deposit\(")` → `re.compile(r"drafting\(|\[draft\]")`, with a trailing comment `# a deposit( commit is not a walk (thread 368)`. A `deposit(` subject on the plan file's log no longer makes `git_has_context` True (:359) or counts toward the walk commits the `uncommitted` test compares with the plan's highest walk (:362). `[draft]` and `drafting(` subjects count as today.
3. **`tests/test_cycle_check.py`** — three tests and two helpers appended after the file's last test (:1264), in the file's own `monkeypatch` style (`_find_git_root` patched, `subprocess.run` mocked to a fixed log): `_t368_plan(tmp_path, fold_baseline_line, lane="lane")` writes `hold-plan.md` under `tmp_path/lane` with a two-walk block (`w1 2 folded — instruction 2 / record 0; w2 dry`) and a manifest carrying the given `fold_baseline:` line; `_t368_mock_log(monkeypatch, subjects)` mocks the log's subjects. **(t1)** `test_t368_assert_3_reads_the_declared_baseline` — the baseline in `tmp_path/gov/`, named absolutely by the manifest, nothing beside the plan, the log `[draft] w1 fold` / `[draft] w2 dry`: `check_assert_3(…, True)` is `PASS`, and `run_check` is neither `ESCALATE:assert-fail:3` nor `ESCALATE:uncommitted-walk`, exit 0. **(t2)** `test_t368_a_deposit_commit_is_not_walk_history` — `fold_baseline: <declare>` (undeclared), a sibling baseline, the log one line `deposit(plan): held on class:shop-infra`: `check_assert_2` returns `git_has_context` False and `uncommitted` False, and `run_check` is not `ESCALATE:uncommitted-walk`. **(t3)** `test_t368_a_declared_baseline_that_does_not_resolve_never_falls_back` — the manifest names `tmp_path/gov/missing.foldcheck.json`, a sibling baseline present, `[draft]` commits: `check_assert_3(…, True)` is `FAIL`. Red on the unedited checker, measured: `3 failed, 143 deselected` (t1 on assert 3's sibling lookup; t2 on `git_has_context`; t3 on the silent sibling fallback).
4. **`knowledge/mutants/cycle-check-baseline-368.json`** — `{"target": "scripts/cycle_check.py", "mutants": [ … ]}`, each mutant's keys `name`, `why`, `anchor`, `replacement` and `expect_fail`, three: (m1) `assert-3-sibling-only` — the resolver call back to the sibling lookup → t1; (m2) `deposit-counted-as-walk` — `deposit\(` restored to the pattern → t2; (m3) `declared-unresolvable-falls-back` — a declared baseline that does not resolve taken from beside the plan → t3. An anchor that matches its target other than once is the run's `ERROR` (`tools/mutation_check.py:266–270`).
5. **The dev-log** `knowledge/development/dev-log-cycle-check-baseline-2026-09-15.md`.
6. **Not changed:** `resolve_fold_baseline` and `_resolve_register_ref` (the ONE resolver, its four steps); `check_assert_1`, the rest of `check_assert_2` (the register resolution, the uncommitted test's arithmetic); `run_check`'s order of returns (:527–541); `run_battery`; `tools/clear_plan.py` (its gate still requires `BAR_MET`); every existing test — `test_assert_fail_3` (:125–140) still escalates on its `[draft]` line, `test_assert_3_baseline_exists` (:558–578) still passes on its sibling baseline with the test stanza declaring none; the six other `test_cycle_check_*` files; `tests/test_clear_plan_override.py`. The pattern's silence on `draft(` — `lens_commit`'s own subject form, which none of its three alternatives match, so a governance draft's lens commits never make `git_has_context` True — is open fork 2, not this plan's edit.

## Why this exists

Thread 368: the release gate is `cycle_check.run_check(hold_path)`, and its third assertion looked for the fold baseline beside whatever file it was handed, but only when `git_has_context` was True — which a `deposit(` commit on the file's log made it. Tranche one of Gate 2 was drafted in governance (its baseline beside the draft, named in its manifest) and held in bellows' lane by a commit titled `deposit(gate2-pt-w30-a): …`: the gate read `ESCALATE:assert-fail:3`; with the real baseline copied beside the hold it read `ESCALATE:uncommitted-walk`, the one `deposit(` commit counted against four walks; the byte-identical draft read `BAR_MET`, as did the 321 and 341 holds committed as `chore`. Two identical plans, one `deposit(…)` and one `chore(…)`, got opposite verdicts. The resolver that reads the manifest's `fold_baseline:` line already exists in the same file and serves the battery and two tools (P1); the assertion never called it. This plan makes it, and stops reading a deposit as a walk.

## What this does NOT do

- It does not widen the walk-commit pattern to `lens_commit`'s `draft(` subject (open fork 2): that would make assertion 3 and the uncommitted test active for every governance draft, a reach this plan has not measured.
- It does not change what the release tool does with the verdict, the daemon's gates, or any other checker.
- It does not remove the three sibling baseline copies placed beside the tranche-one hold on 2026-09-15 (the Planner's wrap chore, after the release).
- It does not write the GLOSSARY addendum (the Planner's apply act under the delegation, quoted in Post-close).

## MUST-PRESERVE

- ⛔ **The ONE resolver stays one:** assertion 3 calls `resolve_fold_baseline`, never a second lookup of its own; a declared baseline that does not resolve is `None`, never a sibling file (t3).
- ⛔ **The verdict order of `run_check` is unchanged** — assert 1, assert 2's register, assert 3, the uncommitted test, then the rest.
- ⛔ **`[draft]` and `drafting(` subjects still count as walk history** (`test_assert_fail_3` unedited, red as before on its `[draft]` line).
- ⛔ **Every existing test passes unedited;** the three new tests are appended after the file's last test.
- ⛔ **Every `expect_fail` is a node id of this plan's tests** — t1, t2, t3, or the test a survivor fix adds (Item 5) — and the mutation run is redirected into its deposit, never piped.
- ⛔ **Test code runs under pytest inside `tests/` or not at all** (bellows `CLAUDE.md`, *Test code*; thread 313).
- ⛔ **The thread-262 stop:** a QA step that must change production code STOPS and requests a verdict.

## Numbers discipline — measured 2026-09-15 by the Planner (bellows main `99d577e3`, its `scripts/cycle_check.py` blob `e41a550e1b9c528a` last written at `99d24c1a`; governance `7d7768d2`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the checker | `scripts/cycle_check.py`, 1128 lines: `check_assert_2` :276–366 — the walk-commit pattern `pat = re.compile(r"drafting\(|\[draft\]|deposit\(")` :356, `git_has_context = True` :359, `uncommitted = True` :362; `check_assert_3` :369–379 — its docstring :370–371, `any_folds` :373, the sibling lookup `baseline = plan_path.parent / f".{plan_path.name}.foldcheck.json"` :376 and `if not baseline.exists():` :377, `return "FAIL" if git_has_context else "N/A"` :378, `return "PASS"` :379; `run_check` :527 `a3 = check_assert_3(parsed, plan_path, a2_git)`, :536–537 the `ESCALATE:assert-fail:3` return, :538–539 `ESCALATE:uncommitted-walk`; `_resolve_register_ref` :771–816 (absolute → the plan's git root → the governance root → its parent); `resolve_fold_baseline(plan_path, manifest=None)` :818–847, returning `(path_or_None, declared)`, a declared field that does not resolve `(None, True)` "with no silent fallback to beside-the-plan"; its callers `run_battery` :872, `scripts/lens_order_check.py:196`, `scripts/substrate_check.py:106`; `parse_manifest_stanza` :639 | `sed -n '356p;369,379p;527,541p;771,847p' scripts/cycle_check.py`; `git grep -n -F 'resolve_fold_baseline(' -- scripts tests` |
| P2 | ⛔ the release gate | `tools/clear_plan.py` `release_class_hold` :79 — `import cycle_check` :111, `verdict, _ = cycle_check.run_check(Path(hold_path))` :112, `if verdict != "BAR_MET": return _fail(f"cycle_check gate: {verdict} (BAR_MET required) — file left held")` :113–114; `override_gate` :159 overrides a (plan, step, gate) triple in the lifecycle DB and has no arm for this gate | the file at those lines |
| P3 | the defect, measured | on the mini, 2026-09-15: `cycle_check.py knowledge/decisions/hold-executable-gate2-pt-w30-a.md` (committed `c1193914` `deposit(gate2-pt-w30-a): …`) → `ESCALATE:assert-fail:3`; with the baseline copied beside it (`99d577e3`) → `ESCALATE:uncommitted-walk`; the byte-identical draft `governance/knowledge/decisions/drafts/executable-gate2-pt-w30-a.md` → `BAR_MET`; `hold-executable-bellows-receipt-key.md` (thread 321) and `Done/executable-100104.md` (thread 341), committed as `chore` → `BAR_MET`. The prototype, a `--no-hardlinks` clone of main `99d577e3` (branch `r1`, `1b6a0dd3`, under the session's scratch): the three tests red on the unedited checker `3 failed, 143 deselected`; green after the two edits `3 passed`; `MUTATION: 3 killed, 0 survived, 0 error` | the register's walk 0 |
| P4 | ⛔ the tests | `tests/test_cycle_check.py`: 143 tests, 1264 lines, a newline at its end; `_MANIFEST_STANZA` :43 (tier, target, class, reads, writes, open_forks, walks … — no `fold_baseline:` line, so `_make_plan`'s plans are undeclared and resolve beside); `_make_plan` :58; `test_assert_fail_3` :125–140, its mock log `abc1234 [draft] w1 fold` / `abc1235 deposit(cycle-check)`; `test_uncommitted_walk` :359–380; `test_assert_3_baseline_exists` :558–578, the sibling baseline `.plan.md.foldcheck.json`, `check_assert_3(parsed, plan, True)` → `PASS`; the six sibling files `test_cycle_check_battery.py`, `_coherence_basis.py`, `_empty_body_silence.py`, `_manifest_provenance.py`, `_register_resolver.py`, `_t0_close.py`; `tests/test_clear_plan_override.py` (its `test_override_gate_tool_writes_all_matching_rows` fails only under a temporary-directory tree — `clear_plan.py:178–179` refuses a `--ref` under `/tmp`, `/private/tmp` or `/var/folders`; a worktree under the checkout passes it) | the files |
| P5 | the mutation tool and the manifest form | `tools/mutation_check.py` runs each mutant's `expect_fail` node alone (its docstring, :5), an anchor matching other than once is `ERROR` (:266–270); the form `{"target", "mutants": [{"name", "why", "anchor", "replacement", "expect_fail"}]}` | the file |
| P6 | class; in flight; interpreters; the suite | the writes → **shop-infra** (bellows, the project floor), HOLDS at admission for the CEO's release; in flight at drafting: #100107 (thread 315, `plan_claim.py`, `bellows.py`) running; held: 317, 307, 321, 322's when closed, and Gate 2 tranche one (the plan this fix unblocks) — none writes `scripts/cycle_check.py` or its tests; the bellows venv 3.12.14, `/usr/bin/python3` 3.9.6, the Air's venv 3.9.6; the full suite at `77f22a52` (#100106's QA, in a worktree): `2432 passed, 2 skipped`; behind the suite fence, over the scratch prototype `r1` of main `99d577e3`: `50 failed, 2383 passed, 3 skipped, 1 deselected` (the deselected test thread 361's fixed port; the fifty the scratch environment's — `test_admission_flip` 21, `test_depositor` 8, `test_depositor_receipts` 5, `test_governance_root` 4, `test_decisions` 4, `test_planner` 3, `test_phase4_planner_retry` 2, `test_notifier_coverage` 1, `test_consume_verdicts` 1, `test_clear_plan_override` 1), the base archive's set compared at walk 0 | `status.py`; `python -V`; the suite |

DEV re-derives P1 and P4 in Item 1 and HALTs on a mechanism mismatch; P2, P3, P5 and P6 are the drafting record.

## ACID, stated

- **Atomicity:** after the DEV's first commit (three files) the tree is green and the checker reads the manifest's baseline for every caller at once — the release tool, the battery, the lens tools; the second commit adds the run file and the dev-log; QA adds two evidence files. No half state: the two sites land in one commit.
- **Consistency:** one resolver for the baseline, in every reader; a deposit commit is a deposit, a walk commit a walk.
- **Isolation:** the checker runs in the calling tool's process; in flight at drafting, no other plan writes this file (P6).
- **Durability:** the run file, the dev-log, the QA receipt with the held file's verdict line, and the redirected suite file are the step's evidence.

## Drafting Cycle

**Tier:** **T1** — T-3 fires; T-6 read against the precedent (open fork 1). **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-cycle-check-baseline-2026-09-15.md
**Walks:** walk 0 pinned (P1–P6 on bellows `99d577e3`; the change prototyped in a scratch clone of the same base — red-first, green, the mutation run, the full suite behind the fence against the base archive's; clone-diff against `Done/executable-100106.md`: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold, re-saved after every intended edit; every lens commit through `/Users/marklehn/Developer/bellows/scripts/lens_commit.py`, invoked directly with its output whole — no filter between it and the screen — its exit read; no lens or walk number in any `--desc`, and no plain commit subject carrying a walk and a lens number; `lens_order_check` after every lens commit. `target_class` is left undeclared deliberately: `plan_lint`'s advisory `(t)` reads the target's basename as a detector's, but the change is an assertion's lookup and a pattern token, not a detector's state space — #100042's precedent for this same file (undeclared) and #100053's (undeclared) stand; #100030 declared `detector` for a plan that had a state space to declare.

- Weak spots:          w1 1 folded — instruction 0 / record 1; w2 1 folded — instruction 0 / record 1; w3 dry
- Destruction:         w1 dry; w2 dry; w3 dry
- Vulnerabilities:     w1 dry; w2 dry; w3 dry
- Integration-record:  w1 dry; w2 dry; w3 dry
- ACID:                w1 dry; w2 dry; w3 dry
**Closing:** WARM close after walk 3 — BAR MET (T1), thread 368's plan, on a dry walk: walk 3 dry on all five lenses. Findings by walk and class — walk 1: 1 (instruction 0 / record 1; QA Item 3 said what its `BAR_MET` rests on); walk 2: 1 (instruction 0 / record 1; `target_class` left undeclared deliberately, the checker's own precedent); walk 3: 0. No panel at T1. The change prototyped on a scratch clone of bellows main `99d577e3`: the three tests red-first (`3 failed, 143 deselected`), green after the two edits, the file and its neighbours green but for the location-dependent override test, `MUTATION: 3 killed, 0 survived, 0 error`, the full suite behind the fence `50 failed, 2383 passed, 3 skipped, 1 deselected` against the base archive's 51, the one difference the archive directory's name. Deposit via `ready-`; HOLDS on class shop-infra, the Planner releases under the CEO's delegation after #100107 closes (one plan in flight at a time). No restart; the Post-close acts are the GLOSSARY addendum on the *identity tie (fold_baseline)* entry and the re-run of tranche one's release through the fixed gate.

## Cycle Manifest
tier: T1
target: scripts/cycle_check.py
class: shop-infra
reads: scripts/cycle_check.py, tools/clear_plan.py, tests/test_cycle_check.py, tests/test_cycle_check_battery.py, tests/test_clear_plan_override.py, tools/mutation_check.py, tools/check_deposit.py, knowledge/decisions/Done/executable-100106.md, knowledge/decisions/hold-executable-gate2-pt-w30-a.md
writes: scripts/cycle_check.py, tests/test_cycle_check.py, knowledge/mutants/cycle-check-baseline-368.json, knowledge/mutants/cycle-check-baseline-368.run.txt, knowledge/development/dev-log-cycle-check-baseline-2026-09-15.md, knowledge/qa/evidence/cycle-check-baseline-qa-receipt-2026-09-15.md, knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt
mutants: knowledge/mutants/cycle-check-baseline-368.json
open_forks: 1. the tier of a checker fix — T-6 ("gates") read against the precedent of #100042 and #100053 at T1; the CEO may call T2 and its panel; 2. the walk-commit pattern's silence on `lens_commit`'s `draft(` subject — a governance draft's lens commits never make `git_has_context` True, so assertions 3 and the uncommitted test are inert for every plan drafted there; widening the pattern is a measured change of its own
walks: 3
yields: 0, 0, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:14
coherence: 3/3 body walks named in the register (2 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-cycle-check-368.md.foldcheck.json

---

## STEP 1 — DEV (two sites in the checker, three tests, the mutation run; two commits)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ Never run the daemon, `run_plan`, a claim, `bellows.py stop|restart`, or `tools/clear_plan.py` in any form — the release gate is the subject, and it is measured through `scripts/cycle_check.py` read-only and through pytest. ⛔ Test code runs under pytest inside `tests/` or not at all (bellows `CLAUDE.md`, *Test code*): no helper is called from `python -c`. ⛔ A commit's message goes to `git commit -F -` on stdin — a quoted heredoc ending the commit's own command: `<<'MSG'`, the message on the lines after it, `MSG` alone on the last — so no file is written for it. ⛔ The step is bound: every commit runs after `tools/check_deposit.py` in ONE command, the shape the pre-commit hook admits (bellows #100105).
>
> **Scope:**
> - `scripts/cycle_check.py`
> - `tests/test_cycle_check.py`
> - `knowledge/mutants/cycle-check-baseline-368.json`
> - `knowledge/mutants/cycle-check-baseline-368.run.txt`
> - `knowledge/development/dev-log-cycle-check-baseline-2026-09-15.md`
>
> **Item 1 — re-derive P1 and P4 and HALT on a mechanism mismatch** (a line-number drift is not one, and a count that moved is a record — paste it; `resolve_fold_baseline` already called in `check_assert_3`, `deposit\(` already gone from the pattern, or a `t368` test already in the file IS one). Paste each pin's re-derived line beside it.
> **Item 2 — the failing tests FIRST:** the two helpers and t1–t3 as *What this changes* 3, appended after the file's last test; run them on the unedited checker and paste the red summary line — `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/test_cycle_check.py -q -p no:cacheprovider -k t368` → `3 failed, 143 deselected`.
> **Item 3 — the edit** as *What this changes* 1, 2 and 4, each manifest anchor matching `scripts/cycle_check.py` once; the three green (`3 passed, 143 deselected`), the file green (`146 passed`), the sibling files green — `tests/test_cycle_check.py tests/test_cycle_check_battery.py tests/test_cycle_check_coherence_basis.py tests/test_cycle_check_empty_body_silence.py tests/test_cycle_check_manifest_provenance.py tests/test_cycle_check_register_resolver.py tests/test_cycle_check_t0_close.py tests/test_close_cycle.py tests/test_lens_order_check.py tests/test_clear_plan_override.py`, predicted `240 passed` in the worktree (the override test passes under the checkout; it fails only under a temporary-directory tree) — then the FULL suite green: predicted `2435 passed, 2 skipped` on `77f22a52` — P6's basis and the three tests — plus the tests of the plans merged before this one (the run supersedes the prediction; HALT only on a non-zero failure count). An existing test failing here is a HALT, never an edit.
> **Item 4 — first commit**, gated on the FULL suite AND the pre-check with the stage flag, path-scoped, run as ONE command — the verdict read checks each commit's place in the step's transcript, and a split chain or a suite piped into another command is a deviation it records: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" --expect-missing knowledge/development/dev-log-cycle-check-baseline-2026-09-15.md knowledge/mutants/cycle-check-baseline-368.run.txt && git add scripts/cycle_check.py tests/test_cycle_check.py knowledge/mutants/cycle-check-baseline-368.json && git commit -F - -- scripts/cycle_check.py tests/test_cycle_check.py knowledge/mutants/cycle-check-baseline-368.json <<'MSG'`, the message on the lines after it and `MSG` closing it — three files (the manifest rides this commit so the mutation run's `HEAD:` archive contains it); the message tagged with the plan id and `thread 368`.
> **Item 5 — the mutation run, REDIRECTED into `knowledge/mutants/cycle-check-baseline-368.run.txt` (a Deposit):** `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/cycle-check-baseline-368.json > knowledge/mutants/cycle-check-baseline-368.run.txt 2>&1`; the last line must read `MUTATION: 3 killed, 0 survived, 0 error` — a survivor is a test to write, not a mutant to delete: the test, at the file's end, and the manifest, that mutant's `expect_fail` naming the new test — `tools/mutation_check.py` runs each mutant's `expect_fail` node alone, so a test the manifest does not name kills nothing — in ONE further commit, run as one command — `rm knowledge/mutants/cycle-check-baseline-368.run.txt` first (a run file recording a survivor fails the pre-check's `mutation_result` gate, and a present path cannot be named), then the FULL suite AND the pre-check with `--expect-missing knowledge/development/dev-log-cycle-check-baseline-2026-09-15.md knowledge/mutants/cycle-check-baseline-368.run.txt`, then `git add` and `git commit -F - … <<'MSG'`, path-scoped to the test file and the manifest — then the run again, which writes the run file anew, its `HEAD:` naming that commit.
> **Item 6 — the dev-log** `knowledge/development/dev-log-cycle-check-baseline-2026-09-15.md` under the four headings declared below; `## Pins re-derived (P1, P4)` opens with P4's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `a worktree under the checkout passes it`; paste through those words, then stop. Under `## Failing-first (red, then green)` the red line and the three green lines (the three, the file, the full suite); under `## The gate observed (t1, t2, t3)` the three PASSED lines of a `-v` run over those node ids — each test asserts the verdict it names, and nothing in this step prints them otherwise; under `## Mutation run` the run file's last line. **The last commit** (the second, or the third after a survivor fix), gated on the FULL suite and the pre-check bare (`… tools/check_deposit.py … 1 --wt "$(git rev-parse --show-toplevel)"`, no stage flag — the run file and the dev-log now exist), run as ONE command, path-scoped to the run file and the dev-log, its message on stdin.
> **Headings:** `## Pins re-derived (P1, P4)`; `## Failing-first (red, then green)`; `## The gate observed (t1, t2, t3)`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P4)` ← P4
>
> **Deposits:**
> - `knowledge/mutants/cycle-check-baseline-368.json`
> - `knowledge/mutants/cycle-check-baseline-368.run.txt`
> - `knowledge/development/dev-log-cycle-check-baseline-2026-09-15.md`
>
> **Post-conditions:** t1–t3 green, `test_assert_fail_3` and `test_assert_3_baseline_exists` unedited and green; the FULL suite green with no `failed`; the mutation run's last line as Item 5 states it; two DEV commits, or three after a survivor fix — the first carrying the three files, the last the run file and the dev-log; the dev-log's four headings present as full lines with P4's cell whole; `grep -cF 'deposit\(' scripts/cycle_check.py` → 0 and `grep -cF 'resolve_fold_baseline(plan_path)' scripts/cycle_check.py` → 1 in the worktree; `git status --porcelain` empty after the last commit.

## STEP 2 — QA (the full suite; the gate read on the held plan it refused; the receipt)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter ABSOLUTE; read-only beyond the two evidence files; no `tools/clear_plan.py` in any form; the commit's message goes on stdin, as STEP 1's header states; the step is bound — the commit runs after the pre-check in ONE command.
>
> **Item 1 — the full suite REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt 2>&1`; in the receipt's Verification table the row quotes the count as `N passed` and names the skips as `two skips (test_gate_watcher live-DB; test_fallback_live_wal_window version gate)` — never pytest's own summary line, whose word the Rule 20 block scans for; a run that fails on a test outside this plan's files is re-run once, both summaries named in the receipt.
> **Item 2 — the gate, read through its own tests:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest -v -p no:cacheprovider` over t1, t2, t3, `test_assert_fail_3`, `test_uncommitted_walk` and `test_assert_3_baseline_exists` of `tests/test_cycle_check.py`, and paste the six PASSED lines. No call into the module outside pytest.
> **Item 3 — the held plan the gate refused, read only:** `/Users/marklehn/Developer/bellows/.venv/bin/python scripts/cycle_check.py knowledge/decisions/hold-executable-gate2-pt-w30-a.md` from the worktree (the worktree carries the held file and its `deposit(` commit as main does) → `BAR_MET` on the last line, pasted, with the manifest's `fold_baseline:` line of that file (`grep -n '^fold_baseline:' knowledge/decisions/hold-executable-gate2-pt-w30-a.md`) beside it — the verdict rests on the file's one `deposit(` commit no longer counting as walk history (assertion 3 is then N/A for it); that a declared baseline resolves from a lane is t1's proof, and the two together are what the gate lacked. And the same command over `knowledge/decisions/Done/executable-100104.md` → `BAR_MET`, pasted: the plans that passed before still pass. No file is renamed, released or written.
> **Item 4 — production writes, stated exactly:** none outside the two evidence files; no lane file, no lifecycle row, no daemon act, no release.
> **Item 5 — receipt** `knowledge/qa/evidence/cycle-check-baseline-qa-receipt-2026-09-15.md`: `numstat` over the DEV commits (`<base>..<dev>`, five files); `git diff <base>..<dev> -- scripts/cycle_check.py` pasted (the two sites alone); the test file's edit, checked — `git diff -U0 <base>..<dev> -- tests/test_cycle_check.py | grep -F '@@'` pasted beside `git show "<base>:tests/test_cycle_check.py" | wc -l` (a base held in a shell variable is written braced, `"${BASE}:tests/…"` — zsh reads `$BASE:t` as a modifier): every hunk adding lines after the base file's last line and removing none — a hunk anywhere else is an edit to a test MUST-PRESERVE freezes, and the step STOPS; a `## Verification` table with one row per Item 1–4, each resting on a quoted line — the suite row quotes Item 1's `N passed` wording, never the summary line itself — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`/Users/marklehn/Developer/eluvian-governance/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: cycle-check-baseline-2026-09-15`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["cycle-check-baseline-suite-2026-09-15.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 6 — commit**, path-scoped and gated, run as ONE command: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/cycle-check-baseline-qa-receipt-2026-09-15.md knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt && git commit -F - -- knowledge/qa/evidence/cycle-check-baseline-qa-receipt-2026-09-15.md knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt <<'MSG'`, the message on the lines after it and `MSG` closing it. ⛔ A QA step that must change production code STOPS and requests a verdict (thread 262).
>
> **Deposits:**
> - `knowledge/qa/evidence/cycle-check-baseline-qa-receipt-2026-09-15.md`
> - `knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/cycle-check-baseline-qa-receipt-2026-09-15.md`
> - `knowledge/qa/evidence/cycle-check-baseline-suite-2026-09-15.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 2 skipped` with N at least the DEV's count and no `failed`; Item 2's six PASSED lines pasted; Item 3's two `BAR_MET` lines pasted with the held file's `fold_baseline:` line; the test file's hunk headers pasted, every hunk after the base's last line; the receipt's `## Verification` table has four rows, and the receipt carries the block's stdout, its `PASSED — SELF-CHECK PASSED` line among it; one QA commit carrying the two evidence files.
