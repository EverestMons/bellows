# governance — DRAFTING_CYCLE.md §3 Cycle Manifest mandate + Rule C refinement (component 2a)
**Date:** 2026-08-19 | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** none (governance doctrine edit; the gate side shipped in 2b — executable-474) | **Execution:** Step 1 (GOVERNANCE, in-place) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

## Context

Component 2a — the DOCTRINE half of component 2 (the manifest) in the cycle-automation arc. Order of execution (proposal §11 + baton): 1) `cycle_check` ✅ → 2) manifest [2b tooling `executable-474` ✅ · **2a doctrine = this plan**] → 3) depositor → 4) capstone (§2 auto-advance, component 4, ships LAST). **The capstone is NOT in this plan** — it "rides 2a's doctrine lane" (same governance-amendment style) but lands after component 3 per §11.

2a amends the ROOT doctrine `DRAFTING_CYCLE.md` to MANDATE the `## Cycle Manifest` stanza and refine the one §3 rule the stanza conflicts with, per the finalized grammar + Rule 27 Gap Assessment in `bellows/knowledge/research/cycle-manifest-stanza-design-2026-08-19.md` (diagnostic-472, T-7 — build from that spec, do not re-derive).

**This is a governance IN-PLACE dispatch** ([[governance-in-place-dispatch]]): `DRAFTING_CYCLE.md` lives at the GitHub root under the top-level `.git` (no project `.git`), so the STEP uses ABSOLUTE operands, a triple-pin (pre-edit sha `acce7eb`), `git add <abspath>` before a pathspec commit, and `git rev-parse` in the commit compound ([[phase-commits-assert-the-repo]]). NOT a worktree.

**CEO-authorized direct amendment — a declared §6 deviation** (v1.5 / v1.6 / v1.7 / v2.1 precedent): the cycle-automation proposal was CEO-routed 2026-08-19, not corpus-path (LESSONS → Gate 1 → Gate 2). The plan declares the deviation and its reason (the arc's subject is the cycle's own tooling), and the History row records it.

**§6 coordinate-doctrine-and-gate:** the §3 stanza's gate side ALREADY SHIPPED in 2b (`plan_lint` stanza check + `cycle_check --emit-manifest`). 2a mandates the stanza's PRESENCE in doctrine while `plan_lint` stays presence-optional (warn-first, no plan carries a stanza yet) — the absence-warn teeth are DEFERRED per §4's break-in posture. Declared, not silent. ⚠️ **When those teeth eventually flip on (a later plan, walk-2 D2), the absence-warn must be SCOPED to plans authored after this mandate (a version/date cutoff) — a retroactive absence-warn would turn the entire stanza-less historical corpus red.**

**Two amendments (walk-0 measured against sha `acce7eb`, 111,294 bytes):**
- **A — §3 stanza mandate.** ADD a new paragraph to §3 defining the 10-field `## Cycle Manifest` stanza (grammar, trust taxonomy, placement) + a stanza in the §3 worked example. Purely additive; removes NO §3 rule (diagnostic-472 Q5: no §3 line is deleted).
- **B — Rule C refinement (Option A, CEO fork resolved 2026-08-19).** Scope the "no running fold-count in the Cycle Log" prohibition to the DRAFTING phase; exempt the post-close computed `yields:`. Two sites: line 210 (§3 proper) + line 194 (§2.8 cross-reference). This LOOSENS a live doctrine prohibition — subtractive-cut discipline applies (§2.7): the loosening must not leak beyond `yields:`.

A and B are COUPLED: A's `yields:` field would violate the unrefined Rule C, so B is the consistency-closure that makes A admissible. They ship atomically.

**Diagnostic-472 gap (b) — its two clauses, both accounted for (declared, cold-scout F5 + panel-DISCOVERY DISC-2).** 472's Q5 lists three §3 gaps: (a) stanza grammar, (b) compaction, (c) the Rule C refinement. Gap (b) has TWO clauses: **(b1)** remove the `Walk N STATUS:` / §5-Conformance prose conventions from §3 — a genuine NO-OP (verified: `Walk N STATUS` and `Conformance` = 0 occurrences in `DRAFTING_CYCLE.md`; nothing to remove, which is also why "removes NO §3 line" above is literally true); **(b2)** respecify the Closing line to carry authored reasoning only, computable facts moving to the stanza — this IS landed, inside Amendment A's stanza paragraph ("the stanza COMPACTS … Closing retains authored reasoning only"), NOT skipped. So 2a lands (a), (b2), and (c); only (b1) is a no-op. Declared so a later plan-vs-472 diff does not read the scope as a silent miss.

## Drafting Cycle
**Tier:** T2 — triggers fired: T-6 (governance surface: edits doctrine), T-8 (novel: first manifest-mandate amendment). Cold panel owed at close (§2.6); walk-0 cold scout RUN (see walk-0 line). Panel FORM: likely FULL (Fork-C magnitude proxy fails — this is a hand-anchored doctrine prose edit, not a committed-builder mechanized clone, and the newest same-class sibling was additive-only so no subtractive-safety machinery is inherited; "any proxy fail → full form"). Determined at close. cycle_check dogfooded per walk ([[cycle-automation-depositor-arc]] adoption decision).
**Walk 0 (context pin) — 5 measurements, real:**
1. Newest same-class plan (clone-diff target): `[420] gate2-347-2026-08-14` → DC v2.11 (the last doctrine amendment). Prior siblings 405/391/386/373 — all §2/§3 prose amendments, all ADDITIVE-only (none removed a live rule).
2. Anchors: Rule C at **L210** (len 1850, the compact-form paragraph) and **L194** (len 1315, §2.8 "Deletion is the third resolution" bullet tail). Stanza insertion: after the judged-stop-exception paragraph (ends "…a class list, not narrative.", ~L216), before "The Cycle Log must therefore contain no string a gate matches" (~L218); example stanza after the Closing line inside the L228–240 code fence.
3. Occurrence counts: `Do not keep a running fold-count in the Cycle Log` = **1** (L210); `prohibition is **unchanged**` = **1** (L194); `Cycle Manifest` = **0** (clean insertion); `running fold-count` = 2 (both edit sites). All anchors count-1 or count-0 as required.
4. Last writer: L210 = `6f7dd2d` (2026-08-11, v2.1-era); L194 = `983136a` (2026-08-11). Both long-settled, `implemented` lifecycle — no in-flight plan owns these lines.
5. Target sha: `acce7eb` (working tree == HEAD blob — clean).
- Clone-diff vs newest same-class (gate2-347 / v2.11): additive-only; 2a's Amendment B is SUBTRACTIVE (loosens a prohibition) — a class the newest sibling does NOT exercise, so no inherited subtractive-safety machinery; §2.7's subtractive-cut discipline is applied fresh, not cloned. No origin drift.
- Cold scout (T2, §2.0): **RUN** — fresh-context, lens-4-shaped (integration-record), no brief-layering, author-verified. 7 findings, 5 CLEAN and independently verified against live evidence (the arc order matches proposal §11; the gate-side-shipped-in-2b discharge is real; the §6 CEO-deviation precedent v1.5/1.6/1.7/2.1 exists verbatim; the Rule C loosening is bounded exactly where diagnostic-472 + the CEO fork placed it; no §3-rule conflict — 472 already adjudicated conflicts i/ii/iii). 2 folds taken: **F4** (add a post-condition assert that the stale `prohibition is **unchanged**` phrase is gone — the plan asserted text was ADDED but not that the CHANGE landed; the load-bearing gap) + **F5** (declare 472 gap (b) a no-op against live §3). 1 record nit: **F6** citation `:37-39`. **No forcing finding — PROCEED reinforced on the integration-record lens.**
**Direction verdict (after walk 1): PROCEED.** The angle — mandate the §3 stanza (A) + refine Rule C to admit its `yields:` field (B) — is right and shippable. No forcing finding: no clone-origin/mechanism/scope premise is invalidated. Walk 1's folds sharpen the STEP; they do not move the premise.
**Walks:** 6 (walk 1–6). Instruction yield: w1 = 8, w2 = 2, w3 = 1, w4 = 2, w5 = 2, w6 = **0** → 8→2→1→2→2→**0**. Record findings counted separately (§3). §2 BAR MET at walk 6 (zero instruction-class). Cold panel (§2.6) owed before the final close (T2). ✅ **§2.8 REGION SIGNAL RESOLVED:** the STEP's verification/commit/identity scaffolding generated instruction folds walks 2–5 (W3,V3 · W4 · W5,D3 · W6,W7) but is now instruction-DRY at walk 6 — the region settled without needing the consolidation-rewrite the signal warned might be required. The trickle stopped on its own.
- Weak spots (1.4):     w1 2 folded — instruction 2 / record 0; w2 1 folded — instruction 1 / record 0; w3 1 folded — instruction 1 / record 0; w4 1 folded — instruction 1 / record 0; w5 2 folded — instruction 2 / record 0; w6 dry.
  - W1: governance in-place needs the pre-edit sha as a triple-pin — folded sha `acce7eb` into the STEP as the pin, with a HALT if HEAD:blob or porcelain-status disagrees before any edit.
  - W2: Amendment A's insertion is described by surrounding-paragraph prose, which is not a probe ([[edit-anchor-is-not-a-probe]]). Folded: the STEP asserts the insertion anchor (the judged-stop paragraph's closing sentence) occurs exactly once and inserts between it and the named following sentence; never "the first match".
  - W3 (w2, instruction): the STEP's "re-run plan_lint on a Done plan to confirm the doctrine edit broke no gate" is a NULL control — neither plan_lint nor cycle_check reads `DRAFTING_CYCLE.md` at runtime (VERIFIED: only WARN-citation strings name it, 0 functional reads; v2.2 History measurement). Editing doctrine cannot change a gate's behavior, so the check passes regardless. Folded: dropped the null run; replaced with a doctrine-integrity check + the note that §3↔§4 lockstep is discharged by 2b's tests.
  - W4 (w3, instruction): the walk-2 verification command claimed "working tree vs HEAD" but used bare `git diff`, which is working-tree-vs-INDEX and prints NOTHING once the file is `git add`-staged (VERIFIED: 0 lines staged vs 10 for `git diff HEAD`). An agent that stages before verifying would read a real edit as "no change". Folded: verification now specifies `git diff HEAD -- DRAFTING_CYCLE.md`, staging-order-independent.
  - W5 (w4, instruction): the verification said the diff must show "exactly the A + B hunks", but the STEP also mandates a Version-line bump (L5) and a History row — so `git diff HEAD` shows SIX changes, and "exactly A+B" would flag the mandated version/History edits as unexpected. Folded: verification now enumerates all six expected changes (stanza para, example stanza, B1, B2, Version line, History row), nothing else.
  - W6 (w5, instruction): the W5 fold said "six HUNKS", but the adjacent §3 edits (B1 L210, stanza para ~L216, example) sit within a few lines and git COALESCES them into fewer diff-hunk blocks (VERIFIED reasoning: 3-line context, ~6-line gaps → contexts touch). A block-COUNT assert would false-alarm. Folded: verification asserts by CONTENT present, not hunk-block count.
  - W7 (w5, instruction): the STEP identity said "Apply ONLY Amendments A + B; touch no other line" — but §6 MANDATES a Version bump + History row for any amendment, and the verification requires them. An agent obeying "touch no other line" literally would skip the required bookkeeping (VERIFIED contradiction: L77 vs the verification's version/History mandate). Folded: identity now names the version+History as required bookkeeping, "touch no other rule/section/line" otherwise.
- Destruction (2.4):    w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 0 / record 1; w3 dry; w4 1 folded — instruction 1 / record 0; w5 dry; w6 dry.
  - D1: Amendment B LOOSENS a live prohibition (Rule C). Per §2.7 subtractive-cut + "any claim a governed text CHANGED is proven by `git`": the STEP scopes the loosening narrowly ("during the drafting phase" + "sole exemption is the post-close `yields:`") so NO other running fold-count becomes permissible, and QA diffs the pre-edit state vs live to prove the loosening is bounded to `yields:`. Folded into the STEP + a verification clause.
  - D2 (w2, record): the deferred absence-warn teeth (I1), when a later plan flips them on, must be SCOPED to post-mandate plans — a retroactive absence-warn turns the whole stanza-less corpus red. Folded a forward-note into the Context §6 deferral.
  - D3 (w4, instruction): the STEP said "append a dated History row", but `DRAFTING_CYCLE.md`'s `## History` is NEWEST-FIRST (2.11 at top; the 2.10 row states "the History is newest-first") — appending would misplace 2.12 at the oldest end. Folded: the STEP now PREPENDS the row (top of History, above 2.11) and bumps the Version line (L5), dated at dispatch.
- Vulnerabilities (3.1): w1 2 folded — instruction 2 / record 0; w2 1 folded — instruction 1 / record 0; w3 dry; w4 dry; w5 dry; w6 dry.
  - V1 (⚠️ the dogfood CAUGHT this live): the added §3 example stanza + this plan's own DC block must not introduce a string a gate matches (§3 L218 gate-token prohibition, reflexive per §3 L220) nor trip cycle_check's step-8. The detector treats the bold close-marker itself as a closure claim (`bellows/scripts/cycle_check.py:37-39`, `CLOSURE_RE`), so (i) a mid-cycle OPEN draft carrying a bold close-marker status line false-fires `ESCALATE:claimed-close-unmet`, and (ii) even a finding note that QUOTES the marker trips the same detector (the reflexive trap). Folded: this open block uses a non-close status marker, every reference DESCRIBES the marker rather than reproducing its literal, and the doctrine EXAMPLE uses lowercase/described tokens.
  - V2: governance in-place environment ([[phase-commits-assert-the-repo]]) — `DRAFTING_CYCLE.md` at the GitHub root under the top-level `.git`; cwd resets between Bash calls. Folded: ABSOLUTE operands throughout, `git add <abspath>` before the pathspec commit, `git rev-parse --show-toplevel` asserted == the GitHub root inside the commit compound.
  - V3 (w2, instruction): the STEP's verification used `git show acce7eb:DRAFTING_CYCLE.md`, but `acce7eb` is a BLOB sha (from `rev-parse HEAD:<path>`), not a commit — the `<rev>:<path>` form ERRORS (`fatal: … exists on disk, but not in 'acce7eb'`, VERIFIED). A command that fatals on execution is not a probe (§2.7 execute-against-real-data). Folded: verification now uses `git diff HEAD -- DRAFTING_CYCLE.md` (refined to `HEAD` at W4; showing the six expected changes per W5/W6), with the broken `git show <blob>:path` form flagged.
- Integration-record:   w1 2 folded — instruction 2 / record 0; w2 1 folded — instruction 0 / record 1; w3 1 folded — instruction 0 / record 1; w4 1 folded — instruction 0 / record 1; w5 dry; w6 2 folded — instruction 0 / record 2 (record-DECAY).
  - I1: §6 coordinate-doctrine-and-gate — the §3 gate side shipped in 2b; 2a mandates PRESENCE while `plan_lint` stays presence-optional. Folded the DEFERRAL declaration into Context (absence-warn teeth deferred per §4 break-in) — declared, not silent.
  - I2: §6 amend-only-through-corpus vs CEO-authorized direct amendment — 2a is CEO-routed (proposal 2026-08-19), a declared §6 deviation (v1.5/v1.6/v1.7/v2.1 precedent). Folded into Context + the History-row requirement in the STEP.
  - I3 (w2, record-DECAY): the Cycle Log itself (covered by every walk, §3) had stale "cold scout owed" / "walk 1 (this turn)" / "walks 2+ owed" lines after the scout ran and the cycle advanced. Folded: swept the Tier line, the Walks line, and the closing status line to current state.
  - I4 (w3, record-DECAY): the walk-2 dogfood line still read "to be re-run this turn" after walk 2's cycle_check had in fact returned CONTINUE. Folded: swept it to the actual result. Plus a clean integration re-verify (no fold): DRAFTING_CYCLE.md HEAD:blob still == `acce7eb` across the date boundary — the walk-0 pin holds, no sibling amendment shipped mid-cycle.
  - I5 (w4, record-DECAY): the walk-3 dogfood line went stale ("to be re-run") — the THIRD instance of the same prospective-placeholder decay (I3/I4/I5), §2.8's three-folds-on-a-region signal. Folded: collapsed the per-walk dogfood lines to one results-only line and retired the prospective placeholder (convention fix, not a structural collapse — no clock reset).
  - I6 (w6, record-DECAY): the V3 fold-note still described `git diff --` / "exactly the A+B hunks" after W4 refined it to `git diff HEAD` and W5/W6 changed it to "six expected changes" — a stale fold-note. Folded: swept the V3 note to current.
  - I7 (w6, record-DECAY): the verification block's provenance header ("§2.7…, walk-2 V3+W3, walk-3 W4") was stale (missing W5/D3/W6) and its list carried dangling "(ii)/(iii)" after "(i)" was absorbed. Folded: re-based the header, fixed the numbering.
- ACID (5.2):           w1 1 folded — instruction 1 / record 0; w2 dry; w3 dry; w4 dry; w5 dry; w6 dry.
  - A1: Consistency/Atomicity — A's `yields:` field violates the unrefined Rule C, so A-without-B leaves doctrine self-contradictory (mandates a stanza field a §3 rule forbids). B is the invariant that closes the gap; the two must apply atomically in one commit. Folded: Context states the A+B coupling; the STEP applies both in one commit and the version bump asserts both hunks present.
  - w2: dry — single-step plan, no multi-step schedule; no new contradiction among the folded requirements.
**Conflicts:** none cross-lens (no ledger oscillation).
**Walk 1 STATUS:** 8 folded — instruction 8 / record 0 — not dry.
**Walk 2 STATUS:** 4 folded — instruction 2 / record 2 (2 of the 4 are record-DECAY / forward-note) — not dry (yield 8→2 on the instruction class, falling; a NEW region: the STEP's verification block + the Cycle Log's own record decay, neither read by walk 1).
**Walk 3 STATUS:** 2 folded — instruction 1 / record 1 (record-DECAY) — not dry (yield 8→2→1, falling; W4 landed in the walk-2 verification-block rewrite, the least-reviewed region). Instruction surface nearly closed.
**Walk 4 STATUS:** 3 folded — instruction 2 / record 1 (record-DECAY) — not dry, instruction yield ROSE 1→2. Both instruction folds (W5, D3) are in the STEP's History/verification commit mechanics — a region prior walks under-covered while focused on the doctrine edits themselves. A rise is the "keep walking" signal (§2/§2.8), not a close; walk 5 owed. NOT a candidate close.
**Walk 5 STATUS:** 2 folded — instruction 2 / record 0 (W6, W7) — not dry, yield flat 2→2. Both again in the STEP scaffolding (verification wording + identity contradiction). §2.8 region signal now standing (see Walks line). NOT a candidate close; walk 6 owed and is the tell — a dry walk 6 settles the region; more scaffolding defects means step back and consolidate.
**Walk 6 STATUS:** 2 folded — **instruction 0** / record 2 (I6, I7 record-DECAY only) — **§2 BAR MET.** Zero instruction-class findings across all five lenses; the two folds are record-decay sweeps of stale fold-notes (no restructuring fold). This is §2's convergence signature — a pass returning record-class only, the artifact having converged before its account of itself did. Residue (record-class): I6, I7, both swept this walk; none left un-swept. The close is NOT final yet — the T2 cold panel (§2.6) is owed and is not waived by a bar-met walk.
**Closing-record re-read (§2.7, walk 6):** ran against the Cycle Log + STATUS lines — the per-lens tallies reconcile with the sub-bullets (w1–w6), the yield series 8→2→1→2→2→0 matches the STATUS lines, and no closing prose over-claims. One record fix applied during the re-read is the I6/I7 sweep itself; re-read post-sweep is clean.
**cycle_check (dogfood) per walk:** w1 `CONTINUE` · w2 `CONTINUE` · w3 `CONTINUE` · w4 **`ESCALATE:yield-rising`** (CEO RESUMED — assessed benign, region-covered) · w5 `CONTINUE` · w6 **`BAR_MET`** (exit 0 — the tool signals the close: instruction-dry walk, asserts #1 PASS / #2 #3 N/A for a scratchpad draft, no restructuring, no plateau). (Results-only — the prospective placeholder that caused I3/I4/I5 is retired. Attestation note: w4 briefly pre-stated "CONTINUE" before the run; corrected to the actual ESCALATE per §2.7.)
**Cold panel (§2.6, FULL form — CEO-approved; T2 doctrine surface). Meter opened at convene.**
- **Seat SCOUT (walk 0):** run — 7 findings, 2 folds (F4, F5). See walk-0 line.
- **Seat DISCOVERY (weak-spots + destruction; evidence-attack + clone-diff briefs) — RUN, 94.3k tokens.** 9 findings (2 HIGH · 4 MED · 3 LOW; 1 confirmed non-finding), all author-verified against 472 + the doctrine live. Folds taken: **DISC-1** (N/A/`<declare>` are the emitter's contract 2b/474, not 472 Q2 — corrected the attribution), **DISC-2** (gap (b) has two clauses; (b2) Closing-respec IS landed in Amendment A, not skipped), **DISC-3/4/5** (History row must carry `Inheritors:` + §6 coordinate-doctrine-and-gate discharge + the no-DB-flip/`proposed`=0 note, per the 13 recent 2.x rows), **DISC-7** (pinned B1/B2 replacement text verbatim from 472 gap (c) so two agents write identical doctrine), **DISC-8** (B2 must preserve L194's §2.8 region-tally function, refining only the trailing clause). DISC-9 (LOW) noted below. DISC-6 non-finding. The panel caught what 6 warm walks missed — the History-row conventions, the 472 misattribution, and the unpinned doctrine text — vindicating the full-form decision.
- **Seat EXECUTION (vulnerabilities + execution brief) — RUN, 86.9k tokens.** Rehearsed the STEP's FULL edit+verification chain end-to-end against a scratch git copy of `DRAFTING_CYCLE.md` — the chain had never been executed. 4 findings (1 HIGH ship-blocker · 1 MED · 2 LOW). Folds taken: **EXEC-1** (⚠️ SHIP-BLOCKER — the pinned B2 replacement reintroduced `prohibition is **unchanged**`, which the F4 assert requires to be 0 post-edit → the STEP failed its OWN verification; fixed by dropping "unchanged" from B2, since the prohibition IS now drafting-scoped), **EXEC-2** (the pinned B1 tail was malformed markdown — unclosed bold + stray `"*`; would ship broken doctrine; balanced it), **EXEC-3/EXEC-4** (bound-check made content-based + integrity check fence-aware). **Positive evidence the seat proved by running:** triple-pin + all anchor-uniqueness asserts PASS; `git diff HEAD` shows exactly the 6 mandated changes, no §3 rule removed; the staging-order variant CONFIRMS the `git diff HEAD` fix is load-bearing (bare `git diff` = 0 after `git add`); code-fence stays balanced with the example inside; commit succeeds. Both EXEC-1/EXEC-2 were in the pinned B1/B2 text the DISCOVERY fold introduced — execution caught what static review could not.
- **Seat CAPSTONE (integration-record + ACID + system brief; runs last on the fold set) — RUN, 82.9k tokens.** 9 cross-checks; **0 HIGH · 0 MED · 1 LOW (CAP-4, non-blocking).** Verdict: **the fold set is COHERENT, plan deposit-ready.** Confirmed by fold-interaction cross-check: EXEC-1's "unchanged"-drop leaves no contradicted claim (the 5 remaining `unchanged` hits are all anchor/assert/note/count); the F4 assert targets the DOCTRINE file not the plan (satisfiable); the DISC-3/4/5 History-row × D1 bound-check interaction is consistently resolved by EXEC-3; yield 8→2→1→2→2→0 reconciles exactly with the STATUS lines; residue battery re-run = cycle_check `BAR_MET`, zero closure tokens, scratch B1+B2 rehearsal clean (anchors 1/1, post-edit `unchanged`=0, both balanced). **CAP-4 (LOW, record) DECLINED:** it proposed cross-linking the existing Closing-line spec (L216) to the stanza, but 472 Q5 authorizes exactly two §3 changes (Rule C + the new paragraph); a third §3 edit is out-of-scope. Additive-only is spec-faithful (the capstone confirmed no contradiction with the unedited L216/L242 spec).
**Cold panel COMPLETE (full form, 4 seats). Meter: scout ~84k + DISCOVERY 94.3k + EXECUTION 86.9k + CAPSTONE 82.9k ≈ 348k tokens.** Yield: scout 2 folds · DISCOVERY 7 folds (1 non-finding, 1 declined-LOW noted) · EXECUTION 4 folds (1 ship-blocker) · CAPSTONE 0 folds (fold set coherent). The panel earned its cost decisively: it caught a self-failing verification (EXEC-1), malformed doctrine markdown (EXEC-2), the 472 misattribution (DISC-1), and the missing History-row conventions (DISC-3/4/5) — none of which 6 warm walks surfaced.
**DISC-9 (LOW, record):** the plan's own DC-block yield series (8→2→1→2→2→0) is a drafting-phase running series — the exact form the *un*refined Rule C prohibits. Retained deliberately: it is the convergence record cycle_check reads (precedent diagnostic-460 carried the same), and once the stanza is adopted the post-close manifest `yields:` supersedes it. Gray-area, precedent-defensible; flagged not removed.
**Bar re-met post-panel:** the DISCOVERY + EXECUTION panel folds re-opened the walk (§2.6); the CAPSTONE seat read the resulting fold set and found it instruction-dry (0 HIGH/MED, 1 declined LOW) — so the bar is re-met on the folded draft, and the panel round is closed (the capstone covered the fold set, §2.6).
**Closing-record re-read (§2.7, final):** re-read the Cycle Log + panel record adversarially against the STEP body — the yield series 8→2→1→2→2→0 reconciles with the per-lens sub-bullets and STATUS lines; the panel meter and fold IDs (DISC-1..9, EXEC-1..4, CAP-1..9) match the folds actually applied; the "bar met walk 6 / re-opened by panel / re-met post-capstone" narrative is internally consistent; no closing prose over-claims. Dry.
**Closing:** 6 walks (bar met walk 6, instruction-dry) + full cold-panel (4 seats: scout, discovery, execution, capstone; ~348k tokens; 13 folds incl. one execution-caught ship-blocker; capstone read the fold set coherent); bar re-met post-panel; §5 conformance owed at deposit; closing-record re-read run (this block), dry; cycle done — deposit exactly once. ⚠️ Governance in-place + register-writing class → **HOLD FOR CEO** (not auto-deposit); the deposit is CEO-authorized.

---
---

## STEP 1 — GOVERNANCE DOCTRINE EDITOR (in-place)

---

> **Identity:** You amend `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` in place (governance root, top-level `.git`, NO worktree). Read `bellows/knowledge/research/cycle-manifest-stanza-design-2026-08-19.md` (Q2 grammar + Q5 gap table) FIRST — implement its spec, do not re-derive. Apply Amendments A + B **and their §6-mandated bookkeeping (the Version-line bump + a prepended History row — §6 requires both for any amendment, walk-5 W7); touch NO OTHER rule, section, or line.** In particular do not touch §2 (the capstone is component 4, a separate later plan).
>
> **⚠️ Triple-pin (governance in-place, W1):** before ANY edit, assert `git -C /Users/marklehn/Developer/GitHub rev-parse HEAD:DRAFTING_CYCLE.md` == `acce7eb...` (the pre-edit blob) AND `git -C /Users/marklehn/Developer/GitHub status --porcelain DRAFTING_CYCLE.md` is empty. If either fails, HALT — the file moved under you.
>
> **Amendment A — the §3 stanza mandate (additive).** Insert a new paragraph into §3 immediately AFTER the judged-stop-exception paragraph (assert its closing sentence — ending "…what enters the block is a class list, not narrative." — occurs exactly ONCE) and BEFORE "The Cycle Log must therefore contain no string a gate matches" (W2 — anchor by count, not "first match"). The paragraph defines the `## Cycle Manifest` stanza per diagnostic-472 Q2: a fixed `key: value` block emitted at BAR_MET by `cycle_check --emit-manifest`, placed after the Closing line and before the step-separator `---` (Rule G holds); ten ordered fields `tier · target · class · reads · writes · open_forks · walks · yields · validation · coherence`; the trust taxonomy (4 COMPUTED and never hand-typed: walks/yields/validation/coherence; 3 AUTHORED-BUT-DEPOSITOR-VERIFIED: reads/writes/class; 3 irreducibly AUTHORED: tier/target/open_forks); `reads:`/`writes:` explicit path lists (repo-relative, absolute cross-repo) driving the depositor's `writes∩writes` HARD-HOLD / `reads∩writes` HOLD; `class:` ∈ `{read-only, governed-tooling, register-writing}` driving auto-deposit; `validation:` = `checker=verdict` pairs the depositor RE-RUNS (mismatch → HOLD); the stanza COMPACTS the `Walk N STATUS:` aggregate lines and §5-Conformance prose (Closing retains authored reasoning only). ⚠️ **The `N/A`-when-unparseable and `<declare>`-when-undeclared sentinels are the EMITTER's contract (component 1 / 2b, shipped + tested in `executable-474`), NOT part of 472 Q2's grammar (panel-DISCOVERY DISC-1: verified 0 occurrences of `N/A`/`<declare>` in 472)** — the doctrine paragraph states them as the emitter's behavior a reader will see, attributed to the emitter, and does not cite 472 for them. THEN add a `## Cycle Manifest` example block into the §3 code-fence example, immediately after its Closing line — use lowercase/described tokens so no gate string is introduced (V1).
>
> **Amendment B — Rule C refinement (Option A). Two count-1-asserted anchors.**
> - **B1 (L210):** anchor `Do not keep a running fold-count in the Cycle Log — fold counts belong in the compact per-lens lines (e.g., \`w1 2 folded; w2 dry\`), not as a separate running tally.` — assert occurrence == 1, then replace with this PINNED, markdown-BALANCED text (from 472 gap (c), so two agents write identical doctrine — panel-DISCOVERY DISC-7; balance verified — panel-EXECUTION EXEC-2). The replacement doctrine sentence is exactly:
>   Do not keep a running fold-count in the Cycle Log **during drafting** — fold counts belong in the compact per-lens lines (e.g., `w1 2 folded; w2 dry`), not as a separate running tally. The sole exemption is the post-close `## Cycle Manifest` stanza's computed `yields:` field: a BAR_MET convergence summary, machine-emitted after the cycle closes, not a running tally that steers the walk.
> - **B2 (L194):** anchor `§3 forbids a running fold-count in the log and mandates the compact per-lens form, and that prohibition is **unchanged**.` — assert occurrence == 1, then replace with exactly:
>   §3 forbids a running fold-count in the log **during drafting** and mandates the compact per-lens form; the sole exemption is the post-close manifest's computed `yields:` field (§3).
>   ⚠️ **The replacement DROPS the words "prohibition is unchanged" (panel-EXECUTION EXEC-1, ship-blocker): the prohibition is NOT unchanged — it is now drafting-scoped — and the F4 verification asserts that exact literal is 0 in the doctrine post-edit, so B2's replacement must not carry it.** ⚠️ **B2 refines ONLY that trailing sentence — L194 is the §2.8 region-tally bullet, whose PRIMARY function (the drafting-time deletion tally lives in the WALK REGISTER, not the Cycle Log) must be preserved intact (panel-DISCOVERY DISC-8); the anchor is exactly that one sentence, do not touch the rest of the bullet.**
> - ⚠️ Scope the loosening NARROWLY (D1) — do not make any OTHER running fold-count permissible.
>
> **Verification (§2.7 subtractive-cut — the diff-review that proves the loosening bounded):** after the edits, `git -C /Users/marklehn/Developer/GitHub diff HEAD -- DRAFTING_CYCLE.md` (working tree vs HEAD) must show ONLY the six expected CHANGES — the §3 stanza paragraph, the §3 example-stanza addition, B1 (L210), B2 (L194), the Version-line bump (L5), and the new History row — with NO removal of any §3 rule and the Rule C loosening bounded to `yields:` (every other "running fold-count" prohibition intact). ⚠️ **The version + History edits are EXPECTED changes, not just A+B (walk-4 W5)** — do not flag them as unexpected. ⚠️ **Assert by CONTENT present, not by hunk-BLOCK count (walk-5 W6): the adjacent §3 edits (B1, stanza para, example) sit within a few lines and git will coalesce them into fewer diff hunks than six** — a block count would false-alarm. ⚠️ **Use `git diff HEAD`, NOT bare `git diff`** — bare `git diff` is working-tree-vs-INDEX and shows NOTHING once the file is `git add`-staged (verified: 0 lines staged vs 10 for `diff HEAD`), so it would falsely read as "no change" if verification runs after staging. ⚠️ **Do NOT use `git show acce7eb:DRAFTING_CYCLE.md` — `acce7eb` is a BLOB sha, not a commit, so the `<rev>:<path>` form errors** (`fatal: … exists on disk, but not in 'acce7eb'`, verified); `git diff HEAD` is the correct pre-edit instrument (or `git show acce7eb` for the raw pre-edit blob). Assert `grep -Fo 'Cycle Manifest' DRAFTING_CYCLE.md | wc -l` increased from 0. ⚠️ **Assert the CHANGE landed, not just that text was added (cold-scout F4, [[assert-the-post-condition-not-the-artifact]]):** `grep -Fc 'prohibition is **unchanged**' DRAFTING_CYCLE.md` must be **0** post-edit (1 pre-edit); B2's replacement must NOT reintroduce the literal. **Doctrine-integrity check (W3):** confirm the markdown is well-formed — the §3 example code-fence is balanced, headings intact, the `## Cycle Manifest` example sits inside the fence. ⚠️ **Do these checks fence-AWARE and content-based (panel-EXECUTION EXEC-3/EXEC-4):** the D1 "loosening bounded" check is per-content, NOT a raw `grep -Fc 'running fold-count'` count (the mandated History row mentions the phrase, so the raw count rises — read the two Rule C sites specifically); and a real-H2 check must exclude the in-fence `## Cycle Manifest` example (a bare `grep -Fc '## '` over-counts by the fenced example + prose mentions). ⚠️ **No plan_lint/cycle_check run belongs here: neither script READS `DRAFTING_CYCLE.md` at runtime** (only WARN-citation strings name it — verified 0 functional reads, per the v2.2 History measurement), so editing doctrine cannot change a gate's behavior and a plan_lint run would be a null control. The §3↔§4 lockstep (§6) is already discharged by 2b's shipped stanza-check tests. Bump the **Version line (L5)**: `2.11 (2026-08-14)` → `2.12 (<dispatch date>)`. ⚠️ **PREPEND** the new History row at the TOP of the `## History` section — immediately after the `## History` heading, ABOVE the `2.11` row — because the History is **newest-first** (the 2.10 row states so); "append" would misplace 2.12 at the oldest end (walk-4 D3). ⚠️ **The row MUST follow the recent-amendment (2.x) row convention — panel-DISCOVERY DISC-3/4/5** (verified: the 13 recent rows all carry these): (a) the units changed (§2.8 L194, §3 L210 + stanza paragraph + example); (b) the version bump as a changelog ordinal; (c) the declared §6 deviation in the v1.5/1.6/1.7 CEO-routed form, with the **no-DB-flip / `proposed`=0, no batch** note (this is not a lessons-forge codification); (d) the **§6 coordinate-doctrine-and-gate discharge** — no NEW gate edit (the stanza gate shipped in 2b/474), §4 stays in lockstep; and (e) an **`Inheritors:` clause** — every future cycle, plus the depositor (component 3) and capstone (component 4) that consume the mandated stanza.
>
> **Commit (governance root, V2):** `cd /Users/marklehn/Developer/GitHub && git rev-parse --show-toplevel` (assert == the GitHub root), `git add /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, then commit `docs(drafting-cycle): §3 Cycle Manifest mandate + Rule C drafting-phase scope — DC v2.12 (component 2a) [<id>]`.
>
> **Deposits:**
> - `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (in-place, governance root — absolute operand)
