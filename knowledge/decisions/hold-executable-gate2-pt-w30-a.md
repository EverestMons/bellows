# bellows — executable: DOCTRINE — Gate 2, thread 336, PT tranche one: proposals 442, 443, 458, 461, 472, 476, 477, 490, 498, 523, 589, 590 onto PLANNER_TEMPLATE.md v4.112 by a committed builder (Rules 112–115, four extensions, Rule 31 rewritten, three live contradictions swept), then the twelve flip `implemented` — 74 accepted rows remain for the six tranches after it

**Date:** 2026-09-15 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full (the bellows suite is UNCHANGED by this plan and is run once by QA so the test gate reads a real summary line; the gate reads FAILURES against `known_failures`, never the pass count — P7 is the expectation, and bellows `main` moves as plans merge) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Discharges:** thread 326 | **Priority:** 2

**auto_close:** false

**Slug:** `gate2-pt-w30-a-2026-09-15`

**Depends on:** the CEO's rulings of 2026-09-14 (thread 336 — one Gate 2 over the 86 accepted proposals, W=29 and W=30 together, superseding threads 101 and 327; the four W=30 disagreements settled by the first tranche's reader) and the CEO's delegation of 2026-09-15 ("Start on gate 2 and you have my delegation to release at the proper time"); the first tranche's read, `gate2-packet-2026-09-15.md` (governance `6cdd1aef` — the four rulings, the seven-tranche partition, tranche one first because it removes the live contradictions the Gate 1 readers named); the non-author Gate 1 packets — W=29's `gate1-packet-2026-09-02.md` (session `22366c52`; rows 442, 443, 458, 461 routed `codify` with their homes) and W=30's `gate1-packet-2026-09-13-a.md` (reader A; 472, 476, 477, 490, 498, and the two inserts 589 and 590 for entries 98 and 106) and `-b.md` (reader B; 523, with thread 326); `Done/executable-100026.md` in bellows (`gate2-pt-w28-b-2026-09-02`, closed 2026-09-02 — the clone origin and the newest same-class plan: doctrine prose onto `PLANNER_TEMPLATE.md` by a committed builder, then a scoped CAS flip in the lessons DB) and `Done/executable-540.md` in governance (the multi-site EXTENSION form); the builder `governance/knowledge/decisions/drafts/build-gate2-pt-w30-a-2026-09-15.py` (committed `c9edf8c6`, digest `3d96ad997d9a9b06`). Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-a-2026-09-15.md`.

**Tier computed, not judged (§1):** **T-6 fires** — `PLANNER_TEMPLATE.md` is the template → **T2**. T-2 fires (twelve canonical rows flip; caps at T1). T-1 fires (two repos, plus the DB). T-8 does NOT fire — a structure-for-structure clone of 100026 carrying 540's extension form; fourteen edits against nine, and three span rewrites against none, is scale, not shape. **Panel form — the magnitude proxy, measured at walk 0 (all three must hold): (i) structure-clone** — the parent is 100026 (closed 2026-09-02, the newest PT tranche), and both walk-0 clone-diffs ran (against 100026's final text and its register); **(ii) mechanized edits** — every template mutation flows through the committed builder, whose eleven anchors and three span bounds are count-1-asserted BY EXECUTION and whose forty-six post-conditions are asserted on the output before any write, with the line and byte deltas asserted against pinned constants (proven at walk 0: three refusals of the builder's own making corrected before its commit, a byte-identical rebuild, a `56	13` numstat, the byte pin firing on a length-changing one-byte mutant); the flip is one CAS statement under a structural pre-check; **(iii) scripted probes** — every new token measured 0 against the pre-edit file, the invariant tokens at their MEASURED pre-edit counts. **→ SMALL form: the walk-0 scout, then EXECUTION, then CAPSTONE.** The CEO may call the full form.

## Why this exists

Gate 1 routed 86 proposals `accepted|codify` — W=29's 23 (2026-09-02) and W=30's 63 (2026-09-13) — and the CEO made their codification one Gate 2 (thread 336). The first tranche's read (`gate2-packet-2026-09-15.md`) partitioned them into seven tranches and put this one first: its twelve rows carry the record-integrity rules and the LIVE CONTRADICTIONS the readers named — doctrine that still prescribes the pre-R-F3 deposit (Checklist 19 and Rule 24 (a)), the pre-declared-Scope gate (Checklist 14 and 22, Workaround 14) and the act 490 refutes (Rule 31). Their homes are the packets', re-verified against live v4.109 at authoring (every probe `/usr/bin/grep -c -i -F`; the zero rows the claim): **442** (entry 434) — two records of one fact diverge unless one is a projection → NEW Rule 112 (`projection` 0, `hand-edit` 0, `two records` 0); **458** (entry 450) — a ruling's promise can exceed any sound implementation → NEW Rule 113 (`promise` → 2, Rule 86 only; `supersed` non-History 3, none this claim); **461** (entry 453) — a migration drops the old substrate's free guarantees → NEW Rule 114 (`substrate` 0, `system of record` 0); **443** (entry 435) — the machine that is not working is the cold reader; a non-author read returns a packet → NEW Rule 115 (`cold read` → 1, the numbers-discipline sense; `idle machine` 0; `active claim` 0); **472** (entry 464) — doctrine states the outcome a function produces, never its decision order → Rule 101, an extension paragraph (`branch order` 0, `decision order` 0, `second implementation` 0); **476** (entry 468) — a refuted remedy left standing in the corpus gets rebuilt → Rule 102, an extension (`prescriptive` 0, `refuted remedy` 0); **477** (entry 469) — declining to fix a limitation is not the same as not seeding it → Rule 95, an extension (`self-appl` 0); **498** (entry 490) — a lesson codified into doctrine reads as discharged while its mechanism may never be built → Rule 47's forge family, an extension (`UNCARRIED` 0 in PT; `two acts` 0); **490** (entry 482) — an untracked file inside a submodule marks the superproject modified → Rule 31 REWRITTEN (`ls-files -s` 0, `ls-remote` 0, `retired-` 0; the rule's `cd ~/Developer/GitHub` literal, which thread 113's v4.102 sweep did not reach); **523** (entry 515; thread 326) — a lane filename is the state machine's input; `ready-` is the deposit act → Checklist 19 and Rule 24 (a) REWRITTEN to the receipt-first `ready-` deposit, cited to bellows `CLAUDE.md`, MACHINE_SETUP §6 and STANDING_CONSTRAINTS line 3 (`` `ready- `` → 1, the R-F3 paragraph; `no_clearance` 0; `deposit_receipt` 0); **589** (entry 98's rider of 2026-09-08, thread 207) — under a declared Scope a path is named in Scope or Deposits and prose authorizes nothing → Checklist 14 REWRITTEN and Checklist 22's distinction sentence follows, both cited to Checklist 23; **590** (entry 106's rider of 2026-09-08, thread 208) — the evidence-directory false positive closes by declaration → Workaround 14 REWRITTEN to the declaration form, the override retired (proposal 111 already `superseded` at the Gate 1 flip). **Homes:** four NEW Orchestration Plan Rules **112, 113, 114, 115** (after Rule 110, before the Lifecycle DB Read Protocol — numbering append-only; Rule 111 keeps its headless form inside Rule 77's section until tranche two's 484 gives it a heading), FOUR extension paragraphs appended after the home's existing last line (540's form), each opening in bold with its proposal, entry and `codified 2026-09-15 — Gate 2, thread 336, PT tranche one`, ONE rewrite (Rule 31's body) and THREE sweeps (Checklist 19 with Rule 24 (a); Checklist 14 with Checklist 22's sentence; Workaround 14), each carrying both sources — the superseded text's and this tranche's — and the `Last Updated` line, stale at v4.98 under eleven later versions, corrected.

## What this plan does

**In the governance checkout, by absolute path (`GOV=/Users/marklehn/Developer/eluvian-governance`), fourteen anchored edits to `PLANNER_TEMPLATE.md` by the committed builder (never by hand):**
- **E1** — four new rule blocks (112, 113, 114, 115), each ending in its `*Source: …*` line and a `---`, inserted between Rule 110's closing clause (f) and the `## Lifecycle DB Read Protocol (Planner)` heading (anchor: (f)'s last words with the heading, count 1; the builder supplies the `---` Rule 110 lacks).
- **E2** — after Rule 101's Source line, the 472 paragraph. **E3** — after Rule 102's Source line, the 476 paragraph. **E4** — after Rule 95's Source line, the 477 paragraph. **E5** — after Rule 47's prop-387 extension (its closing `*(Proposal 387, entry 379, codified 2026-08-26.)*`), the 498 paragraph.
- **E6** — Rule 31's body line replaced whole (anchor: the whole line, count 1). **E7** — Checklist 19 replaced as a span from its heading through its closing words and Source line (the bare Source line occurs twice in the template; the span's end bound carries the item's last sentence). **E8** — Rule 24 (a) replaced whole. **E9** — Checklist 14's body and Source replaced as a span. **E10** — Checklist 22's `Distinction from item 14` sentence replaced. **E11** — Workaround 14 replaced as a span from its heading through its Source line.
- **E12** — `**Version:** 4.111` → `**Version:** 4.112`. **E13** — `**Last Updated:** 2026-09-02 (v4.98)` → `**Last Updated:** 2026-09-15 (v4.112)`. **E14** — the History row inserted directly under the `| Date | Lesson |` header (the v4.105 row that sits there survives below it).

The builder reads the file as bytes, refuses CRLF, refuses an already-built input, asserts each single anchor's count is 1 and each span's two bounds count 1 and in order, applies the fourteen edits, asserts forty-six post-conditions on the OUTPUT (the builder's `POST` list: the four new headings once each; `### 111. ` 0; the five new-rule-form source strings and the nine extension-form strings at 5 and 9; the four extension openers once each; the rewrites' new phrases 1 and their retired phrases 0; the v4.112 tokens 1 and the v4.111 and v4.98 tokens 0; the new History row 1; the section headings and the four extension anchors surviving at their MEASURED pre-edit counts — `### 14. ` at 3, Rule 14, Checklist 14 and the substring of Workaround 14's `#### 14. `; `### 19. `, `### 22. `, `### 23. `, `### 24. `, `### 31. ` at 2; `### 17. ` at 2), asserts the line and byte deltas against PINNED constants (`EXPECTED_LINES = 43`, `EXPECTED_BYTES = 23283` — a post-condition that can fail, proven at walk 0 on a length-changing one-byte mutant) and only then writes — to a SCRATCH path it refuses to let be the live file or anything under the governance root or the input's git toplevel. Apply = copy the proven scratch output over the live file, then re-measure the live file.

**In the lessons DB (`/Users/marklehn/Developer/forge_lessons/lessons-forge.db`, the mini's one live copy), AFTER the doctrine commit:** one scoped CAS flip — `id IN (442,443,458,461,472,476,477,490,498,523,589,590) AND status='accepted' AND route='codify'` → `implemented`, `status_updated_by='ceo'` (the delegation's record), a fresh UTC stamp — under a structural pre-check (a TEMP-TABLE `CHECK(x=12)` that aborts the transaction before the UPDATE if the twelve are not all `accepted|codify`), with the capture of every row with `id <= 590` — the whole table at authoring — `(id, status, route, category, status_updated_by, status_updated_at)` to a SCRATCH file BEFORE the UPDATE (copied into the worktree's evidence dir afterwards — never written into the canonical checkout), and the sentinels by name: `PRE_F=12`, `CHANGES_F=12`, `EXCL_F=12` (the twelve rows now `implemented` with a stamp that is NOT one of the TWO vintage stamps — four of the twelve share W=29's `2026-09-02T23:54:37Z` and eight W=30's `2026-09-13T18:27:07Z`, so EXCL_F is a two-value exclusion where 100026's was one), `ACC_POST=74`, `IMPL_POST=345`. The flip SQL is a committed file in governance; a backup of the DB precedes it.

**In the bellows worktree:** the dev log and the QA evidence.

## What this plan does NOT do

- Does not touch the seventy-four `accepted|codify` rows of tranches two to six and the DC/PST tranche (their homes are named in the packet — each is its own plan), DRAFTING_CYCLE.md, PANEL_SEAT_TEMPLATE.md, COMPANY.md, MACHINE_SETUP.md, GLOSSARY.md, code, or threads. Does not push (the Planner pushes after the pause; the Planner's Rule 22 (b) read at the pause takes the canonical block's stdout and the daemon's gate row as the receipt's evidence, never the QA's `PRECHECK: 0 failure(s)` line — at a QA step `tools/check_deposit.py` verifies presence alone, `_gate_is_qa_step` called without the plan header in `tools/check_deposit.py`'s `is_qa_step` line (thread 362)). Does not project the `LESSONS.md` markers (the Planner's act after the close — `scripts/project_status_markers.py --apply`, the Rule 112 instrument). Does not close thread 326 or 336 (the Planner's `threads done`, after the close; 336 closes with the seventh tranche).
- ⚠️ **Runs after the Gate 1 W=30 flips (2026-09-13, forge_lessons `8341c60`) and BEFORE any W=31 ingest or another tranche** — A0's ladder asserts the accepted population (86 before this plan's flip, 74 after) with every accepted row at one of the two vintage stamps; another tranche landing first moves it, and a W=31 ingest could stale an accepted row (`accepted` is not terminal — `src/lessons_forge.py:31`). The `[status: pending]` headings written since W=30's ingest (37 at authoring, 38 at the panel's close) wait.
- ⚠️ **PLANNER_TEMPLATE's version is moved by the CEO's doctrine acts at other plans' closes** (thread 341's plan names v4.110 too, thread 333's and 322's the versions after): the re-pin surface is the PIN TABLE and its restatements — P1 (sha, lines, bytes); P2 (every line number, which a doctrine act above the anchors shifts, and its `**Version:**` anchor); P4 (the builder's digest, after its two constants `OLD_VERSION`/`NEW_VERSION` are re-committed); P5 (`wc -l`, `wc -c`; the BUILT line's deltas hold unless an act lands inside a span, which the byte pin refuses); Task C's four version tokens; the `wc -l` restated in A2, A3, both RE-ENTRY arms and QA Item 1 and the `wc -c` in A2; every literal of the new version (fifteen: the title, the E12/E13 line, the POST description, MUST-PRESERVE, P3, walk 0's landing clause, the RE-ENTRY text, Task C, the THREE commit subjects) and the six pre-edit-state literals (E12's, the POST description's, P1's, P2's anchor, P3's, Task C's zero probe); the register's Targets line and title — re-pinned TOGETHER at the freeze and before the release by ONE scripted pass over those sites, recorded in the register with each literal's count before and after, and Task C's thirty probes re-run on a scratch build of the new version (done once, at this freeze, after thread 333's act — the CAPSTONE seat's re-pin table); the historical mentions — walk 0's measurement basis, the Numbers heading, the acts named at other plans' closes — are history and keep their versions, so no count of a retired literal is the proof; so the deposited plan carries the version it will find (a partial re-pin halts A1 on P2, or A3 discards the proven build on `wc -l`); a moved version at A0 is a NONE-MATCH halt and a redeposit, never a guess.

## MUST-PRESERVE

- ⚠️ **TWO REPOSITORIES AND A DB, ONE STEP.** Governance by absolute path, `git -C "$GOV"` never `cd`; porcelain EMPTY and sha = P1 before touching `PLANNER_TEMPLATE.md`; commit by explicit pathspec with the message on stdin; no push. The DB by absolute path; `sqlite3` with `-bail`; a backup (`pre-gate2-pt-w30-a-<HHMMSS>.db` at the forge root, untracked) BEFORE the flip.
- **The builder is the only editor of the template.** Its committed blob digest must equal the on-disk file (P4) before it runs; it runs scratch→scratch; the live file is replaced only by `cp` of a scratch output whose post-conditions the builder already asserted, and the live file is then re-measured (Task C).
- **Doctrine before DB (540's C6):** the flip runs only after the governance commit has landed; a HALT between leaves v4.112 committed-unpushed and the flip owed — the RE-ENTRY arm in A0 resumes at the flip.
- ⛔ **Never run any `checkout --` on an A0 halt except the ONE recipe named there** (a pre-commit apply that died, discriminated by the HEAD numstat, the porcelain and the `### 112. ` token); a pin mismatch otherwise means the file carries changes that are not this plan's.
- ⚠️ **ONE landed text carries a Rule 20 hedging keyword** — Rule 112's body names the file-side marker value its own subject is about, measured by scanning the builder's output diff against the canonical block's list (walk 0) — so NO receipt row quotes a landed paragraph's body; Task C pins heading fragments and opener fragments only (100026's X-2 / CP-1 class).
- **`known_failures: 0`.** The suite runs in the worktree under the canonical venv; the QA reads its own run's failure count, and a bellows `main` that gained tests since P7 is stated in `probes-raw.txt` under Item 4 (the probe's command and its commit list; the summary line stays in the suite file), never in the receipt's row and never halted on. Any failure is a HALT/Critical.
- ⛔ **Every commit in the bellows worktree chains `tools/check_deposit.py` before `git add`** (Rule 23; and the step IS bound — thread 333's plan merged (#100105, main `2403ceba`) and the daemon restarted on it at 12:17 on 2026-09-15 — so the hook refuses a commit without a fresh pass; both chains below were rehearsed under it by the CAPSTONE seat (the controls refused, the plan's chains admitted)), and puts its message on stdin (`git commit -F -` with a quoted heredoc), never in a file.

## Numbers discipline — the pins DEV re-derives (measured 2026-09-15 by the Planner on PLANNER_TEMPLATE v4.109 at governance `8fc569f4`, bellows main `f9e35f36`, forge_lessons `a1ebf45`; P1, P4 and P5 re-measured after the scout's fold on v4.110 at governance `bdb32da6` / `f2248ea5`; P1, P2, P4, P5 and the version literals re-pinned at the freeze on v4.111 at governance `eade35b2` / `c9edf8c6`, thread 333's act having moved the version again)

| pin | what | value | how |
|---|---|---|---|
| P1 | **`PT_SHA`**, lines, bytes — pre-edit (v4.111, after bellows #100105's doctrine act of 2026-09-15 — re-pinned at the freeze) | `6846b5a2da16b352`; 2515 lines; 499,815 bytes | `shasum -a 256 "$GOV/PLANNER_TEMPLATE.md" \| cut -c1-16`; `wc -l`; `wc -c` |
| P2 | **`ANCHORS`** — the builder's eleven single anchors and three spans | each count 1: Rule 110 (f)'s closing words with the protocol heading · `*Source: proposal 412, entry 404, codified 2026-08-26 (Gate 1: CEO — author-conflict entry).*` (line 1423) · `*Source: proposal 413, entry 405, codified 2026-08-26 (Gate 1: CEO — author-conflict entry).*` (1429) · `*Source: proposal 324, entry 316, codified 2026-08-12 (Gate 2, the PLANNER_TEMPLATE three)*` (1393) · `*(Proposal 387, entry 379, codified 2026-08-26.)*` (1044) · Rule 31's body line (888) · Rule 24 (a)'s line (725) · Checklist 22's distinction sentence (inside 1657) · `**Version:** 4.111` (5) · `**Last Updated:** 2026-09-02 (v4.98)` (6) · the History header pair (2300–2301; the v4.111 row sits at 2304, below the v4.107 row, never under the header); the spans: Checklist 19 (1637 → its closing words + Source, 1639–1641), Checklist 14 (1605 → 1607), Workaround 14 (2023 → 2027) — each bound count 1 (the bare `Source: Reporting Phase 1, 2026-06-11` counts 2, which is why Checklist 19's end bound carries its closing words) | `/usr/bin/grep -cF` (the multi-line anchors and the spans by the builder's own assertion) |
| P3 | **`TOKENS`** 0 before, their counts after | `### 112. ` · `### 113. ` · `### 114. ` · `### 115. ` · `codified 2026-09-15 (Gate 2, thread 336 — PT tranche one)` · `codified 2026-09-15 — Gate 2, thread 336, PT tranche one` · `**Doctrine states the OUTCOME a function produces` · `**A refuted REMEDY left standing in the corpus` · `**A step body has one reader and MANY copiers` · `**A lesson codified into doctrine reads as DISCHARGED` · `says DIRTY, never MOVED` · `### 19. The deposit is the receipt, then the` · `Write-to-temp, receipt, then the` · `and step prose authorizes NOTHING` · `on plan-required evidence files — declare the directory` · `**Version:** 4.112` · `\| 2026-09-15 \| v4.112: Gate 2, thread 336` — each 0 before, 1 after but the two codified strings (5 and 9); retired after: `the Planner override per Rule 22(d) is the correct disposition`, `confirm the destination filename matches`, `cd ~/Developer/GitHub && git --no-pager status`, `file PATHS must be inlined in step bodies (scope_check requirement)`, `**(a) Write-to-temp + move.**`, `**Version:** 4.111`, `**Last Updated:** 2026-09-02 (v4.98)` — each 1 before, 0 after; invariants: `### 14. ` 3, `### 19. ` 2, `### 22. ` 2, `### 23. ` 2, `### 24. ` 2, `### 31. ` 2, `### 17. ` 2, `### 110. ` 1, `#### 14. ` 1, `### 111. ` 0 (unchanged by this plan) | `/usr/bin/grep -cF` |
| P4 | **`BUILDER`** — on-disk digest = the blob at its OWN last commit | `3d96ad997d9a9b06` (governance `c9edf8c6`) | `shasum -a 256 <builder> \| cut -c1-16`; `BC=$(git -C "$GOV" log -1 --format=%H -- governance/knowledge/decisions/drafts/build-gate2-pt-w30-a-2026-09-15.py); git -C "$GOV" show "$BC:governance/knowledge/decisions/drafts/build-gate2-pt-w30-a-2026-09-15.py" \| shasum -a 256 \| cut -c1-16` |
| P5 | **`DRY_RUN`** — the success line, numstat, sizes | `BUILT: <out> edits=14 blocks=4 lines+43 bytes+23283 post=46/46 deltas=pinned`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" <out>` → `56	13` (exit 1 is the expected differing state — read the numbers, never the exit); `wc -l <out>` → 2558; `wc -c <out>` → 523,098 | as stated, scratch-only |
| P6 | **`FLIP_PRE`** — the twelve rows | 442, 443, 458, 461 each `accepted\|codify\|planner\|2026-09-02T23:54:37Z`; 472, 476, 477, 490, 498, 523, 589, 590 each `accepted\|codify\|planner\|2026-09-13T18:27:07Z`; `SELECT COUNT(*) … WHERE status='accepted'` → 86; `implemented` → 333; `SELECT status_updated_at, COUNT(*) … WHERE status='accepted' GROUP BY 1` → exactly two rows, `2026-09-02T23:54:37Z\|23` and `2026-09-13T18:27:07Z\|63`; `SELECT MAX(id), COUNT(*) FROM lesson_proposals` → `590\|590`; proposal 111 `superseded` | `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db` |
| P7 | **`SUITE`** — the expectation | `2395 passed, 2 skipped`, exit 0 — bellows main `f9e35f36` (#100103's QA, 2026-09-15, in a worktree); the gate reads failures, and `main` moves | `BPY -m pytest tests -q -p no:cacheprovider` |
| P8 | **`NO_INGEST_NO_TRANCHE`** (precondition, in two halves) | UNCONDITIONAL (before the ladder): `SELECT COUNT(*) FROM lesson_entries` → 580 (no W=31 ingest) AND `ls /Users/marklehn/Developer/forge_lessons/knowledge/decisions/ \| /usr/bin/grep -cF -- 'forge-cycle-w31'` → 0 (a zero-match count prints 0 and exits 1 — read the number). ARM-DEPENDENT (ladder step (5)): the accepted population with its at-the-vintages count — `86\|86` on FRESH and flip-owed; `74\|74` on complete — the twelve at their vintages on FRESH and flip-owed (ladder (4)) | as stated |

## Drafting Cycle

**Tier:** T2 — T-6 (the template), T-2 (twelve canonical rows, capped), T-1 fire; T-8 does not (a structure clone). **Panel: SMALL form** (the magnitude proxy measured at walk 0, all three conditions holding) — the walk-0 scout, then EXECUTION, then CAPSTONE; the CEO may call the full form.

**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-a-2026-09-15.md

**Walk 0 (context pin, measured):** the template's sha, lines and bytes; the eleven anchors and three span bounds counted (the four Source-line anchors at lines 1042, 1391, 1421, 1427 of v4.109; the rewritten lines 723 and 886; the spans 1635–1639, 1603–1605, 2021–2025); every new token counted 0 and the invariant tokens at their measured pre-edit counts; the twelve proposals' rows and the two vintage stamps the accepted population carries; the packets' dedup rows re-verified token by token against live v4.109; the builder executed scratch-to-scratch — three refusals of its own making corrected before its commit (the W=28 E1 anchor gone with Rule 110's missing separator, a Source line that occurs twice, an invariant token shared by three headings), then the success line, the three refusals, a `56	13` numstat, a byte-identical rebuild, the block positions (1044, 1395, 1427, 1435, 1513, 1521, 1529, 1537, 1677, 2063, 2342 in the output), the byte pin firing on a length-changing one-byte mutant and on a pins-off-by-one builder — and its on-disk digest against its committed blob; the flip SQL executed on a scratch `.backup` copy of the live DB — the five sentinels by name, the 590-line capture with 86 `accepted|codify` rows, the fresh read-back, and the already-flipped re-run aborting at the CHECK; the clone-diff against 100026's final text and register in three passes; the consumer dry-run (§2.0) recorded in the register; the walk-0 cold scout convened on v0 (`2c8d00aa`) before lens 1 — fourteen findings (HIGH 0, MED 6, LOW 8; instruction 8, record 5, question 1; none DIRECTION-class; every pin it re-ran matched, the flip rehearsed a second time on its own backup copy), all author-verified and folded before lens 1 (v1): eleven findings folded into the builder at nine sites (s1–s10, s14: the Rule 113 citation, the Rule 31 mini clause and its verification, Checklist 19's held class and its restated runbook, the Rule 95 extension's covered clause, Rule 115's mechanization sentence, the Rule 47 extension's checkout, Checklist 14's source date, Rule 24 (a)'s example, Workaround 14's retirement date, the vocabulary of the Air's layout), the builder re-committed and re-pinned, and three into the record (s11–s13); the version pinned moved under the draft at bellows #100104's close (v4.109 → v4.110, the CEO's doctrine act of 2026-09-15) and again at #100105's (v4.110 → v4.111), so the tranche lands v4.112 — P1, P2, P4, P5 and the literals re-pinned at the freeze by one scripted pass (the CAPSTONE seat's re-pin table).

- Weak spots:          w1 dry; w2 2 folded — instruction 1 / record 1; w3 dry; w4 dry
- Destruction:         w1 dry; w2 dry; w3 dry; w4 dry
- Vulnerabilities:     w1 dry; w2 dry; w3 dry; w4 dry
- Integration-record:  w1 1 folded — instruction 0 / record 1; w2 dry; w3 1 folded — instruction 0 / record 1; w4 dry
- ACID:                w1 dry; w2 dry; w3 dry; w4 dry
**Cold panel (SMALL form; meter opened at the scout's dispatch 16:05Z; convened after walk 1's one record fold — EARLY under §2.6, the parent's cadence):** SCOUT (lens 4, local subagent, 321,435 tokens, 39 tool uses) — 14 findings, 0 HIGH / 6 MED / 8 LOW, instruction 8 / record 5 / question 1, 14 folded (v1 `d35b0f85`; the builder `f2248ea5`) · EXECUTION (lens 3 + the execution brief, local subagent, 372,683 tokens, 53 tool uses) — 5 findings, 0 HIGH / 1 MED / 4 LOW, instruction 1 / record 4, 5 folded (`2c31f46f`; two of the fold's sentences corrected at walk 2, `0c58cefb`) · CAPSTONE (the fold set, local subagent, 462,984 tokens, 46 tool uses) — 7 findings, 0 HIGH / 3 MED / 4 LOW, instruction 1 / record 6, 7 folded with the freeze re-pin (`728b4663`). The cycle closes on a fully dry walk after the seats' folds.
**Closing:** ✅ **BAR MET — walk 4 dry (all five lenses) after walk 1's one record fold, walk 2's two folds (the seat-2 fold's own sentences, corrected on its first re-read), walk 3's one record fold, and three cold reads folded in full — the walk-0 SCOUT (14 findings, v0 → v1 before lens 1), the EXECUTION seat (5, folded after walk 1) and the CAPSTONE seat (7, folded with the freeze re-pin after walk 2): 26 findings, 26 folded, none DIRECTION-class, none questioning a ruling.** The yield 1 → 2 → 1 → 0 (walk 2's rise taken under `--allow-yield-rising`, the two findings being the panel fold's corrections; the Cycle Log's walk 1, lost by rewriting instead of appending, restored at the CAPSTONE fold — thread 367). The template moved under the draft twice (v4.109 → v4.110 at bellows #100104's close, v4.110 → v4.111 at #100105's — thread 333's act, 12:17 on 2026-09-15); the tranche lands **v4.112**, the freeze re-pin performed by one scripted pass at the CAPSTONE fold and recorded with before/after counts: the builder `c9edf8c6` (digest `3d96ad997d9a9b06`) builds the pinned deltas on the live v4.111 file (`BUILT: … edits=14 blocks=4 lines+43 bytes+23283 post=46/46 deltas=pinned`, numstat `56	13`, 2558 lines, 523,098 bytes, the four version tokens, BYTE_IDENTICAL on the rebuild, the already-built refusal), P1 `6846b5a2da16b352` / 2515 / 499,815, P2 at +2, Task C 30/30 on the scratch build (the CAPSTONE), the flip rehearsed three times on backup copies (walk 0, the scout, the CAPSTONE). Both bellows chains rehearsed under the commit binding live since 12:17 (the CAPSTONE: the controls refused, the plan's chains admitted). Conformance at the close: `plan_lint` exit 0 / 0 FAIL (four `(f)` for the validation keys the close writes, five `(o2)` relative deposits, `(o1)` path census 11/7/0, three `PIN-CHECK` ambiguous prefixes, the earned closing WARN until this line); `cycle_check` `CONTINUE` with BAR_MET downgraded for the four validation keys alone; `lens_order_check` OK — 20 lens commits across walks 1–4; `walk_register_lint` SHAPE-OK / COVERED; `fold_check` VACUOUS at the close (the baseline re-saved after every intended edit); `propagation_check` DIVERGENT:163 read whole — restated pin values at the sites the re-pin sentence names (P1's sizes, P2's positions, P5's sizes, the version literals, the `wc` restatements), no contradiction; the class parse on the final text → `shop-infra`, 8 writes, the per-step deposits as the register's dry-run. Threads filed from the panel: 362, 363 (EXECUTION), 367 (CAPSTONE). The deposit: receipt first, the class parsed by the depositor's own tool, one byte-identical `ready-` copy, held on `class:shop-infra` for the release under the CEO's Gate 2 delegation — released when no plan is in flight (one at a time; #100106 running at the close).

## Cycle Manifest
tier: T2
target: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md
class: shop-infra
reads: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-pt-w30-a-2026-09-15.py, /Users/marklehn/Developer/eluvian-governance/gate2-packet-2026-09-15.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-02.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-a.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-b.md, /Users/marklehn/Developer/eluvian-governance/LESSONS.md, knowledge/decisions/Done/executable-100026.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-540.md, /Users/marklehn/Developer/forge_lessons/lessons-forge.db
writes: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30a-flip.sql, /Users/marklehn/Developer/forge_lessons/lessons-forge.db, knowledge/development/dev-log-gate2-pt-w30-a-2026-09-15.md, knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt, knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/qa-receipt.md, knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/probes-raw.txt, knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/full-suite-gate2-pt-w30-a.txt
open_forks: 1. the seventy-four accepted rows of tranches two to six and the DC/PST tranche (their own plans, in the packet's order); 2. Rule 111 stays headless until tranche two's 484 gives it its heading (W=30's hazard 4(b)); 3. Rule 24's patterns (b) and (c) keep their single-write text — the deposit act now governs the NAME and the receipt whatever the write pattern, stated in (a); 4. the `LESSONS.md` markers are projected after the close by the Planner, not by this plan; 5. the GLOSSARY says both that `register-writing` auto-clears (`:21`) and that it is class-held (`:981`) — the depositor holds on `shop-infra` alone (`depositor.py:193`), which Checklist 19's text now states; the GLOSSARY pair is a record error routed here per Rule 102, not this plan's to patch (the walk-0 scout's SC-3)
walks: 4
yields: 0, 1, 0, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:163
coherence: 4/4 body walks named in the register (30 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-gate2-pt-w30-a.md.foldcheck.json

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the bellows Developer. ⚠️ Governance runs IN PLACE at `$GOV` (every operand ABSOLUTE); commits there by explicit pathspec; ONE action per compound. ⛔ Every commit's message goes to `git commit -F -` on stdin — a quoted heredoc ending the commit's own command: `<<'MSG'`, the message on the lines after it, `MSG` alone on the last — so no file is written for it. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** (`BPY`); the builder runs under `python3` as 100026's did (it imports only `os`, `sys` and `subprocess`).
>
> ⛔ **A0 — roots, the precondition, then the branch ladder (catch-all LAST):** `cd "$(git rev-parse --show-toplevel)" && [ -f bellows.py ] && [ -d tests ] && echo TREE_OK` — HALT unless TREE_OK; `GOV=/Users/marklehn/Developer/eluvian-governance; PT="$GOV/PLANNER_TEMPLATE.md"; B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-a-2026-09-15.py"; DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db; [ -f "$PT" ] && [ -f "$B" ] && [ -f "$DB" ] && echo GOV_OK` — HALT unless GOV_OK. Re-derive `GOV`, `PT`, `B`, `DB` in every compound. `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python`. **P8's UNCONDITIONAL half → both values, else HALT** (`lesson_entries` → 580 and the `forge-cycle-w31` count → 0; on a mismatch: `a W=31 cycle has started — re-pin P6/P8 and re-deposit`). **P8's accepted-population half is read INSIDE the ladder, never before it:** it changes when THIS plan's flip lands (100026's scout-Air S1a). Then the ladder — **(1)** P1's sha matches; **(2)** `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30a-flip.sql` (state it; EMPTY on FRESH); **(3)** `git -C "$GOV" log -1 --format=%s -- PLANNER_TEMPLATE.md` — does the subject carry `gate2-pt-w30-a`?; **(4)** `sqlite3 -readonly "$DB" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (442,443,458,461,472,476,477,490,498,523,589,590) ORDER BY id;"`; **(5)** `sqlite3 -readonly "$DB" "SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted')||'|'||(SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z'));"` — the accepted population and how many of it sit at the two vintage stamps (every accepted row does, on EVERY arm).
> - **FRESH** = (1) match AND (2) empty AND (3) no AND (4) the twelve `accepted|codify` at their vintages (four at `2026-09-02T23:54:37Z`, eight at `2026-09-13T18:27:07Z`) AND (5) `86|86` → A1.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` (a dirty template on re-entry is a FOREIGN edit — HALT untouched, quoting the porcelain) AND (4) the twelve still `accepted|codify` at their vintages AND (5) `86|86` → verify the COMMITTED state (the Task C counts on the live file, `wc -l` 2558, and `C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show --stat --format= "$C" -- PLANNER_TEMPLATE.md` → 56 insertions, 13 deletions — the PATH-SCOPED commit, never `HEAD`: once A4's SQL commit or any later governance commit lands, `HEAD` no longer names the template's commit and a `HEAD`-scoped show prints NOTHING with exit 0), then A4 (the flip) and A5 only. If (2) shows `g2ptw30a-flip.sql` untracked or modified with the flip NOT applied, that is this arm too — A4 rewrites the file from the exact text, which is idempotent.
> - **RE-ENTRY (complete)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` AND (4) the twelve `implemented|codify` with every stamp NOT one of the two vintages AND (5) `74|74` → verify the COMMITTED state (the Task C counts, `wc -l`, and the path-scoped show → 56/13); then the two idempotent tails of A4: the SQL file — if `git -C "$GOV" log -1 --format=%s -- governance/knowledge/development/g2ptw30a-flip.sql` does not carry `gate2-pt-w30-a`, write it from the exact text and commit it; the capture — if `/tmp/g2ptw30a/flip-capture.txt` exists copy it into the worktree, else reconstruct the worktree copy from the backup DB (`sqlite3 -readonly "file:<backup>?immutable=1" "<the capture SELECT>"` → the same 590 lines, labelled `RECONSTRUCTED (post-commit re-entry)` in the dev log); then A4's read-back (no flip) and A5 with the dev log marked `re-derived on re-entry`.
> - **NONE-MATCH** = anything else → HALT quoting every measurement — and when (5)'s SECOND number ≠ its first on ANY arm, the HALT reads `an accepted row carries a stamp outside the two vintages — an ingest or a foreign flip has moved the population; re-pin P6/P8 and re-deposit`; when (3) = no and (5)'s first number ≠ 86 with the two numbers equal, it reads `the accepted set moved — another Gate 2 tranche has landed first, or an ingest staled an accepted row (accepted is not terminal, src/lessons_forge.py:31); re-pin P6/P8 and re-deposit`; when (1) mismatches and (3) = no and the template's porcelain is EMPTY, it reads `PLANNER_TEMPLATE.md moved under this plan — a doctrine act landed after the freeze; re-pin P1 and the builder's version constants, and re-deposit`. The ONE recipe: (1) mismatch + (3) no + (4) the twelve `accepted|codify` at their vintages + (5) `86|86` = a pre-commit apply that died — BUT ONLY IF the dirty file is THIS plan's build: `git -C "$GOV" diff HEAD --numstat -- PLANNER_TEMPLATE.md` → `56	13` (against HEAD, never the bare index-relative `diff`, which prints NOTHING in the staged half-state this recipe exists for) AND `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md` → ` M` or `M ` (never `MM`: a staged foreign edit beneath an unstaged build would pass the HEAD numstat and be discarded) AND `/usr/bin/grep -cF -- '### 112. ' "$PT"` → 1 (a foreign hand edit to the template presents the SAME (1)+(3)+(4)+(5) shape and must never be discarded; any other numstat or porcelain → HALT untouched, quoting it) → `git -C "$GOV" checkout HEAD -- PLANNER_TEMPLATE.md` (`HEAD`, not the index — a death between `add` and `commit` leaves v4.112 STAGED), re-assert P1, then A1. Every other shape HALTs untouched.
>
> ⛔ **A1 — re-derive P1–P6; state each; a mismatch is a HALT quoting both.** (P4 with the builder's OWN commit, never `HEAD:`.)
>
> **A2 — dry-run, scratch→scratch (LITERAL paths, no variables across compounds):** `rm -rf /tmp/g2ptw30a; mkdir -p /tmp/g2ptw30a; python3 "$B" "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30a/PT-out.md; echo "builder_exit=$?"` → P5's `BUILT:` line verbatim, `builder_exit=0`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30a/PT-out.md` → `56	13`; `wc -l /tmp/g2ptw30a/PT-out.md` → 2558; `wc -c` → 523098. Refusals (each `BUILDER REFUSED`, nonzero; quote each): `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/PLANNER_TEMPLATE.md"` (out == in); `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/x.md"` (under a forbidden root — the literal governance root or the input's git toplevel; no `x.md` is written); `python3 "$B" /tmp/g2ptw30a/PT-out.md /tmp/g2ptw30a/PT-out2.md` (already built).
>
> **A3 — apply and measure the LIVE file (Task C), then commit:** `cp /tmp/g2ptw30a/PT-out.md "$GOV/PLANNER_TEMPLATE.md"`; then from the live file, counts read never exit codes (`/usr/bin/grep -cF -- '<token>' "$PT"`, each token as one single-quoted literal): `### 112. Two records of one fact DIVERGE unless one is a PROJECTION of the other` 1 · `### 113. A ruling` 1 · `### 114. A migration to a better substrate DROPS the old one` 1 · `### 115. The machine that is not working is the COLD READER` 1 · `codified 2026-09-15 (Gate 2, thread 336 — PT tranche one)` 5 · `codified 2026-09-15 — Gate 2, thread 336, PT tranche one` 9 · `**Doctrine states the OUTCOME a function produces` 1 · `**A refuted REMEDY left standing in the corpus` 1 · `**A step body has one reader and MANY copiers` 1 · `**A lesson codified into doctrine reads as DISCHARGED` 1 · `says DIRTY, never MOVED` 1 · `cd ~/Developer/GitHub && git --no-pager status` 0 · `### 19. The deposit is the receipt, then the` 1 · `confirm the destination filename matches` 0 · `Write-to-temp, receipt, then the` 1 · `**(a) Write-to-temp + move.**` 0 · `and step prose authorizes NOTHING` 1 · `file PATHS must be inlined in step bodies (scope_check requirement)` 0 · `on plan-required evidence files — declare the directory` 1 · `the Planner override per Rule 22(d) is the correct disposition` 0 · `**Version:** 4.112` 1 · `**Version:** 4.111` 0 · `**Last Updated:** 2026-09-15 (v4.112)` 1 · `| 2026-09-15 | v4.112: Gate 2, thread 336` 1 · `| 2026-09-09 | v4.105: CEO act at the close of bellows plan 100052` 1 (the row below the header survives) · `## Lifecycle DB Read Protocol (Planner)` 1 · `### 110. ` 1 · `### 111. ` 0 · `### 14. ` 3 · `### 17. ` 2 (unchanged); `wc -l` 2558; `cmp /tmp/g2ptw30a/PT-out.md "$GOV/PLANNER_TEMPLATE.md"` silent; `git -C "$GOV" diff --stat -- PLANNER_TEMPLATE.md` → 56 insertions, 13 deletions. Any failing → FRESH: `git -C "$GOV" checkout -- PLANNER_TEMPLATE.md` + HALT; RE-ENTRY: HALT, no restore. Commit, the message on stdin: `git -C "$GOV" add PLANNER_TEMPLATE.md && git -C "$GOV" commit -F - -- PLANNER_TEMPLATE.md <<'MSG'` with the subject `[<id from your plan filename>] gate2-pt-w30-a: PLANNER_TEMPLATE v4.112 — Rules 112-115 (442, 458, 461, 443), four extensions (472, 476, 477, 498), Rule 31 rewritten (490), three sweeps (523, 589, 590)` and `MSG` closing it; `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` → that commit. Do NOT push.
>
> **A4 — the flip (doctrine before DB):** backup first, by sqlite's own copy — the DB is WAL-journaled, so a filesystem `cp` can lag a non-checkpointed write while sizes match: `sqlite3 "$DB" ".backup /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-a-$(date +%H%M%S).db"`; then on the backup — ⚠️ through `sqlite3 -readonly "file:<backup>?immutable=1"`, never a bare `-readonly <path>`: a `.backup` output of a WAL database has no `-shm` sidecar and a bare read-only open fails `unable to open database file (14)` — `PRAGMA integrity_check` → `ok`; `SELECT COUNT(*) FROM lesson_proposals` → equal to the live count (state both); `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (442,443,458,461,472,476,477,490,498,523,589,590) AND status='accepted'` → 12 (`BK=12`). Write `$GOV/governance/knowledge/development/g2ptw30a-flip.sql` with a QUOTED heredoc, EXACTLY:
> ```
> .timeout 5000
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=12));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (442,443,458,461,472,476,477,490,498,523,589,590) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output /tmp/g2ptw30a/flip-capture.txt
> SELECT COALESCE(id,'')||'|'||COALESCE(status,'')||'|'||COALESCE(route,'')||'|'||COALESCE(category,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 590 ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (442,443,458,461,472,476,477,490,498,523,589,590) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (442,443,458,461,472,476,477,490,498,523,589,590) AND status='implemented' AND status_updated_at NOT IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z');
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> ⚠️ The `.output` path is the LITERAL scratch dir A2 created (`sqlite3` dot-commands take no variables) — never a path inside the canonical bellows checkout. Run: `[ -d /tmp/g2ptw30a ] || mkdir -p /tmp/g2ptw30a; sqlite3 -bail "$DB" < "$GOV/governance/knowledge/development/g2ptw30a-flip.sql"` (⚠️ `-bail` is LOAD-BEARING: with the capture dir missing it stops at `.output` and the transaction never commits; without it sqlite skips the failed dot-command and the flip COMMITS with no capture file) → the five sentinel lines BY NAME: `PRE_F=12`, `CHANGES_F=12`, `EXCL_F=12`, `ACC_POST=74`, `IMPL_POST=345` (a `CHECK` abort prints a line beginning `Runtime error near line 4: CHECK constraint failed: x=12` and writes nothing — HALT on that MESSAGE; never read the exit code through a pipe, it is the last command's). **Read-back on a FRESH read-only connection** (durability is proven post-commit, never by in-transaction sentinels): the twelve rows `implemented|codify|ceo|<stamp>` with every stamp outside the two vintages; `accepted` 74; `implemented` 345; the capture file has 590 lines and 86 rows containing `|accepted|codify|`. Copy the capture into the worktree: `mkdir -p knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15 && cp /tmp/g2ptw30a/flip-capture.txt knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt` (A5 commits it; QA verifies it). Commit the SQL in governance, the message on stdin: `git -C "$GOV" add governance/knowledge/development/g2ptw30a-flip.sql && git -C "$GOV" commit -F - -- governance/knowledge/development/g2ptw30a-flip.sql <<'MSG'` with the subject `[<id>] gate2-pt-w30-a: the flip SQL (442, 443, 458, 461, 472, 476, 477, 490, 498, 523, 589, 590 -> implemented)`.
>
> **A5 — dev log + commit by explicit pathspec, the pre-check chained first.** `knowledge/development/dev-log-gate2-pt-w30-a-2026-09-15.md`: the A0 determination, A1's pins, A2's lines and the three refusals, A3's counts and the governance commit hash, A4's backup lines, sentinels, read-back and the SQL commit hash. Then, as ONE command: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/development/dev-log-gate2-pt-w30-a-2026-09-15.md knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt && git commit -F - -- knowledge/development/dev-log-gate2-pt-w30-a-2026-09-15.md knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt <<'MSG'` with the subject `[<id>] gate2-pt-w30-a: PT v4.112 + flip landed (dev log, flip capture)` and `MSG` closing it. `git status --short` → empty. STOP.
>
> **Deposits:**
> - `knowledge/development/dev-log-gate2-pt-w30-a-2026-09-15.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30a-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt`
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-pt-w30-a-2026-09-15.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30a-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt`

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message.** You are the bellows QA agent. `cd "$(git rev-parse --show-toplevel)"`; `GOV=/Users/marklehn/Developer/eluvian-governance`; `B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-a-2026-09-15.py"`; `DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db`; `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python` — re-derive per compound. Read-only beyond the three evidence files; the commit's message goes on stdin, as STEP 1's header states.
>
> **(A) Rule 20 self-check** — the canonical block at the path the dispatcher's mandate names (this machine's `RULE_20_SELF_CHECK_BLOCK.md`; quote the path you were handed). Run with:
> - `plan_slug`: `gate2-pt-w30-a-2026-09-15`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15"`
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-pt-w30-a.txt", "flip-capture.txt"]` (the capture committed by DEV in A5; QA verifies it is the committed file — `git log -1 --format=%s -- knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt` carries the plan's id)
>
> **(B) Items — raw output appended to `probes-raw.txt`:**
> - **Item 1 — the live template:** `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` (the `[<id>]` commit); every Task C count re-run (thirty probes); `wc -l` 2558; `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30a-flip.sql` → EMPTY.
> - **Item 2 — the builder, by a second pair of hands (byte-identity):** `rm -rf /tmp/g2ptw30a-qa; mkdir -p /tmp/g2ptw30a-qa; C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show "$C^:PLANNER_TEMPLATE.md" > /tmp/g2ptw30a-qa/PT-pre.md; python3 "$B" /tmp/g2ptw30a-qa/PT-pre.md /tmp/g2ptw30a-qa/PT-out.md; echo "builder_exit=$?"; cmp /tmp/g2ptw30a-qa/PT-out.md "$GOV/PLANNER_TEMPLATE.md" && echo BYTE_IDENTICAL` → the `BUILT:` line, `builder_exit=0`, `BYTE_IDENTICAL`; P4 with the builder's own commit; the three refusals reproduced.
> - **Item 3 — the flip, read back on a FRESH read-only connection:** the twelve rows `implemented|codify|ceo|<stamp outside the two vintages>`; `accepted` 74 — and the seventy-four by count, each still at one of the two vintages (`SELECT COUNT(*) … WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z')` → 74); `implemented` 345; `wc -l knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/flip-capture.txt` → 590; the capture's twelve rows for the flipped ids read `accepted|codify` (the pre-flip state, captured before the UPDATE).
> - **Item 4 — the full-suite file:** `"$BPY" -m pytest tests -q -p no:cacheprovider > knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/full-suite-gate2-pt-w30-a.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/full-suite-gate2-pt-w30-a.txt` → `exit=0` and a summary line with NO `failed`/`error` count — P7's counts are the expectation, but the gate (`_gate_qa_test_result`) reads FAILURES against `known_failures`, never the pass count: if bellows `main` moved since P7 was measured (`git log --oneline f9e35f36..HEAD -- tests` non-empty — it is, already — three commits on the `tests` path at `ded3edb2`, four at `e1a477f2`, measured at authoring; the QA reads its own probe — so this clause fires on this run), the drift is stated in `probes-raw.txt` under Item 4 as that probe's command and the commit list it printed, and nothing broader — the summary line stays in the suite file and is NOT copied into `probes-raw.txt`, and no other commit log is pasted into any evidence file, because the suite gate takes the FIRST `.txt` deposit carrying a summary-shaped line, whichever file that is (thread 363); the receipt's suite row stays the FILE and `exit=0`, never a note: a drift note reaches for `not run`, `skipped` or `pending`, and the canonical block fails a positive row carrying any of them (measured: exit 1). A different pass count with zero failures is not a halt.
>
> **(C) The report** `qa-receipt.md`: the verification table — status cells carry the glyph only, and NO positive row quotes a probe token or an output line carrying a Rule 20 hedging keyword (`hedging_keywords` in the canonical block): this plan has THREE such sources — the suite summary line, Rule 112's body, and the agent's OWN prose (a drift note, a re-run note: `not run`, `skipped` and `pending` are the words such a note reaches for, and the block fails a positive row carrying any of its `hedging_keywords` whoever wrote it) — so the suite row names the FILE and `exit=0` with the drift left in `probes-raw.txt`, and Item 1's row reads `30/30 counts as pinned in A3, see probes-raw.txt` (the raw file may carry the tokens; the receipt's positive rows may not — a row quoting any of them FAILS the self-check) — the follow-ups (the six tranches after this one; the Planner pushes governance and projects the markers; threads 326 and 336 at the keyboard), the Rule 20 stdout APPENDED. Commit, as ONE command with the pre-check chained and the message on stdin: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/ && git commit -F - -- knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/ <<'MSG'` with the subject `[<id>] QA: PT v4.112 rebuilt byte-identical from the pre-edit blob; flip read back (12 implemented, 74 accepted remain)` and `MSG` closing it. STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/full-suite-gate2-pt-w30-a.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-a-2026-09-15/full-suite-gate2-pt-w30-a.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
