# Bellows — Lifecycle DB Transition Writes (Reporting Phase 1, Executable B)
**Date:** 2026-06-11 | **Tier:** Large | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always

**Execution Map:** Step 1 (DEV) → Step 2 (QA) — strictly sequential, no parallel steps.

## Context (Rule 27)
Implements Executable B of the A→B split from the approved blueprint `bellows/knowledge/architecture/lifecycle-db-id-threading-blueprint-2026-06-11.md` (Sections 3 and 6.5; locked design in `reporting-phase-0-coverage-map-2026-06-10.md` at governance root). Executable A is SHIPPED and ACTIVE: the daemon now mints ids at claim, `lifecycle.py` exists with `id_sequence` + `plans`, and THIS plan is itself the first id-native claim. B completes the lifecycle DB: (1) extend `lifecycle.py` with the remaining tables per blueprint 3.4 DDL — `steps` (incl. live-status column, cost/turns/duration), `commits`, `deposits`, `verdicts`, `gate_events`, `diagnostic_meta`, `executable_meta`, `derivations` — pointers and small attributes ONLY, no content columns; (2) write helpers for each transition; (3) call sites in `bellows.py` at the seven boundaries mapped in blueprint Section 3: claim (meta-table rows + `derivations` parsed from plan text "implements diagnostic N" citations), step start (steps row, status running), step end/gate check (steps update with cost/turns/duration from the step log, `gate_events` rows incl. reason codes and overrides, `deposits` rows from the declared-deposit set), teardown/land (`commits` rows from the landed SHAs), verdict request written (`verdicts` row pending, with verdict_file_ref), verdict consumed (`verdicts` outcome update), close (`plans` state + `closed_at` for Done/ AND `halted-`/abandoned paths). CRITICAL — STALE LINE NUMBERS: blueprint Section 3's bellows.py line references predate Executable A, which added ~50 lines; RELOCATE every site by symbol/anchor (function names, log strings) and record the current line in your dev log — never edit at a blueprint line number without confirming the symbol matches. All writes are mechanical byproducts of transitions the daemon already performs — Layer 1 only, no domain judgment, no behavior changes to dispatch/gating itself. Write failures must NEVER block the plan lifecycle: wrap every lifecycle write in the established log-and-continue error pattern (a reporting DB outage must not halt dispatch). The new code activates only at daemon restart; this plan runs under the post-A daemon whose claim/mint behavior must remain untouched by B's edits until restart.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file first, then the blueprint `bellows/knowledge/architecture/lifecycle-db-id-threading-blueprint-2026-06-11.md` in full (Sections 3 and 6.5 are your design), then current `lifecycle.py` and the claim path in `bellows.py` as Executable A shipped them. **Rule 39 — re-verify the blueprint's Verification Blocks relevant to Section 3 before editing, RELOCATING each by symbol since line numbers predate Executable A; halt and report any claim that no longer matches.** **Allowed files (scope — exactly these, no others):** `bellows.py`, `lifecycle.py`, `verdict.py`, `runner.py`, `tests/test_lifecycle.py`, `tests/test_bellows.py`, `tests/test_verdict.py`, `tests/test_runner.py`, `tests/conftest.py`. If a file outside this set needs modification, halt and report — do not edit it. **Work items, in order:** **(1) `lifecycle.py` schema extension:** reproduce blueprint Section 3.4's DDL EXACTLY for the remaining tables — table names, column names, and the key strategy VERBATIM, including the `steps.step_id` surrogate primary key and `step_id` foreign-key references in the per-step child tables exactly as 3.4 writes them. DO NOT simplify, flatten, or "improve" the key design (e.g., composite plan_id+step_number keys in place of step_id indirection is NOT authorized — a prior run was stopped for exactly this). Any DDL deviation you believe is necessary is a halt-and-report condition, not a judgment call. Idempotent `CREATE TABLE IF NOT EXISTS` so existing A-era DBs upgrade in place at startup; pointers and small attributes ONLY, no content/blob columns. **(2) `lifecycle.py` write helpers:** `record_step_start`, `record_step_end` (status + cost/turns/duration + log ref), `record_gate_event(s)`, `record_deposits`, `record_commits`, `record_verdict_request`, `record_verdict_outcome`, `record_meta`, `record_derivations` — each a small, single-purpose function; every helper wraps its DB work in try/except that logs a WARN and returns (log-and-continue — lifecycle writes NEVER raise into the daemon loop). **(3) `bellows.py` call sites at the seven boundaries** (relocate each by symbol; record current file:line in dev log): claim → `record_meta` from parsed header fields + `record_derivations` from an "implements diagnostic <id-or-legacy-slug>" scan of the plan text (tolerate absence — derivations row only when a citation exists); step start (where the step prompt dispatches) → `record_step_start`; step end (where the step receipt/log is read and gates run) → `record_step_end` with cost from the step log's `parsed.cost_usd`, turns/duration via the established regex on `raw_output`, plus `record_gate_events` from the gate result (every gate row incl. reason codes and any Rule-22 override flag available at that point) and `record_deposits` from the plan-declared deposit set with existence-at-gate; teardown/land → `record_commits` for the SHAs landed on main (the merge/ff result — capture via the existing teardown git plumbing); verdict request written → `record_verdict_request` with the request filename; verdict consumed → `record_verdict_outcome` with `verdict: continue|stop` and the response filename; close → existing `mark_plan_state` extended usage for Done (`done`), `halted-` (`halted`), and abandoned, with `closed_at`. **(4) `verdict.py`:** only if the request/response filename is not already in hand at the bellows.py call sites — prefer passing existing values over new parsing; keep edits minimal. **(5) `runner.py`:** only if cost/turns/duration extraction requires it — prefer reading the step-log JSON from bellows.py if the path is already known there. **(6) Tests:** extend `tests/test_lifecycle.py` — schema upgrade-in-place from an A-era DB (create id_sequence+plans only, run `init_lifecycle_db()`, assert all tables present and existing rows intact); each write helper happy-path + the log-and-continue contract (e.g. point LIFECYCLE_DB_PATH at an unwritable path, call helper, assert NO exception propagates); `derivations` parsing for id citation, legacy-slug citation, and no-citation. Integration: extend the existing `run_plan()` claim tests in `tests/test_bellows.py` to assert steps/gate_events/verdicts rows land across a full simulated plan lifecycle (the existing Mode A / auto-close test harness gives you this loop). Update `tests/conftest.py` only if the isolation fixture needs to init the new tables (it calls `init_lifecycle_db()` — likely no change; touch only if needed). **(7) FULL suite:** `pytest tests/ --tb=line` to completion, output to `bellows/knowledge/development/pytest_full_lifecycle_writes.txt`, read the TAIL yourself, report exact counts vs the 480-pass baseline. Self-verify in a scratch session against a temp DB: simulate claim → step start → step end → verdict → close and dump the resulting rows from every table. Write a dev log covering every work item with current file:line of each change and the relocated blueprint anchors. Use `with open()` for deposits; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/lifecycle-writes-dev-log-2026-06-11.md`
> - `bellows/knowledge/development/pytest_full_lifecycle_writes.txt`

---
---

## STEP 2 — Bellows QA

---

> **MANDATORY — READ THIS FIRST. This step is gate-enforced by the daemon's `rule_20_self_check` gate.** The gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` followed by `PASSED — SELF-CHECK PASSED`. Your verification table does NOT satisfy this gate — the banner must appear verbatim in the report. Before finishing, self-grep your deposited report for the exact string `Rule 20 — QA Self-Check Results` and confirm it is present; if it is not, you have not finished. **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file, the blueprint `bellows/knowledge/architecture/lifecycle-db-id-threading-blueprint-2026-06-11.md` (Section 3), and the DEV deposits `bellows/knowledge/development/lifecycle-writes-dev-log-2026-06-11.md` + `bellows/knowledge/development/pytest_full_lifecycle_writes.txt`. **Verify, each with executed evidence (commands + output captured into the report):** (1) **Schema completeness + DDL conformance + upgrade-in-place** — against a TEMP DB (never the live `lifecycle.db`): create an A-era schema (id_sequence + plans only, with a seeded plans row), run `init_lifecycle_db()`, dump `sqlite_master`: all blueprint-3.4 tables present, seeded row intact, no content/blob columns anywhere (assert by inspecting column types/names); ADDITIONALLY diff the deployed DDL against blueprint Section 3.4 table-by-table — table names, column names, and key strategy (incl. `steps.step_id` surrogate PK and `step_id` FKs in per-step child tables) must match the blueprint VERBATIM; any key-strategy deviation (e.g., composite plan_id+step_number in place of step_id indirection) is a FAIL finding. (2) **Full-lifecycle write integration** — run the DEV's extended `run_plan()` integration tests AND an independent scratch simulation: claim → step start → step end → verdict request → verdict consumed → close; dump every table and confirm rows at each boundary with correct FKs to the plans row, including cost/turns/duration populated in `steps` and gate rows with reason codes in `gate_events`. (3) **Log-and-continue contract** — point `LIFECYCLE_DB_PATH` at an unwritable target, invoke every write helper, assert no exception propagates and a WARN is logged for each; this is the no-write-failure-blocks-lifecycle guarantee. (4) **Derivations parsing** — three cases: id citation, legacy-slug citation, no citation. (5) **Boundary discipline** — grep the diff for any behavior change to dispatch/gating itself: `lifecycle.record_*` and `mark_plan_state` call sites must be additive (no existing control flow altered); confirm A's mint/claim tests still pass unchanged. (6) **FULL suite under a wall-clock bound** — `timeout`-wrapped `pytest tests/ --tb=line` watched to completion; output to `bellows/knowledge/qa/pytest_full_lifecycle_writes_qa.txt`; read the TAIL; reconcile exact counts against the DEV run — any NEW failure vs the DEV run is a FAIL finding. (7) **Live-DB safety** — confirm no test or scratch step wrote the production `lifecycle.db`: capture its mtime/row-counts before and after your runs and show they are unchanged. Write the QA report with a verification table (item | method | evidence | PASS/FAIL), the reconciled suite counts, and the Rule 20 self-check banner block. **Deposits:**
> - `bellows/knowledge/qa/lifecycle-writes-qa-report-2026-06-11.md`
> - `bellows/knowledge/qa/pytest_full_lifecycle_writes_qa.txt`
