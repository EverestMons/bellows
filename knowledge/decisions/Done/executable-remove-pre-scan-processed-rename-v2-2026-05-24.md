# Bellows — Remove pre-scan `processed-` prefix rename (v2 — fresh full removal)
**Date:** 2026-05-24 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** full | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** after_step_1 | **auto_close:** false

## Context

Fresh full-removal plan. Three prior halted plans on this same fix are now in `Done/` (`halted-executable-remove-pre-scan-processed-rename-2026-05-24`, `halted-executable-remove-pre-scan-processed-rename-continuation-2026-05-24`, `halted-executable-remove-pre-scan-processed-rename-continuation-v2-2026-05-24`). Each halt traced to a different Planner-side prompt error. The unstaged main-repo edits from the first halted DEV have been stashed by Planner (`stash@{0}: halted-DEV-pre-scan-removal-2026-05-24`) so this plan operates on clean origin/main.

**Authority:** the 2026-05-24 diagnostic findings at `bellows/knowledge/architecture/processed-prefix-reconsumption-and-rename-skip-2026-05-24.md`. The pre-scan block at `bellows.py:1129-1162` causes a P0 infinite loop on multi-step plans that pause for verdict between steps (Section B of the diagnostic). Shape 1c (full removal) is the recommended fix because PLANNER_TEMPLATE v4.49 Rule 25 already prohibits the `processed-` prefix at write time, making the pre-scan a belt-and-suspenders safety net that has created a worse bug than it prevented.

**Path convention for this plan (verified against `agent-prompt-feedback.md` 12+ prior occurrences):**
- **In commands inside DEV/QA prompts: bare paths only.** `grep -n '...' bellows.py`, NOT `grep -n '...' bellows/bellows.py`. NEVER `cd bellows`. The agent's cwd IS the bellows project root within its worktree.
- **In the plan's `Deposits:` block: project-prefixed paths.** `bellows/bellows.py`, `bellows/knowledge/development/...`. The deposit_exists gate resolves these via worktree-prefix-aware logic.
- **In prose context within this plan: project-prefixed paths.** Describes the project state from the Planner's vantage.

**Anchors verified by Planner via grep against origin/main state immediately before authoring this plan:**

`bellows.py`:
- Line 33: `_prescan_orphan_logged: set = set()` (module-level set declaration)
- Line 1129: `        # Pre-scan: normalize processed-verdict-* to verdict-* (Planner write-time naming mismatch).` (pre-scan block start)
- Lines 1152 and 1154: `_prescan_orphan_logged` references inside the block
- Line 1162: `                _log("WARN", f"normalized write-time processed- prefix: {fname} → {canonical}")` (pre-scan block end)

`tests/test_consume_verdicts.py` — seven pre-scan test functions to remove:

| Line | Test name |
|---|---|
| 310 | `test_pre_scan_renames_processed_verdict_to_canonical` |
| 373 | `test_pre_scan_collision_guard_does_not_overwrite` |
| 430 | `test_pre_scan_ignores_non_verdict_processed_files` |
| 515 | `test_pre_scan_skips_rename_when_no_paired_plan` |
| 572 | `test_pre_scan_renames_when_verdict_pending_plan_exists` |
| 629 | `test_pre_scan_treats_done_plan_as_no_paired_plan` (contains `_prescan_orphan_logged.discard()` reference) |
| 681 | `test_pre_scan_collision_guard_fires_regardless_of_paired_plan` |

**What gets removed (combined scope):**
- The pre-scan block at `bellows.py:1129-1162` (~34 lines)
- The `_prescan_orphan_logged: set` module-level declaration at `bellows.py:33`
- All seven pre-scan test functions in `tests/test_consume_verdicts.py`

**What stays:**
- The main verdict consumption loop starting after the pre-scan block (unchanged — handles canonical `verdict-*` files correctly)
- The post-consumption rename sites in `bellows.py` that produce `processed-verdict-*` files (the legitimate Bellows-side marker that a verdict has been consumed)
- The no-match WARN that surfaces any future write-time `processed-` violation for manual triage
- Rule 25 governance language in `PLANNER_TEMPLATE.md` v4.49 (unchanged — remains the authoritative fix)
- All non-`test_pre_scan_*` tests in `tests/test_consume_verdicts.py`

**Acknowledged scope:** This executable removes work shipped in two prior plans — commit `3c9344f` (2026-05-21 pre-scan rename fix) and commit `d1855ba` (2026-05-22 orphan guard). Both shipped legitimate fixes for their stated bugs, but they targeted a problem that governance now prevents. The pre-scan's removal is a deliberate hardening choice.

**Daemon-restart note:** This fix touches `bellows.py`, which Bellows does not hot-reload. The running daemon will continue executing pre-fix code through this plan's lifecycle. Expected behavior: gates pass on Step 1 and Step 2 because the pre-scan only fires on plans whose step-1 verdict has already been consumed and is sitting in `resolved/`. After plan close + daemon restart, the pre-scan removal becomes effective for all future plans.

## Execution Map

```
Step 1 (DEV) → Step 2 (QA)
```

Sequential. Step 1 removes the code and tests, commits everything together, deposits dev log. Step 2 verifies and runs full pytest.

---
---

## STEP 1 — Bellows Developer

---

> You are the Bellows Developer. Read your specialist file at `agents/BELLOWS_DEVELOPER.md` first. **Skip glossary read — this is a code-removal task with precise anchors.** **Authority:** the 2026-05-24 diagnostic findings at `knowledge/architecture/processed-prefix-reconsumption-and-rename-skip-2026-05-24.md` (Section A identifies the pre-scan code; Section F Q16 recommends Shape 1c — full removal). The diagnostic's Section G Q19 enumeration was incomplete; this plan supersedes it with the complete seven-test list shown in the Context section above. **PATH CONVENTION: bare paths in all commands. Your cwd is the bellows project root within your worktree. Do NOT prefix paths with `bellows/`. Do NOT `cd bellows`. Files are at `bellows.py`, `tests/test_consume_verdicts.py`, `knowledge/...` — bare, relative to cwd.** **Pre-edit verification:** Confirm the expected starting state. Run each of the following and confirm the listed expected output. If any check fails, STOP — deposit a flag at `knowledge/flags/state-mismatch-remove-pre-scan-processed-rename-v2-2026-05-24-step-1.md` documenting the actual output, and halt. Check (i): `git status --porcelain bellows.py tests/test_consume_verdicts.py` — expected: BOTH files clean (no output, or empty). Check (ii): `grep -c '_prescan_orphan_logged' bellows.py` — expected: 3 (line 33 declaration, lines 1152 and 1154 inside the pre-scan block). Check (iii): `grep -c 'normalized write-time processed- prefix:' bellows.py` — expected: 1 (the log emission at line 1162). Check (iv): `grep -n '^def test_pre_scan' tests/test_consume_verdicts.py` — expected: exactly SEVEN matches at lines 310, 373, 430, 515, 572, 629, 681 with the seven test names listed in the plan's Context table. Check (v): `python -c "import bellows"` from cwd — expected: exits cleanly. **Task A — remove the `_prescan_orphan_logged` module-level set declaration** at line 33 of `bellows.py`. The exact line is `_prescan_orphan_logged: set = set()`. Remove only this line. Preserve surrounding blank lines as appropriate for clean module-level formatting. **Task B — remove the pre-scan block at lines 1129-1162 of `bellows.py`.** The block starts with the comment line `# Pre-scan: normalize processed-verdict-* to verdict-* (Planner write-time naming mismatch).` and ends with the line `_log("WARN", f"normalized write-time processed- prefix: {fname} → {canonical}")`. Remove the entire block including the leading comment. Remove any blank-line padding that becomes orphaned (so the surrounding code reads cleanly). **Task C — remove all seven pre-scan tests from `tests/test_consume_verdicts.py`.** Remove these seven test functions in their entirety, including docstrings: (1) `test_pre_scan_renames_processed_verdict_to_canonical` (line 310); (2) `test_pre_scan_collision_guard_does_not_overwrite` (line 373); (3) `test_pre_scan_ignores_non_verdict_processed_files` (line 430); (4) `test_pre_scan_skips_rename_when_no_paired_plan` (line 515); (5) `test_pre_scan_renames_when_verdict_pending_plan_exists` (line 572); (6) `test_pre_scan_treats_done_plan_as_no_paired_plan` (line 629); (7) `test_pre_scan_collision_guard_fires_regardless_of_paired_plan` (line 681). If any of these references a `@pytest.fixture` or helper function that is NOT used by any other surviving test in the file, also remove that fixture; do NOT remove fixtures shared with surviving tests. Note: line numbers will shift as you remove tests; consider removing from bottom up (line 681 first, then 629, etc.) to keep upstream line numbers stable until each removal. Run `grep -n '^def test_' tests/test_consume_verdicts.py` before and after to confirm only `test_pre_scan_*` functions are removed. **Task D — verify completeness.** Run `grep -n '^def test_pre_scan' tests/test_consume_verdicts.py` and confirm ZERO matches. Run `grep -rn '_prescan_orphan_logged' . --include='*.py'` and confirm ZERO matches anywhere. Run `grep -rn 'normalized write-time processed- prefix' . --include='*.py'` and confirm ZERO matches anywhere. Run `grep -c '# Pre-scan: normalize processed-verdict-' bellows.py` and confirm ZERO. If any check shows non-zero, STOP — deposit a flag at `knowledge/flags/dangling-references-remove-pre-scan-processed-rename-v2-2026-05-24-step-1.md` listing the surviving references and halt. **Task E — preservation check.** Run `grep -n 'processed-' bellows.py` and confirm AT LEAST TWO matches remain — these are the legitimate post-consumption rename sites that produce `processed-verdict-*` files after successful consumption. If fewer than two matches, STOP — deposit a flag at `knowledge/flags/over-removal-remove-pre-scan-processed-rename-v2-2026-05-24-step-1.md` and halt. **Task F — smoke check.** `python -c "import bellows"` from cwd and confirm clean exit. If import fails, paste traceback into `knowledge/flags/import-failure-remove-pre-scan-processed-rename-v2-2026-05-24-step-1.md` and halt. **Task G — deposit dev log:** write to `knowledge/development/remove-pre-scan-processed-rename-v2-2026-05-24.md` documenting: (a) pre-edit verification stdout for checks (i)–(v); (b) before/after line counts of `bellows.py` and `tests/test_consume_verdicts.py`; (c) Task D completeness verification stdout; (d) Task E preservation check stdout (confirming legitimate post-consumption rename sites survived); (e) Task F import smoke stdout; (f) one-paragraph summary citing the 2026-05-24 diagnostic findings, PLANNER_TEMPLATE v4.49 Rule 25 as the governance fix, and the three prior halted plans now in `Done/` with their respective halt reasons (incomplete enumeration, `cd bellows` path errors, worktree-state assumption errors). **Task H — commit everything as ONE commit:** stage `bellows.py`, `tests/test_consume_verdicts.py`, and the dev log. Commit with message `fix(bellows): remove pre-scan processed- prefix rename — eliminates P0 multi-step pause-cycle loop`. **DO NOT push.** **Standard prompt feedback protocol** → append entry to `knowledge/research/agent-prompt-feedback.md`. The entry should explicitly note the three halt sequence and capture as a discipline lesson: (1) Planner should grep live files to verify enumerations from diagnostic findings before authoring fix plans; (2) bare-path convention (no `cd bellows`) for bellows-worktree plan commands (12+-occurrence pattern); (3) Bellows-dispatched DEVs run in fresh worktrees from origin/main, so continuation plans cannot rely on uncommitted main-repo state. Second commit: `docs: prompt feedback — bellows DEV remove pre-scan processed rename v2`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/bellows.py` (modified)
> - `bellows/tests/test_consume_verdicts.py` (modified)
> - `bellows/knowledge/development/remove-pre-scan-processed-rename-v2-2026-05-24.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`

---
---

## STEP 2 — Bellows QA

---

> You are the Bellows QA Agent. Read your specialist file at `agents/BELLOWS_QA.md` first. **Skip glossary read — this is a code-removal QA task.** **PATH CONVENTION: bare paths in all commands. Your cwd is the bellows project root within your worktree. Do NOT prefix paths with `bellows/`. Do NOT `cd bellows`. Files are at `bellows.py`, `tests/test_consume_verdicts.py`, `knowledge/...` — bare, relative to cwd.** **Before starting, read `knowledge/development/remove-pre-scan-processed-rename-v2-2026-05-24.md` (DEV Step 1 deposit) and check its Output Receipt status; if not Complete, stop and report the blocker.** **FIRST — Deliverable Verification (Rule 17).** Verification table: | Deliverable | Expected | Status (✅/❌) | Evidence |. Specifically: (1) `grep -n 'normalized write-time processed- prefix:' bellows.py` returns ZERO matches; (2) `grep -n '_prescan_orphan_logged' bellows.py` returns ZERO matches; (3) `grep -n '# Pre-scan: normalize processed-verdict-' bellows.py` returns ZERO matches; (4) all seven pre-scan test names return ZERO matches in `tests/test_consume_verdicts.py` (test names: `test_pre_scan_renames_processed_verdict_to_canonical`, `test_pre_scan_collision_guard_does_not_overwrite`, `test_pre_scan_ignores_non_verdict_processed_files`, `test_pre_scan_skips_rename_when_no_paired_plan`, `test_pre_scan_renames_when_verdict_pending_plan_exists`, `test_pre_scan_treats_done_plan_as_no_paired_plan`, `test_pre_scan_collision_guard_fires_regardless_of_paired_plan`); (5) `grep -n '^def test_pre_scan' tests/test_consume_verdicts.py` returns ZERO matches; (6) `grep -rn '_prescan_orphan_logged' . --include='*.py'` returns ZERO matches; (7) `grep -rn 'normalized write-time processed- prefix' . --include='*.py'` returns ZERO matches; (8) `python -c "import bellows"` from cwd exits cleanly; (9) the post-consumption rename at the legitimate `processed-` marker site is preserved — `grep -n 'processed-' bellows.py` returns AT LEAST TWO matches; (10) dev log exists at `knowledge/development/remove-pre-scan-processed-rename-v2-2026-05-24.md` with all six documentation items (a-f); (11) `knowledge/research/agent-prompt-feedback.md` has a new 2026-05-24 entry from this plan covering the three halt sequence lessons. Capture each grep to evidence under `knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-v2-2026-05-24/`: `prescan_log_line_absent.txt`, `prescan_orphan_logged_absent.txt`, `prescan_block_comment_absent.txt`, `all_seven_tests_absent.txt`, `no_pre_scan_test_defs.txt`, `repo_wide_orphan_absent.txt`, `repo_wide_log_absent.txt`, `import_smoke.txt`, `legitimate_processed_preserved.txt`, `dev_log_present.txt`, `feedback_entry.txt`. **Full pytest suite (Test Scope: full).** Run `pytest 2>&1 | tee knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-v2-2026-05-24/pytest_full.txt`. Expected: all tests pass EXCEPT the pre-existing `test_run_step_timeout` failure (BACKLOG-documented carry-over, not a regression). The seven removed pre-scan tests should not appear in the test count. If any OTHER test fails, mark ❌ and halt. Capture pytest summary line (test count, passes, fails, skips) to evidence. **Structural compliance check.** Identify the DEV commit SHA (the code-removal commit, NOT the prompt-feedback commit). Run `git --no-pager show --stat <DEV-commit-sha> 2>&1 | tee knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-v2-2026-05-24/dev_commit.txt` and confirm exactly three paths in the commit: `bellows.py`, `tests/test_consume_verdicts.py`, and `knowledge/development/remove-pre-scan-processed-rename-v2-2026-05-24.md`. Run `git --no-pager show <DEV-commit-sha> -- bellows.py 2>&1 | tee knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-v2-2026-05-24/diff_bellows.txt` and verify the diff shows ONLY deletions (no additions, no modifications to surrounding code beyond removing the targeted block and module-level set). Run `git --no-pager show <DEV-commit-sha> -- tests/test_consume_verdicts.py 2>&1 | tee knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-v2-2026-05-24/diff_tests.txt` and verify only deletions, no additions or modifications to surviving tests. If diffs show additions or non-targeted modifications outside the seven pre-scan tests, mark ❌ and halt. **Rule 20 self-check** — run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at governance root. Use these values: `plan_slug` = `executable-remove-pre-scan-processed-rename-v2-2026-05-24`; `qa_report_path` = `knowledge/qa/executable-remove-pre-scan-processed-rename-v2-2026-05-24.md`; `evidence_dir` = `knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-v2-2026-05-24/`; `required_evidence_files` = `["prescan_log_line_absent.txt", "prescan_orphan_logged_absent.txt", "prescan_block_comment_absent.txt", "all_seven_tests_absent.txt", "no_pre_scan_test_defs.txt", "repo_wide_orphan_absent.txt", "repo_wide_log_absent.txt", "import_smoke.txt", "legitimate_processed_preserved.txt", "dev_log_present.txt", "feedback_entry.txt", "pytest_full.txt", "dev_commit.txt", "diff_bellows.txt", "diff_tests.txt"]`. Include literal stdout in QA report. If FAILED, halt — report to CEO. **Final:** Update `PROJECT_STATUS.md` — prepend a 2026-05-24 entry under Completed for "Removed pre-scan `processed-` prefix rename from `_consume_verdicts` (Shape 1c) — eliminates P0 multi-step pause-cycle loop class" with a one-paragraph summary citing the 2026-05-24 diagnostic findings, PLANNER_TEMPLATE v4.49 Rule 25 as the governance fix, and noting the work shipped after three Planner-side prompt errors halted prior attempts. Use `Desktop Commander:edit_block` with the existing topmost Completed entry as the anchor (insert immediately before it). **Commit:** stage QA report, evidence files, and `PROJECT_STATUS.md` update with message `qa(bellows): remove pre-scan processed- prefix rename v2`. **DO NOT push.** **Standard prompt feedback protocol** → append entry to `knowledge/research/agent-prompt-feedback.md`. Second commit: `docs: prompt feedback — bellows QA remove pre-scan processed rename v2`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/knowledge/qa/executable-remove-pre-scan-processed-rename-v2-2026-05-24.md`
> - `bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-v2-2026-05-24/` (15 evidence files per Rule 20 self-check list)
> - `bellows/PROJECT_STATUS.md` (modified)
> - `bellows/knowledge/research/agent-prompt-feedback.md`
