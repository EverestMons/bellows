# bellows — executable: DOCTRINE — Gate 2, thread 336, PT tranche three (contracts, consumers and tools): proposals 453, 462, 463, 464, 480, 500, 509, 520, 544, 559, 583 onto PLANNER_TEMPLATE.md v4.115 by a committed builder (five extensions on Rules 38, 40, 45, 59 and 81; a new Rule 116 — one contract, one parser), then the eleven flip `implemented` — 51 accepted rows remain for the four tranches after it

**Date:** 2026-09-15 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full (the bellows suite is UNCHANGED by this plan and is run once by QA so the test gate reads a real summary line; the gate reads FAILURES against `known_failures`, never the pass count — P7 is the expectation, and bellows `main` moves as plans merge) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 2

**auto_close:** false

**Slug:** `gate2-pt-w30-c-2026-09-15`

**Depends on:** the CEO's rulings of 2026-09-14 (thread 336 — one Gate 2 over the 86 accepted proposals, W=29 and W=30 together) and the delegation of 2026-09-15 ("Start on gate 2 and you have my delegation to release at the proper time"; "No I am good with moving forward on these" — the seven-tranche partition stands); the tranche read `gate2-packet-2026-09-15.md` (governance `6cdd1aef` — its §1.3 ruling: 506 stands accepted, 584, 467 and 508 stay rejected; its partition row "PT three — contracts, consumers and tools"); the non-author Gate 1 packets — W=29's `gate1-packet-2026-09-02.md` (453, 462, 463, 464) and W=30's `gate1-packet-2026-09-13-a.md` (reader A: 480, 500), `-b.md` (reader B: 509, 520, 544) and `-c.md` (reader C: 559, 583); **tranche two** — `Done/executable-100114.md` in bellows (`gate2-pt-w30-b-2026-09-15`, closed 2026-09-15 20:19, the clone origin and the newest same-class plan: doctrine prose onto `PLANNER_TEMPLATE.md` by a committed builder, then a scoped CAS flip, run clean in seventeen minutes with no deviation) and `Done/executable-540.md` in governance (the multi-site EXTENSION form); **thread 322's re-deposited plan** — bellows #100118 — the fifth deposit of one plan (#100113, #100115, #100116, #100117 landed the implementation as `d1ac5cdb`, #100118 the manifest and QA) — at whose Done (2026-09-16 10:07) the CEO's doctrine texts were applied as v4.114 (governance `45b6aed4`, committed 10:08) (the GLOSSARY addendum; Rule 25's and the state machine's terminal-close sentences; a History row) — this tranche's pre-edit version, so its pins are measured on the v4.114 candidate produced by the proven apply script (`scratchpad/apply-322.py --dry`, byte-deterministic) and RE-MEASURED on the live file at the freeze; the builder `governance/knowledge/decisions/drafts/build-gate2-pt-w30-c-2026-09-15.py` (committed `bfe58149` — the freeze re-pin of 2026-09-16 to the act's date, Last Updated line and History row, digest `5d6f66518e21c84b`; `5b3d8b01` / `e609a15247f00484` after the walk-0 scout's folds and `25d2106a` its first are history). Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-c3-2026-09-16.md` (this text's first cycle, under the file name `executable-gate2-pt-w30-c.md`: `walk-register-gate2-pt-w30-c-2026-09-15.md` — its walk 0, the scout, its walk 1 and the EXECUTION seat).

**Tier computed, not judged (§1):** **T-6 fires** — `PLANNER_TEMPLATE.md` is the template → **T2**. T-2 fires (eleven canonical rows flip; caps at T1). T-1 fires (two repos, plus the DB). T-8 does NOT fire — a structure-for-structure clone of tranche two (#100114) carrying 540's extension form and tranche one's new-rule form; nine edits against twelve, one new rule against four, no span, is scale, not shape. **Panel form — the magnitude proxy, measured at walk 0 (all three must hold): (i) structure-clone** — the parent is #100114 (closed 2026-09-15 20:19, the newest PT tranche), and both walk-0 clone-diffs ran (against its final text and its register); **(ii) mechanized edits** — every template mutation flows through the committed builder, whose nine anchors are count-1-asserted BY EXECUTION and whose thirty-five post-conditions are asserted on the output before any write, with the line and byte deltas asserted against pinned constants (proven at the first cycle's walk 0 on the v4.114 candidate and re-proven at this cycle's walk 0 on the LIVE v4.114 — the register's M4: the pinned deltas, a `21	2` numstat, the byte pin firing on an off-by-one copy, the three refusals, and the pre-act v4.113 blob refused on the version anchor); the flip is one CAS statement under a structural pre-check; **(iii) scripted probes** — every new token measured 0 against the pre-edit file, the invariant tokens at their MEASURED pre-edit counts. **→ SMALL form: the walk-0 scout, then EXECUTION, then CAPSTONE — the seats on the Air when a session is live there (the CEO's direction of 2026-09-15, thread 369), else local subagents.** The CEO may call the full form.

## Why this exists

Gate 1 routed 86 proposals `accepted|codify`; tranches one and two landed twenty-four of them today (v4.112 by #100109, v4.113 by #100114). This tranche is the packet's third row: the eleven rows about CONTRACTS, CONSUMERS and TOOLS — what an edit surface is enumerated from, who a shared list's and a served surface's consumers are, which functions of a shared parser an instrument may import, how one contract keeps one parser, what a recalled fact and a tool's arm owe before they are offered, what a generator runs, how a vocabulary resolves before it widens, what a lint letter routes, and how a time-only log is read. Their homes are the packets', re-verified against the v4.114 candidate at authoring (every probe `/usr/bin/grep -c -i -F`; the zero rows the claim): **453** (entry 445) — enumerate from the callee → Rule 38, one extension paragraph (`from the callee` 0; `argument form` 0); **544 + 462 + 500** (536, 454, 492) — consumers by EFFECT, a served surface's clients, the primitives not the classifier → Rule 45, one paragraph (`by EFFECT` 0; `client` 0; `is_fold_table` 0; `classifier` → Rule 45's prop-421 paragraph's sense, the gate direction — this is the measurement direction); **463 + 464 + 480** (455, 456, 472) — a recalled fact checked before offered, the arm read before quoted, the generator's runs read → Rule 59, one paragraph (`falsify` 0; `--help` 0; `emit-manifest` 0; `hand-author` 2 — Rule 60's banner clause and the Checklist, neither this sense); **509** (501) — one contract, one parser → a NEW Rule 116 after Rule 115, in tranche one's new-rule form, opening as kin of Rules 70 and 109 (`ONE parser` 0; `682 plans` 0); **520 + 559** (512, 551) — resolve through existing states; the lint letter a routing key → Rule 81, one paragraph (`residue` 3 — two History rows and one unrelated rule, none this sense; `routing key` 0; `git log -S` 0); **583** (575) — a time-only log anchored by line number → Rule 40, one extension paragraph (`time-only` 0; `crosses midnight` → Rule 40's own parent sentence, the authoring half). **Homes:** FIVE extension paragraphs appended after the home's existing last line (540's form), each opening in bold with its proposals, entries and `codified 2026-09-16 — Gate 2, thread 336, PT tranche three`; ONE new rule (`### 116.`, its body, its Source line carrying `codified 2026-09-16 (Gate 2, thread 336 — PT tranche three)`, its `---`), placed after Rule 115's separator and before the Lifecycle DB Read Protocol; the version, the `Last Updated` line and the History row. Numbering append-only: Rule 116 follows Rule 115. ⚠️ TWO landed texts carry a Rule 20 hedging keyword — Rule 59's extension (`skipped three runs`, entry 472's words) and Rule 40's (`an assumed day`, entry 575's) — measured by the hedging scan of the builder's 21 added lines at walk 0; MUST-PRESERVE names both.

## What this plan does

**In the governance checkout, by absolute path (`GOV=/Users/marklehn/Developer/eluvian-governance`), nine anchored edits to `PLANNER_TEMPLATE.md` by the committed builder (never by hand):**
- **E1** — Rule 38: after its body (the rule's last line; anchor: the blank line and Rule 39's heading, re-emitted), the 453 paragraph. **E2** — Rule 45: after its prop-421 paragraph (anchor: the blank line and Rule 46's heading, re-emitted), the 544 + 462 + 500 paragraph. **E3** — Rule 59: after its prop-386 paragraph (anchor: the blank line and Rule 60's heading, re-emitted), the 463 + 464 + 480 paragraph. **E5** — Rule 81: after its prop-406 paragraph (anchor: the blank line and Rule 82's heading, re-emitted), the 520 + 559 paragraph. **E6** — Rule 40: after its body (anchor: the blank line and Rule 41's heading, re-emitted), the 583 paragraph.
- **E4** — the NEW Rule 116: the anchor is Rule 115's Source line, its `---` and the `## Lifecycle DB Read Protocol (Planner)` heading (count 1); the replacement re-emits them around the new rule — `### 116. One contract, ONE parser, every consumer calls it — two checkers on one contract drift apart, and silencing the weaker leaves the stronger breached`, its body opening in italics as kin of Rules 70 and 109, its Source line `*Source: proposal 509, entry 501 (2026-09-07), codified 2026-09-16 (Gate 2, thread 336 — PT tranche three)*`, its `---` (tranche one's Rules 112–115 form).
- **E7** — `**Version:** 4.114` → `**Version:** 4.115`. **E8** — `**Last Updated:** 2026-09-16 (v4.114)` → `**Last Updated:** 2026-09-16 (v4.115)`. **E9** — the History row inserted directly under the `| Date | Lesson |` header (thread 322's v4.114 row, tranche two's v4.113 row and tranche one's v4.112 row survive below it).

The builder reads the file as bytes, refuses CRLF, refuses an already-built input, asserts each anchor's count is 1, applies the nine edits, asserts thirty-five post-conditions on the OUTPUT (the builder's `POST` list: the Rule 116 heading once; the History row's `**Rule 116 — one contract, ONE parser` once; the codified strings at 5 and 1; the five openers once each; the v4.115 tokens 1 and the v4.114 tokens 0; the new History row 1 and the three rows below it 1 each; the section headings surviving at their MEASURED pre-edit counts — `### 38. `, `### 39. `, `### 40. `, `### 41. `, `### 45. `, `### 46. `, `### 59. `, `### 60. `, `### 70. `, `### 81. `, `### 82. `, `### 109. `, `### 111. `, `### 115. ` at 1, `### 14. ` 3, `### 17. ` 2; Rule 115's Source line 1), asserts the line and byte deltas against PINNED constants (`EXPECTED_LINES = 19`, `EXPECTED_BYTES = 14712` — a post-condition that can fail, proven at walk 0 on an off-by-one copy, twice: before and after the scout's folds) and only then writes — to a SCRATCH path it refuses to let be the live file or anything under the governance root or the input's git toplevel. Apply = copy the proven scratch output over the live file, then re-measure the live file.

**In the lessons DB (`/Users/marklehn/Developer/forge_lessons/lessons-forge.db`, the mini's one live copy), AFTER the doctrine commit:** one scoped CAS flip — `id IN (453,462,463,464,480,500,509,520,544,559,583) AND status='accepted' AND route='codify'` → `implemented`, `status_updated_by='ceo'` (the delegation's record), a fresh UTC stamp — under a structural pre-check (a TEMP-TABLE `CHECK(x=11)` that aborts the transaction before the UPDATE if the eleven are not all `accepted|codify`), with the capture of every row with `id <= 590` — the whole table at authoring — `(id, status, route, category, status_updated_by, status_updated_at)` to a SCRATCH file BEFORE the UPDATE (copied into the worktree's evidence dir afterwards — never written into the canonical checkout), and the sentinels by name: `PRE_F=11`, `CHANGES_F=11`, `EXCL_F=11` (the eleven rows now `implemented` with a stamp that is NOT one of the TWO vintage stamps — 453, 462, 463 and 464 carry W=29's `2026-09-02T23:54:37Z`, the seven W=30's `2026-09-13T18:27:07Z`), `ACC_POST=51`, `IMPL_POST=368`. The flip SQL is a committed file in governance; a backup of the DB precedes it.

**In the bellows worktree:** the dev log and the QA evidence.

## What this plan does NOT do

- Does not touch the fifty-one `accepted|codify` rows of tranches four to six and the DC/PST tranche (their homes are named in the packet — each is its own plan), DRAFTING_CYCLE.md, PANEL_SEAT_TEMPLATE.md, COMPANY.md, MACHINE_SETUP.md, GLOSSARY.md, code, or threads. Does not push (the Planner pushes after the pause; the Planner's Rule 22 (b) read at the pause takes the canonical block's stdout and the daemon's gate row as the receipt's evidence, never the QA's `PRECHECK: 0 failure(s)` line — at a QA step `tools/check_deposit.py` verifies presence alone, thread 362). Does not project the `LESSONS.md` markers (the Planner's act after the close — `scripts/project_status_markers.py --apply`, the Rule 112 instrument). Does not close thread 336 (the Planner's `threads done` with the seventh tranche).
- ⚠️ **Runs after tranche two (#100114, 2026-09-15 20:19) and thread 322's re-deposited plan (#100118, whose doctrine act — applied 2026-09-16 — is this plan's pre-edit version, LIVE) and BEFORE any W=31 ingest or another tranche** — A0's ladder asserts the accepted population (62 before this plan's flip, 51 after) with every accepted row at one of the two vintage stamps; another tranche landing first moves it, and a W=31 ingest could stale an accepted row (`accepted` is not terminal — `src/lessons_forge.py:31`). The 37 `[status: pending]` headings written since W=30's ingest wait (the marker projection of 2026-09-15 20:19 counted them: `unmatched (no DB row): 37`).
- ⚠️ **PLANNER_TEMPLATE's version is moved by the CEO's doctrine acts at other plans' closes** (#100118's act is this plan's pre-edit version, live since 2026-09-16; tranche one moved twice under its own draft, tranche two not at all): the re-pin surface is the PIN TABLE and its restatements — P1 (sha, lines, bytes); P2 (every line number, which a doctrine act above the anchors shifts, and its `**Version:**` anchor); P4 (the builder's digest, after its two constants `OLD_VERSION`/`NEW_VERSION` are re-committed); P5 (`wc -l`, `wc -c`; the BUILT line's deltas hold unless an act lands inside one of the six homes, which an anchor count or the byte pin refuses); Task C's four version tokens; the `wc -l` restated in A2, A3, both RE-ENTRY arms and QA Item 1 and the `wc -c` in A2; every literal of the new version (the title, the E7/E8 line, the POST description, MUST-PRESERVE, P3, the RE-ENTRY text, Task C, the THREE commit subjects) and the pre-edit-state literals (E7's, the POST description's, P1's, P2's anchor, P3's, Task C's zero probe); the register's Targets line and title — re-pinned TOGETHER at the freeze and before the release by ONE scripted pass over those sites (tranche one's CAPSTONE re-pin is the form), recorded in the register with each literal's count before and after, and Task C's probes re-run on a scratch build of the new version, and the Why-line dedup probes re-run against the new pre-edit version with their `live v4.NNN` label and counts re-pinned with them (the CAPSTONE seat's S3-1); the historical mentions keep their versions; a moved version at A0 is a NONE-MATCH halt and a redeposit, never a guess.

## MUST-PRESERVE

- ⚠️ **TWO REPOSITORIES AND A DB, ONE STEP.** Governance by absolute path, `git -C "$GOV"` never `cd`; porcelain EMPTY and sha = P1 before touching `PLANNER_TEMPLATE.md`; commit by explicit pathspec with the message on stdin; no push. The DB by absolute path; `sqlite3` with `-bail`; a backup (`pre-gate2-pt-w30-c-<HHMMSS>.db` at the forge root, untracked) BEFORE the flip.
- **The builder is the only editor of the template.** Its committed blob digest must equal the on-disk file (P4) before it runs; it runs scratch→scratch; the live file is replaced only by `cp` of a scratch output whose post-conditions the builder already asserted, and the live file is then re-measured (Task C).
- **Doctrine before DB (540's C6):** the flip runs only after the governance commit has landed; a HALT between leaves v4.115 committed-unpushed and the flip owed — the RE-ENTRY arm in A0 resumes at the flip.
- ⛔ **Never run any `checkout --` on an A0 halt except the ONE recipe named there** (a pre-commit apply that died, discriminated by the HEAD numstat, the porcelain and the `### 116. ` token); a pin mismatch otherwise means the file carries changes that are not this plan's.
- ⚠️ **TWO landed texts carry a Rule 20 hedging keyword** — Rule 59's extension says a hand-typed stanza "skipped three runs" and Rule 40's names "an assumed day" (the entries' own words; `skipped`, `assumed`), measured by scanning the builder's 21 added lines against the canonical block's list (walk 0) — so NO receipt row quotes a landed paragraph's body; Task C pins heading fragments and opener fragments only (100026's X-2 / CP-1 class).
- **`known_failures: 0`.** The suite runs in the worktree under the canonical venv; the QA reads its own run's failure count, and a bellows `main` that gained tests since P7 is stated in `probes-raw.txt` under Item 4 (the probe's command and the hashes it printed; the summary line stays in the suite file), never in the receipt's row and never halted on. Any failure is a HALT/Critical.
- ⛔ **`probes-raw.txt` carries no summary-shaped line** — no `N passed` restatement of P7, no pasted suite line, no commit log beyond the path-scoped drift probe's: the suite gate (`gates._select_summary_txt`, bellows `91354d2a`) chooses, among the `.txt` deposits that carry a pytest summary, a basename carrying `suite` or `full`, then the largest `passed` count, then the first match — so a summary-shaped line in the raw file is the one read whenever the suite file crashed without a summary or lost its name (thread 363's defect; its "first `.txt`" wording is narrower than the live gate, measured 2026-09-15 on `71c6bfc3`), and it is forbidden here by name so the selection never rests on that tie-break (tranche one's QA wrote "P7 baseline: 2395 passed, 2 skipped" as a header there — harmless beside a named suite file, forbidden here). P7 is cited as "P7" in the raw file, never restated with its numbers.
- ⛔ **Every commit in the bellows worktree chains `tools/check_deposit.py` before `git add`** (Rule 23; the step IS bound — the commit binding was installed by bellows `4bcde8a9` (#100105, thread 333 — `hooks/git/pre-commit` and the daemon's `BELLOWS_PRECHECK_*` env), and tranches one's and two's chains ran under it), and puts its message on stdin (`git commit -F -` with a quoted heredoc), never in a file.

## Numbers discipline — the pins DEV re-derives (measured 2026-09-15 by the Planner on the v4.114 CANDIDATE — the proven apply of thread 322's texts to live v4.113, byte-deterministic — at governance `25d2106a`, bellows main `e9e435f3`, forge_lessons `a1ebf45`; P4 and P5 re-measured at `5b3d8b01` after the walk-0 scout's folds; P1, P2, P3, P4, P5 and P7 RE-MEASURED on the live files 2026-09-16 after #100118's close and the act — governance `45b6aed4`, bellows main `7da2d8df` (the register's M2–M5); P6 and P8 re-read at the freeze)

| pin | what | value | how |
|---|---|---|---|
| P1 | **`PT_SHA`**, lines, bytes — pre-edit (the LIVE v4.114, thread 322's act at #100118's close, governance `45b6aed4`) | `a2847e81389ef164`; 2582 lines; 543,627 bytes (= v4.113 `6113a26cae73ef2a` / 2581 / 542,403 + the act's History row — dated 2026-09-16, naming the five deposits — and two in-line sentences, +1,224 bytes; the candidate `0496423dbdec346e` / 543,490, measured before the act, is history) | `shasum -a 256 "$GOV/PLANNER_TEMPLATE.md" \| cut -c1-16`; `wc -l`; `wc -c` |
| P2 | **`ANCHORS`** — the builder's nine anchors | each count 1: Rule 39's heading with its preceding blank line (E1, the heading at 936 of the live v4.114) · Rule 46's (E2, 1032) · Rule 60's (E3, 1155) · Rule 115's Source line `*Source: proposal 443, entry 435 (2026-09-01), codified 2026-09-15 (Gate 2, thread 336 — PT tranche one)*` with its `---` and the `## Lifecycle DB Read Protocol (Planner)` heading (E4, 1565–1569) · Rule 82's heading with its blank line (E5, 1321) · Rule 41's (E6, 972) · `**Version:** 4.114` (5) · `**Last Updated:** 2026-09-16 (v4.114)` (6) · the History header pair (2364–2365; thread 322's v4.114 row directly under it) — the line numbers re-measured on the live v4.114 (M2): 936, 1032, 1155, 1565–1569, 1321, 972, 5, 6, 2364–2365, the candidate's exactly | `/usr/bin/grep -cF` (the multi-line anchors by the builder's own assertion) |
| P3 | **`TOKENS`** 0 before, their counts after | `### 116. ` · `**An edit surface is enumerated from the CALLEE's definition` · `**A shared list's consumers are enumerated by member name AND by EFFECT` · `**A recalled fact is checked before it is offered` · `**A vocabulary that looks one value short resolves through its EXISTING states first` · `**A log whose lines carry times without dates` · `**Rule 116 — one contract, ONE parser` · `codified 2026-09-16 — Gate 2, thread 336, PT tranche three` · `codified 2026-09-16 (Gate 2, thread 336 — PT tranche three)` · `**Version:** 4.115` · `\| 2026-09-16 \| v4.115: Gate 2, thread 336` — each 0 before, 1 after but the two codified strings (5 and 1); retired after: `**Version:** 4.114`, `**Last Updated:** 2026-09-16 (v4.114)` — each 1 before, 0 after; invariants: `### 38. ` 1, `### 39. ` 1, `### 40. ` 1, `### 41. ` 1, `### 45. ` 1, `### 46. ` 1, `### 59. ` 1, `### 60. ` 1, `### 70. ` 1, `### 81. ` 1, `### 82. ` 1, `### 109. ` 1, `### 111. ` 1, `### 115. ` 1, `### 14. ` 3, `### 17. ` 2, Rule 115's Source line 1, `\| 2026-09-16 \| v4.114: CEO act at the close of bellows plan 100118` 1, `\| 2026-09-15 \| v4.113: Gate 2, thread 336` 1, `\| 2026-09-15 \| v4.112: Gate 2, thread 336` 1 (unchanged by this plan) | `/usr/bin/grep -cF` |
| P4 | **`BUILDER`** — on-disk digest = the blob at its OWN last commit | `5d6f66518e21c84b` (governance `bfe58149` — the builder's own last commit, the freeze re-pin of 2026-09-16; `5b3d8b01` / `e609a15247f00484` after the scout's folds and `25d2106a` its first are history) | `shasum -a 256 <builder> \| cut -c1-16`; `BC=$(git -C "$GOV" log -1 --format=%H -- governance/knowledge/decisions/drafts/build-gate2-pt-w30-c-2026-09-15.py); git -C "$GOV" show "$BC:governance/knowledge/decisions/drafts/build-gate2-pt-w30-c-2026-09-15.py" \| shasum -a 256 \| cut -c1-16` |
| P5 | **`DRY_RUN`** — the success line, numstat, sizes | `BUILT: <out> edits=9 blocks=1 lines+19 bytes+14712 post=35/35 deltas=pinned`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" <out>` → `21	2` (exit 1 is the expected differing state — read the numbers, never the exit); `wc -l <out>` → 2601; `wc -c <out>` → 558,339 (the live v4.114's — M4; the output `d61b6c1108ab8608`) | as stated, scratch-only |
| P6 | **`FLIP_PRE`** — the eleven rows | 453, 462, 463, 464 each `accepted\|codify\|planner\|2026-09-02T23:54:37Z`; 480, 500, 509, 520, 544, 559, 583 each `accepted\|codify\|planner\|2026-09-13T18:27:07Z`; `SELECT COUNT(*) … WHERE status='accepted'` → 62; `implemented` → 357; `SELECT status_updated_at, COUNT(*) … WHERE status='accepted' GROUP BY 1` → exactly two rows, `2026-09-02T23:54:37Z\|18` and `2026-09-13T18:27:07Z\|44`; `SELECT MAX(id), COUNT(*) FROM lesson_proposals` → `590\|590` | `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db` |
| P7 | **`SUITE`** — the expectation | `2522 passed, 2 skipped`, exit 0 — bellows main `7da2d8df` (#100118's QA, 2026-09-16 10:05: thread 322's thirty-seven items and its two survivor tests on top of `2483` at `ff4ac39c`); the gate reads failures, and `main` moves | `BPY -m pytest tests -q -p no:cacheprovider` |
| P8 | **`NO_INGEST_NO_TRANCHE`** (precondition, in two halves) | UNCONDITIONAL (before the ladder): `SELECT COUNT(*) FROM lesson_entries` → 580 (no W=31 ingest) AND `ls /Users/marklehn/Developer/forge_lessons/knowledge/decisions/ \| /usr/bin/grep -cF -- 'forge-cycle-w31'` → 0 (a zero-match count prints 0 and exits 1 — read the number). ARM-DEPENDENT (ladder step (5)): the accepted population with its at-the-vintages count — `62\|62` on FRESH and flip-owed; `51\|51` on complete — the eleven at their vintages on FRESH and flip-owed (ladder (4)) | as stated |

## Drafting Cycle

**Tier:** T2 — T-6 (the template), T-2 (eleven canonical rows, capped), T-1 fire; T-8 does not (a structure clone). **Panel: SMALL form** (the magnitude proxy measured at walk 0, all three conditions holding) — the walk-0 scout, then EXECUTION, then CAPSTONE, on the Air's session by cross-session message when one is live there; the CEO may call the full form.
**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-c3-2026-09-16.md
**Walk 0 (context pin, measured):** the text carried from `executable-gate2-pt-w30-c.md` at `45caba85` with every fold — walk 1's three, the scout's nine, the EXECUTION seat's one — and with the live re-pin of its sixteen candidate-era sites applied in this v0 (M1); the LIVE v4.114 measured — thread 322's act landed 2026-09-16 at #100118's Done: `a2847e81389ef164` / 2582 / 543,627, the nine anchors at the same lines as on the candidate (M2); the builder re-pinned to the act and the chain re-proven on the live file, every delta unchanged (M3); the dry run on the live file with its five refusals, the pre-act v4.113 blob refused on the version anchor, the hedging scan (M4); the suite pin at #100118's `2522 passed, 2 skipped`, `7da2d8df` (M5); `plan_lint` 0 FAIL and the baseline armed (M6); the consumers under pytest, identical to the -c cycle's (M7); the DB unchanged (M8). Verdict after walk 1: PROCEED.


- Weak spots:          w1 dry; w2 dry; w3 dry



- Destruction:         w1 dry; w2 dry; w3 dry



- Vulnerabilities:     w1 dry; w2 dry; w3 dry



- Integration-record:  w1 dry; w2 3 folded — instruction 1 / record 2; w3 dry


- ACID:                w1 dry; w2 dry; w3 dry





**Cold panel (SMALL form):** _the two seats already convened on THIS text under its first file name — the walk-0 SCOUT (`walk-register-gate2-pt-w30-c-2026-09-15.md`, rows s1–s9, all folded) and the EXECUTION seat on the Air (`seat-packet-execution-gate2-pt-w30-c-2026-09-15.md`, one LOW record finding, folded); the CAPSTONE seat follows walk 1 here, reading the whole fold set; the meter is the -c register's._
**Closing:** ✅ **BAR MET — walk 3 dry on every lens after walk 2's three folds (instruction 1 / record 2 — the CAPSTONE seat's S3-1 date pair, S3-2, S3-3) on the carried text; walk 1 dry ×5 on the v0 that carried the live re-pin. The content — five extensions on Rules 38, 40, 45, 59 and 81, one new Rule 116, the eleven rows' flip — is the text the walk-0 scout (nine folds) and the EXECUTION seat (one) read under the first file name and the CAPSTONE seat read under this one, its freeze checklist MATCHING on the LIVE v4.114 (P1, P2, P4, P5; Task C 25/27 before the fold, 27/27 after). The record was re-filed twice for the Planner's own batching errors, each stated in its register; the observer's rename defect found under the re-filing is fixed (bellows dc17a673). Residue: none. Direction verdict after walk 1: PROCEED. Released under the CEO's delegation of 2026-09-15 ("release at the proper time"), after #100118's Done and the v4.114 act; never two bellows plans concurrently.**

## Cycle Manifest
tier: T2
target: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md
class: shop-infra
reads: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-pt-w30-c-2026-09-15.py, /Users/marklehn/Developer/eluvian-governance/gate2-packet-2026-09-15.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-02.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-a.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-b.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-c.md, /Users/marklehn/Developer/eluvian-governance/LESSONS.md, knowledge/decisions/Done/executable-100114.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-540.md, /Users/marklehn/Developer/forge_lessons/lessons-forge.db
writes: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30c-flip.sql, /Users/marklehn/Developer/forge_lessons/lessons-forge.db, knowledge/development/dev-log-gate2-pt-w30-c-2026-09-15.md, knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt, knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/qa-receipt.md, knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/probes-raw.txt, knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/full-suite-gate2-pt-w30-c.txt
open_forks: 1. the fifty-one accepted rows of tranches four to six and the DC/PST tranche (their own plans, in the packet's order); 2. Rule 116 follows Rule 115 (numbering append-only) — the packet's "beside Rules 70 and 109" is carried by the kin clause that opens its body, not by its position; 3. the `LESSONS.md` markers are projected after the close by the Planner, not by this plan; 4. the two hedging keywords in the landed texts are the entries' own words and stand — the receipt rule carries them
walks: 3
yields: 0, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:105
coherence: 3/3 body walks named in the register (3 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-gate2-pt-w30-c3.md.foldcheck.json

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the bellows Developer. ⚠️ Governance runs IN PLACE at `$GOV` (every operand ABSOLUTE); commits there by explicit pathspec; ONE action per compound. ⛔ Every commit's message goes to `git commit -F -` on stdin — a quoted heredoc ending the commit's own command: `<<'MSG'`, the message on the lines after it, `MSG` alone on the last — so no file is written for it. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** (`BPY`); the builder runs under `python3` as tranche one's did (it imports only the stdlib — `os`, `sys`, and `subprocess` for the toplevel guard).
>
> ⛔ **A0 — roots, the precondition, then the branch ladder (catch-all LAST):** `cd "$(git rev-parse --show-toplevel)" && [ -f bellows.py ] && [ -d tests ] && echo TREE_OK` — HALT unless TREE_OK; `GOV=/Users/marklehn/Developer/eluvian-governance; PT="$GOV/PLANNER_TEMPLATE.md"; B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-c-2026-09-15.py"; DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db; [ -f "$PT" ] && [ -f "$B" ] && [ -f "$DB" ] && echo GOV_OK` — HALT unless GOV_OK. Re-derive `GOV`, `PT`, `B`, `DB` in every compound. `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python`. **P8's UNCONDITIONAL half → both values, else HALT** (`lesson_entries` → 580 and the `forge-cycle-w31` count → 0; on a mismatch: `a W=31 cycle has started — re-pin P6/P8 and re-deposit`). **P8's accepted-population half is read INSIDE the ladder, never before it:** it changes when THIS plan's flip lands. Then the ladder — **(1)** P1's sha matches; **(2)** `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30c-flip.sql` (state it; EMPTY on FRESH); **(3)** `git -C "$GOV" log -1 --format=%s -- PLANNER_TEMPLATE.md` — does the subject carry `gate2-pt-w30-c`?; **(4)** `sqlite3 -readonly "$DB" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (453,462,463,464,480,500,509,520,544,559,583) ORDER BY id;"`; **(5)** `sqlite3 -readonly "$DB" "SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted')||'|'||(SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z'));"` — the accepted population and how many of it sit at the two vintage stamps (every accepted row does, on EVERY arm).
> - **FRESH** = (1) match AND (2) empty AND (3) no AND (4) the eleven `accepted|codify` at their vintages (453, 462, 463 and 464 at `2026-09-02T23:54:37Z`, the seven at `2026-09-13T18:27:07Z`) AND (5) `62|62` → A1.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` (a dirty template on re-entry is a FOREIGN edit — HALT untouched, quoting the porcelain) AND (4) the eleven still `accepted|codify` at their vintages AND (5) `62|62` → verify the COMMITTED state (the Task C counts on the live file, `wc -l` 2601, and `C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show --stat --format= "$C" -- PLANNER_TEMPLATE.md` → 21 insertions, 2 deletions — the PATH-SCOPED commit, never `HEAD`: once A4's SQL commit or any later governance commit lands, `HEAD` no longer names the template's commit and a `HEAD`-scoped show prints NOTHING with exit 0), then A4 (the flip) and A5 only. If (2) shows `g2ptw30c-flip.sql` untracked or modified with the flip NOT applied, that is this arm too — A4 rewrites the file from the exact text, which is idempotent.
> - **RE-ENTRY (complete)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` AND (4) the eleven `implemented|codify` with every stamp NOT one of the two vintages AND (5) `51|51` → verify the COMMITTED state (the Task C counts, `wc -l`, and the path-scoped show → 21/2); then the two idempotent tails of A4: the SQL file — if `git -C "$GOV" log -1 --format=%s -- governance/knowledge/development/g2ptw30c-flip.sql` does not carry `gate2-pt-w30-c`, write it from the exact text and commit it; the capture — if `/tmp/g2ptw30c/flip-capture.txt` exists copy it into the worktree, else reconstruct the worktree copy from the newest pre-flip backup — `BK=$(ls -t /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-c-*.db | head -1)` (each A4 attempt mints a second-stamped file; the newest is the one whose flip landed), then `[ -f "$BK" ] && echo "BK=$BK"` — no file is a HALT (`no pre-flip backup found — the capture cannot be reconstructed; re-deposit`), never an empty `$BK` handed to sqlite, which would open a scratch database and fail on the table — `sqlite3 -readonly "file:$BK?immutable=1" "<the capture SELECT>"` → the same 590 lines, labelled `RECONSTRUCTED (post-commit re-entry)` in the dev log); then A4's read-back (no flip) and A5 with the dev log marked `re-derived on re-entry`.
> - **NONE-MATCH** = anything else → HALT quoting every measurement — and when (5)'s SECOND number ≠ its first on ANY arm, the HALT reads `an accepted row carries a stamp outside the two vintages — an ingest or a foreign flip has moved the population; re-pin P6/P8 and re-deposit`; when (3) = no and (4) reads the eleven `implemented` with (5) `51|51`, it reads `this plan's flip has landed and a later act moved PLANNER_TEMPLATE.md after its commit — find the template's commit with git -C "$GOV" log --format=%h --grep=gate2-pt-w30-c -- PLANNER_TEMPLATE.md, verify it as the complete arm does (its show --stat → 21/2), then run the complete arm's idempotent tails`; when (3) = no and (5)'s first number ≠ 62 with the two numbers equal and (4) still reads `accepted`, it reads `the accepted set moved — another Gate 2 tranche has landed first, or an ingest staled an accepted row (accepted is not terminal, src/lessons_forge.py:31); re-pin P6/P8 and re-deposit`; when (1) mismatches and (3) = no and the template's porcelain is EMPTY, it reads `PLANNER_TEMPLATE.md moved under this plan — a doctrine act landed after the freeze; re-pin P1 and the builder's version constants, and re-deposit`. The ONE recipe: (1) mismatch + (3) no + (4) the eleven `accepted|codify` at their vintages + (5) `62|62` = a pre-commit apply that died — BUT ONLY IF the dirty file is THIS plan's build: `git -C "$GOV" diff HEAD --numstat -- PLANNER_TEMPLATE.md` → `21	2` (against HEAD, never the bare index-relative `diff`, which prints NOTHING in the staged half-state this recipe exists for) AND `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md` → ` M` or `M ` (never `MM`: a staged foreign edit beneath an unstaged build would pass the HEAD numstat and be discarded) AND `/usr/bin/grep -cF -- '### 116. ' "$PT"` → 1 (a foreign hand edit to the template presents the SAME (1)+(3)+(4)+(5) shape and must never be discarded; any other numstat or porcelain → HALT untouched, quoting it) → `git -C "$GOV" checkout HEAD -- PLANNER_TEMPLATE.md` (`HEAD`, not the index — a death between `add` and `commit` leaves v4.115 STAGED), re-assert P1, then A1. Every other shape HALTs untouched.
>
> ⛔ **A1 — re-derive P1–P6; state each; a mismatch is a HALT quoting both.** (P4 with the builder's OWN commit, never `HEAD:`.)
>
> **A2 — dry-run, scratch→scratch (LITERAL paths, no variables across compounds):** `rm -rf /tmp/g2ptw30c; mkdir -p /tmp/g2ptw30c; python3 "$B" "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30c/PT-out.md; echo "builder_exit=$?"` → P5's `BUILT:` line verbatim, `builder_exit=0`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30c/PT-out.md` → `21	2`; `wc -l /tmp/g2ptw30c/PT-out.md` → 2601; `wc -c` → 558339. Refusals (each `BUILDER REFUSED`, nonzero; quote each): `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/PLANNER_TEMPLATE.md"` (out == in); `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/x.md"` (under a forbidden root — the literal governance root, or the input's git toplevel when the input is inside a repository, as the live template is, so both bases are active here; no `x.md` is written. The EXECUTION seat's S2-1: on a NON-git input — the scratch candidate — only the literal root refuses, so a reproduction build sends its output to `/tmp`, never under a clone); `python3 "$B" /tmp/g2ptw30c/PT-out.md /tmp/g2ptw30c/PT-out2.md` (already built).
>
> **A3 — apply and measure the LIVE file (Task C), then commit:** `cp /tmp/g2ptw30c/PT-out.md "$GOV/PLANNER_TEMPLATE.md"`; then from the live file, counts read never exit codes (`/usr/bin/grep -cF -- '<token>' "$PT"`, each token as one single-quoted literal): `### 116. One contract, ONE parser, every consumer calls it` 1 · `**Rule 116 — one contract, ONE parser` 1 · `*Source: proposal 443, entry 435 (2026-09-01), codified 2026-09-15 (Gate 2, thread 336 — PT tranche one)*` 1 · `**An edit surface is enumerated from the CALLEE's definition` 1 · `**A shared list's consumers are enumerated by member name AND by EFFECT` 1 · `**A recalled fact is checked before it is offered` 1 · `**A vocabulary that looks one value short resolves through its EXISTING states first` 1 · `**A log whose lines carry times without dates` 1 · `codified 2026-09-16 — Gate 2, thread 336, PT tranche three` 5 · `codified 2026-09-16 (Gate 2, thread 336 — PT tranche three)` 1 · `**Version:** 4.115` 1 · `**Version:** 4.114` 0 · `**Last Updated:** 2026-09-16 (v4.115)` 1 · `**Last Updated:** 2026-09-16 (v4.114)` 0 · `| 2026-09-16 | v4.115: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.114: CEO act at the close of bellows plan 100118` 1 · `| 2026-09-15 | v4.113: Gate 2, thread 336` 1 · `| 2026-09-15 | v4.112: Gate 2, thread 336` 1 (the three rows below the new one survive) · `## Lifecycle DB Read Protocol (Planner)` 1 · `### 38. ` 1 · `### 45. ` 1 · `### 59. ` 1 · `### 81. ` 1 · `### 40. ` 1 · `### 115. ` 1 · `### 14. ` 3 · `### 17. ` 2 (unchanged); `wc -l` 2601; `cmp /tmp/g2ptw30c/PT-out.md "$GOV/PLANNER_TEMPLATE.md"` silent; `git -C "$GOV" diff --stat -- PLANNER_TEMPLATE.md` → 21 insertions, 2 deletions. Any failing → FRESH: `git -C "$GOV" checkout -- PLANNER_TEMPLATE.md` + HALT; RE-ENTRY: HALT, no restore. Commit, the message on stdin: `git -C "$GOV" add PLANNER_TEMPLATE.md && git -C "$GOV" commit -F - -- PLANNER_TEMPLATE.md <<'MSG'` with the subject `[<id from your plan filename>] gate2-pt-w30-c: PLANNER_TEMPLATE v4.115 — five extensions (453, 544+462+500, 463+464+480, 520+559, 583), Rule 116 (509)` and `MSG` closing it; `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` → that commit. Do NOT push.
>
> **A4 — the flip (doctrine before DB):** backup first, by sqlite's own copy — the DB is WAL-journaled, so a filesystem `cp` can lag a non-checkpointed write while sizes match: `sqlite3 "$DB" ".backup /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-c-$(date +%H%M%S).db"`; then on the backup — ⚠️ through `sqlite3 -readonly "file:<backup>?immutable=1"`, never a bare `-readonly <path>`: a `.backup` output of a WAL database has no `-shm` sidecar and a bare read-only open fails `unable to open database file (14)` — `PRAGMA integrity_check` → `ok`; `SELECT COUNT(*) FROM lesson_proposals` → equal to the live count (state both); `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (453,462,463,464,480,500,509,520,544,559,583) AND status='accepted'` → 11 (`BK=11`). Write `$GOV/governance/knowledge/development/g2ptw30c-flip.sql` with a QUOTED heredoc, EXACTLY:
> ```
> .timeout 5000
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=11));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (453,462,463,464,480,500,509,520,544,559,583) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output /tmp/g2ptw30c/flip-capture.txt
> SELECT COALESCE(id,'')||'|'||COALESCE(status,'')||'|'||COALESCE(route,'')||'|'||COALESCE(category,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 590 ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (453,462,463,464,480,500,509,520,544,559,583) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (453,462,463,464,480,500,509,520,544,559,583) AND status='implemented' AND status_updated_at NOT IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z');
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> ⚠️ The `.output` path is the LITERAL scratch dir A2 created (`sqlite3` dot-commands take no variables) — never a path inside the canonical bellows checkout. Run: `[ -d /tmp/g2ptw30c ] || mkdir -p /tmp/g2ptw30c; sqlite3 -bail "$DB" < "$GOV/governance/knowledge/development/g2ptw30c-flip.sql"` (⚠️ `-bail` is LOAD-BEARING: with the capture dir missing it stops at `.output` and the transaction never commits; without it sqlite skips the failed dot-command and the flip COMMITS with no capture file) → the five sentinel lines BY NAME: `PRE_F=11`, `CHANGES_F=11`, `EXCL_F=11`, `ACC_POST=51`, `IMPL_POST=368` (a `CHECK` abort prints a line beginning `Runtime error near line 4: CHECK constraint failed: x=11` and writes nothing — HALT on that MESSAGE; never read the exit code through a pipe, it is the last command's). **Read-back on a FRESH read-only connection** (durability is proven post-commit, never by in-transaction sentinels): the eleven rows `implemented|codify|ceo|<stamp>` with every stamp outside the two vintages; `accepted` 51; `implemented` 368; the capture file has 590 lines and 62 rows containing `|accepted|codify|`. Copy the capture into the worktree: `mkdir -p knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15 && cp /tmp/g2ptw30c/flip-capture.txt knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt` (A5 commits it; QA verifies it). Commit the SQL in governance, the message on stdin: `git -C "$GOV" add governance/knowledge/development/g2ptw30c-flip.sql && git -C "$GOV" commit -F - -- governance/knowledge/development/g2ptw30c-flip.sql <<'MSG'` with the subject `[<id>] gate2-pt-w30-c: the flip SQL (453, 462, 463, 464, 480, 500, 509, 520, 544, 559, 583 -> implemented)`.
>
> **A5 — dev log + commit by explicit pathspec, the pre-check chained first.** `knowledge/development/dev-log-gate2-pt-w30-c-2026-09-15.md`: the A0 determination, A1's pins, A2's lines and the three refusals, A3's counts and the governance commit hash, A4's backup lines, sentinels, read-back and the SQL commit hash. Then, as ONE command: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/development/dev-log-gate2-pt-w30-c-2026-09-15.md knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt && git commit -F - -- knowledge/development/dev-log-gate2-pt-w30-c-2026-09-15.md knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt <<'MSG'` with the subject `[<id>] gate2-pt-w30-c: PT v4.115 + flip landed (dev log, flip capture)` and `MSG` closing it. `git status --short` → empty. STOP.
>
> **Deposits:**
> - `knowledge/development/dev-log-gate2-pt-w30-c-2026-09-15.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30c-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt`
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-pt-w30-c-2026-09-15.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30c-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt`

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message.** You are the bellows QA agent. `cd "$(git rev-parse --show-toplevel)"`; `GOV=/Users/marklehn/Developer/eluvian-governance`; `B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-c-2026-09-15.py"`; `DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db`; `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python` — re-derive per compound. Read-only beyond the three evidence files; the commit's message goes on stdin, as STEP 1's header states.
>
> **(A) Rule 20 self-check** — the canonical block at the path the dispatcher's mandate names (this machine's `RULE_20_SELF_CHECK_BLOCK.md`; quote the path you were handed). Run with:
> - `plan_slug`: `gate2-pt-w30-c-2026-09-15`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15"`
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-pt-w30-c.txt", "flip-capture.txt"]` (the capture committed by DEV in A5; QA verifies it is the committed file — `git log -1 --format=%s -- knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt` carries the plan's id)
>
> **(B) Items — raw output appended to `probes-raw.txt`** (⛔ the raw file carries NO summary-shaped line: P7 is named as "P7", never restated with its numbers; the suite's summary line stays in the suite file; no commit log is pasted beyond Item 4's path-scoped probe — MUST-PRESERVE's raw-file rule, thread 363):
> - **Item 1 — the live template:** `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` (the `[<id>]` commit); every Task C count re-run (twenty-seven probes); `wc -l` 2601; `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30c-flip.sql` → EMPTY.
> - **Item 2 — the builder, by a second pair of hands (byte-identity):** `rm -rf /tmp/g2ptw30c-qa; mkdir -p /tmp/g2ptw30c-qa; C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show "$C^:PLANNER_TEMPLATE.md" > /tmp/g2ptw30c-qa/PT-pre.md; python3 "$B" /tmp/g2ptw30c-qa/PT-pre.md /tmp/g2ptw30c-qa/PT-out.md; echo "builder_exit=$?"; cmp /tmp/g2ptw30c-qa/PT-out.md "$GOV/PLANNER_TEMPLATE.md" && echo BYTE_IDENTICAL` → the `BUILT:` line, `builder_exit=0`, `BYTE_IDENTICAL`; P4 with the builder's own commit; the three refusals reproduced.
> - **Item 3 — the flip, read back on a FRESH read-only connection:** the eleven rows `implemented|codify|ceo|<stamp outside the two vintages>`; `accepted` 51 — and the fifty-one by count, each still at one of the two vintages (`SELECT COUNT(*) … WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z')` → 51); `implemented` 368; `wc -l knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/flip-capture.txt` → 590; the capture's eleven rows for the flipped ids read `accepted|codify` (the pre-flip state, captured before the UPDATE).
> - **Item 4 — the full-suite file:** `"$BPY" -m pytest tests -q -p no:cacheprovider > knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/full-suite-gate2-pt-w30-c.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/full-suite-gate2-pt-w30-c.txt` → `exit=0` and a summary line with NO `failed`/`error` count — P7's counts are the expectation, but the gate (`_gate_qa_test_result`) reads FAILURES against `known_failures`, never the pass count: if bellows `main` moved since P7 was measured (`git log --format=%h <P7's commit>..HEAD -- tests` non-empty — P7's commit is the anchor, re-pinned 2026-09-16 to #100118's QA commit `7da2d8df`), the drift is stated in `probes-raw.txt` under Item 4 as that probe's command and the HASHES it printed (`--format=%h`, never `--oneline` — a bellows QA commit's subject carries `N passed`, the summary shape the raw file must not hold), and nothing broader; the receipt's suite row stays the FILE and `exit=0`, never a note: a drift note reaches for `not run`, `skipped` or `pending`, and the canonical block fails a positive row carrying any of them. A different pass count with zero failures is not a halt. ⛔ A suite file WITHOUT a summary line (a crashed run — a traceback, a killed process) is a HALT, never a re-run note and never a second attempt written over it: with the named suite file summary-less the gate falls to the next `.txt` carrying one, and the raw-file rule above is the only thing standing between that and a stale count (the EXECUTION seat measured both configurations, S2-1).
>
> **(C) The report** `qa-receipt.md`: the verification table — status cells carry the glyph only, and NO positive row quotes a probe token or an output line carrying a Rule 20 hedging keyword (`hedging_keywords` in the canonical block): this plan has THREE such sources — the suite summary line, the landed bodies of Rule 59's and Rule 40's extensions, and the agent's OWN prose (a drift note, a re-run note: `not run`, `skipped` and `pending` are the words such a note reaches for, and the block fails a positive row carrying any of its `hedging_keywords` whoever wrote it) — so the suite row names the FILE and `exit=0` with the drift left in `probes-raw.txt`, and Item 1's row reads `27/27 counts as pinned in A3, see probes-raw.txt` (the raw file may carry the tokens; the receipt's positive rows may not — a row quoting any of them FAILS the self-check) — the follow-ups (the four tranches after this one; the Planner pushes governance and projects the markers; thread 336 at the keyboard), the Rule 20 stdout APPENDED. Commit, as ONE command with the pre-check chained and the message on stdin: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/ && git commit -F - -- knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/ <<'MSG'` with the subject `[<id>] QA: PT v4.115 rebuilt byte-identical from the pre-edit blob; flip read back (11 implemented, 51 accepted remain)` and `MSG` closing it. STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/full-suite-gate2-pt-w30-c.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-c-2026-09-15/full-suite-gate2-pt-w30-c.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
