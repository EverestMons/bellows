# governance — seat-contract honing unit (b): P-6 (PST §6 FIX field) + P-7 (DC §2.6 clone-diff cross-join)
**Date:** 2026-08-21 | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** none (governance doctrine edit; no gate side — see §6 discharge) | **Execution:** Step 1 (GOVERNANCE, in-place) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

## Context

**Unit (b)** of the drafting-cycle honing backlog ([[drafting-cycle-honing-routing-arc]]) — the cold-seat-contract proposals from `governance/knowledge/research/drafting-cycle-honing-notes-2026-08-18.md` (Finding 9), routed by the non-author Gate-1 read of this arc. Unit (a) shipped as DC v2.14 (exec-487); unit (c) (`plan_lint.py` Finding-6) stays queued.

**⚠️ Two routing corrections to the Gate-1 batching, both verified against the live files (recorded so this is not read as scope drift):**
- **P-7's target is `DRAFTING_CYCLE.md` §2.6, NOT `PANEL_SEAT_TEMPLATE.md`.** The clone-diff BRIEF text lives at `DRAFTING_CYCLE.md:117` (the seat-brief registry), not in PST (which carries the invariant body + per-seat slots, and only *references* the §2.6 registry). The reviewer's "P-7 → PST" was a location error; the faithful home is the §2.6 clone-diff bullet.
- **P-8's "seat half" does NOT exist as residual** — the WHOLE of P-8 (the scoped execution re-run + the exactly-ONE-re-run bound) already shipped in unit (a) as Amendment E, live at `DRAFTING_CYCLE.md:140`. Nothing of P-8 remains; the reviewer's "P-8(seat half)" was a mis-split.

**So unit (b) = two genuine remaining seat-contract proposals across two ROOT files (one commit — both under the top-level `.git`):**
- **A — P-6 → `PANEL_SEAT_TEMPLATE.md` §6 (reporting contract):** rename the seat's `smallest honest FIX` field to a **PROPOSED FIX = UNEXECUTED HYPOTHESIS** the Planner verifies before folding (unless the seat executed it and shows output). Self-implicating evidence: exec-487's own DISCOVERY/EXECUTION panel + the honing cycle both folded a seat's unrun fix. PST v1.1→**1.2**.
- **B — P-7 → `DRAFTING_CYCLE.md` §2.6 (clone-diff brief, L117):** add a closing CROSS-JOIN step — for every filed defect scan the dropped-hunk list for its fix; for every dropped hunk ask whether a filed defect is its consequence. DC v2.14→**2.15**.

**This is a governance IN-PLACE dispatch** ([[governance-in-place-dispatch]]): both files live at the GitHub root under the top-level `.git` (no project `.git`) → ABSOLUTE operands, a DOUBLE triple-pin (PST pre-edit blob `42717db`, DC pre-edit blob `2dace6b`), `git add <both abspaths>` before a pathspec commit, `git rev-parse` in the commit compound ([[phase-commits-assert-the-repo]]). NOT a worktree. Both edits are **count-1 anchor REPLACEMENTS** (in-place, not appends) → [[edit-anchor-is-not-a-probe]] governs: assert each anchor occurs exactly once, temp-and-replace, never "first match".

**CEO-authorized direct amendment — a declared §6 deviation** (closest-shape precedent **v2.14/exec-487** — the same non-author-routed honing arc; also v2.13/2.12): Planner-authored honing lessons whose subject is the cycle itself, routed at Gate 1 by a NON-AUTHOR (this arc's cold review — the strengthening the notes' thrice-stated author-disclosure demands), not the lessons-forge DB path. `proposed = 0, no batch, no DB flip` — not a lessons-forge codification. PST has no §6-style Iteration Protocol of its own; it versions in lockstep with DRAFTING_CYCLE.md (its §2.6 owner), so the PST bump rides this same declared deviation.

**§6 coordinate-doctrine-and-gate:** NO new gate edit. Both edits are §2.6/PST prose; no `plan_lint`-parsed structure changes (§4 unchanged, in lockstep). Token sweep in QA (positive control `Drafting Cycle` present in plan_lint; the new tokens = 0 functional reads). Unit (c)'s `plan_lint` Finding-6 fix stays declared-deferred.

## Drafting Cycle
**Tier:** T2 — triggers fired: T-6 (governance surface: edits two doctrine files), T-8 (novel: first seat-contract FIX-field + clone-diff-cross-join amendment). Cold panel owed at close (§2.6). **Panel FORM: FULL** (Fork-C magnitude proxy fails proxy (ii) — hand-anchored in-place doctrine-prose edits, not a committed-builder mechanized clone; sibling exec-487 ran full form). cycle_check run from walk 2 onward (scratchpad draft → register asserts N/A; [[cycle-automation-depositor-arc]]).
**Walk 0 (context pin) — 5 measurements, REAL (2026-08-21):**
1. Newest same-class (clone-diff target): **[487] `582b84a` 2026-08-21 (DC v2.14, honing unit a)** — the immediately-prior governance in-place honing amendment; the FORM clone this draft is built from.
2. Anchors — **2 count-1 in-place REPLACEMENTS** (not appends): P-6 = `smallest honest FIX` (PST L25, count 1); P-7 = `A "proven clone" framing raises this seat's value; it never waives it.` (DC L117, count 1). Plus 2 Version lines (PST L5, DC L5) and 2 History rows PREPENDED (each file newest-first).
3. Occurrence counts (pre-edit): P-6 anchor = 1, P-7 anchor = 1; new tokens (`PROPOSED FIX`, `CROSS-JOINING its two lists`, `UNEXECUTED HYPOTHESIS` — the exact literals the replacements + QA asserts use) = 0 across both files → clean.
4. Last writer: PST L25 = v1.0 (2026-08-12, proposal 327); DC L117 = v2.9 (2026-08-13, clone-diff brief). Both settled `implemented` — no in-flight plan owns them.
5. Target shas: PST **`42717db`**, DC **`2dace6b`** (both `HEAD:<file>` blob == working tree; porcelain clean).
- Clone-diff vs newest (487): 487 was 6 append-only bullets on ONE file; this is 2 in-place REPLACEMENTS across TWO files — a different edit class (replacement, not append), so the subtractive-safety discipline ([[edit-anchor-is-not-a-probe]]: count==1, temp-and-replace) is applied fresh, not cloned. No origin drift.
- Cold scout (T2, §2.0): **RUN (walk 1)** — fresh-context, integration-record-shaped, author-verified. Verdict: **ANGLE RIGHT, no forcing problem.** BOTH routing corrections confirmed TRUE against live files (P-7's home is DC §2.6/L117 — clone-diff brief absent from PST; P-8 fully shipped at DC L140, no residual); both anchors count==1 in place; shas/versions/pins match; both amendments faithful to notes Finding 9 (both directions for P-7). 1 MED + 1 LOW folded/cleared.
**Direction verdict (after walk 1): PROCEED.** The angle — codify P-6 (PST §6) + P-7 (DC §2.6) as two count-1 in-place replacements — is right and shippable. No forcing finding: clone-origin (487 same-class), edit-mechanism (count-1 anchors verified), and scope-premise (the two routing corrections verified) all hold.
**Walk 1 — cold scout + 5 warm lenses, sequential folds applied:**
- Weak spots:         w1 dry — P-6/P-7 replacements grammatical, no gate token introduced, each carries its proposal faithfully.
- Destruction:        w1 dry — renaming `smallest honest FIX` removes nothing (only site is PST:25, verified); P-7 is purely additive to the clone-diff bullet.
- Vulnerabilities:    w1 dry — the P-7 anchor's `seat's` apostrophe is correctly bash-escaped in the STEP assert (`'\''`); count-1 verified live.
- Integration-record: w1 1 folded — instruction 0 / record 1 (SC-MED: walk-0 pin measurement 3 named `CROSS-JOINING the two lists` while the replacement + QA assert write `its` — a pin-narrative token drift (the plan's own record) → swept to `its`, matching the landed literal; [[assert-the-post-condition-not-the-artifact]]).
- ACID:               w1 dry — two-file edit is atomic in one commit; the two version bumps + two History rows are mutually consistent; no cross-requirement conflict.
**Walk 1 STATUS:** 1 folded — instruction 0 / record 1 — not dry (the sole fold is record-class; instruction surface dry on walk 1, but a single walk does not close the bar — walk 2 confirms).
**Walk 2 — warm re-walk + consistency sweep (all 5 lenses):**
- Weak spots / Destruction / Vulnerabilities / ACID: w2 dry.
- Integration-record: w2 dry — token sweep confirms the walk-1 fix has NO sibling site: pin (L30) + replacement (L65) + QA assert (L74) all consistently `its two lists`; the sole `the two lists` (L40) is the fold-note NARRATING the correction (legitimate record prose, [[fold-sweep-every-site]] swept). Anchors both still count==1.
**Walk 2 STATUS:** **DRY — 0 folded. §2 BAR MET at walk 2** (instruction-dry both walks; the walk-1 fold was record-class). cycle_check (dogfood): **BAR_MET** (exit 0). The close is NOT final — the T2 FULL cold panel (§2.6) is owed.
**Cold panel (§2.6, FULL form — meter opened at convene. Scout @walk 1; DISCOVERY + EXECUTION concurrent; capstone pass over the fold set):**
- **Seat DISCOVERY (weak-spots + destruction; evidence-attack + clone-diff briefs) — RUN, ~79k tokens.** Verdict: ANGLE RIGHT, no forcing problem. Both routing corrections re-verified TRUE (P-7 home DC L117; P-8 shipped DC L140); v2.8 lockstep precedent CONFIRMED (`DRAFTING_CYCLE.md:315` landed PST v1.1 in the same row). **Duplication verdict — the highest-value output — NO REDUNDANCY:** P-7's cross-join (findings-list × dropped-hunk tally, clone-diff seat) is a DIFFERENT cross-product from the capstone's probe×fold check (§2.6 L110) and the system brief's instruction-vs-record drift; no existing brief joins a seat's findings to its own dropped-hunk list. 1 MED folded (**DISC-MED**: disambiguate P-7 from the capstone cross-check in the DC History row → done), 2 LOW cleared (no skip-a-fix loophole — the UNLESS branch only drops the *Planner's* re-verify duty, a strengthening; §6-deviation framing consistent + self-flagged).
- **Seat EXECUTION (vulnerabilities + execution brief) — RUN, ~53k tokens.** Rehearsed the full 2-file edit + verification chain on scratch copies + a scratch git repo (blobs seeded to `42717db`/`2dace6b`; **live files provably untouched**, porcelain empty end-to-end). ALL edit-mechanism probes PASS: both anchors count-1 + unique, both replacements grammatical + markdown-clean (P-6 mid-list, P-7 leaves the trailing `(Measured:…)` intact/single), `seat's` apostrophe greps under `'\''`, `git diff HEAD` = `2 files, 6 ins, 4 del` (by content: PST FIX-line+Version+History; DC bullet+Version+History; nothing else removed), unit-a non-regression clean, one-add-one-commit spans both root files. **1 MED — the forcing self-check catch (EXEC-MED):** the STEP's `grep -Fc 'PROPOSED FIX' == 1` measured **2** because the mandated PST History row also contains the literal → false-FAIL on a correct edit ([[earn-the-clean-gate-dont-author-it]], same class as unit-a's CAP-HIGH). Folded: assert site-anchored to `EVIDENCE (command + raw output), **PROPOSED FIX**` == 1, with the whole-file ==2 documented.
- **Capstone pass over the fold set (Planner, per §2.6 "a further capstone pass"; the fold set = 2 mutually-verified edits) + FREEZE below.** Fold-interaction check: the DISC-MED edit (DC History row) PRESERVED the `CROSS-JOIN step` literal that the EXEC-MED note cites as the reason `grep -Fc 'CROSS-JOINING its two lists'` stays == 1 — the two folds are consistent, neither re-opens the other. Record coherence: yields reconcile (w1 1 record / w2 dry; panel MED×2 folded), Closing marker withheld, both routing corrections in the DC History row.
**Cold panel COMPLETE (full form, 4 seats: scout + DISCOVERY + EXECUTION + capstone pass; ~215k tokens).** The panel earned its cost: EXECUTION caught a forcing self-check defect (EXEC-MED, `PROPOSED FIX`==2-not-1) that 2 warm walks + a cold scout missed, and DISCOVERY confirmed P-7 is non-redundant against 3 existing briefs.
**Bar re-met post-panel:** the 2 panel folds (EXEC-MED assert site-anchor + DISC-MED disambiguation) are applied and re-verified in the FREEZE below; they are mutually consistent (neither re-opens the other); the fold set is instruction-dry, so the bar is re-met on the folded draft and the panel round is closed (capstone pass covered the fold set, §2.6).
**Deposit freeze (§5 + §2.6):** cycle_check **BAR_MET**; both replacements rehearsed on scratch copies with the CORRECTED asserts firing (site-anchored `PROPOSED FIX` field ==1, whole-file ==2 documented, `smallest honest FIX`==0, `CROSS-JOINING its two lists`==1); unit-a non-regression + trio intact; PST §6 fields intact; **live files provably untouched**. `propagation_check` N/A (no numbers table). Gate-token sweep: new tokens 0 functional reads in plan_lint/gates.
**Closing:** 2 warm walks (bar met walk 2, instruction-dry; the walk-1 fold was record-class) + walk-0 cold scout + FULL cold panel (4 seats, ~215k tokens, 3 folds incl. the EXEC-MED forcing self-check); bar re-met post-panel; closing-record re-read run (this block), dry; cycle done — deposit exactly once. ⚠️ Governance in-place + doctrine-amendment (register-writing) class → **HOLD FOR CEO** (not auto-deposit); the deposit is CEO-authorized.

---
---

## STEP 1 — GOVERNANCE DOCTRINE EDITOR (in-place, two root files)

---

> **Identity:** You amend TWO files at the GitHub root in place (top-level `.git`, NO worktree): `/Users/marklehn/Developer/GitHub/PANEL_SEAT_TEMPLATE.md` (Amendment A) and `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (Amendment B). Apply A + B **and their bookkeeping (each file's Version-line bump + a PREPENDED History row); touch NO OTHER rule, section, or line.** Both edits are count-1 anchor REPLACEMENTS: assert the anchor occurs exactly ONCE, then replace that exact span; never "first match" ([[edit-anchor-is-not-a-probe]]).
>
> **⚠️ Double triple-pin (governance in-place):** before ANY edit, assert BOTH — `git -C /Users/marklehn/Developer/GitHub rev-parse HEAD:PANEL_SEAT_TEMPLATE.md` == `42717db...` AND `HEAD:DRAFTING_CYCLE.md` == `2dace6b...`, AND `git -C /Users/marklehn/Developer/GitHub status --porcelain PANEL_SEAT_TEMPLATE.md DRAFTING_CYCLE.md` is empty. If any fails, HALT — a file moved under you.
>
> **Amendment A — `PANEL_SEAT_TEMPLATE.md` §6 (P-6, the FIX field).** Assert `grep -Fc 'smallest honest FIX' PANEL_SEAT_TEMPLATE.md` == 1. Replace the exact literal `smallest honest FIX` with:
>   `**PROPOSED FIX** (an UNEXECUTED HYPOTHESIS — the Planner verifies it RUNS before folding, UNLESS you state you executed it and show the output; a seat's fix otherwise carries the seat's authority into the fold unverified, and one folded unrun shipped an unexecutable inter-step binding — Finding 9/P-6)`
>   ⚠️ This is a mid-sentence replacement inside the "Per finding:" list (`… EVIDENCE (command + raw output), smallest honest FIX. Final message: …`) — the replacement must keep the sentence grammatical (comma before, period after stay intact) and introduce NO gate-matching token. After: `grep -Fc 'smallest honest FIX'` == 0 and `grep -Fc 'PROPOSED FIX'` == 1.
>
> **Amendment B — `DRAFTING_CYCLE.md` §2.6 clone-diff brief (P-7, the cross-join step).** Assert `grep -Fc 'A "proven clone" framing raises this seat'\''s value; it never waives it.' DRAFTING_CYCLE.md` == 1. Replace that exact sentence with itself PLUS the cross-join step — i.e. replace:
>   `A "proven clone" framing raises this seat's value; it never waives it.`
>   with:
>   `A "proven clone" framing raises this seat's value; it never waives it. **Close the seat by CROSS-JOINING its two lists: for every defect you filed, scan the per-hunk dropped list for the machinery that addresses it; for every dropped hunk, ask whether any filed defect is its consequence** — the per-hunk tally and the findings list are separate deliverables that otherwise never meet, and a filed defect's correct fix is often already sitting in the dropped-hunk list (measured: a HIGH's fix — a dropped dev-log inter-step channel — was catalogued twice in one seat's report and never joined, so the seat named a binding without the mechanism that carries it and the Planner folded an instruction that could not run).`
>   ⚠️ The anchor sentence sits mid-bullet, between `…re-added-after-a-sibling-deleted-it (the inverse question above).` and ` (Measured: recovered newest-parent hardenings…` — the replacement inserts BEFORE that trailing `(Measured:…)` clause; keep both intact. Introduce no gate token.
>
> **§6 bookkeeping — BOTH files (MANDATORY).**
> - **PST:** bump Version line (L5) `1.1 (2026-08-13)` → `1.2 (2026-08-21)`; PREPEND a `## History` row above the `1.1` row: `**1.2 (2026-08-21):** the seat's FIX field renamed to PROPOSED FIX — an unexecuted hypothesis the Planner verifies (P-6, honing notes Finding 9; non-author Gate-1 read; paired with DRAFTING_CYCLE.md v2.15 in one plan — honing unit b). Declared §6 deviation (PST versions in lockstep with its §2.6 owner; proposed=0, no batch, no DB flip). Inheritors: every future cold-panel seat's report.`
> - **DC:** bump Version line (L5) `2.14 (2026-08-21)` → `2.15 (2026-08-21)`; PREPEND a `## History` row above the `2.14` row: `**2.15 (2026-08-21):** slug honing-unit-b-2026-08-21; the §2.6 clone-diff brief gains a closing CROSS-JOIN step (P-7, honing notes Finding 9) — filed defects × dropped-hunk list, both directions (a DIFFERENT cross-product from the capstone's probe×fold cross-check at §2.6 L110: P-7 joins the clone-diff seat's FINDINGS list to its own DROPPED-HUNK tally, not probes to folds). CEO-authorized direct amendment, a declared §6 deviation (v2.14 precedent; routed at Gate 1 by a NON-AUTHOR — this arc's cold review; proposed=0, no batch, no DB flip; not a lessons-forge codification). ⚠️ Routing correction: P-7's home is §2.6 here, not PANEL_SEAT_TEMPLATE.md as the Gate-1 batching said; and P-8 was fully codified in v2.14 (Amendment E), no residual. §6 coordinate-doctrine-and-gate: no gate edit — §2.6 prose only, §4 in lockstep; Finding-6 plan_lint fix deferred to unit (c). Inheritors: every future clone-diff seat.`
>
> **Verification (§2.7 — prove each replacement landed and nothing else moved):**
> - `git -C /Users/marklehn/Developer/GitHub diff HEAD -- PANEL_SEAT_TEMPLATE.md DRAFTING_CYCLE.md` (working tree vs HEAD, NOT bare `git diff`) shows ONLY: PST — the FIX-field line changed + Version line + 1 History row; DC — the clone-diff bullet changed + Version line + 1 History row. NO other line removed. ⚠️ Assert by CONTENT, not hunk-block count.
> - Post-edit token asserts (⚠️ **site-anchored so the mandated History row cannot inflate the check** — the PST History row ALSO contains the literal `PROPOSED FIX`, so a whole-file `grep -Fc 'PROPOSED FIX'` == **2** post-edit, NOT 1; asserting `== 1` would false-FAIL a correct edit, [[earn-the-clean-gate-dont-author-it]]): `grep -Fc 'EVIDENCE (command + raw output), **PROPOSED FIX**' PANEL_SEAT_TEMPLATE.md` == 1 (the field landed AT ITS SITE); `grep -Fc 'smallest honest FIX' PANEL_SEAT_TEMPLATE.md` == 0; `grep -Fc 'CROSS-JOINING its two lists' DRAFTING_CYCLE.md` == 1 (safe — the DC History row says "CROSS-JOIN step", a distinct literal).
> - **Non-regression (unit-a + prior doctrine UNTOUCHED):** in DRAFTING_CYCLE.md the six unit-a bullets still each == 1 (`grep -Fc 'The SWEEP pass'` == 1, `grep -Fc 'The SUBTRACTIVE walk'` == 1, `grep -Fc 'has its COMMANDS run by no seat'` == 1), the already-codified trio still present (`Declare a set ONCE` == 1, the plain literal bellows/scripts/fold_check.py present, `A falling total finding-count is NOT the convergence signal` == 1), and the P-7 anchor's neighboring clone-diff clauses intact. In PST the other §6 fields (`id`, `severity`, `class`, `origin`, `attribution`, SITE, EVIDENCE) still present.
> - **Doctrine-integrity:** both files markdown-well-formed — no fence opened, headings intact; PST Version reads `1.2`, its `## History` top row is the new 1.2 above 1.1; DC Version reads `2.15`, its `## History` top row is the new 2.15 above 2.14.
> - ⚠️ **No plan_lint/cycle_check run belongs here: neither reads these files at runtime** (only WARN-citation strings name them — the v2.2 History measurement); a lint run is a null control. §4 lockstep untouched (no §4/§1/§3 edit).
>
> **Commit (governance root, ONE commit spanning both files):** `cd /Users/marklehn/Developer/GitHub && git rev-parse --show-toplevel` (assert == the GitHub root), `git add /Users/marklehn/Developer/GitHub/PANEL_SEAT_TEMPLATE.md /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, then commit `docs(drafting-cycle): seat FIX field = unexecuted hypothesis (PST v1.2) + clone-diff cross-join (DC v2.15) — honing unit b [<id>]`.
>
> **Deposits:**
> - `/Users/marklehn/Developer/GitHub/PANEL_SEAT_TEMPLATE.md` (in-place, governance root — absolute operand)
> - `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (in-place, governance root — absolute operand)
