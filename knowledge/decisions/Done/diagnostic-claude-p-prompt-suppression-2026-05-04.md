# Bellows — `claude -p` Prompt Suppression Inventory
**Date:** 2026-05-04 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1 (SA)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and reports findings. The Planner reads the deposited findings file directly per Rule 22 and decides next steps.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-claude-p-prompt-suppression-2026-05-04.md. Execute Step 1.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-claude-p-prompt-suppression-2026-05-04.md", "bellows/knowledge/decisions/in-progress-diagnostic-claude-p-prompt-suppression-2026-05-04.md")`. Skip specialist file and glossary reads — this is a code-tracing and runtime-behavior audit task. **The investigation question:** when the CEO runs `claude` interactively in a terminal, Claude Code stops at multiple points to ask the user for input — tool permission prompts, file overwrite confirmations, web fetch confirmations, authentication prompts, mid-task user-input requests, etc. Bellows dispatches plans via `claude -p` (non-interactive), and we need a complete inventory of which of those interaction points are auto-approved, auto-denied, suppressed, or silently bypassed under Bellows dispatch — so we can decide whether we are missing CEO-judgment moments that matter. The concern is not just tool prompts; it's every prompt-shaped interaction that would happen in a manual session. **Investigation scope (Bellows-side only — do NOT verify against Claude Code CLI docs, scope is purely "what does Bellows's code do and what does our log evidence show"):** **(1) Read `bellows/runner.py` end-to-end.** Document the exact `claude -p` invocation: full argv passed to subprocess, environment variables, stdin handling, stdout/stderr capture, working directory, timeout configuration. Identify every CLI flag passed (e.g., `--allowedTools`, `--disallowedTools`, `--dangerously-skip-permissions`, `--output-format`, `--max-turns`, `--mcp-config`, `--permission-mode`, anything else). For each flag: what does Bellows pass, where does the value come from (config.json, hardcoded, computed), and what is the documented or observed effect on prompt suppression. **(2) Read `bellows/config.json`.** Document any permission-related configuration keys and their values. Cross-reference each key with where it's consumed in `runner.py` or elsewhere. **(3) Read `bellows/parser.py` and any output-stream-handling code.** Identify every signal in the agent's stdout that Bellows interprets as a prompt or permission event. The `no_permission_denials` gate exists (per BACKLOG history with READ_CLASS_TOOLS exemption shipped 2026-04-28) — trace exactly what string patterns or JSON structures it matches against. List every prompt-shape Bellows currently DETECTS in agent output. **(4) Sample recent log evidence.** Read 3–5 of the most recent files in `bellows/logs/` (the actual claude-p output captures, not Bellows's own logs). For each, scan for any line that looks like a prompt, permission event, denial, confirmation, or user-input request. Categorize what you find: was it a tool denial, a content-overwrite warning, an auth prompt, a compaction notice, something else. Quote 2–4 representative lines per category with file path and line number. **(5) Build the prompt category inventory.** Produce a table with columns: (a) Prompt category (Tool permission — Bash, Tool permission — Edit, Tool permission — Write, Tool permission — MultiEdit, Tool permission — MCP tool, Tool permission — WebFetch, Tool permission — WebSearch, File-overwrite confirmation, Authentication / OAuth prompt, Long-running command confirmation, Mid-task user-input request via ask-user tool, Compaction / context-limit prompt, Plan mode confirmation, Anything else surfaced from log evidence in step 4); (b) Manual-mode behavior (what would a manual `claude` session do — based on what Bellows would have to suppress or override, not on external docs); (c) Bellows-dispatched behavior (auto-approved via flag X, auto-denied, silently bypassed, surfaced in stdout as Y, blocked at gate Z) — cite the line in `runner.py` or `config.json` that produces this behavior; (d) CEO judgment risk (what could go wrong if this category is being auto-approved when it shouldn't be — be specific, e.g., "an Edit to a file the agent didn't read first could overwrite content we'd have caught manually"). **(6) Identify gaps and unknowns.** List every prompt category where the answer is "we cannot tell from Bellows-side code alone" — these are candidates for a follow-up Claude Code CLI investigation. Be explicit: do not guess. If the code shows `--dangerously-skip-permissions` is passed, say so with line number. If the code shows no permission flag is passed, say so and note that the actual behavior depends on Claude Code's defaults which are out of scope for this diagnostic. **(7) Critical-finding callout.** If any category in the inventory looks like a real CEO-judgment risk (e.g., "Edit operations are fully auto-approved with no gate coverage"), call it out at the top of the findings file as a Critical Finding section before the inventory table. Use the same severity framing as Security & Testing: 🔴 Critical, 🟡 Minor, 🔵 Advisory. **Deposit:** write findings to `bellows/knowledge/research/claude-p-prompt-suppression-inventory-2026-05-04.md` using `Filesystem:write_file` (or canonical Python `with open(...) as f: f.write(content)` pattern if MCP write isn't reachable). The file structure: (a) one-paragraph executive summary, (b) Critical Findings section if any (severity-coded), (c) `runner.py` invocation reference (full argv + environment summary with line numbers), (d) `config.json` permission-related keys (table), (e) `parser.py` prompt-detection patterns (table), (f) Log evidence samples (categorized, quoted with file:line), (g) Prompt category inventory table (the main deliverable), (h) Gaps and unknowns (list), (i) Output Receipt. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/claude-p-prompt-suppression-inventory-2026-05-04.md`
>
> **STOP. Report findings. Do NOT propose or implement fixes — this is investigation only. The Planner will read the findings file directly and decide what (if anything) needs to ship.**
