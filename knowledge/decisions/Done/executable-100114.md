# bellows — executable: DOCTRINE — Gate 2, thread 336, PT tranche two (pins, measurement and instruments): proposals 449, 469, 471, 482, 484, 488, 492, 501, 502, 506, 512, 553 onto PLANNER_TEMPLATE.md v4.113 by a committed builder (eight extensions on Rules 55, 58, 61, 78, 82, 97, 103 and 104; Rule 111 given its heading), then the twelve flip `implemented` — 62 accepted rows remain for the five tranches after it

**Date:** 2026-09-15 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full (the bellows suite is UNCHANGED by this plan and is run once by QA so the test gate reads a real summary line; the gate reads FAILURES against `known_failures`, never the pass count — P7 is the expectation, and bellows `main` moves as plans merge) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 2

**auto_close:** false

**Slug:** `gate2-pt-w30-b-2026-09-15`

**Depends on:** the CEO's rulings of 2026-09-14 (thread 336 — one Gate 2 over the 86 accepted proposals, W=29 and W=30 together) and the delegation of 2026-09-15 ("Start on gate 2 and you have my delegation to release at the proper time"; "No I am good with moving forward on these" — the seven-tranche partition stands); the tranche read `gate2-packet-2026-09-15.md` (governance `6cdd1aef` — its §1.3 ruling: 506 stands accepted, 584, 467 and 508 stay rejected; its partition row "PT two — pins, measurement and instruments"); the non-author Gate 1 packets — W=29's `gate1-packet-2026-09-02.md` (449, PT Rule 97) and W=30's `gate1-packet-2026-09-13-a.md` (reader A: 469, 471, 482, 484, 488, 492, 501, 502, 506), `-b.md` (reader B: 512) and `-c.md` (reader C: 553); **tranche one** — `Done/executable-100109.md` in bellows (`gate2-pt-w30-a-2026-09-15`, closed 2026-09-15 16:02, the clone origin and the newest same-class plan: doctrine prose onto `PLANNER_TEMPLATE.md` by a committed builder, then a scoped CAS flip; its raw-file P7 restatement was recorded at its verdict as thread 363's shape, a reading the live gate does not bear out — with a named suite file present the gate reads that file, not the raw one; MUST-PRESERVE's raw-file rule states the measured mechanism) and `Done/executable-540.md` in governance (the multi-site EXTENSION form); the builder `governance/knowledge/decisions/drafts/build-gate2-pt-w30-b-2026-09-15.py` (committed `30781aaa` after the walk-0 scout's folds, digest `284265a870342ace`; its first commit `e44ee599`). Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-b-2026-09-15.md`.

**Tier computed, not judged (§1):** **T-6 fires** — `PLANNER_TEMPLATE.md` is the template → **T2**. T-2 fires (twelve canonical rows flip; caps at T1). T-1 fires (two repos, plus the DB). T-8 does NOT fire — a structure-for-structure clone of tranche one (#100109) carrying 540's extension form; twelve edits against fourteen, one span against three, is scale, not shape. **Panel form — the magnitude proxy, measured at walk 0 (all three must hold): (i) structure-clone** — the parent is #100109 (closed 2026-09-15, the newest PT tranche), and both walk-0 clone-diffs ran (against its final text and its register); **(ii) mechanized edits** — every template mutation flows through the committed builder, whose eleven anchors and one span are count-1-asserted BY EXECUTION and whose forty post-conditions are asserted on the output before any write, with the line and byte deltas asserted against pinned constants (proven at walk 0: two refusals of the builder's own making corrected before its commit, a byte-identical rebuild, a `27	4` numstat, the byte pin firing on an off-by-one copy); the flip is one CAS statement under a structural pre-check; **(iii) scripted probes** — every new token measured 0 against the pre-edit file, the invariant tokens at their MEASURED pre-edit counts. **→ SMALL form: the walk-0 scout, then EXECUTION, then CAPSTONE — the seats on the Air when a session is live there (the CEO's direction of 2026-09-15, thread 369), else local subagents.** The CEO may call the full form.

## Why this exists

Gate 1 routed 86 proposals `accepted|codify`; tranche one landed twelve of them today (v4.112, #100109). This tranche is the packet's second row: the twelve rows about PINS, MEASUREMENT and INSTRUMENTS — what a pin names and protects, what population a rule may address, what a scoping argument bounds, how a check is priced before it is wired, what a positive control proves, what a three-instance pattern is, what a diagnostic's authority reaches, what a new checker's first test is, and what a "copies" claim needs. Their homes are the packets', re-verified against live v4.112 at authoring (every probe `/usr/bin/grep -c -i -F`; the zero rows the claim): **471 + 502** (entries 463, 494) — a pin names its target; a pin protects a claim, not a digest → Rule 61, one extension paragraph (`pathspec` → 3, none this claim; `testable sentence` 0); **469** (461) — a quantifier clause's population is pinned before the freeze → Rule 78 (`populations it does NOT` 0; DC §2.7's mechanical-enumeration clause binds a measurement, not a MUST-PRESERVE); **482** (474) — a scoping argument that excludes one population is not a bound → Rule 82 (`populations it does NOT` 0); **501 + 512 + 553** (493, 504, 545) — marginal count, calibration before wiring, the trivial baseline → Rule 104 with Rule 92's positive set, one paragraph (`marginal` → 3, none the checker's cost; `calibrat` → 3, none a predicate's; `trivial baseline` 0); **488** (480) — a positive control proves the instrument, not the call → Rule 55 (`negative control` 0); **492** (484) — a three-instance pattern is a hypothesis about the fourth → Rule 58 (`membership` → 1, not this sense); **484** (476) — a diagnostic's authority extends only to what its instrument executed → Rule 111, which since v4.102 has sat HEADLESS inside Rule 77's section between Rule 77's body and Rule 77's Source line (W=30's hazard 4(b)): the tranche gives it `### 111.` in place, moves Rule 77's Source line above it, and appends the extension with its own Source line (`grep the diagnostic` 0; `unit drift` 0); **506** (498; the packet's §1.3 ruling) — write the vacuity test first → Rule 103 (`vacuity` 0; the GLOSSARY's *vacuous verdict* holds the definition); **449** (441) — the copies-claim triad → Rule 97 (`copies claim` 0; `stat -f` → 1, MACHINE_SETUP's instance). **Homes:** EIGHT extension paragraphs appended after the home's existing last line (540's form), each opening in bold with its proposals, entries and `codified 2026-09-15 — Gate 2, thread 336, PT tranche two`; ONE span (Rule 111's heading, its body verbatim, its extension, its Source line carrying `codified 2026-09-15 (Gate 2, thread 336 — PT tranche two)`); the version, the `Last Updated` line and the History row. Numbering append-only: Rule 111 keeps its POSITION between 77 and 78 and gains only its heading.

## What this plan does

**In the governance checkout, by absolute path (`GOV=/Users/marklehn/Developer/eluvian-governance`), twelve anchored edits to `PLANNER_TEMPLATE.md` by the committed builder (never by hand):**
- **E1** — Rule 61: after its prop-417 amendment paragraph (the rule's last line; anchor: the blank line and Rule 62's heading, re-emitted), the 471 + 502 paragraph. **E2** — after Rule 78's Source line, the 469 paragraph. **E3** — after Rule 82's Source line, the 482 paragraph. **E4** — after Rule 104's Source line, the 501 + 512 + 553 paragraph. **E5** — Rule 55: after its prop-373 line (anchor: the blank line and Rule 56's heading, re-emitted), the 488 paragraph. **E6** — after Rule 58's Source line, the 492 paragraph. **E8** — after Rule 103's Source line, the 506 paragraph. **E9** — Rule 97: after its body (anchor: the blank line and Rule 98's heading, re-emitted), the 449 paragraph.
- **E7** — the span from Rule 111's headless opener (`**Rule 111 — diagnostic output is the only admissible evidence … (CEO ruling 2026-09-04, thread 120).**`, count 1 — the History table's v4.102 row quotes a shorter form) through Rule 77's Source line (`*Source: proposal 267, codified 2026-08-11 (Gate 2 batch 2)*`, count 1), replaced by: Rule 77's Source line; `### 111. Diagnostic output is the only admissible evidence for a question about how the system BEHAVES` (the CEO's own sentence as the title — the 484 clause opens the extension in bold, not a CEO-authored rule's heading); the paragraph verbatim but for its opener, which loses the words `Rule 111 — ` and capitalises the sentence's first word (`**Diagnostic output …`); the 484 extension; a Source line naming the v4.102 amendment and the proposal.
- **E10** — `**Version:** 4.112` → `**Version:** 4.113`. **E11** — `**Last Updated:** 2026-09-15 (v4.112)` → `**Last Updated:** 2026-09-15 (v4.113)`. **E12** — the History row inserted directly under the `| Date | Lesson |` header (tranche one's v4.112 row survives below it).

The builder reads the file as bytes, refuses CRLF, refuses an already-built input, asserts each single anchor's count is 1 and the span's two bounds count 1 and in order, applies the twelve edits, asserts forty post-conditions on the OUTPUT (the builder's `POST` list: the Rule 111 heading once; its old opener once — the History table's v4.102 row; Rule 77's Source line once; the nine codified strings at 9 and 1; the nine openers once each; the v4.113 tokens 1 and the v4.112 tokens 0; the new History row 1 and tranche one's 1; the section headings and the extension anchors surviving at their MEASURED pre-edit counts — `### 55. `, `### 56. `, `### 58. `, `### 61. `, `### 62. `, `### 77. `, `### 78. `, `### 82. `, `### 92. `, `### 97. `, `### 98. `, `### 103. `, `### 104. `, `### 112. ` at 1), asserts the line and byte deltas against PINNED constants (`EXPECTED_LINES = 23`, `EXPECTED_BYTES = 19305` — a post-condition that can fail, proven at walk 0 on an off-by-one copy, twice: before and after the scout's folds) and only then writes — to a SCRATCH path it refuses to let be the live file or anything under the governance root or the input's git toplevel. Apply = copy the proven scratch output over the live file, then re-measure the live file.

**In the lessons DB (`/Users/marklehn/Developer/forge_lessons/lessons-forge.db`, the mini's one live copy), AFTER the doctrine commit:** one scoped CAS flip — `id IN (449,469,471,482,484,488,492,501,502,506,512,553) AND status='accepted' AND route='codify'` → `implemented`, `status_updated_by='ceo'` (the delegation's record), a fresh UTC stamp — under a structural pre-check (a TEMP-TABLE `CHECK(x=12)` that aborts the transaction before the UPDATE if the twelve are not all `accepted|codify`), with the capture of every row with `id <= 590` — the whole table at authoring — `(id, status, route, category, status_updated_by, status_updated_at)` to a SCRATCH file BEFORE the UPDATE (copied into the worktree's evidence dir afterwards — never written into the canonical checkout), and the sentinels by name: `PRE_F=12`, `CHANGES_F=12`, `EXCL_F=12` (the twelve rows now `implemented` with a stamp that is NOT one of the TWO vintage stamps — 449 carries W=29's `2026-09-02T23:54:37Z`, the eleven W=30's `2026-09-13T18:27:07Z`), `ACC_POST=62`, `IMPL_POST=357`. The flip SQL is a committed file in governance; a backup of the DB precedes it.

**In the bellows worktree:** the dev log and the QA evidence.

## What this plan does NOT do

- Does not touch the sixty-two `accepted|codify` rows of tranches three to six and the DC/PST tranche (their homes are named in the packet — each is its own plan), DRAFTING_CYCLE.md, PANEL_SEAT_TEMPLATE.md, COMPANY.md, MACHINE_SETUP.md, GLOSSARY.md, code, or threads. Does not push (the Planner pushes after the pause; the Planner's Rule 22 (b) read at the pause takes the canonical block's stdout and the daemon's gate row as the receipt's evidence, never the QA's `PRECHECK: 0 failure(s)` line — at a QA step `tools/check_deposit.py` verifies presence alone, thread 362). Does not project the `LESSONS.md` markers (the Planner's act after the close — `scripts/project_status_markers.py --apply`, the Rule 112 instrument). Does not close thread 336 (the Planner's `threads done` with the seventh tranche).
- ⚠️ **Runs after tranche one (#100109, 2026-09-15 16:02) and BEFORE any W=31 ingest or another tranche** — A0's ladder asserts the accepted population (74 before this plan's flip, 62 after) with every accepted row at one of the two vintage stamps; another tranche landing first moves it, and a W=31 ingest could stale an accepted row (`accepted` is not terminal — `src/lessons_forge.py:31`). The 38 `[status: pending]` headings written since W=30's ingest wait.
- ⚠️ **PLANNER_TEMPLATE's version is moved by the CEO's doctrine acts at other plans' closes** (thread 322's plan names the next version; tranche one moved it twice under its own draft): the re-pin surface is the PIN TABLE and its restatements — P1 (sha, lines, bytes); P2 (every line number, which a doctrine act above the anchors shifts, and its `**Version:**` anchor); P4 (the builder's digest, after its two constants `OLD_VERSION`/`NEW_VERSION` are re-committed); P5 (`wc -l`, `wc -c`; the BUILT line's deltas hold unless an act lands inside the span, which the byte pin refuses); Task C's four version tokens; the `wc -l` restated in A2, A3, both RE-ENTRY arms and QA Item 1 and the `wc -c` in A2; every literal of the new version (the title, the E10/E11 line, the POST description, MUST-PRESERVE, P3, the RE-ENTRY text, Task C, the THREE commit subjects) and the pre-edit-state literals (E10's, the POST description's, P1's, P2's anchor, P3's, Task C's zero probe); the register's Targets line and title — re-pinned TOGETHER at the freeze and before the release by ONE scripted pass over those sites (tranche one's CAPSTONE re-pin is the form), recorded in the register with each literal's count before and after, and Task C's probes re-run on a scratch build of the new version, and the Why-line dedup probes re-run against the new pre-edit version with their `live v4.NNN` label and counts re-pinned with them (the CAPSTONE seat's S3-1); the historical mentions keep their versions; a moved version at A0 is a NONE-MATCH halt and a redeposit, never a guess.

## MUST-PRESERVE

- ⚠️ **TWO REPOSITORIES AND A DB, ONE STEP.** Governance by absolute path, `git -C "$GOV"` never `cd`; porcelain EMPTY and sha = P1 before touching `PLANNER_TEMPLATE.md`; commit by explicit pathspec with the message on stdin; no push. The DB by absolute path; `sqlite3` with `-bail`; a backup (`pre-gate2-pt-w30-b-<HHMMSS>.db` at the forge root, untracked) BEFORE the flip.
- **The builder is the only editor of the template.** Its committed blob digest must equal the on-disk file (P4) before it runs; it runs scratch→scratch; the live file is replaced only by `cp` of a scratch output whose post-conditions the builder already asserted, and the live file is then re-measured (Task C).
- **Doctrine before DB (540's C6):** the flip runs only after the governance commit has landed; a HALT between leaves v4.113 committed-unpushed and the flip owed — the RE-ENTRY arm in A0 resumes at the flip.
- ⛔ **Never run any `checkout --` on an A0 halt except the ONE recipe named there** (a pre-commit apply that died, discriminated by the HEAD numstat, the porcelain and the `### 111. ` token); a pin mismatch otherwise means the file carries changes that are not this plan's.
- ⚠️ **ONE landed text carries a Rule 20 hedging keyword** — Rule 78's extension quotes the entry's "could not run" case, measured by scanning the builder's output diff against the canonical block's list (walk 0) — so NO receipt row quotes a landed paragraph's body; Task C pins heading fragments and opener fragments only (100026's X-2 / CP-1 class).
- **`known_failures: 0`.** The suite runs in the worktree under the canonical venv; the QA reads its own run's failure count, and a bellows `main` that gained tests since P7 is stated in `probes-raw.txt` under Item 4 (the probe's command and the hashes it printed; the summary line stays in the suite file), never in the receipt's row and never halted on. Any failure is a HALT/Critical.
- ⛔ **`probes-raw.txt` carries no summary-shaped line** — no `N passed` restatement of P7, no pasted suite line, no commit log beyond the path-scoped drift probe's: the suite gate (`gates._select_summary_txt`, bellows `91354d2a`) chooses, among the `.txt` deposits that carry a pytest summary, a basename carrying `suite` or `full`, then the largest `passed` count, then the first match — so a summary-shaped line in the raw file is the one read whenever the suite file crashed without a summary or lost its name (thread 363's defect; its "first `.txt`" wording is narrower than the live gate, measured 2026-09-15 on `71c6bfc3`), and it is forbidden here by name so the selection never rests on that tie-break (tranche one's QA wrote "P7 baseline: 2395 passed, 2 skipped" as a header there — harmless beside a named suite file, forbidden here). P7 is cited as "P7" in the raw file, never restated with its numbers.
- ⛔ **Every commit in the bellows worktree chains `tools/check_deposit.py` before `git add`** (Rule 23; the step IS bound — the commit binding was installed by bellows `4bcde8a9` (#100105, thread 333 — `hooks/git/pre-commit` and the daemon's `BELLOWS_PRECHECK_*` env), and tranche one's two chains ran under it), and puts its message on stdin (`git commit -F -` with a quoted heredoc), never in a file.

## Numbers discipline — the pins DEV re-derives (measured 2026-09-15 by the Planner on PLANNER_TEMPLATE v4.112 at governance `e44ee599`, bellows main `f226d2f9`, forge_lessons `a1ebf45`; P4 and P5 re-measured at governance `30781aaa` after the walk-0 scout's folds)

| pin | what | value | how |
|---|---|---|---|
| P1 | **`PT_SHA`**, lines, bytes — pre-edit (v4.112, tranche one's act of 2026-09-15 16:00; re-pinned at the freeze if another act lands first) | `ad895fa075675681`; 2558 lines; 523,098 bytes | `shasum -a 256 "$GOV/PLANNER_TEMPLATE.md" \| cut -c1-16`; `wc -l`; `wc -c` |
| P2 | **`ANCHORS`** — the builder's eleven single anchors and one span | each count 1: Rule 62's heading with its preceding blank line (E1, the heading at 1175) · `*Source: proposal 268, codified 2026-08-11 (Gate 2 batch 2)*` (E2, 1285) · `*Source: proposal 281, codified 2026-08-11 (Gate 2 batch 2)*` (E3, 1311) · `*Source: proposal 430, entry 422 (2026-08-27), codified 2026-09-02 (Gate 2, cycle W=28 PT tranche one)*` (E4, 1453) · Rule 56's heading with its preceding blank line (E5, 1121) · `Source: proposal 186, lesson 2026-07-22` (E6, 1141) · `*Source: proposal 418, entry 410 (2026-08-27), codified 2026-09-02 (Gate 2, cycle W=28 PT tranche one)*` (E8, 1445) · Rule 98's heading with its preceding blank line (E9, 1409) · `**Version:** 4.112` (5) · `**Last Updated:** 2026-09-15 (v4.112)` (6) · the History header pair (2342–2343; the v4.112 row sits directly under it); the span: Rule 111's opener (1277, its full text through `thread 120).**` — count 1; the History row's v4.102 copy differs after its first sentence) → Rule 77's Source line (1279) — each bound count 1 | `/usr/bin/grep -cF` (the multi-line anchors and the span by the builder's own assertion) |
| P3 | **`TOKENS`** 0 before, their counts after | `### 111. ` · `**A pin's producing command names its TARGET, and a pin protects a CLAIM, not a digest` · `**A quantifier clause's POPULATION is pinned before the freeze` · `**A scoping argument that correctly excludes one population is not a bound on the risk` · `**A check is priced by its MARGINAL count, calibrated on the corpus before it is wired` · `**A positive control proves the instrument works, not that you CALLED it correctly` · `**A pattern that explains three defects is a HYPOTHESIS about the fourth` · `**A diagnostic's authority extends only to what its instrument executed` · `**The defect class just fixed is the one reproduced next` · `**A COPIES claim needs three facts` · `codified 2026-09-15 — Gate 2, thread 336, PT tranche two` · `codified 2026-09-15 (Gate 2, thread 336 — PT tranche two)` · `**Version:** 4.113` · `\| 2026-09-15 \| v4.113: Gate 2, thread 336` — each 0 before, 1 after but the two codified strings (9 and 1); moved: `**Rule 111 — diagnostic output` 2 before (the headless paragraph and the History table's v4.102 row), 1 after (the row); retired after: `**Version:** 4.112`, `**Last Updated:** 2026-09-15 (v4.112)` — each 1 before, 0 after; invariants: `### 55. ` 1, `### 56. ` 1, `### 58. ` 1, `### 61. ` 1, `### 62. ` 1, `### 77. ` 1, `### 78. ` 1, `### 82. ` 1, `### 92. ` 1, `### 97. ` 1, `### 98. ` 1, `### 103. ` 1, `### 104. ` 1, `### 112. ` 1, `### 14. ` 3, `### 17. ` 2, `*Source: proposal 267, codified 2026-08-11 (Gate 2 batch 2)*` 1, `\| 2026-09-15 \| v4.112: Gate 2, thread 336` 1 (unchanged by this plan) | `/usr/bin/grep -cF` |
| P4 | **`BUILDER`** — on-disk digest = the blob at its OWN last commit | `284265a870342ace` (governance `30781aaa` — the builder's own last commit; its first, `e44ee599`, is history) | `shasum -a 256 <builder> \| cut -c1-16`; `BC=$(git -C "$GOV" log -1 --format=%H -- governance/knowledge/decisions/drafts/build-gate2-pt-w30-b-2026-09-15.py); git -C "$GOV" show "$BC:governance/knowledge/decisions/drafts/build-gate2-pt-w30-b-2026-09-15.py" \| shasum -a 256 \| cut -c1-16` |
| P5 | **`DRY_RUN`** — the success line, numstat, sizes | `BUILT: <out> edits=12 blocks=1 lines+23 bytes+19305 post=40/40 deltas=pinned`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" <out>` → `27	4` (exit 1 is the expected differing state — read the numbers, never the exit); `wc -l <out>` → 2581; `wc -c <out>` → 542,403 | as stated, scratch-only |
| P6 | **`FLIP_PRE`** — the twelve rows | 449 `accepted\|codify\|planner\|2026-09-02T23:54:37Z`; 469, 471, 482, 484, 488, 492, 501, 502, 506, 512, 553 each `accepted\|codify\|planner\|2026-09-13T18:27:07Z`; `SELECT COUNT(*) … WHERE status='accepted'` → 74; `implemented` → 345; `SELECT status_updated_at, COUNT(*) … WHERE status='accepted' GROUP BY 1` → exactly two rows, `2026-09-02T23:54:37Z\|19` and `2026-09-13T18:27:07Z\|55`; `SELECT MAX(id), COUNT(*) FROM lesson_proposals` → `590\|590` | `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db` |
| P7 | **`SUITE`** — the expectation | `2483 passed, 2 skipped`, exit 0 — bellows main `ff4ac39c` (#100112's QA, 2026-09-15 18:14, in a worktree; the basis moved twice under this draft — `f226d2f9` 2464, `71c6bfc3` 2467 — and moves again at thread 322's close; re-pinned at the freeze); the gate reads failures, and `main` moves | `BPY -m pytest tests -q -p no:cacheprovider` |
| P8 | **`NO_INGEST_NO_TRANCHE`** (precondition, in two halves) | UNCONDITIONAL (before the ladder): `SELECT COUNT(*) FROM lesson_entries` → 580 (no W=31 ingest) AND `ls /Users/marklehn/Developer/forge_lessons/knowledge/decisions/ \| /usr/bin/grep -cF -- 'forge-cycle-w31'` → 0 (a zero-match count prints 0 and exits 1 — read the number). ARM-DEPENDENT (ladder step (5)): the accepted population with its at-the-vintages count — `74\|74` on FRESH and flip-owed; `62\|62` on complete — the twelve at their vintages on FRESH and flip-owed (ladder (4)) | as stated |

## Drafting Cycle

**Tier:** T2 — T-6 (the template), T-2 (twelve canonical rows, capped), T-1 fire; T-8 does not (a structure clone). **Panel: SMALL form** (the magnitude proxy measured at walk 0, all three conditions holding) — the walk-0 scout, then EXECUTION, then CAPSTONE, on the Air when a session is live there; the CEO may call the full form.

**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-b-2026-09-15.md

**Walk 0 (context pin, measured):** the template's sha, lines and bytes (v4.112); the eleven single anchors and the span's two bounds counted (the four Source-line anchors at 1285, 1311, 1453, 1445 and the bare one at 1141 of v4.112; the three next-heading anchors at 1175, 1121, 1409; the span 1277–1279; the header lines 5–6; the History header 2342–2343); every new token counted 0 and the invariant tokens at their measured pre-edit counts; the twelve proposals' rows and the two vintage stamps; the builder run scratch→scratch with two refusals of its own making corrected, the deltas pinned, the rebuild byte-identical, the three refusals and the off-by-one copy; the flip rehearsed on a scratch backup copy with the five sentinels, the 590-line capture and the CHECK abort on a re-run; the suite's P7 taken from #100110's evidence; the consumers run (the depositor's class, the gates' extractor, the five scripts); the walk-0 SCOUT's 14 findings folded (v0 `f2b37dc7` → v1 `56196de9`; the builder `e44ee599` → `30781aaa`) — the register's M1–M9 and its fold table.

- Weak spots:          w1 4 folded — instruction 0 / record 4; w2 2 folded — instruction 0 / record 2; w3 dry
- Destruction:         w1 dry; w2 dry; w3 dry
- Vulnerabilities:     w1 dry; w2 1 folded — instruction 1 / record 0; w3 dry
- Integration-record:  w1 1 folded — instruction 0 / record 1; w2 dry; w3 dry
- ACID:                w1 dry; w2 dry; w3 dry
**Cold panel (SMALL form; meter opened at the scout's dispatch 22:08Z, 2026-09-15):** SCOUT (lens 4, local subagent, 235,000 tokens, 30 tool uses) — 14 findings, 1 HIGH / 5 MED / 8 LOW, instruction 5 / record 6 / question 3, 14 folded (v1 `56196de9`; the builder `30781aaa`) · EXECUTION (lens 3 + the execution brief, the Air's cold-reader session by committed brief `0f860e01`, 18 min, packet `6c64b262`) — 3 findings, 0 HIGH / 0 MED / 3 LOW, instruction 0 / record 1 / question 2, 3 folded (`59ec64bf`; no builder change); every pin, ladder state, bound chain and receipt shape it measured agreed with the plan · CAPSTONE (lenses 4 + 5 + the system brief on the fold set, the Air's session by committed brief `76ebb1c9` and a cross-session message, 5 min, packet `0f5defbd`) — 2 findings, 0 HIGH / 0 MED / 2 LOW, instruction 0 / record 1 / question 1, 2 folded; the 25-fold set read clean (0 fold-introduced surviving; the Cycle Log append-only), the thirteen dedup probes re-run and agreeing, the re-pin surface complete but for the dedup probes (folded).
**Closing:** ✅ **BAR MET — walk 3 dry (all five lenses) after walk 1's five record folds, walk 2's three (two record, one instruction — the yield-rising override recorded), and three cold reads folded in full — the walk-0 SCOUT (14 findings, one HIGH: E8's stated-basis sentence inverted against entry 498; v0 → v1 before lens 1), the EXECUTION seat (3, folded after walk 1) and the CAPSTONE seat (2, folded after walk 2): 19 findings from the cold reads and 8 from the walks, 27 folded, none DIRECTION-class, none questioning a ruling; the seats ran on the Air's session by committed briefs and a cross-session message (thread 369; thread 242's ruling — outbound only), their packets fast-forwarded from that checkout by a read when the session's push stalled.** The yield 5 → 3 → 0. The template did NOT move under the draft: thread 322's plan (#100113), whose doctrine act would have taken it to v4.113, was halted at 19:59 on 2026-09-15 by the runner's wall-clock cap at its DEV step (thread 373) and is re-deposited later — so the tranche lands **v4.113** as pinned at authoring, on the live v4.112 (`ad895fa075675681` / 2558 / 523,098 re-measured at the close), the freeze re-pin pass proven in dry-run against a scratch v4.113 and kept on file for the tranche that follows the deferred act. The builder `30781aaa` (digest `284265a870342ace`) builds the pinned deltas (`BUILT: … edits=12 blocks=1 lines+23 bytes+19305 post=40/40 deltas=pinned`, numstat `27	4`, 2581 lines, 542,403 bytes, byte-identical on the rebuild, the three refusals and the off-by-one copy refused); the flip rehearsed on scratch backup copies by the Planner and the scout (the five sentinels, the 590-line capture, the CHECK abort on a re-run; the live DB `74|580|345` at the close); P7 `ff4ac39c` / `2483 passed, 2 skipped` with no commit under `tests/` since (`git log ff4ac39c..HEAD -- tests` empty at bellows `e9e435f3`). Deposited to bellows as `ready-executable-gate2-pt-w30-b.md` — the receipt first, the class parsed with the depositor's tool (`shop-infra`, holds for the release); released under the CEO's delegation at the next opening.

## Cycle Manifest
tier: T2
target: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md
class: shop-infra
reads: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-pt-w30-b-2026-09-15.py, /Users/marklehn/Developer/eluvian-governance/gate2-packet-2026-09-15.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-02.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-a.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-b.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-c.md, /Users/marklehn/Developer/eluvian-governance/LESSONS.md, knowledge/decisions/Done/executable-100109.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-540.md, /Users/marklehn/Developer/forge_lessons/lessons-forge.db
writes: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30b-flip.sql, /Users/marklehn/Developer/forge_lessons/lessons-forge.db, knowledge/development/dev-log-gate2-pt-w30-b-2026-09-15.md, knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt, knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/qa-receipt.md, knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/probes-raw.txt, knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/full-suite-gate2-pt-w30-b.txt
open_forks: 1. the sixty-two accepted rows of tranches three to six and the DC/PST tranche (their own plans, in the packet's order); 2. Rule 111 keeps its POSITION between Rules 77 and 78 (numbering append-only) — a renumbering of the Orchestration Plan Rules is not this tranche's, nor any tranche's, act; 3. reader A's 503 (a signal validated on the cases that produced it has no measured precision) is `rejected` in the DB as COVERED by Rule 92 (reader A's verdict word, not a DB state) and is not re-codified — the pricing paragraph cites it; 4. the `LESSONS.md` markers are projected after the close by the Planner, not by this plan
walks: 3
yields: 0, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:74
coherence: 3/3 body walks named in the register (27 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-gate2-pt-w30-b.md.foldcheck.json

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the bellows Developer. ⚠️ Governance runs IN PLACE at `$GOV` (every operand ABSOLUTE); commits there by explicit pathspec; ONE action per compound. ⛔ Every commit's message goes to `git commit -F -` on stdin — a quoted heredoc ending the commit's own command: `<<'MSG'`, the message on the lines after it, `MSG` alone on the last — so no file is written for it. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** (`BPY`); the builder runs under `python3` as tranche one's did (it imports only `os` and `sys`).
>
> ⛔ **A0 — roots, the precondition, then the branch ladder (catch-all LAST):** `cd "$(git rev-parse --show-toplevel)" && [ -f bellows.py ] && [ -d tests ] && echo TREE_OK` — HALT unless TREE_OK; `GOV=/Users/marklehn/Developer/eluvian-governance; PT="$GOV/PLANNER_TEMPLATE.md"; B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-b-2026-09-15.py"; DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db; [ -f "$PT" ] && [ -f "$B" ] && [ -f "$DB" ] && echo GOV_OK` — HALT unless GOV_OK. Re-derive `GOV`, `PT`, `B`, `DB` in every compound. `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python`. **P8's UNCONDITIONAL half → both values, else HALT** (`lesson_entries` → 580 and the `forge-cycle-w31` count → 0; on a mismatch: `a W=31 cycle has started — re-pin P6/P8 and re-deposit`). **P8's accepted-population half is read INSIDE the ladder, never before it:** it changes when THIS plan's flip lands. Then the ladder — **(1)** P1's sha matches; **(2)** `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30b-flip.sql` (state it; EMPTY on FRESH); **(3)** `git -C "$GOV" log -1 --format=%s -- PLANNER_TEMPLATE.md` — does the subject carry `gate2-pt-w30-b`?; **(4)** `sqlite3 -readonly "$DB" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (449,469,471,482,484,488,492,501,502,506,512,553) ORDER BY id;"`; **(5)** `sqlite3 -readonly "$DB" "SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted')||'|'||(SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z'));"` — the accepted population and how many of it sit at the two vintage stamps (every accepted row does, on EVERY arm).
> - **FRESH** = (1) match AND (2) empty AND (3) no AND (4) the twelve `accepted|codify` at their vintages (449 at `2026-09-02T23:54:37Z`, the eleven at `2026-09-13T18:27:07Z`) AND (5) `74|74` → A1.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` (a dirty template on re-entry is a FOREIGN edit — HALT untouched, quoting the porcelain) AND (4) the twelve still `accepted|codify` at their vintages AND (5) `74|74` → verify the COMMITTED state (the Task C counts on the live file, `wc -l` 2581, and `C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show --stat --format= "$C" -- PLANNER_TEMPLATE.md` → 27 insertions, 4 deletions — the PATH-SCOPED commit, never `HEAD`: once A4's SQL commit or any later governance commit lands, `HEAD` no longer names the template's commit and a `HEAD`-scoped show prints NOTHING with exit 0), then A4 (the flip) and A5 only. If (2) shows `g2ptw30b-flip.sql` untracked or modified with the flip NOT applied, that is this arm too — A4 rewrites the file from the exact text, which is idempotent.
> - **RE-ENTRY (complete)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` AND (4) the twelve `implemented|codify` with every stamp NOT one of the two vintages AND (5) `62|62` → verify the COMMITTED state (the Task C counts, `wc -l`, and the path-scoped show → 27/4); then the two idempotent tails of A4: the SQL file — if `git -C "$GOV" log -1 --format=%s -- governance/knowledge/development/g2ptw30b-flip.sql` does not carry `gate2-pt-w30-b`, write it from the exact text and commit it; the capture — if `/tmp/g2ptw30b/flip-capture.txt` exists copy it into the worktree, else reconstruct the worktree copy from the newest pre-flip backup — `BK=$(ls -t /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-b-*.db | head -1)` (each A4 attempt mints a second-stamped file; the newest is the one whose flip landed), then `[ -f "$BK" ] && echo "BK=$BK"` — no file is a HALT (`no pre-flip backup found — the capture cannot be reconstructed; re-deposit`), never an empty `$BK` handed to sqlite, which would open a scratch database and fail on the table — `sqlite3 -readonly "file:$BK?immutable=1" "<the capture SELECT>"` → the same 590 lines, labelled `RECONSTRUCTED (post-commit re-entry)` in the dev log); then A4's read-back (no flip) and A5 with the dev log marked `re-derived on re-entry`.
> - **NONE-MATCH** = anything else → HALT quoting every measurement — and when (5)'s SECOND number ≠ its first on ANY arm, the HALT reads `an accepted row carries a stamp outside the two vintages — an ingest or a foreign flip has moved the population; re-pin P6/P8 and re-deposit`; when (3) = no and (4) reads the twelve `implemented` with (5) `62|62`, it reads `this plan's flip has landed and a later act moved PLANNER_TEMPLATE.md after its commit — find the template's commit with git -C "$GOV" log --format=%h --grep=gate2-pt-w30-b -- PLANNER_TEMPLATE.md, verify it as the complete arm does (its show --stat → 27/4), then run the complete arm's idempotent tails`; when (3) = no and (5)'s first number ≠ 74 with the two numbers equal and (4) still reads `accepted`, it reads `the accepted set moved — another Gate 2 tranche has landed first, or an ingest staled an accepted row (accepted is not terminal, src/lessons_forge.py:31); re-pin P6/P8 and re-deposit`; when (1) mismatches and (3) = no and the template's porcelain is EMPTY, it reads `PLANNER_TEMPLATE.md moved under this plan — a doctrine act landed after the freeze; re-pin P1 and the builder's version constants, and re-deposit`. The ONE recipe: (1) mismatch + (3) no + (4) the twelve `accepted|codify` at their vintages + (5) `74|74` = a pre-commit apply that died — BUT ONLY IF the dirty file is THIS plan's build: `git -C "$GOV" diff HEAD --numstat -- PLANNER_TEMPLATE.md` → `27	4` (against HEAD, never the bare index-relative `diff`, which prints NOTHING in the staged half-state this recipe exists for) AND `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md` → ` M` or `M ` (never `MM`: a staged foreign edit beneath an unstaged build would pass the HEAD numstat and be discarded) AND `/usr/bin/grep -cF -- '### 111. ' "$PT"` → 1 (a foreign hand edit to the template presents the SAME (1)+(3)+(4)+(5) shape and must never be discarded; any other numstat or porcelain → HALT untouched, quoting it) → `git -C "$GOV" checkout HEAD -- PLANNER_TEMPLATE.md` (`HEAD`, not the index — a death between `add` and `commit` leaves v4.113 STAGED), re-assert P1, then A1. Every other shape HALTs untouched.
>
> ⛔ **A1 — re-derive P1–P6; state each; a mismatch is a HALT quoting both.** (P4 with the builder's OWN commit, never `HEAD:`.)
>
> **A2 — dry-run, scratch→scratch (LITERAL paths, no variables across compounds):** `rm -rf /tmp/g2ptw30b; mkdir -p /tmp/g2ptw30b; python3 "$B" "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30b/PT-out.md; echo "builder_exit=$?"` → P5's `BUILT:` line verbatim, `builder_exit=0`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30b/PT-out.md` → `27	4`; `wc -l /tmp/g2ptw30b/PT-out.md` → 2581; `wc -c` → 542403. Refusals (each `BUILDER REFUSED`, nonzero; quote each): `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/PLANNER_TEMPLATE.md"` (out == in); `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/x.md"` (under a forbidden root — the literal governance root or the input's git toplevel; no `x.md` is written); `python3 "$B" /tmp/g2ptw30b/PT-out.md /tmp/g2ptw30b/PT-out2.md` (already built).
>
> **A3 — apply and measure the LIVE file (Task C), then commit:** `cp /tmp/g2ptw30b/PT-out.md "$GOV/PLANNER_TEMPLATE.md"`; then from the live file, counts read never exit codes (`/usr/bin/grep -cF -- '<token>' "$PT"`, each token as one single-quoted literal): `### 111. Diagnostic output is the only admissible evidence for a question about how the system BEHAVES` 1 · `**Rule 111 — diagnostic output` 1 · `*Source: proposal 267, codified 2026-08-11 (Gate 2 batch 2)*` 1 · `**A pin's producing command names its TARGET, and a pin protects a CLAIM, not a digest` 1 · `**A quantifier clause's POPULATION is pinned before the freeze` 1 · `**A scoping argument that correctly excludes one population is not a bound on the risk` 1 · `**A check is priced by its MARGINAL count, calibrated on the corpus before it is wired` 1 · `**A positive control proves the instrument works, not that you CALLED it correctly` 1 · `**A pattern that explains three defects is a HYPOTHESIS about the fourth` 1 · `**A diagnostic's authority extends only to what its instrument executed` 1 · `**The defect class just fixed is the one reproduced next` 1 · `**A COPIES claim needs three facts` 1 · `codified 2026-09-15 — Gate 2, thread 336, PT tranche two` 9 · `codified 2026-09-15 (Gate 2, thread 336 — PT tranche two)` 1 · `**Version:** 4.113` 1 · `**Version:** 4.112` 0 · `**Last Updated:** 2026-09-15 (v4.113)` 1 · `**Last Updated:** 2026-09-15 (v4.112)` 0 · `| 2026-09-15 | v4.113: Gate 2, thread 336` 1 · `| 2026-09-15 | v4.112: Gate 2, thread 336` 1 (tranche one's row survives below the new one) · `## Lifecycle DB Read Protocol (Planner)` 1 · `### 77. ` 1 · `### 78. ` 1 · `### 112. ` 1 · `### 14. ` 3 · `### 17. ` 2 (unchanged); `wc -l` 2581; `cmp /tmp/g2ptw30b/PT-out.md "$GOV/PLANNER_TEMPLATE.md"` silent; `git -C "$GOV" diff --stat -- PLANNER_TEMPLATE.md` → 27 insertions, 4 deletions. Any failing → FRESH: `git -C "$GOV" checkout -- PLANNER_TEMPLATE.md` + HALT; RE-ENTRY: HALT, no restore. Commit, the message on stdin: `git -C "$GOV" add PLANNER_TEMPLATE.md && git -C "$GOV" commit -F - -- PLANNER_TEMPLATE.md <<'MSG'` with the subject `[<id from your plan filename>] gate2-pt-w30-b: PLANNER_TEMPLATE v4.113 — eight extensions (471+502, 469, 482, 501+512+553, 488, 492, 506, 449), Rule 111's heading (484)` and `MSG` closing it; `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` → that commit. Do NOT push.
>
> **A4 — the flip (doctrine before DB):** backup first, by sqlite's own copy — the DB is WAL-journaled, so a filesystem `cp` can lag a non-checkpointed write while sizes match: `sqlite3 "$DB" ".backup /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-b-$(date +%H%M%S).db"`; then on the backup — ⚠️ through `sqlite3 -readonly "file:<backup>?immutable=1"`, never a bare `-readonly <path>`: a `.backup` output of a WAL database has no `-shm` sidecar and a bare read-only open fails `unable to open database file (14)` — `PRAGMA integrity_check` → `ok`; `SELECT COUNT(*) FROM lesson_proposals` → equal to the live count (state both); `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (449,469,471,482,484,488,492,501,502,506,512,553) AND status='accepted'` → 12 (`BK=12`). Write `$GOV/governance/knowledge/development/g2ptw30b-flip.sql` with a QUOTED heredoc, EXACTLY:
> ```
> .timeout 5000
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=12));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (449,469,471,482,484,488,492,501,502,506,512,553) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output /tmp/g2ptw30b/flip-capture.txt
> SELECT COALESCE(id,'')||'|'||COALESCE(status,'')||'|'||COALESCE(route,'')||'|'||COALESCE(category,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 590 ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (449,469,471,482,484,488,492,501,502,506,512,553) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (449,469,471,482,484,488,492,501,502,506,512,553) AND status='implemented' AND status_updated_at NOT IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z');
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> ⚠️ The `.output` path is the LITERAL scratch dir A2 created (`sqlite3` dot-commands take no variables) — never a path inside the canonical bellows checkout. Run: `[ -d /tmp/g2ptw30b ] || mkdir -p /tmp/g2ptw30b; sqlite3 -bail "$DB" < "$GOV/governance/knowledge/development/g2ptw30b-flip.sql"` (⚠️ `-bail` is LOAD-BEARING: with the capture dir missing it stops at `.output` and the transaction never commits; without it sqlite skips the failed dot-command and the flip COMMITS with no capture file) → the five sentinel lines BY NAME: `PRE_F=12`, `CHANGES_F=12`, `EXCL_F=12`, `ACC_POST=62`, `IMPL_POST=357` (a `CHECK` abort prints a line beginning `Runtime error near line 4: CHECK constraint failed: x=12` and writes nothing — HALT on that MESSAGE; never read the exit code through a pipe, it is the last command's). **Read-back on a FRESH read-only connection** (durability is proven post-commit, never by in-transaction sentinels): the twelve rows `implemented|codify|ceo|<stamp>` with every stamp outside the two vintages; `accepted` 62; `implemented` 357; the capture file has 590 lines and 74 rows containing `|accepted|codify|`. Copy the capture into the worktree: `mkdir -p knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15 && cp /tmp/g2ptw30b/flip-capture.txt knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt` (A5 commits it; QA verifies it). Commit the SQL in governance, the message on stdin: `git -C "$GOV" add governance/knowledge/development/g2ptw30b-flip.sql && git -C "$GOV" commit -F - -- governance/knowledge/development/g2ptw30b-flip.sql <<'MSG'` with the subject `[<id>] gate2-pt-w30-b: the flip SQL (449, 469, 471, 482, 484, 488, 492, 501, 502, 506, 512, 553 -> implemented)`.
>
> **A5 — dev log + commit by explicit pathspec, the pre-check chained first.** `knowledge/development/dev-log-gate2-pt-w30-b-2026-09-15.md`: the A0 determination, A1's pins, A2's lines and the three refusals, A3's counts and the governance commit hash, A4's backup lines, sentinels, read-back and the SQL commit hash. Then, as ONE command: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/development/dev-log-gate2-pt-w30-b-2026-09-15.md knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt && git commit -F - -- knowledge/development/dev-log-gate2-pt-w30-b-2026-09-15.md knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt <<'MSG'` with the subject `[<id>] gate2-pt-w30-b: PT v4.113 + flip landed (dev log, flip capture)` and `MSG` closing it. `git status --short` → empty. STOP.
>
> **Deposits:**
> - `knowledge/development/dev-log-gate2-pt-w30-b-2026-09-15.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30b-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt`
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-pt-w30-b-2026-09-15.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30b-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt`

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message.** You are the bellows QA agent. `cd "$(git rev-parse --show-toplevel)"`; `GOV=/Users/marklehn/Developer/eluvian-governance`; `B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-b-2026-09-15.py"`; `DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db`; `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python` — re-derive per compound. Read-only beyond the three evidence files; the commit's message goes on stdin, as STEP 1's header states.
>
> **(A) Rule 20 self-check** — the canonical block at the path the dispatcher's mandate names (this machine's `RULE_20_SELF_CHECK_BLOCK.md`; quote the path you were handed). Run with:
> - `plan_slug`: `gate2-pt-w30-b-2026-09-15`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15"`
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-pt-w30-b.txt", "flip-capture.txt"]` (the capture committed by DEV in A5; QA verifies it is the committed file — `git log -1 --format=%s -- knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt` carries the plan's id)
>
> **(B) Items — raw output appended to `probes-raw.txt`** (⛔ the raw file carries NO summary-shaped line: P7 is named as "P7", never restated with its numbers; the suite's summary line stays in the suite file; no commit log is pasted beyond Item 4's path-scoped probe — MUST-PRESERVE's raw-file rule, thread 363):
> - **Item 1 — the live template:** `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` (the `[<id>]` commit); every Task C count re-run (twenty-six probes); `wc -l` 2581; `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30b-flip.sql` → EMPTY.
> - **Item 2 — the builder, by a second pair of hands (byte-identity):** `rm -rf /tmp/g2ptw30b-qa; mkdir -p /tmp/g2ptw30b-qa; C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show "$C^:PLANNER_TEMPLATE.md" > /tmp/g2ptw30b-qa/PT-pre.md; python3 "$B" /tmp/g2ptw30b-qa/PT-pre.md /tmp/g2ptw30b-qa/PT-out.md; echo "builder_exit=$?"; cmp /tmp/g2ptw30b-qa/PT-out.md "$GOV/PLANNER_TEMPLATE.md" && echo BYTE_IDENTICAL` → the `BUILT:` line, `builder_exit=0`, `BYTE_IDENTICAL`; P4 with the builder's own commit; the three refusals reproduced.
> - **Item 3 — the flip, read back on a FRESH read-only connection:** the twelve rows `implemented|codify|ceo|<stamp outside the two vintages>`; `accepted` 62 — and the sixty-two by count, each still at one of the two vintages (`SELECT COUNT(*) … WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z')` → 62); `implemented` 357; `wc -l knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/flip-capture.txt` → 590; the capture's twelve rows for the flipped ids read `accepted|codify` (the pre-flip state, captured before the UPDATE).
> - **Item 4 — the full-suite file:** `"$BPY" -m pytest tests -q -p no:cacheprovider > knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/full-suite-gate2-pt-w30-b.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/full-suite-gate2-pt-w30-b.txt` → `exit=0` and a summary line with NO `failed`/`error` count — P7's counts are the expectation, but the gate (`_gate_qa_test_result`) reads FAILURES against `known_failures`, never the pass count: if bellows `main` moved since P7 was measured (`git log --format=%h ff4ac39c..HEAD -- tests` non-empty — P7's commit is the anchor, re-pinned with P7 at the freeze; the basis moved under this draft as threads 307's, 321's and 322's plans merged their tests), the drift is stated in `probes-raw.txt` under Item 4 as that probe's command and the HASHES it printed (`--format=%h`, never `--oneline` — a bellows QA commit's subject carries `N passed`, the summary shape the raw file must not hold), and nothing broader; the receipt's suite row stays the FILE and `exit=0`, never a note: a drift note reaches for `not run`, `skipped` or `pending`, and the canonical block fails a positive row carrying any of them. A different pass count with zero failures is not a halt. ⛔ A suite file WITHOUT a summary line (a crashed run — a traceback, a killed process) is a HALT, never a re-run note and never a second attempt written over it: with the named suite file summary-less the gate falls to the next `.txt` carrying one, and the raw-file rule above is the only thing standing between that and a stale count (the EXECUTION seat measured both configurations, S2-1).
>
> **(C) The report** `qa-receipt.md`: the verification table — status cells carry the glyph only, and NO positive row quotes a probe token or an output line carrying a Rule 20 hedging keyword (`hedging_keywords` in the canonical block): this plan has THREE such sources — the suite summary line, Rule 78's extension body, and the agent's OWN prose (a drift note, a re-run note: `not run`, `skipped` and `pending` are the words such a note reaches for, and the block fails a positive row carrying any of its `hedging_keywords` whoever wrote it) — so the suite row names the FILE and `exit=0` with the drift left in `probes-raw.txt`, and Item 1's row reads `26/26 counts as pinned in A3, see probes-raw.txt` (the raw file may carry the tokens; the receipt's positive rows may not — a row quoting any of them FAILS the self-check) — the follow-ups (the five tranches after this one; the Planner pushes governance and projects the markers; thread 336 at the keyboard), the Rule 20 stdout APPENDED. Commit, as ONE command with the pre-check chained and the message on stdin: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/ && git commit -F - -- knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/ <<'MSG'` with the subject `[<id>] QA: PT v4.113 rebuilt byte-identical from the pre-edit blob; flip read back (12 implemented, 62 accepted remain)` and `MSG` closing it. STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/full-suite-gate2-pt-w30-b.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-b-2026-09-15/full-suite-gate2-pt-w30-b.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
