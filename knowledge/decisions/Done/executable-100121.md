# bellows — executable: DOCTRINE — Gate 2, thread 336, PT tranche five (the execution environment, restart and liveness): proposals 447, 456, 459, 497, 517, 527, 546, 573, 576, 581, 582 onto PLANNER_TEMPLATE.md v4.117 by a committed builder (three extensions — Rule 105, Restart Discipline, Rule 55; a new Rule 118 — the interpreter is a pin), then the eleven flip `implemented` — 25 accepted rows remain for the two tranches after it

**Date:** 2026-09-15 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full (the bellows suite is UNCHANGED by this plan and is run once by QA so the test gate reads a real summary line; the gate reads FAILURES against `known_failures`, never the pass count — P7 is the expectation, and bellows `main` moves as plans merge) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 2

**auto_close:** false

**Slug:** `gate2-pt-w30-e-2026-09-16`

**Depends on:** the CEO's rulings of 2026-09-14 (thread 336 — one Gate 2 over the 86 accepted proposals, W=29 and W=30 together) and the delegation of 2026-09-15 ("Start on gate 2 and you have my delegation to release at the proper time"; "No I am good with moving forward on these" — the seven-tranche partition, `gate2-packet-2026-09-15.md` §2, its partition row "PT five — the execution environment, restart and liveness"); the non-author Gate 1 packets — W=29's `gate1-packet-2026-09-02.md` (447, 456, 459) and W=30's `gate1-packet-2026-09-13-a.md` (reader A: 497), `-b.md` (reader B: 517, 527, 546) and `-c.md` (reader C: 573, 576, 581, 582); **tranche four** — `Done/executable-100120.md` in bellows (`gate2-pt-w30-d-2026-09-16`, the clone origin and the newest same-class plan: doctrine prose onto `PLANNER_TEMPLATE.md` by a committed builder, then a scoped CAS flip; its text walked under two file names — `walk-register-gate2-pt-w30-d-2026-09-16.md` (walk 0, the scout's ten folds, walk 1, the EXECUTION seat's zero findings, the broken walk 2) and `walk-register-gate2-pt-w30-d2-2026-09-16.md` (the fresh record, the CAPSTONE seat)) and `Done/executable-100119.md` (tranche three, the grandparent form); the LIVE `PLANNER_TEMPLATE.md` is v4.116 (tranche four's act, governance `d04f2f4a`), this tranche's pre-edit version, so every pin below is measured on it directly; the builder `governance/knowledge/decisions/drafts/build-gate2-pt-w30-e-2026-09-15.py` (committed `668fcaf0` — the walk-0 docstring fix after the scout's s15, the build unchanged — digest `cba5026c71ba6f99`; proven on the v4.116 candidate chain at authoring 2026-09-15, its E2 anchor re-pinned at tranche four's walk 0 to Rule 117's landed Source line, re-proven on the LIVE v4.116 at walk 0). Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-e-2026-09-16.md`.

**Tier computed, not judged (§1):** **T-6 fires** — `PLANNER_TEMPLATE.md` is the template → **T2**. T-2 fires (eleven canonical rows flip; caps at T1). T-1 fires (two repos, plus the DB). T-8 does NOT fire — a structure-for-structure clone of tranche four (#100120) carrying 540's extension form and tranche one's new-rule form; seven edits against eleven, one new rule against one, no span, is scale, not shape. **Panel form — the magnitude proxy, measured at walk 0 (all three must hold): (i) structure-clone** — the parent is #100120 (closed 2026-09-16 12:31, the newest PT tranche), and both walk-0 clone-diffs ran (against its final text and its register); **(ii) mechanized edits** — every template mutation flows through the committed builder, whose seven anchors are count-1-asserted BY EXECUTION and whose 30 post-conditions are asserted on the output before any write, with the line and byte deltas asserted against pinned constants (proven at authoring on the v4.116 candidate and at walk 0 on the LIVE v4.116: a byte-identical rebuild, a `17	2` numstat, the byte pin firing on an off-by-one copy of the constant, the three refusals); the flip is one CAS statement under a structural pre-check; **(iii) scripted probes** — every new token measured 0 against the pre-edit file, the invariant tokens at their MEASURED pre-edit counts. **→ SMALL form: the walk-0 scout, then EXECUTION, then CAPSTONE — the seats on the Air when a session is live there (the CEO's direction of 2026-09-15, thread 369), else local subagents.** The CEO may call the full form.

## Why this exists

Gate 1 routed 86 proposals `accepted|codify`; tranches one to four landed fifty of them (v4.112 by #100109, v4.113 by #100114, v4.115 by #100119, v4.116 by #100120). This tranche is the packet's fifth row: the eleven rows about the EXECUTION ENVIRONMENT, RESTART and LIVENESS — the four further axes a dispatch environment carries beyond the process tree (filesystem position, a daemon subprocess's cwd, the layout the resolvers run on, the import binding inside pytest), the interpreter as a pin and a version test on the oldest interpreter, a restart proven by the process's own facts with liveness per call site and a "manual act" traced to its parent, hosting under an owner whose lifetime is the job's, and a liveness test that reads a signal the death removes. Their homes are the packets', re-verified against the live v4.116 at authoring (every probe `/usr/bin/grep -c -F`; the live count then the build's): `four more axes` 0 → 1; `the interpreter is a PIN` 0 → 1; `process's own facts` 0 → 1 (one line, two occurrences); `the death REMOVES` 0 → 1. **Homes:** THREE extension paragraphs appended after the home's existing last line (540's form) — Rule 105 (447 + 517 + 527 + 546, after its Source line), Restart Discipline (459 + 497 + 573 + 456), Rule 55 (582) — each opening in bold with its proposals, entries and `codified 2026-09-16 — Gate 2, thread 336, PT tranche five`; ONE new rule (`### 118.`, its body opening as kin of Rule 105, its Source line carrying `codified 2026-09-16 (Gate 2, thread 336 — PT tranche five)`, its `---`), placed after Rule 117's separator and before the Lifecycle DB Read Protocol; the version, the `Last Updated` line and the History row. Numbering append-only: Rule 118 follows Rule 117. The Rule 19 keyword scan of the builder's added lines at authoring (word-bounded, the canonical list): none.

## What this plan does

**In the governance checkout, by absolute path (`GOV=/Users/marklehn/Developer/eluvian-governance`), seven anchored edits to `PLANNER_TEMPLATE.md` by the committed builder (never by hand):**
- **E1** — Rule 105: after its Source line `*Source: proposal 434, entry 426 (2026-08-31), codified 2026-09-02 (Gate 2, cycle W=28 PT tranche one)*` (the section's last line before its separator; the anchor re-emitted), the 447 + 517 + 527 + 546 paragraph. **E3** — Restart Discipline: after its last paragraph (anchor: the blank line and the `### Dispatch Path Rules` heading, re-emitted), the 459 + 497 + 573 + 456 paragraph. **E4** — Rule 55: after its last paragraph (anchor: the blank line and Rule 56's heading), the 582 paragraph.
- **E2** — the NEW Rule 118: the anchor is Rule 117's Source line `*Source: proposal 505, entry 497 (2026-09-06), codified 2026-09-16 (Gate 2, thread 336 — PT tranche four)*`, its `---` and the `## Lifecycle DB Read Protocol (Planner)` heading (count 1); the replacement re-emits them around the new rule — `### 118. The interpreter is a PIN` …, its body opening in italics as kin of Rule 105, its Source line `*Source: proposals 576 and 581, entries 568 and 573 (2026-09-11, 2026-09-12), codified 2026-09-16 (Gate 2, thread 336 — PT tranche five)*` (two proposals, two entries each dated — the packet's row; the Rules 112–117 form extended to a second entry), its `---` (tranche one's Rules 112–115 form).
- **E5** — `**Version:** 4.116` → `**Version:** 4.117`. **E6** — `**Last Updated:** 2026-09-16 (v4.116)` → `**Last Updated:** 2026-09-16 (v4.117)`. **E7** — the History row inserted directly under the `| Date | Lesson |` header (tranche four's v4.116 row, tranche three's v4.115 row, thread 322's v4.114 row, tranche two's v4.113 row and tranche one's v4.112 row survive below it).

The builder reads the file as bytes, refuses CRLF, refuses an already-built input, asserts each anchor's count is 1, applies the seven edits, asserts 30 post-conditions on the OUTPUT (the builder's `POST` list: the Rule 118 heading once; the History row's `**Rule 118 — the interpreter is a PIN` once; the codified strings at 3 and 1; the three openers once each; the v4.117 tokens 1 and the v4.116 tokens 0; the new History row 1 and the five rows below it 1 each; the section headings surviving at their MEASURED pre-edit counts — `### 55. `, `### 56. `, `### 105. `, `### 106. `, `### 116. `, `### 117. ` at 1, `### 14. ` 3, `### 17. ` 2, `### Restart Discipline` 1, `### Dispatch Path Rules` 1, the Lifecycle DB Read Protocol heading 1; Rule 105's and Rule 117's Source lines 1 each), asserts the line and byte deltas against PINNED constants (`EXPECTED_LINES = 15`, `EXPECTED_BYTES = 12798` — a post-condition that can fail, proven at authoring on an off-by-one copy of the constant) and only then writes — to a SCRATCH path it refuses to let be the live file or anything under the governance root or the input's git toplevel. Apply = copy the proven scratch output over the live file, then re-measure the live file.

**In the lessons DB (`/Users/marklehn/Developer/forge_lessons/lessons-forge.db`, the mini's one live copy), AFTER the doctrine commit:** one scoped CAS flip — `id IN (447,456,459,497,517,527,546,573,576,581,582) AND status='accepted' AND route='codify'` → `implemented`, `status_updated_by='ceo'` (the delegation's record), a fresh UTC stamp — under a structural pre-check (a TEMP-TABLE `CHECK(x=11)` that aborts the transaction before the UPDATE if the eleven are not all `accepted|codify`), with the capture of every row with `id <= 590` — the whole table at authoring — `(id, status, route, category, status_updated_by, status_updated_at)` to a SCRATCH file BEFORE the UPDATE (copied into the worktree's evidence dir afterwards — never written into the canonical checkout), and the sentinels by name: `PRE_F=11`, `CHANGES_F=11`, `EXCL_F=11` (the eleven rows now `implemented` with a stamp that is NOT one of the TWO vintage stamps — 447, 456 and 459 carry W=29's `2026-09-02T23:54:37Z`, the eight W=30's `2026-09-13T18:27:07Z`), `ACC_POST=25`, `IMPL_POST=394`. The flip SQL is a committed file in governance; a backup of the DB precedes it.

**In the bellows worktree:** the dev log and the QA evidence.

## What this plan does NOT do

- Does not touch the twenty-five `accepted|codify` rows of tranche six and the DC/PST tranche (their homes are named in the packet — each is its own plan), DRAFTING_CYCLE.md, PANEL_SEAT_TEMPLATE.md, COMPANY.md, MACHINE_SETUP.md, GLOSSARY.md, code, or threads. Does not push (the Planner pushes after the pause; the Planner's Rule 22 (b) read at the pause takes the canonical block's stdout and the daemon's gate row as the receipt's evidence, never the QA's `PRECHECK: 0 failure(s)` line — at a QA step `tools/check_deposit.py` verifies presence alone, thread 362). Does not project the `LESSONS.md` markers (the Planner's act after the close — `scripts/project_status_markers.py --apply`, the Rule 112 instrument). Does not close thread 336 (the Planner's `threads done` with the seventh tranche).
- ⚠️ **Runs after tranche four (#100120, 2026-09-16 — its v4.116 is this plan's pre-edit version, LIVE) and BEFORE any W=31 ingest or another tranche** — A0's ladder asserts the accepted population (36 before this plan's flip, 25 after) with every accepted row at one of the two vintage stamps; another tranche landing first moves it, and a W=31 ingest could stale an accepted row (`accepted` is not terminal — `src/lessons_forge.py:31`). The 70 `[status: pending]` headings written since W=30's ingest wait (the marker projection at governance `8174dfb5`, 2026-09-16 12:31, is the newest; its dry run at this derivation counted `unmatched (no DB row): 70` — re-read at the freeze).
- ⚠️ **PLANNER_TEMPLATE's version is moved by the CEO's doctrine acts at other plans' closes** (tranche four's act is this plan's pre-edit version, live since 2026-09-16 12:16 (`d04f2f4a`; #100120 Done 12:31); tranche one moved twice under its own draft, tranches two to four not at all): the re-pin surface is the PIN TABLE and its restatements — P1 (sha, lines, bytes); P2 (every line number, which a doctrine act above the anchors shifts, and its `**Version:**` anchor); P4 (the builder's digest, after its two constants `OLD_VERSION`/`NEW_VERSION` are re-committed); P5 (`wc -l`, `wc -c`; the BUILT line's deltas hold unless an act lands inside one of the four homes, which an anchor count or the byte pin refuses); Task C's four version tokens; the `wc -l` restated in A2, A3, both RE-ENTRY arms and QA Item 1 and the `wc -c` in A2; every literal of the new version (the title, the E5/E6 lines, the POST description, MUST-PRESERVE, P3, the RE-ENTRY text, Task C, the THREE commit subjects) and the pre-edit-state literals (E7's, the POST description's, P1's, P2's anchor, P3's, Task C's zero probe); the register's Targets line and title — re-pinned TOGETHER at the freeze and before the release by ONE scripted pass over those sites (tranche one's CAPSTONE re-pin is the form), recorded in the register with each literal's count before and after, and Task C's probes re-run on a scratch build of the new version, and the Why-line dedup probes re-run against the new pre-edit version with their `live v4.NNN` label and counts re-pinned with them (the CAPSTONE seat's S3-1); the historical mentions keep their versions; a moved version at A0 is a NONE-MATCH halt and a redeposit, never a guess.

## MUST-PRESERVE

- ⚠️ **TWO REPOSITORIES AND A DB, ONE STEP.** Governance by absolute path, `git -C "$GOV"` never `cd`; porcelain EMPTY and sha = P1 before touching `PLANNER_TEMPLATE.md`; commit by explicit pathspec with the message on stdin; no push. The DB by absolute path; `sqlite3` with `-bail`; a backup (`pre-gate2-pt-w30-e-<HHMMSS>.db` at the forge root, untracked) BEFORE the flip.
- **The builder is the only editor of the template.** Its committed blob digest must equal the on-disk file (P4) before it runs; it runs scratch→scratch; the live file is replaced only by `cp` of a scratch output whose post-conditions the builder already asserted, and the live file is then re-measured (Task C).
- **Doctrine before DB (540's C6):** the flip runs only after the governance commit has landed; a HALT between leaves v4.117 committed-unpushed and the flip owed — the RE-ENTRY arm in A0 resumes at the flip.
- ⛔ **Never run any `checkout --` on an A0 halt except the ONE recipe named there** (a pre-commit apply that died, discriminated by the HEAD numstat, the porcelain and the `### 118. ` token); a pin mismatch otherwise means the file carries changes that are not this plan's.
- ⚠️ **NO landed text carries a Rule 20 hedging keyword** — the scan of the builder's added lines against the canonical block's list (at authoring, on the live build) returned nothing; still, NO receipt row quotes a landed paragraph's body: Task C pins heading fragments and opener fragments only, and the receipt cites counts
- **`known_failures: 0`.** The suite runs in the worktree under the canonical venv; the QA reads its own run's failure count, and a bellows `main` that gained tests since P7 is stated in `probes-raw.txt` under Item 4 (the probe's command and the hashes it printed; the summary line stays in the suite file), never in the receipt's row and never halted on. Any failure is a HALT/Critical.
- ⛔ **`probes-raw.txt` carries no summary-shaped line** — no `N passed` restatement of P7, no pasted suite line, no commit log beyond the path-scoped drift probe's: the suite gate (`gates._select_summary_txt`, bellows `91354d2a`) chooses, among the `.txt` deposits that carry a pytest summary, a basename carrying `suite` or `full`, then the largest `passed` count, then the first match — so a summary-shaped line in the raw file is the one read whenever the suite file crashed without a summary or lost its name (thread 363's defect; its "first `.txt`" wording is narrower than the live gate, measured 2026-09-15 on `71c6bfc3`), and it is forbidden here by name so the selection never rests on that tie-break (tranche one's QA wrote "P7 baseline: 2395 passed, 2 skipped" as a header there — harmless beside a named suite file, forbidden here). P7 is cited as "P7" in the raw file, never restated with its numbers.
- ⛔ **Every commit in the bellows worktree chains `tools/check_deposit.py` before `git add`** (Rule 23; the step IS bound — the commit binding was installed by bellows `4bcde8a9` (#100105, thread 333 — `hooks/git/pre-commit` and the daemon's `BELLOWS_PRECHECK_*` env), and tranches one's to four's chains ran under it), and puts its message on stdin (`git commit -F -` with a quoted heredoc), never in a file.

## Numbers discipline — the pins DEV re-derives (measured 2026-09-16 by the Planner's derivation script on the LIVE PLANNER_TEMPLATE v4.116 at governance `d04f2f4a`, bellows main `4431bc3a`, forge_lessons `a1ebf45`; re-measured at the freeze)

| pin | what | value | how |
|---|---|---|---|
| P1 | **`PT_SHA`**, lines, bytes — pre-edit (the LIVE v4.116, tranche four's act at #100120's DEV, governance `d04f2f4a`) | `23969632693f592c`; 2624 lines; 578,618 bytes (= v4.115 `d61b6c1108ab8608` / 2601 / 558,339 + tranche four's twenty-three lines, +20,279 bytes) | `shasum -a 256 "$GOV/PLANNER_TEMPLATE.md" \| cut -c1-16`; `wc -l`; `wc -c` |
| P2 | **`ANCHORS`** — the builder's seven anchors | each count 1: Rule 105's Source line `*Source: proposal 434, entry 426 (2026-08-31), codified 2026-09-02 (Gate 2, cycle W=28 PT tranche one)*` (E1, at 1505 of the live v4.116) · Rule 117's Source line `*Source: proposal 505, entry 497 (2026-09-06), codified 2026-09-16 (Gate 2, thread 336 — PT tranche four)*` with its `---` and the `## Lifecycle DB Read Protocol (Planner)` heading (E2, 1603) · the `### Dispatch Path Rules` heading with its preceding blank line (E3, 2023) · Rule 56's heading with its preceding blank line (E4, 1141) · `**Version:** 4.116` (5) · `**Last Updated:** 2026-09-16 (v4.116)` (6) · the History header pair (2404–2405; tranche four's v4.116 row directly under it) — the line numbers measured on the live v4.116, re-measured at the freeze | `/usr/bin/grep -cF` (the multi-line anchors by the builder's own assertion) |
| P3 | **`TOKENS`** 0 before, their counts after | `### 118. ` · `**The dispatch environment has four more axes the process tree does not name` · `**A restart is proven by the process's own facts and a canary through the changed path` · `**A liveness test reads a signal the death REMOVES` · `**Rule 118 — the interpreter is a PIN` · `codified 2026-09-16 — Gate 2, thread 336, PT tranche five` · `codified 2026-09-16 (Gate 2, thread 336 — PT tranche five)` · `**Version:** 4.117` · `\| 2026-09-16 \| v4.117: Gate 2, thread 336` — each 0 before, 1 after but the two codified strings (3 and 1); retired after: `**Version:** 4.116`, `**Last Updated:** 2026-09-16 (v4.116)` — each 1 before, 0 after; invariants: `### 55. ` 1, `### 56. ` 1, `### 105. ` 1, `### 106. ` 1, `### 116. ` 1, `### 117. ` 1, `### 14. ` 3, `### 17. ` 2, `### Restart Discipline` 1, `### Dispatch Path Rules` 1, Rule 105's and Rule 117's Source lines 1 each, `\| 2026-09-16 \| v4.116: Gate 2, thread 336` 1, `\| 2026-09-16 \| v4.115: Gate 2, thread 336` 1, `\| 2026-09-16 \| v4.114: CEO act at the close of bellows plan 100118` 1, `\| 2026-09-15 \| v4.113: Gate 2, thread 336` 1, `\| 2026-09-15 \| v4.112: Gate 2, thread 336` 1 (unchanged by this plan) | `/usr/bin/grep -cF` |
| P4 | **`BUILDER`** — on-disk digest = the blob at its OWN last commit | `cba5026c71ba6f99` (governance `668fcaf0` — the builder's own last commit: the walk-0 docstring fix, the build unchanged; `6c2632e0` its E2 anchor re-pinned to Rule 117's landed Source line at tranche four's walk 0; `db049c3f` the chain proof's re-pin, `bfe58149` the re-pin to thread 322's act, `e7019082` its first) | `shasum -a 256 <builder> \| cut -c1-16`; `BC=$(git -C "$GOV" log -1 --format=%H -- governance/knowledge/decisions/drafts/build-gate2-pt-w30-e-2026-09-15.py); git -C "$GOV" show "$BC:governance/knowledge/decisions/drafts/build-gate2-pt-w30-e-2026-09-15.py" \| shasum -a 256 \| cut -c1-16` |
| P5 | **`DRY_RUN`** — the success line, numstat, sizes | `BUILT: <out> edits=7 blocks=1 lines+15 bytes+12798 post=30/30 deltas=pinned`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" <out>` → `17	2` (exit 1 is the expected differing state — read the numbers, never the exit); `wc -l <out>` → 2639; `wc -c <out>` → 591,416 (the live v4.116's; the output `fa7f800a63abe875`) | as stated, scratch-only |
| P6 | **`FLIP_PRE`** — the eleven rows | 447, 456, 459 each `accepted\|codify\|planner\|2026-09-02T23:54:37Z`; 497, 517, 527, 546, 573, 576, 581, 582 each `accepted\|codify\|planner\|2026-09-13T18:27:07Z`; `SELECT COUNT(*) … WHERE status='accepted'` → 36; `implemented` → 383; `SELECT status_updated_at, COUNT(*) … WHERE status='accepted' GROUP BY 1` → exactly two rows, `2026-09-02T23:54:37Z\|11` and `2026-09-13T18:27:07Z\|25`; `SELECT MAX(id), COUNT(*) FROM lesson_proposals` → `590|590` | `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db` |
| P7 | **`SUITE`** — the expectation | `2524 passed, 2 skipped`, exit 0 — bellows main `4431bc3a` (the newest QA's commit, 2026-09-16), the last suite file on main (`full-suite-gate2-pt-w30-d.txt`) — the drift probe is anchored at `4431bc3a`; re-read at the freeze; the gate reads failures, and `main` moves | `BPY -m pytest tests -q -p no:cacheprovider` |
| P8 | **`NO_INGEST_NO_TRANCHE`** (precondition, in two halves) | UNCONDITIONAL (before the ladder): `SELECT COUNT(*) FROM lesson_entries` → 580 (no W=31 ingest) AND `ls /Users/marklehn/Developer/forge_lessons/knowledge/decisions/ \| /usr/bin/grep -cF -- 'forge-cycle-w31'` → 0 (a zero-match count prints 0 and exits 1 — read the number). ARM-DEPENDENT (ladder step (5)): the accepted population with its at-the-vintages count — `36\|36` on FRESH and flip-owed; `25\|25` on complete — the eleven at their vintages on FRESH and flip-owed (ladder (4)) | as stated |

## Drafting Cycle

**Tier:** T2 — T-6 (the template), T-2 (eleven canonical rows, capped), T-1 fire; T-8 does not (a structure clone). **Panel: SMALL form** (the magnitude proxy measured at walk 0, all three conditions holding) — the walk-0 scout, then EXECUTION, then CAPSTONE, on the Air's session by cross-session message when one is live there, a local fresh-context seat after forty minutes of silence; the CEO may call the full form.
**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-e-2026-09-16.md
**Walk 0 (context pin, measured):** the v0 derived by script from tranche four's closed text (`executable-gate2-pt-w30-d2.md` at `fa2b6ff9`; #100120 Done 12:31:23), the script proven first on the v4.116 candidate — an explicit site list with the builder's basename and the parent's names set by site, every number measured on the LIVE v4.116 at run time, the leftover scan read, every named path checked (M1); the seven anchors each count 1 at their measured lines (M2); the eleven entries and their Gate 1 rows read, the dedup probes 0 on the live file and at their Why-line counts on the build (M3); the live v4.116 `23969632693f592c` / 2624 / 578,618 and the builder `cba5026c71ba6f99` = its blob at `668fcaf0` (M4; `f5633f760dfe72e8` at `6c2632e0` before the scout's s15); every home's last writer by blame (M5); the dry run on the live file `lines+15 bytes+12798 post=30/30`, the output `fa7f800a63abe875` / 2639 / 591,416, numstat `17 2`, BYTE_IDENTICAL on a rebuild, the three refusals, the off-by-one constant refused, the pre-act v4.115 refused on the Source-line anchor, CRLF refused, the chain e→f proven, the Rule 19 scan none (M6); the flip rehearsal owed to the scout (M7); the DB `11|25` of 36 accepted, 383 implemented, 590 rows, 580 entries (M8); the suite `2524 passed` at `4431bc3a`, the drift probe empty (M9); the consumers under pytest `WRITES 8`, `ASSIGNED shop-infra`, the extractor's two lists (M10); `plan_lint` 0 FAIL, the WARN set read, the baseline armed after this register existed (signals=14), `propagation_check` 144 all detector (1) (M11). Written 12:34 from the printed output. Verdict at walk 0: the magnitude proxy holds → SMALL form; the scout convened on the v0.

- Weak spots:          w1 dry; w2 dry; w3 dry; w4 dry
- Destruction:         w1 dry; w2 dry; w3 dry; w4 dry
- Vulnerabilities:     w1 dry; w2 dry; w3 dry; w4 dry
- Integration-record:  w1 dry; w2 dry; w3 8 folded — instruction 1 / record 7; w4 dry
- ACID:                w1 dry; w2 dry; w3 dry; w4 dry
**Cold panel (SMALL form):** _the walk-0 SCOUT convened 12:36 on the frozen v0 (`fb18dd6b`; a local fresh-context reader — 15 findings, all folded at walk 0, `a4bc7dde`); the EXECUTION seat on the Air after walk 1 (`f4c58076`, zero findings); the CAPSTONE seat after walk 2 as a LOCAL fallback on the mini under the forty-minute rule after the Air's silence (8 findings, folded at walk 3)._
**Closing:** ✅ **BAR MET — walk 4 dry on every lens after walk 3's eight folds (instruction 1 / record 7 — the local CAPSTONE seat's S3-1..S3-8), walks 1 and 2 dry ×5 each after the scout's fifteen walk-0 folds and the Air's EXECUTION seat's zero findings. The content — three extensions on Rule 105, Restart Discipline and Rule 55, one new Rule 118, the eleven rows' flip — is the text the walk-0 scout, the EXECUTION seat (the Air, `f4c58076`) and the CAPSTONE seat (a local fallback under the forty-minute rule after the Air's silence, `faec2125`) read, its freeze checklist MATCHING on the LIVE v4.116 at every seat (P1–P8, Task C 30/30, the chain e→f), the flip rehearsed by the scout on its own backup with the missing-dir abort under `-bail`. Residue: none. Direction verdict after walk 1: PROCEED. Released under the CEO's delegation of 2026-09-15 ("release at the proper time"), after #100120's Done and the v4.116 act; never two bellows plans concurrently.**

## Cycle Manifest
tier: T2
target: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md
class: shop-infra
reads: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-pt-w30-e-2026-09-15.py, /Users/marklehn/Developer/eluvian-governance/gate2-packet-2026-09-15.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-02.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-a.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-b.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-c.md, /Users/marklehn/Developer/eluvian-governance/LESSONS.md, knowledge/decisions/Done/executable-100120.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-540.md, /Users/marklehn/Developer/forge_lessons/lessons-forge.db
writes: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30e-flip.sql, /Users/marklehn/Developer/forge_lessons/lessons-forge.db, knowledge/development/dev-log-gate2-pt-w30-e-2026-09-16.md, knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt, knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/qa-receipt.md, knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/probes-raw.txt, knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/full-suite-gate2-pt-w30-e.txt
open_forks: 1. the twenty-five accepted rows of tranche six and the DC/PST tranche (their own plans, in the packet's order); 2. Rule 118 follows Rule 117 (numbering append-only) — the packet's "beside Rule 105" placement is carried by the kin clause that opens its body, not by its position; 3. the `LESSONS.md` markers are projected after the close by the Planner, not by this plan; 4. P7's suite count moves with bellows main and is re-pinned at the freeze
walks: 4
yields: 0, 0, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:145
coherence: 4/4 body walks named in the register (23 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-gate2-pt-w30-e.md.foldcheck.json

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the bellows Developer. ⚠️ Governance runs IN PLACE at `$GOV` (every operand ABSOLUTE); commits there by explicit pathspec; ONE action per compound. ⛔ Every commit's message goes to `git commit -F -` on stdin — a quoted heredoc ending the commit's own command: `<<'MSG'`, the message on the lines after it, `MSG` alone on the last — so no file is written for it. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** (`BPY`); the builder runs under `python3` as tranche one's did (it imports only the stdlib — `os`, `sys`, and `subprocess` for the toplevel guard).
>
> ⛔ **A0 — roots, the precondition, then the branch ladder (catch-all LAST):** `cd "$(git rev-parse --show-toplevel)" && [ -f bellows.py ] && [ -d tests ] && echo TREE_OK` — HALT unless TREE_OK; `GOV=/Users/marklehn/Developer/eluvian-governance; PT="$GOV/PLANNER_TEMPLATE.md"; B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-e-2026-09-15.py"; DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db; [ -f "$PT" ] && [ -f "$B" ] && [ -f "$DB" ] && echo GOV_OK` — HALT unless GOV_OK. Re-derive `GOV`, `PT`, `B`, `DB` in every compound. `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python`. **P8's UNCONDITIONAL half → both values, else HALT** (`lesson_entries` → 580 and the `forge-cycle-w31` count → 0; on a mismatch: `a W=31 cycle has started — re-pin P6/P8 and re-deposit`). **P8's accepted-population half is read INSIDE the ladder, never before it:** it changes when THIS plan's flip lands. Then the ladder — **(1)** P1's sha matches; **(2)** `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30e-flip.sql` (state it; EMPTY on FRESH); **(3)** `git -C "$GOV" log -1 --format=%s -- PLANNER_TEMPLATE.md` — does the subject carry `gate2-pt-w30-e`?; **(4)** `sqlite3 -readonly "$DB" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (447,456,459,497,517,527,546,573,576,581,582) ORDER BY id;"`; **(5)** `sqlite3 -readonly "$DB" "SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted')||'|'||(SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z'));"` — the accepted population and how many of it sit at the two vintage stamps (every accepted row does, on EVERY arm).
> - **FRESH** = (1) match AND (2) empty AND (3) no AND (4) the eleven `accepted|codify` at their vintages (447, 456 and 459 at `2026-09-02T23:54:37Z`, the eight at `2026-09-13T18:27:07Z`) AND (5) `36|36` → A1.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` (a dirty template on re-entry is a FOREIGN edit — HALT untouched, quoting the porcelain) AND (4) the eleven still `accepted|codify` at their vintages AND (5) `36|36` → verify the COMMITTED state (the Task C counts on the live file, `wc -l` 2639, and `C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show --stat --format= "$C" -- PLANNER_TEMPLATE.md` → 17 insertions, 2 deletions — the PATH-SCOPED commit, never `HEAD`: once A4's SQL commit or any later governance commit lands, `HEAD` no longer names the template's commit and a `HEAD`-scoped show prints NOTHING with exit 0), then A4 (the flip) and A5 only. If (2) shows `g2ptw30e-flip.sql` untracked or modified with the flip NOT applied, that is this arm too — A4 rewrites the file from the exact text, which is idempotent.
> - **RE-ENTRY (complete)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` AND (4) the eleven `implemented|codify` with every stamp NOT one of the two vintages AND (5) `25|25` → verify the COMMITTED state (the Task C counts, `wc -l`, and the path-scoped show → 17/2); then the two idempotent tails of A4: the SQL file — if `git -C "$GOV" log -1 --format=%s -- governance/knowledge/development/g2ptw30e-flip.sql` does not carry `gate2-pt-w30-e`, write it from the exact text and commit it; the capture — if `/tmp/g2ptw30e/flip-capture.txt` exists copy it into the worktree, else reconstruct the worktree copy from the newest pre-flip backup — `BK=$(ls -t /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-e-*.db | head -1)` (each A4 attempt mints a second-stamped file; the newest is the one whose flip landed), then `[ -f "$BK" ] && echo "BK=$BK"` and `sqlite3 -readonly "file:$BK?immutable=1" "SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted'"` → 36 (THIS plan's pre-flip population; any other number is a HALT quoting every `pre-gate2-pt-w30-e-*.db` with its accepted count — a `-e-` backup reading 25 was minted by a second A4 attempt after the flip landed, and the capture is reconstructed from the newest backup reading 36, named in the HALT for the Planner, never chosen by the DEV) — no file is a HALT (`no pre-flip backup found — the capture cannot be reconstructed; re-deposit`), never an empty `$BK` handed to sqlite, which would open a scratch database and fail on the table — `sqlite3 -readonly "file:$BK?immutable=1" "<the capture SELECT>"` → the same 590 lines, labelled `RECONSTRUCTED (post-commit re-entry)` in the dev log); then A4's read-back (no flip) and A5 with the dev log marked `re-derived on re-entry`.
> - **NONE-MATCH** = anything else → HALT quoting every measurement — and when (5)'s SECOND number ≠ its first on ANY arm, the HALT reads `an accepted row carries a stamp outside the two vintages — an ingest or a foreign flip has moved the population; re-pin P6/P8 and re-deposit`; when (3) = no and (4) reads the eleven `implemented` with (5) `25|25`, it reads `this plan's flip has landed and a later act moved PLANNER_TEMPLATE.md after its commit — find the template's commit with git -C "$GOV" log --format=%h --grep=gate2-pt-w30-e -- PLANNER_TEMPLATE.md, verify it as the complete arm does (its show --stat → 17/2), then run the complete arm's idempotent tails`; when (3) = no and (5)'s first number ≠ 36 with the two numbers equal and (4) still reads `accepted`, it reads `the accepted set moved — another Gate 2 tranche has landed first, or an ingest staled an accepted row (accepted is not terminal, src/lessons_forge.py:31); re-pin P6/P8 and re-deposit`; when (1) mismatches and (3) = no and the template's porcelain is EMPTY, it reads `PLANNER_TEMPLATE.md moved under this plan — a doctrine act landed after the freeze; re-pin P1 and the builder's version constants, and re-deposit`. The ONE recipe: (1) mismatch + (3) no + (4) the eleven `accepted|codify` at their vintages + (5) `36|36` = a pre-commit apply that died — BUT ONLY IF the dirty file is THIS plan's build: `git -C "$GOV" diff HEAD --numstat -- PLANNER_TEMPLATE.md` → `17	2` (against HEAD, never the bare index-relative `diff`, which prints NOTHING in the staged half-state this recipe exists for) AND `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md` → ` M` or `M ` (never `MM`: a staged foreign edit beneath an unstaged build would pass the HEAD numstat and be discarded) AND `/usr/bin/grep -cF -- '### 118. ' "$PT"` → 1 (a foreign hand edit to the template presents the SAME (1)+(3)+(4)+(5) shape and must never be discarded; any other numstat or porcelain → HALT untouched, quoting it) → `git -C "$GOV" checkout HEAD -- PLANNER_TEMPLATE.md` (`HEAD`, not the index — a death between `add` and `commit` leaves v4.117 STAGED), re-assert P1, then A1. Every other shape HALTs untouched.
>
> ⛔ **A1 — re-derive P1–P6; state each; a mismatch is a HALT quoting both.** (P4 with the builder's OWN commit, never `HEAD:`.)
>
> **A2 — dry-run, scratch→scratch (LITERAL paths, no variables across compounds):** `rm -rf /tmp/g2ptw30e; mkdir -p /tmp/g2ptw30e; python3 "$B" "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30e/PT-out.md; echo "builder_exit=$?"` → P5's `BUILT:` line verbatim, `builder_exit=0`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30e/PT-out.md` → `17	2`; `wc -l /tmp/g2ptw30e/PT-out.md` → 2639; `wc -c` → 591416. Refusals (each `BUILDER REFUSED`, nonzero; quote each): `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/PLANNER_TEMPLATE.md"` (out == in); `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/x.md"` (under a forbidden root — the literal governance root, or the input's git toplevel when the input is inside a repository, as the live template is, so both bases are active here; no `x.md` is written. The EXECUTION seat's S2-1: on a NON-git input — the scratch candidate — only the literal root refuses, so a reproduction build sends its output to `/tmp`, never under a clone); `python3 "$B" /tmp/g2ptw30e/PT-out.md /tmp/g2ptw30e/PT-out2.md` (already built).
>
> **A3 — apply and measure the LIVE file (Task C), then commit:** `cp /tmp/g2ptw30e/PT-out.md "$GOV/PLANNER_TEMPLATE.md"`; then from the live file, counts read never exit codes (`/usr/bin/grep -cF -- '<token>' "$PT"`, each token as one single-quoted literal): `### 118. The interpreter is a PIN` 1 · `**Rule 118 — the interpreter is a PIN` 1 · `*Source: proposal 505, entry 497 (2026-09-06), codified 2026-09-16 (Gate 2, thread 336 — PT tranche four)*` 1 · `*Source: proposal 434, entry 426 (2026-08-31), codified 2026-09-02 (Gate 2, cycle W=28 PT tranche one)*` 1 · `**The dispatch environment has four more axes the process tree does not name` 1 · `**A restart is proven by the process's own facts and a canary through the changed path` 1 · `**A liveness test reads a signal the death REMOVES` 1 · `codified 2026-09-16 — Gate 2, thread 336, PT tranche five` 3 · `codified 2026-09-16 (Gate 2, thread 336 — PT tranche five)` 1 · `**Version:** 4.117` 1 · `**Version:** 4.116` 0 · `**Last Updated:** 2026-09-16 (v4.117)` 1 · `**Last Updated:** 2026-09-16 (v4.116)` 0 · `| 2026-09-16 | v4.117: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.116: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.115: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.114: CEO act at the close of bellows plan 100118` 1 · `| 2026-09-15 | v4.113: Gate 2, thread 336` 1 · `| 2026-09-15 | v4.112: Gate 2, thread 336` 1 (the five rows below the new one survive) · `## Lifecycle DB Read Protocol (Planner)` 1 · `### Restart Discipline` 1 · `### Dispatch Path Rules` 1 · `### 55. ` 1 · `### 56. ` 1 · `### 105. ` 1 · `### 106. ` 1 · `### 116. ` 1 · `### 117. ` 1 · `### 14. ` 3 · `### 17. ` 2 (unchanged); `wc -l` 2639; `cmp /tmp/g2ptw30e/PT-out.md "$GOV/PLANNER_TEMPLATE.md"` silent; `git -C "$GOV" diff --stat -- PLANNER_TEMPLATE.md` → 17 insertions, 2 deletions. Any failing → FRESH: `git -C "$GOV" checkout -- PLANNER_TEMPLATE.md` + HALT; RE-ENTRY: HALT, no restore. Commit, the message on stdin: `git -C "$GOV" add PLANNER_TEMPLATE.md && git -C "$GOV" commit -F - -- PLANNER_TEMPLATE.md <<'MSG'` with the subject `[<id from your plan filename>] gate2-pt-w30-e: PLANNER_TEMPLATE v4.117 — three extensions (447+517+527+546, 459+497+573+456, 582), Rule 118 (576+581)` and `MSG` closing it; `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` → that commit. Do NOT push.
>
> **A4 — the flip (doctrine before DB):** backup first, by sqlite's own copy — the DB is WAL-journaled, so a filesystem `cp` can lag a non-checkpointed write while sizes match: `sqlite3 "$DB" ".backup /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-e-$(date +%H%M%S).db"`; then on the backup — ⚠️ through `sqlite3 -readonly "file:<backup>?immutable=1"`, never a bare `-readonly <path>`: a `.backup` output of a WAL database has no `-shm` sidecar and a bare read-only open fails `unable to open database file (14)` — `PRAGMA integrity_check` → `ok`; `SELECT COUNT(*) FROM lesson_proposals` → equal to the live count (state both); `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (447,456,459,497,517,527,546,573,576,581,582) AND status='accepted'` → 11 (`BK=11`). Write `$GOV/governance/knowledge/development/g2ptw30e-flip.sql` with a QUOTED heredoc, EXACTLY:
> ```
> .timeout 5000
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=11));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (447,456,459,497,517,527,546,573,576,581,582) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output /tmp/g2ptw30e/flip-capture.txt
> SELECT COALESCE(id,'')||'|'||COALESCE(status,'')||'|'||COALESCE(route,'')||'|'||COALESCE(category,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 590 ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (447,456,459,497,517,527,546,573,576,581,582) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (447,456,459,497,517,527,546,573,576,581,582) AND status='implemented' AND status_updated_at NOT IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z');
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> ⚠️ The `.output` path is the LITERAL scratch dir A2 created (`sqlite3` dot-commands take no variables) — never a path inside the canonical bellows checkout. Run: `[ -d /tmp/g2ptw30e ] || mkdir -p /tmp/g2ptw30e; sqlite3 -bail "$DB" < "$GOV/governance/knowledge/development/g2ptw30e-flip.sql"` (⚠️ `-bail` is LOAD-BEARING twice: with the capture dir missing it stops at `.output` and the transaction never commits; and the CHECK pre-check aborts the flip only under it — without `-bail` sqlite skips the failed statement or dot-command and the flip COMMITS, with no capture file or with the eleven not all accepted) → the five sentinel lines BY NAME: `PRE_F=11`, `CHANGES_F=11`, `EXCL_F=11`, `ACC_POST=25`, `IMPL_POST=394` (a `CHECK` abort prints a line beginning `Runtime error near line 4: CHECK constraint failed: x=11` and writes nothing — HALT on that MESSAGE; never read the exit code through a pipe, it is the last command's). **Read-back on a FRESH read-only connection** (durability is proven post-commit, never by in-transaction sentinels): the eleven rows `implemented|codify|ceo|<stamp>` with every stamp outside the two vintages; `accepted` 25; `implemented` 394; the capture file has 590 lines and 36 rows containing `|accepted|codify|`. Copy the capture into the worktree: `mkdir -p knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16 && cp /tmp/g2ptw30e/flip-capture.txt knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt` (A5 commits it; QA verifies it). Commit the SQL in governance, the message on stdin: `git -C "$GOV" add governance/knowledge/development/g2ptw30e-flip.sql && git -C "$GOV" commit -F - -- governance/knowledge/development/g2ptw30e-flip.sql <<'MSG'` with the subject `[<id>] gate2-pt-w30-e: the flip SQL (447, 456, 459, 497, 517, 527, 546, 573, 576, 581, 582 -> implemented)`.
>
> **A5 — dev log + commit by explicit pathspec, the pre-check chained first.** `knowledge/development/dev-log-gate2-pt-w30-e-2026-09-16.md`: the A0 determination, A1's pins, A2's lines and the three refusals, A3's counts and the governance commit hash, A4's backup lines, sentinels, read-back and the SQL commit hash. Then, as ONE command: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/development/dev-log-gate2-pt-w30-e-2026-09-16.md knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt && git commit -F - -- knowledge/development/dev-log-gate2-pt-w30-e-2026-09-16.md knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt <<'MSG'` with the subject `[<id>] gate2-pt-w30-e: PT v4.117 + flip landed (dev log, flip capture)` and `MSG` closing it. `git status --short` → empty. STOP.
>
> **Deposits:**
> - `knowledge/development/dev-log-gate2-pt-w30-e-2026-09-16.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30e-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt`
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-pt-w30-e-2026-09-16.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30e-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt`

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message.** You are the bellows QA agent. `cd "$(git rev-parse --show-toplevel)"`; `GOV=/Users/marklehn/Developer/eluvian-governance`; `B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-e-2026-09-15.py"`; `DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db`; `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python` — re-derive per compound. Read-only beyond the three evidence files; the commit's message goes on stdin, as STEP 1's header states.
>
> **(A) Rule 20 self-check** — the canonical block at the path the dispatcher's mandate names (this machine's `RULE_20_SELF_CHECK_BLOCK.md`; quote the path you were handed). Run with:
> - `plan_slug`: `gate2-pt-w30-e-2026-09-16`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16"`
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-pt-w30-e.txt", "flip-capture.txt"]` (the capture committed by DEV in A5; QA verifies it is the committed file — `git log -1 --format=%s -- knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt` carries the plan's id)
>
> **(B) Items — raw output appended to `probes-raw.txt`** (⛔ the raw file carries NO summary-shaped line: P7 is named as "P7", never restated with its numbers; the suite's summary line stays in the suite file; no commit log is pasted beyond Item 4's path-scoped probe — MUST-PRESERVE's raw-file rule, thread 363):
> - **Item 1 — the live template:** `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` (the `[<id>]` commit); every Task C count re-run (30 probes); `wc -l` 2639; `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30e-flip.sql` → EMPTY.
> - **Item 2 — the builder, by a second pair of hands (byte-identity):** `rm -rf /tmp/g2ptw30e-qa; mkdir -p /tmp/g2ptw30e-qa; C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show "$C^:PLANNER_TEMPLATE.md" > /tmp/g2ptw30e-qa/PT-pre.md; python3 "$B" /tmp/g2ptw30e-qa/PT-pre.md /tmp/g2ptw30e-qa/PT-out.md; echo "builder_exit=$?"; cmp /tmp/g2ptw30e-qa/PT-out.md "$GOV/PLANNER_TEMPLATE.md" && echo BYTE_IDENTICAL` → the `BUILT:` line, `builder_exit=0`, `BYTE_IDENTICAL`; P4 with the builder's own commit; the three refusals reproduced.
> - **Item 3 — the flip, read back on a FRESH read-only connection:** the eleven rows `implemented|codify|ceo|<stamp outside the two vintages>`; `accepted` 25 — and the twenty-five by count, each still at one of the two vintages (`SELECT COUNT(*) … WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z')` → 25); `implemented` 394; `wc -l knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/flip-capture.txt` → 590; the capture's eleven rows for the flipped ids read `accepted|codify` (the pre-flip state, captured before the UPDATE).
> - **Item 4 — the full-suite file:** `"$BPY" -m pytest tests -q -p no:cacheprovider > knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/full-suite-gate2-pt-w30-e.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/full-suite-gate2-pt-w30-e.txt` → `exit=0` and a summary line with NO `failed`/`error` count — P7's counts are the expectation, but the gate (`_gate_qa_test_result`) reads FAILURES against `known_failures`, never the pass count: if bellows `main` moved since P7 was measured (`git log --format=%h <P7's commit>..HEAD -- tests` non-empty — P7's commit is the anchor — `4431bc3a` at the derivation, the newest QA's; re-read at the freeze), the drift is stated in `probes-raw.txt` under Item 4 as that probe's command and the HASHES it printed (`--format=%h`, never `--oneline` — a bellows QA commit's subject carries `N passed`, the summary shape the raw file must not hold), and nothing broader; the receipt's suite row stays the FILE and `exit=0`, never a note: a drift note reaches for `not run`, `skipped` or `pending`, and the canonical block fails a positive row carrying any of them. A different pass count with zero failures is not a halt. ⛔ A suite file WITHOUT a summary line (a crashed run — a traceback, a killed process) is a HALT, never a re-run note and never a second attempt written over it: with the named suite file summary-less the gate falls to the next `.txt` carrying one, and the raw-file rule above is the only thing standing between that and a stale count (the EXECUTION seat measured both configurations, S2-1).
>
> **(C) The report** `qa-receipt.md`: the verification table — status cells carry the glyph only, and NO positive row quotes a probe token or an output line carrying a Rule 20 hedging keyword (`hedging_keywords` in the canonical block): this plan has TWO such sources — the suite summary line and the agent's OWN prose (a drift note, a re-run note: `not run`, `skipped` and `pending` are the words such a note reaches for, and the block fails a positive row carrying any of its `hedging_keywords` whoever wrote it) — so the suite row names the FILE and `exit=0` with the drift left in `probes-raw.txt`, and Item 1's row reads `30/30 counts as pinned in A3, see probes-raw.txt` (the raw file may carry the tokens; the receipt's positive rows may not — a row quoting any of them FAILS the self-check) — the follow-ups (the two tranches after this one; the Planner pushes governance and projects the markers; thread 336 at the keyboard), the Rule 20 stdout APPENDED. Commit, as ONE command with the pre-check chained and the message on stdin: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/ && git commit -F - -- knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/ <<'MSG'` with the subject `[<id>] QA: PT v4.117 rebuilt byte-identical from the pre-edit blob; flip read back (11 implemented, 25 accepted remain)` and `MSG` closing it. STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/full-suite-gate2-pt-w30-e.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-e-2026-09-16/full-suite-gate2-pt-w30-e.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
