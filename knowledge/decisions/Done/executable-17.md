# Bellows — BACKLOG → FORWARD Register Cutover (Reporting Phase 3, Stage 4 of 5)
**Date:** 2026-06-12 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (Doc Analyst: registers) → Step 2 (Doc Analyst: trim) → Step 3 (QA) | **pause_for_verdict:** always | **qa_steps:** 3 | **Test Scope:** targeted

**Test Scope justification:** targeted — markdown register migration plus one file deletion; no production code, no tests.

## Context (Rule 27)
This executable implements diagnostic 13 — Stage 4 of the staged cutover (forge ✓ → anvil ✓ → invoice-pulse ✓/in-flight → bellows → shop_backlog), per the CEO fork verdicts in processed-verdict-13-step-1: Option (a) archive-freeze + delete; staged per-project; governance edits separate; every-wrap reconciliation. The stands-alone gate's mechanical checks were discharged at Stage 1 (`forge/knowledge/qa/evidence/forge-backlog-cutover-2026-06-12/`); the human half (CEO review of FORWARD.md before trim) repeats here via the step-1 pause. The diagnostic findings at `/Users/marklehn/Developer/GitHub/governance/knowledge/research/reporting-phase-3-pre-cutover-unknowns-2026-06-12.md` are AUTHORITATIVE for the FORWARD.md format and migration rule (Section 4); its bellows survey (Section 1: 284 lines, ~20 open, ~14 closed — approximate) is superseded by your per-entry table. BELLOWS-SPECIFIC SHAPE: `## Open` (file line ~7) and `## Closed` (~line 69) sections; entries are top-level `- ` bullets, many verbose and bold-prefixed (`- **Added YYYY-MM-DD:**` or `- Added YYYY-MM-DD:`); several Open-section entries carry inline `**FIX:** shipped` / `SHIPPED` annotations (closed-inline) or shipped-fix-plus-residual text — classify per entry, never by section position alone for the Open section. STAGE-3 PRECEDENT (CEO disposition, plan 16): entries whose text reads OPEN but which sit under `## Closed` without a closure annotation are misfiled-open — count them open-class and migrate them, recording the misfiling in your table (do not silently archive them as closed). NOTE: this plan migrates the BACKLOG of the project whose daemon is executing it — knowledge-file edits only, same class as plans 4/5/11 precedent; do not touch any `.py` file.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Documentation Analyst (registers)

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DOCUMENTATION_ANALYST.md`, then the diagnostic findings Sections 1 and 4 (path in Context), then `knowledge/BACKLOG.md` in full.
>
> **Part A — per-entry classification table (dev log; QA's conservation baseline):** for EVERY top-level `- ` bullet in BOTH sections: date + short title | section | classification: `truly-open` / `closed` / `closed-inline` (Open-section entry with a shipped/FIX-shipped annotation and no residual) / `shipped-with-open-residual` (migrates as its residual) / `misfiled-open` (Closed-section entry with no closure annotation that reads open — per the Stage-3 CEO precedent in Context) | one-line justification quoting the deciding marker. Record totals and any divergence from the diagnostic's ~20/~14 survey with justification.
>
> **Part B — create the two register files (BACKLOG.md untouched in this step):**
> - `knowledge/FORWARD.md` — diagnostic Section 4 format VERBATIM (title `# Bellows — Forward Register`, the standing-queue preamble blockquote, the 6-column table `| # | Added | Item | Type | Plan-id link | Status |`). One row per open-class entry (truly-open + residuals + misfiled-open), chronological, numbered from 1. Item text: ONE line distilled to the actionable core with `(context in BACKLOG-ARCHIVE.md)` appended where verbose context matters. Type: `deferred-work` unless the entry parks a CEO decision → `ceo-decision-fork`. Plan-id link: use the integer id when an entry explicitly references one (e.g., observations citing plan 6 or plan 7 use `—` unless a PLAN was authored for the work itself).
> - `knowledge/BACKLOG-ARCHIVE.md` — the full current BACKLOG.md content byte-identical, prepended with exactly: `# Bellows — BACKLOG Archive (frozen 2026-06-12)` + blank line + `> Read-only freeze of BACKLOG.md at the Reporting Phase 3 cutover (implements diagnostic 13, CEO Option (a)). The forward-looking queue now lives in knowledge/FORWARD.md; id-era closed history is generated reporting (forge reconstruction reports over bellows/lifecycle.db). Do not edit this file.` + blank line + `---` + blank line + original content unmodified.
>
> Write the dev log (Part A table + register confirmations + `git status` showing BACKLOG.md untouched) to `bellows/knowledge/development/bellows-backlog-cutover-step1-dev-log-2026-06-12.md`. Use `with open()` for all file writes; no heredocs. **Deposits:**
> - `bellows/knowledge/FORWARD.md`
> - `bellows/knowledge/BACKLOG-ARCHIVE.md`
> - `bellows/knowledge/development/bellows-backlog-cutover-step1-dev-log-2026-06-12.md`

---
---

## STEP 2 — Bellows Documentation Analyst (trim)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. The CEO continue-verdict that resumed you IS the human gate review of FORWARD.md. This step does exactly one thing: `git rm knowledge/BACKLOG.md` and commit with message `chore: BACKLOG.md retired — FORWARD.md + BACKLOG-ARCHIVE.md per Phase 3 cutover (implements diagnostic 13) [<your plan id>]`. Confirm with `git status` (clean) and `ls knowledge/ | grep -i backlog` (only BACKLOG-ARCHIVE.md remains). Append a 3-line step-2 section (deletion commit SHA + both confirmations) to the step-1 dev log using `with open(..., "a")`. **Deposits:**
> - `bellows/knowledge/development/bellows-backlog-cutover-step1-dev-log-2026-06-12.md` (appended)

---
---

## STEP 3 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, the diagnostic findings Sections 1 and 4, and both register files. **Verify, each with executed evidence (files into `knowledge/qa/evidence/bellows-backlog-cutover-2026-06-12/`):** (1) **Format conformance** — FORWARD.md matches the diagnostic Section 4 spec (title, preamble blockquote, exact 6-column header); output to `forward_format.txt`. (2) **Entry conservation** — independently classify every top-level bullet of the archived BACKLOG body into the same five classes (including misfiled-open per the Stage-3 precedent); your open-class count must equal the FORWARD.md row count, every open-class entry must appear, and no closed/closed-inline entry may have leaked in; record your independent table and the comparison; output to `entry_conservation.txt`. (3) **Archive integrity** — archive body below the frozen-header block byte-identical to the deleted BACKLOG.md (`git show <pre-deletion-ref>:knowledge/BACKLOG.md` diffed against the extracted body; exact commands recorded); output to `archive_integrity.txt`. (4) **Trim landed** — `knowledge/BACKLOG.md` absent AND the deletion commit carries the `[<plan id>]` tag; output to `trim_landed.txt`. If ANY check fails: fix in place only for trivial formatting; for classification disputes, halt and report to CEO.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `bellows-backlog-cutover-2026-06-12`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/bellows-backlog-cutover-qa-report-2026-06-12.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/bellows-backlog-cutover-2026-06-12/`; `required_evidence_files`: `[forward_format.txt, entry_conservation.txt, archive_integrity.txt, trim_landed.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table (item | method | evidence file | PASS/FAIL) and the Rule 20 banner block to `bellows/knowledge/qa/bellows-backlog-cutover-qa-report-2026-06-12.md`. Per Rule 8, also prepend a short dated entry to `PROJECT_STATUS.md` if the bellows repo has one (worktree; if absent, note that in the QA report instead). Use `with open()` for deposits; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/bellows-backlog-cutover-qa-report-2026-06-12.md`
> - `bellows/knowledge/qa/evidence/bellows-backlog-cutover-2026-06-12/` (four evidence files per Rule 20 self-check)
