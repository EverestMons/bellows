# PLANNER_TEMPLATE — Parser Self-Trip + Session-Wrap Hygiene (v4.38)
**Date:** 2026-05-11 | **Tier:** small | **Test Scope:** targeted | **Execution:** Step 1 (BELLOWS_DOC) → Step 2 (BELLOWS_QA) | **pause_for_verdict:** after_step_1 | **Total Steps:** 2

## Background

Bundle two PLANNER_TEMPLATE.md governance additions for v4.37 → v4.38:
1. **Bellows-side parser fix-plan self-trip warning** (Restart Discipline subsection) — closes the 2026-05-11 BACKLOG entry on fix-plans tripping their own bug. Two reproductions today (`executable-fence-strip` commit `4d57fd3`, `executable-step-header-line-anchor` commit `0fab609`) both hit gate failures on Step 2; recommended fix shapes were governance-only (mitigation 1 + 4).
2. **Session-wrap git hygiene** (Session Wrap subsection) — closes the 2026-05-06 (rewritten 2026-05-10) BACKLOG entry on plan lifecycle file accumulation. Today's cleanup commit `8cadade` captured 213 accumulated files across 2-3 weeks; codifying the discipline prevents recurrence.

Two Lessons Learned rows appended documenting both patterns.

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation ("ok") before proceeding to Step 2.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

---
---

## STEP 1 — BELLOWS_DOC

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11.md", "bellows/knowledge/decisions/in-progress-executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11.md")`.
>
> You are the Bellows Documentation Agent. Skip specialist file and glossary reads — this is a governance edit task. **Task:** Make four anchored edits to `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md` in one pass: (1) version bump 4.37 → 4.38, (2) add a Bellows-side parser fix-plan self-trip warning to the Restart Discipline subsection, (3) extend the Session Wrap (end of conversation) subsection with a git hygiene step for plan lifecycle files, (4) append two Lessons Learned rows documenting both patterns. Each edit uses `Filesystem:edit_file` with a verbatim `oldText` anchor copied from the current file state via `Filesystem:read_text_file` BEFORE writing each edit. **Edit 1 — Version bump.** Anchor on the literal two-line block beginning `**Version:** 4.37` and ending `**Last Updated:** 2026-05-11 (v4.37)`. Replace with `**Version:** 4.38` followed by `**Last Updated:** 2026-05-11 (v4.38)`. **Edit 2 — Parser self-trip warning.** Locate the `### Restart Discipline` subsection (inside the Bellows Execution Model section, near the end of the section). The subsection currently has two paragraphs. Append a third paragraph after the existing second paragraph (which ends with "the restart itself is the mandatory step.") with this content: a new paragraph explaining that Bellows-side fix-plans modifying a plan-text parser (e.g., `_extract_step_text`, `_gate_is_qa_step`, `extract_total_steps`, `_extract_step_text_from_plan`, `strip_fenced_code_blocks`) will trip the bug they fix during their own execution because the running daemon executes pre-fix code through the entire fix-plan's lifecycle. The paragraph must state: the failure manifests as a gate trip (typically `rule_20_self_check` or `deposit_exists`) on the QA step; the override is correct (the fix is verifiably good); the Planner pre-documents the override pattern in the plan header so the CEO knows what to expect at QA-step gate failure; and the bait-laden canary pattern (see LESSONS.md 2026-05-11) verifies the fix loaded after restart. The new paragraph should be roughly 4-6 sentences, prose form, matching the existing paragraphs' tone. Anchor the `oldText` on the existing closing sentence of paragraph 2 (`After restarting Bellows following a code change, depositing a small canary plan is a recommended practice to confirm the new code is loaded. The canary pattern — a trivial plan with characteristics that exercise the changed code path — was established during Phase 3b/3c step-state-resume verification and has proven effective at catching stale-code scenarios before real plans encounter them. This is a recommendation, not a rule; the restart itself is the mandatory step.`); the `newText` is that same paragraph followed by `\n\n` and the new paragraph. **Edit 3 — Session Wrap git hygiene.** Locate the `### Session Wrap (end of conversation)` subsection (under Core Responsibilities, near the top of the file). The subsection currently has three bullets. Append a fourth bullet after the existing third bullet `- Surface pending items: incomplete work, deferred decisions, unexecuted plans`. The new bullet must state: `- Stage and commit accumulated Bellows lifecycle artifacts before session close. Plan-file renames (in-progress-* → verdict-pending-* → Done/), consumed verdict-request files moved to archived/ or resolved/, and processed-verdict-* files in resolved/ are Bellows-side shutil.move operations with no associated git commit and accumulate as untracked changes until session-wrap explicitly stages them. Session-wrap commit pattern: \`git add -A bellows/knowledge/decisions/Done/ bellows/verdicts/\` followed by a chore commit. Applies cross-project — any project with a Bellows-watched decisions/ directory should be checked at session close.` Anchor on the verbatim existing third bullet line. **Edit 4 — Lessons Learned rows.** Locate the Lessons Learned table near the end of the file (search for the table containing rows dated 2026-05-11). Identify the last existing row, which begins with `| 2026-05-11 | Pre-executable scan-for-contradictions caught a stale rule`. Append two new rows after that row, each a single-line markdown table row matching the existing 2-column schema (`| Date | Lesson |`). Row A (Bellows parser self-trip): date `2026-05-11`, content stating that Bellows-side fix-plans modifying plan-text parsers (`_extract_step_text`, `_gate_is_qa_step`, `extract_total_steps`, `_extract_step_text_from_plan`, `strip_fenced_code_blocks`) will trip the bug they fix during their own QA step because the running daemon executes pre-fix code through the plan's lifecycle; both reproductions today (`executable-fence-strip-plan-text-parsers-2026-05-11` commit `4d57fd3` and `executable-step-header-line-anchor-2026-05-11` commit `0fab609`) hit identical gate failures on Step 2 followed by CEO override; the new Restart Discipline paragraph in v4.38 documents the override pattern; meta-lesson is that when a fix-plan's correctness depends on code that does not load until after the plan completes, the gate failure at QA is structurally inevitable, not a defect. Row B (session-wrap hygiene): date `2026-05-11`, content stating that plan lifecycle file moves (in-progress-* renames, Done/ moves, verdict-request consumption) are Bellows-side filesystem operations with no associated git commit and accumulate as untracked changes; today's cleanup commit `8cadade` captured 213 lifecycle files accumulated over 2-3 weeks; meta-lesson is that infrastructure operations producing filesystem state changes need an explicit commit pattern in the session-wrap discipline (Session Wrap bullet 4 in v4.38) rather than implicit accumulation that grows unboundedly. Anchor on the verbatim text of the last existing row (the full row line including trailing pipe). **Constraints.** Use `Filesystem:edit_file` for every edit (not Python file I/O). Use exact verbatim `oldText` anchors copied from the current file state via `Filesystem:read_text_file` BEFORE writing each edit. Do NOT modify any other content in PLANNER_TEMPLATE.md. Do NOT change the structure of the Lessons Learned table headers. **Commits.** Two commits per Rule 8 split-commit pattern for governance-root edits. Commit 1 (governance-root): `cd /Users/marklehn/Desktop/GitHub/ && git add PLANNER_TEMPLATE.md && git commit -m "docs(planner): v4.38 — parser self-trip warning, session-wrap hygiene"`. Commit 2 (bellows dev log): `cd /Users/marklehn/Desktop/GitHub/bellows/ && git add knowledge/development/planner-template-parser-self-trip-and-session-wrap-2026-05-11.md && git commit -m "docs: dev log for PLANNER_TEMPLATE v4.38"`. **Dev log content:** Output Receipt with sections: Files Modified (Governance), Files Created (Knowledge), Commits (2 SHAs in order), Edits Verified (4 edits with one-line confirmation each — version bump, parser self-trip paragraph, session-wrap bullet, two Lessons rows), Constraints Verified (no other content modified, table structure preserved), Status: Complete. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`
> - `bellows/knowledge/development/planner-template-parser-self-trip-and-session-wrap-2026-05-11.md`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — BELLOWS_QA

---

> **Before starting, read `bellows/knowledge/development/planner-template-parser-self-trip-and-session-wrap-2026-05-11.md` and check the Output Receipt status field. If status is not Complete, stop and report the issue to the CEO before proceeding.**
>
> You are the Bellows QA Analyst. Skip specialist file and glossary reads — this is a mechanical verification task. **FIRST — Deliverable Verification.** Read the Step 1 Output Receipt "Files Modified (Governance)" and "Edits Verified" sections. For each of the 4 edits, verify it landed in `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md` via grep. Produce a verification table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. Required deliverable checks: (1) Version bump: `grep -n "Version:" PLANNER_TEMPLATE.md` returns `**Version:** 4.38`, deposit grep output to `evidence/version_bump.txt`; (2) Last-updated bump: `grep -n "Last Updated:" PLANNER_TEMPLATE.md` returns `**Last Updated:** 2026-05-11 (v4.38)`, deposit grep output to `evidence/last_updated.txt`; (3) Parser self-trip paragraph: grep for the key phrase that appears in the new paragraph (e.g., `grep -n "fix-plan's correctness depends on code that does not load" PLANNER_TEMPLATE.md` or another unique phrase from the new paragraph), deposit grep output to `evidence/parser_self_trip.txt`; (4) Session-wrap bullet: grep for the key phrase from the new bullet (e.g., `grep -n "Stage and commit accumulated Bellows lifecycle" PLANNER_TEMPLATE.md`), deposit grep output to `evidence/session_wrap_bullet.txt`; (5) Lessons row A (parser self-trip): grep for the unique phrase from Row A (e.g., `grep -n "structurally inevitable, not a defect" PLANNER_TEMPLATE.md`), deposit grep output to `evidence/lessons_row_a.txt`; (6) Lessons row B (session-wrap): grep for the unique phrase from Row B (e.g., `grep -n "8cadade" PLANNER_TEMPLATE.md` or another unique phrase including the commit SHA), deposit grep output to `evidence/lessons_row_b.txt`; (7) Table structure preserved: `grep -c "^| 20" PLANNER_TEMPLATE.md` returns a count matching baseline + 2 new rows, deposit grep output to `evidence/lessons_table_count.txt`. If any deliverable is ❌, attempt to fix (re-edit, re-commit) before proceeding. If unfixable, stop and report to CEO — do NOT authorize close. **Commit log verification.** Verify both commits landed in correct repos: `cd /Users/marklehn/Desktop/GitHub/ && git --no-pager log --oneline -2 -- PLANNER_TEMPLATE.md` should show the v4.38 commit at HEAD; deposit output to `evidence/governance_commit.txt`. `cd /Users/marklehn/Desktop/GitHub/bellows/ && git --no-pager log --oneline -2 -- knowledge/development/planner-template-parser-self-trip-and-session-wrap-2026-05-11.md` should show the dev log commit at HEAD; deposit output to `evidence/dev_log_commit.txt`. **Targeted test run.** No code changes in this plan; test scope is `targeted` and the targeted scope is "none" — no test execution required. Document this in the QA report under "Targeted Test Run" with the literal text `No code changes; test execution skipped per targeted scope.`. **Run Rule 20 self-check.** Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `executable-planner-template-parser-self-trip-and-session-wrap-2026-05-11`; `qa_report_path`: absolute path to QA report below; `evidence_dir`: `bellows/knowledge/qa/evidence/planner-template-parser-self-trip-and-session-wrap-2026-05-11/` (absolute path); `required_evidence_files`: `["version_bump.txt", "last_updated.txt", "parser_self_trip.txt", "session_wrap_bullet.txt", "lessons_row_a.txt", "lessons_row_b.txt", "lessons_table_count.txt", "governance_commit.txt", "dev_log_commit.txt"]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, do not proceed — halt and report to CEO. **Deposit QA report** to `bellows/knowledge/qa/planner-template-parser-self-trip-and-session-wrap-qa-2026-05-11.md`. Include the verification table, evidence file references, commit log verification output, and the literal Rule 20 self-check stdout. **Final.** Update PROJECT_STATUS.md — add a completed milestone entry summarizing the v4.38 changes (version bump, parser self-trip paragraph in Restart Discipline, session-wrap git hygiene bullet, two Lessons rows). Commit with descriptive message `docs: PROJECT_STATUS milestone for PLANNER_TEMPLATE v4.38`. **STOP.** Do NOT move this plan to Done/. Per the disable-auto-close model, the Planner performs the terminal move after Rule 22 verification passes. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. Final operation order: deliverable verification → QA deposit → PROJECT_STATUS.md update → feedback append → final commit.
>
> **Deposits:**
> - `bellows/knowledge/qa/planner-template-parser-self-trip-and-session-wrap-qa-2026-05-11.md`
> - `bellows/knowledge/qa/evidence/planner-template-parser-self-trip-and-session-wrap-2026-05-11/` (multiple files per Rule 20 self-check)
> - `bellows/PROJECT_STATUS.md`
