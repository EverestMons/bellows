# Bellows — Line-Start Anchor for `## STEP N` Parsers
**Date:** 2026-05-11 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation. Test Scope: targeted — fix touches 2 files (`bellows/gates.py`, `bellows/verdict.py`) with regex anchor changes across 3 functions; targeted tests for affected parsers are sufficient. Live integration smoke test is OUT OF SCOPE per the 2026-04-19 Lesson — Bellows does not hot-reload; CEO performs canary after daemon restart.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/executable-step-header-line-anchor-2026-05-11.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-step-header-line-anchor-2026-05-11.md", "bellows/knowledge/decisions/in-progress-executable-step-header-line-anchor-2026-05-11.md")`. You are the Bellows Developer. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first. Skip the domain glossary read — this is a code-tracing fix task. **Context.** The diagnostic at `bellows/knowledge/research/rule-20-gate-false-positive-root-cause-2026-05-11.md` (Q5) identified 3 parsers using `## STEP N` regex without line-start anchor: (1) `_extract_step_text` at `gates.py:259-261` uses `r"## STEP {step_number}\b.*?(?=\n## STEP |\Z)"` with `re.DOTALL` only, (2) `_extract_step_text_from_plan` at `verdict.py:39-41` uses the same pattern (documented duplicate of gates.py per circular-import avoidance), (3) `_gate_is_qa_step` at `gates.py:352-356` uses `r"## STEP {step_number}\b[^\n]*"` with no flags. Without line-start anchor, `re.search` matches the first occurrence of `## STEP N` anywhere in the plan text — including inline single-backtick code references in plan prose. The 2026-05-11 false positive on `_gate_rule_20_self_check` was caused by inline `` `## STEP 2` `` references in the fence-strip plan's prose causing `_extract_step_text` to extract Step 1's body instead of Step 2's body. Companion case in `extract_total_steps` at `bellows.py:109` already uses `^` + `re.MULTILINE` correctly — this fix aligns the three unanchored parsers with that pattern. **Implementation.** **Part A — Patch `_extract_step_text` in `gates.py`.** Locate the function at `gates.py:253-262`. Two changes: (i) change the regex pattern from `rf"## STEP {step_number}\b.*?(?=\n## STEP |\Z)"` to `rf"^## STEP {step_number}\b.*?(?=^## STEP |\Z)"` — add `^` before `## STEP` in both the main pattern AND the lookahead, (ii) add `re.MULTILINE` to the flags so `^` anchors to line-start. The existing `re.DOTALL` flag is preserved (still needed so `.` matches newlines for the body span). Final flag expression: `re.DOTALL | re.MULTILINE`. **Part B — Patch `_extract_step_text_from_plan` in `verdict.py`.** Locate the function at `verdict.py:39-41`. Apply the IDENTICAL changes as Part A — same pattern, same flags. This function is the documented duplicate of `_extract_step_text` per the circular-import avoidance pattern. The existing "keep in sync" comment must be preserved. **Part C — Patch `_gate_is_qa_step` in `gates.py`.** Locate the function at `gates.py:352-356`. Two changes: (i) change the regex pattern from `rf"## STEP {step_number}\b[^\n]*"` to `rf"^## STEP {step_number}\b[^\n]*"`, (ii) add `re.MULTILINE` flag (currently no flags). The pattern is intentionally line-scoped (`[^\n]*` not `.*?`) — this is correct for grabbing only the header line. **Part D — Tests.** Add 3 regression tests, each verifying the parser correctly ignores inline `## STEP N` references appearing before the real header. (1) `bellows/tests/test_gates.py` — add `test_extract_step_text_ignores_inline_code_references` near the existing `test_extract_step_text_ignores_in_fence_headers` test added in commit `4d57fd3`. Fixture: a plan-shaped string where Step 1 body contains inline `` `## STEP 2` `` references (single backticks, not fenced) followed by the real `## STEP 2 — Bellows QA` header at line-start. Assert the function returns the real Step 2's body (containing "Bellows QA"), not the inline-reference-bounded text. (2) `bellows/tests/test_gates.py` — add `test_gate_is_qa_step_ignores_inline_code_references` near the existing `test_gate_is_qa_step_ignores_in_fence_headers` test. Fixture: a plan-shaped string where Step 1 body contains inline `` `## STEP 2 — Bellows QA` `` (single backticks) followed by real `## STEP 2 — Developer` header at line-start. Assert `_gate_is_qa_step(plan_text, 2)` returns False (real step is Developer, not QA). (3) `bellows/tests/test_verdict.py` — add `test_extract_step_text_from_plan_ignores_inline_code_references` near the existing `test_extract_step_text_from_plan_ignores_in_fence_headers` test. Same fixture shape as test (1) but exercise `_extract_step_text_from_plan` in verdict.py. **Implementation constraints.** Do NOT modify any other parser regex. Do NOT change `extract_total_steps` — already correctly anchored. Do NOT modify the `strip_fenced_code_blocks` utility added by commit `4d57fd3` — this fix is complementary (inline-code variant of the same parser-vs-structure class). Do NOT deduplicate `_extract_step_text` and `_extract_step_text_from_plan` — keep the documented duplicate pattern. **Run tests.** Run `cd bellows && python3 -m pytest tests/test_gates.py tests/test_verdict.py -x` from the repo root. All new tests must pass. No regressions in existing tests. Deposit the full test output to evidence file (path declared in Deposits). **Commit.** Single commit with message `fix(parsers): anchor ## STEP N regex to line-start in 3 parsers (_extract_step_text, _extract_step_text_from_plan, _gate_is_qa_step)`. Body cites the diagnostic findings file path. **Deposit dev log.** Deposit a development log file at the path in Deposits documenting: exact LOC delta per file, test count delta, and any decisions made within specialist authority. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/step-header-line-anchor-2026-05-11.md`
> - `bellows/knowledge/qa/evidence/executable-step-header-line-anchor-2026-05-11/`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — Bellows QA

---

> **Before starting, read `bellows/knowledge/development/step-header-line-anchor-2026-05-11.md` and check the Output Receipt status field. If status is not Complete, stop and report the issue to the CEO before proceeding.** You are the Bellows QA agent. Read your specialist file at `bellows/agents/BELLOWS_QA.md` first. Skip the domain glossary read — this is a code-level QA task. **FIRST — Deliverable Verification.** Read the prior DEV step's Output Receipt "Files Created or Modified (Code)" list. For EVERY listed file: verify it exists on disk and contains the described change. Verification commands: (a) `grep -nE "\^## STEP" bellows/gates.py bellows/verdict.py` — must return matches showing the `^` anchor is present in all 3 patched functions. Note: backslash-escape the `^` if your shell requires it. (b) `grep -nE "re\.MULTILINE" bellows/gates.py bellows/verdict.py` — verify `re.MULTILINE` appears in the affected functions. (c) `grep -nE "def test_(extract_step_text_ignores_inline_code|gate_is_qa_step_ignores_inline_code|extract_step_text_from_plan_ignores_inline_code)" bellows/tests/test_gates.py bellows/tests/test_verdict.py` — verify all 3 new tests exist. (d) `git --no-pager log -1 --stat` — verify single commit, all expected files present in the diff. Produce a verification table: `| Deliverable | Expected | Status (✅/❌) | Evidence |` with one row per deliverable. If ANY item is ❌, attempt to fix (re-commit, re-create) before proceeding. If unfixable, stop and report to CEO. **Test verification.** Run the targeted test suite: `cd bellows && python3 -m pytest tests/test_gates.py tests/test_verdict.py -v 2>&1 | tee bellows/knowledge/qa/evidence/executable-step-header-line-anchor-2026-05-11/pytest_targeted.txt`. All 3 new tests must pass. No new failures in existing tests. **Behavioral regression check.** Re-run the diagnostic's Q5 REPL fixture against the patched code. Deposit output to evidence file `pytest_repl_inline_code_reference.txt`. Fixture: construct a plan-shaped string with Step 1 containing inline `` `## STEP 2` `` references followed by real `## STEP 2` at line-start. Call all three patched functions (`_extract_step_text`, `_extract_step_text_from_plan`, `_gate_is_qa_step`) with `step_number=2` and assert each returns the result corresponding to the REAL Step 2 (not the inline reference). Capture each call's input fixture, return value, and pass/fail classification. **Complementary class verification.** The fence-strip fix from commit `4d57fd3` patched the FENCED-code variant of the same parser-vs-structure bug class. This fix patches the INLINE-code variant. Verify they coexist correctly: run one additional REPL fixture with BOTH fenced AND inline `## STEP N` references in Step 1's body, plus the real Step 2 header. Assert all 3 patched parsers correctly skip both variants and return real Step 2's data. Deposit output to evidence file `pytest_repl_combined_inline_and_fenced.txt`. **No live integration smoke test.** Per the 2026-04-19 Lesson on Bellows hot-reload absence, do NOT dispatch a canary plan to verify live behavior in this QA step — the running Bellows daemon was started before this fix and would still run pre-fix code. The CEO performs the post-restart canary as a separate action. **QA report deposit.** Write the QA report at the path declared in Deposits. Structure: top-level Deliverable Verification table (Rule 17), Test Results section with summary + pre-existing failure note, Behavioral Regression Verification table (3 parsers × inline-reference fixture), Complementary Class Verification (combined fenced+inline fixture results), and Rule 20 Self-Check Results at the end. **Rule 20 Self-Check.** Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `executable-step-header-line-anchor-2026-05-11`, `qa_report_path`: `/Users/marklehn/Desktop/GitHub/bellows/knowledge/qa/step-header-line-anchor-qa-2026-05-11.md`, `evidence_dir`: `/Users/marklehn/Desktop/GitHub/bellows/knowledge/qa/evidence/executable-step-header-line-anchor-2026-05-11/`, `required_evidence_files`: `["pytest_targeted.txt", "pytest_repl_inline_code_reference.txt", "pytest_repl_combined_inline_and_fenced.txt"]`. Include the literal stdout of the block in the QA report. If FAILED, halt and report to CEO — do not proceed with PROJECT_STATUS update. **Final.** Update `bellows/PROJECT_STATUS.md` — add a completed milestone entry summarizing all changes (3 parsers anchored to line-start, 3 tests added, fence-strip-fix complement). Commit with descriptive message. **STOP.** Do NOT move this plan to Done. Per the disable-auto-close model, the Planner performs the terminal move after Rule 22 verification passes. Order of final operations (per Rule 23): QA report deposit → behavioral evidence files → Rule 20 self-check → PROJECT_STATUS.md update → feedback append → final commit. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/qa/step-header-line-anchor-qa-2026-05-11.md`
> - `bellows/knowledge/qa/evidence/executable-step-header-line-anchor-2026-05-11/`

---
