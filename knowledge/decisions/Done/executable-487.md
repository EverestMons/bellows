# governance — DRAFTING_CYCLE.md: cut/target/sweep passes + propagation-at-freeze (honing unit a)
**Date:** 2026-08-20 | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** none (governance doctrine edit; no gate side — see §6 discharge) | **Execution:** Step 1 (GOVERNANCE, in-place) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

## Context

**Unit (a)** of the drafting-cycle honing backlog — the DOCTRINE-only proposals from `governance/knowledge/research/drafting-cycle-honing-notes-2026-08-18.md`, codified into the root `DRAFTING_CYCLE.md`. The honing notes carry ten findings and nine labelled proposal-candidates; a NON-AUTHOR Gate-1 read (2026-08-20, this arc) routed them. Unit (a) is the six DRAFTING_CYCLE.md-surface proposals; PANEL_SEAT_TEMPLATE.md (P-6/P-7/P-8-seat-half → unit b) and the `plan_lint.py` F6 fix (unit c) are SEPARATE plans, not in this one.

**Six append-only bullets (append-only, NO mid-line edit — the low-risk Gate-2 pattern, v2.3/v2.5 precedent):**
- **A — P-2 (§2.7):** a TARGETED pass is legitimate only when declared not-a-walk and can never discharge the bar (the sanctioned form of §2.7's covers-not-targets rule — it bars mislabelling, not aim).
- **B — P-4-widened (§2.7):** the SWEEP pass — mechanical, non-bar-discharging, fired by the `pre-existing-yield=0 AND total did not fall` signal; runs `propagation_check.py`; carries the mandatory ⚠️ non-convergence guard (ACCEPT-REVISE condition from the Gate-1 read).
- **C — P-1 (§2.8):** the SUBTRACTIVE walk — a declared pass whose unit is deletion (the proactive form of §2.8's deletion-is-third-resolution), gated by §2.7's subtractive-trim diff-review.
- **D — P-3 (§2.8):** cut/target TRIGGERS — measurable signals (rising yield / region-folds-3× / record>instruction), a prompt not a quota.
- **E — P-8-§2.6-half (§2.6):** the last fold round before deposit has no executor → a scoped execution re-run over the final fold set, with the recursion bound stated (ACCEPT-REVISE condition).
- **F — P-9 (§5):** run `propagation_check.py` at the conformance freeze beside `plan_lint`, with the finds-restatement-never-correctness caveat.

**EXPLICITLY NOT ROUTED (ALREADY-CODIFIED, per the Gate-1 read — recorded so a later notes-vs-doctrine diff does not read them as a silent miss):** the notes' headline (the DONE-bar class test at DC §2 L42 + the falling-total line L46 — "bar is not a count test" is the notes' paraphrase, not a doctrine string), Finding 2's fold-landing check (`fold_check` mandated, §2.7 L144, v2.11), and **P-5 single-declaration (§2.7 L149 verbatim).** B references L149's existing rule rather than restating it — no re-codification.

**This is a governance IN-PLACE dispatch** ([[governance-in-place-dispatch]]): `DRAFTING_CYCLE.md` lives at the GitHub root under the top-level `.git` (no project `.git`), so the STEP uses ABSOLUTE operands, a triple-pin (pre-edit blob sha `1d5e11f`), `git add <abspath>` before a pathspec commit, and `git rev-parse` in the commit compound ([[phase-commits-assert-the-repo]]). NOT a worktree.

**CEO-authorized direct amendment — a declared §6 deviation** (closest-shape precedent **v2.13 / v2.12** — the two 2026-08-20 append-only section-end-bullet DC amendments; also v1.5/1.6/1.7/2.1): the proposals are Planner-authored honing lessons whose subject is the cycle itself; the CEO ratified the non-author Gate-1 read and routed unit (a) directly (2026-08-20), not the lessons-forge LESSONS→Gate-1→Gate-2 DB path. `proposed = 0, no batch` — this is NOT a lessons-forge codification, and no DB proposal row flips. The honing notes' own precondition — "doctrine changes go through §6, routed at Gate 1 by a NON-AUTHOR" — is genuinely satisfied by this arc's cold review (a real strengthening over the recent rows, which record CEO-routing but no non-author read); the author-disclosure the notes flag three times is answered by the non-author read, not waved past.

**§6 coordinate-doctrine-and-gate:** NO new gate edit. B/F name `bellows/scripts/propagation_check.py` (built + canary-validated 2026-08-19, exit 0/1/2), which is a **Planner-run tool at §5's conformance pass**, not a `plan_lint` structure — §5 carries no structure `plan_lint` parses (v1.8 History), so no §4 edit is forced and §4 stays in lockstep. Finding 6's `plan_lint` check-(f) change (last-lens-line → class-split) is a REAL code fix but is **unit (c), deferred** — declared here, not silent. Token sweep against `plan_lint.py`/`gates.py` recorded in QA (positive control: `Drafting Cycle` present; the six new tokens = 0 functional reads).

## Drafting Cycle
**Tier:** T2 — triggers fired: T-6 (governance surface: edits `DRAFTING_CYCLE.md` doctrine), T-8 (novel: first cut/target/sweep-pass amendment). Cold panel owed at close (§2.6). **Panel FORM: FULL** (Fork-C magnitude proxy fails proxy (ii) — these are hand-anchored doctrine prose inserts, not a committed-builder mechanized clone; siblings 483/476 both ran full form; "any proxy fail → full form"). cycle_check run from walk 2 onward (scratchpad draft → register asserts N/A; [[cycle-automation-depositor-arc]]).
**Walk 0 (context pin) — 5 measurements, REAL (2026-08-20):**
1. Newest same-class (clone-diff targets): **[483] `e53c00a` 2026-08-20 (v2.13, §2 auto-advance)** and **[476] `8ba803c` 2026-08-20 (v2.12, §3 stanza)** — both governance in-place DC amendments, append-only/additive prose. 483 = newest same-class; 476 = the FORM clone this draft is built from.
2. Anchors — **6 append-only section-END inserts**, each between a named last-sentence anchor (assert count==1) and the next heading, NO mid-line edit: §2.6 end (before `### 2.7` @141), §2.7 end ×2 (before `### 2.8` @188), §2.8 end ×2 (before the section's closing paragraph @204), §5 end (before `---` @284). Version line @5; History row PREPENDED (newest-first) after `## History` @302.
3. Occurrence counts (pre-edit): all six new tokens (`SUBTRACTIVE WALK`, `SWEEP PASS`, `TARGETED PASS`, `propagation_check.py`, `proposal-candidate P-`) = **0** → clean insertions. Each anchor sentence count==1 (asserted in-STEP).
4. Last writer: the section-end anchors were written by the v2.3/v2.5 codification batches (2026-08-11) + 476/483 (2026-08-20); all `implemented`/settled — no in-flight plan owns them.
5. Target sha: **`1d5e11f`** (`HEAD:DRAFTING_CYCLE.md` blob == working tree; porcelain clean), 119,576 bytes.
- Clone-diff vs newest (483/476): both additive/append-only prose; THIS plan is append-only too (NO subtractive doctrine edit — unlike 476's Rule C loosening), so no inherited subtractive-safety machinery is needed. No origin drift.
- Cold scout (T2, §2.0): **RUN (walk 1)** — fresh-context, integration-record-shaped, no brief-layering, author-verified. Verdict: **ANGLE RIGHT, no forcing finding.** All mechanical asserts PASS (sha `1d5e11f`; 6 anchors count==1; excluded trio present; `propagation_check.py` exists, exit 0/1/2). 4 refinements folded (SC-MED headline citation; SC-LOW1 non-author-convention; SC-LOW2 precedent lead; SC-LOW3 §5 lone-bullet = valid, no fix).
**Direction verdict (after walk 1): PROCEED.** The angle — codify the 6 doctrine-surface proposals as append-only section-end bullets — is right and shippable. No forcing finding: clone-origin (483/476 same-class, confirmed), edit-mechanism (append-only, all 6 anchors count==1 verified), and scope-premise (the 3 already-codified exclusions verified present) all hold. Walk-1 folds sharpen the STEP; they do not move the premise.
**Walk 1 — cold scout + 5 warm lenses, sequential folds applied.** ⚠️ **Class re-tag (CAPSTONE CAP-LOW): a fold that edits the plan's CONTEXT framing / DC record is RECORD-class (read by no executing agent); only a fold that changes the STEP's instructions is instruction-class** ([[reclassification-widens-what-proceeds]] discipline applied to the count itself).
- Weak spots:         w1 1 folded — instruction 0 / record 1 (W1 = SC-MED: the excluded-headline cite in CONTEXT L46 → L42+L46; dropped the "bar is not a count test" paraphrase presented as a doctrine quote — a fix to the plan's scope-framing prose = RECORD).
- Destruction:        w1 1 folded — instruction 1 / record 0 (D1: the STEP verification "eight additions" wording → precise change accounting; the agent runs this = INSTRUCTION).
- Vulnerabilities:    w1 1 folded — instruction 1 / record 0 (V1: the fold_check non-regression PROBE the agent runs used a backtick-escaped literal that false-greps 0 on a PRESENT line → plain `bellows/scripts/fold_check.py` = INSTRUCTION; [[grep-is-ugrep-use-F]]).
- Integration-record: w1 2 folded — instruction 0 / record 2 (I1 = SC-LOW1: §6 CONTEXT framing — non-author-Gate-1 restated as a strengthening; I2 = SC-LOW2: deviation precedent led with 2.13/2.12 — both edits to CONTEXT prose = RECORD).
- ACID:               w1 dry — single-step additive plan; the 6 bullets are mutually consistent (all three new pass-types explicitly do-NOT-discharge the bar), no cross-requirement contradiction.
**Walk 1 STATUS:** 5 folded — **instruction 2 / record 3** — not dry.
**Walk 2 — warm re-walk over the folded draft (all 5 lenses):**
- Weak spots:         w2 dry.
- Destruction:        w2 dry.
- Vulnerabilities:    w2 1 folded — instruction 1 / record 0 (V2 = a SIBLING SITE of walk-1's V1: Amendment F's verification PROBE token `at this conformance pass` was greped with escaped backticks — the SAME false-grep-0 defect, at the twin site walk 1 fixed only where noticed → swept to a backtick-free token = INSTRUCTION; [[fold-sweep-every-site]], and this is exactly the propagation/restatement class the plan itself codifies).
- Integration-record: w2 1 folded — instruction 0 / record 1 (I3 = gate-token discipline: the DC-block RECORD's `**Closing:** PENDING` marker false-fired cycle_check's `CLOSURE_RE` mid-cycle → `ESCALATE:claimed-close-unmet`; withheld the bold marker until the genuine close = RECORD, [[earn-the-clean-gate-dont-author-it]]. Diagnosed against `cycle_check.py:38` — a real gate false-positive, then re-run clean = CONTINUE).
- ACID:               w2 dry.
**Walk 2 STATUS:** 2 folded — **instruction 1 / record 1** — not dry. cycle_check (dogfood): **CONTINUE** (exit 0, after the closure-marker fix; escalated once on the mid-cycle marker, resolved).
**Walk 3 — warm re-walk + mechanical sweep (all 5 lenses over the whole artifact):**
- Weak spots:         w3 dry — the 6 bullets each carry their proposal + caution faithfully (re-checked vs honing-notes Findings 8/9/10 + P-1/P-2/P-3).
- Destruction:        w3 dry — every bullet is ADDITIVE; none loosens a live rule (B reinforces the L149 declare-once rule rather than weakening it). No subtractive doctrine edit.
- Vulnerabilities:    w3 dry — **P-4-style probe SWEEP run: every `grep -F` token in the STEP checked for the escaped-backtick false-grep-0 defect; ZERO remaining** (walk-2's V2 fix had no further siblings). propagation_check dogfooded on the draft = **exit 2 (N/A, no numbers-discipline table)** — a legitimate could-not-run, not a clean-or-dirty result; noted for Amendment F (exit 2 is a recordable §5 outcome).
- Integration-record: w3 dry — all 4 live anchors STILL count==1 (nothing shifted); the C-anchor closing-para count==1; cross-references sound (D→C above it in §2.8, D→A/B in §2.7; the class split D cites is really kept in §3 L218).
- ACID:               w3 dry — the set is consistent: all three new pass-types (targeted/sweep/subtractive) explicitly do-NOT-discharge the bar, so none contradicts §2's full-coverage bar; no cross-requirement conflict.
**Walk 3 STATUS:** **DRY — 0 folded, instruction 0 / record 0. §2 BAR MET at walk 3** (zero instruction-class across all five lenses; instruction-yield **2→1→0**). The close is NOT final — the T2 FULL cold panel (§2.6) is owed and is not waived by a bar-met walk. cycle_check (dogfood): **BAR_MET** (exit 0).
**Cold panel (§2.6, FULL form — meter opened at convene). Scout ran at walk 1; now DISCOVERY + EXECUTION (concurrent, independent reads of the frozen draft) → CAPSTONE last on the fold set.**
- **Seat DISCOVERY (weak-spots + destruction; evidence-attack + clone-diff briefs) — RUN, ~119k tokens.** Verdict: ANGLE SOUND, no forcing problem. Both mandatory carries CONFIRMED present (B's never-convergence/never-defer-panel guard; E's exactly-ONE-re-run bound). Clone-diff vs 476: the plan's History row SUPERSETS 476's (all 5 elements + the trio-exclusion + the non-author strengthening; nothing dropped). 3 folds: **DISC-MED** (E over-claimed "run by no downstream seat" — §2.6 L110's capstone ALREADY re-runs the freeze checklist over folds; the real gap is COMMAND re-execution → E rewritten), **DISC-LOW1** (A's tension with §2.7 L145's "narrowing the pass is not" closing clause named → folded a clarifying clause), **DISC-LOW2** (E's delta over the in-panel Execution brief stated → folded into the E rewrite). Non-findings cleared with citations: P-1/§2.8-L196 no conflict, P-2 no covers-not-targets loophole, all 3 already-codified exclusions present+correctly quoted, "no gate edit" correct (§4 refs no §5 structure; plan_lint.py clean; v1.8 History).
- **Seat EXECUTION (vulnerabilities + execution brief) — RUN, ~61k tokens.** Rehearsed the FULL edit+verification chain on a scratch git repo (base blob `1d5e11f` == live; **live file provably UNTOUCHED** end-to-end). ALL probes PASS: 4 anchor-uniqueness ==1, C closing-para ==1, 6 pre-edit-zero, 6 post-edit lead-tokens ==1 (incl. the reworded backtick-free `at this conformance pass, beside` = 1, and NO escaped-backtick probe survives), 3 non-regression, 6 placement, 2 ordering (A<B, C<D), markdown integrity (fences 2/2, headings 21/21, blockquote-prefix stripped on all 6), Version=2.14, History top-row new+2.13 beneath. **`git diff HEAD` = exactly 8 insertions / 1 deletion (sole `-` = old Version line)** — the plan's change-accounting claim proven by execution. 0 HIGH / 0 MED. 1 LOW (DISC-LOW-exec: propagation_check exit code is pipe-masked — capture `$?` before piping; NOT in this plan's chain, no plan change — a general tool-use note).
- **Seat CAPSTONE (integration-record + ACID + system brief; runs LAST on the fold set) — RUN, ~87k tokens.** Read the fold set as a unit. **1 HIGH + 1 dependent MED + 1 LOW — the fold set RE-OPENED the walk** (§2.6 panel-findings-re-open). **CAP-HIGH (the single most valuable catch of the whole cycle):** the DISCOVERY E-rewrite changed E's body lead from `…NO EXECUTOR` to `…has its COMMANDS run by no seat` but left E's OWN verification probe + placement assert (STEP) pointing at the deleted `NO EXECUTOR` literal → `grep -Fc == 1` would return 0 and FALSE-HALT a correct apply. This is [[fold-sweep-every-site]] violated INSIDE a plan codifying sweep discipline — the exact "a fold changes what another fold's probe counts" class §2.6 L110 names. Folded: both probe (L97) + placement (L98) swept to E's new lead literal. **CAP-MED** (dependent): the EXECUTION green-rehearsal attestation went stale when DISCOVERY rewrote E's body — resolved by the CAP-HIGH fix (probe now matches body again; re-verified below). **CAP-LOW:** walks 1–2 mis-tagged CONTEXT/DC-record framing folds as instruction-class → re-tagged (instruction-yield corrected 5→2→0 to the honest **2→1→0**; convergence unaffected — walk 3 dry either way). Clean confirmations: E-vs-§2.6-L110 delta faithful, A-clause vs L145 consistent, all 6 bullets do-not-discharge-the-bar coherent, D's triggers real (§2.8 L196 / §3 L218 / §2 L42), Closing marker correctly withheld, §6 bookkeeping complete, all 5 live-doctrine citations present.
**Cold panel COMPLETE (full form, 4 seats: scout + DISCOVERY + EXECUTION + CAPSTONE; ~350k tokens).** The panel earned its cost: it caught a self-false-halting verification probe (CAP-HIGH) that 3 warm walks + a full mechanical sweep missed, plus the E over-claim (DISC-MED) and the class-count drift (CAP-LOW) — none surfaced warm.
**Bar re-met post-panel:** the DISCOVERY/EXECUTION/CAPSTONE folds re-opened the walk; the CAP-HIGH/MED/LOW folds are all applied and re-verified (see the freeze below), the fold set is now instruction-dry, so the bar is re-met on the folded draft and the panel round is closed (the capstone covered the fold set, §2.6). No CAP fold re-opened another.
**Closing:** 3 warm walks (bar met walk 3, instruction-dry; instruction-yield 2→1→0) + FULL cold panel (4 seats, ~350k tokens, ~9 folds incl. the CAP-HIGH self-false-halting probe); bar re-met post-panel; §5 conformance + the freeze propagation run owed at deposit; closing-record re-read run (this block), dry; cycle done — deposit exactly once. ⚠️ Governance in-place + doctrine-amendment (register-writing) class → **HOLD FOR CEO** (not auto-deposit); the deposit is CEO-authorized.

---
---

## STEP 1 — GOVERNANCE DOCTRINE EDITOR (in-place)

---

> **Identity:** You amend `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` in place (governance root, top-level `.git`, NO worktree). Apply Amendments A–F (six append-only bullets) **and their §6-mandated bookkeeping (the Version-line bump + a PREPENDED History row — §6 requires both for any amendment); touch NO OTHER rule, section, or line.** Every insert is APPEND-ONLY at a section end — assert each anchor's occurrence is exactly 1, insert the new bullet between that anchor and the next heading; never a mid-line edit, never "the first match" ([[edit-anchor-is-not-a-probe]]).
>
> **⚠️ Triple-pin (governance in-place):** before ANY edit, assert `git -C /Users/marklehn/Developer/GitHub rev-parse HEAD:DRAFTING_CYCLE.md` == `1d5e11f...` (the pre-edit blob) AND `git -C /Users/marklehn/Developer/GitHub status --porcelain DRAFTING_CYCLE.md` is empty. If either fails, HALT — the file moved under you.
>
> **Amendment E — §2.6 (P-8 §2.6 half).** Anchor: the §2.6-ending sentence `A hand-rebuilt seat prompt is an unreviewed re-derivation of a safety-bearing artifact.` (assert count==1; it is the last bullet before `### 2.7`). Insert AFTER that bullet, BEFORE `### 2.7`, this bullet verbatim:
>   - ⚠️ **The last fold round before deposit has its COMMANDS run by no seat — a scoped execution RE-RUN closes it.** In the Fork-C full form (DISCOVERY → EXECUTION → CAPSTONE) the capstone covers the folds made in response to EXECUTION and CAPSTONE two ways — it re-runs the mechanical FREEZE CHECKLIST over the folded state and reads the fold set for INTERACTIONS (proposal 333, the §2.6 capstone-closure rule above) — but NEITHER re-EXECUTES the commands those last folds touched: *does this command still run* is a different question from either. Before deposit, RE-RUN — scratch-only, per the execution brief's copy discipline — every command, probe, or rehearsal the FINAL fold set touched, logging expected-vs-measured (this is the last-fold-round delta over the in-panel Execution brief, which runs BEFORE the capstone's folds exist). ⚠️ **Bound the recursion explicitly: exactly ONE re-run; a fold IT forces that changes a command re-runs only THAT command, never the whole set again.** Measured: a HIGH EXECUTION fold shipped an unexecutable inter-step value binding and was caught only because a later seat happened to run afterwards — under current packaging the last-folded commands otherwise reach deposit unexecuted. *(Proposal candidate P-8 §2.6-half, honing notes 2026-08-18 Finding 9; non-author Gate-1 read 2026-08-20.)*
>
> **Amendment A — §2.7 (P-2).** Anchor: the §2.7-ending sentence `Measured: six composed-probe false absences in one session, all on verification steps; five would have licensed a wrong action.` (assert count==1; last bullet before `### 2.8`). Insert AFTER that bullet, BEFORE `### 2.8`, this bullet verbatim:
>   - **A TARGETED pass is legitimate ONLY when declared not to be a walk — and it can never discharge the bar.** The covers-not-targets rule above forbids recording an aimed pass AS a walk; it does not forbid an aimed pass. The honest form NAMES its target and its reason, is recorded as a declared non-walk, and leaves bar-clearing to full coverage. This is the sanctioned way to spend a cheap pass on one region without the false choice of faking a walk or spending a full one — the aim is legitimate; the mislabelling is the only thing covers-not-targets bars. That rule's closing clause — "narrowing the pass is not [legitimate]" — governs narrowing a WALK (which must stay full-coverage), not the existence of a separately-declared non-walk that clears nothing. *(Proposal candidate P-2, honing notes 2026-08-18; non-author Gate-1 read 2026-08-20.)*
>
> **Amendment B — §2.7 (P-4-widened).** Insert IMMEDIATELY AFTER Amendment A's bullet (so both sit at the §2.7 end, A then B), BEFORE `### 2.8`, this bullet verbatim:
>   - **The SWEEP pass — a named, mechanical, non-bar-discharging pass fired by a signal.** Trigger (mechanical, both halves measured per walk): **pre-existing-class yield reaches 0 on a walk AND total yield did not fall** — at that point every finding is the cycle auditing its own repairs, a class a targeted `grep` closes completely and an untargeted walk closes slowly. Content: enumerate every quantity, ordering, and relation the artifact states more than once, drive each to a single declaration (the declare-a-set-ONCE rule above), and run `bellows/scripts/propagation_check.py` (restated-value / ordering / arithmetic detectors, exit 0/1/2). It is explicitly NOT a walk, discharges NOTHING, and the walk that FOLLOWS it is a first pass over the swept arrangement. ⚠️⚠️ **A sweep drains a MECHANICAL class and says NOTHING about correctness — the zero-pre-existing signal that fires it is a statement about what warm walks can still SEE, not about the artifact; it may NEVER read as convergence evidence and NEVER defer the cold panel.** Measured: a swept plan reported clean, and cold panel seat 1 then found five pre-existing defects, three ship-blocking. The check finds RESTATEMENT only — a value declared once and wrong everywhere, or a claim made once, is invisible to it. *(Proposal candidate P-4 (widened, honing notes Findings 8+10, 2026-08-18); non-author Gate-1 read 2026-08-20.)*
>
> **Amendment C — §2.8 (P-1).** Anchor: the §2.8-ending codified bullet `Measured: a non-ASCII-probe constraint opened at walk 1 was violated at two load-bearing pins written BEFORE it existed; twenty lens passes read past them.` (assert count==1; last bullet before the section's closing paragraph `The ledger makes the cross-requirement constraint set`). Insert AFTER that bullet, BEFORE the closing paragraph, this bullet verbatim:
>   - **The SUBTRACTIVE walk — a declared pass whose unit is DELETION, not folding.** The deletion-is-the-third-resolution rule above is REACTIVE (a region re-folded 3+ times prompts an ask); the subtractive walk is its PROACTIVE form — a declared pass that removes restatement and collapses regions that keep generating folds, measured by what the executing agent no longer has to READ. Precedent: repeated-folds-on-one-region-means-delete is already recorded, and collapsing one premise list closed four findings at once. ⚠️ **Deletion is the operation the cycle is WORST at reviewing** — a subtractive walk's cut MUST run §2.7's subtractive-trim discipline (the mandatory DIFF REVIEW: read the removed lines one by one, open every rule the cut cites), and on T2 the cut is itself a fold the later panel seats cover as a fold-set read. A subtractive walk does NOT discharge the bar by subtraction — a subsequent dry walk over the collapsed arrangement does. *(Proposal candidate P-1, honing notes 2026-08-18 — CEO proposal; non-author Gate-1 read 2026-08-20.)*
>
> **Amendment D — §2.8 (P-3).** Insert IMMEDIATELY AFTER Amendment C's bullet (C then D at the §2.8 end), BEFORE the closing paragraph, this bullet verbatim:
>   - **Cut-and-target TRIGGERS — measurable signals, not a judgment call.** The subtractive walk (above) and the targeted / sweep passes (§2.7) fire on signals already computable from the per-lens class split §3 keeps: (i) yield RISING rather than falling across consecutive passes; (ii) one region generating folds on three consecutive passes; (iii) record-class findings outnumbering instruction-class in a walk. These PROMPT a cut or a targeted pass. Consistent with the oscillation-is-a-signal-not-a-quota stance above, reaching a trigger is never by itself a mandate to cut — it is the prompt to consider one, and a cut still runs the subtractive-trim discipline. *(Proposal candidate P-3, honing notes 2026-08-18 — CEO proposal; non-author Gate-1 read 2026-08-20.)*
>
> **Amendment F — §5 (P-9).** Anchor: the §5-ending sentence `this rule fixes the FIRST run's timing, and the recorded exit code is the LAST run's.` (assert count==1; end of the single §5 paragraph, before the `---` closing §5). Insert AFTER that paragraph (as a new bullet), BEFORE the `---`, this bullet verbatim:
>   - **Run `bellows/scripts/propagation_check.py` at this conformance pass, beside `plan_lint`, and record its exit code with the linter's.** The propagation class — a count, ordering, or arithmetic relation restated across prose sites — re-enters on EVERY fold, including the panel's and the sweep's own, so a single sweep-time run (§2.7) is a snapshot; the freeze run catches what later folds reintroduced. ⚠️ **It finds RESTATEMENT, never correctness** — a value declared once and wrong everywhere, or a claim made only once, is invisible to it; the check is a floor under the propagation class, not evidence the plan is right, and it never substitutes for a lens or the cold panel. *(Proposal candidate P-9, honing notes 2026-08-18 Finding 10; non-author Gate-1 read 2026-08-20.)*
>
> **§6 bookkeeping (MANDATORY for any amendment).** Bump the **Version line (L5)**: `2.13 (2026-08-20)` → `2.14 (<dispatch date>)`. Then **PREPEND** a new row at the TOP of `## History` — immediately after the `## History` heading, ABOVE the `2.13` row (History is **newest-first**; "append" would misplace it). The row MUST follow the recent-2.x-row convention (the ~14 recent rows all carry these): (a) the units changed — §2.6 (P-8 half), §2.7 ×2 (P-2, P-4), §2.8 ×2 (P-1, P-3), §5 (P-9); (b) the version bump as a changelog ordinal; (c) the **declared §6 deviation** in the established recent-row form — "CEO-authorized direct amendment, a declared §6 deviation (2.13/2.12 precedent; `proposed`=0, no batch, no DB flip; not a lessons-forge codification)" — PLUS the strengthening this arc adds: **routed at Gate 1 by a NON-AUTHOR** (answering the notes' thrice-stated author-disclosure), stated as a new improvement, not a pre-existing convention; (d) the **ALREADY-CODIFIED trio explicitly excluded** (headline/§2 L42+L46, F2/§2.7 L144, P-5/§2.7 L149) so a later diff does not read them as missing; (e) the **§6 coordinate-doctrine-and-gate discharge** — NO new gate edit (`propagation_check` is Planner-run at §5, not a `plan_lint` structure), §4 in lockstep, Finding-6 `plan_lint` fix DEFERRED to unit (c); (f) an **`Inheritors:`** clause — every future cycle (the cut/target/sweep passes and the freeze propagation run).
>
> **Verification (§2.7 — prove each insert landed and nothing else moved):**
> - `git -C /Users/marklehn/Developer/GitHub diff HEAD -- DRAFTING_CYCLE.md` (working tree vs HEAD, NOT bare `git diff` — bare is vs-INDEX and shows nothing once staged) must show ONLY the expected changes — **six ADDED bullets** (E/A/B/C/D/F) + **one ADDED History row** (prepended) + **the Version-line REPLACEMENT** (one del / one add — the sole modified line) — and NO OTHER removed line (the amendment is otherwise append-only). ⚠️ Assert by CONTENT present, not by hunk-block count (adjacent §2.7 inserts A+B and §2.8 inserts C+D coalesce into fewer hunks).
> - Each new bullet's lead token count rose 0→≥1: `grep -Fc 'The SUBTRACTIVE walk'`, `grep -Fc 'The SWEEP pass'`, `grep -Fc 'A TARGETED pass is legitimate'`, `grep -Fc 'Cut-and-target TRIGGERS'`, `grep -Fc 'The last fold round before deposit has its COMMANDS run by no seat'`, and (backtick-free token — the propagation_check literal must NOT be greped single-quoted with escaped backticks, which false-greps 0, [[grep-is-ugrep-use-F]]) `grep -Fc 'at this conformance pass, beside'` — each == 1.
> - Placement asserts: `The SUBTRACTIVE walk` and `Cut-and-target TRIGGERS` fall inside §2.8 (between `### 2.8` and `## 3.`); `A TARGETED pass` and `The SWEEP pass` inside §2.7 (between `### 2.7` and `### 2.8`); `has its COMMANDS run by no seat` inside §2.6 (between `### 2.6` and `### 2.7`); the propagation-at-freeze bullet inside §5 (between `## 5.` and `## 6.`).
> - **Already-codified NON-regression (assert the three excluded rules are UNTOUCHED):** `grep -Fc` for the P-5 anchor `Declare a set ONCE` == 1 (unchanged), the fold_check anchor still present (probe the PLAIN literal bellows/scripts/fold_check.py, NOT a backtick-wrapped form — a backtick-escaped probe false-greps 0 on a present line, [[grep-is-ugrep-use-F]]), and the headline `A falling total finding-count is NOT the convergence signal` still present. This plan must NOT edit them.
> - **Doctrine-integrity:** markdown well-formed — no code fence opened, headings intact, every new bullet begins with `>   - ` in the plan but lands as a top-level `- ` doctrine bullet (strip the blockquote prefix on insert). Version line now reads `2.14`; `## History` top row is the new one, `2.13` directly beneath.
> - ⚠️ **No `plan_lint`/`cycle_check` run belongs here: neither script READS `DRAFTING_CYCLE.md` at runtime** (only WARN-citation strings name it — the v2.2 History measurement), so editing doctrine cannot change a gate's behavior; a lint run would be a null control. §3↔§4 lockstep is untouched (no §3/§4 edit).
>
> **Commit (governance root):** `cd /Users/marklehn/Developer/GitHub && git rev-parse --show-toplevel` (assert == the GitHub root), `git add /Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md`, then commit `docs(drafting-cycle): cut/target/sweep passes + propagation-at-freeze — DC v2.14 (honing unit a) [<id>]`.
>
> **Deposits:**
> - `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` (in-place, governance root — absolute operand)
