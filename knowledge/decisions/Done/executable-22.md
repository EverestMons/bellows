# Bellows — Daemon Single-Instance Guard + Project-Scoped Recovery + Age Guard + in_progress Write
**Date:** 2026-06-12 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 21. CEO fork verdicts (processed-verdict-21-step-1): DF1 = flock lockfile single-instance guard; DF2 = ONE executable shipping all layers together; DF3 = 5-minute age-guard window. The diagnostic findings at `knowledge/research/daemon-collision-recovery-race-2026-06-12.md` are AUTHORITATIVE for every specific — its Section 5 Gap Assessment (G1–G4) defines the work and its Verification Blocks (V1–V4) must be re-verified at edit time; if current code diverges from a cited file:line, halt work on that gap and report the divergence in your receipt instead of improvising. Root cause being fixed: `recover_half_claimed` selects claimed plans GLOBALLY but checks files against each watched directory in sequence (G1), compounded by `lifecycle_state` never advancing past `claimed` (G4) — on 2026-06-12 a second daemon instance marked actively-running plan 7 abandoned via the first-iterated foreign directory. This plan closes FORWARD rows 19 and 20 (`knowledge/FORWARD.md` — Step 2 updates their Status per the Rule 42 reconciliation convention). NOTE: this plan modifies the code of the daemon executing it — the running daemon keeps its loaded code; changes take effect only at the next restart (flag in the receipt: DAEMON RESTART REQUIRED, no hot reload).

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, then the diagnostic IN FULL. Re-verify each Verification Block before trusting its line numbers.
>
> **Implement, per the diagnostic Section 5 Gap Assessment — scope is exactly these files: `bellows.py`, `lifecycle.py`, `tests/` (conftest.py only if needed):**
> - **G1 (project-scoped recovery):** `recover_half_claimed` filters candidate plans to those whose `target_project` corresponds to the `decisions_dir` being scanned (derive the project root from the decisions path or pass it from the callsite — diagnostic G1 row). A plan must only ever be classified by the recovery pass that scans ITS OWN project's decisions directory.
> - **G2 (flock single-instance guard):** exclusive non-blocking `flock` on `.bellows.lock` at BELLOWS_ROOT, acquired in `__main__` BEFORE any DB migration, recovery, or watcher startup (diagnostic G2 row anchors the callsite). On contention: log one clear ERROR line (`another Bellows instance holds .bellows.lock — exiting`) and exit non-zero. The lock handle must persist for daemon lifetime (module-level or main-scope reference; kernel releases on process death — no cleanup code).
> - **G3 (age guard, N=5min per DF3):** in `recover_half_claimed`, before classifying any plan `abandoned`, skip (log INFO `recovery: plan <id> younger than 5m — skipping`) when `created_at` is less than 5 minutes old.
> - **G4 (in_progress write):** immediately after the claim rename (`shutil.move` to the in-progress name — diagnostic G4 row anchors it), call `lifecycle.mark_plan_state(plan_id, "in_progress")`. Log-and-continue semantics like every lifecycle write. This also makes recovery's `already_renamed` branch consistent — verify recovery still handles rows in either `claimed` or `in_progress` state correctly and adjust its SELECT if the diagnostic's G1 fix requires it.
>
> **Tests (extend the existing patterns in `tests/test_lifecycle.py` / `tests/test_bellows.py`):** (a) cross-project isolation — a claimed plan for project X with its in-progress file present in X's dir is NOT touched when recovery scans project Y's dir; (b) age guard — a claimed plan younger than 5 minutes with no files is NOT abandoned; older than 5 minutes IS; (c) `mark_plan_state` to `in_progress` after claim (integration-level per repo pattern, or unit on the callsite helper); (d) flock guard — acquiring the lock twice in-process (or via a forked subprocess per repo conventions) fails the second acquisition; (e) regression: the three existing `TestRecoverHalfClaimed` behaviors still pass with the new signature/filtering. Then run the FULL suite (`python3 -m pytest tests/`) to an explicit pass/fail and READ THE TAIL — never infer green from a subset. Write the dev log (per-gap edits with line ranges, test count before/after, full-suite tail) to `bellows/knowledge/development/daemon-guard-recovery-fix-dev-log-2026-06-12.md`. Use `with open()` for deposits; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/daemon-guard-recovery-fix-dev-log-2026-06-12.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, and the diagnostic Sections 1, 4, 5. **Verify, each with executed evidence (files into `knowledge/qa/evidence/daemon-guard-recovery-fix-2026-06-12/`):** (1) **Full suite** — `python3 -m pytest tests/` final 15 lines, zero failures/errors, new-test count matches the dev log; output to `full_suite_tail.txt`. (2) **G1 landed** — grep `recover_half_claimed` for the project filter and run a live read-only sanity query proving plan rows carry `target_project` values the filter can key on; output to `g1_check.txt`. (3) **G2 landed** — grep the `__main__` path for the flock acquisition ordered BEFORE migration/recovery/watcher startup; confirm the contention branch logs and exits; output to `g2_check.txt`. (4) **G3 landed** — grep the 5-minute age check before the abandoned branch; output to `g3_check.txt`. (5) **G4 landed** — grep the `mark_plan_state(plan_id, "in_progress")` call after the claim rename, and confirm THIS plan's own row reads `in_progress`... NOTE: it will NOT — the running daemon predates this fix; state that explicitly as the expected live-canary condition (the FIRST plan claimed after daemon restart shows `lifecycle_state='in_progress'` mid-run) rather than as a failure; output the reasoning and the grep to `g4_check.txt`. (6) **FORWARD reconciliation (Rule 42)** — update `knowledge/FORWARD.md` rows 19 and 20: Status → `closed-by-plan-<this plan's id>`; output the diff to `forward_reconciliation.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `daemon-guard-recovery-fix-2026-06-12`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/daemon-guard-recovery-fix-qa-report-2026-06-12.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/daemon-guard-recovery-fix-2026-06-12/`; `required_evidence_files`: `[full_suite_tail.txt, g1_check.txt, g2_check.txt, g3_check.txt, g4_check.txt, forward_reconciliation.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table (item | method | evidence file | PASS/FAIL) and the Rule 20 banner block to `bellows/knowledge/qa/daemon-guard-recovery-fix-qa-report-2026-06-12.md`. **Receipt Flags for CEO must include:** (1) DAEMON RESTART REQUIRED — no hot reload; (2) live canaries after restart: the first claimed plan shows `lifecycle_state='in_progress'` mid-run, and a deliberate second `python3 bellows.py` start exits immediately on the flock. Use `with open()` for deposits; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/daemon-guard-recovery-fix-qa-report-2026-06-12.md`
> - `bellows/knowledge/qa/evidence/daemon-guard-recovery-fix-2026-06-12/` (six evidence files per Rule 20 self-check)
