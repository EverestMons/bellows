# bellows — Diagnostic: on_moved Fix Shape
**Date:** 2026-04-19 | **Tier:** Small (design question) | **Test Scope:** n/a (diagnostic) | **Execution:** Step 1 (SA)
**Priority:** 1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS. The Planner reads the deposited findings file via direct tool call and performs Rule 22 verification.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-on-moved-fix-shape-2026-04-19.md. Execute Step 1 ONLY. After completing Step 1, STOP.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-on-moved-fix-shape-2026-04-19.md", "bellows/knowledge/decisions/in-progress-diagnostic-on-moved-fix-shape-2026-04-19.md")`. **Read your specialist file and skip glossary reads — this is a code-shape and test-surface investigation.** The prior diagnostic (`bellows/knowledge/research/bellows-rerun-cascade-2026-04-19.md`) identified that `PlanHandler.on_moved` at `bellows.py` L486–488 fires for moves whose destination is inside a subdirectory like `Done/`, causing Bellows to re-claim and re-execute plans that the Planner moved out of the active queue. The chosen fix (Candidate A) is: reject moves whose destination is not directly in the watched top-level directory. Your job is to surface the implementation details needed to write a correct, minimal, surgical fix — AND to confirm that no other dispatch paths (startup scan, `_rescan`, manual claim paths) can reach a plan file from inside `Done/`. Do NOT write code, do NOT modify any file outside your deposit target. **Investigation questions, in order:** **(1) PlanHandler's watched-directory reference.** Read the `PlanHandler` class in `bellows.py` in full. Cite the class's line range. Report: (a) does `PlanHandler` hold a reference to the watched directory (e.g., `self.watched_dir`, `self.decisions_path`, etc.)? Cite the attribute name and where it's set. (b) If no such attribute exists, how is the handler instantiated — what directory context does it have access to at `on_moved` call time? (c) Is there a module-level or class-level constant that identifies the top-level watched directory? Cite it. **(2) Exact guard condition shape.** Given the watched-directory reference from (1), propose the exact guard condition that rejects moves-to-subdirectory but permits legitimate moves-into-top-level (e.g., Planner depositing a new plan via write-to-temp-then-move per Rule 24). Write out the precise Python condition (e.g., `if Path(event.dest_path).parent != self.watched_dir: return`) that would go at the start of `on_moved`. If the watched_dir reference requires normalization (e.g., realpath, resolve), note it. Also note: should the guard also check `src_path`? (The original BACKLOG #4 fix was for same-directory renames — src and dest both in top-level. The new bug is dest in subdirectory. Confirm the guard doesn't accidentally block the BACKLOG #4 case.) **(3) on_created parallel path check.** Read the `on_created` method in `PlanHandler` (if it exists) and report whether it has the same subdirectory vulnerability. If `on_created` fires with `event.src_path` inside `Done/`, does it also dispatch to `_handle`? Report the line range and behavior. If the same bug exists in `on_created`, the fix scope needs to include both methods — report this explicitly. **(4) Other dispatch paths that could reach Done/.** Enumerate every code path in `bellows.py` that can trigger `_handle()`, `handle_new_plan()`, `run_plan()`, or any plan-claiming function. For each: (a) does it operate on a filename scanned from the top-level `decisions/` directory only, or could it scan recursively / receive an absolute path from Done/? (b) report with line number + one-line summary. Candidates to check: the `watchdog` handler methods, the startup scan, `_rescan`, `_consume_verdicts`, any CLI or API-driven claim path, any DB-recovery path. This answers whether Candidate A (fix on_moved only) is structurally sufficient, or whether belt-and-suspenders protection (Candidate B — reject from `is_runnable_plan` or similar) is actually needed. **(5) Existing tests for on_moved.** Search `bellows/tests/` for any test that exercises `on_moved` (grep for `on_moved`, `on_created`, `PlanHandler`, `FileSystemEventHandler`). For each test found: report the test name, file path, what behavior it verifies (the same-directory rename case, new plan creation, something else), and whether adding the dest-in-subdirectory guard would break it. The original BACKLOG #4 fix shipped 2 new unit tests — find them specifically and confirm they would still pass with the guard added. **(6) Test-surface proposal.** Propose the minimum tests that would verify the guard works correctly AND doesn't regress the BACKLOG #4 fix. Cover: (a) move into top-level decisions/ from temp — should dispatch, (b) move into decisions/Done/ from top-level decisions/ — should NOT dispatch, (c) move into decisions/ from decisions/ (same-directory rename, BACKLOG #4 case) — should dispatch, (d) directory moves — should NOT dispatch (existing is_directory check). Name each test and describe the fixture shape. **(7) Cheap sanity check on hypothesis.** State in 2-3 sentences: is Candidate A (fix on_moved only) sufficient to stop the re-run cascade? Single strongest evidence FOR. Single strongest evidence AGAINST. Do NOT recommend an implementation. **Deposit format:** write findings to `bellows/knowledge/research/on-moved-fix-shape-2026-04-19.md` as a 7-section report, one section per question. Cite line numbers and file paths throughout; quote at most 5 lines per excerpt. End with an "Implementation Ready" subsection stating what the executable needs to specify (line number for guard insertion, exact guard condition, test files to add/modify). **Write the deposit using the canonical Python file-write pattern:** `with open("bellows/knowledge/research/on-moved-fix-shape-2026-04-19.md", "w") as f: f.write(content)` where `content` is a triple-quoted Python string defined before the open call. Do NOT use heredoc. Do NOT use `python3 -c`. Commit with message: `"docs: diagnostic — on_moved fix shape characterization"`. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/on-moved-fix-shape-2026-04-19.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to write any fix. Do NOT move the plan to Done. The Planner will read your findings and handle housekeeping per Rule 22.**
