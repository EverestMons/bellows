# bellows — Verdict Directory Validator
**Date:** 2026-05-12 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1 (BELLOWS_DEVELOPER) → Step 2 (BELLOWS_QA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation ("ok") before proceeding to Step 2.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/parallel-1-executable-verdict-directory-validator-2026-05-12.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

## CEO Context

Verdict filename/directory validator. Implements Artifact 2 / Response A+C from `bellows/knowledge/research/planner-authored-contract-validation-step-2-evaluation.md`. New scan inside `_consume_verdicts()` (bellows.py:1054+) that, on each tick (~30s), scans `verdicts/pending/` for files matching `verdict-*.md` but NOT `verdict-request-*.md` — these are verdict response files in the wrong directory (should be in `verdicts/resolved/`). Logs a loud WARN and posts a Pushover notification deduped per (filename, "misplaced_directory") for the daemon lifetime.

Addresses the 2026-05-12 misplaced-verdict incident structurally identical to Artifact 1's silent-skip. Currently no feedback exists when a verdict file lands in the wrong directory.

Parallel-1 with `parallel-1-executable-verdict-content-validator-2026-05-12.md`. Different production files (`bellows.py` vs `verdict.py`), different test files — no conflicts.

---
---

## STEP 1 — BELLOWS_DEVELOPER

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/parallel-1-executable-verdict-directory-validator-2026-05-12.md", "bellows/knowledge/decisions/in-progress-parallel-1-executable-verdict-directory-validator-2026-05-12.md")`. You are the Bellows Developer. Read your specialist file and domain glossary first. Read `bellows/knowledge/research/planner-authored-contract-validation-step-2-evaluation.md` (Part 3, Artifact 2 section) and `bellows/knowledge/qa/evidence/planner-authored-contract-validation-2026-05-12/consumer-code-grep-transcripts.md` (Artifact 2 section) for the exact target code. **Target:** `bellows.py::_consume_verdicts()` (currently around lines 1054-1064 per evidence file; verify via grep). **Change 1 — directory validator function:** add a new function `_scan_misplaced_verdicts(self, pending_dir)` on the `Bellows` class (or as a module-level helper if class scope is awkward; match the convention of nearby code). The function scans `pending_dir` (typically `bellows/verdicts/pending/`) for files where `fname.startswith("verdict-") and fname.endswith(".md") and not fname.startswith("verdict-request-")`. For each match, log a WARN line to stderr (use the same logging pattern as elsewhere in `bellows.py` — `_log("WARN", msg)` if available, else `print` with timestamp prefix) with the literal message format: `verdict file in wrong directory: <full_filepath> — expected location: verdicts/resolved/ — file will be silently ignored by verdict consumption until moved`. Then call `notifier.push(title="Bellows — Misplaced Verdict", body=f"{fname} found in pending/ — should be in resolved/")` deduped via a module-level `_NOTIFIED_MISPLACED: set[tuple[str, str]] = set()` set keyed by `(fname, "misplaced_directory")` — skip Pushover if key in set, add after successful push. Wrap the Pushover call in try/except to swallow notification failures. **Change 2 — invoke from `_consume_verdicts`:** at the top of `_consume_verdicts`, after resolving the pending_dir path, call `self._scan_misplaced_verdicts(pending_dir)` (or the module-level equivalent). The scan must run BEFORE the existing resolved-directory consumption loop. **Change 3 — observability logging:** at the start of `_scan_misplaced_verdicts`, log a DEBUG-level line (or stderr gated by module-level `MISPLACED_VERDICT_SCAN_VERBOSE = False`) of the form `misplaced-verdict scan: pending_dir=<path> — found=<count>` so the scan is observable in diagnostic mode. **Tests:** create `tests/test_misplaced_verdicts.py` with at minimum these tests — (a) `test_scan_misplaced_verdicts_warns_on_verdict_in_pending` — write `verdicts/pending/verdict-some-slug-step-1.md` (note: NOT `verdict-request-`), call `_scan_misplaced_verdicts`, capsys captures the WARN with expected format and filename, monkeypatched `notifier.push` called exactly once. (b) `test_scan_misplaced_verdicts_ignores_verdict_request_files` — write `verdicts/pending/verdict-request-some-slug-step-1.md`, call scan, assert no WARN, monkeypatched push not called. (c) `test_scan_misplaced_verdicts_ignores_non_md_files` — write `verdicts/pending/verdict-something.txt`, assert no WARN. (d) `test_scan_misplaced_verdicts_pushover_deduped_per_file` — write the misplaced file, call scan twice in succession (simulating two `_consume_verdicts` ticks), assert push called exactly once. (e) `test_scan_misplaced_verdicts_pushover_failure_swallowed` — monkeypatch `notifier.push` to raise, write misplaced file, assert scan returns normally without exception (WARN still fires). (f) `test_scan_misplaced_verdicts_invoked_from_consume_verdicts` — set up a Bellows instance with a temp verdicts directory containing one misplaced file, call `_consume_verdicts()`, assert the scan ran (WARN captured, push called). (g) `test_scan_misplaced_verdicts_empty_pending_directory` — pending_dir exists but is empty, scan completes without error, no warnings, no pushes. **Use pytest fixtures with tmp_path; reset `_NOTIFIED_MISPLACED.clear()` in setup of dedupe-sensitive tests.** Run targeted `pytest tests/test_misplaced_verdicts.py -v` and confirm all pass. Run `pytest tests/test_bellows.py tests/test_consume_verdicts.py -v` to confirm no regressions in nearby tests. Run full suite `pytest -x --tb=short` and confirm only the pre-existing `test_run_step_timeout` failure remains. Commit with message: `feat(bellows): directory validator for misplaced verdict files — closes silent-skip on verdicts in pending/`. After your output receipt, read `bellows/knowledge/research/agent-prompt-feedback.md` and append a dated entry per the standard protocol. Commit the feedback entry with message: `docs: prompt feedback — DEV verdict directory validator`. **Deposits:** - `bellows/knowledge/development/verdict-directory-validator-2026-05-12.md` - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — BELLOWS_QA

---

> Before starting, read `bellows/knowledge/development/verdict-directory-validator-2026-05-12.md` and check the Output Receipt status field. If status is not Complete or Partial with known acceptable scope, stop and report the issue to the CEO before proceeding. You are the Bellows QA Analyst. Read your specialist file and domain glossary first. Read the deposited dev log to understand what shipped. **Verify deliverables per Rule 17:** (1) `bellows.py` has a new `_scan_misplaced_verdicts` function (method or module-level) — read the code and confirm filter logic (`startswith("verdict-") and endswith(".md") and not startswith("verdict-request-")`). (2) `_consume_verdicts` invokes `_scan_misplaced_verdicts` BEFORE the resolved-directory consumption loop — read the relevant lines and confirm ordering. (3) Module-level `_NOTIFIED_MISPLACED` set is declared. (4) `MISPLACED_VERDICT_SCAN_VERBOSE` constant is declared. (5) The 7 new tests are present in `tests/test_misplaced_verdicts.py` with the exact function names listed in Step 1; read each test to confirm the assertions match the test name's claim. (6) Run targeted `pytest tests/test_misplaced_verdicts.py -v` — confirm all 7 pass. (7) Run `pytest tests/test_bellows.py tests/test_consume_verdicts.py -v` — confirm no regressions. (8) Run full suite `pytest --tb=short` — confirm only `test_run_step_timeout` (pre-existing) fails. (9) Behavioral spot-check: write a Python REPL one-liner that creates a tmp pending/ directory with one misplaced file `verdict-test-slug-step-1.md` and one well-placed request file `verdict-request-test-slug-step-1.md`, calls `_scan_misplaced_verdicts(tmp_pending)`, captures stderr, confirms (a) the misplaced file triggers a WARN containing its filename, (b) the request file does NOT trigger a WARN. Save to `bellows/knowledge/qa/evidence/verdict-directory-validator-2026-05-12/repl-mixed-files-spot-check.txt`. **Rule 20 self-check:** run the canonical Python block from `/Users/marklehn/Desktop/GitHub/RULE_20_SELF_CHECK_BLOCK.md` against `bellows/knowledge/qa/evidence/verdict-directory-validator-2026-05-12/`. **PROJECT_STATUS.md update:** append a 2026-05-12 entry covering this ship (commit SHA, files changed, test delta, REMINDER: restart Bellows daemon to load fix). **Deposit QA report at `bellows/knowledge/qa/verdict-directory-validator-qa-2026-05-12.md`** with all 9 checks + Rule 20 self-check banner + Output Receipt Complete. Commit with message: `qa: verdict directory validator — 7 new tests + REPL spot-check pass; daemon restart required`. After your output receipt, read `bellows/knowledge/research/agent-prompt-feedback.md` and append a dated entry per the standard protocol. Commit the feedback entry with message: `docs: prompt feedback — QA verdict directory validator`. **Deposits:** - `bellows/knowledge/qa/verdict-directory-validator-qa-2026-05-12.md` - `bellows/knowledge/qa/evidence/verdict-directory-validator-2026-05-12/` - `bellows/PROJECT_STATUS.md` - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT move the plan to Done — that is the Planner's responsibility after Rule 22 verification.**
