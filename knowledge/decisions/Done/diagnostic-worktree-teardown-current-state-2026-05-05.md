# Bellows — Worktree Teardown Current State Diagnostic (OP-001)
**Date:** 2026-05-05 | **Tier:** Small | **Test Scope:** none (read-only investigation) | **Execution:** Step 1 (DEV)

## How to Run This Plan

**IMPORTANT — DO NOT DISPATCH VIA BELLOWS.** Bellows worktree teardown is the subject of this diagnostic. Run via manual Claude Code session in the bellows repo. Halt-prefix or pause Bellows before pasting the bootstrap if it is currently running.

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1. After completing Step 1, the agent STOPS and waits for CEO confirmation. The Planner then performs Rule 22 verification on the deposited findings file and authorizes housekeeping.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-worktree-teardown-current-state-2026-05-05.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to any further step or move the plan to Done.
```

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-worktree-teardown-current-state-2026-05-05.md", "bellows/knowledge/decisions/in-progress-diagnostic-worktree-teardown-current-state-2026-05-05.md")`. **Skip glossary read — this is a code-tracing, log-inspection, and recent-history reconstruction task on Bellows. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first** for architectural context. You are investigating the current state of Bellows worktree teardown. Per CEO memory (which may be stale), OP-001 was: `_teardown_worktree` crashes with `'string indices must be integers'` after an agent commit lands in a worktree, cherry-pick to main never executes, plan strands at `in-progress-`. Two issues were noted — literal Python error + worktree pointed at governance-root because Bellows had no own `.git`. CEO believes `git init` happened since but is unsure of the current overall state, and there has been substantial worktree-related work shipped May 3-5 (worktree-impl, worktree-tests, monorepo-worktree-backlog-close, scope-check-external-vector). The original OP-001 description may no longer reflect reality. **Static analysis only — do NOT propose fixes, do NOT edit Bellows source, do NOT dispatch reproduction plans.** **Investigation tasks (execute in order):** **(1) Confirm Bellows `.git` state.** Run `git -C bellows rev-parse --git-dir 2>&1`, `git -C bellows rev-parse --show-toplevel 2>&1`, and `git -C bellows worktree list 2>&1`. Capture stdout and stderr verbatim. Confirm whether the repo root resolves to `bellows/` (correct) or to `/Users/marklehn/Desktop/GitHub/` (incorrect — original OP-001 condition). If the latter, flag it as a still-broken precondition. **(2) Reconstruct what worktree work shipped May 3-5.** List `bellows/knowledge/decisions/Done/` and identify all plans with `worktree`, `monorepo`, or `scope-check` in the filename modified between 2026-05-01 and 2026-05-05. For each one, read the corresponding QA report or development log in `bellows/knowledge/qa/` or `bellows/knowledge/development/` (only the ones from those dates). Build a chronological summary table: filename | plan purpose (one line from the plan header) | files changed (from the deposited dev log or QA report) | verdict (shipped / partial / halted). The goal is to understand what the worktree implementation actually did so that the current `_teardown_worktree` source is read in the right context. **(3) Locate and read current `_teardown_worktree` source.** Search Bellows source for the function definition: `grep -rn "_teardown_worktree\|def teardown_worktree" bellows/*.py 2>&1`. Identify the file and line range. Read the full function body verbatim and quote it in findings. Identify EVERY line in the function that performs a subscript operation (any `x['key']`, `x[0]`, `x[i]` pattern). For each subscript line, identify what `x` is — its source (function parameter, return value of a subprocess call, parsed git command output, dict from JSON, etc.). The Python error `'string indices must be integers'` means a `str` is being subscripted with a string key as if it were a dict — find every candidate location where this could occur. **(4) Trace the call path inward.** Find every caller via `grep -rn "_teardown_worktree\|teardown_worktree(" bellows/*.py 2>&1`. For each caller, identify what arguments are passed in. Trace each argument back to its origin (config field, subprocess output, parsed git output, etc.). The goal is a complete data-flow map: caller → argument → origin → expected type vs. actual type at the failing subscript. **(5) Trace the call path outward.** What functions does `_teardown_worktree` call? Identify subprocess invocations (`subprocess.run`, `git ...` commands), parsing logic (`.split()`, `.strip()`, JSON parsing, etc.), and any return-value handling. The TypeError might originate from a downstream function that returns a string when a dict was expected. **(6) Scan recent Bellows logs for `string indices` occurrences.** Bellows logs are in `bellows/logs/` as `YYYYMMDD-HHMMSS-step.json` files (~590+ files; do NOT read all of them). Use `grep -l "string indices" bellows/logs/*.json 2>/dev/null | sort | tail -50` to find the most recent files containing the error. For each match (up to 10 most recent), extract the timestamp from the filename and grep the full traceback from inside the JSON. Capture the matching log entries verbatim with surrounding context. Build a table: log filename | timestamp | full traceback (if available) | plan name (extractable from log context). Identify the latest occurrence — if it is before 2026-05-03, the worktree work may have resolved the bug; if it is on/after 2026-05-03, the bug is still live. **(7) Cross-reference against worktree work timeline.** Compare the latest `string indices` log occurrence (step 6) against the worktree implementation timeline (step 2). Is the latest occurrence before, during, or after the worktree work shipped? **(8) Synthesize findings into five answered questions.** Question A: is Bellows's `.git` state correct (resolves to `bellows/`)? Question B: what worktree work shipped May 3-5, and what did it change in `_teardown_worktree` or the call path? Question C: where exactly is the TypeError thrown in the CURRENT source — file, function, line number, the specific subscript expression, the data flow that produces the wrong type? Question D: when was the most recent occurrence of `'string indices must be integers'` in Bellows logs, and is the bug still live (occurrence after worktree work shipped) or resolved (last occurrence predates worktree work)? Question E: what is the recommended next investigation step — fix is needed (bug live, root cause identified), bug is resolved and only memory/BACKLOG needs updating, OR additional reproduction is needed (static analysis inconclusive)? **Deposit findings to `bellows/knowledge/research/worktree-teardown-current-state-findings-2026-05-05.md`** using the canonical Python file write pattern: `with open("bellows/knowledge/research/worktree-teardown-current-state-findings-2026-05-05.md", "w") as f: f.write(content)` where content is a triple-quoted string defined before the open call. The file structure: header (date, agent, plan reference), Investigation Section 1 (`.git` state with verbatim command output), Investigation Section 2 (worktree work timeline), Investigation Section 3 (current `_teardown_worktree` source + subscript inventory), Investigation Section 4 (call path inward), Investigation Section 5 (call path outward), Investigation Section 6 (log evidence table with latest occurrence), Investigation Section 7 (timeline cross-reference), Findings (the five answered questions A–E), Output Receipt (canonical format). **Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.** **Final commit:** `git add bellows/knowledge/research/worktree-teardown-current-state-findings-2026-05-05.md bellows/knowledge/research/agent-prompt-feedback.md && git --no-pager log -1 && git commit -m "diag: worktree teardown current state findings (OP-001 recheck)"`. **STOP. Do NOT proceed beyond Step 1. Do NOT move the plan to Done. Wait for CEO confirmation.**
>
> **Deposits:**
> - `bellows/knowledge/research/worktree-teardown-current-state-findings-2026-05-05.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
