# Bellows — `_extract_plan_required_deposits` set→list conversion

**Date:** 2026-05-25 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_step_1

## Context

The 2026-05-24 BACKLOG entry at `bellows/knowledge/BACKLOG.md` line 9 documents a capability addition: `_extract_plan_required_deposits()` returns a `set` (4 return sites in `bellows/gates.py`, plus helper `_filter_transient_paths`), making `md_paths[0]` selection of "the QA report" hash-dependent. Two consumers select `md_paths[0]` by convention: `_gate_rule_20_self_check` (`gates.py:432`) and `_gate_rule_22_verification` (`gates.py:493`). Currently works because QA steps typically have one `.md` deposit; reproduces as a Rule 22 override when a QA step declares multiple `.md` deposits.

This plan applies BACKLOG resolution option (a): convert `_extract_plan_required_deposits` and `_filter_transient_paths` to return `list` preserving insertion order. The block-form path uses `re.finditer` which walks bullets top-to-bottom — list order = authoring order. Plan-authoring convention: the QA report MUST be the first `.md` entry in the `**Deposits:**` block.

**Test Scope: targeted** — `gates.py` only, no cross-bucket regression risk; full-suite covered by the session-end run per Rule 21.

**Sources:** BACKLOG entry at `bellows/knowledge/BACKLOG.md` line 9; production code at `bellows/gates.py` lines 358–449 (function `_extract_plan_required_deposits` + helper `_filter_transient_paths`); consumers at `bellows/gates.py:432` (`_gate_rule_20_self_check`) and `bellows/gates.py:493` (`_gate_rule_22_verification`).

**Worktree-prefix note for the agent:** plan references use `bellows/` prefix for Planner readability. Inside the worktree, files are at the worktree root (strip the `bellows/` prefix when reading or editing).

**Pre-existing test failures expected (5 carry-overs, not regressions):** 4 × `test_decisions.py` worktree artifacts, 1 × `test_run_step_timeout`.

## How to Run This Plan

Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md", "bellows/knowledge/decisions/in-progress-executable-extract-plan-required-deposits-set-to-list-2026-05-25.md")`. You are the Bellows Developer. Skip specialist file and glossary reads — this is a small refactor with explicit edit instructions. **Task:** convert `_extract_plan_required_deposits` and its helper `_filter_transient_paths` in `gates.py` to return `list` (preserving authoring order from regex match order) instead of `set`, closing the BACKLOG entry at `knowledge/BACKLOG.md` line 9 (`md_paths[0]` hash-dependence). **Read first** `gates.py` lines 358–449 (the function and helper) to confirm the four return sites: (1) `_filter_transient_paths` set comprehension; (2) block-form `paths = set()` → return; (3) inline-form `paths = set()` → return; (4) legacy prose-matching `paths = set()` → return. **Edit 1 — `_filter_transient_paths`:** change the set comprehension `return {p for p in paths if not os.path.basename(p).startswith("_staging_")}` to a list comprehension `return [p for p in paths if not os.path.basename(p).startswith("_staging_")]`. **Edit 2 — block-form path:** change `paths = set()` to `paths = []`, change `paths.add(m.group(1))` to `paths.append(m.group(1))`. **Edit 3 — inline-form path:** change `paths = set()` to `paths = []`, change `paths.add(m.group(1))` to `paths.append(m.group(1))`. **Edit 4 — legacy prose-matching path:** change `paths = set()` to `paths = []`, and the three `paths.add(...)` calls in the three prose patterns to `paths.append(...)`. **Critical:** the legacy prose-matching path has three regex loops (Pattern 1: backtick-quoted deposit-to; Pattern 2: unquoted .md path; Pattern 3: `with open(...)` Python write). All three append into the same `paths` collection. Since list does NOT deduplicate, you MUST add a deduplication step before the `return _filter_transient_paths(paths)` line that preserves first-seen order. Use `dict.fromkeys(paths)` (Python 3.7+ ordered-dict guarantee): `paths = list(dict.fromkeys(paths))`. This is mandatory — without it, a path matched by both Pattern 1 and Pattern 2 would appear twice in the returned list. The block-form and inline-form do NOT need deduplication because each uses a single `re.finditer` over a bounded section, and the BACKLOG semantics (`md_paths[0]` = first-authored deposit) is exactly what insertion order delivers there. **Edit 5 — docstring update for `_extract_plan_required_deposits`:** add a sentence to the docstring stating "Returns a list preserving insertion order; for the block form, this equals the authoring order of bullets in the `**Deposits:**` block. Convention: the QA report is the first `.md` entry in the block." **Edit 6 — docstring update for `_filter_transient_paths`:** change "Drop paths whose basename..." to "Drop paths whose basename starts with `_staging_` and return as a list preserving input order." **Edit 7 — stale comment cleanup in `tests/test_gates.py`:** two existing tests at approximately lines 1516 and 1537 have comments stating `"_extract_plan_required_deposits returns a set with hash-randomized ordering"` and `"Uses patch to control deposit ordering since _extract_plan_required_deposits returns a set."`. Update both comments to reflect the new behavior: replace with `"_extract_plan_required_deposits now returns a list preserving authoring order (per BACKLOG 2026-05-24 fix); patch is retained for test isolation."`. Do NOT remove the `patch.object` mocks themselves — they serve test-isolation purposes beyond ordering control, and removing them is out of scope for this plan. **Add one regression test** to `tests/test_gates.py` named `test_extract_plan_required_deposits_preserves_block_order`: construct a step text containing a `**Deposits:**` block with three `.md` bullets in a specific order (e.g., `knowledge/qa/foo.md`, `knowledge/qa/evidence/`, `knowledge/research/bar.md`), call `_extract_plan_required_deposits(step_text)`, assert the return is a `list` (not a `set`) via `isinstance(result, list)`, and assert the first `.md` entry is `knowledge/qa/foo.md` (the QA-report-first convention). **Verification before committing:** run from the worktree root: `python3 -m pytest tests/test_gates.py -v -k 'extract_plan_required_deposits or filter_transient'` to confirm existing tests in those areas still pass plus the new test passes. Capture full output to confirm count. **Deposit a dev log** at `knowledge/development/extract-plan-required-deposits-set-to-list-2026-05-25.md` with sections: (a) verbatim diff of all edits in `gates.py` (use `git --no-pager diff gates.py`), (b) the new test function name with the assertion list, (c) targeted pytest output showing the affected tests and the new test, (d) confirmation that `_extract_plan_required_deposits` now returns a `list` (per `type(...)` check in the test), (e) any deviations from this plan and why, (f) Output Receipt with Status: Complete. **Commit** with message `fix(gates): _extract_plan_required_deposits set→list — deterministic md_paths[0] selection — closes BACKLOG capability` and a brief body listing the two files changed. Standard prompt feedback protocol → `knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/development/extract-plan-required-deposits-set-to-list-2026-05-25.md`

---
---

## STEP 2 — Bellows QA

---

> Before starting, read `bellows/knowledge/development/extract-plan-required-deposits-set-to-list-2026-05-25.md` and check the Output Receipt status field. If status is not Complete, stop and report the blocker before proceeding. You are the Bellows QA agent. Skip specialist file and glossary reads — this is a mechanical refactor verification. **FIRST — Deliverable Verification.** Build a verification table with these checks, each row including line-number evidence and a verbatim quote excerpt from `gates.py`: (1) `_filter_transient_paths` now returns a list comprehension `[p for p in paths if ...]` (not set comprehension `{p for p in paths if ...}`); (2) block-form branch uses `paths = []` and `paths.append(...)` (not `paths = set()` / `paths.add(...)`); (3) inline-form branch uses `paths = []` and `paths.append(...)`; (4) legacy prose-matching branch uses `paths = []`, three `paths.append(...)` calls, AND a `paths = list(dict.fromkeys(paths))` deduplication line immediately before `return _filter_transient_paths(paths)`; (5) docstring updates present on both `_extract_plan_required_deposits` (mentions "list preserving insertion order" and "QA report is first .md") and `_filter_transient_paths` (mentions "list preserving input order"); (6) `test_extract_plan_required_deposits_preserves_block_order` present in `tests/test_gates.py`, asserts `isinstance(result, list)`, and asserts the first `.md` entry matches the authored-first order; (7) stale-comment cleanup applied at the two `patch.object(gates, '_extract_plan_required_deposits', ...)` sites in `tests/test_gates.py` — both comments updated to reflect list return (no longer mention "set with hash-randomized ordering"). Mark each row ✅ or ❌ with evidence. If any ❌, stop and report. **Then — targeted pytest.** Run from the worktree root: `python3 -m pytest tests/test_gates.py -v` and capture output to `evidence/pytest_targeted.txt`. Verify zero new regressions vs. pre-change state — every test in `test_gates.py` that previously passed must still pass, and the new `test_extract_plan_required_deposits_preserves_block_order` must PASSED. **Then — call-site safety check.** Run `grep -n 'md_paths\[0\]' gates.py` and confirm exactly two hits at the two known consumers (`_gate_rule_20_self_check` and `_gate_rule_22_verification`). Capture to `evidence/grep_md_paths.txt`. Confirm both still index `md_paths[0]` — the fix is making the index deterministic, not changing the consumers. **Then — structural compliance check.** Run `git --no-pager show --stat HEAD` and confirm the DEV commit touched exactly 3 files: `gates.py`, `tests/test_gates.py`, `knowledge/development/extract-plan-required-deposits-set-to-list-2026-05-25.md`. Capture to `evidence/dev_commit.txt`. Run `git --no-pager diff HEAD~1 gates.py` and confirm the diff is bounded to `_extract_plan_required_deposits` and `_filter_transient_paths` — no other functions modified. Capture to `evidence/diff_gates.txt`. **Then — Rule 20 self-check.** Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `executable-extract-plan-required-deposits-set-to-list-2026-05-25`; `qa_report_path`: `<absolute-path-to-knowledge/qa/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md>`; `evidence_dir`: `<absolute-path-to-knowledge/qa/evidence/executable-extract-plan-required-deposits-set-to-list-2026-05-25/>`; `required_evidence_files`: `["pytest_targeted.txt", "grep_md_paths.txt", "dev_commit.txt", "diff_gates.txt"]`. Include the literal stdout output of the block in the QA report under an `**Output:**` heading. If FAILED, halt and report to CEO. **Then — QA report deposit.** Write `knowledge/qa/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md` with the verification table, targeted pytest summary, call-site safety check results, structural compliance results, Rule 20 self-check stdout, and Output Receipt with Status: Complete. **DEPOSITS RULE:** declare ONLY the QA report file and the evidence directory in the **Deposits** block below. Do NOT list PROJECT_STATUS.md as a deposit even though you will update it. **Final:** Update `PROJECT_STATUS.md` — prepend a new Completed entry under today's date (2026-05-25) summarizing: `_extract_plan_required_deposits` and `_filter_transient_paths` converted from `set` to `list` (preserving authoring order via `re.finditer` walk order + `dict.fromkeys` dedup on the legacy prose-matching path); `md_paths[0]` selection in `_gate_rule_20_self_check` and `_gate_rule_22_verification` is now deterministic; convention: QA report is the first `.md` in the `**Deposits:**` block; 1 new regression test added; closes 2026-05-24 BACKLOG `_extract_plan_required_deposits` capability entry. Include the **REMINDER: restart Bellows daemon** line. Use `Filesystem:edit_file` with verbatim anchor on the existing top entry. **STOP.** Do NOT move this plan to Done/. Standard prompt feedback protocol → `knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/qa/executable-extract-plan-required-deposits-set-to-list-2026-05-25.md`
> - `bellows/knowledge/qa/evidence/executable-extract-plan-required-deposits-set-to-list-2026-05-25/`
