# Bellows — Daemon-Owned Ledgers ACTIVATION Slice 3 (final): FORWARD New-Rows Go-Live
**Date:** 2026-06-14 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 42 — ACTIVATION Slice 3 (FORWARD new-rows), the FINAL ledger, after feedback (plan 49) and PROJECT_STATUS (plan 51) activated and the multi-turn extraction fix (plan 54) verified live (re-canary plan 55: both ledgers landed). FORWARD disposition is (a) daemon-post-merge for NEW-ROW additions only (design Section 3 File 3). SCOPE NUANCE: only agent-filed NEW rows move to the channel — the Planner's session-wrap STATUS-UPDATE reconciliation (open → closed-by-plan-X, Rule 42) happens directly on main and stays AS-IS (no conflict, not a worktree write). The Phase 3 handler is already built DORMANT: `_append_forward_row` (bellows.py:1265), forward branch ~1206–1213, parser `#### Forward Register`/`#### FORWARD Additions` extraction (parser.py:73–81) — the handler computes the next row number + date, appends, commits. Activation = governance flip (agents emit new FORWARD rows via channel instead of writing FORWARD.md) + confirm idempotency covers the FORWARD append. FORWARD.md is NOT in SCOPE_ALLOWLIST (verified) so no allowlist change. Once agents stop writing FORWARD.md new rows, coexistence passes and the daemon appends. The design doc `knowledge/research/daemon-owned-ledgers-design-2026-06-13.md` Section 5 + Section 3 File 3 is AUTHORITATIVE. Re-verify Verification Blocks at edit time; halt-and-report on divergence. With FORWARD activated, the append-only worktree-conflict class is fully eliminated → FORWARD rows 4, 5, 13 CLOSE (QA reconciles them). DAEMON RESTART REQUIRED after close; the first plan after restart is the FORWARD live canary. DO NOT restart until this plan fully CLOSES.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, design doc Section 3 File 3 + Section 5, and the existing `_append_forward_row` / forward branch / parser forward extraction. **Scope is exactly these files: `bellows.py`, `lifecycle.py`, `tests/test_bellows.py`, `tests/test_lifecycle.py`, and the governance-root `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`.**
> - **(1) Idempotency confirm/extend:** verify the `ledger_writes` idempotency guard covers the FORWARD append path (a teardown retry must not double-append a row); if it only guards feedback/project_status, extend it to forward. Add a test for a duplicate FORWARD apply being a no-op.
> - **(2) Governance flip (PLANNER_TEMPLATE.md):** at the agent-files-a-FORWARD-row sites — Rule 44 (~L963, "filing a new FORWARD.md entry") and Rule 46 (~L973, "file (or update) a FORWARD.md entry … route the underlying bug to the project's FORWARD.md") — update so that AGENTS filing a NEW forward entry during step execution emit it via the Output Receipt `### Ledger Updates > #### Forward Register` channel (the daemon appends the row post-merge), rather than writing FORWARD.md directly. Preserve the dedup/check-before-filing guidance. Rule 42 session-wrap reconciliation (~L942–955, Planner status updates on main) stays UNCHANGED — call this out explicitly in the edited text so the distinction is clear (Planner direct-edits status; agents emit new rows via channel). Add the `#### Forward Register` subsection to the `### Ledger Updates` channel-format spec. Bump `**Version:**` + changelog row. Locate every site by ANCHOR/grep; zero or multiple matches → halt and report. Do NOT touch the feedback or PROJECT_STATUS instructions (already activated).
>
> **Tests:** (a) FORWARD idempotency no-op on duplicate; (b) the forward handler appends a correctly-numbered new row when coexistence allows (regression, Phase 3 behavior preserved); (c) coexistence still skips when FORWARD.md in files_changed. Run the FULL suite (`python3 -m pytest tests/`) to explicit pass/fail and READ THE TAIL. Write the dev log to `bellows/knowledge/development/forward-activation-dev-log-2026-06-14.md`. Use `with open()`; no heredocs. Standard prompt feedback → emit via `### Ledger Updates > #### Prompt Feedback` (the fix is live now — it will land). **Deposits:**
> - `bellows/knowledge/development/forward-activation-dev-log-2026-06-14.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, design doc Sections 3 File 3 + 5. **Verify, each with executed evidence (files into `knowledge/qa/evidence/forward-activation-2026-06-14/`):** (1) **Full suite** — final 15 lines, zero failures, new-test count matches dev log; `full_suite_tail.txt`. (2) **Idempotency** — FORWARD duplicate-apply no-op test passes; `idempotency.txt`. (3) **Governance flip** — Rule 44/46 now route agent-filed new rows via `### Ledger Updates > #### Forward Register`; Rule 42 session-wrap reconciliation explicitly preserved as direct; `#### Forward Register` in the format spec; feedback/PROJECT_STATUS instructions untouched; version bumped; `governance.txt`. (4) **Coexistence intact** — skip-when-FORWARD.md-in-files_changed regression passes; `coexistence.txt`. (5) **All-three-ledgers proof** — grep the daemon code/log confirming feedback + project_status + forward handlers are all live (the full ledger trio); `all_three.txt`. (6) **FORWARD rows 4/5/13 reconciliation (Rule 42, direct status edit)** — update `bellows/knowledge/FORWARD.md`: set rows 4, 5, 13 Status → `closed-by-plan-<this plan's id>` with Plan-id link, Item suffix ` — closed 2026-06-14: append-only worktree-conflict class eliminated by daemon-owned ledgers (diagnostic 42; feedback+PROJECT_STATUS+FORWARD slices)`; output the diff to `rows_reconciled.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `forward-activation-2026-06-14`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/forward-activation-qa-report-2026-06-14.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/forward-activation-2026-06-14/`; `required_evidence_files`: `[full_suite_tail.txt, idempotency.txt, governance.txt, coexistence.txt, all_three.txt, rows_reconciled.txt]`. Include the literal stdout of the block. If it prints `FAILED`, halt and report to CEO. Write the QA report (verification table + Rule 20 banner) to `bellows/knowledge/qa/forward-activation-qa-report-2026-06-14.md`. **Receipt Flags for CEO MUST include:** (1) DAEMON RESTART REQUIRED — do NOT restart until this plan is fully closed; (2) FORWARD live canary — first plan after restart that files a forward entry via channel must be daemon-appended; (3) all three ledgers now activated — the daemon-owned-ledgers effort (diagnostic 41 → 42 → phases → activations → regression fix) is COMPLETE; rows 4/5/13 closed. Use `with open()`; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/forward-activation-qa-report-2026-06-14.md`
> - `bellows/knowledge/qa/evidence/forward-activation-2026-06-14/` (six evidence files per Rule 20 self-check)
