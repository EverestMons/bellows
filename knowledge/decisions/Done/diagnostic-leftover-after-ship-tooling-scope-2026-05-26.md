# Bellows — Leftover-After-Ship Tooling: Scope Diagnostic
**Date:** 2026-05-26 | **Tier:** diagnostic | **Dispatch Mode:** bellows | **Test Scope:** n/a | **Execution:** Step 1 (SA) | **pause_for_verdict:** always

## How to Run This Plan

Deposit into `bellows/knowledge/decisions/`. Bellows claims and dispatches Step 1 to the SA agent. Agent deposits findings and stops. Planner reads findings and authors a follow-up executable in a later session.

---
---

## STEP 1 — SA

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-leftover-after-ship-tooling-scope-2026-05-26.md", "bellows/knowledge/decisions/in-progress-diagnostic-leftover-after-ship-tooling-scope-2026-05-26.md")`. Read your specialist file and domain glossary first. Read-only diagnostic — do not modify any file. The Planner is scoping a small Python script that compares BACKLOG.md Open entries against recent PROJECT_STATUS Completed entries and recent git commit messages, surfacing candidate closures (the "leftover after ship" problem captured in today's LESSON, 5th recurrence). This diagnostic enumerates the inputs the script will read so the executable plan can be authored without ambiguity. **Investigate and report:** **(A) BACKLOG file shape.** Read `bellows/knowledge/BACKLOG.md`. Report: top-level section structure (heading levels, names — Open / Closed / Archive / etc.); how an Open entry is formatted (sample 3 verbatim, anonymizing nothing — paste exact lines including blank lines and indentation); how a Closed entry is formatted (sample 3 verbatim); whether Closed entries live in the same file or a separate file; how an entry is uniquely identified (slug, sequence number, date, free text title); whether entries carry any structured fields (date opened, last touched, tags, severity) and if so what they look like. **(B) PROJECT_STATUS file shape.** Read `bellows/PROJECT_STATUS.md`. Report: top-level section structure; how a Completed entry is formatted (sample 3 verbatim from the most recent session); whether Completed entries are grouped by session, date, or freely interleaved; whether session boundaries are explicit (heading? horizontal rule? date stamp?); and what text appears in a Completed entry that a script could match against a BACKLOG entry's identifier (commit SHAs? plan filenames? slugs? free text titles?). **(C) Git log shape.** Run `git --no-pager log --since="30 days ago" --pretty=format:"%h %s" -- bellows/` from the bellows submodule and from the governance root for the bellows submodule pointer bumps. Paste the last 30 commit subjects verbatim from each. Report whether commit subjects reference BACKLOG entries explicitly (e.g., "closes BACKLOG-XYZ", "fixes leftover entry") or only the implementation work. **(D) Recurrence enumeration.** The LESSON shipped today documents the 5th recurrence of "leftover after ship." Read the LESSONS.md entry at governance root for 2026-05-26 tagged with the leftover-after-ship pattern. Identify each of the 5 recurrences by date and BACKLOG entry. For each recurrence, locate the BACKLOG entry that was the "leftover" and the corresponding PROJECT_STATUS Completed entry and/or commit that should have triggered its closure. Paste both sides verbatim for all 5 cases. These become the script's ground-truth test cases. **(E) Existing scripts.** List `bellows/scripts/` (`ls -la`). Report what already exists, naming conventions, language (Python? bash?), how each is invoked. Note any existing script that does BACKLOG/PROJECT_STATUS scanning or git log mining — the new script must not duplicate functionality. **(F) Invocation context.** Search `bellows/knowledge/` for any reference to "session wrap", "session-start protocol", "Phase 1.5", or housekeeping checklists that might be the natural insertion point for the new tooling. Report what you find with file paths and line numbers. The Planner needs to decide whether the script runs at session-start, session-wrap, on-demand, or as a CI hook. **Deposit findings:** `bellows/knowledge/research/leftover-after-ship-tooling-scope-findings-2026-05-26.md` with sections A–F matching this prompt's structure. Each section reports verbatim samples (in fenced code blocks) plus a one-paragraph SA observation per section. End with an Output Receipt: status (Complete | Partial | Blocked), files read, sample counts. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. This is a single-step diagnostic. Wait for verdict.**
