# bellows — executable: DOCTRINE — Gate 2, thread 336, the DC/PST tranche (the cycle's own rules — the LAST tranche): proposals 445, 448, 450, 451, 452, 454, 466, 468, 473, 475, 478, 479, 531, 560, 585, 586, 543 onto DRAFTING_CYCLE.md v2.38 and PANEL_SEAT_TEMPLATE.md v1.3 by a committed builder in ONE act (ten DC edits — walk-0 acts (7)–(9), five §2.6 bullets, five §2.7 rules, the §3 cold-panel line; four PST edits — the writers named in §2, pytest-only test-shaped code in §5), then the seventeen flip `implemented` — the 86 accepted rows of W=29 and W=30 all codified; Gate 2 closes

**Date:** 2026-09-16 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full (the bellows suite is UNCHANGED by this plan and is run once by QA so the test gate reads a real summary line; the gate reads FAILURES against `known_failures`, never the pass count — P7 is the expectation, and bellows `main` moves as plans merge) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 2

**auto_close:** false

**Slug:** `gate2-dc-w30-2026-09-16`

**Depends on:** the CEO's rulings of 2026-09-14 (thread 336 — one Gate 2 over the 86 accepted proposals, W=29 and W=30 together; the four W=30 disagreements ruled) and the delegation of 2026-09-15 ("Start on gate 2 and you have my delegation to release at the proper time"; "No I am good with moving forward on these" — the seven-tranche partition, `gate2-packet-2026-09-15.md` §2, its partition row for the DC/PST tranche); the non-author Gate 1 packets — W=29's `gate1-packet-2026-09-02.md` (445, 448, 450, 451, 452, 454, 466) and W=30's `gate1-packet-2026-09-13-a.md`, `-b.md` and `-c.md` (468, 473, 475, 478, 479, 531, 543, 560, 585, 586); **PT tranche six** — `Done/executable-100122.md` in bellows (`gate2-pt-w30-f-2026-09-16`, the clone origin and the newest same-class plan: doctrine prose onto a governance doctrine file by a committed builder, then a scoped CAS flip; its register `walk-register-gate2-pt-w30-f-2026-09-16.md`) and the six PT tranches before it (#100109, #100114, #100119, #100120, #100121, #100122 — v4.112 to v4.118, the PT route emptied); the LIVE `DRAFTING_CYCLE.md` is v2.37 (governance `9df017c5`) and the LIVE `PANEL_SEAT_TEMPLATE.md` v1.2 (`3d2c1aae`), this tranche's pre-edit versions, so every pin below is measured on them directly; the builder `governance/knowledge/decisions/drafts/build-gate2-dc-w30-2026-09-15.py` (committed `f83bbb29` — the walk-0 re-pin after the scout's s1 and s2; `8c72cfed` the `DATE` re-pin at the freeze — digest `86865d1aebba8468`; proven on the live v2.37 and v1.2 at authoring 2026-09-15, at the re-pin and at walk 0; `fe165999` its first). Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-dc-w30-2026-09-16.md`.

**Tier computed, not judged (§1):** **T-6 fires** — `DRAFTING_CYCLE.md` is the drafting-cycle law and `PANEL_SEAT_TEMPLATE.md` its seat template (the files this cycle runs under) → **T2**. T-2 fires (seventeen canonical rows flip; caps at T1). T-1 fires (two repos, plus the DB). T-8 does NOT fire — a structure-for-structure clone of PT tranche six (#100122) carrying 540's extension form onto two files; fourteen edits against ten, no new numbered rule, no span, is scale, not shape. **Panel form — the magnitude proxy, measured at walk 0 (all three must hold): (i) structure-clone** — the parent is #100122 (closed 2026-09-16 15:22, the newest doctrine tranche), and both walk-0 clone-diffs ran (against its final text and its register); **(ii) mechanized edits** — every mutation of either file flows through the committed builder, whose fourteen anchors are count-1-asserted BY EXECUTION and whose 44 + 11 post-conditions are asserted on the two outputs before any write, with the line and byte deltas asserted against pinned constants per file (proven at authoring and at walk 0 on the LIVE v2.37 and v1.2: byte-identical rebuilds, a `16	2` and a `4	3` numstat, the byte pin firing on an off-by-one copy of either constant, the three refusals); the flip is one CAS statement under a structural pre-check; **(iii) scripted probes** — every new token measured 0 against the pre-edit files, the invariant tokens at their MEASURED pre-edit counts. **→ SMALL form: the walk-0 scout, then EXECUTION, then CAPSTONE — the seats on the Air when its session takes the work up within a few minutes (thread 382), else local subagents.** The CEO may call the full form.

## Why this exists

Gate 1 routed 86 proposals `accepted|codify`; the six PLANNER_TEMPLATE tranches landed sixty-nine of them (v4.112 by #100109 through v4.118 by #100122). This tranche is the packet's seventh and last row: the sixteen rows about the DRAFTING CYCLE's own rules and the one about the PANEL SEAT — three further walk-0 acts (an anchor pinned by its enclosing block, the consumer dry-run at the consumer's own entry point with the fail-open fallback audited, the whole suite under the change in a scratch archive and every gate command of a cross-repo plan against an archive of the target repo), five cold-panel bullets (two cold readers read as a pair, a seat finding against a ruling is a CEO question, one disposition per seat finding, falsify the claim that buys something, budget the scout, the execution seat and the capstone), four cross-cutting rules (the exit read as the very next statement with explicit exit checks across heredocs, a search window bounded at machine-written lines, the verdict recorded verbatim with the owned detector run and the claim-sweep, the walk close running `lens_order_check`), a fold post-condition (a fold-added instruction is new v0 for the standing-rules diff), the cold-panel line's own rule in §3 (panel rounds on the cold-panel line; `wN` lines are warm walks only), and the seat template's read-only contract naming the writers a seat must not call with its execution discipline binding test-shaped scratch code to pytest. Their homes are the packets', re-verified against the live v2.37 and v1.2 at authoring by the builder's own post-conditions (every new token 0 on the live file, its count on the build — the builder's `DC_POST` and `PST_POST` lists, 44 and 11 probes). **Homes:** DRAFTING_CYCLE — §2.0 (D1: acts (7)–(9)), §2.6 (D2: five bullets), §2.7 (D3, D4 probe integrity; D5 record integrity; D6 the per-lens bullet; D7 fold post-conditions), §3 (D8), the version line (D9), the History row (D10); PANEL_SEAT_TEMPLATE — §2 (P1), §5 (P2), the version line (P3), the History row (P4) — each landed text opening in bold and carrying its proposals, entries and `codified 2026-09-16 (Gate 2, thread 336 — the DC/PST tranche)` — inline in the opener for D1, P1 and P2, in the file's trailing `*(Proposal …)*` Source line for D2–D8 (12 such strings in DC, 2 in PST). The Rule 19 keyword scan of the builder's added lines at authoring (word-bounded, the canonical list, both files): none.

## What this plan does

**In the governance checkout, by absolute path (`GOV=/Users/marklehn/Developer/eluvian-governance`), fourteen anchored edits to TWO files — `DRAFTING_CYCLE.md` (ten) and `PANEL_SEAT_TEMPLATE.md` (four) — by the committed builder in ONE run (never by hand):**
- **D1** — §2.0: after the walk-0 half's Source line (anchor at 64 of the live v2.37), acts (7)–(9) — 475, 478, 560 + 585. **D2** — §2.6: before the `### 2.7` heading (anchor 146), five bullets — 445, 450, 451, 452, 454. **D3** — §2.7 probe integrity: after the proposal-237 rule (anchor 179), the exit-read rule — 468 + 473. **D4** — after the proposal-253 rule (anchor 182), the machine-written-window rule — 448. **D5** — §2.7 record integrity: at the `cp`-plus-assert sentence (anchor 204), the verbatim-verdict rule — 479. **D6** — the per-lens bullet (anchor 196): the walk close runs `lens_order_check` — 531. **D7** — fold post-conditions (anchor 150): a fold-added instruction is new v0 — 586. **D8** — §3: before the Cycle Manifest stanza paragraph (anchor 257), the cold-panel line's rule — 466. **D9** — `**Version:** 2.37 (2026-09-11)` → `**Version:** 2.38 (2026-09-16)` (line 5). **D10** — the History row above the 2.37 row (anchor 342).
- **P1** — PANEL_SEAT_TEMPLATE §2 (anchor 13): the writers named — 543. **P2** — §5 (anchor 22): test-shaped scratch code under pytest inside `tests/` — 543. **P3** — `**Version:** 1.2 (2026-08-21)` → `**Version:** 1.3 (2026-09-16)` (line 5). **P4** — the History row above the 1.2 row (anchor 39).

The builder reads both files as bytes, refuses CRLF, refuses an already-built input, asserts each anchor's count is 1, applies the ten DC edits and the four PST edits, asserts 44 post-conditions on the DC OUTPUT and 11 on the PST OUTPUT (the builder's `DC_POST` and `PST_POST` lists: the codified string at its count; every landed opener once; the new version lines 1 and the old 0; the new History rows 1 with the 2.37 / 1.2 rows below them 1; the section headings surviving at their measured counts), asserts the line and byte deltas against PINNED constants per file (`DC_EXPECTED_LINES = 14`, `DC_EXPECTED_BYTES = 20191`, `PST_EXPECTED_LINES = 1`, `PST_EXPECTED_BYTES = 1899` — post-conditions that can fail, proven at authoring on off-by-one copies) and only then writes BOTH outputs — to SCRATCH paths it refuses to let be the live files or anything under the governance root or the inputs' git toplevel; if either file's assertions fail, neither is written. Apply = copy the two proven scratch outputs over the two live files, then re-measure both live files.

**In the lessons DB (`/Users/marklehn/Developer/forge_lessons/lessons-forge.db`, the mini's one live copy), AFTER the doctrine commit:** one scoped CAS flip — `id IN (445,448,450,451,452,454,466,468,473,475,478,479,531,560,585,586,543) AND status='accepted' AND route='codify'` → `implemented`, `status_updated_by='ceo'` (the delegation's record), a fresh UTC stamp — under a structural pre-check (a TEMP-TABLE `CHECK(x=17)` that aborts the transaction before the UPDATE if the seventeen are not all `accepted|codify`), with the capture of every row with `id <= 590` — the whole table at authoring — `(id, status, route, category, status_updated_by, status_updated_at)` to a SCRATCH file BEFORE the UPDATE (copied into the worktree's evidence dir afterwards — never written into the canonical checkout), and the sentinels by name: `PRE_F=17`, `CHANGES_F=17`, `EXCL_F=17` (the seventeen rows now `implemented` with a stamp that is NOT one of the TWO vintage stamps — 445, 448, 450, 451, 452, 454 and 466 carry W=29's `2026-09-02T23:54:37Z`, the ten W=30's `2026-09-13T18:27:07Z`), `ACC_POST=0`, `IMPL_POST=419`. After this flip NO accepted row remains: Gate 2 over W=29 and W=30 is complete. The flip SQL is a committed file in governance; a backup of the DB precedes it.

**In the bellows worktree:** the dev log and the QA evidence.

## What this plan does NOT do

- Does not touch `PLANNER_TEMPLATE.md` (the six PT tranches are landed; v4.118 is live), COMPANY.md, MACHINE_SETUP.md, GLOSSARY.md, code, or threads. Does not push (the Planner pushes after the pause; the Planner's Rule 22 (b) read at the pause takes the canonical block's stdout and the daemon's gate row as the receipt's evidence, never the QA's `PRECHECK: 0 failure(s)` line — at a QA step `tools/check_deposit.py` verifies presence alone, thread 362). Does not project the `LESSONS.md` markers (the Planner's act after the close — `forge_lessons/scripts/project_status_markers.py --apply`, the Rule 112 instrument). Does not close thread 336 (the Planner's `threads done` at the keyboard, after this plan's Done).
- ⚠️ **Runs after PT tranche six (#100122, 2026-09-16) and BEFORE any W=31 ingest** — A0's ladder asserts the accepted population (17 before this plan's flip, 0 after) with every accepted row at one of the two vintage stamps; a W=31 ingest could stale an accepted row (`accepted` is not terminal — `forge_lessons/src/lessons_forge.py:31`). The 74 `[status: pending]` headings written since W=30's ingest wait (the marker projection at governance `4b2e2b03`, 2026-09-16 15:23, is the newest; its dry run at this derivation counted `unmatched (no DB row): 74` — re-read at the freeze).
- ⚠️ **The two files' versions are moved by the CEO's doctrine acts at other plans' closes** (DRAFTING_CYCLE last moved at v2.37 on 2026-09-11, PANEL_SEAT_TEMPLATE at v1.2 on 2026-08-21 — neither moved during the six PT tranches): the re-pin surface is the PIN TABLE and its restatements — P1 (both shas, lines, bytes); P2 (every anchor line number, which an act above an anchor shifts, and the two version anchors); P4 (the builder's digest, after its `DC_OLD`/`PST_OLD` constants or `DATE` are re-committed); P5 (both `wc -l`, `wc -c`; the BUILT line's deltas hold unless an act lands inside one of the ten content homes (D1–D8, P1–P2), which an anchor count or a byte pin refuses); Task C's version tokens; the `wc -l` restated in A2, A3, both RE-ENTRY arms and QA Item 1 and the `wc -c` in A2; every literal of the new versions (the title, the D9/P3 lines, the POST description, MUST-PRESERVE, P3, the RE-ENTRY text, Task C, the THREE commit subjects) and the pre-edit-state literals; the register's Targets line and title — re-pinned TOGETHER at the freeze and before the release by ONE scripted pass over those sites, recorded in the register with each literal's count before and after, and Task C's probes re-run on scratch builds of the new versions; the historical mentions keep their versions; a moved version at A0 is a NONE-MATCH halt and a redeposit, never a guess.

## MUST-PRESERVE

- ⚠️ **TWO REPOSITORIES AND A DB, ONE STEP.** Governance by absolute path, `git -C "$GOV"` never `cd`; porcelain EMPTY and both shas = P1 before touching `DRAFTING_CYCLE.md` or `PANEL_SEAT_TEMPLATE.md`; commit by explicit pathspec with the message on stdin; no push. The DB by absolute path; `sqlite3` with `-bail`; a backup (`pre-gate2-dc-w30-<HHMMSS>.db` at the forge root, untracked) BEFORE the flip.
- **The builder is the only editor of the two files.** Its committed blob digest must equal the on-disk file (P4) before it runs; it runs scratch→scratch on both files in one run; each live file is replaced only by `cp` of its scratch output whose post-conditions the builder already asserted, and both live files are then re-measured (Task C); the two `cp`s and the ONE commit that carries both files are one act — a death between the first `cp` and the commit is the ONE recipe's shape.
- **Doctrine before DB (540's C6):** the flip runs only after the governance commit has landed; a HALT between leaves v2.38 and v1.3 committed-unpushed and the flip owed — the RE-ENTRY arm in A0 resumes at the flip.
- ⛔ **Never run any `checkout --` on an A0 halt except the ONE recipe named there** (a pre-commit apply that died, discriminated by the two HEAD numstats, the two porcelains and the two tokens — `(7) **An insertion anchor is pinned by its ENCLOSING BLOCK` in DC, `**This mandate names the WRITERS you must not call, by name` in PST); a pin mismatch otherwise means the file carries changes that are not this plan's.
- ⚠️ **NO landed text carries a Rule 20 hedging keyword** — the scan of the builder's added lines against the canonical block's list (at authoring, on the live build) returned nothing; still, NO receipt row quotes a landed paragraph's body: Task C pins heading fragments and opener fragments only, and the receipt cites counts
- **`known_failures: 0`.** The suite runs in the worktree under the canonical venv; the QA reads its own run's failure count, and a bellows `main` that gained tests since P7 is stated in `probes-raw.txt` under Item 4 (the probe's command and the hashes it printed; the summary line stays in the suite file), never in the receipt's row and never halted on. Any failure is a HALT/Critical.
- ⛔ **`probes-raw.txt` carries no summary-shaped line** — no `N passed` restatement of P7, no pasted suite line, no commit log beyond the path-scoped drift probe's: the suite gate (`gates._select_summary_txt`, bellows `91354d2a`) chooses, among the `.txt` deposits that carry a pytest summary, a basename carrying `suite` or `full`, then the largest `passed` count, then the first match — so a summary-shaped line in the raw file is the one read whenever the suite file crashed without a summary or lost its name (thread 363's defect; its "first `.txt`" wording is narrower than the live gate, measured 2026-09-15 on `71c6bfc3`), and it is forbidden here by name so the selection never rests on that tie-break (tranche one's QA wrote "P7 baseline: 2395 passed, 2 skipped" as a header there — harmless beside a named suite file, forbidden here). P7 is cited as "P7" in the raw file, never restated with its numbers.
- ⛔ **Every commit in the bellows worktree chains `tools/check_deposit.py` before `git add`** (Rule 23; the step IS bound — the commit binding was installed by bellows `4bcde8a9` (#100105, thread 333 — `hooks/git/pre-commit` and the daemon's `BELLOWS_PRECHECK_*` env), and the six PT tranches' chains ran under it), and puts its message on stdin (`git commit -F -` with a quoted heredoc), never in a file.

## Numbers discipline — the pins DEV re-derives (measured 2026-09-16 by the Planner's derivation script on the LIVE DRAFTING_CYCLE v2.37 at governance `9df017c5` and PANEL_SEAT_TEMPLATE v1.2 at `3d2c1aae`, bellows main `8e7c4609`, forge_lessons `a1ebf45`; re-measured at the freeze)

| pin | what | value | how |
|---|---|---|---|
| P1 | **`DC_SHA`, `PST_SHA`**, lines, bytes — pre-edit (the LIVE v2.37 and v1.2) | DC `8d9a0a87797e0de8`; 389 lines; 189,685 bytes — PST `82c0ff1626f7b1a5`; 42 lines; 6,193 bytes | `shasum -a 256 "$GOV/DRAFTING_CYCLE.md" \| cut -c1-16`; the same for `PANEL_SEAT_TEMPLATE.md`; `wc -l`; `wc -c` |
| P2 | **`ANCHORS`** — the builder's ten DC anchors and four PST anchors | each count 1 on its file: DC — D1 64 (the walk-0 half's Source line) · D2 146 (`### 2.7`'s heading with its preceding blank line) · D3 179 (the proposal-237 rule's Source line and the guard bullet) · D4 182 (the proposal-253 Source line and the probe-integrity bullet) · D5 204 (the `cp`-plus-assert sentence) · D6 196 (the per-lens bullet's record sentence) · D7 150 (the proposals-347-348 Source line) · D8 257 (the Cycle Manifest stanza paragraph with its preceding blank line) · D9 `**Version:** 2.37 (2026-09-11)` (5) · D10 the History header with the 2.37 row (342); PST — P1 13 (the fold-nothing sentence) · P2 22 (the expected-vs-measured sentence) · P3 `**Version:** 1.2 (2026-08-21).` (5) · P4 the History header with the 1.2 row (39) — the line numbers measured on the live files, re-measured at the freeze | `/usr/bin/grep -cF` (the multi-line anchors by the builder's own assertion) |
| P3 | **`TOKENS`** 0 before, their counts after | the builder's `DC_POST` (44) and `PST_POST` (11) lists — Task C below is those lists verbatim: every new token 0 on its live file and at its listed count on the build; the retired version lines 1 before, 0 after; the invariant headings and the older History rows at 1 (unchanged by this plan) | `/usr/bin/grep -cF` on each file |
| P4 | **`BUILDER`** — on-disk digest = the blob at its OWN last commit | `86865d1aebba8468` (governance `f83bbb29` — the builder's own last commit: the walk-0 re-pin after the scout's s1 and s2; `8c72cfed` the `DATE` re-pin at the freeze, `fe165999` its first) | `shasum -a 256 <builder> \| cut -c1-16`; `BC=$(git -C "$GOV" log -1 --format=%H -- governance/knowledge/decisions/drafts/build-gate2-dc-w30-2026-09-15.py); git -C "$GOV" show "$BC:governance/knowledge/decisions/drafts/build-gate2-dc-w30-2026-09-15.py" \| shasum -a 256 \| cut -c1-16` |
| P5 | **`DRY_RUN`** — the success line, numstats, sizes | `BUILT: DC <dc-out> edits=10 lines+14 bytes+20191 post=44/44; PST <pst-out> edits=4 lines+1 bytes+1899 post=11/11 deltas=pinned`; `git diff --no-index --numstat -- "$GOV/DRAFTING_CYCLE.md" <dc-out>` → `16	2` and the PST pair → `4	3` (exit 1 is the expected differing state — read the numbers, never the exit); `wc -l` → 403 and 43; `wc -c` → 209,876 and 8,092 (the outputs `700bb69663d9a87e` and `34abdc6c40ab0eff`) | as stated, scratch-only |
| P6 | **`FLIP_PRE`** — the seventeen rows | 445, 448, 450, 451, 452, 454, 466 each `accepted\|codify\|planner\|2026-09-02T23:54:37Z`; 468, 473, 475, 478, 479, 531, 543, 560, 585, 586 each `accepted\|codify\|planner\|2026-09-13T18:27:07Z`; `SELECT COUNT(*) … WHERE status='accepted'` → 17; `implemented` → 402; `SELECT status_updated_at, COUNT(*) … WHERE status='accepted' GROUP BY 1` → exactly two rows, `2026-09-02T23:54:37Z\|7` and `2026-09-13T18:27:07Z\|10`; `SELECT MAX(id), COUNT(*) FROM lesson_proposals` → `590|590` | `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db` |
| P7 | **`SUITE`** — the expectation | `2524 passed, 2 skipped`, exit 0 — bellows main `8e7c4609` (the newest QA's commit, 2026-09-16), the last suite file on main (`full-suite-gate2-pt-w30-f.txt`) — the drift probe is anchored at `8e7c4609`; re-read at the freeze; the gate reads failures, and `main` moves | `BPY -m pytest tests -q -p no:cacheprovider` |
| P8 | **`NO_INGEST`** (precondition, in two halves) | UNCONDITIONAL (before the ladder): `SELECT COUNT(*) FROM lesson_entries` → 580 (no W=31 ingest) AND `ls /Users/marklehn/Developer/forge_lessons/knowledge/decisions/ \| /usr/bin/grep -cF -- 'forge-cycle-w31'` → 0 (a zero-match count prints 0 and exits 1 — read the number). ARM-DEPENDENT (ladder step (5)): the accepted population with its at-the-vintages count — `17\|17` on FRESH and flip-owed; `0\|0` on complete — the seventeen at their vintages on FRESH and flip-owed (ladder (4)) | as stated |

## Drafting Cycle

**Tier:** T2 — T-6 (the cycle's law and its seat template), T-2 (seventeen canonical rows, capped), T-1 fire; T-8 does not (a structure clone). **Panel: SMALL form** (the magnitude proxy measured at walk 0, all three conditions holding) — the walk-0 scout, then EXECUTION, then CAPSTONE, on the Air's session by cross-session message when it takes the work up within a few minutes (thread 382), else a local fresh-context seat at once; the CEO may call the full form.
**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-dc-w30-2026-09-16.md
**Walk 0 (context pin, measured):** the v0 derived by script from PT tranche six's closed text (`executable-gate2-pt-w30-f.md` at `94989a79`; #100122 Done 15:22:46), the sections naming the target rewritten whole from measured values for the two-file shape, the script proven dry before the Done and run live after it, every number measured on the LIVE v2.37 and v1.2 at run time, the scans read, every named path checked (M1); the fourteen anchors each count 1 at their measured lines (M2); the seventeen entries and their Gate 1 rows read, the builder's two POST lists the dedup measurement (M3); the live v2.37 `8d9a0a87797e0de8` / 389 / 189,685 and v1.2 `82c0ff1626f7b1a5` / 42 / 6,193, the builder `86865d1aebba8468` = its blob at `f83bbb29` (M4; `03df7ec7e8bb2438` at `8c72cfed` before the scout's s1 and s2); every home's last writer by blame (M5); the dry run on the live files `DC lines+14 bytes+20191 post=44/44; PST lines+1 bytes+1899 post=11/11`, the outputs `700bb69663d9a87e` / 403 / 209,876 and `34abdc6c40ab0eff` / 43 / 8,092, numstats `16 2` and `4 3`, BYTE_IDENTICAL on a rebuild of both, the three refusals, either off-by-one constant refusing both files, the pre-act v2.36 refused on the version anchor, CRLF refused, the Rule 19 scan none (M6); the flip rehearsal owed to the scout (M7); the DB `7|10` of 17 accepted, 402 implemented, 590 rows, 580 entries — the last accepted rows (M8); the suite `2524 passed` at `8e7c4609`, the drift probe empty (M9); the consumers under pytest `WRITES 9`, `ASSIGNED shop-infra`, the extractor's five and three paths (M10); `plan_lint` 0 FAIL, the WARN set read, the baseline armed after this register existed (signals=14), `propagation_check` 164 all detector (1) (M11). Written 15:23 from the printed output. Verdict at walk 0: the magnitude proxy holds → SMALL form; the scout convened on the v0.

- Weak spots:          w1 dry; w2 dry
- Destruction:         w1 dry; w2 dry
- Vulnerabilities:     w1 dry; w2 dry
- Integration-record:  w1 dry; w2 dry
- ACID:                w1 dry; w2 dry
**Cold panel (SMALL form):** _not yet convened — the meter opens at the scout's dispatch._
**Closing:** ✅ **BAR MET — walk 2 dry on every lens after the EXECUTION seat's zero findings (the Air, `aa8a5bff`), walk 1 dry ×5 after the scout's seven walk-0 folds (two of them in the builder, re-pinned at `f83bbb29` — the History row's literal builder basename, and D5 held to the Gate 1 reader's routing); the CAPSTONE seat (the Air, `9cbbabff`) read the fold set and the record with zero findings. The content — ten DRAFTING_CYCLE edits (walk-0 acts (7)–(9), five §2.6 bullets, five §2.7 rules, the §3 cold-panel line) and four PANEL_SEAT_TEMPLATE edits by one builder in one act, the seventeen rows' flip — is the text the three seats read, its freeze checklist MATCHING on the LIVE v2.37 and v1.2 at every seat (P1–P8, Task C 55/55), the flip rehearsed by the scout on its own backup to zero accepted rows. With this plan Gate 2 over W=29 and W=30 is complete: no accepted row remains. Residue: none. Direction verdict after walk 1: PROCEED. Released under the CEO's delegation of 2026-09-15 ("release at the proper time"), after #100122's Done and the v4.118 act; never two bellows plans concurrently.**

## Cycle Manifest
tier: T2
target: /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md
class: shop-infra
reads: /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md, /Users/marklehn/Developer/eluvian-governance/PANEL_SEAT_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-dc-w30-2026-09-15.py, /Users/marklehn/Developer/eluvian-governance/gate2-packet-2026-09-15.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-02.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-a.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-b.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-c.md, /Users/marklehn/Developer/eluvian-governance/LESSONS.md, knowledge/decisions/Done/executable-100122.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-540.md, /Users/marklehn/Developer/forge_lessons/lessons-forge.db
writes: /Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md, /Users/marklehn/Developer/eluvian-governance/PANEL_SEAT_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2dcw30-flip.sql, /Users/marklehn/Developer/forge_lessons/lessons-forge.db, knowledge/development/dev-log-gate2-dc-w30-2026-09-16.md, knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt, knowledge/qa/evidence/gate2-dc-w30-2026-09-16/qa-receipt.md, knowledge/qa/evidence/gate2-dc-w30-2026-09-16/probes-raw.txt, knowledge/qa/evidence/gate2-dc-w30-2026-09-16/full-suite-gate2-dc-w30.txt
open_forks: 1. no accepted row remains after this flip — Gate 2 over W=29 and W=30 closes with it; thread 336 closes at the keyboard; 2. the `LESSONS.md` markers are projected after the close by the Planner, not by this plan; 3. P7's suite count moves with bellows main and is re-pinned at the freeze; 4. the two files land in ONE governance commit — the ladder's arms read that commit through DRAFTING_CYCLE.md's history, PANEL_SEAT_TEMPLATE.md's checked beside it
walks: 2
yields: 0, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:167
coherence: 2/2 body walks named in the register (7 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-gate2-dc-w30.md.foldcheck.json

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the bellows Developer. ⚠️ Governance runs IN PLACE at `$GOV` (every operand ABSOLUTE); commits there by explicit pathspec; ONE action per compound. ⛔ Every commit's message goes to `git commit -F -` on stdin — a quoted heredoc ending the commit's own command: `<<'MSG'`, the message on the lines after it, `MSG` alone on the last — so no file is written for it. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** (`BPY`); the builder runs under `python3` as the PT tranches' did (it imports only the stdlib — `os`, `sys`, and `subprocess` for the toplevel guard).
>
> ⛔ **A0 — roots, the precondition, then the branch ladder (catch-all LAST):** `cd "$(git rev-parse --show-toplevel)" && [ -f bellows.py ] && [ -d tests ] && echo TREE_OK` — HALT unless TREE_OK; `GOV=/Users/marklehn/Developer/eluvian-governance; DC="$GOV/DRAFTING_CYCLE.md"; PST="$GOV/PANEL_SEAT_TEMPLATE.md"; B="$GOV/governance/knowledge/decisions/drafts/build-gate2-dc-w30-2026-09-15.py"; DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db; [ -f "$DC" ] && [ -f "$PST" ] && [ -f "$B" ] && [ -f "$DB" ] && echo GOV_OK` — HALT unless GOV_OK. Re-derive `GOV`, `DC`, `PST`, `B`, `DB` in every compound. `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python`. **P8's UNCONDITIONAL half → both values, else HALT** (`lesson_entries` → 580 and the `forge-cycle-w31` count → 0; on a mismatch: `a W=31 cycle has started — re-pin P6/P8 and re-deposit`). **P8's accepted-population half is read INSIDE the ladder, never before it:** it changes when THIS plan's flip lands. Then the ladder — **(1)** P1's two shas match; **(2)** `git -C "$GOV" status --porcelain -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md governance/knowledge/development/g2dcw30-flip.sql` (state it; EMPTY on FRESH); **(3)** `git -C "$GOV" log -1 --format=%s -- DRAFTING_CYCLE.md` — does the subject carry `gate2-dc-w30`? (the ONE commit carries both files; `git -C "$GOV" log -1 --format=%s -- PANEL_SEAT_TEMPLATE.md` must read the SAME subject whenever (3) is yes, else NONE-MATCH); **(4)** `sqlite3 -readonly "$DB" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (445,448,450,451,452,454,466,468,473,475,478,479,531,560,585,586,543) ORDER BY id;"`; **(5)** `sqlite3 -readonly "$DB" "SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted')||'|'||(SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z'));"` — the accepted population and how many of it sit at the two vintage stamps (every accepted row does, on EVERY arm).
> - **FRESH** = (1) both match AND (2) empty AND (3) no AND (4) the seventeen `accepted|codify` at their vintages (445, 448, 450, 451, 452, 454, 466 at `2026-09-02T23:54:37Z`, the ten at `2026-09-13T18:27:07Z`) AND (5) `17|17` → A1.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND (2) EMPTY for both files (a dirty file on re-entry is a FOREIGN edit — HALT untouched, quoting the porcelain) AND (4) the seventeen still `accepted|codify` at their vintages AND (5) `17|17` → verify the COMMITTED state (the Task C counts on both live files, `wc -l` 403 and 43, and `C=$(git -C "$GOV" log -1 --format=%H -- DRAFTING_CYCLE.md); git -C "$GOV" show --stat --format= "$C" -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md` → two files, 16 + 4 insertions, 2 + 3 deletions — the PATH-SCOPED commit, never `HEAD`: once A4's SQL commit or any later governance commit lands, `HEAD` no longer names the doctrine commit and a `HEAD`-scoped show prints NOTHING with exit 0), then A4 (the flip) and A5 only. If (2) shows `g2dcw30-flip.sql` untracked or modified with the flip NOT applied, that is this arm too — A4 rewrites the file from the exact text, which is idempotent.
> - **RE-ENTRY (complete)** = (3) yes AND (2) EMPTY for both files AND (4) the seventeen `implemented|codify` with every stamp NOT one of the two vintages AND (5) `0|0` → verify the COMMITTED state (the Task C counts, both `wc -l`, and the path-scoped show → the two files' 16/2 and 4/3); then the two idempotent tails of A4: the SQL file — if `git -C "$GOV" log -1 --format=%s -- governance/knowledge/development/g2dcw30-flip.sql` does not carry `gate2-dc-w30`, write it from the exact text and commit it; the capture — if `/tmp/g2dcw30/flip-capture.txt` exists copy it into the worktree, else reconstruct the worktree copy from the newest pre-flip backup — `BK=$(ls -t /Users/marklehn/Developer/forge_lessons/pre-gate2-dc-w30-*.db | head -1)` (each A4 attempt mints a second-stamped file; the newest is the one whose flip landed), then `[ -f "$BK" ] && echo "BK=$BK"` and `sqlite3 -readonly "file:$BK?immutable=1" "SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted'"` → 17 (THIS plan's pre-flip population; any other number is a HALT quoting every `pre-gate2-dc-w30-*.db` with its accepted count — a backup reading 0 was minted by a second A4 attempt after the flip landed, and the capture is reconstructed from the newest backup reading 17, named in the HALT for the Planner, never chosen by the DEV) — no file is a HALT (`no pre-flip backup found — the capture cannot be reconstructed; re-deposit`), never an empty `$BK` handed to sqlite, which would open a scratch database and fail on the table — `sqlite3 -readonly "file:$BK?immutable=1" "<the capture SELECT>"` → the same 590 lines, labelled `RECONSTRUCTED (post-commit re-entry)` in the dev log); then A4's read-back (no flip) and A5 with the dev log marked `re-derived on re-entry`.
> - **NONE-MATCH** = anything else → HALT quoting every measurement — and when (5)'s SECOND number ≠ its first on ANY arm, the HALT reads `an accepted row carries a stamp outside the two vintages — an ingest or a foreign flip has moved the population; re-pin P6/P8 and re-deposit`; when (3) = no and (4) reads the seventeen `implemented` with (5) `0|0`, it reads `this plan's flip has landed and a later act moved DRAFTING_CYCLE.md after its commit — find the doctrine commit with git -C "$GOV" log --format=%h --grep=gate2-dc-w30 -- DRAFTING_CYCLE.md, verify it as the complete arm does (its show --stat → the two files), then run the complete arm's idempotent tails`; when (3) = no and (5)'s first number ≠ 17 with the two numbers equal and (4) still reads `accepted`, it reads `the accepted set moved — an ingest staled an accepted row (accepted is not terminal, forge_lessons/src/lessons_forge.py:31); re-pin P6/P8 and re-deposit`; when (1) mismatches on either file and (3) = no and both porcelains are EMPTY, it reads `DRAFTING_CYCLE.md or PANEL_SEAT_TEMPLATE.md moved under this plan — a doctrine act landed after the freeze; re-pin P1 and the builder's version constants, and re-deposit`. The ONE recipe: (1) mismatch on one or both files + (3) no + (4) the seventeen `accepted|codify` at their vintages + (5) `17|17` = a pre-commit apply that died — BUT ONLY IF the dirty files are THIS plan's builds: `git -C "$GOV" diff HEAD --numstat -- DRAFTING_CYCLE.md` → `16	2` and/or `git -C "$GOV" diff HEAD --numstat -- PANEL_SEAT_TEMPLATE.md` → `4	3` (against HEAD, never the bare index-relative `diff`, which prints NOTHING in the staged half-state this recipe exists for) AND each dirty file's porcelain → ` M` or `M ` (never `MM`: a staged foreign edit beneath an unstaged build would pass the HEAD numstat and be discarded) AND the file's token — `/usr/bin/grep -cF -- '(7) **An insertion anchor is pinned by its ENCLOSING BLOCK' "$DC"` → 1 for DC, `/usr/bin/grep -cF -- '**This mandate names the WRITERS you must not call, by name' "$PST"` → 1 for PST (a foreign hand edit presents the SAME (1)+(3)+(4)+(5) shape and must never be discarded; a dirty file whose numstat, porcelain or token differs → HALT untouched, quoting it) → `git -C "$GOV" checkout HEAD -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md` (`HEAD`, not the index — a death between `add` and `commit` leaves the builds STAGED), re-assert P1 on both, then A1. Every other shape HALTs untouched.
>
> ⛔ **A1 — re-derive P1–P6; state each; a mismatch is a HALT quoting both.** (P4 with the builder's OWN commit, never `HEAD:`.)
>
> **A2 — dry-run, scratch→scratch (LITERAL paths, no variables across compounds):** `rm -rf /tmp/g2dcw30; mkdir -p /tmp/g2dcw30; python3 "$B" "$GOV/DRAFTING_CYCLE.md" /tmp/g2dcw30/DC-out.md "$GOV/PANEL_SEAT_TEMPLATE.md" /tmp/g2dcw30/PST-out.md; echo "builder_exit=$?"` → P5's `BUILT:` line verbatim, `builder_exit=0`; `git diff --no-index --numstat -- "$GOV/DRAFTING_CYCLE.md" /tmp/g2dcw30/DC-out.md` → `16	2`; the PST pair → `4	3`; `wc -l /tmp/g2dcw30/DC-out.md` → 403; `wc -l /tmp/g2dcw30/PST-out.md` → 43; `wc -c` → 209876 and 8092. Refusals (each `BUILDER REFUSED`, nonzero; quote each): `python3 "$B" "$GOV/DRAFTING_CYCLE.md" "$GOV/DRAFTING_CYCLE.md" "$GOV/PANEL_SEAT_TEMPLATE.md" /tmp/g2dcw30/x.md` (out == in); `python3 "$B" "$GOV/DRAFTING_CYCLE.md" "$GOV/x.md" "$GOV/PANEL_SEAT_TEMPLATE.md" /tmp/g2dcw30/x.md` (under a forbidden root — the literal governance root, or the input's git toplevel; no `x.md` is written under `$GOV`); `python3 "$B" /tmp/g2dcw30/DC-out.md /tmp/g2dcw30/DC-out2.md /tmp/g2dcw30/PST-out.md /tmp/g2dcw30/PST-out2.md` (already built).
>
> **A3 — apply and measure BOTH LIVE files (Task C), then ONE commit:** `cp /tmp/g2dcw30/DC-out.md "$GOV/DRAFTING_CYCLE.md" && cp /tmp/g2dcw30/PST-out.md "$GOV/PANEL_SEAT_TEMPLATE.md"`; then from the live files, counts read never exit codes (`/usr/bin/grep -cF -- '<token>' "$DC"` and `"$PST"`, each token as one single-quoted literal): on DRAFTING_CYCLE.md — `codified 2026-09-16 (Gate 2, thread 336 — the DC/PST tranche)` 12 · `**Three further walk-0 acts, each an EXECUTION` 1 · `(7) **An insertion anchor is pinned by its ENCLOSING BLOCK` 1 · `(8) **The sixth act's consumer dry-run enters at the consumer's OWN outermost entry point` 1 · `(9) **Run the whole suite under the change at drafting, in a scratch archive**` 1 · `- **A cold seat's dry return is a statement about that READER` 1 · `- **A seat finding that would change what a RULING decided is a CEO question` 1 · `- **Every seat finding gets exactly ONE disposition` 1 · `- **Hand every cold reader the claim that BUYS something` 1 · `- **Budget the panel where the HIGHs come from` 1 · `- **The exit read is the VERY NEXT statement` 1 · `- **Bound a search window at MACHINE-WRITTEN lines` 1 · `- **A tool's verdict is recorded VERBATIM` 1 · `**The WALK close runs `cycle_check` AND `lens_order_check` in the same act` 1 · `- **A fourth duty of every fold: an instruction a fold ADDS is new v0` 1 · `**Panel rounds are recorded on the block's cold-panel line` 1 · `**Version:** 2.38 (2026-09-16). Amended only through the Iteration Protocol (§6).` 1 · `**Version:** 2.37 (2026-09-11). Amended only through the Iteration Protocol (§6).` 0 · `- **2.38 (2026-09-16):** slug gate2-dc-w30-2026-09-16` 1 · `- **2.37 (2026-09-11):**` 1 · `- **2.23 (2026-09-01):**` 1 · `### 2.0 ` 1 · `### 2.6 ` 1 · `### 2.7 ` 1 · `### 2.8 ` 1 · `## 3. ` 1 · `## 6. ` 1 · `## History` 1 · `#### Fold post-conditions` 1 · `#### Probe and measurement integrity` 1 · `#### Record and attestation integrity` 1 · `#### Walk cadence` 1 · `*(Proposal 237, codified 2026-08-11.)*` 1 · `*(Proposal 253, codified 2026-08-11.)*` 1 · `*(Proposal 313, codified 2026-08-11.)*` 1 · `A DECISION fold is a fold ROUND` 1 · `Aim the panel at the premises that LICENSE a deletion` 1 · `*(Proposals 347 + 348, entries 339 + 340, codified 2026-08-14; the tool shipped first, plan 418.)*` 1 · `**The `## Cycle Manifest` stanza: a fixed `key: value` block emitted at BAR_MET.**` 1 · `When cloning a plan, diff the machinery` 1 · `- **Any marker whose practical meaning` 1 · `- **Three probe-integrity clauses` 1 · `- **When a guard's safety depends on a claim about text` 1 · `- **After a walk's folds land` 1; on PANEL_SEAT_TEMPLATE.md — `codified 2026-09-16 (Gate 2, thread 336 — the DC/PST tranche)` 2 · `**This mandate names the WRITERS you must not call, by name` 1 · `**A scratch script that exercises test-shaped code runs under `pytest` inside `tests/` or not at all**` 1 · `**Version:** 1.3 (2026-09-16).` 1 · `**Version:** 1.2 (2026-08-21).` 0 · `- **1.3 (2026-09-16):**` 1 · `- **1.2 (2026-08-21):**` 1 · `### 2. Read-only contract` 1 · `### 5. Execution discipline` 1 · `## History` 1 · `## Per-seat slots` 1; `wc -l "$DC"` 403; `wc -l "$PST"` 43; `cmp /tmp/g2dcw30/DC-out.md "$DC"` silent; `cmp /tmp/g2dcw30/PST-out.md "$PST"` silent; `git -C "$GOV" diff --stat -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md` → two files, 16 + 4 insertions, 2 + 3 deletions. Any failing → FRESH: `git -C "$GOV" checkout -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md` + HALT; RE-ENTRY: HALT, no restore. Commit BOTH files in ONE commit, the message on stdin: `git -C "$GOV" add DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md && git -C "$GOV" commit -F - -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md <<'MSG'` with the subject `[<id from your plan filename>] gate2-dc-w30: DRAFTING_CYCLE v2.38 + PANEL_SEAT_TEMPLATE v1.3 — sixteen DC proposals and 543 (the DC/PST tranche; Gate 2 W=29+W=30 complete)` and `MSG` closing it; `git -C "$GOV" log --oneline -1 -- DRAFTING_CYCLE.md` → that commit, and `-- PANEL_SEAT_TEMPLATE.md` → the SAME commit. Do NOT push.
>
> **A4 — the flip (doctrine before DB):** backup first, by sqlite's own copy — the DB is WAL-journaled, so a filesystem `cp` can lag a non-checkpointed write while sizes match: `sqlite3 "$DB" ".backup /Users/marklehn/Developer/forge_lessons/pre-gate2-dc-w30-$(date +%H%M%S).db"`; then on the backup — ⚠️ through `sqlite3 -readonly "file:<backup>?immutable=1"`, never a bare `-readonly <path>`: a `.backup` output of a WAL database has no `-shm` sidecar and a bare read-only open fails `unable to open database file (14)` — `PRAGMA integrity_check` → `ok`; `SELECT COUNT(*) FROM lesson_proposals` → equal to the live count (state both); `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (445,448,450,451,452,454,466,468,473,475,478,479,531,560,585,586,543) AND status='accepted'` → 17 (`BK=17`). Write `$GOV/governance/knowledge/development/g2dcw30-flip.sql` with a QUOTED heredoc, EXACTLY:
> ```
> .timeout 5000
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=17));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (445,448,450,451,452,454,466,468,473,475,478,479,531,560,585,586,543) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output /tmp/g2dcw30/flip-capture.txt
> SELECT COALESCE(id,'')||'|'||COALESCE(status,'')||'|'||COALESCE(route,'')||'|'||COALESCE(category,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 590 ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (445,448,450,451,452,454,466,468,473,475,478,479,531,560,585,586,543) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (445,448,450,451,452,454,466,468,473,475,478,479,531,560,585,586,543) AND status='implemented' AND status_updated_at NOT IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z');
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> ⚠️ The `.output` path is the LITERAL scratch dir A2 created (`sqlite3` dot-commands take no variables) — never a path inside the canonical bellows checkout. Run: `[ -d /tmp/g2dcw30 ] || mkdir -p /tmp/g2dcw30; sqlite3 -bail "$DB" < "$GOV/governance/knowledge/development/g2dcw30-flip.sql"` (⚠️ `-bail` is LOAD-BEARING twice: with the capture dir missing it stops at `.output` and the transaction never commits; and the CHECK pre-check aborts the flip only under it — without `-bail` sqlite skips the failed statement or dot-command and the flip COMMITS, with no capture file or with the seventeen not all accepted) → the five sentinel lines BY NAME: `PRE_F=17`, `CHANGES_F=17`, `EXCL_F=17`, `ACC_POST=0`, `IMPL_POST=419` (a `CHECK` abort prints a line beginning `Runtime error near line 4: CHECK constraint failed: x=17` and writes nothing — HALT on that MESSAGE; never read the exit code through a pipe, it is the last command's). **Read-back on a FRESH read-only connection** (durability is proven post-commit, never by in-transaction sentinels): the seventeen rows `implemented|codify|ceo|<stamp>` with every stamp outside the two vintages; `accepted` 0; `implemented` 419; the capture file has 590 lines and 17 rows containing `|accepted|codify|`. Copy the capture into the worktree: `mkdir -p knowledge/qa/evidence/gate2-dc-w30-2026-09-16 && cp /tmp/g2dcw30/flip-capture.txt knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt` (A5 commits it; QA verifies it). Commit the SQL in governance, the message on stdin: `git -C "$GOV" add governance/knowledge/development/g2dcw30-flip.sql && git -C "$GOV" commit -F - -- governance/knowledge/development/g2dcw30-flip.sql <<'MSG'` with the subject `[<id>] gate2-dc-w30: the flip SQL (445, 448, 450, 451, 452, 454, 466, 468, 473, 475, 478, 479, 531, 560, 585, 586, 543 -> implemented; no accepted row remains)`.
>
> **A5 — dev log + commit by explicit pathspec, the pre-check chained first.** `knowledge/development/dev-log-gate2-dc-w30-2026-09-16.md`: the A0 determination, A1's pins, A2's lines and the three refusals, A3's counts (both files) and the governance commit hash, A4's backup lines, sentinels, read-back and the SQL commit hash. Then, as ONE command: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/development/dev-log-gate2-dc-w30-2026-09-16.md knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt && git commit -F - -- knowledge/development/dev-log-gate2-dc-w30-2026-09-16.md knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt <<'MSG'` with the subject `[<id>] gate2-dc-w30: DC v2.38 + PST v1.3 + flip landed (dev log, flip capture)` and `MSG` closing it. `git status --short` → empty. STOP.
>
> **Deposits:**
> - `knowledge/development/dev-log-gate2-dc-w30-2026-09-16.md`
> - `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md`
> - `/Users/marklehn/Developer/eluvian-governance/PANEL_SEAT_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2dcw30-flip.sql`
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt`
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-dc-w30-2026-09-16.md`
> - `/Users/marklehn/Developer/eluvian-governance/DRAFTING_CYCLE.md`
> - `/Users/marklehn/Developer/eluvian-governance/PANEL_SEAT_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2dcw30-flip.sql`
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt`

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message.** You are the bellows QA agent. `cd "$(git rev-parse --show-toplevel)"`; `GOV=/Users/marklehn/Developer/eluvian-governance`; `DC="$GOV/DRAFTING_CYCLE.md"`; `PST="$GOV/PANEL_SEAT_TEMPLATE.md"`; `B="$GOV/governance/knowledge/decisions/drafts/build-gate2-dc-w30-2026-09-15.py"`; `DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db`; `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python` — re-derive per compound. Read-only beyond the three evidence files; the commit's message goes on stdin, as STEP 1's header states.
>
> **(A) Rule 20 self-check** — the canonical block at the path the dispatcher's mandate names (this machine's `RULE_20_SELF_CHECK_BLOCK.md`; quote the path you were handed). Run with:
> - `plan_slug`: `gate2-dc-w30-2026-09-16`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/gate2-dc-w30-2026-09-16/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-dc-w30-2026-09-16"`
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-dc-w30.txt", "flip-capture.txt"]` (the capture committed by DEV in A5; QA verifies it is the committed file — `git log -1 --format=%s -- knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt` carries the plan's id)
>
> **(B) Items — raw output appended to `probes-raw.txt`** (⛔ the raw file carries NO summary-shaped line: P7 is named as "P7", never restated with its numbers; the suite's summary line stays in the suite file; no commit log is pasted beyond Item 4's path-scoped probe — MUST-PRESERVE's raw-file rule, thread 363):
> - **Item 1 — the live files:** `git -C "$GOV" log --oneline -1 -- DRAFTING_CYCLE.md` and `-- PANEL_SEAT_TEMPLATE.md` (the SAME `[<id>]` commit); every Task C count re-run on both files (55 probes — 44 on DC, 11 on PST); `wc -l` 403 and 43; `git -C "$GOV" status --porcelain -- DRAFTING_CYCLE.md PANEL_SEAT_TEMPLATE.md governance/knowledge/development/g2dcw30-flip.sql` → EMPTY.
> - **Item 2 — the builder, by a second pair of hands (byte-identity, both files):** `rm -rf /tmp/g2dcw30-qa; mkdir -p /tmp/g2dcw30-qa; C=$(git -C "$GOV" log -1 --format=%H -- DRAFTING_CYCLE.md); git -C "$GOV" show "$C^:DRAFTING_CYCLE.md" > /tmp/g2dcw30-qa/DC-pre.md; git -C "$GOV" show "$C^:PANEL_SEAT_TEMPLATE.md" > /tmp/g2dcw30-qa/PST-pre.md; python3 "$B" /tmp/g2dcw30-qa/DC-pre.md /tmp/g2dcw30-qa/DC-out.md /tmp/g2dcw30-qa/PST-pre.md /tmp/g2dcw30-qa/PST-out.md; echo "builder_exit=$?"; cmp /tmp/g2dcw30-qa/DC-out.md "$GOV/DRAFTING_CYCLE.md" && cmp /tmp/g2dcw30-qa/PST-out.md "$GOV/PANEL_SEAT_TEMPLATE.md" && echo BYTE_IDENTICAL` → the `BUILT:` line, `builder_exit=0`, `BYTE_IDENTICAL`; P4 with the builder's own commit; the three refusals reproduced.
> - **Item 3 — the flip, read back on a FRESH read-only connection:** the seventeen rows `implemented|codify|ceo|<stamp outside the two vintages>`; `accepted` 0 (`SELECT COUNT(*) … WHERE status='accepted'` → 0 — no accepted row remains); `implemented` 419; `wc -l knowledge/qa/evidence/gate2-dc-w30-2026-09-16/flip-capture.txt` → 590; the capture's seventeen rows for the flipped ids read `accepted|codify` (the pre-flip state, captured before the UPDATE).
> - **Item 4 — the full-suite file:** `"$BPY" -m pytest tests -q -p no:cacheprovider > knowledge/qa/evidence/gate2-dc-w30-2026-09-16/full-suite-gate2-dc-w30.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-dc-w30-2026-09-16/full-suite-gate2-dc-w30.txt` → `exit=0` and a summary line with NO `failed`/`error` count — P7's counts are the expectation, but the gate (`_gate_qa_test_result`) reads FAILURES against `known_failures`, never the pass count: if bellows `main` moved since P7 was measured (`git log --format=%h <P7's commit>..HEAD -- tests` non-empty — P7's commit is the anchor — `8e7c4609` at the derivation, the newest QA's; re-read at the freeze), the drift is stated in `probes-raw.txt` under Item 4 as that probe's command and the HASHES it printed (`--format=%h`, never `--oneline` — a bellows QA commit's subject carries `N passed`, the summary shape the raw file must not hold), and nothing broader; the receipt's suite row stays the FILE and `exit=0`, never a note: a drift note reaches for `not run`, `skipped` or `pending`, and the canonical block fails a positive row carrying any of them. A different pass count with zero failures is not a halt. ⛔ A suite file WITHOUT a summary line (a crashed run — a traceback, a killed process) is a HALT, never a re-run note and never a second attempt written over it: with the named suite file summary-less the gate falls to the next `.txt` carrying one, and the raw-file rule above is the only thing standing between that and a stale count.
>
> **(C) The report** `qa-receipt.md`: the verification table — status cells carry the glyph only, and NO positive row quotes a probe token or an output line carrying a Rule 20 hedging keyword (`hedging_keywords` in the canonical block): this plan has TWO such sources — the suite summary line and the agent's OWN prose (a drift note, a re-run note: `not run`, `skipped` and `pending` are the words such a note reaches for, and the block fails a positive row carrying any of its `hedging_keywords` whoever wrote it) — so the suite row names the FILE and `exit=0` with the drift left in `probes-raw.txt`, and Item 1's row reads `55/55 counts as pinned in A3, see probes-raw.txt` (the raw file may carry the tokens; the receipt's positive rows may not — a row quoting any of them FAILS the self-check) — the follow-ups (Gate 2 complete: no tranche after this one; the Planner pushes governance and projects the markers; thread 336 closes at the keyboard), the Rule 20 stdout APPENDED. Commit, as ONE command with the pre-check chained and the message on stdin: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/gate2-dc-w30-2026-09-16/ && git commit -F - -- knowledge/qa/evidence/gate2-dc-w30-2026-09-16/ <<'MSG'` with the subject `[<id>] QA: DC v2.38 and PST v1.3 rebuilt byte-identical from the pre-edit blobs; flip read back (17 implemented, 0 accepted remain — Gate 2 complete)` and `MSG` closing it. STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/full-suite-gate2-dc-w30.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-dc-w30-2026-09-16/full-suite-gate2-dc-w30.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
