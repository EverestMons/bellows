# Bellows — BACKLOG #1 Close + Bellows-Self Constraint + Lessons Capture

**Date:** 2026-05-05 | **Tier:** Small | **Test Scope:** targeted (markdown-only — no test execution; grep + git log + Rule 20 self-check) | **Execution:** Step 1 (Bellows Documentation Analyst) → Step 2 (Bellows QA)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After Step 1, the agent STOPS and waits for CEO confirmation before Step 2 — at which point Bellows itself dispatches Step 2 via verdict-continue once the Planner Rule 22 pass is recorded.

**Bootstrap:**

```
Read the plan at bellows/knowledge/decisions/executable-backlog-1-close-and-lessons-2026-05-05.md. Execute Step 1 ONLY. After completing Step 1, STOP.
```

---
---

## STEP 1 — Bellows Documentation Analyst

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-backlog-1-close-and-lessons-2026-05-05.md", "bellows/knowledge/decisions/in-progress-executable-backlog-1-close-and-lessons-2026-05-05.md")`. **You are the Bellows Documentation Analyst.** Read your specialist file at `bellows/agents/BELLOWS_DOCUMENTATION_ANALYST.md`. **Skip domain glossary read — bellows has no glossary; this is a markdown-edits-only task across two files.** **Context (read these BEFORE editing, in order):** (1) `bellows/knowledge/research/backlog-1-reproduction-audit-2026-05-05.md` — the audit findings that authorize this close; cite directly when writing the new BACKLOG entries. (2) `bellows/knowledge/research/worktree-delta-check-2026-05-05.md` — the prior delta-check confirming the worktree fix shipped in commit `36b2bba` (2026-05-03) and detect-and-skip in commit `06aa938` (2026-05-04). (3) `bellows/knowledge/BACKLOG.md` — the file you will edit; the open BACKLOG #1 entry begins with the line `- 2026-04-30: scope_check diff-collision from concurrent activity (REOPENED 2026-05-01 after failed close attempt; external-vector reproduction added 2026-05-04) — originally surfaced as a parallel-sibling collision`. (4) `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md` — the governance file you will also edit; current version is v4.32, you will bump to v4.33. **Task — three precise edits across two files in two repos. The repos are different and the commits are split per the governance-root commit rule.** **Edit 1 — `bellows/knowledge/BACKLOG.md`: remove the BACKLOG #1 entry from Open.** The entry to remove is one long bullet beginning `- 2026-04-30: scope_check diff-collision from concurrent activity (REOPENED 2026-05-01 after failed close attempt; external-vector reproduction added 2026-05-04) — originally surfaced as a parallel-sibling collision` and ending with `Planner conversation override that depositd a continue verdict authorizing Step 2 dispatch.` followed by a blank line. Use `Filesystem:read_text_file` to read the entire file, then `Filesystem:write_file` to write back the file with the entire bullet removed (and the blank line that immediately follows it removed too, so the next entry follows naturally). Do NOT use `edit_block` for this — the bullet is a single very long line and edit_block's diff display will be unwieldy. Read-and-rewrite with full file content is cleaner here. Verify post-write: the string `- 2026-04-30: scope_check diff-collision from concurrent activity` no longer appears anywhere in the file's Open section. **Edit 2 — `bellows/knowledge/BACKLOG.md`: add a new Open entry capturing the bellows-self constraint AND a new Closed entry capturing the close.** The new Open entry goes at the top of the Open section, immediately after the `## Open` header line and its blank line, BEFORE the existing top entry (`- 2026-05-01: test_startup_sweep_removes_done_plan_orphans uses inline-replicated sweep logic`). New Open entry text — write it as a single bullet, NO line breaks within the bullet (single long line per BACKLOG convention): `- 2026-05-05: bellows-self parallel/concurrent activity exposure (known constraint — accepted tradeoff). Bellows-self plans (those whose project_path is the bellows/ directory itself) run in-place without worktree isolation per the 2026-05-04 detect-and-skip close (commit 06aa938) — bellows is a subdirectory of the governance-root monorepo and has no project-local .git, so _create_worktree returns project_path as-is. This means concurrent activity in the bellows/ subtree during a bellows-self plan's execution window can trip scope_check on files the agent did not touch. Four reproductions documented during the 2026-05-05 BACKLOG #1 audit: close-monorepo-worktree-backlog-2026-05-04 (CEO mv of stale verdict-request files), diagnostic-bash-permission-rules-audit-2026-05-04 (verdict-request files + agent-prompt-feedback.md), parallel-1-executable-planner-template-parallel-group-dispatch-subsection-2026-05-05 (CEO mv into archived/), parallel-1-diagnostic-startup-sweep-test-refactor-2026-05-05 (verdict-request files + agent-prompt-feedback.md). Mitigation guidance: (1) avoid CEO mv/cleanup operations inside bellows/ subtree while a bellows-self plan is dispatched (verdict archival, log rotation, file moves); (2) avoid dispatching parallel-N- groups for bellows-self plans that commit during DEV; (3) when scope_check trips on a bellows-self plan with files the agent demonstrably did not touch, run Rule 22 on the actual deposits and override via continue verdict. Workaround for full isolation deferred at 2026-05-04 close: option (b) governance-root-worktree with subdirectory cwd (~50-80 LOC, may confuse agents expecting absolute paths). Reference: bellows/knowledge/research/backlog-1-reproduction-audit-2026-05-05.md (population audit), 2026-05-04 monorepo-worktree-fix close in this file's Closed section.` Then ALSO add a new Closed entry. The new Closed entry goes at the top of the Closed section (immediately after the `## Closed` header line and its blank line), BEFORE the existing top Closed entry (`- **Closed 2026-05-05:** BACKLOG \`2026-05-01: Bellows section omits parallel-N- dispatch mechanics\``). New Closed entry text — single bullet: `- **Closed 2026-05-05:** BACKLOG \`2026-04-30: scope_check diff-collision from concurrent activity (REOPENED 2026-05-01 after failed close attempt; external-vector reproduction added 2026-05-04)\`. Closed via population audit at \`bellows/knowledge/research/backlog-1-reproduction-audit-2026-05-05.md\` after delta-check at \`bellows/knowledge/research/worktree-delta-check-2026-05-05.md\` confirmed the per-plan git worktree fix (recommended in \`worktree-cost-coverage-recommendation-2026-05-03.md\`) shipped in commit \`36b2bba\` on 2026-05-03 with detect-and-skip for monorepo-bellows added in commit \`06aa938\` on 2026-05-04. Audit window 2026-04-30 through 2026-05-05 enumerated 7 scope_check failures across resolved verdicts, archived pending verdicts, halted plans, and verdict log: 3 classified as (b) pre-fix (dispatched before commit 36b2bba's daemon restart at 2026-05-03T13:47:09 per bellows.db session boundary aa0739cc → ef08be5f), 4 classified as (a) bellows-self exposure (project_path was bellows/ itself, worktree intentionally skipped per detect-and-skip), 0 classified as (c) real-\`.git\` post-fix gap. Zero real-\`.git\` projects (forge, invoice-pulse, BrewBuddy, study, ai-career-digest, freight-kb, anvil) tripped scope_check in the audit window. The remaining bellows-self exposure vector is captured as a separate known-constraint Open entry (2026-05-05, top of Open section) — it is an accepted tradeoff from the 2026-05-04 close, not an unfixed bug. Reference plans: \`Done/diagnostic-worktree-delta-2026-05-05.md\`, \`Done/diagnostic-backlog-1-reproduction-audit-2026-05-05.md\`, this executable.` **Edit 3 — `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`: bump version to v4.33 and append a Lessons Learned entry.** First, edit the header. Use `edit_block` with old_string `**Version:** 4.32\n**Last Updated:** 2026-05-05 (v4.32)` and new_string `**Version:** 4.33\n**Last Updated:** 2026-05-05 (v4.33)`. Second, append a new Lessons Learned table row immediately after the existing final row in the Lessons Learned table — the existing final row begins `| 2026-05-01 | Rule 13 anchoring gap: when a plan modifies production code that an existing test encodes as "correct"`. Use `edit_block` with old_string being the complete final row line (read the file first to capture it verbatim including its terminal `|`) and new_string being the same line followed by `\n` followed by the new row. New row text: `| 2026-05-05 | NEXT_SESSION.md framing "BACKLOG #1 still open, three reproductions logged, three fix shapes on the table" was hypothesis-from-memory, not state-from-code. The shipped fix landed 2026-05-03 in commit \`36b2bba\` with detect-and-skip in commit \`06aa938\` 2026-05-04, but NEXT_SESSION.md was authored end-of-session 2026-05-05 without checking git log or the live \`bellows.py\`. Two diagnostics caught it: a delta-check (\`worktree-delta-check-2026-05-05.md\`) confirmed the recommendation shipped, and a population audit (\`backlog-1-reproduction-audit-2026-05-05.md\`) confirmed zero post-fix reproductions on real-\`.git\` projects. **Lesson 1 — verify shipped state before designing follow-up.** Same pattern as the 2026-05-01 phantom-recovery plan: an agent's (or the Planner's own) hypothesis that "fix needed" implies checking the artifacts that would prove or disprove it (git log, live code, ledger). When NEXT_SESSION.md, a BACKLOG entry, or a CEO statement asserts "the fix is still pending," the Planner's first move is to grep for the recommended code, not to design the diagnostic. The cost is one tool call; the savings are an entire diagnostic→executable session that would have re-derived shipped work. **Lesson 2 — population audits are a cheap, high-leverage diagnostic technique.** When a recurring failure has accumulated multiple reproductions and a "fix needed" hypothesis, a population audit (enumerate all reproductions in a date window, classify each by pre-fix / accepted-exposure / post-fix-gap) is often the right first diagnostic — it produces a count and a verdict, not a recommendation, and it answers "is this still a bug or is it a known-and-accepted constraint?" Cost: ~10 minutes of agent time, ~$0.20. The 2026-05-05 BACKLOG #1 audit consumed 9 minutes and produced a clean close. Use this pattern when (a) a BACKLOG item has 3+ reproductions and (b) a fix has shipped between any two of those reproductions. **Mechanical guidance:** before authoring a follow-up diagnostic for any BACKLOG item with multiple reproductions, run \`git --no-pager log --since=<earliest-reproduction-date> --oneline -- <suspected-file>\` and check whether any commit message matches the recommended fix shape from the BACKLOG entry. If yes, the next diagnostic should be a population audit, not a fresh design pass. |` **Output receipt and feedback.** Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Commits — split per governance-root commit rule.** TWO separate commits in TWO separate repos. Sub-step A (in `cd /Users/marklehn/Desktop/GitHub/bellows`): `git --no-pager add knowledge/BACKLOG.md && git --no-pager commit -m "docs(backlog): close #1 (worktree fix shipped 2026-05-03), open bellows-self exposure as known constraint"`. Sub-step B (in `cd /Users/marklehn/Desktop/GitHub`): `git --no-pager add PLANNER_TEMPLATE.md && git --no-pager commit -m "docs(planner): v4.33 — Lessons Learned entry on hypothesis-from-memory and population audit technique"`. **Deposits:**
> - `bellows/knowledge/BACKLOG.md`
> - `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — Bellows QA

---

> **Before starting, read `bellows/knowledge/decisions/in-progress-executable-backlog-1-close-and-lessons-2026-05-05.md` Step 1's Output Receipt status field. If status is not Complete, stop and report the issue to the CEO before proceeding.** **You are the Bellows QA agent.** Read your specialist file at `bellows/agents/BELLOWS_QA.md`. **Skip domain glossary read — markdown-only verification, no test suite.** **Task — verify the three edits from Step 1 landed correctly via grep + git log, then run the Rule 20 self-check.** **Verification 1 — BACKLOG #1 removal from Open.** Run `cd /Users/marklehn/Desktop/GitHub/bellows && grep -n "2026-04-30: scope_check diff-collision from concurrent activity" knowledge/BACKLOG.md` and pipe the output to `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_backlog_1_removal.txt`. Expected: at most ONE match in the Closed section (the new Closed entry that references the original entry's title). Zero matches in the Open section. If there are 2+ matches OR a match in the Open section, mark ❌. **Verification 2 — new Open entry exists.** Run `cd /Users/marklehn/Desktop/GitHub/bellows && grep -n "2026-05-05: bellows-self parallel/concurrent activity exposure (known constraint" knowledge/BACKLOG.md` and pipe to `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_new_open_entry.txt`. Expected: exactly ONE match, and it must appear before the line containing `## Closed`. **Verification 3 — new Closed entry exists.** Run `cd /Users/marklehn/Desktop/GitHub/bellows && grep -n "Closed 2026-05-05.*scope_check diff-collision" knowledge/BACKLOG.md` and pipe to `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_new_closed_entry.txt`. Expected: exactly ONE match, after the line containing `## Closed`. **Verification 4 — PLANNER_TEMPLATE version bump.** Run `cd /Users/marklehn/Desktop/GitHub && grep -n -E "^\*\*Version:\*\* 4\.33$" PLANNER_TEMPLATE.md` and pipe to `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_planner_version.txt`. Expected: exactly ONE match. Also run `cd /Users/marklehn/Desktop/GitHub && grep -c -E "^\*\*Version:\*\* 4\.32$" PLANNER_TEMPLATE.md` and confirm the count is 0 (no stale 4.32 references remain). **Verification 5 — PLANNER_TEMPLATE Lessons row appended.** Run `cd /Users/marklehn/Desktop/GitHub && grep -n "2026-05-05.*hypothesis-from-memory" PLANNER_TEMPLATE.md` and pipe to `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_planner_lessons_row.txt`. Expected: exactly ONE match. **Verification 6 — split commit verification.** Run `cd /Users/marklehn/Desktop/GitHub/bellows && git --no-pager log --oneline -3` and pipe to `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/git_log_bellows.txt`. Expected: most recent commit message starts with `docs(backlog): close #1`. Then run `cd /Users/marklehn/Desktop/GitHub && git --no-pager log --oneline -3` and pipe to `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/git_log_governance.txt`. Expected: most recent commit message starts with `docs(planner): v4.33`. **Write the QA report.** Write a verification table to `bellows/knowledge/qa/backlog-1-close-and-lessons-qa-2026-05-05.md` with columns `| # | Verification | Expected | Status | Evidence |`. Status uses ✅ or ❌. Evidence column cites the evidence file path. After the table, run the Rule 20 self-check (canonical block — see template in PLANNER_TEMPLATE Rule 20). Use `plan_slug = "executable-backlog-1-close-and-lessons-2026-05-05"`, `qa_report_path = "bellows/knowledge/qa/backlog-1-close-and-lessons-qa-2026-05-05.md"`, `evidence_dir = f"bellows/knowledge/qa/evidence/{plan_slug}/"`, and `required_evidence_files = ["grep_backlog_1_removal.txt", "grep_new_open_entry.txt", "grep_new_closed_entry.txt", "grep_planner_version.txt", "grep_planner_lessons_row.txt", "git_log_bellows.txt", "git_log_governance.txt"]`. Include the literal stdout of the self-check in the QA report. If self-check prints SELF-CHECK FAILED, STOP — do not proceed to commit or PROJECT_STATUS update; report the failure to the CEO. **Update PROJECT_STATUS.md.** If self-check passed, append a new completed-milestone entry to `bellows/PROJECT_STATUS.md` describing this plan: BACKLOG #1 closed via population audit, bellows-self constraint opened as known-tradeoff entry, PLANNER_TEMPLATE bumped to v4.33 with hypothesis-from-memory + population-audit Lessons entry. Use `Filesystem:read_text_file` to read the file, find the most recent milestone entry, anchor your edit to it via `edit_block` (anchored edit per Rule 23). **Standard prompt feedback protocol** → `bellows/knowledge/research/agent-prompt-feedback.md`. **Final commits — split per governance-root rule. ONE commit per repo. Order: feedback append → final commit, per Rule 23.** Sub-step A (in `cd /Users/marklehn/Desktop/GitHub/bellows`): `git --no-pager add knowledge/qa/backlog-1-close-and-lessons-qa-2026-05-05.md knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/ PROJECT_STATUS.md knowledge/research/agent-prompt-feedback.md && git --no-pager commit -m "docs(qa): backlog-1 close and lessons capture verification 2026-05-05"`. (No governance-root commit needed for QA — only the bellows repo has new artifacts.) **Do NOT move the plan to Done.** The Planner performs the terminal Done/ move after Rule 22 verification on the QA report. **Deposits:**
> - `bellows/knowledge/qa/backlog-1-close-and-lessons-qa-2026-05-05.md`
> - `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_backlog_1_removal.txt`
> - `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_new_open_entry.txt`
> - `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_new_closed_entry.txt`
> - `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_planner_version.txt`
> - `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/grep_planner_lessons_row.txt`
> - `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/git_log_bellows.txt`
> - `bellows/knowledge/qa/evidence/executable-backlog-1-close-and-lessons-2026-05-05/git_log_governance.txt`
> - `bellows/PROJECT_STATUS.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
