# bellows — diagnostic: DOCTRINE STALENESS CENSUS — every path, tool, function and machine-fact token in the seven doctrine and command files checked against the live tree, so thread 248's edits are made from a list with line numbers, and the census is priced as a wrap-time check (thread 258, the mechanism behind 248)

**Date:** 2026-09-09 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic; writes one research doc, one TSV and a dev-log) | **Execution:** Step 1 (DIAGNOSTIC) | **pause_for_verdict:** after_step_1 | **Discharges:** none

**auto_close:** false

**Post-close:** no restart, no doctrine. The doc's stale list is the input to thread 248's direct-edit lane (the CEO's act on doctrine); whether the census becomes a wrap-time check is a ruling the doc's false-positive rate informs. This plan recommends nothing.

**Depends on:** the CEO's ruling of 2026-09-09 (session d04ebd33): evidence before implementation — the census runs from a scratch script written INTO the doc, not deposited as a tool; the doctrine is read, never edited. Clone origin by kind and layout: `Done/diagnostic-100057.md` and `diagnostic-bellows-forge-lifecycle-reporter` (#100058, the same night): one DIAGNOSTIC step, numbered questions, doc + TSV as Planner-commit deposits, no recommendation. Dispatched in the bellows lane because the census reads bellows' hooks and command files as well as governance doctrine, and governance has no lane of its own on this machine.

**Tier computed (§1):** **T1** — T-1 fires (a read across governance doctrine, bellows hooks and commands, and every repo root the tokens resolve against); T-6 does not (no doctrine edit — the list is produced, the edits are 248's); T-2 does not.

## CEO Context

The `/eluvian` alignment pass on 2026-09-09 found three stale machine facts by reading: ELUVIAN_PATH.md names `/Users/marklehn/Developer/GitHub` as the governance root and `.../GitHub/GLOSSARY.md` as the central glossary; the `/eluvian` command says STOPPED is the correct daemon state on the mini, which has run the live daemon since 2026-09-02; the command's lessons-forge resolution tries an empty submodule directory first. Thread 248 holds the edits; thread 258 asks for the mechanism, salvaged from forge's Lab ("agent staleness audit": specialist files that reference removed code). Walk 0 ran a first census: 90 backticked path tokens across the seven files, 15 unresolved against the live roots — two are the old GitHub root, one is a retired register (`bellows/knowledge/BACKLOG.md`), and twelve are template placeholders (`path/to/file.md`, `app.py`, `my-report.md`) that any naive check would flag. The census is worth a wrap-time check only if the placeholder class is excluded by a stated rule and the remaining rate is low.

## Numbers discipline — measured 2026-09-09 by the Planner (governance `48456f1a`, bellows `af5ec62`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the doctrine set and its path tokens | seven files: `ELUVIAN_PATH.md` (7 tokens), `PLANNER_TEMPLATE.md` (43), `DRAFTING_CYCLE.md` (15), `MACHINE_SETUP.md` (17), bellows `CLAUDE.md` (3), `hooks/commands/wrap.md` (3), `hooks/commands/eluvian.md` (2) — 90 backticked tokens matching `[A-Za-z0-9_./~-]+\.(py\|md\|sh\|json\|db)` with a `/` or a `.py` suffix, `<`/`*` forms skipped | Item 1's script, in the doc |
| P2 | ⛔ unresolved, by line | `ELUVIAN_PATH.md:131 /Users/marklehn/Developer/GitHub/GLOSSARY.md`; `PLANNER_TEMPLATE.md:2307` the same path; `PLANNER_TEMPLATE.md:995 bellows/knowledge/BACKLOG.md` (retired 2026-06-12 to FORWARD.md); `PLANNER_TEMPLATE.md:751 knowledge/decisions/Done/executable-42.md`; `:842 forge/knowledge/research/foo-diagnostic-2026-05-13.md`; `:492 agents/BREWBUDDY_DEVELOPER.md`; `:2395 test_bellows.py`; placeholders at `:149 app.py`, `:810 path/to/file.md`, `:810 path/to/report.md`, `:816`, `:817`, `:820 bellows/knowledge/qa/my-report.md`, `:1523 path/a.md`, `:1523 path/b.md`, `:1529 path/to/file.md` — 15 unresolved of 90 | the same script, per line |
| P3 | ⛔ the non-path machine facts (sentences a path check cannot see) | `ELUVIAN_PATH.md:21` "cwd is the governance root (`/Users/marklehn/Developer/GitHub`)"; `hooks/commands/eluvian.md:21` "on the mini, STOPPED is the correct, deliberate state"; `hooks/commands/eluvian.md:7` and `hooks/eluvian/eluvian_align_hook.py:19,29` name `~/Developer/GitHub` as the shop machine's root (true for the Air, per MACHINE_SETUP §1; thread 89 decides whether the Air moves); thread 248's third point: the lessons-forge resolution order | `grep -n 'STOPPED\|GitHub'` over the set; thread 248's body |
| P4 | the resolution roots | a token resolves if it exists as an absolute path (after `~` expansion) or relative to any of: the governance root, its `governance/` dir, the bellows root, `hooks/eluvian/`, `scripts/`, `tools/`, `~/Developer`, `~/Developer/tuyere`, `~/Developer/forge_lessons` — the walk-0 rule; the doc states it and its false negatives (a token that exists in some repo the rule does not list) | Item 1 |
| P5 | the ancestor | forge `PROJECT_BRIEF.md` Module 6, Lab experiment "Agent staleness audit — detects agent specialist files that reference outdated code or removed features"; `forge/src/lab.py` (the March chunk model; not importable here without forge's venv and not needed) | quoted, not run |
| P6 | in-flight; class; interpreter | `diagnostic-bellows-forge-lifecycle-reporter` (#100058) in flight at drafting — this plan deposits only after it closes; writes: a bellows dev-log and two governance research files → **app-feature** (auto-clears; the depositor's parse recorded at deposit); Python 3.9 stdlib only, the bellows venv | `python3 status.py`; `_assign_class` |
| P7 | the truth the facts are checked against | `MACHINE_SETUP.md` v1.7 §1 (roots per machine via `$ELUVIAN_WRAP_ROOT`), §0 (the mini is the server, runs the live daemon since 2026-09-02); `python3 status.py` on the mini: RUNNING pid 70398 | read at Item 3 |

## MUST-PRESERVE

- ⛔ **Read-only.** No doctrine file, hook, command file or lane file is edited; the census script lives in `$T` and in the doc as a code block; nothing under `tools/` or `scripts/`.
- ⛔ **No recommendation.** The doc answers Q1–Q5 and ends with "What this does not establish"; 248's edits and the wrap-time question are the CEO's.
- ⛔ **Every stale claim carries file:line and the live fact it contradicts**, quoted from the live tree or MACHINE_SETUP, never restated.

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-doctrine-staleness-census-2026-09-09.md
**Walks:** walk 0 pinned (P1–P7 measured on governance `48456f1a`, bellows `af5ec62`; clone-diff against `Done/diagnostic-100057.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit.

**Closing:** WARM close after walk 2 — thread 258's first act, a judged stop. Two walks: instruction 2 → 0 (walk 1 folded two). T1, no panel. Deposit via `ready-` after #100058 closes; class app-feature auto-clears; the daemon claims. Close acts: the Planner commits the two governance deposits at the wrap; thread 248's edits are the CEO's, from the doc's list.
- Weak spots:          w1 1 folded — instruction 1 / record 0
- Destruction:         w1 dry
- Vulnerabilities:     w1 1 folded — instruction 1 / record 0
- Integration-record:  w1 dry
- ACID:                w1 dry
- Weak spots:          w2 dry
- Destruction:         w2 dry
- Vulnerabilities:     w2 dry
- Integration-record:  w2 dry
- ACID:                w2 dry
**Walk 2 — DRY, all five lenses, one commit per lens. Instruction 0 on a full pass: the WARM close meets the bar (§2) — a judged stop (2 findings over one warm walk plus f0; yield 2 → 0). T1: no panel owed.**
**Walk 1 — two folds across two lenses (instruction 2 / record 0), three lenses dry; one commit per lens.**

## Cycle Manifest
tier: T1
target: knowledge/development/dev-log-doctrine-staleness-census-2026-09-09.md
class: app-feature
reads: ELUVIAN_PATH.md, PLANNER_TEMPLATE.md, DRAFTING_CYCLE.md, MACHINE_SETUP.md, CLAUDE.md, hooks/commands/wrap.md, hooks/commands/eluvian.md, hooks/eluvian/eluvian_align_hook.py, knowledge/decisions/Done/diagnostic-100057.md
writes: knowledge/development/dev-log-doctrine-staleness-census-2026-09-09.md, governance/knowledge/research/doctrine-staleness-census-2026-09-09.md, governance/knowledge/research/doctrine-staleness-census-2026-09-09.tsv
open_forks: 1. thread 248's direct edits, made from the doc's list (the CEO's act on doctrine); 2. the census as a wrap-time check (`hooks/eluvian/` or the align hook's Stage 4 asserts) if Q4's false-positive rate after the placeholder rule is under one per run; 3. widening the token classes (function names, CLI flags) if Q2 shows the path class alone misses the facts 248 found
walks: 2
yields: 2, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:7
coherence: 2/2 body walks named in the register (3 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.diagnostic-governance-doctrine-staleness-census.md.foldcheck.json

---

## STEP 1 — DIAGNOSTIC: five questions over seven files, every claim with a line number

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ **Read-only:** never run the daemon, `run_plan`, a claim; never import `lifecycle`; the governance root is `/Users/marklehn/Developer/eluvian-governance` and the bellows CANONICAL checkout is `/Users/marklehn/Developer/bellows` (the seven files are read there, not in this worktree, whose tree carries the plan's own files); `T=$(mktemp -d /tmp/doctrine-census.XXXXXX)` holds the script and scratch output; nothing else is written outside the three deposits.
>
> **Scope:**
> - `knowledge/development/dev-log-doctrine-staleness-census-2026-09-09.md`
>
> **Item 1 — re-derive P1 and P2 and HALT on a mechanism mismatch** (a token count that moved because a file moved is reported as both figures). Write the census script to `$T/census.py` — the walk-0 script, verbatim in the doc: the seven files (P1), the token regex, the `<`/`*` skip, the resolution roots (P4) — and run it; paste the per-file counts and the unresolved list with line numbers beside P2's.
> **Item 2 — Q1: classify the unresolved.** For each of the 15 (or the re-derived count), one of: `stale-fact` (a path that existed and moved or was retired — cite the commit or the doctrine version that moved it, e.g. `BACKLOG.md` → `FORWARD.md` 2026-06-12), `placeholder` (a template example: `path/to/…`, `app.py`, `my-report.md`, `foo-…`, `executable-42.md`, `a.md`/`b.md`), `foreign` (a real path in a repo the roots do not list — name the repo), `other`. State the placeholder RULE as a regex the wrap-time check would use (`^path/|^(app|my-|foo-)|/foo-|executable-42\.md$|^[ab]\.md$` is the walk-0 candidate; the DEV writes the one that classifies all 15 and says which it would miss), and the counts per class.
> **Item 3 — Q2: the non-path facts (P3).** Extend the census with a second token class: sentences in the seven files that assert a machine fact — a line containing BOTH a machine word (`mini`, `Air`, `shop machine`, `server`, `this machine`) AND a fact word (`STOPPED`, `RUNNING`, `GitHub`, `root`, `lessons-forge`, `daemon`) — the rule stated in the doc with the count of lines it yields per file — and check each against P7's truth (MACHINE_SETUP v1.7 §0/§1; `status.py` on the mini). List every asserting line with file:line, the claim, the live fact, and `stale`/`current`/`machine-conditional` (true on one machine, false on another — the `~/Developer/GitHub` root is true for the Air today). Thread 248's three points must each appear in the list, or the list says which the class missed and why.
> **Item 4 — Q3: coverage of the widening (open fork 3), measured not built.** Count the backticked function tokens (`name()` and `module.name`) and CLI flags (`--flag`) in the seven files; resolve function tokens with `git grep -n "def name"` over bellows and tuyere and flags STATICALLY — `grep -n -- '--flag'` over the named tool's source under `tools/` or `scripts/`, never by executing the tool (a tool without argparse reads `--help` as its positional argument); report resolved/unresolved per class WITHOUT classifying the unresolved (that is the fork's diagnostic) — one table.
> **Item 5 — Q4: the wrap-time price.** Wall-clock of the census (Items 1–3) in seconds; the false-positive count after the placeholder rule (Q1's `placeholder` class removed) — the number the wrap-time fork turns on; the lines the align hook's Stage 4 asserts already check (`hooks/eluvian/eluvian_align_hook.py`: the GLOSSARY, lifecycle.db and lessons-forge DB asserts) so the doc can say what a census adds to what the hook does.
> **Item 6 — Q5: the ancestor (P5), one paragraph of facts.** What forge's Lab staleness audit scanned (agent specialist files), what this census scans (doctrine and command files), and that the Lab's code is not run here.
> **Item 7 — write the research doc** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/doctrine-staleness-census-2026-09-09.md` answering Q1–Q5 in order, each section opening with the script's raw output line it rests on, the census script as a code block in an appendix, and the TSV `doctrine-staleness-census-2026-09-09.tsv` — one row per token or asserting line, columns `file, line, token_or_claim, class, resolved, live_fact, note`; end with "What this does not establish" (at least: the roots list is a rule, not the shop; a placeholder rule written from 15 instances; the function/flag classes were counted, not judged; one machine's truth).
> **Item 8 — dev-log** with the headings declared below (the `dev_log_declared_text` gate reads them at the pause); `## Pins re-derived (P1, P2)` opens with P2's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `15 unresolved of 90`; paste through those words, then stop. **Commit** the dev-log alone (message tagged with the plan id and `thread 258`), gated on the FULL suite (`.venv/bin/python -m pytest -q` from the worktree root — nothing under bellows changed but the rule is the rule), path-scoped to the Scope list; the two governance files are Planner-commit deposits. Last act, after the commit: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P1, P2)`; `## The unresolved, classified (Q1)`; `## Q1–Q5 in one table`; `## What the doc does not establish`
> **Verbatim:** `## Pins re-derived (P1, P2)` ← P2
>
> **Deposits:**
> - `knowledge/development/dev-log-doctrine-staleness-census-2026-09-09.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/doctrine-staleness-census-2026-09-09.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/doctrine-staleness-census-2026-09-09.tsv`
>
> **Post-conditions:** the doc answers five questions, every stale claim with file:line and the live fact, and ends without a recommendation; thread 248's three points appear in Q2's list or the list says which it missed; the TSV has one row per token and per asserting line; the dev-log's four headings present as full lines with P2's cell whole; one bellows commit carrying the dev-log only; no doctrine, hook, command or lane file changed (`git -C /Users/marklehn/Developer/eluvian-governance status --porcelain` shows only the two research files); nothing under `$T` that survives the step.
