# Bellows — Mode B Static Analysis (Failure 3 Code-Path Audit)
**Date:** 2026-05-05 | **Tier:** Small | **Test Scope:** none (read-only investigation) | **Execution:** Step 1 (DEV)

## How to Run This Plan

Bellows OP-001 is resolved (2026-05-05). This is a read-only code-tracing task on Bellows source \u2014 no risk to Bellows itself. Safe to dispatch via Bellows.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-failure-3-mode-b-static-analysis-2026-05-05.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to any further step or move the plan to Done.
```

---
---

## STEP 1 \u2014 Bellows Developer

---

> **FIRST \u2014 before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-failure-3-mode-b-static-analysis-2026-05-05.md", "bellows/knowledge/decisions/in-progress-diagnostic-failure-3-mode-b-static-analysis-2026-05-05.md")`. **Skip glossary read \u2014 this is a code-tracing task on Bellows source. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first** for architectural context on the gate dispatch and verdict consumption flow. You are determining whether Failure 3 Mode B is structurally possible in current Bellows code. Mode B = Bellows itself moves a plan to Done before its own gate evaluation completes for the relevant step. The 2026-05-05 population audit (`failure-3-population-audit-findings-2026-05-05.md`) confirmed Mode A (agent-side ordering) is resolved by the v4.30 Rule 8/23/25 hardening, but found that Mode B cannot be assessed from artifacts because Bellows step logs do not persist gate evaluation results. This diagnostic substitutes code analysis for artifact analysis: if the source code shows gates always run before any Done/ move, Mode B is impossible; if gates can run after, are skippable, or race against state transitions, Mode B is a real bug. **Static analysis only \u2014 do NOT propose fixes, do NOT edit Bellows source, do NOT dispatch reproduction plans.** **Investigation tasks (execute in order):** **(1) Locate `gates.check()` call sites.** Run `grep -rn "gates.check\\|gates\\.check(" bellows/*.py 2>&1`. For each call site, identify the file, line, and surrounding function. Build a table: file | line | enclosing function | what state the plan is in when gates are called (e.g., post-step, pre-verdict-post, post-verdict-consume). Quote the relevant code blocks verbatim (5\u201310 lines around each call site). **(2) Locate Done/ move sites.** Bellows moves plans to Done/ via `shutil.move` or `os.rename`. Run `grep -rn "Done\\|/Done/\\|knowledge/decisions/Done" bellows/*.py 2>&1` and identify every site where a plan file path is mutated to land in Done/. For each, identify: file | line | enclosing function | what triggers the move (continue verdict on terminal step, auto-close path, manual close, other). Quote the relevant code blocks verbatim. **(3) Map the post-step flow.** Read the orchestration loop in `bellows.py` (the function or functions that handle a step completing). Identify the exact sequence of operations after a step's `claude -p` subprocess returns: parse output \u2192 gate check \u2192 verdict request post \u2192 ... \u2192 Done/ move. Quote the relevant code section verbatim. The goal is a numbered ordering: (a) what is the FIRST operation after step completion, (b) where does gate evaluation fit in that sequence, (c) where does the Done/ move fit, (d) is there ANY path that reaches Done/ without going through gate evaluation first? **(4) Map the verdict consumption flow.** Read `_consume_verdicts()` in `bellows.py`. Identify what happens when a continue verdict is consumed for: (a) a non-terminal step (step < total_steps), (b) a terminal step (step == total_steps). For terminal-step continue verdicts specifically, what is the ordering: does Bellows call `gates.check()` again before moving to Done/? Or does the terminal-step Done/ move skip re-evaluation because gates were already run when the step originally completed? Quote the verdict consumption code verbatim. **(5) Map the auto-close path.** When a plan has `auto_close: true` (in plan header or config), and the terminal step completes with all gates passing, what is the ordering of operations? Does the auto-close path skip the verdict request entirely and jump straight to Done/? If so, where does the gate check fit? Quote the auto-close code path verbatim. **(6) Map the disable-auto-close path (default).** When `auto_close: false` (default), the terminal step completes \u2192 gates run \u2192 verdict request posted \u2192 plan renamed to `verdict-pending-*` \u2192 ... wait for verdict ... \u2192 continue verdict consumed \u2192 plan moved to Done/. Confirm this is the actual sequence in code. Specifically check: when the terminal-step continue verdict is consumed, does Bellows re-run gates before the Done/ move, or does it trust that gates already passed when the verdict request was posted? **(7) Identify race conditions and skip paths.** Are there any code paths that bypass gate evaluation? Examples to check: (a) error-handling branches that move plans to Done/ on exceptions (b) timeout handlers that close plans (c) manual override flags or environment variables that disable gates (d) any `try/except` that swallows gate evaluation failures and proceeds (e) any condition where a plan reaches Done/ via a path that does not flow through `gates.check()` at any point. Quote any such paths verbatim. **(8) Synthesize findings into five answered questions.** Question A: where does `gates.check()` get called in the post-step flow, and where does the Done/ move happen \u2014 are gates ALWAYS evaluated before any Done/ move? Question B: in the disable-auto-close terminal-step path (the default), does Bellows re-run gates when consuming the continue verdict, or trust the original evaluation? Question C: in the auto-close terminal-step path, where does the gate check fit relative to the Done/ move? Question D: are there any race conditions, error paths, or skip paths that could let a plan reach Done/ without complete gate evaluation? Question E: based on (A)\u2013(D), is Mode B structurally possible in current code (yes/no with specific code citations supporting the answer)? **Deposit findings to `bellows/knowledge/research/failure-3-mode-b-static-analysis-findings-2026-05-05.md`** using the canonical Python file write pattern: `with open("bellows/knowledge/research/failure-3-mode-b-static-analysis-findings-2026-05-05.md", "w") as f: f.write(content)` where content is a triple-quoted string defined before the open call. The file structure: header (date, agent, plan reference), Investigation Section 1 (gates.check() call sites table + verbatim code), Investigation Section 2 (Done/ move sites table + verbatim code), Investigation Section 3 (post-step flow ordering), Investigation Section 4 (verdict consumption flow with terminal-step path), Investigation Section 5 (auto-close path), Investigation Section 6 (disable-auto-close path \u2014 default), Investigation Section 7 (race conditions and skip paths), Findings (the five answered questions A\u2013E), Output Receipt (canonical format). **Standard prompt feedback protocol \u2192 `bellows/knowledge/research/agent-prompt-feedback.md`.** **Final commit:** `git add bellows/knowledge/research/failure-3-mode-b-static-analysis-findings-2026-05-05.md bellows/knowledge/research/agent-prompt-feedback.md && git --no-pager log -1 && git commit -m "diag: failure 3 mode b static analysis findings"`. **STOP. Do NOT proceed beyond Step 1. Do NOT move the plan to Done. Wait for CEO confirmation.**
>
> **Deposits:**
> - `bellows/knowledge/research/failure-3-mode-b-static-analysis-findings-2026-05-05.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
