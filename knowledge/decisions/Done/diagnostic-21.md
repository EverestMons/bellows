# Bellows — Daemon-Collision Recovery Race Root Cause (Plan 7 Abandoned-While-Running, 2026-06-12)
**Date:** 2026-06-12 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## Context (Rule 27)
INCIDENT (2026-06-12, fully log-anchored): daemon instance A started 11:30:32; it detected and claimed plan 7 at 11:31:52 (`minted id 7 — renamed to in-progress-executable-7.md`) and dispatched step 1 at 11:31:54. A SECOND daemon instance B was started in a terminal at ~11:31; its startup recovery logged `lifecycle recovery: plan 7 — abandoned` at 11:31:55 — marking an actively-RUNNING plan abandoned even though `in-progress-executable-7.md` had existed on disk for ~3 seconds. Expected classification per the recovery contract (see `tests/test_lifecycle.py::TestRecoverHalfClaimed::test_already_renamed_transitions_to_in_progress`): `already_renamed` → `in_progress`. The mis-state self-healed at plan close (the close path overwrites `lifecycle_state` unconditionally — plan 7 reads `closed` now), but the misclassification window is real, and a daemon collision also risks double-dispatch and double verdict-consumption. Instance B was killed at ~11:33; B also logged a Flask bind attempt on port 5000 already held by A. This is FORWARD row 19 (`knowledge/FORWARD.md`). This is a forensic diagnostic ONLY — author NO fix code; the fix executable comes after CEO review. ALL `lifecycle.db` access read-only (`?mode=ro`). Anchors as of today: `recover_half_claimed` at `lifecycle.py:190`; its startup callsite at `bellows.py:1826` — re-verify by grep, never trust line numbers.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_SYSTEMS_ANALYST.md` first. **Post a 1-line "Read X." after each file read and a 1-line "Drafting Section N." at the start of each section.** Ground every claim in code at file:line, log lines, or DB rows. ALL `lifecycle.db` reads via `sqlite3 "file:/Users/marklehn/Developer/GitHub/bellows/lifecycle.db?mode=ro" "<query>"`.
>
> **Section 1 — Recovery logic anatomy.** Read `lifecycle.py::recover_half_claimed` in full and its startup callsite in `bellows.py` (grep `recover_half_claimed`). Document at file:line: which plans-rows it selects (state filter? all 'claimed'?); for each candidate, EXACTLY what filesystem checks it performs (deposit-placeholder existence, in-progress-name construction, which directory it scans and where that directory comes from — the callsite's `decisions_path` per watched project vs the plan's `target_project`); and the classification decision tree (re_renamed / already_renamed / abandoned). State the precise condition under which a plan whose `in-progress-<type>-<id>.md` EXISTS is still classified `abandoned` — enumerate every path that can produce that outcome (wrong directory scanned; target_project mismatch; placeholder-name-only check; exception swallowed; state filter selecting the row before A's rename committed, etc.).
>
> **Section 2 — Incident forensics.** From `logs/terminal/bellows-2026-06-12.log` (both instances write the same dated file — note the `── session restart ──` separators) and `logs/terminal/nohup-restart-2026-06-12.out`: reconstruct the interleaved two-daemon timeline 11:30:30–11:34:00 — A's startup/claim/mint/rename/dispatch lines, B's startup/recovery/bind lines. Cross-check the DB: plan 7's `created_at`, its verdicts/steps timestamps. Then run the Section 1 decision tree against the reconstructed state at 11:31:55 and name the EXACT branch that fired — the confirmed root cause, with the discriminating evidence pointer. If the log evidence is insufficient to pick between two branches, say so explicitly and rank them.
>
> **Section 3 — Collision surface census.** Two daemons watching the same projects: enumerate at file:line every shared mutable surface and what a collision does to it — (a) plan claiming (watcher + 30s rescan: can both instances claim the same deposit? what makes the rename atomic-or-not); (b) verdict consumption (`_consume_verdicts` scanning `verdicts/resolved/` — double-resume risk); (c) lifecycle.db writes (two writers vs the sole-writer contract; WAL behavior); (d) shadow cache; (e) the port-5000 bind (read `server.py` / the startup path: does a failed bind kill instance B or does it continue daemonless — what did B actually do per its log?); (f) anything else found. One row each: surface | collision behavior | observed-today? | severity.
>
> **Section 4 — Fix shapes.** Propose and compare: (a) correct the `recover_half_claimed` existence check (whatever Section 2 confirmed — e.g., scan the right directory or check the id-canonical name against the plan's own target_project decisions dir); (b) single-instance guard — evaluate at least: pidfile with liveness check, exclusive flock on a lockfile at BELLOWS_ROOT, and bind-port-5000-first-or-exit (fail-fast before any recovery/claim work); (c) defense-in-depth: make recovery refuse to mark `abandoned` any plan whose `created_at` is younger than N minutes (race guard). For each: behavior on crash-without-cleanup, on intentional restart, and on the observed double-start. Recommend a combination with justification.
>
> **Section 5 — Gap Assessment + `### Verification Blocks` (Rule 39).** Gap Assessment table `| Gap | Current State (file:line) | Proposed State | Change Required |` for the fix executable. Verification Blocks with `(claim, query, expected_output)` triples for: the recovery decision-tree branch that fired, the directory/state evidence that proves it, B's port-bind behavior, and the double-claim atomicity finding from Section 3(a). Close with **CEO decision forks** (each with a recommendation): which single-instance mechanism; whether the recovery-side fix and the instance-guard ship in one executable or two; race-guard window N.
>
> **BEFORE FINISHING — explicitly `git add` your deposit file and `git commit` it.** Use `with open()` for the deposit; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/research/daemon-collision-recovery-race-2026-06-12.md`
