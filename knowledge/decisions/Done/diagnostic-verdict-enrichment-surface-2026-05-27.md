# Diagnostic — verdict-request enrichment surface mapping

**Project:** bellows
**Dispatch Mode:** bellows
**Plan Type:** diagnostic
**Date:** 2026-05-27
**Source roadmap:** `/Users/marklehn/Developer/GitHub/roadmap-bellows-verdict-enrichment-2026-05-27.md`

## Context

The verdict-enrichment roadmap proposes (1) a new `_gate_rule_22_verification` gate consolidating Rule 22 (a)+(c)+(d) checks, (2) verdict-request enrichment that re-surfaces all 8 existing gates in a unified Verification Results table with per-row pass/fail/detail, (3) a `rule_22_check_failed` Pause Reason Code routed via Rule 25 to CEO review. LOC estimate from the roadmap is ~244 across one DEV + one QA step. This estimate is a guess from outside the code; this diagnostic firms up the call-chain surface so the implementing executable can cite anchors per Rule 27 rather than re-derive them.

Six questions. Read-only investigation. No code changes.

**Recursion-risk constraint (LESSONS 2026-05-19 + 2026-05-27):** the executable this diagnostic informs will modify `bellows/gates.py` (gate code, which the gate `_gate_deposit_exists` itself uses). Q6 includes authoring guidance for the fix plan's `**Deposits:**` block path form to satisfy the constraint. This diagnostic is read-only and produces no code deposits, so it is not at risk.

## STEP 1

You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. Prior research to cite per Rule 27 (do NOT re-derive): roadmap at `/Users/marklehn/Developer/GitHub/roadmap-bellows-verdict-enrichment-2026-05-27.md` (design intent and proposed shape), `bellows/knowledge/research/deposit-exists-path-form-normalization-2026-05-27.md` (current `_gate_deposit_exists` and `_resolve_deposit_path` shape, since the new Rule 22 gate overlaps with `_gate_deposit_exists` on Component A path normalization). Investigate the surface required to ship the verdict-enrichment roadmap. Report findings to `bellows/knowledge/research/verdict-enrichment-surface-2026-05-27.md`. **Investigation surface — six questions:**

**Q1 — `post_verdict_request` current shape.** Open `bellows/verdict.py` and read `post_verdict_request` in full. Report: (a) full function signature with parameter types and defaults; (b) the data inputs it receives (which keys from `parsed`, `gate_result`, plan metadata, etc.); (c) the markdown sections it writes today, in order, with each section's source data (e.g., "## Pause Reason" comes from `pause_reason` parameter; "## Files Changed" comes from `files_changed` parameter); (d) the file path it writes to and any naming conventions; (e) any helper functions it calls. Cite line numbers throughout.

**Q2 — `bellows.py` call site(s) for `post_verdict_request`.** Locate every call site in `bellows/bellows.py` (and any other module if applicable). For each call site, report: (a) line number and surrounding context (which lifecycle event triggers it — pause, gate-failure, qa-checkpoint, etc.); (b) the values passed for each parameter; (c) what data is in scope at the call site that is NOT currently passed (e.g., the full `gate_result` dict, the parsed agent output, the plan text, the worktree path). The roadmap proposes per-gate result surfacing — Q2 establishes whether the gate-result data already flows to the call site or whether the call signature needs to change.

**Q3 — "Intermediate Decisions Detected" data flow.** This section currently appears in verdict requests. Trace it from source (probably `bellows/decisions.py::extract_decision_blocks` or similar) through to where it gets written into the verdict request. Report: (a) the data shape at source (list of dicts? list of strings? structured object?); (b) the call chain from extraction to verdict-write; (c) whether it composes cleanly into a "row in the unified Verification Results table" or requires a separate section. The roadmap proposes treating it as a row with count: `0 phrase-matched blocks` (PASS) or `N phrase-matched blocks` (INFORMATIONAL). Q3 determines if that fits the existing data shape or requires reshaping.

**Q4 — Per-gate return shape audit.** Read each of the 8 existing gate functions in `bellows/gates.py`: `_gate_receipt_status`, `_gate_ceo_flags`, `_gate_errors`, `_gate_no_permission_denials`, `_gate_deposit_exists`, `_gate_qa_step_detection`, `_gate_file_change_audit`, `_gate_scope_check`, `_gate_rule_20_self_check`. For each: (a) function signature; (b) what it returns on failure (the `failures.append({"gate": "...", "evidence": "..."})` shape is the current pattern — confirm each gate matches); (c) what it returns on success — does it produce any structured success message, or only append-on-failure (silent on PASS)? The roadmap proposes PASS rows with one-line details (e.g., `receipt_status | PASS | Status: Complete`). If every gate currently only appends on failure, the new design needs either: (i) gate signature change so each returns a `(passed: bool, evidence: str)` tuple; (ii) post-hoc inference where the verdict-writer composes PASS messages by reading the same data the gate read; (iii) a registry pattern where each gate has a `passes_message()` function alongside its check. Report the per-gate current shape and recommend ONE approach (i/ii/iii), with trade-offs.

**Q5 — `_gate_rule_20_self_check` QA-report parse reuse.** The new `_gate_rule_22_verification` needs to read the QA report for: (c) deliverable-verification-table ❌ row scan; (d) hedging-keyword scan across the full report. The existing `_gate_rule_20_self_check` already reads the QA report for banner detection. Read its implementation in `bellows/gates.py` and report: (a) where it opens the QA report (path resolution, encoding, error handling); (b) whether it parses the report into a structure that the new gate could reuse, or only does a regex scan with no preserved parse; (c) whether the gate's evaluation order in `gates.check()` would allow `_gate_rule_22_verification` to be added immediately after `_gate_rule_20_self_check` and reuse a cached parse via shared state, or whether each gate must independently open the file. Recommend whether the new gate opens the QA report independently (simpler, ~5 LOC extra) or shares a cached parse (more efficient, ~15 LOC of plumbing). The simpler path is acceptable given QA reports are small files (< 100KB typical).

**Q6 — Pause Reason Code routing surface.** The roadmap proposes a new `rule_22_check_failed` code routed via Rule 25 to "stop and report CEO". Locate every place in `bellows/bellows.py` (and `verdict.py` if applicable) where Pause Reason Codes are assigned. Report: (a) the full enum/string-set of Pause Reason Codes currently in use; (b) where each is assigned in the code (line number + surrounding context); (c) where the new `rule_22_check_failed` code would be assigned — which lifecycle branch, after which check; (d) whether any other module consumes the Pause Reason Code (e.g., notifier dispatch routing) and would need to be aware of the new code. **Authoring guidance for the fix plan (recursion-risk):** the executable this diagnostic informs will modify `bellows/gates.py` (touching `_gate_deposit_exists` and adding `_gate_rule_22_verification`). Per LESSONS 2026-05-19 and 2026-05-27, the fix plan's `**Deposits:**` block must use **project-prefixed relative paths** (`bellows/gates.py`, NOT `/Users/marklehn/Developer/GitHub/bellows/gates.py`) to match the form agents declare in their Output Receipts. Confirm in the report that this form remains canonical post-path-form-normalization-fix (i.e., that the new normalization helper added today does not change which form is canonical for plan authoring). Include this confirmation as part of Q6's findings.

Do not fix anything — read-only investigation. After findings, append a dated entry to `bellows/knowledge/research/agent-prompt-feedback.md` per standard protocol. Commit: `git add bellows/knowledge/research/verdict-enrichment-surface-2026-05-27.md bellows/knowledge/research/agent-prompt-feedback.md`, `git --no-pager commit -m "diag: verdict-request enrichment surface mapping"`, `git --no-pager push origin main`.

**Deposits:**
- `bellows/knowledge/research/verdict-enrichment-surface-2026-05-27.md`
- `bellows/knowledge/research/agent-prompt-feedback.md`
