# Bellows — extract_total_steps Fenced-Code Class Audit
**Date:** 2026-05-11 | **Tier:** Small (diagnostic) | **Test Scope:** targeted | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation. Per Rule 22, the Planner reads the deposited findings file directly before authorizing any follow-up work.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-extract-total-steps-fence-class-audit-2026-05-11.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further. Do NOT move the plan to Done.
```

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-extract-total-steps-fence-class-audit-2026-05-11.md", "bellows/knowledge/decisions/in-progress-diagnostic-extract-total-steps-fence-class-audit-2026-05-11.md")`. You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. Skip the domain glossary read — this is a code-tracing audit task. **Context.** BACKLOG entry 2026-05-10 reports `extract_total_steps()` in `bellows/bellows.py` counts `## STEP N` patterns regardless of code-fence or string-literal context. Observed during dispatch of `executable-header-parser-multiline-fix-2026-05-10`: a 2-step plan that embedded `## STEP 1` / `## STEP 2` strings inside Python heredoc test-fixture literals caused Bellows to log "plan has 4 steps" and dispatch 4 steps. The BACKLOG entry hypothesizes the same class of bug (parser sees a literal-looking pattern in prose and treats it as structural) may also exist in `_gate_is_qa_step` and possibly other parsers in `bellows.py` and `gates.py`. The Planner needs a population-level view of this bug class before authoring a fix, per the 2026-05-05 Lessons on population audits and verifying shipped state before designing follow-up. **Investigate only; do NOT propose fixes inside the findings file beyond what Q6 explicitly asks for. Do not write source code changes. Do not run tests beyond REPL fixtures captured inline in findings.** **Q0 — Has any fix shipped since 2026-05-10?** Run `git --no-pager log --since=2026-05-09 --oneline -- bellows.py gates.py parser.py verdict.py` from inside the `bellows/` directory. For every commit returned, run `git --no-pager show --stat <sha>` and check whether the commit touches `extract_total_steps`, `_gate_is_qa_step`, or adds any code-fence stripping logic to a parser. If a fix has shipped, stop the diagnostic here: write a one-paragraph findings file that says so and cite the commit SHA, then jump straight to the Output Receipt. If no shipped fix exists, proceed to Q1. **Q1 — Confirm current behavior of `extract_total_steps()` empirically.** Read `bellows/bellows.py` and locate `extract_total_steps()`. Capture its exact source (function signature + body) and line range in the findings (cite as `bellows.py:NNN-MMM`). Then write a Python REPL fixture (paste it as a code block inside the findings; do not commit a test file) that constructs a string with exactly 2 real `## STEP 1 —` / `## STEP 2 —` headers plus one fenced code block containing `## STEP 1` and `## STEP 2` strings inside it. Run `extract_total_steps(fixture)` against the live code and capture the returned integer. State whether it returns 2 (clean) or 4+ (bug confirmed). **Q2 — Survey `STEP N` regex use.** Run `grep -nE "STEP\s*\\\\d\|## STEP" bellows/bellows.py bellows/gates.py` (use `grep -P` if `-E` doesn't accept `\d`; or use `python3 -c "import re; ..."`). Enumerate every match in a markdown table with columns: filename, line number, function name (use enclosing-def detection), regex pattern, intended purpose (one short phrase), whether the parser strips code fences before counting/matching (yes/no). For each row that does NOT strip code fences, classify as IN-SCOPE (potentially affected by this bug class) or OUT-OF-SCOPE (not a counting/structural parser — e.g., display string, log line, comment) with a one-line justification. **Q3 — Probe each IN-SCOPE parser from Q2.** For each IN-SCOPE row, write a one-line Python REPL fixture demonstrating the parser's behavior on a string containing the pattern inside a fenced code block (triple-backtick). Capture each parser's output and classify as: (a) BUG-CONFIRMED (parser counts/matches the in-fence string), (b) CLEAN (parser correctly ignores it), or (c) NOT-APPLICABLE (parser semantics make code-fence-vs-prose distinction irrelevant — e.g., parser only ever runs on a single header line in isolation, not on multi-line plan_text). State the classification per parser in a per-parser block. **Q4 — Audit for the broader pattern.** Beyond `## STEP N`, search for other parsers that match against full plan-content strings. Run `grep -nE "re\.(findall\|search\|match\|finditer)\(" bellows/bellows.py bellows/gates.py bellows/parser.py bellows/verdict.py` and enumerate. For each match, classify: (a) reads plan/step content (RELEVANT), or (b) operates on agent output, file paths, or other non-plan text (IRRELEVANT). For RELEVANT parsers not already covered in Q2/Q3, repeat the Q3 probe to determine whether the same code-fence-blindness exists. **Q5 — Population scan of recent plans.** Identify the 30 most recent plans (by mtime) under `bellows/knowledge/decisions/Done/` and `invoice-pulse/knowledge/decisions/Done/` that contain at least one fenced code block. Use a Python script (paste as a code block inside findings) that for each plan: (a) reads the file, (b) splits content into in-fence and out-of-fence regions using a triple-backtick state machine, (c) counts real `## STEP N —` headers in the out-of-fence region, (d) counts `## STEP N` patterns in the in-fence region. Report the count of plans where in-fence `## STEP N` patterns exist AT ALL — these are the plans that would have triggered the bug if dispatched today. Do NOT re-dispatch any plan; this is a static scan only. **Q6 — Recommend a fix shape per affected parser.** For each parser classified BUG-CONFIRMED in Q3 or Q4, evaluate the three fix shapes from the BACKLOG entry: (a) strip fenced code blocks from plan_text before counting/matching, (b) add a comment-marker convention like `<!-- NOT A STEP -->` that the parser respects, (c) governance-only mandate that example `## STEP N` patterns inside plan prose use a different prefix (e.g., `## EXAMPLE STEP 1`). State the recommended shape per parser with one paragraph of reasoning. If shape (a) is recommended for ANY parser, the recommendation MUST include the concrete fence-stripping approach (e.g., `re.sub(r"```.*?```", "", text, flags=re.DOTALL)` vs. a character-by-character state machine vs. an existing fence-stripper if one already exists in the codebase — search for it). Note whether the strip should be a shared utility function or per-parser inline (preference: shared utility if 2+ parsers need it). State any cost in scope/complexity (LOC, test count, breaking changes to existing plans). **Q7 — Identify the test surface.** For each BUG-CONFIRMED parser, run `grep -nE "extract_total_steps\|_gate_is_qa_step\|<parser-name>" bellows/tests/*.py` and enumerate existing tests that exercise it. List each test's filename, function name, and one-line description. Identify which existing tests would need regression coverage for the fence-handling behavior (do NOT write new tests in this diagnostic; just enumerate gaps). **Findings file structure.** Deposit a single findings file at the path declared in **Deposits** below. Structure: top-level sections per Q0–Q7. Use markdown tables where the question asks for an enumeration. Include all REPL fixture code in fenced Python blocks. Cite line ranges as `bellows.py:NNN-MMM` format. End the findings with a one-paragraph "Planner-facing summary" stating: (i) whether a fix has shipped (Q0), (ii) the confirmed count of BUG-CONFIRMED parsers, (iii) the recommended fix shape distribution (e.g., "3 parsers want shape (a), 1 wants shape (c)"), and (iv) the population-level risk count from Q5. Be precise — the Planner uses this summary to scope the executable. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/research/extract-total-steps-fence-class-audit-2026-05-11.md`
>
> **STOP. Do NOT proceed to any further step. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
