# Bellows — Phase 3b Implementation Verification Diagnostic
**Date:** 2026-04-28 | **Tier:** Small | **Test Scope:** none | **Execution:** Step 1 (Bellows Systems Analyst)
**Priority:** 5

## Context

Phase 3b implementation (DB-based step state recovery + plan_slug column) is about to be authored. The design at `bellows/knowledge/architecture/step-state-resume-design-2026-04-28.md` is comprehensive and specifies exact line numbers for all integration points in `bellows.py` and `verdict.py`. However, the design was written 2026-04-28 morning, BEFORE the BACKLOG #2 fix (commit `3ca8361`) and BACKLOG #4 fix (commit `8db0adc`) landed in `bellows.py` and `gates.py`. Line numbers may have drifted.

This diagnostic is a light verification pass that confirms or refutes the design's specific anchors before the executable is written. It re-verifies line numbers, current schemas, function locations, and prefix-handling rules. It does NOT re-design or re-question the design — that work is done. Execution should be fast (~3-5 min).

Investigation only. No code changes. Single step per Rule 22.

## How to Run This Plan

Bellows watcher claims this plan automatically. Step 1 (Bellows Systems Analyst) re-verifies the design's anchors against current code, deposits findings, and pauses for Planner verdict. Per disable-auto-close, terminal step pauses for Planner verdict. Single-step diagnostic per Rule 22.

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-step-state-resume-phase-3b-verification-2026-04-28.md", "bellows/knowledge/decisions/in-progress-diagnostic-step-state-resume-phase-3b-verification-2026-04-28.md")`. **Skip glossary read** — code-tracing task per Rule 16. **Read your specialist file** at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` — Phase 3b implementation has architectural implications and specialist context is warranted. **Mandatory read:** `bellows/knowledge/architecture/step-state-resume-design-2026-04-28.md` — the design under verification. Treat it as the canonical specification; this diagnostic verifies its anchors are still accurate, NOT whether the design is correct (it is — CEO already approved). **Q1 — record_run() current location and shape.** In `bellows.py`, locate `record_run()`. Quote the function signature, the `CREATE TABLE IF NOT EXISTS` DDL, and the `INSERT INTO runs` statement with line numbers. Compare to the design's claim: signature around line 140, DDL at line 151, INSERT at lines 162-164. Report current line numbers. List every column currently in the `runs` table DDL. **Q2 — record_run() call sites.** Find every call to `record_run(` in `bellows.py`. Report exact line numbers and the calling function name for each. Design claims 4 call sites at lines 269, 307, 327, 365 inside `run_plan()`. Verify count, locations, and that all are inside `run_plan()`. If any call site is OUTSIDE `run_plan()`, flag it — that affects how the new `plan_slug` parameter would be derived. **Q3 — run_plan() integration points.** Locate `run_plan()` in `bellows.py`. Report: (a) function definition line; (b) shadow cache load line (design says ~215, look for `shadow_text`); (c) atomic claim move line (design says ~231, look for `shutil.move` claiming `executable-` → `in-progress-`); (d) bootstrap_prompt construction line (design says ~252). Quote 2-3 lines of context around each. **Q4 — _slug_from_path() current shape.** In `verdict.py`, locate `_slug_from_path()`. Quote the entire function with line numbers. Design says lines 65-75, strips `in-progress-`, `verdict-pending-`, `executable-`, `diagnostic-`. Verify: which prefixes are currently stripped? Is `halted-` stripped? If not, would using `_slug_from_path()` on a `halted-*` path (theoretical edge case from Open Q#2) yield a slug containing `halted-` in it? Also report whether the function is exported (no leading underscore) or private (leading underscore as named). **Q5 — runs table live schema.** Run `python3 -c "import sqlite3; c = sqlite3.connect('/Users/marklehn/Desktop/GitHub/bellows/bellows.db'); print(list(c.execute('PRAGMA table_info(runs)')))"`. Report all columns with their types and nullability. Compare against the DDL from Q1 — any drift between live DB and code DDL would block the migration. **Q6 — diagnostic plan handling.** In `bellows.py`, find where diagnostic plans are detected and where `total_steps = 1` is forced for them. Quote the lines. The design's Open Q#3 raised a concern that DB resume on diagnostics could be a problem. Trace: if a diagnostic plan completes and is moved to Done, is the shadow cache deleted? (If yes, the `shadow_text is not None` guard in the proposed Deliverable 3 prevents DB resume from firing on a fresh diagnostic re-dispatch — confirming the design's assumption.) Report what happens to the shadow cache on plan completion — search for `.bellows-cache` deletion code. **Q7 — pre-existing slug code or naming conflicts.** Search `bellows.py` for any existing function or variable named `slug`, `plan_slug`, `_get_last_completed_step`, or related. Design assumes none exist. If anything overlaps, report it — it would force a rename in the executable. **Q8 — Import graph between core modules.** Report the import graph between `bellows.py`, `verdict.py`, `gates.py`, and `parser.py`. For each module, list which of the other three it imports from (if any), and which specific names are imported. Decision 3 (CEO-resolved as "new shared util module" for `slug_from_path`) was made on the assumption that the cleanest separation is via a new module. Verify there is no current `verdict.py → bellows.py` edge that would have forced this decision anyway, and no circular import that a new `slug.py` module would create. Report the data; do NOT recommend a different path — D3 is locked. **Deliverable.** Use `Filesystem:write_file` to deposit findings to `bellows/knowledge/research/step-state-resume-phase-3b-verification-2026-04-28.md`. Structure: (1) Executive Summary — 4-6 sentences confirming whether design anchors are still accurate, flagging any drift; (2) Q1-Q8 findings with quoted code and current line numbers; (3) Anchor Drift Table with columns: Design Reference | Design Line | Current Line | Drift Severity (None/Minor/Significant); (4) Implementation-blocking findings, if any (e.g., conflicting names, circular import risk, schema drift); (5) Recommendation — one paragraph confirming the executable can proceed with updated line anchors, OR identifying issues that need design revision before Phase 3b can ship. **Do NOT propose code, redesign anything, or re-evaluate decisions** — the design is locked. **Do NOT commit anything to git.** **Standard prompt feedback protocol** → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/step-state-resume-phase-3b-verification-2026-04-28.md`
