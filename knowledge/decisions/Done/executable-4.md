# PLANNER_TEMPLATE v4.61 — Id-Native Naming + Lifecycle DB Read Protocol Codification
**Date:** 2026-06-11 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always

**Execution Map:** Step 1 (DEV) → Step 2 (QA) — strictly sequential, no parallel steps.

## Context (Rule 27)
Codifies into governance the Bellows conventions shipped and active since 2026-06-11 (Reporting Phase 1, Executables A+B — id mint at claim, id-native filenames, lifecycle.db, `[id]` commit tagging; `Done/executable-3.md` is the first id-canonical filing and the format source for this plan's header), plus two 2026-06-11 LESSONS (test-infrastructure scope enumeration; no inline paraphrase of artifact-authoritative specifics). All edits are documentation-only changes to two governance-root files: `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (version 4.60 → 4.61) and `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` (two placeholder-description lines). No code, no tests, no behavior changes. CROSS-REPO NOTE: this plan dispatches from bellows but edits governance-root files outside the bellows worktree — worktree `files_changed=0` is the EXPECTED artifact of this pattern (2026-06-03 precedent), and benign gate noise on that axis is a known false positive. The edit payloads below are authoritative and complete — there is no external spec to defer to; apply each edit exactly as written. Locate every edit site by its ANCHOR text via grep — never by line number (line numbers drift). This plan implements no diagnostic: its source diagnostic ran pre-id under legacy naming and has no integer id, so no `implements diagnostic <id>` citation exists by design.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file first, then read `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` (the edit target) sections around each anchor below before editing. **Allowed files (scope — exactly these, no others):** `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`, `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`. If any other file needs modification, halt and report. **Apply edits E1–E10 below. For each: grep for the ANCHOR string in the named file, confirm exactly one match (if zero or multiple, halt and report the grep output), apply the edit, and record the anchor plus post-edit line number in your dev log.**
>
> **E1 — Version bump.** File: PLANNER_TEMPLATE.md. ANCHOR: `**Version:** 4.60`. Replace with `**Version:** 4.61`.
>
> **E2 — Plan file naming convention.** File: PLANNER_TEMPLATE.md. ANCHOR: `- `executable-[feature]-[YYYY-MM-DD].md` — copy-paste-ready orchestration plans`. Replace that bullet AND the immediately following `diagnostic-[feature]-[YYYY-MM-DD].md` bullet (keep the `roadmap-` bullet) with: `- Bellows-watched deposits (executable / diagnostic / qa): `<type>-draft-<HHMMSS>.md` — transient placeholder; see "Id-Native Naming" below`. Then locate the three example lines beginning `- `parallel-1-executable-[feature-` and replace all three with the single line: `- `parallel-1-executable-draft-<HHMMSS>.md` (the `parallel-N-` prefix composes with the placeholder)`. Then, immediately after the roadmap bullet's paragraph block, insert this new paragraph: `**Id-Native Naming (in force 2026-06-11).** Any file with an `executable-`, `diagnostic-`, or `qa-` prefix deposited to a Bellows-watched `knowledge/decisions/` directory is claimed by filename pattern regardless of `dispatch_mode`. Deposit with the transient placeholder `<type>-draft-<HHMMSS>.md` (current clock time; uniqueness within the directory at deposit time is the only requirement). No descriptive slug and no date in the filename: the date lives in the header `**Date:**` field and descriptive identity lives in the `# Title` line only. Bellows mints a global-monotonic integer id at claim and renames the file to `in-progress-<type>-<id>.md`; the lifecycle thereafter is `verdict-pending-<type>-<id>.md`, `halted-<type>-<id>.md`, and id-canonical Done filing as `Done/<type>-<id>.md`. Legacy `<type>-<slug>-YYYY-MM-DD.md` names remain accepted everywhere (dual-format tolerance) for historical artifacts — never use them for new deposits to watched directories. Non-watched locations (roadmaps, research, architecture docs) keep descriptive+date naming.`
>
> **E3 — Knowledge-artifact save convention.** File: PLANNER_TEMPLATE.md. ANCHOR: `The plan itself is a knowledge artifact. Save it with the naming convention:`. Replace the sentence beginning at that anchor through the end of `for multi-phase master plans` (preserving the rest of the paragraph, including the Execution Map requirement sentence) with: `The plan itself is a knowledge artifact. Plans deposited to Bellows-watched directories use the `<type>-draft-<HHMMSS>.md` placeholder (Bellows renames to the id-canonical name at claim); roadmaps use `roadmap-[feature]-[YYYY-MM-DD].md`.`
>
> **E4 — plan_slug redefinition (three sites).** (a) File: PLANNER_TEMPLATE.md. ANCHOR: `where `<plan-slug>` is the plan filename minus the `.md` extension`. Replace with: `where `<plan-slug>` is a stable descriptive slug the Planner fixes at plan-authoring time (derived from the plan title plus date, e.g. `lifecycle-writes-2026-06-11`). It is NOT derived from the filename — the filename is a transient `draft-` placeholder at authoring time and an integer id thereafter.` (b) File: RULE_20_SELF_CHECK_BLOCK.md. ANCHOR: `- `plan_slug`: `<plan-filename-without-md>``. Replace with: `- `plan_slug`: `<authoring-time descriptive slug — title-derived plus date, NOT the filename>``. (c) Same file. ANCHOR: `plan_slug = "<plan-filename-without-md>"  # PLACEHOLDER`. Replace with: `plan_slug = "<authoring-time-descriptive-slug>"  # PLACEHOLDER`. Do not modify any other line of the canonical Python block.
>
> **E5 — Rule 25 id-correlation.** File: PLANNER_TEMPLATE.md. (a) ANCHOR: `The Planner tracks these plans internally — by slug — as part of normal conversation state.` Replace with: `The Planner tracks these plans internally — by integer id once minted, and by deposit placeholder name during the brief pre-claim window — as part of normal conversation state.` (b) ANCHOR: `verdict-request-<session-plan-slug>-step-*.md` where `<session-plan-slug>` is any plan the Planner has dispatched this session`. Replace with: `verdict-request-<id>-step-*.md` where `<id>` is the integer id of any plan the Planner has dispatched this session (legacy in-flight plans: the historical slug)`. (c) Immediately after the paragraph beginning `**Scan mechanics:**`, insert this new paragraph: `**Id correlation at deposit.** Immediately after the atomic deposit, the Planner learns the minted id by querying the lifecycle DB read-only: `sqlite3 "file:/Users/marklehn/Developer/GitHub/bellows/lifecycle.db?mode=ro" "SELECT id, lifecycle_state FROM plans WHERE deposit_placeholder_name = '<placeholder-filename>';"` — the claim lands within seconds of deposit. If the query returns no row (claim still pending, or a log-and-continue write failure left the DB stale), fall back to `Filesystem:list_directory` on the watched decisions/ directory and read the id from the new `in-progress-<type>-<id>.md` filename. Track the session plan by that integer id for all subsequent Rule 25 scans and verdict filenames.`
>
> **E6 — Verdict filename terminology.** File: PLANNER_TEMPLATE.md. (a) ANCHOR: `where `{slug}` is the plan's slug (filename minus lifecycle prefix and `.md` extension)`. Replace with: `where `{id}` is the plan's integer id for id-native plans (legacy plans: the historical slug — filename minus lifecycle prefix, type prefix, and `.md`). In all cases the mechanical rule is format-agnostic and authoritative: copy the verdict-request filename and replace the `verdict-request-` prefix with `verdict-`.` (b) ANCHOR: `bellows/verdicts/resolved/verdict-[slug]-step-[N].md`. Replace the token `verdict-[slug]-step-[N].md` with `verdict-<id>-step-<N>.md` at that anchor. (c) ANCHOR: `bellows/verdicts/resolved/verdict-<plan-slug>-step-N.md` — NEVER to `bellows/verdicts/pending/``. In that sentence replace `verdict-<plan-slug>-step-N.md` with `verdict-<id>-step-N.md`. (d) ANCHOR: `The canonical write-time filename is `verdict-<plan-slug>-step-N.md`.`. Replace with: `The canonical write-time filename is `verdict-<id>-step-N.md` (legacy plans: `verdict-<slug>-step-N.md`).` Leave all other slug-form mentions in legacy/workaround/changelog text untouched — they describe historical artifacts that dual-format tolerance still accepts.
>
> **E7 — Commit lookup by id tag.** File: PLANNER_TEMPLATE.md. ANCHOR: `rely on slug-based lookup via `git log --grep='<plan-slug>'``. Replace with: `rely on id-based lookup via `git log --grep='\[<id>\]'` (the `[<id>]` commit tag Bellows injects into every agent prompt; slug-grep remains for legacy plans)`.
>
> **E8 — New section: Lifecycle DB Read Protocol.** File: PLANNER_TEMPLATE.md. ANCHOR: `## Plan Authoring Checklist`. Insert the following new section IMMEDIATELY BEFORE that heading, exactly as written:
>
> `## Lifecycle DB Read Protocol (Planner)` — then this body: `Bellows maintains `bellows/lifecycle.db` (10 tables: id_sequence, plans, diagnostic_meta, executable_meta, derivations, steps, commits, deposits, verdicts, gate_events), written by the daemon at every plan transition. The daemon is the SOLE writer. Planner access is READ-ONLY, always via the read-only URI form: `sqlite3 "file:/Users/marklehn/Developer/GitHub/bellows/lifecycle.db?mode=ro" "<query>"`. Never open the DB read-write.` New paragraph: `**Authority model: DB-as-index, filesystem-as-ground-truth.** Every lifecycle write is log-and-continue (best-effort by design): a write failure WARNs and the daemon proceeds, so the DB can be silently stale or incomplete. Route, correlate, and report on the DB; verify terminal states, deposits, and anything authorizing an irreversible action directly on disk (Rule 22 is unchanged by this protocol). Every DB read on an operational path requires a filesystem fallback.` New paragraph: `**Canonical queries.** (1) Placeholder→id correlation at deposit — see Rule 25's "Id correlation at deposit." (2) Diagnostic id for a derivations citation: `SELECT id, title, lifecycle_state FROM plans WHERE type='diagnostic' ORDER BY id DESC LIMIT 10;` — copy the integer from query output, never from recall (Checklist item 15). (3) Session state spot-check: `SELECT id, title, lifecycle_state FROM plans WHERE closed_at IS NULL;` — replaces queue-heartbeat inference and ad-hoc decisions/ scans for status questions; NULL `closed_at` means in-flight. (4) Session-wrap cost roll-up: `SELECT plan_id, SUM(cost_usd), SUM(turns), SUM(duration_s) FROM steps WHERE plan_id IN (<session ids>) GROUP BY plan_id;` — replaces regex-on-raw_output step-log spelunking for closed session plans; step JSON logs remain the source for in-flight forensic detail.` New paragraph: `**Derivations citation convention.** An executable that implements a diagnostic MUST include the literal phrase `implements diagnostic <id>` (bare integer id) in its plan text — the Context (Rule 27) section is the recommended location. The daemon parses citations at claim with `re.finditer(r'implements\s+diagnostic\s+(\d+)', plan_text, re.IGNORECASE)` (lifecycle.py); only integer ids create `derivations` rows — legacy slug citations are skipped by design and produce no lineage row. The convention is UNENFORCED at claim (no gate validates it); Plan Authoring Checklist item 20 is the only guard.`
>
> **E9 — Plan Authoring Checklist items 19–22.** File: PLANNER_TEMPLATE.md. ANCHOR: `Source: proposal 119, lesson 2026-06-03` (the source line of checklist item 18). Insert the following four items immediately after that line and before the `---` separator that precedes `## Guardrails`:
>
> `### 19. Deposited filename is the draft placeholder` — body: `Before the atomic move into a Bellows-watched directory, confirm the destination filename matches `^(parallel-\d+-)?(executable|diagnostic|qa)-draft-\d{6}\.md$`. No descriptive slug and no date in the filename — the date belongs in the header `**Date:**` field and identity in the `# Title` line. A descriptive or date-bearing filename is not an error Bellows rejects (dual-format tolerance accepts it), which is exactly why this check exists at deposit time.` Source line: `Source: Reporting Phase 1, 2026-06-11`.
>
> `### 20. Derivations citation present and integer` — body: `If the plan implements a diagnostic, grep the composed plan for the literal phrase `implements diagnostic <id>` and confirm the id is a bare integer copied from a lifecycle-DB query or a `Done/diagnostic-<id>.md` filename (tool call, not recall — Checklist item 15). A legacy-slug citation parses to nothing and silently produces no derivations row. If the source diagnostic predates id-native naming (no integer id exists), omit the citation and note the lineage in the Context section prose instead.` Source line: `Source: Reporting Phase 1, 2026-06-11`.
>
> `### 21. Scope enumerations include implied test-infrastructure files` — body: `If the plan introduces any new module-level path or state constant, the allowed-file set MUST include the test-infrastructure files the repo's isolation pattern implies — `tests/conftest.py` at minimum (autouse isolation-fixture precedents). More generally, pre-authorize plausible-but-uncertain files with "only if needed" guidance: a wider list with conditional guidance costs nothing, while a narrow list that forces the agent to choose between halting and violating scope costs a full stop→reset→redeposit cycle. Second instance of the hand-typed-list failure class (BP2 2026-06-09 was the first).` Source line: `Source: lesson 2026-06-11`.
>
> `### 22. Never re-enumerate artifact-authoritative specifics inline` — body: `When a step designates a design artifact as authoritative for technical specifics (DDL, schemas, protocol formats, config shapes), reference it WITHOUT restating those specifics in the step prose — either quote the artifact byte-for-byte or instruct "the artifact is authoritative; deviation is a halt-and-report condition, not a judgment call," and pair the mandate with a QA conformance check against the artifact so deviation is caught mechanically. A Planner paraphrase placed next to an artifact reference is a competing spec, and paraphrase drift silently overrides the artifact — the agent follows the concrete inline enumeration, predictably. Distinction from item 14: file PATHS must be inlined in step bodies (scope_check requirement); technical SPECS must not be re-enumerated.` Source line: `Source: lesson 2026-06-11`.
>
> **E10 — Changelog row.** File: PLANNER_TEMPLATE.md. Locate the version-history table near the end of the file (the table whose rows begin with dates, e.g. the row anchored by `| 2026-06-03 | Lessons Forge 2026-06-03 cycle Gate 2 ratification (v4.59).`). Append a new final row: `| 2026-06-11 | v4.61: Reporting Phase 1 conventions codified. Id-native naming (deposit placeholder `<type>-draft-<HHMMSS>.md`, mint-at-claim rename to `in-progress-<type>-<id>.md`, id-canonical Done filing, `[id]` commit tags); plan_slug redefined as authoring-time descriptive slug (Rule 18/20 + RULE_20_SELF_CHECK_BLOCK.md placeholders); Rule 25 id-correlation via lifecycle.db placeholder lookup with filesystem fallback; verdict filenames id-keyed with format-agnostic copy-the-request-filename rule; new Lifecycle DB Read Protocol section (read-only URI, DB-as-index/filesystem-as-ground-truth, canonical queries, integer-only `implements diagnostic <id>` derivations citation); Checklist items 19–22 (placeholder filename check; derivations citation; implied test-infrastructure scope; no inline paraphrase of artifact-authoritative specifics — folds both 2026-06-11 LESSONS). |`
>
> **Conditional sweep (only if needed):** grep PLANNER_TEMPLATE.md for any discipline instructing a DB or directory query for the current maximum plan sequence number before naming a plan; if present, remove it (obsolete — Bellows mints all ids); if absent, record the no-op in the dev log. **After all edits:** run `grep -c "Version: 4.61" /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` style spot-checks for each edit and record results in the dev log. Commit BOTH governance-root files at the governance root (`/Users/marklehn/Developer/GitHub/`) with a single commit. Write a dev log covering every edit E1–E10 with its anchor, the post-edit line number, and the conditional-sweep result, to `bellows/knowledge/development/planner-template-v461-dev-log-2026-06-11.md`. Use `with open()` for deposits; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/planner-template-v461-dev-log-2026-06-11.md`

---
---

## STEP 2 — Bellows QA

---

> **MANDATORY — READ THIS FIRST. This step is gate-enforced by the daemon's `rule_20_self_check` gate.** The gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` followed by `PASSED — SELF-CHECK PASSED`. Your verification table does NOT satisfy this gate — the banner must appear verbatim in the report. Before finishing, self-grep your deposited report for the exact string `Rule 20 — QA Self-Check Results` and confirm it is present; if it is not, you have not finished. **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file, the DEV deposit `bellows/knowledge/development/planner-template-v461-dev-log-2026-06-11.md`, and the edited files `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` and `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` around each edit anchor. **Verify, each with executed evidence (commands + output captured to evidence files):** (1) **Edit landing** — for each edit E1–E10 in Step 1, grep the target file for the new text's distinctive phrase and confirm presence, and grep for the replaced anchor phrase and confirm absence where the edit replaced it; output to evidence file `edits_landed.txt`. (2) **Contradiction scan, deposit naming** — `grep -n "YYYY-MM-DD" /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` and classify EVERY hit: permitted classes are roadmap naming, header `**Date:**` field templates, `session-YYYY-MM-DD` evidence paths, changelog/history rows, and explicit legacy-tolerance text; ANY hit that prescribes a date-bearing deposit filename for executable/diagnostic/qa plans is a FAIL finding; output with classifications to `contradiction_scan_dates.txt`. (3) **Contradiction scan, plan_slug** — `grep -n "filename minus" /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md /Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`: remaining hits must only be the legacy parenthetical inside E6(a)'s replacement text; any surviving filename-derived plan_slug definition is a FAIL; output to `contradiction_scan_slug.txt`. (4) **Version integrity** — confirm `**Version:** 4.61` appears exactly once in the header and the v4.61 changelog row exists; output to `version_check.txt`. (5) **Canonical-block integrity** — diff RULE_20_SELF_CHECK_BLOCK.md against `git show HEAD~1:RULE_20_SELF_CHECK_BLOCK.md` (run at the governance root) and confirm exactly the two placeholder-description lines changed, banner strings byte-identical; output to `rule20_block_diff.txt`. (6) **Read-protocol query validity** — run each of the four canonical queries from the new Lifecycle DB Read Protocol section verbatim against `sqlite3 "file:/Users/marklehn/Developer/GitHub/bellows/lifecycle.db?mode=ro"` (substituting plan id 3 where a parameter is needed) and confirm each executes without error against the live schema; READ-ONLY URI ONLY — never open the DB read-write; output to `query_validity.txt`. Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `planner-template-v461-2026-06-11`; `qa_report_path`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/planner-template-v461-qa-report-2026-06-11.md`; `evidence_dir`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/planner-template-v461-2026-06-11/`; `required_evidence_files`: `[edits_landed.txt, contradiction_scan_dates.txt, contradiction_scan_slug.txt, version_check.txt, rule20_block_diff.txt, query_validity.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, do not proceed with closure — halt and report to CEO. Write the QA report with a verification table (item | method | evidence file | PASS/FAIL) and the Rule 20 self-check banner block to `bellows/knowledge/qa/planner-template-v461-qa-report-2026-06-11.md`. **Deposits:**
> - `bellows/knowledge/qa/planner-template-v461-qa-report-2026-06-11.md`
> - `bellows/knowledge/qa/evidence/planner-template-v461-2026-06-11/` (multiple files per Rule 20 self-check)
