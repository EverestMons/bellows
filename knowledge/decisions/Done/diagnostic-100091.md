# bellows — diagnostic: THE ABANDONED-RUNNER CENSUS — every way the daemon can stop while a step runs, what each way leaves on the eight surfaces a run writes (the plan row, the step row, the lane file, the worktree and its branch, the tuyere claim, the deposit receipt and its watcher, the verdict files, the model child), which existing tool restores which surface, and how the three running phantoms in the lifecycle DB arose — measured from the code, the agent's plist and its manual, the daemon logs, the DB and the two abandoned DEV transcripts of 2026-09-11, so the recovery thread 296 names is drafted from a table, not from one evening; no recommendation (thread 296)

**Date:** 2026-09-11 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic; writes one research doc, one TSV and a dev-log) | **Execution:** Step 1 (DIAGNOSTIC) | **pause_for_verdict:** after_step_1

**auto_close:** false

**Post-close:** no restart, no doctrine. The doc's tables are the input to the fix plan for thread 296 — and to the half of thread 294 that thread 297 left open, the daemon's own defence on a bare kickstart or kill — drafted after this doc by a plan of its own. This plan recommends nothing. Close acts: the Planner commits the two governance deposits at the wrap; thread 296 stays open for the fix.

**Depends on:** thread 296 (measured 2026-09-11 while recovering #100081 and #100082: four hand acts the tools did not perform) and the CEO's "I'm ready to continue" after the 31st wrap, on a list that named 296 next; the evidence-first rule of thread 290 (a plan's v0 rests on a measurement, captured). Clone origin by kind and layout: `Done/diagnostic-100085.md` (the action census, this evening): one DIAGNOSTIC step, numbered questions, doc + TSV as Planner-commit deposits, the WHOLE instrument in the appendix, the read-only rule extended to every write-bearing act, no recommendation.

**Tier computed (§1):** **T1** — T-1 fires (a read across the daemon, lifecycle, the claims gate, the watcher, the agent's plist, the logs and two transcripts); T-6 does not (no doctrine); T-2 does not (no act is performed to be measured).

## CEO Context

Two runners were abandoned today, and recovering each took four hand acts the tools do not perform (thread 296). Before a fix touches the daemon's exit path, the record should say every way the daemon stops while a step runs, what each way leaves behind, and what restores each piece — read from the code and the records, not generalised from the one path we saw. Several facts already shape the question. The drain exits 0 and writes nothing about which plan it abandoned. The agent restarts only a non-zero exit, so a bare `kill` leaves the daemon down, and launchd kills the rest of the job's process group when it exits, so the model child dies with the daemon: both lost DEV transcripts end on the drain's own second. Startup recovery skips any in-progress plan whose worktree exists. The reconcile tool restores the plan row, its verdict rows and its pending files, and nothing else. Claims are released at seven normal-end sites and never on an abandonment. The deposit watcher resolves a placeholder to its newest plan by id, so a redeposit's watcher reads the abandoned run and exits. A repeated claim decline is memoised and logged once per daemon process, while each rescan still logs `▶ started`. And of the three running phantoms in the DB, one is not an abandonment at all. This diagnostic puts all of that in one table, with the lines it rests on.

## Numbers discipline — measured 2026-09-11 by the Planner (bellows `36901bd`, governance `4ab04c65`, daemon pid 3106 under the agent)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the exit path in the daemon | `bellows.py:114` `_DRAIN_TIMEOUT = 30`; `_sigterm_handler` (:3581): a second signal during the drain logs `second signal during drain — forcing immediate exit` and exits 1 (:3583); otherwise it sets `_shutting_down`, logs `SIGTERM received — draining in-flight plans (30s timeout)` (:3588), waits for `_active_count == 0`, on timeout logs `drain timeout — <n> daemon thread(s) will be abandoned` (:3600), then `SIGTERM shutdown complete` (:3602), stops the observer and the response server, and calls `sys.exit(0)`; `_run_tracked` (:2863) decrements `_active_count` in a `finally` after `run_plan` returns; `handle_new_plan` (:2892) spawns the thread and logs `▶ started` (:2901) whatever the thread then does; startup recovery, `lifecycle.recover_half_claimed(` (:3738), runs once per watched project | `grep -nE 'def _sigterm_handler\|drain timeout\|▶ started\|recover_half_claimed\(' bellows.py` |
| P2 | ⛔ what launchd does next | `scripts/com.eluvian.bellows-daemon.plist.template`: `RunAtLoad` true (:36), `KeepAlive` with `SuccessfulExit` false (:37–41), `ExitTimeOut` 45 (:45), `ThrottleInterval` 30 (:46), and no `AbandonProcessGroup` key; per `man launchd.plist`, `SuccessfulExit` false restarts the job only after a non-zero exit, and without `AbandonProcessGroup` launchd kills the processes left in the job's process group when the job exits | `grep -nE 'KeepAlive\|SuccessfulExit\|ExitTimeOut\|AbandonProcessGroup' <template>`; `man launchd.plist \| col -b` at the two keys |
| P3 | ⛔ the model child | `runner.py:218` `proc = subprocess.Popen(` with neither `start_new_session` nor `preexec_fn` — the `claude -p` child shares the daemon's process group; the runner kills it only on its own timeouts (:287, :294); the two abandoned DEV transcripts under `~/.claude/projects/-Users-marklehn-Developer-bellows--bellows-worktrees-<id>/`: #100081 first event 20:07:05Z, last 20:25:27.987Z, 534 lines, beside the drain timeout logged at 15:25:27 local (20:25:27Z); #100082 21:01:28Z to 21:15:10.362Z, 306 lines, beside the drain timeout at 16:15:10 local | `grep -nE 'Popen\|start_new_session\|preexec_fn' runner.py`; the first and last `timestamp` of each transcript |
| P4 | ⛔ recovery and reconcile | `lifecycle.py:330` `recover_half_claimed(decisions_dir, db_path=None, project_root=None, age_guard_seconds=300)`: a half-claimed arm (a row whose deposit file still exists is re-renamed, else abandoned past the age guard) and an in-progress-strand arm (:402–440) whose discriminator is the worktree — "a legitimately-running step has its worktree" — so a plan whose `.bellows-worktrees/<id>` exists is `skipped_worktree_exists` (:434) and one without it is abandoned; `tools/reconcile_plan.py` writes `UPDATE plans` (:102) and `UPDATE verdicts` (:106), renames pending verdict-request files into `verdicts/archived/` (:126), and prints the remaining human acts, naming `knowledge/decisions/<type>-<id>.md`, a file an in-progress plan never has; `tools/reconcile_steps.py` lists `running` steps on plans not in progress and, under `--apply`, flips only `awaiting_verdict` rows on closed plans | `sed -n 330,440p lifecycle.py`; `grep -nE 'UPDATE\|rename' tools/reconcile_plan.py`; `tools/reconcile_steps.py --help` |
| P5 | ⛔ the claims gate | `plan_claim.py` `claim_gate` (:123): a repeated outcome for the same slug returns False with no log — `last = _outcome_memo.get(slug)` (:133), `if last == outcome:` (:134), `_outcome_memo[slug] = outcome` (:136), a memo that lives as long as the daemon process; the first decline logs `claim declined for <slug>: <detail> — if the holder is this machine this is a stranded claim — recover: tuyere.claims release <slug> --reason self-strand` (:150); `release_for_plan` (:157) is called at seven sites in `bellows.py` — teardown-failed park :953, disk-preflight abort :1138, zero-step skip :1190, auto-close :1575, rejected verdict :3412, continue-to-done :3450, stop verdict :3489 — and at none on an abandonment | `sed -n 123,160p plan_claim.py`; `grep -nF 'release_for_plan(' bellows.py` |
| P6 | ⛔ the deposit watcher | `tools/gate_watcher.py` `read_state(name, …)` (:62) resolves a placeholder with `SELECT id, lifecycle_state FROM plans WHERE deposit_placeholder_name = ? ORDER BY id DESC LIMIT 1` (:73–74) — the newest plan by id for that name; the watcher is armed by `tools/deposit_receipt.py` at the receipt, before any claim; measured in `logs/watch/executable-bellows-readonly-wal-open-v2.md.log`: at 15:57:27 and 15:57:36 on 2026-09-11, `WATCH: abandoned id=100081` then `WATCH: terminal — abandoned; exiting`, each within a second of arming | `sed -n 62,80p tools/gate_watcher.py`; the watch log |
| P7 | ⛔ the lifecycle DB (`mode=ro`) | plan 100051 `abandoned`, placeholder `d.md`, created 2026-09-09T11:10:35, closed 2026-09-09T16:34:27Z; plan 100081 `abandoned`, created 2026-09-11T15:07:00, closed 20:57:16Z by the reconcile tool; plan 100082 `abandoned`, created 16:01:24, closed 21:27:57Z; their steps 90, 141 and 142 all `running`, with no end and no cost; no verdict rows for 100081 or 100082; three abandoned plans in the DB and three running phantoms; the tuyere claim on `executable-bellows-readonly-wal-open-v2`: event released, seq 2, at #100083's close | the three queries against `file:lifecycle.db?mode=ro`; `tuyere.claims status <slug>` |
| P8 | the logs | `SIGTERM received` 38 times in `logs/terminal/bellows-*.log` from 2026-08-27 to 20:44 on 2026-09-11 (by day: 08-27 1, 08-31 1, 09-01 4, 09-02 3, 09-04 1, 09-05 1, 09-07 5, 09-08 5, 09-09 5, 09-11 12); `drain timeout` twice, both on 2026-09-11 (15:25:27, 16:15:10); `skipped_worktree_exists` five times, all on 2026-09-11; no drain timeout on 2026-09-09 — plan 100051's placeholder `d.md` is a test fixture's name, and LESSONS.md:6564 records a cold-panel execution seat on 2026-09-09 that was permitted to import `lifecycle`, the incident behind STANDING_CONSTRAINTS line 5 | `grep -cF 'SIGTERM received' logs/terminal/bellows-*.log`; `grep -cF 'drain timeout'`; `sed -n 6564,6570p LESSONS.md` under the governance root |
| P9 | in-flight; class; interpreter | #100088 in flight at drafting; thread 299's and thread 298's plans closed and queued ahead of this one; writes: a bellows dev-log and two governance research files → **app-feature** (`_assign_class`, walk 0) — auto-clears, and the verdict is the Planner's own call after the (b) read; the bellows venv for bellows and the DB, `/Users/marklehn/Developer/tuyere/.venv/bin/python` for tuyere's read verbs | `.venv/bin/python status.py`; `_assign_class` |

## MUST-PRESERVE

- ⛔ **Read-only, and no act is performed to be measured.** No signal is sent, no daemon is stopped or restarted, no `launchctl` verb runs, no reconcile, claim release, worktree or branch command runs; the model child's fate is read from the two transcripts, never reproduced.
- ⛔ **Enumerated, never recalled.** Every row cites the file and line, the log line or the DB query it rests on; a surface the DEV cannot read is listed as `unread` with the reason.
- ⛔ **The instrument is whole in the appendix** — every line of the script, never a header with a pointer to `$T`.
- ⛔ **No recommendation.** The doc answers Q1–Q5 and ends with "What this does not establish"; which surfaces a recovery restores, and whether the daemon refuses to exit mid-step, are the fix plan's.
- ⛔ **`lifecycle` is never imported;** the DB is read with `sqlite3` in `mode=ro`; tuyere is invoked only through `claims status` and `threads show`.

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-abandoned-runner-census-2026-09-11.md
**Walks:** walk 0 pinned (P1–P9 measured on bellows `36901bd`, governance `4ab04c65`; clone-diff against `Done/diagnostic-100085.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py`; the tool run bare, its exit read; no lens or walk number in any `--desc`; `lens_order_check` after every walk.

- Weak spots:          w1 2 folded — instruction 2 / record 0; w2 1 folded — instruction 0 / record 1
- Destruction:         w1 dry; w2 dry
- Vulnerabilities:     w1 1 folded — instruction 1 / record 0; w2 dry
- Integration-record:   w1 dry; w2 dry
- ACID:                 w1 dry; w2 dry
**Closing:** WARM close after walk 2 — BAR MET (T1), thread 296's measurement before its fix. Two walks: 3 → 0 (walk 1 named where the hand acts of 2026-09-11 are recorded, made the census take its two before-readings at its start so the after-readings have something to equal, and limited what it reads from the two transcripts to their timestamps and line counts; walk 2 removed a drafting placeholder from the record and was dry on every lens). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-` after #100088 and the plans for threads 299 and 298 close (P9); class app-feature auto-clears; the daemon claims and the verdict is the Planner's own call after the (b) read. Close acts: the Planner commits the two governance deposits at the wrap; the fix plan for 296 follows.

## Cycle Manifest
tier: T1
target: knowledge/development/dev-log-abandoned-runner-census-2026-09-11.md
class: app-feature
reads: bellows.py, lifecycle.py, runner.py, plan_claim.py, tools/gate_watcher.py, tools/deposit_receipt.py, tools/reconcile_plan.py, tools/reconcile_steps.py, scripts/com.eluvian.bellows-daemon.plist.template, lifecycle.db, logs/terminal/bellows-2026-09-11.log, logs/watch/executable-bellows-readonly-wal-open-v2.md.log, knowledge/decisions/Done/diagnostic-100085.md, LESSONS.md, STANDING_CONSTRAINTS.md
writes: knowledge/development/dev-log-abandoned-runner-census-2026-09-11.md, governance/knowledge/research/abandoned-runner-census-2026-09-11.md, governance/knowledge/research/abandoned-runner-census-2026-09-11.tsv
open_forks: 1. the fix plan for thread 296 — which surfaces a recovery restores and how the daemon records an abandonment; 2. the daemon's own defence on a bare kickstart or kill (the open half of thread 294) — a drain that refuses to exit while a step runs, or a non-zero exit so the agent restarts it; 3. the watcher's resolution keyed on the receipt's time, so a redeposit never reads an earlier run
walks: 2
yields: 3, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:12
coherence: 2/2 body walks named in the register (4 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.diagnostic-bellows-abandoned-runner-census.md.foldcheck.json

---

## STEP 1 — DIAGNOSTIC: five questions over every way the daemon stops mid-step and every surface a run leaves

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE; tuyere's is `/Users/marklehn/Developer/tuyere/.venv/bin/python`, run with `cd /Users/marklehn/Developer/tuyere`.** ⛔ **Read-only:** never run the daemon, `run_plan`, a claim, `bellows.py stop|restart`, any `launchctl` verb, `kill`, `tools/reconcile_plan.py`, `tools/reconcile_steps.py --apply`, `tuyere.claims release`, or a `git worktree`/`git branch` command; never import `lifecycle`, `bellows`, `plan_claim`, `runner` or `gate_watcher` — the census reads them as TEXT; the DB is opened `sqlite3.connect("file:/Users/marklehn/Developer/bellows/lifecycle.db?mode=ro", uri=True)` and never written; the logs and the two transcripts are read, never moved; `T=$(mktemp -d /tmp/abandoned-census.XXXXXX)` holds the script and scratch output; nothing is written outside the three deposits.
>
> **Scope:**
> - `knowledge/development/dev-log-abandoned-runner-census-2026-09-11.md`
>
> **Item 1 — re-derive P1, P4, P5 and P6 and HALT on a mechanism mismatch** (a line-number drift is not a mismatch; a drain that already records the plan it abandons, a recovery whose discriminator is no longer the worktree, a `release_for_plan` call already on an abandonment path, or a watcher that no longer resolves by newest id, IS one). First record the two before-readings the post-conditions compare against — `PRAGMA data_version` on the DB in `mode=ro`, and `git -C /Users/marklehn/Developer/bellows status --porcelain -- knowledge/qa/evidence` — and paste both. Then write the census script to `$T/census.py` — verbatim in the doc's appendix — reading the named files as text (regular expressions or `ast`), the DB in `mode=ro`, the logs, and, from each transcript, only the `timestamp` field of its first and last lines and its line count — no other field of a transcript is read, and none is quoted in the doc, because a transcript carries whole tool outputs; paste each pin's re-derived value beside it.
> **Item 2 — Q1: the exit paths.** One row per way the daemon can stop while a step runs — SIGTERM with the step finishing inside the drain; SIGTERM with the step outliving the drain; a second signal during the drain; SIGKILL; an uncaught exception on the daemon's main thread; an exception inside a runner thread; the host asleep; a logout or reboot — with the daemon's code path quoted from P1, the exit status, what launchd does next by P2, what happens to the model child by P3, the log line the path leaves, and its count in the logs by P8; a path whose behaviour is read from the manual and not observed in a log is marked `inferred`.
> **Item 3 — Q2: the surfaces after an abandonment.** For the path that happened twice (a step outliving the drain), the eight surfaces — plan row, step row, lane file, worktree and branch, tuyere claim, deposit receipt and watcher, verdict files, model child — each with its state after the exit, after the daemon's restart and startup recovery, after `tools/reconcile_plan.py`, and after the hand acts of 2026-09-11 (thread 296's body names them: the worktree removed, the branch deleted, the lane file moved to `Done/abandoned-executable-<id>.md`, the claim released `self-strand`, the stale receipt archived); every state cites the DB row, the log line, or the record that holds it — for the hand acts, thread 296's body (`tuyere.threads show 296`), the two committed lane files `knowledge/decisions/Done/abandoned-executable-100081.md` and `knowledge/decisions/Done/abandoned-executable-100082.md`, the wrap commit `36901bd`'s file list (`git show --stat 36901bd`), and the 31st wrap's block in governance `shop_next_session.md`.
> **Item 4 — Q3: the restore matrix.** Each existing mechanism — `recover_half_claimed`'s two arms, `tools/reconcile_plan.py`, `tools/reconcile_steps.py` in both modes, `tuyere.claims release`, `plan_claim.release_for_plan` at its seven sites, the gate watcher — against each surface: `restores`, `lists`, or `untouched`, with the code line that decides it.
> **Item 5 — Q4: the three phantoms.** Steps 90, 141 and 142 by P7: the plan, its placeholder, its created and closed times, and the path that produced each — the drain timeout and its log line for 141 and 142, and for 90 the record of 2026-09-09 (the `d.md` placeholder and LESSONS.md:6564), stated as what the record shows, not re-run.
> **Item 6 — Q5: the two quiet signals.** The claims memo (a decline logged once per slug per daemon process, while `▶ started` is logged on every rescan) and the watcher's resolution (the newest plan by id for a placeholder), each with the lines of 2026-09-11's logs that show it.
> **Item 7 — write the research doc** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/abandoned-runner-census-2026-09-11.md` answering Q1–Q5 in order, each section opening with the script's raw output line it rests on, the census script as a code block in an appendix (⛔ the WHOLE script, every line — the appendix is the instrument's only surviving copy), and the TSV `abandoned-runner-census-2026-09-11.tsv` — one row per (exit path, surface), columns `exit_path, surface, state_after_exit, state_after_restart, restored_by, observed_or_inferred, source`; end with "What this does not establish" (at least: the paths marked `inferred` were not observed; the model child's death on the drain's second is two samples; phantom 90's origin is read from the record, not reproduced; the Air runs the same code and was not read; which surfaces a recovery should restore, and whether the daemon should refuse to exit mid-step, are the fix plan's).
> **Item 8 — dev-log** with the headings declared below (the `dev_log_declared_text` gate reads them at the pause); `## Pins re-derived (P1, P4, P5, P6)` opens with P7's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `at #100083's close`; paste through those words, then stop. **Commit** the dev-log alone (message tagged with the plan id and `thread 296`), gated on the FULL suite (`/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q` from the worktree root — nothing under bellows changed but the rule is the rule), path-scoped to the Scope list; the two governance files are Planner-commit deposits. Last act, after the commit: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P1, P4, P5, P6)`; `## The surfaces after an abandonment (Q2)`; `## Q1–Q5 in one table`; `## What the doc does not establish`
> **Verbatim:** `## Pins re-derived (P1, P4, P5, P6)` ← P7
>
> **Deposits:**
> - `knowledge/development/dev-log-abandoned-runner-census-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/abandoned-runner-census-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/abandoned-runner-census-2026-09-11.tsv`
>
> **Post-conditions:** the doc answers five questions, every row citing its source, and ends without a recommendation; the TSV has one row per (exit path, surface) and the row count the doc states; the appendix holds the whole script (`grep -cE '^(import|def |from )' <doc>` at least the script's own count of those lines, pasted beside it); the dev-log's four headings present as full lines with P7's cell whole; one bellows commit carrying the dev-log only; no gate, tool, script, lane file, DB row, daemon, claim or worktree changed (`git -C /Users/marklehn/Developer/eluvian-governance status --porcelain -- governance/knowledge/research/` lists the two research files, any other entry named as pre-existing; `git status --porcelain` in the worktree empty after the commit; `PRAGMA data_version` on the DB equal before and after the census, or the doc names the daemon write that moved it; `git -C /Users/marklehn/Developer/bellows status --porcelain -- knowledge/qa/evidence` equal before and after); nothing under `$T` that survives the step.
