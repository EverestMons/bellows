# Bellows — Scope_check False Positives on Infrastructure Files
**Date:** 2026-04-16 | **Type:** diagnostic (investigation only, no fixes)

## How to Run This Plan

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bellows-scope-check-infra-files-2026-04-16.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further or move the plan to Done.
```

---
---

## STEP 1 — INVESTIGATION AGENT (bellows developer)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-scope-check-infra-files-2026-04-16.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-scope-check-infra-files-2026-04-16.md")`. Skip specialist file and glossary reads — pure code-tracing. **Context:** Scope_check fires false positives on every plan that runs through Bellows. The flagged files are always Bellows' own infrastructure: `verdicts/ledger.jsonl` (written by `verdict.log_to_ledger` on every verdict cycle), plan files themselves (moved by `shutil.move` during claim/verdict-pending/Done transitions), and `knowledge/research/agent-prompt-feedback.md` (appended by agents per standard feedback protocol). These are NOT agent scope violations — they're expected side effects of Bellows' own dispatch and the feedback protocol. Today's session had ~8 plans flag scope_check for these exact files, requiring manual `continue` verdicts each time. **Q1 — How does scope_check work?** Read `gates.py` in full. Find the scope_check gate function. Show: (a) what it receives as input (the plan text, the files_changed list, the step number?), (b) how it determines what's "in scope" for a step — does it parse the step's prompt text for file references? Does it use a whitelist? (c) how it determines what's "out of scope" — is it everything in files_changed that doesn't appear in the step text? Show the literal comparison logic. **Q2 — What files trigger false positives and why?** For each recurring false-positive file, trace why scope_check flags it: (a) `verdicts/ledger.jsonl` — written by Bellows itself between steps, not by the agent. Why does it appear in `files_changed`? Is it because `_parse_diff_stat` captures it in the post-step git diff? Is `ledger.jsonl` even tracked by git, or is it in `.gitignore`? (b) Plan files (e.g., `in-progress-executable-*.md`, `verdict-pending-*.md`) — moved by Bellows during dispatch. Same questions. (c) `agent-prompt-feedback.md` — appended by the agent as part of the standard feedback protocol. This IS an agent-authored change but it's a known cross-cutting concern (every step appends to it). Why does scope_check flag it? **Q3 — What's the right fix approach?** Three options to evaluate: **(A) Allowlist in scope_check** — maintain a list of known infrastructure files/patterns (e.g., `*/ledger.jsonl`, `*/agent-prompt-feedback.md`, `*in-progress-*`, `*verdict-pending-*`) that scope_check ignores. Simple, explicit, but requires maintenance when new infrastructure files are added. **(B) Filter in `_parse_diff_stat`** — strip infrastructure files from the files_changed list before passing to gates.check. Keeps scope_check clean but moves the filtering upstream. **(C) `.gitignore` for non-tracked files** — if `ledger.jsonl` and plan-file moves show up in diff because they're untracked, adding them to `.gitignore` would prevent them from appearing in `git diff --stat`. Check: is `ledger.jsonl` gitignored? Are the `knowledge/decisions/` plan files tracked? Would gitignoring them break anything? Evaluate each option on: (i) how many lines of code, (ii) whether it handles future infrastructure files automatically or requires manual updates, (iii) whether it could accidentally suppress a real scope violation. Recommend one. **Q4 — Could scope_check be smarter about step context?** The current scope_check (from Q1) presumably parses the step text to determine what files are expected. Could it be improved to: (a) recognize `agent-prompt-feedback.md` as a standard protocol file that every step touches, (b) recognize that files OUTSIDE the project directory (like bellows' own files) are inherently out of the agent's control, (c) distinguish between "agent modified this file" vs "Bellows infrastructure modified this file between pre and post diff"? The last point is key — `_parse_diff_stat` now uses diff-of-diffs, but Bellows' own writes (ledger, plan moves) happen DURING the step execution window, so they show up as real diffs. Is there a way to exclude Bellows-authored changes? **Q5 — What's in `.gitignore` today?** Read `/Users/marklehn/Desktop/GitHub/bellows/.gitignore`. List what's ignored. Specifically check: is `verdicts/ledger.jsonl` ignored? Is `verdicts/pending/` ignored? Is `verdicts/resolved/` ignored? Is `bellows.db` ignored? Is `knowledge/decisions/` tracked? Understanding what git sees vs doesn't see is essential for knowing which files can appear in `git diff --stat`. **Deposit:** write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/scope-check-infra-files-diagnostic-2026-04-16.md`. Verify you answered ALL FIVE questions before marking Status=Complete. **Commit:** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/scope-check-infra-files-diagnostic-2026-04-16.md && git commit -m "docs: diagnostic — scope_check false positives on infrastructure files"`. **Feedback:** standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed. Do NOT move the plan to Done. Report completion and wait for CEO confirmation.**
