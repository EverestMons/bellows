# Bellows — isinstance(f, dict) asymmetry between pause-reason blocks
**Date:** 2026-05-21 | **Tier:** Diagnostic | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1 | **auto_close:** false

## Context

`bellows/knowledge/BACKLOG.md` open item: Block 1 at `bellows.py:505` has a defensive `isinstance(f, dict)` guard before processing failure entries; Block 2 at `bellows.py:594` does not. The 2026-05-21 verdict-enrichment QA classified the asymmetry as non-defect because Block 2's code path only receives dict-format failures in practice. This diagnostic determines whether the asymmetry is genuinely safe (and the proposed symmetric guard is purely cosmetic) or whether there is a real future-refactor risk worth a 1–2 LOC defensive fix.

**Scope:** read-only investigation of `bellows.py` only. No fixes, no code changes. Findings deposit goes to `bellows/knowledge/research/`. The Planner reads findings and authors a separate executable if action is warranted.

---
---

## STEP 1 — Bellows Systems Analyst

---

> You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. **Skip glossary read — this is code-tracing task.** **Task:** investigate the `isinstance(f, dict)` asymmetry between Block 1 (`bellows.py:505`) and Block 2 (`bellows.py:594`). Both blocks process the same conceptual data (gate failure entries) but only Block 1 has the defensive type guard. Determine whether the asymmetry is safe or whether a symmetric guard is warranted. **Investigation questions (answer each in the findings deposit):** (1) **Block identification.** Read `bellows.py` and locate the exact code at lines 505 and 594. For each, report: the function or method containing the block, the variable name being checked (or NOT checked) for dict-ness, the loop or iteration context, and the immediate next operation that depends on the dict-ness assumption (e.g., `.get()`, subscript access, `**` unpacking). Quote the actual code for each block with 5 lines of surrounding context. (2) **Upstream data flow.** For each block, trace backward to identify every code path that populates the variable being iterated. Where do failure entries enter the data structure? List every assignment, append, or extend operation that contributes to the iterable Block 1 and Block 2 each read. For each contributing site, report the format of the entry being added (dict literal, dict constructor, namedtuple, dataclass, string, other) and the line number. (3) **Format invariants.** Based on the upstream trace, classify each contributing site by the entry format it produces. Is there ANY upstream site that can produce a non-dict entry reaching Block 1 OR Block 2? If yes, which sites and under what conditions? If no, what enforces the dict invariant — is it explicit (type annotation, assertion, schema validator) or implicit (convention only, future-refactor-fragile)? (4) **Defensive-guard cost.** What would adding a symmetric `isinstance(f, dict)` guard to Block 2 cost? Quote the exact 1–2 line diff that would mirror Block 1's pattern. Are there any code paths where the guard would change observed behavior today (i.e., is there a non-dict entry currently reaching Block 2 that the guard would silently skip)? If yes, that's a bug-masking risk and the fix needs different handling. (5) **Future-refactor risk.** Identify the realistic future changes that could introduce non-dict entries into Block 2's iterable. Are there any in-flight or planned features (verdict-enrichment Phase 2, gate additions, failure-format normalization) that would touch the failure entry shape? Reference `bellows/knowledge/BACKLOG.md` and recent commits to `bellows.py` (since 2026-05-01) to identify pending work that interacts with this code. (6) **Recommendation.** Produce a Gap Assessment table with columns `| Gap | Current State | Proposed State | Change Required |` per the Diagnostic Prompt Engineering convention. Rows: (a) Block 2 defensive guard, (b) any non-dict-producing upstream site identified in Q3, (c) any other asymmetry surfaced during investigation. For each row, specify whether the change is recommended (yes/no/defer) with one-sentence rationale. **Read depth:** `bellows.py` full read is required for accurate trace; do NOT skim. All other reads (test_bellows.py, gates.py, verdict.py) are on-demand only if the data-flow trace surfaces dependencies into those files. **Output format:** structured markdown deposit at `bellows/knowledge/research/bellows-isinstance-asymmetry-2026-05-21.md` with sections matching the question numbers above (1–6). Quote code verbatim with line numbers. Cite anchor lines for any future executable's edit_block to use. **Do NOT propose code edits in this deposit.** The Planner authors the executable based on findings. **Standard prompt feedback protocol** → append entry to `bellows/knowledge/research/agent-prompt-feedback.md`. **Commit:** stage findings file and feedback log; commit message `docs: SA findings — bellows isinstance asymmetry diagnostic`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/knowledge/research/bellows-isinstance-asymmetry-2026-05-21.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
