# bellows — THE CLASSIFY TOOL RUNS AS A SCRIPT: `tools/classify_deposit.py` puts the bellows root and `scripts/` on `sys.path` as its peer tools do, and a test runs it as a script from outside the repository — the fix #100100's stop asked for (thread 329)

**Date:** 2026-09-14 | **Project:** bellows | **Tier:** Small (three lines in one tool, one test and one import in its test file, one mutation manifest, one dev-log) | **Dispatch Mode:** bellows | **Test Scope:** full-suite (Rule 21 — `pytest tests/ -q`) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 329 | **cycle_tier:** T1

**auto_close:** false

**Post-close:** no restart — the tool and its test file; no daemon module imports the tool. Two bound follow-ups, carried from #100100, which stopped before either fell due. (1) The CEO's apply act: bellows `CLAUDE.md`'s *Depositing a plan* step 2 runs `.venv/bin/python tools/classify_deposit.py <plan-path> --project-root <root>` and reads its `RESULT:` line in place of the scratch `Depositor` import — the doctrine half of thread 329, split off under DRAFTING_CYCLE §1's split rule so these plans stay T1; until it lands, nothing runs the tool and step 2 does not mention it. (2) The Planner's check on T-3's environment: once the Air pulls, run the tool there under its 3.9.6 venv, read-only, as a script from outside the repository — `cd /tmp && ~/Developer/GitHub/bellows/.venv/bin/python -B ~/Developer/GitHub/bellows/tools/classify_deposit.py ~/Developer/GitHub/bellows/knowledge/decisions/Done/executable-100073.md --project-root ~/Developer/GitHub/bellows`, every path absolute (from outside the repository a relative plan path is `plan is not a file`, exit 2), the project root the Air's checkout, the argument being required, and `-B` so the imports write no bytecode cache into the Air's tree — and compare its `RESULT:` line with the mini's — pasted into `tuyere.threads done 329 --commit bellows:<this plan's first DEV commit>`.

**Depends on:** #100100's stop (the Planner's verdict of 2026-09-14 20:36:36; `halted-executable-100100.md`, tracked with the next wrap): its tool, merged as `d1f595c`, `a9d3d05`, `870e53b` and `65f713c`, does not start when run as a script — `ModuleNotFoundError: No module named 'bellows_root'` — and QA Item 2's first run, the plan's one script run, caught it (P3); thread 329 (open) and the CEO's rulings of 2026-09-14 on it — build the tool first, the first overnight plan of the reliability stretch. Clone origin BY KIND: #100100 itself (`knowledge/decisions/halted-executable-100100.md`: the same tool, its test file and fixtures, the DEV/QA shape and QA Item 2's three plans, re-run here as written); the path lines its peers carry (P2).

**Tier computed (§1):** **T1** — T-3 fires: the tool runs at each deposit under that machine's venv — the Air's Python 3.9.6 as well as the mini's 3.12.14, where the suite runs; t5 parses it under `/usr/bin/python3` 3.9.6, and the Post-close run on the Air is its runtime check. T-6 does not fire: the one doctrine edit thread 329 needs stays split off as a bound Post-close apply act (P5 of #100100, carried). T-8 does not fire (the fix is its peers' path setup — `check_deposit.py`'s root, `fold_signal_census.py`'s order — and the plan a narrower clone of #100100); T-1, T-2, T-4, T-5 and T-7 do not.

## CEO Context

Your release of thread 329's tool plan ran tonight as #100100, and I stopped it: run the way the plan said — `.venv/bin/python tools/classify_deposit.py <plan> --project-root <root>` — the tool could not start, because it never puts the bellows folder on Python's import path; its tests ran it inside pytest, where the path was already set, and its one script run, QA's, found it. The defect was my plan's. This plan adds the three lines the tool's sibling tools carry, and a test that runs the tool the way you and I will — as a script, with nothing on the path, here from outside the repository. Nothing else about the tool changes, and QA re-runs #100100's three plans as that plan wrote them. The class is shop-infra, so it holds for your release; nothing restarts. After it merges, the `CLAUDE.md` step-2 edit — carried from #100100's Post-close — becomes yours to apply.

## What this changes

1. **`tools/classify_deposit.py`** — after `import sys` (:6) and the blank line below it (:7), three module-level lines and then one blank line, so the first `def` keeps its two blank lines and the tool's `git diff` adds four lines and removes none: `_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`, `sys.path.insert(0, os.path.join(_ROOT, "scripts"))`, `sys.path.insert(0, _ROOT)` — the repository root and `scripts/` at the front of the import path, the root searched first: the root's expression is `tools/check_deposit.py:25`'s and the order `tools/fold_signal_census.py:60–61`'s, while `tools/cycle_log_signal_census.py:37–38` inserts the two the other way round — no module name is in both directories or in the standard library (measured, the register's walk 1), so the order decides nothing (P2): the tool imports `bellows_root` (:55) and `depositor` (:84) from the root and `cycle_check` (:83) from `scripts/`. The imports stay inside `main()`, after the arguments parse, so `--help` and every usage error still import nothing of the depositor's; nothing else changes.
2. **`tests/test_classify_deposit.py`** — `import os` added to its imports; **c9** appended after c8: the tool run as a script — `subprocess.run([sys.executable, <the tool's path>, <plan>, "--project-root", <root>, "--config", <cfg>], cwd=<tmp_path>)` — from a directory outside the repository, with `PYTHONPATH` removed from its environment and `ELUVIAN_WRAP_ROOT` pointed at the fixture's governance root, a `COMPANY.md` written in it (the resolver's marker, so the run resolves the same in any environment, a fenced HOME included), over c1's plan → no `Traceback` on stderr, `writes (2) from manifest` and a `RESULT:` line on stdout, exit 0, 1 or 3. c9 pins the entry point, not the class: the class is c1–c8's, pinned in-process under the fixture's patched resolvers; a subprocess sees the real ones, which place a tmp project root by where the repository copy lives (`_assign_class` classes each write by the repository `_resolve_write` finds under the projects parent, and `resolve_projects_parent()` is the bellows root's parent), so the class a tmp root reads varies with the copy's location (P4). c1–c8 unchanged, byte for byte.
3. **`knowledge/mutants/classify-deposit-script.json`** — `{"target": "tools/classify_deposit.py", "mutants": [ … ]}`, each mutant's keys `name`, `why`, `anchor`, `replacement` and `expect_fail`, two, each `expect_fail` c9's node id: (m1) `no-path-setup` — `_ROOT` pointed at a missing directory, #100100's shipped state; (m2) `root-only` — `scripts/` left off, so `cycle_check` cannot import. An anchor that matches its target other than once is the run's `ERROR` (`tools/mutation_check.py:266–270`).
4. **The dev-log** `knowledge/development/dev-log-classify-deposit-script-2026-09-14.md`.
5. **Not changed:** the tool's logic, its output and its exit statuses; c1–c8; `tests/test_tools_safe_to_invoke.py`, which runs every tool with `--help` alone; `depositor.py`; `knowledge/mutants/classify-deposit.json`, #100100's seven mutants, each anchor still matching once over the fix (`MUTATION: 7 killed, 0 survived, 0 error`, measured on the prototype).

## Why this exists

#100100 shipped a Planner-run tool that does not start when run as a script: Python puts `tools/` on the path, not the repository root, and the tool imports `bellows_root`, `cycle_check` and `depositor` without the `sys.path` setup its peers carry. Its plan — the Planner's — had the tests put the root and `scripts/` on `sys.path` and run the tool in-process through `main(argv)`, and never asked the tool to do the same; its only subprocess runs, c7 and the safety file's `--help`, exit in argparse before any import, which the plan's own import order designed. QA Item 2's first run — the plan's one script run — failed with the error, a result the plan made a HALT; the QA worked around it with `PYTHONPATH`, and the Planner's verdict stopped the plan (P3). This plan makes the tool run as its consumer runs it, and pins that with a test that runs it so.

## What this does NOT do

- It does not change what the tool decides or prints: the class logic is the depositor's, c1–c8 pin it, and they stay byte for byte.
- It does not pin the class of a plan run as a script — c9 pins the entry point (What this changes 2).
- It does not write the `CLAUDE.md` step-2 edit (the CEO's apply act, carried in Post-close).
- It does not revert #100100's commits: this plan fixes forward, over them.

## MUST-PRESERVE

- ⛔ **No database.** The tool calls no `sqlite3.connect`, no `lifecycle` function and no `status` function; c5 proves it with `connect` made to raise. *(Carried from #100100, as its next four.)*
- ⛔ **The depositor's own functions, unchanged.** The tool calls `_parse_plan` and `_assign_class` and re-implements neither; `depositor.py` is not in Scope.
- ⛔ **Writes FIRST.** `_parse_plan` returns `(writes, reads, declared_class)`; the tool passes `writes` to `_assign_class` — #100100's m1 is the mutant that swaps them.
- ⛔ **The project root is required** — no default, as `_assign_class`'s own argument has none.
- ⛔ **t1–t6 of `tests/test_tools_safe_to_invoke.py` pass unchanged,** with the tool in their globs.
- ⛔ **The tool's logic, output and exit statuses are unchanged** — the three path lines, with the blank line below them, are the only edit to it.
- ⛔ **`--help` and every usage error import nothing of the depositor's** — the imports stay inside `main()`, after the arguments parse (t3 of `tests/test_tools_safe_to_invoke.py`; c7, c8).
- ⛔ **c1–c8 pass unedited, byte for byte;** `import os` is the file's one edit above c9.
- ⛔ **c9 runs the tool with no `PYTHONPATH`** — a run that sets one proves nothing this plan exists for.
- ⛔ **Every `expect_fail` is a node id of this plan's tests** — c9's, or the test a survivor fix adds (Item 5) — and the mutation run is redirected into its deposit, never piped.
- ⛔ **Test code runs under pytest inside `tests/` or not at all** (bellows `CLAUDE.md`, *Test code*; thread 313).
- ⛔ **The thread-262 stop:** a QA step that must change production code STOPS and requests a verdict.

## Numbers discipline — measured 2026-09-14 by the Planner (bellows `65f713c`; governance `f4347fe9`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the tool | `tools/classify_deposit.py`, 149 lines, by #100100 (`d1f595c`): the imports `argparse` … `sys` :2–6; `main(argv=None)` :34, `parser.parse_args(argv)` :50; `import bellows_root` :55 (the default config's path); `import cycle_check` :83 and `import depositor as dep_mod` :84; no sys.path line anywhere in the file | `sed -n '1,8p;50,56p;80,86p' tools/classify_deposit.py`; `grep -c -F sys.path tools/classify_deposit.py` → 0 |
| P2 | ⛔ the peers' path setup | `tools/check_deposit.py:25` `sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`; `tools/cycle_log_signal_census.py:37–38` the root then `scripts/`, `tools/fold_signal_census.py:60–61` `scripts/` then the root — each at index 0, so the second insert is searched first; `tools/clear_plan.py:110`, `:143`, `:166` inside functions | `grep -n -F "sys.path.insert(0" tools/*.py` |
| P3 | the defect | #100100's QA Item 2, first run: `ModuleNotFoundError: No module named 'bellows_root'`, exit 1 (`logs/20260914-201458-step.json`, command 7); reproduced by the Planner on main (`65f713c`) from the canonical checkout, no `PYTHONPATH`, at `tools/classify_deposit.py:55`; `--help` exits 0 (argparse exits before the import) | `.venv/bin/python tools/classify_deposit.py knowledge/decisions/Done/executable-100073.md --project-root /Users/marklehn/Developer/bellows` |
| P4 | ⛔ the tests | `tests/test_classify_deposit.py`: 8 tests, 241 lines; the root and `scripts/` put on `sys.path` at its head; `TOOL_PATH`, `PYTHON = sys.executable`; the autouse `patch_roots` :93–99 — `tmp_path / "governance"` made and `resolve_governance_root` and `resolve_projects_parent` monkeypatched on the `bellows_root` module; the tool loaded by path and run in-process through `main(argv)` in c1–c6 and c8; c7 (:196–204) the one subprocess run, which exits 2 in argparse; `tests/test_tools_safe_to_invoke.py` `test_help_does_no_work` (:140) runs every `tools/*.py` with `--help` alone; `bellows_root.resolve_governance_root` checks `$ELUVIAN_WRAP_ROOT` first (a directory holding `COMPANY.md`, :72–75), and `resolve_projects_parent()` is `resolve_bellows_root().parent` (:100–104); `depositor._assign_class` (:409) classes each write by the repository `_resolve_write` (:296) finds | the files at those lines |
| P5 | class; in flight; interpreters; the suite | the writes → **shop-infra** (bellows, the project floor), HOLDS at admission for the CEO's release; in flight at drafting: nothing — #100101 Done, #100100 halted, thread 341's plan held for release (`gates.py`, `verdict.py`, …), threads 333's and 322's plans in drafting (`bellows.py` and others — none of these files); the bellows venv 3.12.14, `/usr/bin/python3` 3.9.6, the Air's venv 3.9.6; the full suite at `65f713c`, derived and not run in a canonical-shaped tree: 2377 passed, 2 skipped — `61794c4`'s 2364 with #100100's eight and #100101's five (their DEVs counted 2372 and 2369, each over `61794c4`); the prototype's run over the same base, behind the suite fence, collected the same 2379 items (`51 failed, 2325 passed, 3 skipped`, the fence's environment failing fifty-one) | `status.py`; `python -V`; the suite |

DEV re-derives P1, P2 and P4 in Item 1 and HALTs on a mechanism mismatch; P3 and P5 are the drafting record.

## ACID, stated

- **Atomicity:** after the DEV's first commit (three files) the tool runs as a script and the tree is green; the second commit adds the run file and the dev-log; QA adds two evidence files. Nothing runs the tool until the CEO's apply act, so no half-state reaches a deposit.
- **Consistency:** the tool resolves its imports as its peers do, and its logic is unchanged (c1–c8).
- **Isolation:** a read-only tool; in flight at drafting, none of these files is another plan's write (P5). Thread 333's commit binding, should it merge and its daemon restart before this plan is dispatched, binds both steps — each names `tools/check_deposit.py` — and a commit then needs a passing pre-check recorded over the same content: every commit here runs after its pre-check in one command, path-scoped, with nothing written between them, the shape that binding admits; a survivor fix removes the run file before its pre-check.
- **Durability:** the run file, the dev-log, the QA receipt with QA Item 2's six runs, and the redirected suite file are the step's evidence.

## Drafting Cycle

**Tier:** **T1** — T-3 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-classify-deposit-script-2026-09-14.md
**Walks:** walk 0 pinned (P1–P5 measured on bellows `65f713c`; the fix prototyped in a scratch archive of `65f713c`; clone-diff against `halted-executable-100100.md`: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold, re-saved after every intended edit; every lens commit through `/Users/marklehn/Developer/bellows/scripts/lens_commit.py`, invoked directly with its output whole, its exit read; no lens or walk number in any `--desc`, and no plain commit subject carrying a walk and a lens number; `lens_order_check` after every lens commit.

- Weak spots:          w1 6 folded — instruction 4 / record 2; w2 3 folded — instruction 2 / record 1; w3 dry
- Destruction:         w1 1 folded — instruction 1 / record 0; w2 dry; w3 dry
- Vulnerabilities:     w1 1 folded — instruction 1 / record 0; w2 dry; w3 dry
- Integration-record:  w1 dry; w2 1 folded — instruction 0 / record 1; w3 dry
- ACID:                w1 1 folded — instruction 0 / record 1; w2 dry; w3 dry
**Closing:** WARM close after walk 3 — BAR MET (T1), thread 329's fix plan, on a dry walk: walk 3 dry on all five lenses. Findings by walk and class — walk 1: 9 (instruction 6 / record 3; 1 of 9 fold-introduced in part — w1-7, whose clause on w1-1's `WT` answered that walk's own fold); walk 2: 4 (instruction 2 / record 2; 2 of 4 fold-introduced, w2-1 and w2-2 on w1-6's and w1-7's folds); walk 3: 0 — no residue, since walk 3 folded nothing. Walk 2's commit chain ran a display filter, declared in the register with the echo it hid re-read: the same 17 WARN lines at each of its five commits. Deposit via `ready-`; HOLDS on class shop-infra, the CEO releases (`clear_plan --release-class-hold`). No close act: nothing restarts; Post-close (1), the `CLAUDE.md` step-2 edit, is the CEO's apply act after the merge, and Post-close (2)'s run on the Air is the Planner's, its `RESULT:` line pasted into `threads done 329`.

## Cycle Manifest
tier: T1
target: tools/classify_deposit.py
class: shop-infra
reads: tools/classify_deposit.py, tests/test_classify_deposit.py, tests/test_tools_safe_to_invoke.py, tools/check_deposit.py, tools/cycle_log_signal_census.py, tools/fold_signal_census.py, bellows_root.py, depositor.py, knowledge/decisions/halted-executable-100100.md, knowledge/mutants/classify-deposit.json
writes: tools/classify_deposit.py, tests/test_classify_deposit.py, knowledge/mutants/classify-deposit-script.json, knowledge/mutants/classify-deposit-script.run.txt, knowledge/development/dev-log-classify-deposit-script-2026-09-14.md, knowledge/qa/evidence/classify-deposit-script-qa-receipt-2026-09-14.md, knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt
mutants: knowledge/mutants/classify-deposit-script.json
open_forks: 1. #100100's lane file, `halted-executable-100100.md`, is committed at the next wrap, with its processed verdict
walks: 3
yields: 6, 2, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:83
coherence: 3/3 body walks named in the register (13 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-classify-deposit-script.md.foldcheck.json

---

## STEP 1 — DEV (three path lines, one test and one import, the mutation run; two commits)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ Never run the daemon, `run_plan`, a claim or a gate outside a test; the tool runs in this step only inside its tests, and no command it runs opens a database. ⛔ Test code runs under pytest inside `tests/` or not at all (bellows `CLAUDE.md`, *Test code*): no helper is called from `python -c`.
>
> **Scope:**
> - `tools/classify_deposit.py`
> - `tests/test_classify_deposit.py`
> - `knowledge/mutants/classify-deposit-script.json`
> - `knowledge/mutants/classify-deposit-script.run.txt`
> - `knowledge/development/dev-log-classify-deposit-script-2026-09-14.md`
>
> **Item 1 — re-derive P1, P2 and P4 and HALT on a mechanism mismatch** (a line-number drift is not one, and a count that moved is a record — paste it; a `sys.path` line already in the tool, its imports no longer inside `main()`, or c9 already present IS one). Paste each pin's re-derived line beside it.
> **Item 2 — the failing test FIRST:** `import os` and c9 as *What this changes* 2; run the file on the unedited tool and paste the red summary line — `1 failed, 8 passed` (c9: the script run fails its import).
> **Item 3 — the edit** as *What this changes* 1 and 3, each manifest anchor matching its target once; the file green (`9 passed`), then `tests/test_tools_safe_to_invoke.py` green — paste its summary line — then the FULL suite green: predicted `2378 passed, 2 skipped` on `65f713c` — P5's derived base and c9 — (the run supersedes the prediction; HALT only on a non-zero failure count). An existing test failing here is a HALT, never an edit.
> **Item 4 — first commit**, gated on the FULL suite AND the pre-check with the stage flag, path-scoped, run as ONE command — the verdict read checks each commit's place in the step's transcript, and a split chain or a suite piped into another command is a deviation it records: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" --expect-missing knowledge/development/dev-log-classify-deposit-script-2026-09-14.md knowledge/mutants/classify-deposit-script.run.txt && git add tools/classify_deposit.py tests/test_classify_deposit.py knowledge/mutants/classify-deposit-script.json && git commit -F <msg-file> -- tools/classify_deposit.py tests/test_classify_deposit.py knowledge/mutants/classify-deposit-script.json` — three files (the manifest rides this commit so the mutation run's `HEAD:` archive contains it); the message tagged with the plan id and `thread 329`.
> **Item 5 — the mutation run, REDIRECTED into `knowledge/mutants/classify-deposit-script.run.txt` (a Deposit):** `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/classify-deposit-script.json > knowledge/mutants/classify-deposit-script.run.txt 2>&1`; the last line must read `MUTATION: 2 killed, 0 survived, 0 error` — a survivor is a test to write, not a mutant to delete: the test, at the file's end, and the manifest, that mutant's `expect_fail` naming the new test — `tools/mutation_check.py` runs each mutant's `expect_fail` node alone (:5), so a test the manifest does not name kills nothing — in ONE further commit, run as one command — `rm knowledge/mutants/classify-deposit-script.run.txt` first (a run file recording a survivor fails the pre-check's `mutation_result` gate, and a present path cannot be named), then the FULL suite AND the pre-check with `--expect-missing knowledge/development/dev-log-classify-deposit-script-2026-09-14.md knowledge/mutants/classify-deposit-script.run.txt`, then `git add` and `git commit`, path-scoped to the test file and the manifest — then the run again, which writes the run file anew, its `HEAD:` naming that commit.
> **Item 6 — the dev-log** `knowledge/development/dev-log-classify-deposit-script-2026-09-14.md` under the four headings declared below; `## Pins re-derived (P1, P2, P4)` opens with P1's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `no sys.path line anywhere in the file`; paste through those words, then stop. Under `## Failing-first (red, then green)` the red line and the three green lines (the file, the safety file, the full suite); under `## The script run observed (c9)` the PASSED line of a `-v` run over c9's node id; under `## Mutation run` the run file's last line. **The last commit** (the second, or the third after a survivor fix), gated on the FULL suite and the pre-check bare (`… tools/check_deposit.py … 1 --wt "$(git rev-parse --show-toplevel)"`, no stage flag — the run file and the dev-log now exist), run as ONE command, path-scoped to the run file and the dev-log.
> **Headings:** `## Pins re-derived (P1, P2, P4)`; `## Failing-first (red, then green)`; `## The script run observed (c9)`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P2, P4)` ← P1
>
> **Deposits:**
> - `knowledge/mutants/classify-deposit-script.json`
> - `knowledge/mutants/classify-deposit-script.run.txt`
> - `knowledge/development/dev-log-classify-deposit-script-2026-09-14.md`
>
> **Post-conditions:** c1–c9 green; t1–t6 green; the FULL suite green with no `failed`; the mutation run's last line as Item 5 states it; two DEV commits, or three after a survivor fix — the first carrying the three files, the last the run file and the dev-log; the dev-log's four headings present as full lines with P1's cell whole; `git status --porcelain` empty after the last commit.

## STEP 2 — QA (full suite; #100100's three plans re-run as written; the receipt)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter ABSOLUTE; read-only beyond the two evidence files; no scratch directory — the runs from outside the repository go from `/tmp`, and a shell variable lives for one command.
>
> **Item 1 — the full suite REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt 2>&1`; in the receipt's Verification table the Item 1 row quotes the count as `N passed` and names the skips as `two skips (test_gate_watcher live-DB; test_fallback_live_wal_window version gate)` — never pytest's own summary line, whose word the Rule 20 block scans for; a run that fails on a test outside this plan's files is re-run once, both summaries named in the receipt.
> **Item 2 — #100100's three plans, run as that plan wrote them and as the Planner runs the tool: as a script, with no `PYTHONPATH`** — `env -u PYTHONPATH /Users/marklehn/Developer/bellows/.venv/bin/python tools/classify_deposit.py <plan> --project-root /Users/marklehn/Developer/bellows --config /Users/marklehn/Developer/bellows/config.json`, first from the worktree root, then again from `/tmp`, outside the repository — each run one command that holds the worktree root before it leaves, `WT="$(git rev-parse --show-toplevel)" && cd /tmp && env -u PYTHONPATH /Users/marklehn/Developer/bellows/.venv/bin/python "$WT"/tools/classify_deposit.py "$WT"/<plan> --project-root /Users/marklehn/Developer/bellows --config /Users/marklehn/Developer/bellows/config.json`, since a variable set in one command is gone in the next, and the tool and each plan named by absolute path, because from outside the repository a relative plan path is `plan is not a file`, exit 2 (measured): `knowledge/decisions/Done/executable-100073.md` → `RESULT: MATCH` (shop-infra declared and assigned), exit 0; `knowledge/decisions/Done/executable-100098.md` → `RESULT: MATCH` (app-feature declared and assigned), exit 0; `knowledge/decisions/Done/executable-worktree-precheck-hardening-2026-05-29.md` (no Cycle Manifest) → `from fallback` and `RESULT: FALLBACK`, exit 3 — each run's output pasted whole with its exit status, read on the line after the run. ⛔ Any other result — a traceback above all — is a HALT and a verdict request, never an edit and never a `PYTHONPATH`.
> **Item 3 — production writes, stated exactly:** none outside the two evidence files; no lane file, no lifecycle row, no daemon act; and the tool's source names neither `sqlite3` nor `.connect(` — `grep -n -E 'sqlite3|\.connect\(' tools/classify_deposit.py` prints nothing.
> **Item 4 — receipt** `knowledge/qa/evidence/classify-deposit-script-qa-receipt-2026-09-14.md`: `numstat` over the DEV commits (`<base>..<dev>`, five files); `git diff <base>..<dev> -- tools/classify_deposit.py` pasted (four lines added — the three path lines and the blank line below them — none removed); the test file's edit, checked — `git diff -U0 <base>..<dev> -- tests/test_classify_deposit.py | grep -F '@@'` pasted beside `git show "<base>:tests/test_classify_deposit.py" | wc -l` (a base held in a shell variable is written braced, `"${BASE}:tests/…"` — zsh reads `$BASE:t` as a modifier): every hunk adds lines and removes none, one after the base file's line 3 (`import os`) and the rest after its last line — a hunk anywhere else is an edit to c1–c8, which MUST-PRESERVE freezes, and the step STOPS; a `## Verification` table with one row per Item 1–3, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`/Users/marklehn/Developer/eluvian-governance/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: classify-deposit-script-2026-09-14`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["classify-deposit-script-suite-2026-09-14.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 5 — commit**, path-scoped and gated, run as ONE command: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/classify-deposit-script-qa-receipt-2026-09-14.md knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt && git commit -F <msg-file> -- knowledge/qa/evidence/classify-deposit-script-qa-receipt-2026-09-14.md knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt`. ⛔ A QA step that must change production code STOPS and requests a verdict (thread 262).
>
> **Deposits:**
> - `knowledge/qa/evidence/classify-deposit-script-qa-receipt-2026-09-14.md`
> - `knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/classify-deposit-script-qa-receipt-2026-09-14.md`
> - `knowledge/qa/evidence/classify-deposit-script-suite-2026-09-14.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 2 skipped` with N at least the DEV's count and no `failed`; Item 2's six runs pasted whole with their exit statuses; the test file's hunk headers pasted, every hunk adding lines after the base's line 3 or its last line and none removing any; the receipt's `## Verification` table has three rows, and the receipt carries the block's stdout, its `PASSED — SELF-CHECK PASSED` line among it; one QA commit carrying the two evidence files.
