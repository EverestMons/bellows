# Bellows — BACKLOG #1 Reproduction Audit

**Date:** 2026-05-05 | **Tier:** Small | **Test Scope:** targeted (no test execution — read-only diagnostic) | **Execution:** Step 1 (Bellows SA)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS. The Planner reads the deposited findings file via Rule 22 verification and authorizes housekeeping directly.

**Bootstrap:**

```
Read the plan at bellows/knowledge/decisions/diagnostic-backlog-1-reproduction-audit-2026-05-05.md. Execute Step 1 ONLY. After completing Step 1, STOP.
```

---
---

## STEP 1 — Bellows SA

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-backlog-1-reproduction-audit-2026-05-05.md", "bellows/knowledge/decisions/in-progress-diagnostic-backlog-1-reproduction-audit-2026-05-05.md")`. **You are the Bellows Systems Analyst.** Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md`. **Skip domain glossary read — bellows has no glossary; this is a code-tracing + ledger/verdict-archive audit task.** **Context (read these BEFORE investigating, in order):** (1) `bellows/knowledge/research/worktree-delta-check-2026-05-05.md` — yesterday's finding that the 2026-05-03 worktree recommendation shipped in commit `36b2bba` (2026-05-03), with detect-and-skip for projects without project-local `.git` added in commit `06aa938` (2026-05-04). Critical context for this audit: real-`.git` projects get worktree isolation as of 2026-05-03; bellows-self does NOT (no project-local `.git`, runs in-place). (2) `bellows/knowledge/BACKLOG.md` — the open BACKLOG #1 entry (`2026-04-30: scope_check diff-collision from concurrent activity`). (3) `bellows/knowledge/decisions/Done/` — the canonical record of plans dispatched. **Hypothesis to test.** BACKLOG #1 may already be effectively closed for the population it can fix (real-`.git` projects). The three reproductions cited in NEXT_SESSION.md (2026-04-30, 2026-05-04, 2026-05-05) may all be on bellows-self plans, where the worktree fix intentionally doesn't apply because bellows-self lives inside the governance-root monorepo and was an accepted tradeoff at the 2026-05-04 close. If hypothesis holds, BACKLOG #1's open status reflects only bellows-self exposure, which is a known constraint, not an unfixed bug. If hypothesis fails (any reproduction post-2026-05-03 on a real-`.git` project), the worktree fix has a gap and BACKLOG #1 stays open with a different framing. **Task — answer four specific questions and ONLY these four. Do not propose new fixes. Do not re-evaluate the worktree implementation. This is a population audit, not an architecture audit.** **Question 1 — Enumerate ALL scope_check failures since 2026-04-30.** Audit (a) `bellows/verdicts/resolved/processed-*` files for verdict requests where `Pause Reason Code: gate_failure` AND the failure references `scope_check`, (b) `bellows/verdicts/pending/archived/*` for the same pattern (CEO-archived stranded verdict-request files), (c) `bellows/knowledge/decisions/Done/halted-*` plans whose halt reason was scope_check, (d) `bellows/knowledge/verdict-log.md` if it captures pause history. Date range: 2026-04-30 inclusive through 2026-05-05 inclusive. For each scope_check failure found, record: (i) plan slug, (ii) project (which project's `knowledge/decisions/` did it live in), (iii) date/time, (iv) which files tripped the gate, (v) whether the underlying project had its own `.git` at the time of dispatch (key indicator: was `project_path` one of the 7 real-`.git` projects, or was it `bellows/`?). **Question 2 — Classify each reproduction.** For every scope_check failure enumerated in Q1, classify as one of: (a) **bellows-self exposure** — plan lived in `bellows/knowledge/decisions/`, project_path was bellows itself, no project-local `.git`, worktree was skipped (intentional per 2026-05-04 detect-and-skip). (b) **pre-fix** — dispatched before commit `36b2bba` on 2026-05-03 landed, so worktree code wasn't yet active. Use `git --no-pager log --format='%H %ci %s' -- bellows.py` to determine when the worktree code shipped to the running daemon (note: depends on Bellows restart, not just commit time — flag this caveat in findings). (c) **real-`.git` post-fix gap** — dispatched after worktree shipped, on a real-`.git` project, but still tripped scope_check. THIS is the case that would invalidate the close hypothesis. Report counts per category. **Question 3 — If any (c)-class reproduction exists, characterize it.** For each (c)-class reproduction (post-fix real-`.git` failure): (i) was the worktree actually created for that plan (check Bellows logs at `bellows/logs/` for the dispatch timestamp's "no project-local .git" warning, or absence of it), (ii) what files tripped the gate, (iii) is there a pattern (e.g., shared resource, test fixture path, cross-project reference). **Question 4 — Restart-window confirmation.** Bellows does not hot-reload. The worktree code in commit `36b2bba` only became active after the daemon was restarted. Determine the earliest restart timestamp where the worktree code became live. Sources: `bellows/logs/` startup messages (look for `Bellows started` or equivalent), or commit history of `bellows.db` if it persists daemon state. Cite the specific log line or evidence. Any reproduction between commit `36b2bba` (2026-05-03) and the first post-`36b2bba` restart should be classified as (b) pre-fix, not (c) post-fix gap. **Hard scope rule.** This is a population audit producing a classification table and counts. Do not propose new fix shapes. Do not re-evaluate the worktree implementation's correctness. If you find a (c)-class reproduction, flag it for CEO and stop — the Planner decides next steps. If all reproductions classify as (a) or (b), the finding section should explicitly state "BACKLOG #1 close hypothesis confirmed: no real-`.git` post-fix reproduction in the audit window." **Deliverable.** Write findings to `bellows/knowledge/research/backlog-1-reproduction-audit-2026-05-05.md`. Structure: (i) "Q1 — Reproduction inventory" — table with columns plan slug, project, date, files-tripped, has-project-local-.git. (ii) "Q2 — Classification" — same table extended with classification column (a/b/c) plus per-category counts. (iii) "Q3 — (c)-class characterization" — only if any (c) found; otherwise state "no (c)-class reproductions found." (iv) "Q4 — Restart-window evidence" — the earliest post-`36b2bba` Bellows restart timestamp with citation. (v) "Verdict" — one of: "Close hypothesis confirmed — BACKLOG #1 fix complete for the population it targets" / "Close hypothesis fails — (c)-class reproduction(s) found, BACKLOG #1 stays open with new framing" / "Inconclusive — restart timestamp cannot be determined, audit blocked." **Standard prompt feedback protocol** → `bellows/knowledge/research/agent-prompt-feedback.md`. **Final commit message:** `docs: backlog-1 reproduction audit 2026-05-05`. **Deposits:**
> - `bellows/knowledge/research/backlog-1-reproduction-audit-2026-05-05.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to any other step. Do NOT move the plan to Done. The Planner performs Rule 22 verification on the deposited findings file and the move-to-Done.**
