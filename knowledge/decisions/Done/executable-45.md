# Bellows — Daemon-Owned Ledgers Phase 3: FORWARD New-Rows→Daemon-Post-Merge (Dormant, Coexistence-Safe)
**Date:** 2026-06-13 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 42, Phase 3 (final mechanism phase) of the phased migration (CEO fork verdicts, processed-verdict-42-step-1): relocate `knowledge/FORWARD.md` NEW-ROW additions OUT of the worktree merge path via daemon-post-merge append. SCOPE NUANCE (design Section 3 File 3): ONLY new-row additions move — those are the conflict source (agents filing deferred work append rows at EOF; parallel plans collide). The Planner's STATUS-UPDATE edits (`open` → `closed-by-plan-X`) happen at session-wrap directly on main (not in a worktree) and do NOT conflict — they stay exactly as-is, unchanged by this plan. Phase 3 REUSES the framework from plans 43/44: the `### Ledger Updates` channel, `_apply_ledger_updates()` per-handler branches, and `files_changed` coexistence. Disposition (a) daemon-post-merge. Stays DORMANT/coexistence-safe (skip if FORWARD.md in files_changed). Does NOT close FORWARD rows 4/5/13 — the governance activation follow-on does that. The design doc `knowledge/research/daemon-owned-ledgers-design-2026-06-13.md` is AUTHORITATIVE (Section 3 File 3, Section 4, Section 6 Phase 3). Re-verify Verification Blocks at edit time; halt-and-report on divergence. Anchors (re-verify by grep): `_apply_ledger_updates` per-handler branches and the `### Ledger Updates` parse (plans 43/44); the FORWARD.md 6-column table format `| # | Added | Item | Type | Plan-id link | Status |`. DAEMON RESTART REQUIRED after close (flag in receipt).

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, then the design doc Section 3 File 3 + Section 4, then the EXISTING Phase 1/2 code in `parser.py` and `bellows.py` (`_apply_ledger_updates` per-handler branches, `_append_project_status`) so you extend them consistently. **Scope is exactly: `parser.py`, `bellows.py`, `tests/test_parser.py`, `tests/test_bellows.py`.**
> - **Channel parse extension:** in `parser.py`, extend the `### Ledger Updates` extraction to also capture a `#### Forward Register` subsection into `parsed["ledger_updates"]["forward"]`. The agent emits the deferred-work content (the Item text, and optionally Type / Plan-id link / Status — default Type=`deferred-work`, Plan-id link=`—`, Status=`open` if unspecified). Absent → None, no error. Mirror the Phase 1/2 subsection pattern.
> - **Apply extension (daemon-post-merge + coexistence):** add a forward handler to `_apply_ledger_updates` (independent branch, like feedback/project_status): if `FORWARD.md` (path ends `knowledge/FORWARD.md`) appears in `files_changed` → SKIP (coexistence). Else if `parsed["ledger_updates"]["forward"]` is present → append a new row to `<project_path>/knowledge/FORWARD.md`'s 6-column table. The daemon COMPUTES the next row number (max existing `#` + 1) and fills `Added` with the current date (the agent cannot read the file to number its own row), then `git -C <project_path> add knowledge/FORWARD.md && git commit` on main. Handle a project with no FORWARD.md gracefully (log-and-skip; not every project has one). Log-and-continue; never crash teardown. Do NOT touch the feedback or project_status handlers, and do NOT alter Planner status-update behavior.
> - Do NOT touch Rule 42 / the feedback protocol / Rule 8 — governance is the separate follow-on.
>
> **Tests:** (a) parser extracts `#### Forward Register` (feedback + project_status still extract — regression); (b) forward handler SKIPS when FORWARD.md in files_changed (coexistence); (c) WRITES a correctly-numbered new row (next # = max+1, today's date, defaults applied) when not in files_changed and receipt carries forward content — assert against a fixture FORWARD.md; (d) graceful no-op when the project has no FORWARD.md; (e) row-number computation handles withdrawn/closed rows present (counts all `| <n> |` rows, not just open). Then run the FULL suite (`python3 -m pytest tests/`) to an explicit pass/fail and READ THE TAIL. Write the dev log to `bellows/knowledge/development/daemon-ledgers-phase3-forward-dev-log-2026-06-13.md`. Use `with open()`; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/daemon-ledgers-phase3-forward-dev-log-2026-06-13.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, and design doc Section 3 File 3. **Verify, each with executed evidence (files into `knowledge/qa/evidence/daemon-ledgers-phase3-2026-06-13/`):** (1) **Full suite** — final 15 lines, zero failures, new-test count matches dev log; output to `full_suite_tail.txt`. (2) **Parser extension** — `#### Forward Register` extracts; feedback + project_status still work (no regression); output to `parser_extension.txt`. (3) **Row numbering + defaults** — the new-row test proves next-number computation (max+1 across ALL rows incl. withdrawn/closed), today's date, and the type/link/status defaults; output to `row_numbering.txt`. (4) **Dormancy/coexistence (load-bearing)** — forward handler SKIPS when FORWARD.md in files_changed; grep confirms no worktree-side FORWARD write; no real FORWARD.md modified by this plan; output to `dormancy_proof.txt`. (5) **Phases 1+2 intact** — feedback + project_status handlers unchanged (additive extension only); output to `prior_phases_intact.txt`. (6) **No scope bleed** — `git diff HEAD~1 --stat` only in-scope files; Rule 42/8 + Planner status-update behavior untouched; output to `scope_check.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `daemon-ledgers-phase3-2026-06-13`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/daemon-ledgers-phase3-qa-report-2026-06-13.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/daemon-ledgers-phase3-2026-06-13/`; `required_evidence_files`: `[full_suite_tail.txt, parser_extension.txt, row_numbering.txt, dormancy_proof.txt, prior_phases_intact.txt, scope_check.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table and the Rule 20 banner block to `bellows/knowledge/qa/daemon-ledgers-phase3-qa-report-2026-06-13.md`. **Receipt Flags for CEO:** (1) DAEMON RESTART REQUIRED; (2) Phase 3 DORMANT — all three mechanism phases now landed; (3) ONLY the governance activation follow-on remains to flip agents to the channel, freeze the 309KB feedback file, and CLOSE FORWARD rows 4/5/13. Use `with open()`; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/daemon-ledgers-phase3-qa-report-2026-06-13.md`
> - `bellows/knowledge/qa/evidence/daemon-ledgers-phase3-2026-06-13/` (six evidence files per Rule 20 self-check)
