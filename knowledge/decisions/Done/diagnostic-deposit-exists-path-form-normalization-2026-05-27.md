# Diagnostic — _gate_deposit_exists Path-Form Normalization

**Project:** bellows
**Dispatch Mode:** bellows
**Plan Type:** diagnostic
**Date:** 2026-05-27

## Context

Three Planner overrides on `gate_failure` in the 2026-05-23 config-split session (Steps 1, 2, 3 of `executable-bellows-config-split-2026-05-23`), all matching the byte-level-contract / path-form-mismatch family. Captured in shop_next_session.md Thread 3. The recurrence pattern spans 12 days (2026-05-12 verdict-format mismatch, 2026-05-22 Rule 26 deposit-marker defect, 2026-05-23 path-form mismatch ×3). Two prior diagnostics solved adjacent problems but not this one: `gate-path-resolution-post-teardown-2026-05-10.md` (added worktree-first Strategy 0 to `_resolve_deposit_path`, RC1) and `deposit-exists-false-positive-audit-2026-05-11.md` (identified Cause 5, plan-agent evidence path convention mismatch, governance-side fix). The 2026-05-23 reproductions are mechanistically distinct from both: even when `_resolve_deposit_path` succeeds, the gate compares the resolved path to a set of agent-declared paths (`agent_declared`), and form mismatches in that membership check produce `deposit_exists` failures.

This diagnostic characterizes the path-form mismatch precisely, audits the 2026-05-23 reproductions, and recommends a fix shape. The shop baton also flags a deeper architectural question — gate-vs-teardown race — that this diagnostic must address before fix scope can be finalized.

**Recursion-risk constraint (LESSONS 2026-05-19):** the fix executable this diagnostic informs must produce deposits whose declared paths match the plan's `**Deposits:**` block paths in form. Otherwise the fix plan trips the very bug it's fixing. This diagnostic itself is read-only and produces no source-code deposits, so it is not at risk.

## STEP 1

You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. Prior findings to read (cite per Rule 27, do not re-derive): `bellows/knowledge/research/gate-path-resolution-post-teardown-2026-05-10.md` (RC1 worktree threading fix) and `bellows/knowledge/research/deposit-exists-false-positive-audit-2026-05-11.md` (Cause 5 convention mismatch). Investigate the path-form normalization gap in `_gate_deposit_exists` that produced three Planner overrides on 2026-05-23 (`executable-bellows-config-split-2026-05-23` Steps 1, 2, 3). Report findings to `bellows/knowledge/research/deposit-exists-path-form-normalization-2026-05-27.md`. **Investigation surface:** (Q1) `_gate_deposit_exists` implementation in `bellows/gates.py` — read the function in full and identify where `agent_declared` is constructed (the set or list parsed from the agent's Output Receipt "Files Deposited" or "Files Created or Modified" sections), where plan-required paths are extracted (`_extract_plan_required_deposits`), and where the two are compared. Show the exact comparison: is it a set membership check, a string equality, a normalized-string equality, or path-API equivalence (`os.path.samefile`, `pathlib.Path.resolve` equality)? Quote the exact lines. (Q2) Empirical audit of the 2026-05-23 reproductions. Read the three verdict-request files from the resolved/ archive: `processed-verdict-bellows-config-split-2026-05-23-step-1.md`, `-step-2.md`, `-step-3.md`. For each, extract: (a) the plan's `**Deposits:**` block paths from the plan file at `bellows/knowledge/decisions/Done/executable-bellows-config-split-2026-05-23.md`, (b) the agent's declared paths from the corresponding step's Output Receipt (look in the Done/ plan file or in `bellows/logs/` for step JSON), (c) the gate's failure message verbatim (which path it reported missing). Produce a comparison table: | Step | Plan-declared path | Agent-declared path | Gate-reported missing path | Form mismatch type |. Form mismatch types to identify: abs-vs-rel, project-prefix-included-vs-stripped, worktree-prefix-included-vs-stripped, trailing-slash, symlink-vs-target, case sensitivity, OS-path-separator. (Q3) Confirm the order of operations from the 2026-05-10 diagnostic still holds: `gates.check()` at `bellows.py:333` runs BEFORE `_teardown_worktree` at `bellows.py:362`. Show the current line numbers and code path. If the order has changed since 2026-05-10, document the new ordering. (Q4) Gate-vs-teardown race investigation — the shop baton flags this as a candidate concern. Examine `gates.check()` and any code it calls between Step N's completion and the gate evaluation. Specifically: between the moment the agent's Output Receipt is captured and the moment `_gate_deposit_exists` constructs `agent_declared`, is there any code path that could mutate the worktree state? Things to look for: a subprocess that runs `git status` or `git diff` and might trigger an index refresh; a sweep that touches files; any I/O that could rename or remove deposit files. Show the call chain. Conclusion: either (a) no race exists, the path-form mismatch alone explains all 3 reproductions; or (b) a race exists and gate-vs-teardown order needs to change in addition to normalization. (Q5) Fix-shape recommendation — choose ONE of: (a) **pure normalization** — `os.path.realpath` or `os.path.relpath(path, project_path)` applied to both sides before the `agent_declared` membership check (~10 LOC + 1 test as the baton estimated); (b) **normalization + order-of-operations change** — normalization PLUS a structural change to when gates evaluate relative to teardown; (c) **broader restructure** — neither (a) nor (b) is sufficient and a larger redesign is needed (specify scope, LOC estimate, blast radius). For the recommended shape, list every call site that needs to change, every test that needs to be added (positive: cross-form equivalence; negative: actually-missing path still fails), and any test fixture that needs updating. Include LOC estimate (production / test / total). (Q6) **Authoring guidance for the fix plan** — given the recursion-risk constraint from LESSONS 2026-05-19, the fix plan must produce deposits whose declared paths match the plan's `**Deposits:**` block paths in form. Specify what path form (absolute / project-relative / repo-relative) the fix plan should use throughout, citing the form Bellows currently treats as canonical (the form `_extract_plan_required_deposits` returns after parsing the block). This guidance lets the next Planner author the fix plan correctly on the first attempt. Do not fix anything — read-only investigation. After findings, append a dated entry to `bellows/knowledge/research/agent-prompt-feedback.md` per standard protocol. Commit: `git add bellows/knowledge/research/deposit-exists-path-form-normalization-2026-05-27.md bellows/knowledge/research/agent-prompt-feedback.md`, `git --no-pager commit -m "diag: deposit_exists path-form normalization"`, `git --no-pager push origin main`.

**Deposits:**
- `bellows/knowledge/research/deposit-exists-path-form-normalization-2026-05-27.md`
- `bellows/knowledge/research/agent-prompt-feedback.md`
