# bellows — diagnostic: QA-SCOPE CENSUS — how often a later step edited a file that only an earlier step declared, measured over every step the lifecycle DB records (the `step_files` rows since #100050 and the per-step commits back to #100005), so the per-step arm of `scope_check` thread 262 proposes is drafted from a rate, not from the one instance that opened the thread

**Date:** 2026-09-11 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic; writes one research doc, one TSV and a dev-log) | **Execution:** Step 1 (DIAGNOSTIC) | **pause_for_verdict:** after_step_1

**auto_close:** false

**Post-close:** no restart, no doctrine. The doc's rate and its per-instance list are the input to thread 262's second act (the per-step arm, WARN first) and to the PLANNER_TEMPLATE sentence 262 proposes for QA steps; both are drafted after this doc, by a plan of their own. This plan recommends nothing.

**Depends on:** thread 262 (opened 2026-09-10 from #100061 STEP 2: the QA commit `d6da69b` changed `scripts/close_cycle.py`, a file in STEP 1's Scope and absent from STEP 2's, and `scope_check` reported PASS) and its rule, "evidence-first: the corpus measurement before the arm". Clone origin by kind and layout: `Done/diagnostic-100059.md` (the doctrine staleness census, 2026-09-09): one DIAGNOSTIC step, numbered questions, doc + TSV as Planner-commit deposits, the census script in the doc's appendix, no recommendation. The gate's rule is QUOTED from `gates.py` and RE-USED through its own extractor functions — never restated by the census script.

**Tier computed (§1):** **T1** — T-1 fires (a read across the lifecycle DB, the Done corpus and the bellows commit history); T-6 does not (no doctrine edit); T-2 does not (no source, hook or tool changes).

## CEO Context

Thread 262 names the mechanism: `_gate_scope_check` (gates.py:1127) builds its declared set from steps 1 through N — `for s in range(1, step_number + 1)` (:1133), `scope_files_raw.update(files)` (:1147) — so a later step may edit any file an earlier step declared. Thread 262 calls that union a deliberate choice for multi-step plans whose steps share files; the record says it shipped for a different reason — a cumulative working-tree diff that no longer exists in that form (P10). Its cost today is a QA step that changes the DEV's deliverable after the DEV's mutation run and failing-first record were taken. On #100061 the change was benign, but the record half describes a script one commit older than the one shipped. The thread asks for the corpus measurement before the arm. Walk 0 ran the first arm of that measurement over the `step_files` table (157 rows, 35 steps, 20 plans) against each plan's per-step Scope blocks: 153 rows in their own step's Scope, ONE in an earlier step's Scope only (the #100061 instance itself), three in no Scope block at all — and the gate's own ledger says those three were `scope_check` FAILs in DEV steps that the Planner overrode or the daemon halted. The `step_files` table starts at #100050; the `commits` table reaches back to #100005 with 124 bellows commits over 84 steps of 58 plans, every sha resolvable in the canonical checkout, so the second arm of the census widens the corpus threefold by reading each step's commits.

## Numbers discipline — measured 2026-09-11 by the Planner (bellows `cc58328`, governance `34470449`, daemon pid 70398)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the `step_files` arm | 157 rows over 35 steps of 20 plans (#100050–#100070; 19 closed, #100054 halted); 15 of the 35 steps are step 2 or later; the table's columns are `id, step_id, path`; the rows are written by `lifecycle.record_gate_events` (lifecycle.py:545–547: `DELETE FROM step_files WHERE step_id = ?` then `INSERT OR IGNORE`) from the gate's `files_changed`, which is `_parse_diff_stat(post_diff, pre_diff, wt_path)` (bellows.py:1206, :1365) — the worktree diff across the step's run, NOT its commits, and only the LAST gate run of a retried step survives; every one of the 20 plans' `plan_doc_ref` resolves under the canonical checkout and every one carries a `**Scope:**` block per step | `sqlite3 "file:lifecycle.db?mode=ro"` — `SELECT count(*), count(DISTINCT s.id), count(DISTINCT s.plan_id) FROM step_files sf JOIN steps s ON sf.step_id=s.id` |
| P2 | ⛔ the walk-0 classification of P1 against Scope blocks ONLY (a parser that read `> **Scope:**` bullets and nothing else) | own-step Scope 153; earlier-step Scope only 1 — `100061` step 2 `scripts/close_cycle.py`; no Scope block 3 — `100052` step 1 `tests/test_step_files_record.py`, `100053` step 1 `tests/test_substrate_check.py`, `100054` step 1 `tests/conftest.py`; 157 rows, 153 own, 1 union-only, 3 unscoped | Item 1's script, in the doc; the walk-0 parser did not model the Deposits arm or the allowlist — the DEV's does, through `gates._extract_plan_scope`, `gates._extract_deposits_block_paths`, `gates._extract_step_text` |
| P3 | ⛔ the `commits` arm | `commits` (`id, step_id, repo, sha, message_ref`): 148 rows over 68 plans (#3–#100070); by repo — bellows 124, forge_lessons 13, tuyere 11; the 124 bellows shas belong to 84 steps of 58 plans and ALL 124 resolve in the canonical checkout (`git cat-file -e <sha>^{commit}`); the 24 other-repo shas do not, and are outside this census; none of the 124 is a merge commit (`git show -s --format=%P` — one parent each); all 58 plans' `plan_doc_ref` resolve under the canonical checkout (`test -f`) | the same DB; the loop in Item 3 |
| P4 | ⛔ the gate's own ledger (the control) | `gate_events` where `gate_name='scope_check'`: pass 117, fail 6; the six fails — `100037` step 1 (`tests/test_cycle_check.py`, overridden, ref a scratchpad file), `100037` step 2 (`tests/test_depositor_receipts.py, tests/test_wrap_receipts.py`, overridden, `knowledge/overrides/override-100037-step2-scope_check.md`), `100045` step 2 (`tests/test_gate_qa_test_result.py`, overridden, `verdicts/resolved/override-100045-step-2-scope_check.md`), `100052` step 1 (`tests/test_step_files_record.py`, overridden, `verdicts/resolved/override-100052-step-1-scope_check.md`), `100053` step 1 (`tests/test_substrate_check.py`, overridden, `verdicts/resolved/override-100053-step-1-scope_check.md`), `100054` step 1 (`tests/conftest.py`, NOT overridden — the plan is halted); every fail names a `tests/` file; the #100061 step-2 event is a pass | `SELECT s.plan_id, s.step_number, g.result, g.overridden, g.override_ref, g.reason_code FROM gate_events g JOIN steps s ON g.step_id=s.id WHERE g.gate_name='scope_check'` |
| P5 | ⛔ the instance that opened the thread | `d6da69b` — `qa(close_cycle): full suite + governance replay evidence; two dry-run fixes [100061] [thread 260]`; `git show --stat`: `scripts/close_cycle.py | 22 ++-`, `3 files changed, 212 insertions(+), 1 deletion(-)`; STEP 2's Scope in `Done/executable-100061.md:149–151` lists the two evidence files only; STEP 1's Scope (`:104–107`) lists `scripts/close_cycle.py` first | `git show --stat --format=%s d6da69b`; the Done file |
| P6 | ⛔ the gate's rule, quoted | gates.py:1133 `for s in range(1, step_number + 1):`; :1145–1147 `for st in all_step_texts: files, prefixes = _extract_plan_scope(st); scope_files_raw.update(files)`; :1152–1158 the declared set is Scope ∪ Deposits (`_extract_deposits_block_paths`); :1180 `if basename in SCOPE_ALLOWLIST` (`[".gitkeep"]`) and :1182 the lifecycle-file prefixes (`in-progress-`, `verdict-pending-`, `halted-`, `hold-`, `ready-`); :1189 `d == fpath or ("/" in d and d.split("/", 1)[1] == fpath)` — one extra leading segment on the declared side; the extractors: `_extract_step_text` :565, `_extract_plan_scope` :643, `_extract_deposits_block_paths` :1103 | `sed -n 1127,1200p gates.py` |
| P7 | the step's role | `steps.role` is NULL on every row read (`SELECT step_number, role FROM steps WHERE plan_id IN (100061,100070)` → `1|`, `2|`); the role comes from the step header — `## STEP N — DEV …`, `## STEP N — QA …`, `## STEP 1 — DIAGNOSTIC: …` — read by `_extract_step_text` | the same query; the Done file |
| P8 | the corpus outside the DB | `knowledge/decisions/Done/` holds 576 files: 223 id-named (`executable-<id>.md`, `diagnostic-<id>.md`), all 223 with a `**Scope:**` block; the ~250 pre-lifecycle names (`executable-<slug>-2026-0N.md`) have no DB row and are outside both arms — the doc says so | `ls knowledge/decisions/Done \| grep -cE '^(executable\|diagnostic)-[0-9]+\.md$'`; `grep -L '\*\*Scope:\*\*'` over them |
| P10 | ⛔ the union's origin, and the diff's present form | the union shipped in `706fbe7` (2026-06-10, `Done/executable-bellows-scope-check-fp-fix-impl-2026-06-10.md`, design `knowledge/architecture/scope-check-false-positive-fix-design-2026-06-09.md` §(a)) for BACKLOG #2, "continuous-run cumulative diff": the changed set was then `git diff --stat <pre_diff> -- .` against the WORKING TREE, so step 2's diff carried step 1's uncommitted files; the commit calls the fix "purely additive — only widens the authorization haystack, never narrows it". Today `pre_diff` is captured at each step's start (bellows.py:1150–1151 for the first or resumed step, :1309 for each auto-continued step) and the set is `_parse_diff_stat(post_diff, pre_diff, wt_path)` (:1206) — a `pre..post` commit range when `post_diff` is captured, the working tree only as its fallback (the docstring) | `git show -s 706fbe7`; the design doc §(a); `sed -n 1150,1151p;1309p;1206p bellows.py` |
| P9 | in-flight; class; interpreter | queue empty at drafting (`python3 status.py`: IN-FLIGHT none, AWAITING VERDICT none); writes: a bellows dev-log and two governance research files → **app-feature** (auto-clears; the depositor's parse recorded at deposit); Python 3.9 stdlib plus `gates` imported from the worktree root, the bellows venv; `gates.py` imports `logging, os, re, subprocess, yaml` and nothing of bellows — importing it never imports `lifecycle` | `python3 status.py`; `_assign_class` |

## MUST-PRESERVE

- ⛔ **Read-only.** No gate, tool, script or lane file is edited; the census script lives in `$T` and in the doc as a code block; nothing under `tools/` or `scripts/`; the lifecycle DB is opened `mode=ro` and only ever read; `lifecycle` is never imported — the script reads with `sqlite3` directly.
- ⛔ **The gate's rule is re-used, not restated.** The classification calls `gates._gate_scope_check` itself — twice per path, Item 1 — and imports `SCOPE_ALLOWLIST` and `SCOPE_ALLOWLIST_PREFIXES`; nothing of `:1133–1195` is re-implemented; the doc quotes those lines beside the script.
- ⛔ **No recommendation.** The doc answers Q1–Q5 and ends with "What this does not establish"; the arm, its WARN/FAIL threshold and the template sentence are 262's next act, the CEO's to open.
- ⛔ **Every instance carries plan, step, file, sha and the diff stat**, quoted from `git show --stat`, never summarised.

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-qa-scope-census-2026-09-11.md
**Walks:** walk 0 pinned (P1–P10 measured on bellows `cc58328`, governance `34470449`; clone-diff against `Done/diagnostic-100059.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py`; the tool run bare, its exit read; no lens or walk number in any `--desc`; `lens_order_check` after every walk.

- Weak spots:          w1 3 folded — instruction 2 / record 1; w2 1 folded — instruction 1 / record 0; w3 dry
- Destruction:         w1 2 folded — instruction 2 / record 0; w2 dry; w3 dry
- Vulnerabilities:     w1 2 folded — instruction 2 / record 0; w2 dry; w3 dry
- Integration-record:  w1 2 folded — instruction 2 / record 0; w2 dry; w3 dry
- ACID:                w1 6 folded — instruction 2 / record 4; w2 dry; w3 dry
**Closing:** WARM close after walk 3 — BAR MET (T1), thread 262's measurement before the arm. Three walks: 10 → 1 → 0 (walk 1's ACID pass reconciled four cross-fold contradictions; walk 2 folded one instruction; walk 3 dry on every lens). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-`; class app-feature auto-clears; the daemon claims and the verdict is issued under the CEO's 2026-09-11 overnight delegation (continue on all-pass with the (b) check, the verdict its own call). Close acts: the Planner commits the two governance deposits at the wrap; the arm and the template sentence are 262's next act.

## Cycle Manifest
tier: T1
target: knowledge/development/dev-log-qa-scope-census-2026-09-11.md
class: app-feature
reads: gates.py, lifecycle.db, knowledge/decisions/Done/diagnostic-100059.md, knowledge/decisions/Done/executable-100061.md, knowledge/decisions/Done/executable-100052.md, knowledge/decisions/Done/executable-100053.md, knowledge/decisions/halted-executable-100054.md
writes: knowledge/development/dev-log-qa-scope-census-2026-09-11.md, governance/knowledge/research/qa-scope-census-2026-09-11.md, governance/knowledge/research/qa-scope-census-2026-09-11.tsv
open_forks: 1. the per-step arm of `scope_check` (a file changed by step N must be in step N's own Scope ∪ Deposits), WARN first, its WARN rate from Q5's count and its production class from Q3; 2. the PLANNER_TEMPLATE sentence 262 proposes — a QA step that must change production code STOPS and requests a verdict; 3. a `step_files`-vs-commit agreement check if Q2 shows the two arms disagree on any step they both cover
walks: 3
yields: 10, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:34
coherence: 3/3 body walks named in the register (20 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.diagnostic-bellows-qa-scope-census.md.foldcheck.json

---

## STEP 1 — DIAGNOSTIC: five questions over two arms of the lifecycle DB, every instance with plan, step, file and sha

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ **Read-only:** never run the daemon, `run_plan`, a claim; never import `lifecycle`; the lifecycle DB is `/Users/marklehn/Developer/bellows/lifecycle.db`, opened as `sqlite3.connect("file:/Users/marklehn/Developer/bellows/lifecycle.db?mode=ro", uri=True)` and never written; the Done files are read at `/Users/marklehn/Developer/bellows/<plan_doc_ref>` (the CANONICAL checkout, not this worktree, whose tree carries the plan's own files); the commits are read with `git -C /Users/marklehn/Developer/bellows show --name-only --format= <sha>`; `gates` is imported from THIS worktree's root (`sys.path.insert(0, os.getcwd())` after the TREE_OK line — its extractors are pure functions over text); `T=$(mktemp -d /tmp/qa-scope-census.XXXXXX)` holds the script and scratch output; nothing else is written outside the three deposits.
>
> **Scope:**
> - `knowledge/development/dev-log-qa-scope-census-2026-09-11.md`
>
> **Item 1 — re-derive P1 and P2 and HALT on a mechanism mismatch** (a count that moved because a plan closed since drafting is reported as both figures). Write the census script to `$T/census.py` — verbatim in the doc's appendix — with ONE classifier used by both arms, built from TWO CALLS to the gate itself — never a re-implementation of its match: for a step (plan_id, step_number) and a path, `allowlist` if the basename is in `gates.SCOPE_ALLOWLIST` or starts with one of `gates.SCOPE_ALLOWLIST_PREFIXES` (the two constants, imported); else `own` if `gates._gate_scope_check(own_text, N, [path], failures, project_path="/Users/marklehn/Developer/bellows")` appends nothing to `failures`, where `own_text` is the plan text with `gates._extract_step_text(plan_text, s)` replaced by the empty string for every `s < N` (the gate's own span, so the cut matches the gate's read; `_extract_step_text(own_text, s)` then returns `None` for each removed step and the union at `:1133–1137` collapses to step N alone — the doc shows the collapse by printing, for one QA step, the count of `s in 1..N` whose extraction from `own_text` is not `None`, which must be 1); else `earlier-step-only` if the same call on the FULL plan text appends nothing (the gate's own live verdict — the union of steps 1..N); else `undeclared`. Before the arm runs, CALIBRATE the classifier on the instances the ledger already names and HALT on any mismatch (a mechanism mismatch, not a finding): `100061` step 2 `scripts/close_cycle.py` must classify `earlier-step-only`, and the files the six `scope_check` fails name (P4) must classify `undeclared` for their steps — the script asserts both and prints each verdict. Then run it over the `step_files` arm; paste the four counts beside P2's and, for every row that is not `own`, the row. The walk-0 parser read Scope bullets only — where the DEV's count differs from P2, the doc says which arm (Deposits, allowlist) moved the row.
> **Item 2 — Q1: the `step_files` arm by role.** For the 35 steps, the role from the step header (`DEV`, `QA`, `DIAGNOSTIC`, other — P7: `steps.role` is NULL) and the four classes per role, one table; the `earlier-step-only` rows listed with plan, step, role, path.
> **Item 3 — Q2: the `commits` arm.** For each of the 124 bellows commits (P3), `git show --name-only --no-renames --format= <sha>` from the canonical checkout (a rename lists as its old and new paths — the gate's diff-stat writes a rename as `old => new`, so the agreement check names that form where it sees it; a commit listing no file is reported as such), every path classified with the same classifier against the commit's step (a plan whose `plan_doc_ref` no longer resolves is class `no-doc`, listed, never a halt — P3 says all 58 resolve at drafting); the four counts by role, one table; every `earlier-step-only` and `undeclared` path listed with plan, step, role, path, sha. Then the agreement check on the steps BOTH arms cover (the #100050+ steps with a bellows commit) — two INDEPENDENT sources (P1: the gate's worktree diff-stat of the last run; P3: the step's commits): for each such step the set difference `step_files` − commit paths and commit paths − `step_files`, counted; every disagreeing step listed with the reason the sources can differ named beside it where it is visible (a retried step, an uncommitted change, a Planner commit). The check also separates the two classes the union conflates (P10): an `earlier-step-only` path in step N's `step_files` that NO step-N commit carries is the cumulative-diff residue the union was built for; one a step-N commit carries is a genuine later-step edit, the class thread 262 names — the doc counts each (this is the input to open fork 3 — measured, not judged).
> **Item 4 — Q3: the rate the arm turns on.** Over the union of both arms (a step counted once), the number of steps of role QA (or any step ≥ 2) that COMMITTED a change to a file in an earlier step's declared set only — an `earlier-step-only` path a step-N commit carries (Item 3's genuine-edit class; a path seen in the diff-stat alone is residue, reported in Q2 and NOT counted here); for each such step: the file, `git show --stat --format=%s <sha>` for the commit that changed it (P5's form), and the file's kind by path — `production` (a `*.py` at the root or under `hooks/`, `scripts/`, `tools/`), `test` (under `tests/`), `record` (under `knowledge/`), `other`; the rate as `<steps flagged> of <QA steps in the corpus>` and, separately, `<production files> of <files flagged>`.
> **Item 5 — Q4: the control, from the gate's own ledger.** P4 re-derived: the `scope_check` events by result; every fail with plan, step, role, the files from `reason_code`, `overridden`, `override_ref`, and whether the same paths later appear in `step_files` for that step (they do for `100052`, `100053`, `100054` at walk 0 — the gate caught DEV-step test files the Scope omitted, the Planner overrode, and the rows were recorded); the #100061 step-2 event quoted (a pass) beside the union rule that passed it (P6 `:1133`). The doc states what the ledger shows the union arm catching (files declared by NO step) and what it cannot catch by construction (files declared by an earlier step), and cites the disposition the five overrides followed — LESSONS 2026-05-28, "`scope_check` false-positive on plan-required evidence files — Planner override is the right disposition" — beside each override ref, as the record's precedent, not as a judgement of the arm.
> **Item 6 — Q5: the per-step arm simulated, measured not built.** Apply the classifier's `own`-or-`allowlist` test alone (no union) to every step in both arms and count the steps it would WARN on; list each with plan, step, role, the paths and their kinds; name the ones where the later step's edit is to a `record` file an earlier step declared (a QA step appending to the DEV's dev-log, say) as the arm's false-positive candidates, and the ones where it is `production` as the class 262 names — counts of each, no judgement on the threshold. State the relation as a fact: the per-step arm is the single-step rule `706fbe7` replaced (P10), so its false-positive class is the residue Q2 counted plus the `record` candidates here; the doc reports the three numbers side by side.
> **Item 7 — write the research doc** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/qa-scope-census-2026-09-11.md` answering Q1–Q5 in order, each section opening with the script's raw output line it rests on, P6's gate lines quoted once beside the classifier, the census script as a code block in an appendix, and the TSV `qa-scope-census-2026-09-11.tsv` — one row per (arm, plan, step, path), columns `arm, plan, step, role, path, sha, class, kind, note`; end with "What this does not establish" (at least: the corpus starts where the DB starts — the ~250 pre-lifecycle Done files are outside it (P8); the `commits` arm sees the daemon's commits, not a Planner's; a kind-by-path rule; one instance's benignity says nothing about the next; Q3's rate is over QA steps while the arm 262 proposes fires on EVERY step — Q5's count, not Q3's, is the arm's WARN rate; the threshold is not derived here).
> **Item 8 — dev-log** with the headings declared below (the `dev_log_declared_text` gate reads them at the pause); `## Pins re-derived (P1, P2)` opens with P2's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `1 union-only, 3 unscoped`; paste through those words, then stop. **Commit** the dev-log alone (message tagged with the plan id and `thread 262`), gated on the FULL suite (`.venv/bin/python -m pytest -q` from the worktree root — nothing under bellows changed but the rule is the rule), path-scoped to the Scope list; the two governance files are Planner-commit deposits. Last act, after the commit: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P1, P2)`; `## The rate (Q3)`; `## Q1–Q5 in one table`; `## What the doc does not establish`
> **Verbatim:** `## Pins re-derived (P1, P2)` ← P2
>
> **Deposits:**
> - `knowledge/development/dev-log-qa-scope-census-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/qa-scope-census-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/qa-scope-census-2026-09-11.tsv`
>
> **Post-conditions:** the doc answers five questions, every flagged instance with plan, step, file, sha and diff stat, and ends without a recommendation; Q3 states the rate in the two forms named; Q4 lists all six ledger fails with their override refs; the TSV has one row per (arm, plan, step, path); the dev-log's four headings present as full lines with P2's cell whole; one bellows commit carrying the dev-log only; no gate, tool, script or lane file changed (`git -C /Users/marklehn/Developer/eluvian-governance status --porcelain` shows only the two research files; `git status --porcelain` in the worktree shows nothing after the commit); the DB unchanged (`PRAGMA data_version` read before and after the census is equal, or the doc names the daemon write that moved it); nothing under `$T` that survives the step.
