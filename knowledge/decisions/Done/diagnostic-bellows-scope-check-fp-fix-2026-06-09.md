# Bellows — scope_check False-Positive Fix Design (Diagnostic)
**Date:** 2026-06-09 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## Context (Rule 27)
Two Open BACKLOG entries are the same root cause: #9 (2026-05-29 — `scope_check` cannot evaluate plans that delegate file lists to a referenced blueprint) and #2 (2026-06-08 — `scope_check` false-positive on continuous-run multi-step plans: it evaluates the CUMULATIVE worktree diff against only the FINAL step's prompt context). Both reduce to: the allowed-set is derived from one step's prompt text while the checked-set is the whole-plan cumulative diff. This fires a manual Rule 22(b) override on essentially every SA→DEV→QA executable — including BP2's DEV step earlier today. The established `_gate_scope_check` clause structure to extend was shipped in the directory-mention fix (BACKLOG Closed 2026-06-05, `Done/executable-scope-check-dir-mention-2026-06-04.md`). This is a design diagnostic only — the implementing executable is authored after CEO review. **Bellows self-fix constraints (for the eventual executable, noted here so the design accounts for them):** the change is in `gates.py`, loaded at daemon startup, so it needs a daemon restart to activate; and the executable's own DEV scope must be tight (`gates.py` + `tests/test_gates.py`, explicitly enumerated, no blueprint delegation) so it does not trip the very bug it is fixing while the pre-fix daemon evaluates it.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file first, then the source: `gates.py` (`_gate_scope_check` and its allowlist constants + the deposit/scope extraction helpers) and `bellows.py` (how the plan-step context is captured and passed into `gates.check()` at a step, and how a continuous multi-step plan accumulates the worktree diff that scope_check evaluates). **Post a 1-line "Read X." after each file read and a 1-line "Drafting Section N." at the start of each section** (keeps the inactivity timer warm). This is a design diagnostic — propose the fix; author NO source, no `gates.py` edits (the implementing executable comes after CEO review). Ground every claim in current code, not the BACKLOG's prior framing (BACKLOG lags code — verify line anchors live). **Section 1 — Mechanism proof (both FP classes).** Against current `gates.py`/`bellows.py`, prove: (a) #2 continuous-run — that `_gate_scope_check` evaluates the CUMULATIVE changed-file set (all steps' files in the shared worktree) against only the terminal step's captured prompt context; show exactly where the changed-file list is computed and where/how the step context is captured (the ~80-char body capture noted in BACKLOG #9 — confirm or correct the current capture width and source). (b) #9 blueprint-delegation — that a file list living in a referenced blueprint document (not the step body) is not followed; the gate matches only literal paths/basenames present in the step text. State the single shared root cause precisely. **Section 2 — Fix design.** Propose the concrete mechanical shape, with the primary recommendation being: evaluate each changed file against the UNION of ALL step prompt contexts in the plan, not only the terminal step's. Determine from the code whether `gates.check()` at the terminal step even HAS access to all step texts or only the current step's — if only the current step's, specify the caller-side change in `bellows.py` to pass the full set (this is the load-bearing feasibility question). Assess the two alternatives the BACKLOG names — per-producing-step attribution (via per-step commit boundaries) and follow-the-blueprint-reference (view-and-extract the referenced doc's file list) — and recommend one with reasoning; note the blueprint-follow option's regex-fragility risk. **Keep the fix strictly Layer-1 mechanical** (text/path matching) — it must NOT require Bellows to interpret plan correctness or agent intent (SA guardrail); flag immediately if any proposed shape would cross that boundary. Do NOT recommend the PLANNER_TEMPLATE `**Target Files:**`-inline alternative as the fix (it is non-load-bearing and the SA contract bars template edits pending the watcher-reliability items). **Section 3 — Preserve the gate's teeth.** The negative case is load-bearing: a genuinely out-of-scope write (a file mentioned in NO step context) must STILL FAIL after the fix. Specify the exact insertion point in `_gate_scope_check` and confirm the change is purely additive (only moves a file from out-of-scope to in-scope, never the reverse), mirroring the directory-mention fix's additive-clause pattern. **Section 4 — Test surface.** Enumerate the `tests/test_gates.py` additions: positive cases (a blueprint-delegated file and an earlier-step file are in-scope under the union) AND >=2 NEGATIVE cases proving teeth (an unmentioned file is still flagged; the union does not blanket-authorize). **Section 5 — Layer Impact (required) + executable hand-off.** State which layers are affected (expect: Layer 1 gates.py change that REDUCES Layer 3 Planner override burden, no judgment shift), the exact files the executable will touch (`gates.py`, `tests/test_gates.py`, and any `bellows.py` caller change), and the daemon-restart + tight-scope self-fix constraints. Produce a Gap Assessment table `| Gap | Current State (file:line) | Proposed State | Change Required |`. **Section 6 — `### Verification Blocks` (Rule 39)** with `(claim, query, expected_output)` triples for the load-bearing current-state facts (e.g., scope_check evaluates against terminal-step context only; the changed-file set is the cumulative worktree diff) so the implementing executable can re-verify at edit time. Use `with open()` for the deposit; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md` (or the bellows-conventional feedback path if different — match existing). **Deposits:**
> - `bellows/knowledge/architecture/scope-check-false-positive-fix-design-2026-06-09.md`
