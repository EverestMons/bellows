---
type: diagnostic
project: bellows
created: 2026-05-12
auto_close: false
pause_for_verdict: after_step_1
---

# Diagnostic — Bellows BACKLOG Full Sweep

**Execution Map:** Step 1 (BELLOWS_SYSTEMS_ANALYST)

**Purpose:** Classify every Open item in `bellows/knowledge/BACKLOG.md` as one of (a) shipped-but-not-closed, (b) verified-still-open, (c) needs-investigation. Memory is stale; recent governance edits and fix plans (2026-05-11 Rule 26 tightening, fence-strip parser fix, step-header anchor fix, 213-file lifecycle cleanup commit) likely closed items that BACKLOG still shows as Open. Produce a per-item verdict table so the Planner can author closures in a follow-up housekeeping pass.

---

## STEP 1 — BELLOWS_SYSTEMS_ANALYST

Read your specialist file and domain glossary first. Then read `bellows/knowledge/BACKLOG.md` in full. For every entry currently marked **Open** (or whatever the equivalent unresolved status field uses — "Open", "Active", absence of a closure date, etc.), produce a classification verdict. Enumerate the Open items first by listing them: report the count and a numbered list of `(BACKLOG number or anchor, one-line title)` so the CEO can see scope before reading verdicts. Then for each Open item perform this verification sequence in order. **First**, read the BACKLOG entry in full — its description, reproduction history, recommended fix shape, and any update log. **Second**, identify the suspected file(s) and function(s) named in the entry. If the entry names specific code paths (e.g., `gates.py::_resolve_deposit_path`, `verdict.py:102`), record those as the verification targets. If the entry is vague about code location, record "no code anchor" and skip to the fourth step. **Third**, for each named code path, read the current file at HEAD and check whether the recommended fix shape is present. Cite line numbers from your read. For shipped-fix detection use these signals — a function or branch matching the recommended fix-shape description, a comment or docstring referencing the BACKLOG number, or a recent commit message in `git --no-pager log --since=2026-04-01 --oneline -- <file>` matching the fix shape. **Fourth**, check the BACKLOG entry's reproduction history against the project's actual recent activity — run `git --no-pager log --since=<earliest-reproduction-date-in-entry> --oneline` and scan for commit messages that match the entry's recommended fix shape or BACKLOG number. Also grep `bellows/knowledge/decisions/Done/` for plan filenames matching the entry's topic to detect fixes shipped via Bellows-dispatched plans. **Fifth**, cross-reference the PLANNER_TEMPLATE.md Lessons table (governance root at `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`) for entries dated 2026-04-15 or later that mention the BACKLOG item by number, by topic, or by code-path. A Lessons entry citing the BACKLOG item as resolved counts as a shipped-fix signal. **Sixth**, classify the item using this taxonomy. **(a) shipped-but-not-closed** — code or governance evidence shows the recommended fix shipped, but BACKLOG still lists the item as Open. Cite the commit hash or Lessons entry and the file:line where the fix is visible. **(b) verified-still-open** — no fix-shape evidence found in code, no matching commit, no Lessons closure. The item is genuinely awaiting work. Note any partial work (e.g., diagnostic shipped but no executable) and what would be needed to close. **(c) needs-investigation** — the entry is too vague, contradicts itself, or its claimed reproductions cannot be matched to current code state. Note what a follow-up diagnostic would need to determine. Do NOT recommend fixes — classification only. Do NOT edit `BACKLOG.md` — read-only sweep. Do NOT skip items because they look obviously open or obviously closed; every Open item gets the same five-step verification. Deposit your findings to `bellows/knowledge/research/backlog-sweep-2026-05-12.md` with this structure — a header section listing the total Open count and the date of your read, a per-item section for each Open item with subsections `**Entry summary**` (1-2 sentences), `**Code/governance check**` (what you read and what you found, with file:line citations), `**Git/Lessons check**` (commits matched, Lessons entries matched), `**Classification**` (one of the three labels above, with rationale), and a closing **Summary Table** with columns `BACKLOG # | Title | Classification | Closure evidence (if shipped) | Open scope (if open)`. Run a Rule 20 self-check at the end of the report per the canonical block at `/Users/marklehn/Desktop/GitHub/RULE_20_SELF_CHECK_BLOCK.md` — write evidence files to `bellows/knowledge/qa/evidence/backlog-sweep-2026-05-12/` covering at minimum: one shipped-but-not-closed item's grep output proving the fix is in current code, one verified-still-open item's grep output proving the fix shape is absent. Append a one-line entry to `bellows/feedback.log` reading `2026-05-12 — backlog sweep — <count> Open items classified: <a-count> shipped-but-not-closed, <b-count> verified-still-open, <c-count> needs-investigation`. Final commit message: `docs: BACKLOG sweep 2026-05-12 — classify N Open items`.

**Deposits:**
- `bellows/knowledge/research/backlog-sweep-2026-05-12.md`
- `bellows/knowledge/qa/evidence/backlog-sweep-2026-05-12/`
- `bellows/feedback.log`
