# bellows + governance — Fix Step 2 Auto-Advance: PLANNER_TEMPLATE pause_for_verdict + Bellows Warning
**Date:** 2026-05-08 | **Tier:** Small | **Test Scope:** targeted + full-suite | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation before proceeding to Step 2.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/executable-step2-auto-advance-fix-2026-05-08.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

## CEO Context

Two independent fixes for the Step 2 auto-advance bug diagnosed earlier today (findings: `bellows/knowledge/development/diagnostic-step2-auto-advance-findings-2026-05-08.md`):

**Fix A — PLANNER_TEMPLATE update (governance-root):** Add `pause_for_verdict: after_step_1` to the standard plan header format. Add a brief Bellows-Execution-Model section entry explaining the recognized values (`always`, `after_step_1`, `after_qa_step`). After this fix, every new multi-step plan the Planner emits will include the header field, and Bellows will pause correctly between steps.

**Fix B — Bellows defensive WARNING (not behavioral change):** When a multi-step plan is dispatched without a `pause_for_verdict` header, emit a startup-time warning. The orchestrator behavior does NOT change — auto-advance still happens for headerless plans. This gives visibility without behavioral risk to existing flows. Per CEO decision: do NOT default to `after_step_1` — only warn. After Fix A has been live for ~2 weeks with zero warnings firing, escalate Fix B to a behavioral default in a separate executable.

This is a Bellows-self change for Fix B + a governance-root change for Fix A. Bellows-self runs in-place (no worktree isolation per accepted bellows-self behavior). The PLANNER_TEMPLATE lives at `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md` (governance root, NOT inside any watched project). Plan governance-root file edits via Filesystem tools; commit them in the bellows repo since the bellows repo is the closest semantic home for this orchestration-driven governance change. Bellows does not hot-reload — CEO must restart the daemon manually after the change ships.

**Specifics from diagnostic, locked:**
- `header_says_pause()` is at `bellows.py:188-197`
- The 4 call sites are at `bellows.py:348, 431, 440, 467` (no changes needed at any of them)
- Fix B targets `_handle_new_plan()` or wherever a new plan's `total_steps` is first computed and the header is parsed — find it via the diagnostic's reference to "startup-time warning when a multi-step plan is dispatched"
- PLANNER_TEMPLATE current header line shape (around line 328-329): `**Date:** YYYY-MM-DD | **Tier:** [tier] | **Test Scope:** [...] | **Execution:** Step 1 (AGENT) → Step 2 (AGENT)`

---
---

## STEP 1 — DEV

---

> **FIRST — claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-step2-auto-advance-fix-2026-05-08.md", "bellows/knowledge/decisions/in-progress-executable-step2-auto-advance-fix-2026-05-08.md")`. You are the Bellows Developer. Read your specialist file and the diagnostic findings at `bellows/knowledge/development/diagnostic-step2-auto-advance-findings-2026-05-08.md` before touching code — the diagnostic has exact line/code citations and behavior decisions. Implement two fixes. **Fix A — PLANNER_TEMPLATE update at governance root:** edit `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`. Two changes. (1) In the plan header format section (around line 328-329 per diagnostic — verify by searching for the literal substring `**Execution:** Step 1`), append ` | **pause_for_verdict:** after_step_1` to the example plan header line. The new header line shape: `**Date:** YYYY-MM-DD | **Tier:** [tier] | **Test Scope:** [...] | **Execution:** Step 1 (AGENT) → Step 2 (AGENT) | **pause_for_verdict:** after_step_1`. (2) Add a short subsection (3-6 sentences) titled `### pause_for_verdict header field` under the existing Bellows Execution Model section. Find that section by searching for the literal substring `Bellows Execution Model` in PLANNER_TEMPLATE.md. The new subsection must explain: the field is required for all multi-step plans; recognized values are `always`, `after_step_1`, `after_qa_step`; default for multi-step plans is `after_step_1`; if absent, Bellows currently auto-advances and emits a warning (forward-reference to Fix B). Do NOT remove or reword anything else in the template. Use Desktop Commander:edit_block for both edits with old_string/new_string targeting precise context. **Fix B — Bellows warning (no behavioral change):** edit `bellows.py`. Find the code path where a new plan is dispatched and `total_steps` becomes known — likely in `handle_new_plan()` or shortly after `_parse_plan_header()` is called for the first time on a fresh dispatch. Locate that path by reading `bellows.py:343` (the while-loop guard `is_final_step(current_step, total_steps)`) backwards to find where `total_steps` and the parsed `header` first become available together. Add a one-shot warning: when `total_steps > 1` AND `header.get("pause_for_verdict", "").strip() == ""`, emit `print(f"Bellows: ⚠️  {plan_name} has {total_steps} steps but no pause_for_verdict header — Bellows will auto-advance. Add 'pause_for_verdict: after_step_1' to the plan header.")`. Critically — do NOT change `header_says_pause()` semantics. Do NOT default to `after_step_1`. Behavior must remain identical; the warning is purely advisory. Add the warning at the earliest point where `plan_name`, `total_steps`, and `header` are all available; emit it once per plan dispatch, not per step. **Tests for Fix B (Fix A is template-text only and does not require code tests):** add to whichever `bellows/tests/` file already exercises the dispatch / new-plan-handling code path. Find it via `grep -rn "handle_new_plan\|total_steps" bellows/tests/`. Test 1 — multi-step plan without `pause_for_verdict` header: dispatch a fresh plan with 2 steps and no pause field; capture stdout; assert the warning string appears exactly once. Test 2 — multi-step plan WITH `pause_for_verdict: after_step_1` header: dispatch; assert warning does NOT appear. Test 3 — single-step plan without `pause_for_verdict` header: dispatch; assert warning does NOT appear (single-step plans don't need the field). Test 4 — multi-step plan with `pause_for_verdict: always`: dispatch; assert warning does NOT appear (any non-empty value suppresses the warning). **Verification:** run targeted tests with `pytest bellows/tests/[file] -v`. Then run full suite with `pytest bellows/tests/ -v` and confirm zero new regressions vs the most recent baseline at `bellows/knowledge/qa/evidence/qa-prefix-and-skip-logging/pytest_full.txt` (1 pre-existing `test_run_step_timeout` failure — must remain at exactly 1, no new failures). **Deposit dev log to `bellows/knowledge/development/dev-log-step2-auto-advance-fix-2026-05-08.md` using a single-call write (Filesystem:write_file).** The dev log MUST include an Output Receipt with the "Files Created or Modified (Code)" list per Rule 17 — list every file touched (PLANNER_TEMPLATE.md, bellows.py, the test file) with a one-line change description per Rule 17. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. Commit with message: "feat: PLANNER_TEMPLATE pause_for_verdict header + Bellows warning for multi-step plans without the field". The PLANNER_TEMPLATE.md change is a governance-root edit but lives in the bellows commit because bellows is the closest semantic home for this orchestration-driven governance change.
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — QA

---

> Before starting, read `bellows/knowledge/development/dev-log-step2-auto-advance-fix-2026-05-08.md` and check the Output Receipt status field. If status is not Complete, stop and report the issue to the CEO before proceeding. You are the Bellows QA Engineer. Read your specialist file first. Verify both fixes. **Deliverable verification (Rule 17, runs FIRST):** read the dev log's "Files Created or Modified (Code)" list. For every listed deliverable, verify it exists with the described content. Run `git --no-pager diff HEAD~1 -- bellows.py` (Fix B) and `git --no-pager diff HEAD~1 -- /Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md` (Fix A — note this is at governance root, outside the bellows repo, so it may require a separate `cd` and `git diff` in the governance-root checkout if it has its own git history; if PLANNER_TEMPLATE.md is NOT under git in the governance root, simply read the file and confirm the new content is present). Confirm: (a) PLANNER_TEMPLATE.md header line includes `pause_for_verdict: after_step_1` literal, (b) PLANNER_TEMPLATE.md has a new subsection on `pause_for_verdict` under Bellows Execution Model, (c) bellows.py has a warning print line containing the literal substring `pause_for_verdict header — Bellows will auto-advance`, (d) the warning is gated on `total_steps > 1` AND empty `pause_for_verdict`, (e) `header_says_pause()` is UNCHANGED at `bellows.py:188-197` (same return False default — confirm via diff or full read of those lines). Output as a verification table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. Any ❌ blocks the plan. Deposit the table to `bellows/knowledge/qa/qa-step2-auto-advance-fix-deliverable-verification-2026-05-08.md`. **Functional verification, each area with its own evidence file under absolute path `bellows/knowledge/qa/evidence/step2-auto-advance-fix/`.** Area A — Warning fires for multi-step plan without header: build a Python harness that constructs an in-memory plan with 2 steps and an empty `pause_for_verdict` field, invokes the dispatch path, captures stdout, asserts the warning string appears exactly once. Deposit harness output to `evidence/warning_fires.txt`. Area B — Warning suppressed when header is declared: same harness with `pause_for_verdict: after_step_1`; assert no warning. Repeat with `pause_for_verdict: always` and `pause_for_verdict: after_qa_step`; assert no warning in either case. Deposit to `evidence/warning_suppressed.txt`. Area C — Warning suppressed for single-step plans: harness with 1 step, no header field; assert no warning. Deposit to `evidence/warning_single_step.txt`. Area D — Behavioral non-change: build a harness that dispatches a 2-step plan without `pause_for_verdict`, lets Step 1 complete (mock the runner to return a passing gate result), and asserts that Step 2 IS still dispatched (the warning does not block advance). This is the critical safety check — Fix B must not change behavior. Deposit to `evidence/behavior_unchanged.txt`. Area E — PLANNER_TEMPLATE content check: grep PLANNER_TEMPLATE.md for the literal substring `pause_for_verdict: after_step_1` in the header line; assert exactly one match. Grep for the subsection header `### pause_for_verdict`; assert exactly one match. Deposit raw grep output to `evidence/template_content_check.txt`. Area F — Targeted regression: `pytest bellows/tests/[the file the DEV step touched] -v`. Deposit to `evidence/pytest_targeted.txt`. Area G — Full Bellows suite regression: `pytest bellows/tests/ -v`. Compare to baseline at `bellows/knowledge/qa/evidence/qa-prefix-and-skip-logging/pytest_full.txt` (223 passed, 1 pre-existing failure). Document any new failures explicitly. Deposit to `evidence/pytest_full.txt`. Each row in the QA report MUST cite an evidence file by absolute path. **No hedging keywords (Rule 19): "pending", "inferred", "extrapolated", "estimated", "approximate", "skipped", "assumed", "close enough", "should pass", "would pass", "not run" auto-invalidate any ✅ row containing them.** Deposit the QA report to `bellows/knowledge/qa/qa-step2-auto-advance-fix-2026-05-08.md` using a single-call write (Filesystem:write_file). The report MUST end with the mandatory Rule 20 self-check Python block — execute it and include literal stdout (banner + PASSED line). **Step A (housekeeping):** append a feedback entry to `bellows/knowledge/research/agent-prompt-feedback.md`. **Step B (commit):** commit with message "test: QA verification for step 2 auto-advance fix (PLANNER_TEMPLATE + Bellows warning)". The Planner performs the move-to-Done after Rule 22 verification.
>
> **STOP. Wait for CEO confirmation. Do NOT move the plan to Done — that is the Planner's responsibility after Rule 22 verification.**
