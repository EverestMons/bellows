# bellows — ANVIL'S 'WHAT WORKS' CALLERS FROM ONE EDGE SET: `verified_callers` collects the verified caller LABELS per production def over the same edges `verified_inbound` counts, a module-level caller is rendered `<file> (module)` (never `file::file`), `entry` takes its sample from that list instead of its own unverified query, so a row's caller count (verified call sites) and its name list (the distinct callers of those sites) are two readings of one set and can never disagree; the generator re-run on bellows deposits a second entries doc beside the first for the CEO to read (thread 283)

**Date:** 2026-09-11 | **Project:** bellows | **Tier:** Small (one library edit, one CLI edit, four tests, one mutation manifest, one dev-log; two research deposits) | **Dispatch Mode:** bellows | **Test Scope:** full-suite (anvil's suite under anvil's own venv — `pytest -q` from the anvil checkout — AND the bellows suite at each bellows commit, Rule 21) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 283 | **cycle_tier:** T1

**auto_close:** false

**Post-close:** no restart (nothing bellows loads changes), no doctrine. The three anvil files are Planner-commit deposits: the Planner commits them on anvil `main` after the merge (the #100075 shape, `d1f2819`), then closes the thread with that commit — `tuyere.threads done 283 --commit anvil:<sha> --repo-root /Users/marklehn/Developer/anvil` (a defect thread's `done` requires the commit, thread 184) — and commits the two governance research files. Whether the corrected entries are 'what works, applied elsewhere' stays the CEO's reading of the deposited doc; this plan writes nothing to `GLOSSARY.md` and claims nothing.

**Depends on:** thread 283 (found at the Planner's verdict read of #100075, 2026-09-11) and the CEO's ruling of 2026-09-11 16:29 (session d04ebd33): "283: fix". Clone origin by kind and layout: `Done/executable-100075.md` (this morning: a bellows-lane plan whose code deposits live under anvil and are committed by the Planner; the two-commit DEV with the manifest riding the first, failing-first, the mutation run on a scratch clone with `--repo-root`, `check_deposit.py --expect-missing` at the first commit, the one-token status cell, the thread-262 stop; a QA that runs the generator once on bellows and deposits the entries). Simplified against it in ONE place: anvil has its own venv on this machine since MACHINE_SETUP §1's wiring (`/Users/marklehn/Developer/anvil/.venv`, Python 3.9.6, `datasketch 2.0.0`, `274 passed`), so no scratch venv is built.

**Tier computed (§1):** **T1** — T-1 fires (anvil's library and CLI; a read across bellows' history); T-6 does not (no doctrine); T-2 does not (a scratch DB under a temp dir; no live store).

## CEO Context

#100075's entries doc carried two readings that cannot both be right. Row 2 (`lifecycle.py::init_lifecycle_db`) names its first caller `bellows.py::bellows.py`; rows 9 and 10 (`depositor.py::_assign_class`, 74 callers; `bellows.py::_apply_ledger_updates`, 32) show a count with an EMPTY name list. Measured on a fresh scratch DB of bellows this afternoon (P2, P3): anvil names every module-level chunk by its file path (5244 of 5244), so `entry`'s filter `src_c.name NOT LIKE '%/%'` excludes only modules in SUBDIRECTORIES — a root-level module (`bellows.py`) passes and renders `bellows.py::bellows.py`; and all 74 of `_assign_class`'s edges (all 32 of `_apply_ledger_updates`'s) come from `tests/…` module chunks, which `verified_inbound` counts (the test file imports the target's module, so the edge is verified) and `entry` filters out — the count comes from one query with the import check and the sample from another without it. The fix is one edge set with two readings: a function that walks the verified edges once and keeps the caller LABELS per target (`file::name` for a def, `file (module)` for a module chunk), the count being the length of that list, and `entry` drawing its sample from the same list. A caller count and an empty list can then never co-occur, by construction; the test that pins it asserts `len(sample) == min(distinct callers, 3)` on a method target with a module caller that calls twice — the count stays the EDGE count (P3's 74 is call sites; the three first edges of `_assign_class` came from ONE chunk), so Q2's numbers do not move.

## What this changes

1. **`anvil/src/what_works.py`** — (a) new `verified_callers(conn, checkout, defs) -> dict` mapping `'file::name'` → the list of caller labels, ONE PER VERIFIED EDGE in the query's row order (duplicates kept — a caller with three call sites appears three times, so the list's length IS the edge count), built from the SAME query and import check `verified_inbound` runs today (the `chunk_dependencies` rows with `dependency_type = 'call'`, `scope = 'cross_file'`, the caller's file read at `checkout` and passed through `_check_import`) with `src_c.name` and `src_c.chunk_type` added to the SELECT; the label is `f"{caller_file} (module)"` when `src_chunk_type == "module"` (anvil's module chunks carry `name == file_path`, P2 — the type is the discriminator, never the name's shape) and `f"{caller_file}::{caller_name}"` otherwise; (b) `verified_inbound(conn, checkout, defs)` becomes `{key: len(labels) for key, labels in verified_callers(conn, checkout, defs).items()}` — the same EDGE count as today (P3's 74 is 74 call sites, not 74 files), its signature, its docstring's rule and every existing test unchanged; (c) `entry(conn, checkout, row)` drops its own caller SQL (:321–333) and sets `"verified_callers_sample": sorted(set(row.get("verified_callers", [])))[:3]` — the DISTINCT callers, sorted, the first three (the old query's `DISTINCT … ORDER BY caller LIMIT 3`, now over the verified set); `verified_callers_count` stays `row.get("score_a", 0)`. The test-binding query, the role query and the returned keys are unchanged.
2. **`anvil/scripts/what_works.py`** — beside `vi = verified_inbound(conn, checkout, defs)` (:125): `vc = verified_callers(conn, checkout, defs)`; in the row loop, `row["score_a"] = len(vc.get(key, []))` and `row["verified_callers"] = vc.get(key, [])` — the count and the list from one dict; `vi` is no longer needed by the CLI and is removed from it (the library keeps the function). The TSV columns are unchanged (`render_tsv` reads `score_a`).
3. **`anvil/tests/test_what_works.py`** — four tests in the file's own shape (`conn` fixture, `_make_git_repo`, `create_chunk`, `create_dependency`; `test_verified_inbound_import_check` :180 is the clone): (t13) `test_verified_callers_module_caller_label` — `b.py` defines `f`; `a.py` is `import b\nb.f()\n` and its chunk is `chunk_type="module", name="a.py"` (a ROOT-level module, the row-2 case); `verified_callers(...)["b.py::f"] == ["a.py (module)"]` and `verified_inbound(...)["b.py::f"] == 1`; (t14) `test_entry_sample_agrees_with_count_on_method_target` — `b.py` holds `class C:\n    def m(self): pass`, the target chunk `chunk_type="method", name="m"`; three verified edges from two callers: one from `a.py::caller` (a function chunk in `a.py`, which imports `b`) and TWO from `tests/t_b.py` (a module chunk, name `tests/t_b.py`, whose file imports `b` and calls `m` twice — two `create_dependency` rows; the row-9 case, a caller under `tests/` with several call sites); `verified_inbound["b.py::m"] == 3` (edges), `verified_callers["b.py::m"]` has length 3 with `tests/t_b.py (module)` twice, and `entry(conn, checkout, {"file": "b.py", "name": "m", "score_a": 3, "verified_callers": <that list>})["verified_callers_sample"] == ["a.py::caller", "tests/t_b.py (module)"]` — `len(sample) == min(distinct, 3)` and no duplicate in the sample; (t15) `test_entry_sample_capped_at_three` — four verified callers, one edge each → count 4, sample the first three of the sorted distinct list; (t16) `test_verified_inbound_is_len_of_callers` — on t6's fixture (`a.py` verified, `c.py` not): `verified_callers(conn, checkout, defs)["b.py::f"] == ["a.py::caller"]` (the unverified `c.py` edge absent from the LIST, not only from the count) and `verified_inbound(conn, checkout, defs) == {"b.py::f": 1}` — the value asserted, never only the relation (a test that asserts `count == len(list)` against the function that defines one as the other cannot fail).
4. **`knowledge/mutants/what-works-callers.json`** (bellows) — five mutants with `target` paths relative to the anvil repo root, run with `--repo-root` on a scratch clone and `--python /Users/marklehn/Developer/anvil/.venv/bin/python` (Item 4): (m1) the module branch removed — every label `file::name` → t13; (m2) the module discriminator flipped (`!= "module"`) → t14; (m3) the `if verified:` guard removed in the edge loop (unverified edges counted) → t6 (existing `test_verified_inbound_import_check`); (m4) `entry`'s slice `[:3]` → `[:2]` → t15; (m5) `entry`'s `sorted(set(...))` → `sorted(...)` (duplicates kept in the sample) → t14 (three labels, one twice).
5. **Not changed:** `production_defs`, `survived`, `rank`, `precision_recall`, `render_markdown`, `render_tsv`, `ROLE_WEIGHTS`, the glossary guard and the CLI's flags; `src/config.py`; `GLOSSARY.md`; bellows code; the FIRST entries doc (`what-works-entries-bellows-2026-09-11.md/.tsv`) — it stays as the record #100075 deposited, the corrected run lands beside it with a `-v2` suffix.

## Why this exists

The entries are the smallest product the CEO ruled on, and their first reading was wrong in the two columns a reader trusts most — who calls this, and how many. One edge set with two readings is the whole fix; the second doc is the reading the CEO asked for.

## What this does NOT do

- Write `GLOSSARY.md`, or any file under a `knowledge/decisions` directory — the CLI's guard is unchanged and stays tested (t10).
- Change the definitions, the weights, the tie rule, or the count (the count is the same edge set; only the LIST is now drawn from it — Q2's numbers move only if bellows `main` moved since #100075's run, and the receipt states the HEADs).
- Run in the anvil lane (`config.json` watches `anvil/knowledge/decisions` on this machine since MACHINE_SETUP §1's wiring — an open fork; this plan keeps the proven bellows-lane shape and the Planner-commit deposit).
- Overwrite #100075's entries doc.

## MUST-PRESERVE

- ⛔ **One edge set.** `verified_inbound` is defined as the length of `verified_callers`' lists; no second caller query exists anywhere in `what_works.py` after this plan (`grep -c "src_c.file_path || '::'" src/what_works.py` is 0).
- ⛔ **Read-only over bellows, anvil's data and governance.** The scratch DB and the scratch clone live under `mktemp -d`; the only anvil files touched are the three *What this changes* names; the only governance files written are the two `-v2` research deposits.
- ⛔ **The canonical anvil checkout is never committed by the DEV or the QA** — the Planner commits after the merge; the mutation run uses a scratch clone.
- ⛔ **Every `expect_fail` is a class-qualified node id where the test is a method**, and the mutation run is redirected into its deposit, never piped.
- ⛔ **The thread-262 stop** (PT v4.108 §8): a QA step that must change production code STOPS and pauses for a verdict.

## Numbers discipline — measured 2026-09-11 by the Planner (bellows `d3456b9`, governance `8289745f`, anvil `fc32f63`, daemon pid 60199 under the agent)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the two queries | `anvil/src/what_works.py:146` `def verified_inbound(conn, checkout, defs)` — SELECT `src_c.file_path, tgt_c.file_path, tgt_c.name` over `chunk_dependencies d JOIN code_chunks tgt_c … JOIN code_chunks src_c …` `WHERE d.dependency_type = 'call' AND d.scope = 'cross_file'`, then per row `_check_import(source, target_module, target_name, caller_file)` and `counts[key] += 1` when verified; `:310` `def entry(conn, checkout, row)` — its OWN query `:321–333`: `SELECT DISTINCT src_c.file_path \|\| '::' \|\| src_c.name AS caller … AND tgt_c.file_path = ? AND tgt_c.name = ? AND src_c.name NOT LIKE '%/%' ORDER BY caller LIMIT 3` (no import check), `:335` `verified_count = row.get("score_a", 0)`; the CLI `anvil/scripts/what_works.py:125` `vi = verified_inbound(conn, checkout, defs)`, `:130` `row["score_a"] = vi.get(key, 0)`, `:157` `entries = [entry(conn, checkout, row) for row in top]`, `:159` `e["score_a"] = row.get("score_a", 0)` | `sed -n 146,192p;310,370p anvil/src/what_works.py`; `sed -n 120,160p anvil/scripts/what_works.py` |
| P2 | ⛔ how anvil names a module chunk | on a scratch DB of bellows built this afternoon with `build_scratch_db` (chunks: class 243, function 793, method 56, module 5244, test_case 2103): `SELECT sum(name = file_path), count(*) FROM code_chunks WHERE chunk_type = 'module'` → `5244, 5244` — every module chunk's name IS its file path, e.g. `('bellows.py', 'bellows.py', 'module')`, `('agents/BELLOWS_DEVELOPER.md', …)`; so `NOT LIKE '%/%'` keeps a root-level module and drops a subdirectory one | rebuild under `$T` (Item 0) and run the query |
| P3 | ⛔ the measured mismatch | same scratch DB: target `depositor.py::_assign_class` (chunk_type `method`) — 74 cross-file call edges, ALL from chunks `(chunk_type='module', name has '/', name == file_path)`, e.g. `('tests/test_admission_flip.py', 'tests/test_admission_flip.py', 'module')`; `verified_inbound` → 74 (EDGES — call sites: the first three rows of the 74 came from the same chunk, `tests/test_admission_flip.py`), `entry(...)["verified_callers_sample"]` → `[]`; `bellows.py::_apply_ledger_updates` (function) — 32 edges, all `tests/test_bellows.py` module chunks; 32 and `[]`; `lifecycle.py::init_lifecycle_db` — 120 edges: module-root 1 (`bellows.py`), function 17, module-subdir 86, test_case 16; `verified_inbound` 120, sample `['bellows.py::bellows.py', 'tests/conftest.py::isolate_lifecycle_db', 'tests/test_admission_flip.py::tmp_db']` — the doc's rows 2, 9 and 10 reproduced | the probe: `verified_inbound` and `entry` on the three keys against the scratch DB |
| P4 | ⛔ anvil's suite and fixtures | `274 passed in 1.60s` under `/Users/marklehn/Developer/anvil/.venv/bin/python` (3.9.6, `datasketch 2.0.0`, per-machine venv since MACHINE_SETUP §1's wiring 2026-09-11; `.gitignore` carries `.venv/` at `fc32f63`); `tests/test_what_works.py`: t1–t12, the `conn` fixture (`:56`, `sqlite3.connect(":memory:")`, `init_db`, `PRAGMA foreign_keys=ON`), helpers `_make_git_repo` `:26`, `_git_commit` `:44`; `test_verified_inbound_import_check` `:180` (the two-caller fixture: `a.py` imports `b`, `c.py` does not — `result["b.py::f"] == 1`); `src.db.create_chunk(conn, project_id, file_path, chunk_type, name, content, content_hash, start_line, end_line)`, `create_dependency(conn, src, tgt, "call", "cross_file")` | `cd anvil && .venv/bin/python -m pytest -q -p no:cacheprovider`; `sed -n 56,64p;180,216p tests/test_what_works.py` |
| P5 | ⛔ the lane and the deposit shape | `config.json` `watched_projects` on the mini: tuyere, bellows, forge_lessons, anvil (anvil added by the §1 wiring) — this plan nevertheless runs in the BELLOWS lane as #100075 did (`Done/executable-100075.md`: the anvil writes were the DEV's direct edits to `/Users/marklehn/Developer/anvil`, committed by the Planner at `d1f2819`); `scope_check` sees the bellows worktree only, so the anvil writes are verified by the QA's `git -C anvil status --porcelain` against the declared list; anvil `main` at `fc32f63`, clean | `grep -A6 watched_projects /Users/marklehn/Developer/bellows/config.json`; `git -C /Users/marklehn/Developer/anvil status --porcelain; log -1 --format=%h` |
| P6 | ⛔ the build instrument and the mutation tool | `anvil/scripts/reprice_bellows_build.py:15` `build_scratch_db(checkout, tmp_root) -> str` (the DB path; the temp-dir assert; used by the probe in P2/P3); bellows `tools/mutation_check.py <manifest> [--repo-root] [--keep-sandbox] [--python] [--timeout]` — `--python` names the interpreter that runs the audited suite (thread 29: a cross-repo audit needs the target repo's venv); the manifest form `{"target", "mutants": [{"name", "why", "target", "anchor", "replacement", "expect_fail"}]}` (`knowledge/mutants/what-works-generator.json`); a clean run's last line `MUTATION: <n> killed, 0 survived, 0 error` | the files; `--help` |
| P7 | the first entries doc | `governance/knowledge/research/what-works-entries-bellows-2026-09-11.md` (#100075, checkout `/Users/marklehn/Developer/bellows`, top-20, definition (c)): row 2 `lifecycle.py::init_lifecycle_db … 120 (bellows.py::bellows.py, tests/conftest.py::isolate_lifecycle_db, tests/test_admission_flip.py::tmp_db)`, row 3 `verdict.py::slug_from_path … 55 (bellows.py::bellows.py, bellows.py::run_plan, …)`, row 9 `depositor.py::_assign_class … 74 () … 0 ()`, row 10 `bellows.py::_apply_ledger_updates … 32 () … 0 ()`; #100075's QA receipt `knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md` row 2: `P@33=0.394 R@33=0.394 P@20=0.450 R@20=0.273` at bellows `a7ba78c` (equal to #100072's at `cc58328`) | the two files |
| P8 | in-flight; class; interpreter | #100083 in flight at drafting (this plan deposits after it and #100084 — the commits-per-step plan — close); writes: three anvil files (`src/what_works.py`, `scripts/what_works.py`, `tests/test_what_works.py`), the bellows manifest, run file, dev-log, two QA evidence files, two governance research files → **shop-infra** (`_assign_class`, walk 0 — the `anvil/` writes, as #100075) — HOLDS at admission, the CEO releases; the bellows venv for the bellows suite and tools, `/Users/marklehn/Developer/anvil/.venv/bin/python` for everything anvil | `.venv/bin/python status.py`; `_assign_class` |

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-what-works-callers-2026-09-11.md
**Walks:** walk 0 pinned (P1–P8 measured on bellows `d3456b9`, anvil `fc32f63`; clone-diff against `Done/executable-100075.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py`; the tool run bare, its exit read; no lens or walk number in any `--desc`; `lens_order_check` after every walk.

- Weak spots:          w1 3 folded — instruction 2 / record 1; w2 1 folded — instruction 0 / record 1
- Destruction:         w1 1 folded — instruction 1 / record 0; w2 dry
- Vulnerabilities:      w1 dry; w2 dry
- Integration-record:   w1 dry; w2 dry
- ACID:                 w1 dry; w2 dry
**Closing:** WARM close after walk 2 — BAR MET (T1), thread 283's fix. Two walks: 3 → 0 (walk 1 kept the count as the edge count and made the sample the distinct callers of those edges so Q2's numbers cannot move, let the QA's governance status check name strangers instead of halting, and gave t16 values it can fail on; walk 2 removed a drafting placeholder from the record and was dry on every lens). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-` AFTER #100083 and the commits-per-step plan close (P8); HOLDS on class shop-infra, the CEO releases; after the merge the Planner commits the three anvil files and the two `-v2` research files, then `threads done 283 --commit anvil:<sha> --repo-root`; the corrected entries are the CEO's reading.

## Cycle Manifest
tier: T1
target: anvil/src/what_works.py
class: shop-infra
reads: anvil/src/what_works.py, anvil/scripts/what_works.py, anvil/scripts/reprice_bellows_build.py, anvil/src/db.py, anvil/tests/test_what_works.py, governance/knowledge/research/what-works-entries-bellows-2026-09-11.md, knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md, knowledge/decisions/Done/executable-100075.md, knowledge/mutants/what-works-generator.json, config.json
writes: anvil/src/what_works.py, anvil/scripts/what_works.py, anvil/tests/test_what_works.py, knowledge/mutants/what-works-callers.json, knowledge/mutants/what-works-callers.run.txt, knowledge/development/dev-log-what-works-callers-2026-09-11.md, knowledge/qa/evidence/what-works-callers-qa-receipt-2026-09-11.md, knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt, governance/knowledge/research/what-works-entries-bellows-2026-09-11-v2.md, governance/knowledge/research/what-works-entries-bellows-2026-09-11-v2.tsv
mutants: knowledge/mutants/what-works-callers.json
open_forks: 1. anvil as the lane its own plans run in (`watched_projects` already names it on the mini) — the first anvil-lane plan proves the gates on that tree, and the Planner-commit deposit retires; 2. `tests_bound` 0 on rows whose 74 callers are test modules — the bindings table and the call edges disagree about what a test is (rows 9 and 10's `tests 0 ()`), a thread of its own once the entries are read; 3. the second project through the generator — 'applied elsewhere' measured, not claimed
walks: 2
yields: 3, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:104
coherence: 2/2 body walks named in the register (5 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-anvil-what-works-callers.md.foldcheck.json

---

## STEP 1 — DEV (one edge set with two readings, the CLI's row, four tests; the mutation run on a scratch clone; two bellows commits; the anvil files left for the Planner)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **Read-only over bellows** (the checkout under analysis is the CANONICAL `/Users/marklehn/Developer/bellows` at `main`; never this worktree); never run the daemon, `run_plan`, a claim, anvil's `run_cycle` or `run_lab`; never import `lifecycle`; ⛔ the anvil checkout `/Users/marklehn/Developer/anvil` is EDITED in place for the three files *What this changes* names and NEVER committed by this step (P5 — the Planner commits after the merge); `A=/Users/marklehn/Developer/anvil; AP=$A/.venv/bin/python` — every anvil call below uses `"$AP"`; `T=$(mktemp -d /tmp/wwc.XXXXXX)` holds the scratch DB and the scratch clone.
>
> **Scope:**
> - `knowledge/mutants/what-works-callers.json`
> - `knowledge/mutants/what-works-callers.run.txt`
> - `knowledge/development/dev-log-what-works-callers-2026-09-11.md`
>
> **Item 0 — the interpreter and the baseline.** `test -x "$AP" && "$AP" -c "import datasketch, sys; print(sys.version.split()[0], datasketch.__version__)"` (expected `3.9.6 2.0.0`; a different pair is RECORDED in the dev-log, never a halt) then `cd "$A" && "$AP" -m pytest -q -p no:cacheprovider` and paste the summary line beside P4's `274 passed` — the baseline before any edit; HALT on a `failed`; `git -C "$A" status --porcelain` must be EMPTY and `git -C "$A" log -1 --format=%h` must read `fc32f63` — HALT otherwise (a dirty anvil tree means another plan's edits are in flight there).
> **Item 1 — re-derive P1, P2 and P3 and HALT on a mechanism mismatch** (a `verified_callers` already in `what_works.py`, an `entry` without its own caller SQL, or a module chunk whose `name != file_path` in the rebuilt scratch DB, IS one). P2 and P3 are re-derived on a scratch DB built under `$T`: `cd "$A" && "$AP" -c "from scripts.reprice_bellows_build import build_scratch_db; print(build_scratch_db('/Users/marklehn/Developer/bellows', '$T'))"` (the bellows checkout is READ only), then the three queries of P2/P3 against it — paste each pin's re-derived value beside it (the edge counts may move with bellows `main`; the SHAPE — all-module callers on rows 9/10, `name == file_path` on every module chunk — is the mechanism).
> **Item 2 — write the failing tests FIRST:** `anvil/tests/test_what_works.py` t13–t16 as *What this changes* 3 (red: `ImportError: cannot import name 'verified_callers'`). Run the file from the anvil checkout with `"$AP"`; paste the red summary line.
> **Item 3 — the edits** as *What this changes* 1–2; then the file green (t13–t16 with t1–t12 still green), then anvil's FULL suite green (`N passed` with N ≥ Item 0's recorded baseline + 4 — 278 at drafting), then `grep -c "src_c.file_path || '::'" "$A/src/what_works.py"` → `0` (the second query is gone — the MUST-PRESERVE line).
> **Item 4 — the mutation run on a SCRATCH CLONE, redirected into `knowledge/mutants/what-works-callers.run.txt` (a Deposit):** `git clone -q "$A" "$T/anvil-mut" && for f in src/what_works.py scripts/what_works.py tests/test_what_works.py; do cp "$A/$f" "$T/anvil-mut/$f"; done && git -C "$T/anvil-mut" add -A && git -C "$T/anvil-mut" -c user.email=dev@scratch -c user.name=dev commit -q -m "scratch: what-works callers for the mutation run"` (every source path ABSOLUTE under `$A` — this worktree is bellows); write `knowledge/mutants/what-works-callers.json` (*What this changes* 4; `target` paths relative to the clone's root); then `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/what-works-callers.json --repo-root "$T/anvil-mut" --python "$AP" > knowledge/mutants/what-works-callers.run.txt 2>&1`; the last line must read `MUTATION: 5 killed, 0 survived, 0 error` — a survivor is a test to write, not a mutant to delete.
> **Item 5 — first bellows commit**, gated on the bellows FULL suite AND the pre-check with the stage flag (#100067), path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" --expect-missing knowledge/development/dev-log-what-works-callers-2026-09-11.md && git add knowledge/mutants/what-works-callers.json knowledge/mutants/what-works-callers.run.txt && git commit -F <msg-file> -- knowledge/mutants/what-works-callers.json knowledge/mutants/what-works-callers.run.txt` — two files (the manifest and its run; the code lives in anvil, uncommitted, P5); the message tagged with the plan id and `thread 283`.
> **Item 6 — the dev-log** `knowledge/development/dev-log-what-works-callers-2026-09-11.md` under the four headings declared below; `## Pins re-derived (P1, P2, P3)` opens with P3's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `rows 2, 9 and 10 reproduced`; paste through those words, then stop. Under `## Failing-first (red, then green)` the red line, the green line and anvil's suite line; under `## The anvil tree left for the Planner` the output of `git -C /Users/marklehn/Developer/anvil status --porcelain` — exactly the three declared paths, nothing else; under `## Mutation run` the run file's last line. **Second bellows commit**, gated on the bellows FULL suite and the pre-check bare (`… tools/check_deposit.py … 1 --wt "$(git rev-parse --show-toplevel)"`), path-scoped to the dev-log. Last act: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P1, P2, P3)`; `## Failing-first (red, then green)`; `## The anvil tree left for the Planner`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P2, P3)` ← P3
>
> **Deposits:**
> - `knowledge/mutants/what-works-callers.json`
> - `knowledge/mutants/what-works-callers.run.txt`
> - `knowledge/development/dev-log-what-works-callers-2026-09-11.md`
>
> **Post-conditions:** t13–t16 green with t1–t12; anvil's full suite `N passed` with N ≥ 278 and no `failed` (278 = P4's 274 plus four; Item 0 MEASURES the baseline before any edit and RECORDS it in the dev-log, and the recorded baseline plus four SUPERSEDES 278 if anvil moved); `grep -c "src_c.file_path || '::'" src/what_works.py` is 0 in the anvil tree; the mutation run's last line `MUTATION: 5 killed, 0 survived, 0 error`; two bellows commits — the first carrying the manifest and the run, the second the dev-log; the dev-log's four headings present as full lines with P3's cell whole; `git status --porcelain` empty in this worktree after the second commit; `git -C /Users/marklehn/Developer/anvil status --porcelain` lists exactly the three declared paths and `git -C /Users/marklehn/Developer/anvil log -1 --format=%h` is still `fc32f63` (nothing committed there); nothing under `$T` survives.

## STEP 2 — QA (anvil's suite; the generator re-run once on bellows; the second entries doc deposited beside the first; the receipt)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. `A=/Users/marklehn/Developer/anvil; AP=$A/.venv/bin/python; T=$(mktemp -d /tmp/wwc-qa.XXXXXX)`; read-only beyond the two bellows evidence files and the two governance research deposits; the anvil checkout is READ (its working tree carries the DEV's edits) and never committed.
>
> **Item 1 — anvil's full suite REDIRECTED not piped** (the suite file is a BELLOWS deposit, so the output path is absolute under THIS worktree): `WT=$(git rev-parse --show-toplevel); cd "$A" && "$AP" -m pytest -q -p no:cacheprovider > "$WT/knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt" 2>&1; cd "$WT"` — run FROM the anvil checkout (its tests import `src.…` by the cwd), the output path captured absolute before the `cd`; the summary line quoted in the receipt as `N passed` — never the word the Rule 20 block scans for.
> **Item 2 — the generator re-run once, on bellows, beside the first doc:** `cd "$A" && "$AP" scripts/what_works.py --checkout /Users/marklehn/Developer/bellows --tmp "$T" --definition c --top 20 --reference tests/fixtures/what-works-reference-bellows-2026-09-11.txt --out-md /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11-v2.md --out-tsv /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11-v2.tsv`; paste the printed `P@33 R@33 P@20 R@20` beside #100075's receipt values (P7) with `git -C /Users/marklehn/Developer/bellows rev-parse --short main` beside #100075's HEAD — the count is the same edge set, so the four numbers move only if bellows `main` moved (state both figures, never a silent accept); then the four rows the thread named, read from the NEW doc and pasted beside P7's: every caller label is either `file::name` or `file (module)` (`grep -c '::[a-zA-Z_./-]*\.py[,)]' <v2.md>` → 0 — no `file::file` form survives), and no row shows a count above 0 with an empty list (`grep -cE '\| [1-9][0-9]* \(\)' <v2.md>` → 0); the two greps' outputs pasted.
> **Item 3 — production writes, stated exactly:** none outside the two bellows evidence files and the two `-v2` governance research files (`git -C /Users/marklehn/Developer/eluvian-governance status --porcelain -- governance/knowledge/research/` lists the two `-v2` files; any OTHER entry it lists is named in the receipt as pre-existing, never touched — the Planner may hold an uncommitted register there mid-draft); `$T` removed; `git -C "$A" status --porcelain` still exactly the three declared paths, `log -1` still `fc32f63`; the first entries doc byte-identical (`git -C /Users/marklehn/Developer/eluvian-governance diff --quiet -- governance/knowledge/research/what-works-entries-bellows-2026-09-11.md`); no lane file, no live lifecycle row, no daemon.
> **Item 4 — receipt** `knowledge/qa/evidence/what-works-callers-qa-receipt-2026-09-11.md`: `numstat` over the DEV's two bellows commits AND `git -C /Users/marklehn/Developer/anvil diff --numstat` for the anvil tree (the three paths); a `## Verification` table with one row per Item 1–3, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`$ELUVIAN_WRAP_ROOT/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: what-works-callers-2026-09-11`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["what-works-callers-suite-2026-09-11.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the closing `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 5 — commit**, path-scoped and gated: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/what-works-callers-qa-receipt-2026-09-11.md knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt && git commit -F <msg-file> -- knowledge/qa/evidence/what-works-callers-qa-receipt-2026-09-11.md knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt`. ⛔ A QA step that must change production code STOPS and pauses for a verdict (PT v4.108 §8).
>
> **Deposits:**
> - `knowledge/qa/evidence/what-works-callers-qa-receipt-2026-09-11.md`
> - `knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11-v2.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11-v2.tsv`
>
> **Scope:**
> - `knowledge/qa/evidence/what-works-callers-qa-receipt-2026-09-11.md`
> - `knowledge/qa/evidence/what-works-callers-suite-2026-09-11.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed` with N ≥ the DEV's recorded baseline plus four (278 at drafting; the dev-log's recorded figure supersedes it) and no `failed`; Item 2's four numbers pasted beside #100075's with the HEAD comparison, the four named rows pasted from the new doc beside P7's, the two greps' zeros; the receipt's `## Verification` table has three rows and closes with the block's own PASSED line; one QA commit carrying the two evidence files; the anvil tree unchanged since the DEV (three paths, `fc32f63`); the first entries doc byte-identical; `GLOSSARY.md` untouched.
