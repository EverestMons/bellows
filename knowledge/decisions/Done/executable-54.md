# Bellows — Ledger Extraction Fix (multi-turn) + in_progress-Strand Recovery (#48)
**Date:** 2026-06-14 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 53. CEO fork verdicts (processed-verdict-53-step-1): (A) runner-side concatenation; ONE executable combining the ledger fix + the #48 recovery fix; plan 48 self-heals at the post-fix restart; re-canary after. ROOT CAUSE (diagnostic 53, authoritative — `knowledge/research/ledger-apply-regression-rootcause-2026-06-14.md`): `parser.parse()` reads `### Ledger Updates` only from the terminal stream-json `result` event's final message (parser.py:10 `result_text = raw.get("result","")`); multi-turn agents emit the Output Receipt in an INTERMEDIATE assistant turn, so it is invisible and the ledger comes back empty → silent skip (no WARN). Not a Slice-2 regression; Slice 2 just happened to run multi-turn agents. Two fixes here:
1. **Ledger extraction (shape A):** in `runner.py`, after assembling the stream, concatenate the text of ALL assistant-type events and make that available to the parser (so the `### Ledger Updates` block is found regardless of which turn it lands in) — keep `parser.py`'s contract clean. Plus defense-in-depth: emit a WARN if the raw output contains `### Ledger Updates` but the parsed `ledger_updates` is all-None (silent drops become loud).
2. **#48 recovery:** extend `recover_half_claimed` (lifecycle.py:~237/243, currently `WHERE lifecycle_state='claimed'`) to ALSO recover restart-stranded `in_progress` plans — discriminated by worktree-absence (a live step has its worktree; a stranded one does not) plus the plan-22 age guard — marking them `abandoned` + `closed_at`. This self-heals plan 48 at the next startup.
Anchors (re-verify by grep): runner.py result extraction ~215–226; parser.py:10; bellows.py `ledger = parsed.get("ledger_updates") or {}` ~1132; lifecycle.py `recover_half_claimed` (190) + its `claimed` query (~237/243). DAEMON RESTART REQUIRED after close — the restart both activates the fix AND triggers the recovery that cleans plan 48; the re-canary (a fresh PROJECT_STATUS+feedback canary) is the live proof. Slice 3 (FORWARD) stays blocked until this fix's re-canary passes.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, the diagnostic root-cause IN FULL, and the existing `runner.py` result-extraction, `parser.py:parse`, the bellows.py ledger read, and `recover_half_claimed`. **Scope is exactly these files: `runner.py`, `parser.py`, `bellows.py`, `lifecycle.py`, `tests/test_runner.py`, `tests/test_parser.py`, `tests/test_bellows.py`, `tests/test_lifecycle.py` (create any that do not exist).**
> - **(1) Multi-turn ledger extraction (shape A):** in `runner.py`, while/after iterating the NDJSON stream to find the `type:"result"` event, also collect the text content of every `type:"assistant"` event; concatenate it; and ensure the parser sees it for `### Ledger Updates` extraction — e.g. set/append a synthetic field on the result event (or pass the concatenated text into the parse path) so `parser.parse()` finds the Output Receipt regardless of which turn carried it. Do NOT break the existing `result`/`ceo_flags`/`verdict_requested` extraction (those read the final message and must keep working). If cleaner, add a small parser entry that accepts the concatenated text — but keep the change minimal and the existing parser contract intact.
> - **(2) Defense-in-depth WARN:** where the ledger is read (bellows.py ~1132) or in `_apply_ledger_updates`, if the agent's raw output contains the literal `### Ledger Updates` but the parsed `ledger_updates` is empty/all-None, emit a `WARN` (e.g. `⚠ ledger: agent emitted ### Ledger Updates but parser extracted nothing`). This converts future silent drops into loud signals.
> - **(3) in_progress-strand recovery:** extend `recover_half_claimed` to also select `lifecycle_state='in_progress'` plans and recover the STRANDED ones — a plan is stranded if its worktree does not exist on disk (a legitimately-running step has its worktree); apply the plan-22 age guard so a just-claimed in-flight plan is never touched. Mark stranded plans `abandoned` + `closed_at` (same write as the existing claimed-path). MUST NOT abandon a plan whose worktree exists or that is within the age guard. (Optional cosmetic: you may note but need NOT rename the function.)
>
> **Tests (name them — full coverage):** (a) `test_runner.py`: a multi-turn stream where `### Ledger Updates` appears in an intermediate assistant event is extracted (the regression repro); the single-turn/final-message case still works. (b) `test_bellows.py`: the defense WARN fires when raw has the heading but parsed ledger is empty; does NOT fire when ledger is populated. (c) `test_lifecycle.py`: `recover_half_claimed` recovers a stranded in_progress plan (no worktree, past age guard) → abandoned+closed_at; does NOT touch an in_progress plan WITH a worktree; does NOT touch one within the age guard; existing claimed-recovery still works. Then run the FULL suite (`python3 -m pytest tests/`) to explicit pass/fail and READ THE TAIL. Write the dev log to `bellows/knowledge/development/ledger-extraction-recovery-fix-dev-log-2026-06-14.md`. Use `with open()`; no heredocs. Standard prompt feedback → emit via `### Ledger Updates > #### Prompt Feedback` (will be dropped on this still-broken daemon — that's expected; the fix activates on restart). **Deposits:**
> - `bellows/knowledge/development/ledger-extraction-recovery-fix-dev-log-2026-06-14.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, and the diagnostic. **Verify, each with executed evidence (files into `knowledge/qa/evidence/ledger-extraction-recovery-fix-2026-06-14/`):** (1) **Full suite** — final 15 lines, zero failures, new-test count matches dev log; `full_suite_tail.txt`. (2) **Multi-turn extraction (the regression repro)** — the runner test proves `### Ledger Updates` in an intermediate assistant turn is now extracted; reproduce against plan-52's actual step JSON if feasible (show parsed ledger_updates is now populated); `multiturn_extraction.txt`. (3) **Defense WARN** — fires on heading-present-but-empty-parse, silent otherwise; `defense_warn.txt`. (4) **in_progress recovery** — stranded (no worktree) recovered to abandoned+closed_at; worktree-present NOT touched; age-guarded NOT touched; claimed-path regression intact; `recovery.txt`. (5) **No regression to existing extraction** — ceo_flags / verdict_requested / result still parse from the final message; `existing_extraction.txt`. (6) **Scope** — `git diff HEAD~1 --stat` only in-scope files; `scope.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `ledger-extraction-recovery-fix-2026-06-14`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/ledger-extraction-recovery-fix-qa-report-2026-06-14.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/ledger-extraction-recovery-fix-2026-06-14/`; `required_evidence_files`: `[full_suite_tail.txt, multiturn_extraction.txt, defense_warn.txt, recovery.txt, existing_extraction.txt, scope.txt]`. Include the literal stdout of the block. If it prints `FAILED`, halt and report to CEO. Write the QA report (verification table + Rule 20 banner) to `bellows/knowledge/qa/ledger-extraction-recovery-fix-qa-report-2026-06-14.md`. **Receipt Flags for CEO MUST include:** (1) DAEMON RESTART REQUIRED — do NOT restart until this plan fully closes; the restart activates the fix AND triggers recovery that cleans plan 48 (verify post-restart: plan 48 no longer in the `closed_at IS NULL` set); (2) RE-CANARY — dispatch a fresh PROJECT_STATUS+feedback canary after restart; it must now land both ledgers; (3) Slice 3 (FORWARD) unblocks only after the re-canary passes. Use `with open()`; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/ledger-extraction-recovery-fix-qa-report-2026-06-14.md`
> - `bellows/knowledge/qa/evidence/ledger-extraction-recovery-fix-2026-06-14/` (six evidence files per Rule 20 self-check)
