# Governance — PLANNER_TEMPLATE Rule 26 (Deposits Field Convention)
**Date:** 2026-04-18 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1 (Documentation Analyst)

## CEO Context

Test Scope: targeted — single-file governance edit to PLANNER_TEMPLATE.md. No code, no tests, no cross-file impact. Adds new Rule 26 between Rule 25 and the `### Diagnostic Prompt Engineering` subsection. Also bumps version 4.24 → 4.25. Governance-first pairing: this rule establishes the `**Deposits:**` declaration convention; a future gates.py plan will extend the Bellows deposit parser to recognize the plural-list form and scope the `deposit_exists` gate to declared deposits only. During the parser-gap window (between Rule 26 shipping and the gates.py plan landing), Rule 26-compliant plans will see `Deposit: none` in verdict requests even when they declare multiple deposits — Rule 26 documents this explicitly and specifies the Planner's resolution behavior (read the plan's `**Deposits:**` block directly).

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1. Single-step plan — no intermediate stop.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/executable-planner-template-rule-26-2026-04-18.md. Execute Step 1.
```

---
---

## STEP 1 — GOVERNANCE DOCUMENTATION ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-planner-template-rule-26-2026-04-18.md", "bellows/knowledge/decisions/in-progress-executable-planner-template-rule-26-2026-04-18.md")`. **Skip specialist file and glossary reads — this is a governance documentation edit with specified exact old-string and new-string.** You are editing the single governance file at `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`. This plan adds a new Rule 26 (Deposits Field Convention) between Rule 25 and the `### Diagnostic Prompt Engineering` subsection. Use `Desktop Commander:edit_block` exclusively — do NOT use `write_file rewrite` mode (that tool has clobbered PLANNER_TEMPLATE.md in prior sessions). **Step A — Version bump.** Use `edit_block` to change the version line. `old_string`: `**Version:** 4.24` — `new_string`: `**Version:** 4.25`. **Step B — Last-Updated bump.** Use `edit_block` to change the date. `old_string`: `**Last Updated:** 2026-04-18 (v4.24)` — `new_string`: `**Last Updated:** 2026-04-18 (v4.25)`. **Step C — Insert Rule 26.** Use `edit_block` with this exact `old_string` (verbatim — the last paragraph of Rule 25 through the start of the `### Diagnostic Prompt Engineering` subsection header): `**Operational note — this rule is not blocked by BACKLOG #4 or #5:** The Bellows-side reliability items (filesystem watcher reliability, step state across re-claim) operate on the Bellows dispatch side. Rule 25 operates on the Planner's read side — it scans files Bellows has already written. The two are structurally independent. Rule 25 can be adopted immediately and will continue to work correctly as BACKLOG #4 and #5 are resolved.\n\n### Diagnostic Prompt Engineering` — and this exact `new_string`: `**Operational note — this rule is not blocked by BACKLOG #4 or #5:** The Bellows-side reliability items (filesystem watcher reliability, step state across re-claim) operate on the Bellows dispatch side. Rule 25 operates on the Planner's read side — it scans files Bellows has already written. The two are structurally independent. Rule 25 can be adopted immediately and will continue to work correctly as BACKLOG #4 and #5 are resolved.\n\n### 26. Deposits field convention\n\nEvery step in every executable and diagnostic plan MUST declare its deposits via a `**Deposits:**` field. The field lists every file the step will create or write to — deposit files, dev logs, QA reports, evidence files, specialist syncs, any file the step is responsible for producing. The list is the canonical enumeration; anything not on it is not a deposit of that step.\n\n**Why this rule exists:** Prior plans referenced deposits inside the prose of each step ("deposit findings to `path/to/file.md`", "write the QA report to `path/to/report.md`"), which forced Bellows's `deposit_exists` gate to keyword-scan the entire step text for path-like strings. That scan has tripped repeatedly on non-deposit paths — specialist-file content embedded inside plans, test-fixture strings embedded inside QA smoke tests, documentation examples referencing paths they don't create. Every false positive stranded a plan in `verdict-pending-*` state and required manual CEO bootstrap to resume. BACKLOG #6 enumerates the failure class. Rule 26 establishes a declarative field so future plans have a single unambiguous source of truth for deposits; a subsequent Bellows-side plan will extend the deposit parser to read the declared field and ignore prose-embedded paths.\n\n**Field format and placement:** The field lives inside the step prompt, near the end, separated from surrounding prose by the bold marker `**Deposits:**` followed by a bulleted list. The format is:\n\n```\n**Deposits:**\n- `path/to/first/deposit.md`\n- `path/to/second/deposit.md`\n```\n\nEach bullet holds exactly one path wrapped in backticks. Paths are project-relative (e.g., `bellows/knowledge/qa/my-report.md`) unless the deposit target is an absolute governance-root path (e.g., `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md`) — the absolute-path case is rare and reserved for governance edits. The list is complete: every file the step writes to MUST appear. Evidence files (Rule 18) do NOT need individual bullets — list the evidence directory as a single bullet (e.g., `- `bellows/knowledge/qa/evidence/<plan-slug>/` (multiple files per Rule 20 self-check)`), because the Rule 20 self-check already enumerates the individual evidence filenames.\n\n**The "no deposits" case:** If a step makes no deposits at all — pure diagnostic investigation reported in conversation, pure housekeeping with no file writes, etc. — the field is still present, with a single bullet `- none`. Omission of the field is NOT permitted. Explicit `- none` declaration distinguishes "this step intentionally produces no files" from "the author forgot to list the deposits." Bellows's future deposit parser will treat a missing `**Deposits:**` field as a plan authoring error, not a no-deposit step.\n\n**Relationship to Bellows's current deposit parser:** The `extract_primary_deposit()` function currently reads `**Deposit:**` (singular, bold-noun-colon form, cluster A of the 2026-04-18 deposit-path format survey) and does NOT recognize the plural-list form Rule 26 specifies. This is a known parser gap that will be closed by a future gates.py plan. During the gap window, verdict request files written by Bellows for Rule 26-compliant plans will show `Deposit: none` (because the singular-form parser finds nothing to match) even when the plan declares three deposits. This is the expected state during the transition.\n\n**Planner behavior during the parser-gap window:** When Rule 25 surfaces a verdict with `Deposit: none` for a session-plan the Planner knows was written under Rule 26, the Planner does NOT escalate to the CEO as the generic "none" case does. Instead, the Planner reads the plan file directly (via `Filesystem:read_text_file`), extracts the `**Deposits:**` block for the relevant step, and applies Rule 22's (a)–(e) checks to each path in that block. The plan's declared `**Deposits:**` block is canonical; the verdict request's `Deposit` field is advisory during this window. This Planner-side resolution absorbs the parser gap without triggering CEO noise for every Rule 26 plan. Once the gates.py plan ships, the verdict request's `Deposit` field will reflect the declared `**Deposits:**` content directly and this resolution path falls away.\n\n**Retroactive application:** This rule applies to NEW plans written after v4.25 lands. Existing plans (in-progress or in Done/) do NOT require retroactive amendment — legacy plans continue to work under the existing keyword-scan gate (with its known false-positive rate). The Planner does not go back and edit old plans to add the field.\n\n**Interaction with Rule 21 (test scope) and Rule 18 (evidence files):** Rule 26 complements these rules, doesn't replace them. A QA step still declares its test scope in the plan header (Rule 21) and still pipes command output to evidence files (Rule 18). The `**Deposits:**` field lists the QA report file and the evidence directory; it does NOT list each individual evidence filename (Rule 20's self-check already does that). The three rules compose: Rule 21 decides what to test, Rule 18 requires output be captured in evidence files, Rule 20 mechanically verifies those files exist, and Rule 26 declares the overall deposits shape.\n\n**Migration scope:** The governance edit is Rule 26's only scope. A separate future plan will (a) extend the deposit parser in `verdict.py` (or `bellows.py`, wherever `extract_primary_deposit` lives — confirmed today to live in `verdict.py` per Plan B's circular-import avoidance) to recognize the `**Deposits:**` block as a list, (b) change the `deposit_exists` gate in `gates.py` to scope its scan to the declared block only when the plan has one, (c) preserve the legacy keyword-scan fallback for plans without a declared block. That plan is out of scope for Rule 26. Rule 26 ships the governance convention alone so authorship compliance can be observed before Bellows-side enforcement is added.\n\n### Diagnostic Prompt Engineering`. Note carefully: the markdown code fence syntax triple-backtick inside `new_string` must be preserved literally; all `\n` sequences must be preserved as literal newlines; the backtick characters inside the example block must survive verbatim. Do NOT break this into multiple edit_block calls — a single call with the full new_string avoids intermediate states. **Step D — Add Lessons Learned entry.** Use `edit_block` to add a new row after the most recent entry (Rule 25 entry from v4.24). `old_string` is the entire Rule 25 Lessons Learned row starting with `| 2026-04-18 | Rule 25 (v4.24) codifies Planner verdict-polling discipline.` and ending with `Implementable immediately. |` — read the current Lessons Learned section with `Filesystem:read_text_file` first to get the exact verbatim row text. `new_string` is that same row followed by a new row: `\n| 2026-04-18 | Rule 26 (v4.25) codifies the `**Deposits:**` field convention. Every step in every plan now declares its deposits as a bulleted list rather than burying paths in prose. Solves BACKLOG #6's class of false positives structurally — Bellows's future deposit parser can scope to the declared block instead of keyword-scanning the whole plan text. Plural-list form `**Deposits:**` commits to the final form now; the existing `extract_primary_deposit` parser in verdict.py reads the singular `**Deposit:**` form, so a parser gap exists until a future gates.py plan lands. During the gap, Rule 26-compliant plans show `Deposit: none` in verdict requests; Rule 25's Planner resolves these by reading the plan's declared `**Deposits:**` block directly instead of escalating. Governance-first ship pattern — observe authorship compliance before shipping Bellows-side enforcement. |`. **Step E — Verify the edit.** Use `Filesystem:read_text_file` with `head: 10` on PLANNER_TEMPLATE.md to confirm the version line reads `**Version:** 4.25` and `**Last Updated:** 2026-04-18 (v4.25)`. Then use `Filesystem:read_text_file` on the full file, grep within your output for the string `### 26. Deposits field convention` — it must appear exactly once. Also grep for `### Diagnostic Prompt Engineering` — it must appear exactly once, and MUST appear AFTER the Rule 26 header in file order. Also confirm the string `**Deposits:**` appears inside the inserted Rule 26 body (the convention itself references the field name). Also confirm the "no deposits" single-bullet `- none` case is described. **Step F — Commit.** `cd /Users/marklehn/Desktop/GitHub && git add PLANNER_TEMPLATE.md && git --no-pager log -1 --format=%H && git commit -m "docs(planner): Rule 26 — Deposits field convention (v4.25)"`. Capture the new commit hash via `git --no-pager log -1 --format=%H` and record it in the dev log. **Step G — Deposit dev log.** Write a dev log to `bellows/knowledge/development/planner-template-rule-26-2026-04-18.md` using the canonical Python file write pattern (`with open(path, "w") as f: f.write(content)` — NO heredoc). Dev log includes: one-line summary per Step A–F, the commit hash, a verbatim copy of the Rule 26 block as inserted (audit reference if the governance file is corrupted), a note that Rule 20 self-check does not apply (this is a single-step Documentation Agent governance edit, not a QA step), and an Output Receipt per template format. **Skip Rule 20 self-check — Rule 20 applies to QA steps in executable plans, not to single-step Documentation Agent governance edits.** There is no QA step in this plan because the deliverable is a governance text change with a specified exact edit; Step E is the verification. **Step H — Move to Done.** `import shutil; shutil.move("bellows/knowledge/decisions/in-progress-executable-planner-template-rule-26-2026-04-18.md", "bellows/knowledge/decisions/Done/executable-planner-template-rule-26-2026-04-18.md")`. This is the absolute last operation per Rule 23c. **Deposits:**\n- `/Users/marklehn/Desktop/GitHub/PLANNER_TEMPLATE.md` (edit in place — version bump, Rule 26 insert, Lessons Learned row)\n- `bellows/knowledge/development/planner-template-rule-26-2026-04-18.md` (dev log)\n- `bellows/knowledge/research/agent-prompt-feedback.md` (feedback append) **Feedback:** standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
