# Bellows — Diagnostic Phase 2: Read the Dispatch Path
**Date:** 2026-04-15 | **Type:** Read-only investigation | **Source:** Phase 1 findings narrowed cause to commits f86f1a2 (Phase 2A threading) and c16e1d3 (Phase 2B parallel groups), plus a separate "all runs Unknown status" bug | **Single-step:** Yes (diagnostic-prefixed) | **Run mode:** Manual bootstrap in Claude Code (Bellows remains paused)

## Context

Phase 1 (`bellows-strand-timeline-2026-04-15.md`) established that the strand pattern started yesterday morning around the time Phase 2A (f86f1a2) and Phase 2B (c16e1d3) introduced threading and parallel groups to bellows.py. Six subsequent bug-fix commits patched specific symptoms but did not resolve the root cause. Additionally, ALL Bellows runs record status "Unknown" rather than "Complete," indicating a separate (possibly related) bug in the status-recording code path.

This Phase 2 diagnostic reads the relevant source code and the suspect commits to reconstruct what changed in the dispatch loop and where the race condition or completion-signaling bug lives. Phase 2 is read-only. Phase 3 will reproduce the failure with a targeted test. Phase 4 will fix.

**Bellows remains paused.** Run this plan via manual Claude Code bootstrap, not through Bellows dispatch.

## How to Run This Plan (manual bootstrap)

In a Claude Code session, paste:

```
Read the plan at /Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-dispatch-path-2026-04-15.md and execute Step 1. Claim the plan first by renaming it to in-progress-, then complete Step 1 and move it to Done.
```

---
---

## STEP 1 — Investigation Agent

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-dispatch-path-2026-04-15.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-dispatch-path-2026-04-15.md")`. Skip specialist file and glossary reads — this is a code-archaeology investigation. Working directory is `/Users/marklehn/Desktop/GitHub/bellows`. **READ-ONLY: do not modify any source file, configuration file, or database. Do not start, stop, or interact with any running Bellows process.** Investigation has seven numbered questions (Q1–Q7). Answer each in the deposited findings file under its own section header. **Q1 — Read bellows.py end-to-end.** Read the entire current bellows.py. Report the file's overall structure: what classes, functions, and module-level state exist; how they connect. Identify the dispatch loop entry point (likely a `while True` or a watcher callback) and the path from plan-detection to step-dispatch to completion-recording. The Planner needs a mental model of the current dispatch path to reason about where stranding and status-recording happen. **Q2 — Trace the threading model introduced by f86f1a2.** Run `git --no-pager show f86f1a2 -- bellows.py` and report the literal diff. Then describe in prose: what threads are created, what they do, what shared state they touch, and whether any locking exists. Specifically: is there a thread per plan? A thread per step? A worker pool? When a thread completes, how does the main loop know? Identify any shared mutable state (`_seen` set, dict of running plans, etc.) that multiple threads might write to. **Q3 — Trace the parallel-group code introduced by c16e1d3.** Run `git --no-pager show c16e1d3 -- bellows.py` and report the literal diff. Then describe in prose: how parallel-group plans are detected, how their concurrent execution is coordinated, and whether there's any synchronization between them (especially around the move-to-Done step). Look for: do parallel plans share a thread or get separate threads? Do they share `_seen`? Does completion of one parallel plan affect detection of others? **Q4 — Read the six bug-fix commits as a group.** For each of c7340a4, be72e9e, d3daeec, 5ac6ae2, 1cd47ab, fd75e76: run `git --no-pager show <SHA> -- bellows.py` and summarize what each one tried to fix in 1-2 sentences. Then write a one-paragraph synthesis: what failure modes were these patches addressing, and what does the pattern of patches reveal about the root cause? Specifically, do these patches all touch the same code region (suggesting a single root cause) or different regions (suggesting multiple independent bugs)? **Q5 — Find the status-recording code path.** Search bellows.py for where `runs.status` is written to bellows.db. The Phase 1 finding was that ALL runs record status "Unknown" — find the code that should be writing "Complete" or other status values and explain why it's not running (or is running with the wrong value). Check: is there a status-update call that's commented out, behind a condition that's never true, or never reached because the dispatch path returns early? Quote the relevant code lines verbatim with line numbers. **Q6 — Identify the strand-detection logic.** Find the code that decides "agent reported step complete but plan file not in Done/" and emits the strand message. Quote the lines verbatim with line numbers. Then trace: what triggers this check? Is it polled, event-driven, or invoked at a specific point in the dispatch loop? Does it run on every step or only on the final step? Is its timing affected by the threading model? This question matters because the strand message is correctly catching agent failures — Phase 4 should preserve this behavior even when fixing the underlying bug. **Q7 — Best-guess root cause hypothesis.** Based on Q1-Q6, write a one-paragraph hypothesis identifying the most likely root cause of the stranding (and, separately, the most likely cause of the "all Unknown status" bug — they may share a root cause or may be independent). Rank the hypotheses if multiple are plausible. Do NOT propose a fix yet — Phase 3 reproduces the failure, Phase 4 fixes. Just state the most likely cause(s) with reasoning. **Step A — Findings deposit.** Write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/bellows-dispatch-path-2026-04-15.md`. Use sections Q1–Q7. Include a standard Output Receipt with Status=Complete, Files Deposited, Files Created or Modified (Code) = `[]` (read-only), Decisions Made = the Q7 root cause hypothesis, Flags for CEO = the recommended Phase 3 reproduction approach. **Step B — Feedback append.** Append a feedback entry to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/agent-prompt-feedback.md` per the standard prompt feedback protocol. **Step C — Commit.** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/bellows-dispatch-path-2026-04-15.md knowledge/research/agent-prompt-feedback.md && git commit -m "docs: phase 2 diagnostic — bellows dispatch path"`. **Step D — Move-to-Done as the absolute last operation:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-dispatch-path-2026-04-15.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/Done/diagnostic-bellows-dispatch-path-2026-04-15.md")`. **Steps A → B → C → D in strict order. The move-to-Done must be the last thing done.**
