# QA Verification — Diagnostic 6 Fix (resume-path plan_id recovery + turns passthrough) + Live Resume Canary
**Date:** 2026-06-12 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (QA) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 1,2

**Execution Map:** Step 1 (QA) → Step 2 (QA) — strictly sequential, no parallel steps.

## Context (Rule 27)
This plan implements diagnostic 6 verification. The diagnostic-6 fix (G1 resume-path plan_id recovery, G2 parser num_turns extraction, G3 record_step_end turns passthrough, G4 [id] commit tag restored transitively) was committed MANUALLY on 2026-06-12 as `2df6d91` — contrary to the everything-through-Bellows rule — so no lifecycle rows or derivations lineage exist for that execution. This Bellows-dispatched plan closes that gap three ways at once: (1) Step 1 independently verifies the fix is present and the suite is green on main; (2) the plan's own claim records the derivations lineage to diagnostic 6 via this paragraph's citation; (3) the `pause_for_verdict: always` pause between Step 1 and Step 2 makes Step 2's dispatch travel the post-verdict RESUME path — the exact path the fix repairs — so Step 2 is the live canary, verifying from inside the run that the resumed dispatch wrote its lifecycle rows. The daemon was restarted at 2026-06-12 11:30 CDT and reports `bellows.py @ 2df6d91`, so the fixed code is what dispatches this plan. The lifecycle DB is at `/Users/marklehn/Developer/GitHub/bellows/lifecycle.db` (main repo root, untracked); the daemon is its SOLE writer — all queries in this plan are strictly read-only via the `?mode=ro` URI form. Reference diagnostic: `knowledge/research/steps-table-coverage-forensics-2026-06-12.md`.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows QA

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, then the diagnostic `knowledge/research/steps-table-coverage-forensics-2026-06-12.md` (Section 7 Gap Assessment G1–G4), then `git show 2df6d91 --stat` for the fix commit's file list. **This step verifies the fix is present and green on main; it changes NO code.** **Allowed files (scope — exactly these, no others):** the QA report and evidence files listed in Deposits below. If any code file appears to need modification, halt and report.
>
> **Verify, each with executed evidence (commands + output to evidence files in `knowledge/qa/evidence/diagnostic-6-fix-qa-step-1-2026-06-12/`):** (1) **G1 helper + callsite** — `grep -n "def recover_plan_id_from_filename" bellows.py` returns exactly one definition, and `grep -n "plan_id = recover_plan_id_from_filename(plan_filename)" bellows.py` returns exactly one callsite; output both to `g1_greps.txt`. (2) **G2 parser extraction** — `grep -n "num_turns" parser.py` shows `turns = raw.get("num_turns")` and `grep -n "turns" parser.py` shows a `"turns": turns` return-dict entry; output to `g2_greps.txt`. (3) **G3 callsites** — `grep -n "turns=parsed.get" bellows.py` returns exactly two hits, both on `record_step_end` call lines; output to `g3_greps.txt`. (4) **G4 transitive** — `grep -n "_id_tag_instruction = " bellows.py` shows the `if plan_id else ""` form, unchanged (G4 is fixed transitively by G1 recovering plan_id on resume); output to `g4_grep.txt`. (5) **Unit coverage** — `python3 -m pytest tests/test_diagnostic_6_coverage.py -v`: all collected tests pass; output to `unit_tests.txt`. (6) **Full suite** — `python3 -m pytest tests/` capturing the final 15 lines; the summary line must report zero failures and zero errors; output to `full_suite_tail.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root (`/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md`). Use these values when filling in the template: `plan_slug`: `diagnostic-6-fix-qa-2026-06-12`; `qa_report_path`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/diagnostic-6-fix-qa-report-step-1-2026-06-12.md`; `evidence_dir`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/diagnostic-6-fix-qa-step-1-2026-06-12/`; `required_evidence_files`: `[g1_greps.txt, g2_greps.txt, g3_greps.txt, g4_grep.txt, unit_tests.txt, full_suite_tail.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table (item | method | evidence file | PASS/FAIL) and the Rule 20 self-check banner block to `bellows/knowledge/qa/diagnostic-6-fix-qa-report-step-1-2026-06-12.md`. Use `with open()` for deposits; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/diagnostic-6-fix-qa-report-step-1-2026-06-12.md`
> - `bellows/knowledge/qa/evidence/diagnostic-6-fix-qa-step-1-2026-06-12/` (six evidence files per Rule 20 self-check)

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`. **Context: you are Step 2 of a 2-step plan that paused for a CEO verdict after Step 1 — your dispatch traveled the post-verdict RESUME path whose lifecycle writes the diagnostic-6 fix (commit `2df6d91`) restored. This step verifies, live and from inside the resumed run, that those writes happened.** Derive this plan's integer id `<id>` first: the plan file your bootstrap prompt told you to read is named `in-progress-executable-<id>.md` — parse the integer from that filename; your bootstrap commit-tag instruction `[<id>]` carries the same value. Record the derived id in your QA report. The lifecycle DB is at `/Users/marklehn/Developer/GitHub/bellows/lifecycle.db` (main repo root — NOT inside your worktree). The daemon is its SOLE writer: every query MUST be read-only via `sqlite3 "file:/Users/marklehn/Developer/GitHub/bellows/lifecycle.db?mode=ro" "<SQL>"`. **Allowed files (scope — exactly these, no others):** the QA report and evidence files listed in Deposits below.
>
> **Verify, each with executed evidence (commands + output to evidence files in `knowledge/qa/evidence/diagnostic-6-fix-qa-step-2-2026-06-12/`):** (1) **Plans row** — `SELECT id, type, lifecycle_state, total_steps FROM plans WHERE id = <id>` returns one row with type='executable' and total_steps=2; output to `db_plan_row.txt`. (2) **Derivations lineage** — `SELECT executable_id, diagnostic_id FROM derivations WHERE executable_id = <id>` returns one row with diagnostic_id=6 (recorded at claim from this plan's literal citation); output to `db_derivations.txt`. (3) **Step-1 end data (G2/G3 live)** — `SELECT step_number, status, cost_usd, turns, duration_s FROM steps WHERE plan_id = <id> AND step_number = 1` returns one row with status='complete', turns a positive integer (NOT NULL), and cost_usd NOT NULL; output to `db_step1_row.txt`. (4) **Step-2 row exists (G1 live — the headline check)** — `SELECT step_number, status, step_started_at FROM steps WHERE plan_id = <id> AND step_number = 2` returns exactly one row with status='running'. This is YOUR OWN in-flight row: before the fix it could not exist, because the resume dispatch left plan_id None and skipped the insert; its presence proves G1 end-to-end. Output to `db_step2_row.txt`. (5) **[id] commit tag (G4 live)** — `git log --oneline -25` in your worktree includes Step 1's deposit commit(s) carrying the literal `[<id>]` tag; grep for it; output to `id_tag_gitlog.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `diagnostic-6-fix-qa-2026-06-12`; `qa_report_path`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/diagnostic-6-fix-qa-report-step-2-2026-06-12.md`; `evidence_dir`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/diagnostic-6-fix-qa-step-2-2026-06-12/`; `required_evidence_files`: `[db_plan_row.txt, db_derivations.txt, db_step1_row.txt, db_step2_row.txt, id_tag_gitlog.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table (item | method | evidence file | PASS/FAIL) and the Rule 20 self-check banner block to `bellows/knowledge/qa/diagnostic-6-fix-qa-report-step-2-2026-06-12.md`. In a prose paragraph AFTER the table (not in any table row), note for the CEO: this step's own row (step_number=2) gets its end-of-step data (status='complete', turns, cost_usd) written by the daemon only after this step finishes, so it cannot be checked from inside this run — the post-close CEO query is `SELECT * FROM steps WHERE plan_id = <id>`, which should then show two complete rows with turns populated on both. Use `with open()` for deposits; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/diagnostic-6-fix-qa-report-step-2-2026-06-12.md`
> - `bellows/knowledge/qa/evidence/diagnostic-6-fix-qa-step-2-2026-06-12/` (five evidence files per Rule 20 self-check)
