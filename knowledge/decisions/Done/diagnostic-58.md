# Bellows — FORWARD Ledger Extraction Failure Root Cause (plan 57 canary)
**Date:** 2026-06-14 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## Context (Rule 27)
The FORWARD activation canary (plan 57, diagnostic) failed to land: the agent emitted a `#### Forward Register` channel item (CANARY-FORWARD-160138) but the daemon appended NO row to FORWARD.md, logged NO `ledger: appended new row` line, AND the defense WARN did NOT fire. Feedback (plan 50/55) and PROJECT_STATUS (plan 55) canaries BOTH worked on the same daemon — so the multi-turn extraction fix (plan 54) and the core apply path are sound; FORWARD extraction specifically failed. Preliminary observations (VERIFY, do not assume): (1) plan-57's raw output (logs/20260614-160153-step.json) contains 11 occurrences of `#### Forward Register`, MOST of them echoes of the canary plan's own prompt instructions (e.g. the literal text "include a `#### Forward Register` subsection containing exactly this item line:"); (2) the CANARY-FORWARD string at the first occurrence is INSIDE a backtick-quoted instruction echo, not a clean newline-delimited subsection; (3) a manual `#### (?:Forward Register|FORWARD(?: Additions)?)\s*\n(.*?)` search returned no match. LEADING HYPOTHESIS to confirm or kill: the parser's `### Ledger Updates` SECTION match (parser.py:54, `re.search`) takes the FIRST occurrence — in the multi-turn-concatenated output (plan-54 fix concatenates ALL assistant turns), the first `### Ledger Updates` may be a PROMPT-ECHO region (the agent restating instructions in an early turn) that lacks a clean `#### Forward Register` body, so the subsection search runs on the wrong region. Investigation ONLY — author no fix. All access read-only.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_SYSTEMS_ANALYST.md` first, then `parser.py` (the `### Ledger Updates` section match at ~L54 and the three subsection regexes ~L59–81), `runner.py` (the multi-turn concatenation from plan 54), and the defense-WARN code (plan 54). **Post a 1-line "Read X." / "Drafting Section N." cadence.** Ground every claim at file:line or a reproduced parse.
>
> **Section 1 — Did the agent emit a clean block?** In plan-57's step JSON raw output, locate EVERY `### Ledger Updates` occurrence and EVERY `#### Forward Register` occurrence; classify each as (a) prompt-echo / instruction restatement, or (b) a genuine Output-Receipt subsection (heading on its own line followed by the canary item as body). Determine definitively whether the agent produced AT LEAST ONE clean, parseable `### Ledger Updates` → `#### Forward Register\n<item>` block. State the answer with the exact surrounding text.
>
> **Section 2 — Reproduce the daemon's parse.** Reproduce what the daemon actually computed for `parsed["ledger_updates"]["forward"]` on plan-57's output, using the SAME path the daemon uses (the runner multi-turn concatenation feeding `parser.parse`). Is `forward` None or populated? Then test the HYPOTHESIS: does the `### Ledger Updates` section `re.search` (parser.py:54) match a prompt-echo region first? Show what `lu_body` it captured and whether a clean `#### Forward Register` body exists OUTSIDE that captured region. Compare against a WORKING case (plan 55's output, which had feedback+project_status) — what made plan 55's section-match land on the real receipt while plan 57's did not (e.g. count/position of `### Ledger Updates` echoes, ordering of subsections, single-vs-multi-subsection)?
>
> **Section 3 — Why didn't the defense WARN fire?** Read the defense-WARN condition (plan 54). It should fire when raw contains `### Ledger Updates` but parsed ledger is all-None. For plan 57 it did NOT fire. Determine why: does the WARN check `result_text` (final message only) rather than the concatenated multi-turn text? Or was `ledger_updates` not all-None (something extracted from an echo)? Pinpoint the gap at file:line.
>
> **Section 4 — Classify + root cause.** State the CONFIRMED root cause and classify it: (a) PARSER fragility — section-match grabs the first/echo `### Ledger Updates` instead of the agent's real receipt (multi-turn concat amplified this); (b) AGENT EMISSION — the agent never produced a clean block; (c) CANARY-DESIGN artifact — the canary prompt's literal headings polluted the parse (would not occur for real plans whose feedback content does not echo channel headings); or a combination. Assess whether real (non-canary) FORWARD filings are actually at risk, or only the canary. Note the defense-WARN gap separately (it should be fixed regardless, so silent drops are impossible).
>
> **Section 5 — Gap Assessment + `### Verification Blocks` (Rule 39).** Gap Assessment `| Gap | Current State (file:line) | Proposed State | Change Required |`. Verification Blocks `(claim, query, expected_output)` for: the agent-emitted-clean-block finding, the reproduced forward extraction, the section-match-grabs-echo confirmation, and the defense-WARN gap. Close with **CEO decision forks** (recommendations): the extraction fix shape (e.g. match the LAST `### Ledger Updates` / the one in the final result message / skip backtick-quoted echoes); the defense-WARN fix; whether a cleaner re-canary (whose prompt does NOT echo the channel headings literally) is sufficient to prove FORWARD vs a parser change being required; whether feedback/PROJECT_STATUS are at any latent risk from the same echo issue.
>
> **BEFORE FINISHING — explicitly `git add` your deposit file and `git commit` it.** Use `with open()` for the deposit; no heredocs. Standard prompt feedback → emit via `### Ledger Updates > #### Prompt Feedback`. **Deposits:**
> - `bellows/knowledge/research/forward-extraction-failure-rootcause-2026-06-14.md`
