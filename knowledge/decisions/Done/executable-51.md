# Bellows — Daemon-Owned Ledgers ACTIVATION Slice 2: PROJECT_STATUS Go-Live (+ feedback agent-column fix)
**Date:** 2026-06-14 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 42 — ACTIVATION Slice 2 (PROJECT_STATUS), following the proven feedback activation (plan 49) + live canary (plan 50). PROJECT_STATUS disposition is (a) daemon-written post-merge (design Section 3 File 1) — NOT DB-generated: the daemon appends the agent's milestone narrative verbatim to `PROJECT_STATUS.md` on main at a canonical position. So unlike feedback, there is NO freeze and NO generation/archive — the existing PROJECT_STATUS.md files stay; the daemon just becomes their writer. The Phase 2 handler (`_append_project_status` bellows.py:1191; project_status branch ~1170–1175) is already built DORMANT with canonical-append + coexistence; activation = governance flip (agents stop writing PROJECT_STATUS.md, emit via channel) + remove PROJECT_STATUS.md from SCOPE_ALLOWLIST + confirm idempotency covers it. Once agents stop writing the file, the coexistence check passes and the daemon appends. RIDER: the feedback canary (plan 50) showed `prompt_feedback.agent` came through empty — fix the `record_prompt_feedback(...)` call (bellows.py:~1137) to pass the agent value (from the parsed step data); same handler area, trivial. The design doc `knowledge/research/daemon-owned-ledgers-design-2026-06-13.md` Section 5 is AUTHORITATIVE for the Rule 8 governance changes. Re-verify Verification Blocks at edit time; halt-and-report on divergence. SCOPE NOTE — this slice activates PROJECT_STATUS only; FORWARD stays dormant (Slice 3). FORWARD rows 4/5/13 still do NOT fully close until Slice 3. DAEMON RESTART REQUIRED after close; the FIRST plan after restart is the live canary (its PROJECT_STATUS milestone must be daemon-appended, not agent-written). DO NOT restart until this plan fully CLOSES (premature restart stranded plan 48 earlier).

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, the design doc Section 3 File 1 + Section 5, and the EXISTING `_apply_ledger_updates` / `_append_project_status` / `record_prompt_feedback` code. **Scope is exactly these files: `bellows.py`, `lifecycle.py`, `gates.py`, `tests/test_bellows.py`, `tests/test_gates.py`, `tests/test_lifecycle.py`, and the governance-root `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`.**
> - **(1) Idempotency confirm/extend:** verify the `ledger_writes` idempotency guard (added in plan 49) covers the PROJECT_STATUS append path; if it only guards feedback, extend it so a teardown retry cannot double-append PROJECT_STATUS. Add a test proving a duplicate PROJECT_STATUS apply is a no-op.
> - **(2) Gate allowlist:** remove `PROJECT_STATUS.md` from `gates.py` `SCOPE_ALLOWLIST` (agents no longer write it). After this, both feedback and PROJECT_STATUS are out of the allowlist.
> - **(3) Agent-column rider:** fix the `record_prompt_feedback(...)` call (bellows.py:~1137) to pass the actual agent (from the parsed step/output data) so `prompt_feedback.agent` is populated. Add/adjust a test asserting the agent column is non-empty.
> - **(4) Governance flip (PLANNER_TEMPLATE.md, design Section 5):** Rule 8 (~L473): replace "update `PROJECT_STATUS.md` with a summary…" with "emit the PROJECT_STATUS milestone summary in the Output Receipt `### Ledger Updates > #### Project Status`; do NOT write PROJECT_STATUS.md directly". Rule 8 ordering (~L473) and the embedded prompt mention (~L378) and the `**Final:** Update PROJECT_STATUS.md` instruction template (~L477): update the ordering to "deliverable verification → QA deposit → ledger updates in Output Receipt → final commit" (remove `PROJECT_STATUS.md update`; confirm `feedback append` is already gone from plan 49 — remove if still present). Add the `#### Project Status` subsection to the `### Ledger Updates` channel-format spec. Session-wrap (~L1502) `Update PROJECT_STATUS.md`: this is the Planner's session-summary (distinct from per-plan milestones) — leave it, but add a parenthetical that per-plan milestones are now daemon-written via the channel. Do NOT touch the FORWARD/Rule 42 instructions (Slice 3). Bump `**Version:**` + changelog row. Locate every site by ANCHOR/grep; zero or multiple matches → halt and report.
>
> **Tests:** (a) PROJECT_STATUS idempotency no-op on duplicate; (b) agent column populated by record_prompt_feedback; (c) the project_status handler still appends at the canonical position when coexistence allows (regression); (d) coexistence still skips when PROJECT_STATUS.md in files_changed. Run the FULL suite (`python3 -m pytest tests/`) to explicit pass/fail and READ THE TAIL. Write the dev log to `bellows/knowledge/development/projectstatus-activation-dev-log-2026-06-14.md`. Use `with open()`; no heredocs. Standard prompt feedback: emit via the `### Ledger Updates > #### Prompt Feedback` channel (the new protocol is now in force) — do NOT write agent-prompt-feedback.md. **Deposits:**
> - `bellows/knowledge/development/projectstatus-activation-dev-log-2026-06-14.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, and design doc Sections 3 File 1 + 5. **Verify, each with executed evidence (files into `knowledge/qa/evidence/projectstatus-activation-2026-06-14/`):** (1) **Full suite** — final 15 lines, zero failures, new-test count matches dev log; `full_suite_tail.txt`. (2) **Idempotency** — PROJECT_STATUS duplicate-apply no-op test passes; `idempotency.txt`. (3) **Allowlist** — `PROJECT_STATUS.md` AND `agent-prompt-feedback.md` both absent from `SCOPE_ALLOWLIST`; `allowlist.txt`. (4) **Agent-column rider** — the record_prompt_feedback call passes agent; test proves the column is populated; `agent_column.txt`. (5) **Governance flip** — Rule 8 (L473/477) + prompt mention (L378) now say emit-via-channel with the new ordering; `#### Project Status` in the format spec; FORWARD/Rule 42 untouched; version bumped; `governance.txt`. (6) **Coexistence intact** — skip-when-in-files_changed regression passes; `coexistence.txt`. (7) **Emit your own feedback via channel** — confirm THIS QA step emitted its feedback via `### Ledger Updates > #### Prompt Feedback` and did not write agent-prompt-feedback.md (proves the new protocol is followed); `self_protocol.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `projectstatus-activation-2026-06-14`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/projectstatus-activation-qa-report-2026-06-14.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/projectstatus-activation-2026-06-14/`; `required_evidence_files`: `[full_suite_tail.txt, idempotency.txt, allowlist.txt, agent_column.txt, governance.txt, coexistence.txt, self_protocol.txt]`. Include the literal stdout of the block in the QA report. If it prints `FAILED`, halt and report to CEO. Write the QA report (verification table + Rule 20 banner) to `bellows/knowledge/qa/projectstatus-activation-qa-report-2026-06-14.md`. **Receipt Flags for CEO MUST include:** (1) DAEMON RESTART REQUIRED — do NOT restart until this plan is fully closed; (2) LIVE CANARY — first plan after restart: its PROJECT_STATUS milestone must be daemon-appended (channel), not agent-written; (3) feedback + PROJECT_STATUS now both activated; FORWARD remains for Slice 3; rows 4/5/13 close after Slice 3. Use `with open()`; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/projectstatus-activation-qa-report-2026-06-14.md`
> - `bellows/knowledge/qa/evidence/projectstatus-activation-2026-06-14/` (seven evidence files per Rule 20 self-check)
