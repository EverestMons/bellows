# Shop-Level Lifecycle DB References + Id-Native Naming Sync (COMPANY, SPECIALIST_TEMPLATE, verdicts README, PLANNER_TEMPLATE v4.62)
**Date:** 2026-06-11 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always

**Execution Map:** Step 1 (DEV) → Step 2 (QA) — strictly sequential, no parallel steps.

## Context (Rule 27)
Extends the v4.61 codification (Done/executable-4.md) to the remaining shop-level documents: COMPANY.md still prescribes legacy `<type>-[feature]-[YYYY-MM-DD].md` naming for watched decisions and never mentions `lifecycle.db`; SPECIALIST_TEMPLATE.md has zero Bellows-execution references; `bellows/verdicts/README.md` (the named format authority for verdict filenames) still defines the slug as filename-with-prefix-stripped; and PLANNER_TEMPLATE.md L532 retains a stale `<plan-filename-without-md>` in the embedded plan-side template copy (plan-4 QA flag — E4 scoping gap, rolled in here). All edits are documentation-only. CROSS-REPO NOTE: this plan mixes in-worktree and governance-root edits — `verdicts/README.md` is inside the bellows worktree and WILL appear in files_changed; the three governance-root files are outside the worktree and will NOT (2026-06-03 / plan-4 precedent; both are expected, not noise). The edit payloads below are authoritative and complete; locate every site by ANCHOR text via grep — never by line number. This plan implements no diagnostic: it extends governance codification of shipped Reporting Phase 1 behavior (legacy-named pre-id diagnostic; no integer id exists), per Checklist item 20's carve-out.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file first, then read each edit target around its anchor before editing. **Allowed files (scope — exactly these, no others):** `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`, `/Users/marklehn/Developer/GitHub/COMPANY.md`, `/Users/marklehn/Developer/GitHub/SPECIALIST_TEMPLATE.md`, and `verdicts/README.md` (the bellows repo file, edited in your worktree; canonical path `bellows/verdicts/README.md`). If any other file needs modification, halt and report. **Apply edits E1–E6. For each: grep for the ANCHOR in the named file, confirm exactly one match (zero or multiple: halt and report the grep output), apply, and record anchor + post-edit line number in your dev log.**
>
> **E1 — Embedded plan-side template sync.** File: PLANNER_TEMPLATE.md. ANCHOR: `> - `plan_slug`: `<plan-filename-without-md>``. Replace with: `> - `plan_slug`: `<authoring-time descriptive slug — title-derived plus date, NOT the filename>``. This must leave zero occurrences of `plan-filename-without-md` in the file.
>
> **E2 — COMPANY.md decisions naming.** File: COMPANY.md. ANCHOR: `Prefixed by type: `executable-[feature]-[YYYY-MM-DD].md``. Replace the full sentence run from that anchor through `Completed plans are moved to a `Done/` subfolder within decisions.` with: `Prefixed by type. Bellows-watched deposits (executable / diagnostic / qa) use the transient placeholder `<type>-draft-<HHMMSS>.md`; Bellows mints a global-monotonic integer id at claim, renames to `in-progress-<type>-<id>.md`, and files completed plans id-canonically as `Done/<type>-<id>.md`. Descriptive identity lives in the plan's `# Title` line; the date lives in the header `**Date:**` field. Roadmaps (`roadmap-[feature]-[YYYY-MM-DD].md`, multi-phase master plans, not claimable) keep descriptive+date naming. Full protocol: PLANNER_TEMPLATE.md "Id-Native Naming" and "Lifecycle DB Read Protocol (Planner)".`
>
> **E3 — COMPANY.md plan-lifecycle system of record.** File: COMPANY.md. ANCHOR: `Projects with `knowledge/decisions/` dispatch through Bellows:`. Immediately after the list block that follows that line (insert before the next heading or paragraph that is not part of the list), insert this new paragraph: `**Plan lifecycle system of record.** Bellows records every plan transition in `bellows/lifecycle.db` (10 tables: id_sequence, plans, diagnostic_meta, executable_meta, derivations, steps, commits, deposits, verdicts, gate_events) — integer ids, state transitions, per-step cost/turns/duration, gate events, deposits, landed commit SHAs, and verdict outcomes. The daemon is the SOLE writer; the Planner reads it strictly read-only as an index (the filesystem remains ground truth — lifecycle writes are best-effort log-and-continue). Diagnostic→executable lineage is recorded from the literal plan-text citation `implements diagnostic <id>` (integer ids only). Reporting Phase 2 will generate cycle reports from this DB, replacing hand-maintained backward-looking BACKLOG content (see `roadmap-reporting-vs-backlog-2026-06-09.md`).`
>
> **E4 — SPECIALIST_TEMPLATE.md execution context.** File: SPECIALIST_TEMPLATE.md. ANCHOR: `**Output location:**`. Immediately after the paragraph/line block containing that anchor, insert this new paragraph: `**Bellows execution context.** When dispatched by Bellows, your bootstrap prompt includes the plan's integer id and an instruction to tag every commit message with `[<id>]` — comply exactly; that tag is the shop's commit→plan linkage. Plan lifecycle (steps, costs, gate results, deposits, commit SHAs) is recorded mechanically by the daemon in `bellows/lifecycle.db` — no specialist action is required, and specialists never write that DB. Do not rename plan files; Bellows owns all lifecycle renames.`
>
> **E5 — Verdicts README id-native filename authority.** File: `verdicts/README.md` (bellows worktree). (a) ANCHOR: `verdict-<plan-slug>-step-<N>.md`. Replace that token with `verdict-<id>-step-<N>.md`. (b) ANCHOR: ``<plan-slug>` = plan filename with leading prefix (`in-progress-`, `verdict-pending-`, `executable-`, `diagnostic-`) and `.md` stripped.`. Replace with: ``<id>` = the plan's integer id (id-native plans). Legacy plans: `<plan-slug>` = plan filename with leading prefix (`in-progress-`, `verdict-pending-`, `executable-`, `diagnostic-`) and `.md` stripped — dual-format tolerance accepts both. In all cases the mechanical rule: copy the verdict-request filename and replace `verdict-request-` with `verdict-`.` Do NOT modify any other line of the README — in particular the `verdict:` line-1 format regex is gate-consumed and out of scope.
>
> **E6 — Version bump + changelog.** File: PLANNER_TEMPLATE.md. (a) ANCHOR: `**Version:** 4.61`. Replace with `**Version:** 4.62`. (b) Locate the version-history table row beginning `| 2026-06-11 | v4.61:` and append a new final row after it: `| 2026-06-11 | v4.62: Shop-level lifecycle DB reference sync. Embedded Rule 20 plan-side template copy synced to authoring-time plan_slug (plan-4 QA flag); COMPANY.md decisions naming updated to id-native convention with PLANNER_TEMPLATE pointer; COMPANY.md gains plan-lifecycle system-of-record paragraph (lifecycle.db, sole-writer/read-only roles, derivations citation, Reporting Phase 2 pointer); SPECIALIST_TEMPLATE.md gains Bellows execution-context paragraph ([id] commit-tag compliance, mechanical lifecycle recording, no plan-file renames); bellows/verdicts/README.md verdict-filename authority updated to id form with legacy tolerance. ARCHITECTURE.md deliberately omitted (no Bellows content). |`
>
> **After all edits:** spot-check each edit by grep and record results. Commit the three governance-root files at the governance root (`/Users/marklehn/Developer/GitHub/`) in a single commit; commit `verdicts/README.md` in the worktree via your standard final commit. Write a dev log covering E1–E6 with anchors and post-edit line numbers to `bellows/knowledge/development/shop-db-references-dev-log-2026-06-11.md`. Use `with open()` for deposits; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/shop-db-references-dev-log-2026-06-11.md`

---
---

## STEP 2 — Bellows QA

---

> **MANDATORY — READ THIS FIRST. This step is gate-enforced by the daemon's `rule_20_self_check` gate.** The gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` followed by `PASSED — SELF-CHECK PASSED`. Your verification table does NOT satisfy this gate — the banner must appear verbatim in the report. Before finishing, self-grep your deposited report for the exact string `Rule 20 — QA Self-Check Results` and confirm it is present; if it is not, you have not finished. **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file, the DEV deposit `bellows/knowledge/development/shop-db-references-dev-log-2026-06-11.md`, and each edited file around its anchor: `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`, `/Users/marklehn/Developer/GitHub/COMPANY.md`, `/Users/marklehn/Developer/GitHub/SPECIALIST_TEMPLATE.md`, `verdicts/README.md`. **Verify, each with executed evidence (commands + output to evidence files):** (1) **Edit landing** — for each edit E1–E6, grep for the new text's distinctive phrase (present) and the replaced anchor phrase (absent where replaced); output to `edits_landed.txt`. (2) **Stale-placeholder sweep** — `grep -rn "plan-filename-without-md" /Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md /Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md /Users/marklehn/Developer/GitHub/COMPANY.md /Users/marklehn/Developer/GitHub/SPECIALIST_TEMPLATE.md` must return ZERO hits; output to `stale_placeholder.txt`. (3) **COMPANY.md date-naming scan** — `grep -n "YYYY-MM-DD" /Users/marklehn/Developer/GitHub/COMPANY.md` and classify every hit: permitted classes are general knowledge-artifact naming (non-watched), roadmap naming, flag-file naming, and header Date-field templates; any hit prescribing date-bearing deposit filenames for executable/diagnostic/qa plans is a FAIL; output with classifications to `company_dates_scan.txt`. (4) **Verdicts README integrity** — diff `verdicts/README.md` against `git show HEAD~1:verdicts/README.md` (worktree) and confirm only the E5 filename-authority lines changed and the `verdict:` line-1 format regex text is byte-identical; output to `verdicts_readme_diff.txt`. (5) **Version integrity** — `**Version:** 4.62` exactly once in the PLANNER_TEMPLATE header plus the v4.62 changelog row present; output to `version_check.txt`. (6) **Cross-reference validity** — confirm the two pointers added by E2/E3 resolve: grep PLANNER_TEMPLATE.md for the literal headings `Id-Native Naming` and `Lifecycle DB Read Protocol (Planner)`, and confirm `/Users/marklehn/Developer/GitHub/roadmap-reporting-vs-backlog-2026-06-09.md` exists on disk; output to `crossref_check.txt`. Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `shop-db-references-2026-06-11`; `qa_report_path`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/shop-db-references-qa-report-2026-06-11.md`; `evidence_dir`: `/Users/marklehn/Developer/GitHub/bellows/knowledge/qa/evidence/shop-db-references-2026-06-11/`; `required_evidence_files`: `[edits_landed.txt, stale_placeholder.txt, company_dates_scan.txt, verdicts_readme_diff.txt, version_check.txt, crossref_check.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, do not proceed with closure — halt and report to CEO. Write the QA report with a verification table (item | method | evidence file | PASS/FAIL) and the Rule 20 self-check banner block to `bellows/knowledge/qa/shop-db-references-qa-report-2026-06-11.md`. **Deposits:**
> - `bellows/knowledge/qa/shop-db-references-qa-report-2026-06-11.md`
> - `bellows/knowledge/qa/evidence/shop-db-references-2026-06-11/` (multiple files per Rule 20 self-check)
