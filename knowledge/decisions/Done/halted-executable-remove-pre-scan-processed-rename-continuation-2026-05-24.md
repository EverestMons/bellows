# Bellows — Remove pre-scan `processed-` prefix rename (continuation — complete the test removal)
**Date:** 2026-05-24 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** full | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** after_step_1 | **auto_close:** false

## Context

Continuation of the halted plan `halted-executable-remove-pre-scan-processed-rename-2026-05-24` (now in `Done/`). That plan correctly removed the pre-scan block and module-level set from `bellows.py`, and removed four of the pre-scan tests, but stopped at Task D when DEV found a dangling `_prescan_orphan_logged` reference in a surviving test. The original plan's enumeration was incomplete: it named four pre-scan tests, but `tests/test_consume_verdicts.py` contains SEVEN pre-scan tests, all of which exercise the now-deleted pre-scan code.

**Why the original plan was incomplete:** the 2026-05-24 diagnostic's Section G Q19 enumerated only four tests. The Planner relied on that enumeration without independently grepping the file. The complete list (verified by Planner via `grep -n '^def test_pre_scan' tests/test_consume_verdicts.py` against the live file before authoring this plan) is:

| # | Test name | Already removed by halted DEV? |
|---|---|---|
| 1 | `test_pre_scan_renames_processed_verdict_to_canonical` | Yes |
| 2 | `test_pre_scan_collision_guard_does_not_overwrite` | Yes |
| 3 | `test_pre_scan_skips_rename_when_no_paired_plan` | Yes |
| 4 | `test_pre_scan_renames_when_verdict_pending_plan_exists` | Yes |
| 5 | `test_pre_scan_ignores_non_verdict_processed_files` | **No — to remove** |
| 6 | `test_pre_scan_treats_done_plan_as_no_paired_plan` | **No — to remove (contains the dangling `_prescan_orphan_logged` reference)** |
| 7 | `test_pre_scan_collision_guard_fires_regardless_of_paired_plan` | **No — to remove** |

**Current working-tree state at the start of this plan (Planner-verified just before authoring):**
- `bellows.py` is modified, unstaged. Diff shows 38 lines removed (the pre-scan block at the former 1129-1162 range + the `_prescan_orphan_logged` module-level set at the former line 33). `grep -n '_prescan_orphan_logged' bellows.py` returns ZERO matches. `grep -n 'normalized write-time processed- prefix:' bellows.py` returns ZERO matches.
- `tests/test_consume_verdicts.py` is modified, unstaged. Diff shows 236 lines removed (four pre-scan tests). Three pre-scan tests remain at lines 310, 393, 445 (line numbers pre-removal of remaining three; will shift after edit).
- No commits have been made for this work. Working tree is dirty in exactly these two files.
- The halted plan file is at `bellows/knowledge/decisions/Done/halted-executable-remove-pre-scan-processed-rename-2026-05-24.md`.
- The legitimate post-consumption `processed-` rename sites at bellows.py:1252 and 1275 (line numbers shifted from the pre-edit 1291) are preserved per `grep -n 'processed-' bellows.py`.

**Goal:** complete the three remaining test removals, then commit everything (the halted DEV's uncommitted bellows.py + tests edits, plus the three new test removals) as ONE atomic commit so git history has a clean single-commit story for the full removal.

**What this plan does NOT do:** does not re-do work that the halted DEV already correctly performed. Tasks A, B, C from the halted plan (pre-scan block removal, module-level set removal, four named test removals) are already in the working tree as uncommitted edits. This plan picks up at the equivalent of "Task C continued" by removing the three remaining pre-scan tests, then runs Task D verification on the combined result.

**Daemon-restart note:** Same as the halted plan. The running daemon will continue executing pre-fix code through this plan's lifecycle. After plan close + daemon restart, the pre-scan removal becomes effective for all future plans.

## Execution Map

```
Step 1 (DEV) → Step 2 (QA)
```

Sequential. Step 1 verifies the partial state, removes three more tests, commits everything together, deposits dev log. Step 2 verifies removal and runs full pytest.

---
---

## STEP 1 — Bellows Developer

---

> You are the Bellows Developer. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first. **Skip glossary read — this is a code-removal task with precise anchors and a partial-state starting condition.** **Authority:** the 2026-05-24 diagnostic findings at `bellows/knowledge/architecture/processed-prefix-reconsumption-and-rename-skip-2026-05-24.md` (Section A identifies the pre-scan code; Section F Q16 recommends Shape 1c — full removal). The diagnostic's Section G Q19 test enumeration was incomplete; this plan supersedes it with the complete seven-test list shown in the Context section above. **CRITICAL — partial-state verification (do this FIRST before any edits):** This plan starts from a working tree where the prior halted DEV's edits are uncommitted. Confirm the partial state matches expected before adding to it. Run each of the following and confirm the listed expected output. If any check fails, STOP — deposit a flag at `bellows/knowledge/flags/partial-state-mismatch-remove-pre-scan-processed-rename-continuation-2026-05-24-step-1.md` documenting the actual output for each check, and halt. The Planner triages. Check (i): `cd bellows && git status --porcelain bellows.py tests/test_consume_verdicts.py` — expected: exactly two `^ M ` lines, one for each file. Check (ii): `grep -c '_prescan_orphan_logged' bellows.py` — expected: 0. Check (iii): `grep -c 'normalized write-time processed- prefix:' bellows.py` — expected: 0. Check (iv): `grep -c '_prescan_orphan_logged' tests/test_consume_verdicts.py` — expected: 1 (the one surviving reference in `test_pre_scan_treats_done_plan_as_no_paired_plan` that this plan will remove). Check (v): `grep -c '^def test_pre_scan_renames_processed_verdict_to_canonical' tests/test_consume_verdicts.py` — expected: 0 (already removed by halted DEV). Check (vi): `grep -c '^def test_pre_scan_collision_guard_does_not_overwrite' tests/test_consume_verdicts.py` — expected: 0. Check (vii): `grep -c '^def test_pre_scan_skips_rename_when_no_paired_plan' tests/test_consume_verdicts.py` — expected: 0. Check (viii): `grep -c '^def test_pre_scan_renames_when_verdict_pending_plan_exists' tests/test_consume_verdicts.py` — expected: 0. Check (ix): `grep -n '^def test_pre_scan' tests/test_consume_verdicts.py` — expected: exactly THREE matches, all of these test names (in any line order): `test_pre_scan_ignores_non_verdict_processed_files`, `test_pre_scan_treats_done_plan_as_no_paired_plan`, `test_pre_scan_collision_guard_fires_regardless_of_paired_plan`. Check (x): `python -c "import bellows.bellows"` from `bellows/` — expected: exits cleanly (bellows.py imports without error in current partial state). **Task A — remove the three remaining pre-scan tests from `tests/test_consume_verdicts.py`:** Remove these three test functions in their entirety, including docstrings and any helper fixtures used only by them: (1) `test_pre_scan_ignores_non_verdict_processed_files`; (2) `test_pre_scan_treats_done_plan_as_no_paired_plan`; (3) `test_pre_scan_collision_guard_fires_regardless_of_paired_plan`. If any of these references a `@pytest.fixture` or helper function that is NOT used by any other surviving test in the file, also remove the fixture; do NOT remove fixtures shared with surviving tests. **Task B — verify completeness:** Run `grep -n '^def test_pre_scan' tests/test_consume_verdicts.py` and confirm ZERO matches. Run `grep -rn '_prescan_orphan_logged' .` (excluding `.git/` and `knowledge/`) and confirm ZERO matches anywhere in the repo. Run `grep -rn 'normalized write-time processed- prefix' .` (excluding `.git/` and `knowledge/`) and confirm ZERO matches anywhere in the repo. If any check shows non-zero, STOP — deposit a flag at `bellows/knowledge/flags/dangling-references-remove-pre-scan-processed-rename-continuation-2026-05-24-step-1.md` listing the surviving references and halt. **Task C — smoke check:** `python -c "import bellows.bellows"` from `bellows/` and confirm clean exit. If import fails, paste traceback into `bellows/knowledge/flags/import-failure-remove-pre-scan-processed-rename-continuation-2026-05-24-step-1.md` and halt. **Task D — deposit dev log:** write to `bellows/knowledge/development/remove-pre-scan-processed-rename-continuation-2026-05-24.md` documenting: (a) the partial-state verification results from checks (i)–(x) above (paste literal stdout for each grep/python invocation), (b) the three test functions removed in Task A with before/after line counts of `tests/test_consume_verdicts.py`, (c) the Task B completeness verification stdout, (d) the Task C import smoke stdout, (e) explicit acknowledgement that this plan picks up the halted DEV's uncommitted edits and commits them together with the three new test removals as one commit, (f) one-paragraph summary citing the 2026-05-24 diagnostic findings, PLANNER_TEMPLATE v4.49 Rule 25 as the governance fix that makes the pre-scan removable, and the halted predecessor plan `halted-executable-remove-pre-scan-processed-rename-2026-05-24.md`. **Task E — commit everything as ONE commit:** stage `bellows/bellows.py`, `bellows/tests/test_consume_verdicts.py`, and the dev log. Verify with `git status --porcelain` that exactly these three paths are staged and nothing else (other than potential dev log additions in the staging dir). Commit with message `fix(bellows): remove pre-scan processed- prefix rename — eliminates P0 multi-step pause-cycle loop`. **DO NOT push.** **Standard prompt feedback protocol** → append entry to `bellows/knowledge/research/agent-prompt-feedback.md`. The entry should explicitly note that the predecessor plan's test enumeration was incomplete (named 4 of 7 pre-scan tests), and that DEV's stop-on-dangling-reference behavior was the correct outcome of the safety check. Second commit: `docs: prompt feedback — bellows DEV remove pre-scan processed rename continuation`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/bellows.py` (modified — already pre-modified by halted DEV; committed by this plan)
> - `bellows/tests/test_consume_verdicts.py` (modified — partial pre-modified by halted DEV, completed by this plan)
> - `bellows/knowledge/development/remove-pre-scan-processed-rename-continuation-2026-05-24.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`

---
---

## STEP 2 — Bellows QA

---

> You are the Bellows QA Agent. Read your specialist file at `bellows/agents/BELLOWS_QA.md` first. **Skip glossary read — this is a code-removal QA task.** **Before starting, read `bellows/knowledge/development/remove-pre-scan-processed-rename-continuation-2026-05-24.md` (DEV Step 1 deposit) and check its Output Receipt status; if not Complete, stop and report the blocker.** **FIRST — Deliverable Verification (Rule 17).** Verification table: | Deliverable | Expected | Status (✅/❌) | Evidence |. Specifically: (1) `grep -n 'normalized write-time processed- prefix:' bellows/bellows.py` returns ZERO matches; (2) `grep -n '_prescan_orphan_logged' bellows/bellows.py` returns ZERO matches; (3) `grep -n '# Pre-scan: normalize processed-verdict-' bellows/bellows.py` returns ZERO matches; (4) ALL SEVEN pre-scan test names (`test_pre_scan_renames_processed_verdict_to_canonical`, `test_pre_scan_collision_guard_does_not_overwrite`, `test_pre_scan_skips_rename_when_no_paired_plan`, `test_pre_scan_renames_when_verdict_pending_plan_exists`, `test_pre_scan_ignores_non_verdict_processed_files`, `test_pre_scan_treats_done_plan_as_no_paired_plan`, `test_pre_scan_collision_guard_fires_regardless_of_paired_plan`) return ZERO matches in `tests/test_consume_verdicts.py`; (5) `grep -n '^def test_pre_scan' tests/test_consume_verdicts.py` returns ZERO matches (no other pre-scan tests should exist); (6) `grep -rn '_prescan_orphan_logged' .` (excluding `.git/` and `knowledge/`) returns ZERO matches anywhere in the repo; (7) `grep -rn 'normalized write-time processed- prefix' .` (excluding `.git/` and `knowledge/`) returns ZERO matches anywhere in the repo; (8) `python -c "import bellows.bellows"` from `bellows/` exits cleanly; (9) the post-consumption rename at the legitimate `processed-` marker site is preserved — `grep -n 'processed-' bellows/bellows.py` returns AT LEAST TWO matches (the `shutil.move` sites at the post-consumption marker that creates `processed-verdict-*` after successful consumption); (10) dev log exists at `bellows/knowledge/development/remove-pre-scan-processed-rename-continuation-2026-05-24.md` with all six documentation items (a-f) from the DEV prompt including the partial-state verification stdout; (11) `agent-prompt-feedback.md` has a new 2026-05-24 entry from this plan noting the predecessor's incomplete enumeration. Capture each grep to evidence under `bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-continuation-2026-05-24/`: `prescan_log_line_absent.txt`, `prescan_orphan_logged_absent.txt`, `prescan_block_comment_absent.txt`, `all_seven_tests_absent.txt`, `no_pre_scan_test_defs.txt`, `repo_wide_orphan_absent.txt`, `repo_wide_log_absent.txt`, `import_smoke.txt`, `legitimate_processed_preserved.txt`, `dev_log_present.txt`, `feedback_entry.txt`. **Full pytest suite (Test Scope: full).** Run `pytest 2>&1 | tee bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-continuation-2026-05-24/pytest_full.txt`. Expected: all tests pass EXCEPT for the pre-existing `test_run_step_timeout` failure (per BACKLOG-documented carry-over, not a regression). The seven removed pre-scan tests should not appear in the test count because they no longer exist. If any OTHER test fails, mark ❌ and halt — the removal has caused a regression in some path not predicted by the diagnostic. Capture pytest summary line (test count, passes, fails, skips) to evidence. **Structural compliance check.** Identify the DEV commit SHA (the code-removal commit, NOT the prompt-feedback commit). Run `git --no-pager show --stat <DEV-commit-sha> 2>&1 | tee bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-continuation-2026-05-24/dev_commit.txt` and confirm exactly three paths in the commit: `bellows.py`, `tests/test_consume_verdicts.py`, and `knowledge/development/remove-pre-scan-processed-rename-continuation-2026-05-24.md`. Run `git --no-pager show <DEV-commit-sha> -- bellows.py 2>&1 | tee bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-continuation-2026-05-24/diff_bellows.txt` and verify the diff shows ONLY deletions in `bellows.py` (no additions, no modifications). Run `git --no-pager show <DEV-commit-sha> -- tests/test_consume_verdicts.py 2>&1 | tee bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-continuation-2026-05-24/diff_tests.txt` and verify only deletions, no additions or modifications to surviving tests. If the diffs show any additions or non-targeted modifications outside the seven pre-scan tests, mark ❌ and halt. **Rule 20 self-check** — run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at governance root. Use these values: `plan_slug` = `executable-remove-pre-scan-processed-rename-continuation-2026-05-24`; `qa_report_path` = `bellows/knowledge/qa/executable-remove-pre-scan-processed-rename-continuation-2026-05-24.md`; `evidence_dir` = `bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-continuation-2026-05-24/`; `required_evidence_files` = `["prescan_log_line_absent.txt", "prescan_orphan_logged_absent.txt", "prescan_block_comment_absent.txt", "all_seven_tests_absent.txt", "no_pre_scan_test_defs.txt", "repo_wide_orphan_absent.txt", "repo_wide_log_absent.txt", "import_smoke.txt", "legitimate_processed_preserved.txt", "dev_log_present.txt", "feedback_entry.txt", "pytest_full.txt", "dev_commit.txt", "diff_bellows.txt", "diff_tests.txt"]`. Include literal stdout in QA report. If FAILED, halt — report to CEO. **Final:** Update `bellows/PROJECT_STATUS.md` — prepend a 2026-05-24 entry under Completed for "Removed pre-scan `processed-` prefix rename from `_consume_verdicts` (Shape 1c) — eliminates P0 multi-step pause-cycle loop class" with a one-paragraph summary citing the 2026-05-24 diagnostic findings, PLANNER_TEMPLATE v4.49 Rule 25 as the governance fix, and noting that the work spanned a halted predecessor plan plus this continuation. Use `Desktop Commander:edit_block` with the existing topmost Completed entry as the anchor (insert immediately before it). **Commit:** stage QA report, evidence files, and `PROJECT_STATUS.md` update with message `qa(bellows): remove pre-scan processed- prefix rename continuation`. **DO NOT push.** **Standard prompt feedback protocol** → append entry to `bellows/knowledge/research/agent-prompt-feedback.md`. Second commit: `docs: prompt feedback — bellows QA remove pre-scan processed rename continuation`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/knowledge/qa/executable-remove-pre-scan-processed-rename-continuation-2026-05-24.md`
> - `bellows/knowledge/qa/evidence/executable-remove-pre-scan-processed-rename-continuation-2026-05-24/` (15 evidence files per Rule 20 self-check list)
> - `bellows/PROJECT_STATUS.md` (modified)
> - `bellows/knowledge/research/agent-prompt-feedback.md`
