# Bellows — Failure 3 Population Audit (Move-to-Done vs Gate Ordering)
**Date:** 2026-05-05 | **Tier:** Small | **Test Scope:** none (read-only investigation) | **Execution:** Step 1 (DEV)

## How to Run This Plan

Bellows OP-001 is resolved (2026-05-05) and Bellows-self plans run in-place without worktree isolation. This is a read-only artifact inspection — no risk to Bellows source. Safe to dispatch via Bellows.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-failure-3-population-audit-2026-05-05.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to any further step or move the plan to Done.
```

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-failure-3-population-audit-2026-05-05.md", "bellows/knowledge/decisions/in-progress-diagnostic-failure-3-population-audit-2026-05-05.md")`. **Skip glossary read — this is a read-only artifact inspection task on lifecycle metadata. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first** for context on the Done/ lifecycle and verdict consumption flow. You are auditing whether Failure 3 (move-to-Done before gate evaluation completes) is still a live failure mode following the structural fixes shipped 2026-04-24 (disable-auto-close model) and the v4.30 series of Planner-side rule changes (Rule 8 + Rule 23 + Rule 25 instructing agents not to perform the Done/ move). The original Failure 3 description: "agent moves plan to Done inside its execution window, before Bellows's diff-based gate check runs, making gate failures advisory rather than blocking." This audit determines whether the bug class (a) still fires from the agent side — agents bypassing Rule 8/23 by moving plans to Done themselves — or (b) still fires from the Bellows side — Bellows itself moving plans to Done before its own gate evaluation completes. Population audit pattern, same as the 2026-05-05 BACKLOG #1 audit. **Static analysis only — do NOT propose fixes, do NOT edit Bellows source.** **Investigation tasks (execute in order):** **(1) Population scope: enumerate Done/ plans shipped since the disable-auto-close model landed.** Run `find bellows/knowledge/decisions/Done -maxdepth 1 -type f -name "*.md" -newermt "2026-04-24" 2>/dev/null | sort > /tmp/done-population.txt && wc -l /tmp/done-population.txt`. Capture the count and the first 3 / last 3 filenames in findings. Repeat for the other Bellows-watched projects: `invoice-pulse/knowledge/decisions/Done/`, `forge/knowledge/decisions/Done/`, `BrewBuddy/knowledge/decisions/Done/`, `SimpleScreen/knowledge/decisions/Done/`, `freight-kb/knowledge/decisions/Done/`, `study/knowledge/decisions/Done/`, `ai-career-digest/knowledge/decisions/Done/`. Some of these may not have a Done/ subdirectory or may have zero plans in the window — report each project's count separately. The total population is what we audit. **(2) Identify gate-failure verdicts in the same window.** Run `find bellows/verdicts/resolved -maxdepth 1 -type f -name "verdict-*-step-*.md" -newermt "2026-04-24" 2>/dev/null > /tmp/resolved-verdicts.txt && wc -l /tmp/resolved-verdicts.txt`. Also check the archived directory: `find bellows/verdicts/pending/archived -maxdepth 1 -type f -name "verdict-request-*-step-*.md" -newermt "2026-04-24" 2>/dev/null > /tmp/archived-requests.txt && wc -l /tmp/archived-requests.txt`. For each archived verdict request, read the file and extract the `**Pause Reason Code:**` field. Build a count table: pause reason code | count. Specifically count how many had `gate_failure` or `scope_violation` as their pause reason — these are the plans where gate evaluation produced a blocking failure. **(3) Cross-reference Mode A: agent-moved-to-Done despite gate failure.** This is the agent-side bug — Rule 8/23 violation. For each `gate_failure` or `scope_violation` verdict request identified in step 2, extract the plan slug from the verdict request filename (pattern: `verdict-request-{slug}-step-{N}.md` → slug). Then check whether the corresponding plan file exists in any `Done/` directory. Cross-reference logic: a plan that tripped a gate but is also in Done/ is a Mode A reproduction IF the timestamp of the Done/ file predates the timestamp of the resolved verdict file. If the Planner's terminal Done/ move (per Rule 25's resolution path) happened AFTER the verdict was resolved, that's correct behavior, not Failure 3. Use file mtime (`stat -f %m` or `os.path.getmtime`) for the comparison. Build a table: plan slug | gate failure type | Done/ mtime | verdict resolved mtime | violation? (yes/no — yes if Done predates verdict resolution by more than ~5 seconds buffer). **(4) Cross-reference Mode B: Bellows-moved-to-Done before own gate evaluation.** This is the Bellows-side bug. Mode B fires when Bellows moves a plan to Done in response to a continue verdict, but the gate evaluation for the verdict-triggering step was incomplete or contradicted by a later gate result. This is harder to detect from artifacts alone because Bellows's gate evaluation timestamps are not exposed in plan filenames or verdict files. Best proxy: look for plans in Done/ where the corresponding step log in `bellows/logs/` shows gate failures POST-DATING the Done/ move timestamp. For each plan in the Done/ population (step 1), extract the slug, then `grep -l "{slug}" bellows/logs/*.json | sort | tail -5` to find the most recent step logs for that plan. Read each log's `parsed.gate_result.failures` field. If any log has `failures` non-empty AND the log's timestamp post-dates the Done/ file's mtime, that's a Mode B candidate. This is a sampling check — do NOT iterate over the full population (~50-100 plans × log read each is too expensive). Sample the 10 most recent Done/ plans across all projects. Build a table: plan slug | most recent gate failure log timestamp | Done/ mtime | violation? (yes/no). **(5) Sanity check: verify the disable-auto-close model is actually applied.** Read `bellows/config.json` and confirm the `auto_close` field is `false` (or absent, defaulting to false). Quote the relevant config line in findings. If `auto_close` is `true` for any project, that's a separate finding worth flagging — the structural fix isn't applied. **(6) Synthesize findings into four answered questions.** Question A: how many plans shipped to Done/ in the audit window (2026-04-24 to 2026-05-05), aggregated across all 8 projects? Question B: how many had `gate_failure` or `scope_violation` verdict requests in the same window? Question C: of those, how many showed Mode A violations (Done/ predates verdict resolution — agent moved to Done before gate-failure verdict was resolved)? Question D: of the sampled 10 most recent plans, how many showed Mode B violations (gate failure log post-dates Done/ mtime — Bellows moved to Done before its own gate evaluation completed)? Question E: is the disable-auto-close model actually applied per `config.json`? **Deposit findings to `bellows/knowledge/research/failure-3-population-audit-findings-2026-05-05.md`** using the canonical Python file write pattern: `with open("bellows/knowledge/research/failure-3-population-audit-findings-2026-05-05.md", "w") as f: f.write(content)` where content is a triple-quoted string defined before the open call. The file structure: header (date, agent, plan reference), Investigation Section 1 (population scope with per-project counts), Investigation Section 2 (gate-failure verdict counts with pause reason breakdown), Investigation Section 3 (Mode A cross-reference table), Investigation Section 4 (Mode B sampling table), Investigation Section 5 (config.json sanity check), Findings (the five answered questions A–E), Output Receipt (canonical format). **Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.** **Final commit:** `git add bellows/knowledge/research/failure-3-population-audit-findings-2026-05-05.md bellows/knowledge/research/agent-prompt-feedback.md && git --no-pager log -1 && git commit -m "diag: failure 3 population audit findings"`. **STOP. Do NOT proceed beyond Step 1. Do NOT move the plan to Done. Wait for CEO confirmation.**
>
> **Deposits:**
> - `bellows/knowledge/research/failure-3-population-audit-findings-2026-05-05.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
