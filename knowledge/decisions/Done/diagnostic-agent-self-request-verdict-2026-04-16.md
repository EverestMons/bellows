# Bellows — Agent Self-Request Verdict Mechanism Fix
**Date:** 2026-04-16 | **Type:** diagnostic (investigation only, no fixes)

## How to Run This Plan

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-agent-self-request-verdict-2026-04-16.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further or move the plan to Done.
```

---
---

## STEP 1 — INVESTIGATION AGENT (bellows developer)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-agent-self-request-verdict-2026-04-16.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-agent-self-request-verdict-2026-04-16.md")`. Skip specialist file and glossary reads — pure code-tracing. You are investigating how to fix the broken agent self-request verdict mechanism so an executing Claude Code agent can signal "pause for Planner review" without needing to know the verdict filename convention. Do NOT write code or modify anything. **Background:** `gates.py` has a `_verdict_requested` check that scans `verdicts/pending/` for a file the agent supposedly deposits during execution. It's broken (filename prefix mismatch + slug mismatch — see `bellows/knowledge/research/resume-path-pending-cleanup-diagnostic-2026-04-16.md` Q6 ancillary finding). The Planner's design preference is: instead of fixing the file-based mechanism, have the agent signal through its structured output (the parsed dict from `runner.run_step`) and let Bellows/gates detect the signal without filesystem coordination. **Q1 — Trace the parsed output flow.** Read `runner.py` in full. What fields does `run_step` return in its parsed dict? Read `parser.py` in full — what does the parser extract from Claude Code's output? Show the complete set of keys in the returned dict. The Planner needs to know if there's an existing field (like a flag in the output receipt or a special marker) that could carry an "I want a verdict" signal, or if a new field needs to be added. **Q2 — Trace how `gates.check` receives and uses the parsed dict.** Read `gates.py` in full. Which fields from the parsed dict does `gates.check` currently consume? Show the `_verdict_requested` function in full — what does it check today, and what does it return? How does its return value flow into the gate_result dict that `run_plan` uses for pause decisions? **Q3 — Design the signal mechanism.** The Planner's hypothesis: the agent writes a specific marker string in its conversation output (e.g., `VERDICT_REQUESTED: <reason>`) and the parser extracts it into `parsed["verdict_requested"] = {"requested": True, "reason": "..."}`. Then `_verdict_requested` in gates.py checks `parsed["verdict_requested"]["requested"]` instead of scanning the filesystem. Evaluate this approach: (a) Is it viable given how `runner.run_step` captures Claude Code output? Does the parser have access to the full conversation text or only structured fields? (b) What's the minimal change to `parser.py` to extract a `VERDICT_REQUESTED:` marker from the output? (c) What's the minimal change to `gates.py` to check the parsed field instead of the filesystem? (d) Is there a better signal mechanism you see in the code? For example, does Claude Code's output already have a structured field (like tool results or status codes) that would be more reliable than a text marker? **Q4 — What happens to the dead file-based code?** Once the signal goes through the parsed dict, the filesystem-scanning code in `_verdict_requested` becomes dead. Should it be removed entirely, or kept as a fallback? List the exact lines in `gates.py` that would be removed or replaced. **Q5 — How would the agent know to use this?** The agent learns its behavior from the plan prompt. For this to work, plan prompts that want to enable agent-initiated pauses would need to include an instruction like "If you encounter [condition], output `VERDICT_REQUESTED: [reason]` on its own line." Is there a better way to teach the agent, or is a prompt instruction sufficient? Are there any CLAUDE.md or plan-header mechanisms that could make this automatic? **Deposit:** write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/agent-self-request-verdict-diagnostic-2026-04-16.md`. Verify you answered ALL FIVE questions before marking Status=Complete. **Commit:** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/agent-self-request-verdict-diagnostic-2026-04-16.md && git commit -m "docs: diagnostic — agent self-request verdict mechanism"`. **Feedback:** standard prompt feedback protocol → `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed. Do NOT move the plan to Done. Report completion and wait for CEO confirmation.**
