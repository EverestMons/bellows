# Bellows — Verdict Request File Schema Diagnostic
**Date:** 2026-04-18 | **Tier:** Small (Diagnostic) | **Test Scope:** n/a (diagnostic — no code changes) | **Execution:** Step 1 (Investigation Agent) → [Planner verification per Rule 22]

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 (investigation only — no code changes). After completing Step 1, the agent deposits findings and reports Complete via Output Receipt. The Planner then reads the deposited findings file (Rule 22 verification) and performs housekeeping directly. No Step 2.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bellows-verdict-file-schema-2026-04-18.md. Execute Step 1. After depositing findings and reporting Complete, STOP — do NOT move the plan to Done. The Planner will verify findings and handle housekeeping.
```

---
---

## STEP 1 — INVESTIGATION AGENT

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-bellows-verdict-file-schema-2026-04-18.md", "bellows/knowledge/decisions/in-progress-diagnostic-bellows-verdict-file-schema-2026-04-18.md")`. **Skip specialist file and glossary reads — this is a code-tracing + file-inspection task, no domain interpretation required.** Characterize the verdict request file format so the Planner can reliably parse verdict files to perform Rule 22 verification on Bellows-dispatched plans. Read `bellows/verdict.py` in full and trace `post_verdict_request()` (and any related functions that write verdict request files) to answer the five questions below. Then inspect actual verdict request files in `bellows/verdicts/pending/` and `bellows/verdicts/` (including any subdirectories like `resolved/`, `failed/`, or similar if they exist) to confirm the source matches what's actually on disk — sample at least 3 files if that many exist; if fewer than 3 exist, sample all of them; if zero exist, note that explicitly. Answer these five questions in the findings deposit: **(a) Field inventory** — what fields does `post_verdict_request()` (and any related writers) put into each verdict request file? List every field with its name, type, and whether it's always present or conditional. Include the filename convention itself as a field if the filename carries structured metadata. **(b) Plan-slug and step-number references** — does every verdict file reference the plan slug AND step number in a machine-parseable way? Where (filename, header field, embedded metadata, or multiple)? Is the reference unambiguous (one plan slug per file, one step number per file)? Is there any case where either is missing? **(c) Deposit file paths** — does the verdict file reference the deposit file path(s) the Planner should read to perform Rule 22 verification? If yes, in what field and what format (absolute path, relative path, list, single string)? If no, what field would the Planner use as a proxy to locate deposits (e.g., does it cite the plan file path, from which the Planner could read the plan to extract deposit paths)? **(d) Gate result visibility** — does the verdict file include the gate_result dict or a subset? If yes, what subset — specifically, does it tell the Planner (i) which gate triggered the verdict request (gate name), (ii) why the gate tripped (the gate's failure reason / message), (iii) any evidence the gate captured (grep results, diff output, command stdout)? **(e) Cleanup lifecycle** — trace the full lifecycle of a verdict request file: when is it created, when is it moved, renamed, or deleted, and by which code path? Specifically: does `pending/` get cleaned up on plan move-to-Done? On manual verdict approval? On reboot? Are stranded verdict files (e.g., the stale-verdict condition from the 2026-04-18 scope_check race) a known condition with any cleanup path, or do they accumulate indefinitely? Inspect `bellows/verdicts/pending/` right now and report how many files are there and how old they are. After answering the five questions, produce a **Proposed Schema** section that enumerates the required fields every verdict request file MUST contain for the Planner to rely on it for Rule 22 routing — with required/optional status per field, and for each required field a justification citing which question (a–e) motivates it. Then produce a **Gap Assessment** section that compares the current format against the proposed schema — for every proposed-required field that is currently missing or unreliably present, flag it and recommend what `verdict.py` would need to change. **Do NOT write any code changes in this plan.** If gaps exist, the recommendation is a follow-up executable the Planner will write after reviewing findings — not code deposited by this diagnostic. **Deposit:** write findings to `bellows/knowledge/research/verdict-file-schema-2026-04-18.md` using canonical Python file write pattern: define the full findings content as a triple-quoted string in a variable, then `with open("bellows/knowledge/research/verdict-file-schema-2026-04-18.md", "w") as f: f.write(content)`. Do NOT use heredoc syntax (`python3 << 'PYEOF'`) or `python3 -c` — these are banned per PLANNER_TEMPLATE Compression Principles. Include an Output Receipt at the end of the findings file per the template format. **Feedback:** standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT move the plan to Done. The Planner performs Rule 22 verification on the findings file and handles housekeeping directly.**
