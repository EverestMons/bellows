# Bellows — Duplicate Runs on Rescan Race Diagnostic
**Date:** 2026-04-16 | **Type:** diagnostic (investigation only, no fixes)

## How to Run This Plan

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bellows-rescan-duplicate-runs-2026-04-16.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further or move the plan to Done.
```

---
---

## STEP 1 — INVESTIGATION AGENT (bellows developer)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-rescan-duplicate-runs-2026-04-16.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-rescan-duplicate-runs-2026-04-16.md")`. Skip specialist file and glossary reads — pure code-tracing. **Context:** Today's session had two confirmed duplicate runs: (1) Bellows re-ran the Approach A viability diagnostic after it had already completed and been verdicted — the original was in `verdict-pending-` state when `_consume_verdicts` resolved it and moved it back to a runnable state, but `_rescan` then detected the unclaimed original file still in the directory and dispatched it again. (2) The notifier timeout plan was dispatched twice for similar reasons. Both duplicate runs were no-ops (code already committed), but they wasted ~$0.50-1.00 in API costs each. **Q1 — Trace the `_seen` set lifecycle.** `PlanHandler` has a `self._seen` set. (a) When is a path added to `_seen`? Show the exact lines in `_handle`. (b) Does `_seen` persist across rescans, or is it reset? (c) When `_consume_verdicts` moves a plan from `verdict-pending-` to `in-progress-` (continue verdict, non-final step), does the new `in-progress-` path get added to `_seen`? Or is it a "new" path that `_handle` will pick up? (d) When `_consume_verdicts` moves a plan to `Done/` (continue-to-done), is the Done path ever scanned? **Q2 — Trace the rescan-vs-verdict-consumption race.** The 30-second rescan timer calls `_rescan`, which calls `_consume_verdicts()` FIRST, then scans all watched directories for runnable plans. (a) Show this ordering in the code. (b) If `_consume_verdicts` successfully moves a `verdict-pending-` plan to Done in a single `_rescan` call, can the directory scan that follows possibly pick up the same file? (The file is in Done, not in the parent directory.) (c) If `_consume_verdicts` moves `verdict-pending-` to `in-progress-` AND calls `handle_new_plan`, and THEN the directory scan runs and also finds `in-progress-`, does `_seen` prevent the double-dispatch? **Q3 — What about the watchdog `on_created`/`on_modified` path?** The filesystem watcher fires independently of the rescan timer. (a) When `_consume_verdicts` does `shutil.move(verdict-pending → in-progress)`, does that rename trigger `on_created` or `on_modified` on the `in-progress-` file? (b) If yes, does `_handle` pick it up as a new plan (double dispatch with the one already started by `_consume_verdicts`)? (c) Does `_seen` protect against this? The `_consume_verdicts` path calls `handle_new_plan` which adds to `_seen` — but does it add BEFORE or AFTER the watchdog fires? **Q4 — What happened in today's concrete cases?** Reconstruct the sequence for the Approach A diagnostic duplicate run: (a) the Planner wrote the verdict file to `bellows/verdicts/resolved/`, (b) on the next rescan, `_consume_verdicts` found the verdict, moved the plan from `verdict-pending-` to Done (it was a final-step continue), (c) but somehow the plan got re-dispatched. Was it because the plan had ALSO been left as an unclaimed copy in `decisions/` (from the race condition earlier in the session where multi-chunk writes created orphan files)? Or was it a genuine `_seen` / rescan race? Check the bellows.db `runs` table: `SELECT plan_path, step, status, timestamp FROM runs WHERE plan_path LIKE '%approach-a%' ORDER BY timestamp`. **Q5 — Propose fixes.** (a) Should `_consume_verdicts` add the new path to `PlanHandler._seen` after moving a plan to `in-progress-`? This requires `_consume_verdicts` to have a reference to the handler. (b) Should `_rescan` skip files that are in `in-progress-` or `verdict-pending-` state (they're already being handled by the verdict system)? Currently `is_runnable_plan` filters these — verify: does `is_runnable_plan("in-progress-executable-foo.md")` return True or False? Does `is_runnable_plan("verdict-pending-executable-foo.md")` return True or False? (c) Should the watchdog be temporarily paused during `_consume_verdicts`'s file moves to prevent race-triggered re-dispatch? (d) Which fix is simplest and most reliable? **Deposit:** write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/rescan-duplicate-runs-diagnostic-2026-04-16.md`. Verify you answered ALL FIVE questions before marking Status=Complete. **Commit:** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/rescan-duplicate-runs-diagnostic-2026-04-16.md && git commit -m "docs: diagnostic — rescan duplicate runs race condition"`. **Feedback:** standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed. Do NOT move the plan to Done. Report completion and wait for CEO confirmation.**
