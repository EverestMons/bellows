# Bellows — Id-Threading + Lifecycle Mint v2 (Reporting Phase 1, Executable A)
**Date:** 2026-06-11 | **Tier:** Large | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always

**Execution Map:** Step 1 (DEV) → Step 2 (QA) — strictly sequential, no parallel steps.

## Context (Rule 27)
Implements the approved blueprint `bellows/knowledge/architecture/lifecycle-db-id-threading-blueprint-2026-06-11.md` (Reporting Phase 1, Executable A of the A→B split; see also `reporting-phase-0-coverage-map-2026-06-10.md` at governance root). Scope of A: (1) NEW `lifecycle.py` carrying the MINIMAL lifecycle-DB subset — `id_sequence` + `plans` tables only (blueprint Section 3.4 DDL for those two tables; the other six tables are Executable B, do NOT create them) — with a `mint_and_claim()` that mints the global-monotonic id and writes the initial plans row in ONE `BEGIN IMMEDIATE` transaction, DB at `<bellows_root>/lifecycle.db` resolved via `resolve_bellows_root()` (NEVER `Path(__file__).parent`); (2) claim-path change in `bellows.py` (~395-414): after `validate_at_claim` passes, mint the id, then a SINGLE rename from the deposit filename (any accepted format — descriptive slug, date-slug, or `<type>-draft-<HHMMSS>.md` placeholder) to `in-progress-<type>-<id>.md`, storing the original deposit filename in `plans.deposit_placeholder_name`; type is parsed from the deposit filename prefix (`diagnostic-`/`executable-`/`qa-`); title from the plan's `# Title` line; (3) fix the two latent substring-match bugs that become ACTIVE with bare-integer slugs — `plan_slug in pname` at bellows.py:1424 and the Done/halted stale-verdict scan at bellows.py:1521-1538 — both convert to exact slug extraction + `==` comparison, correct for BOTH legacy and id-only formats; (4) commit-message id-tagging: append a `Tag all commits with [<id>] in the commit message.` instruction to the bootstrap prompts at bellows.py:431-438 and the next-step prompt at :553; include `[<id>]` in the daemon auto-stage commit message at :943-953 and replace `--no-edit` with an explicit `[<id>]`-bearing message on the `--no-ff` merge at :1049-1051 (the `--ff-only` path produces no commit — leave it); thread `plan_id` through `run_plan()` → `_teardown_worktree()` for this; (5) startup recovery scan for half-claimed plans (blueprint 2.4a): on daemon start, for each `plans` row with `lifecycle_state='claimed'` whose `in-progress-<type>-<id>.md` is absent, re-execute the rename if `deposit_placeholder_name` still exists on disk, else mark the row `abandoned`. DUAL-FORMAT TOLERANCE is a hard requirement: every parse site must continue to accept legacy slug+date names (all of `Done/`, `halted-*`, resolved verdicts) — NO migration of historical artifacts. The new code activates only at daemon restart; this plan itself runs under the PRE-fix daemon with legacy naming — that is expected and correct. Layer 1 only: the mint and rename are mechanical, no domain judgment.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file first, then the blueprint `bellows/knowledge/architecture/lifecycle-db-id-threading-blueprint-2026-06-11.md` in full — it is the design; this step implements blueprint Sections 1 (edit-required rows only), 2, and 4, plus the minimal DDL subset from 3.4. **Rule 39 — before editing each site, re-verify the blueprint's Verification Blocks V1-V10 against current code** (run each query; if any expected output does not match, halt and report the discrepancy in your dev log rather than editing on a stale claim). **Allowed files (scope — exactly these, no others):** `bellows.py`, `lifecycle.py` (NEW), `tests/test_lifecycle.py` (NEW), `tests/conftest.py` (authorized change: add an autouse `isolate_lifecycle_db` fixture monkeypatching `lifecycle.LIFECYCLE_DB_PATH` to a tmp_path DB and initializing it, mirroring the existing `isolate_verdicts_dir`/`isolate_runner_logs_dir` fixtures — so no test ever touches the production `lifecycle.db`), and existing tests whose assertions encode the legacy naming or the substring-match behavior: `tests/test_bellows.py`, `tests/test_consume_verdicts.py`, `tests/test_cleanup_verdicts.py`, `tests/test_misplaced_verdicts.py`, `tests/test_verdict.py`, `tests/test_worktree.py`, `tests/test_validators.py`, `tests/test_gates.py`. If a file outside this set needs modification, halt and report — do not edit it. **Work items, in order:** **(1) `lifecycle.py` (NEW):** module-level root via `from bellows_root import resolve_bellows_root`; `LIFECYCLE_DB_PATH = resolve_bellows_root() / "lifecycle.db"`; `init_lifecycle_db()` creating ONLY `id_sequence` (single-row counter, seeded `next_id=1`) and `plans` (blueprint 3.4 columns incl. `deposit_placeholder_name`, `lifecycle_state` CHECK incl. `abandoned`) — idempotent `CREATE TABLE IF NOT EXISTS`, WAL mode; `mint_and_claim(plan_type, target_project, title, dispatch_mode, tier, total_steps, deposit_placeholder_name) -> int` executing the mint (`UPDATE id_sequence SET next_id = next_id + 1 RETURNING next_id - 1`, SQLite ≥3.35) AND the `plans` INSERT in one `BEGIN IMMEDIATE ... COMMIT`; `mark_plan_state(plan_id, state, closed_at=None)`; `recover_half_claimed(decisions_dir) -> list` implementing blueprint 2.4a. **(2) `bellows.py` claim path:** call `init_lifecycle_db()` once at daemon startup (adjacent to the existing `migrate_db()` at ~:159); in the claim sequence after `validate_at_claim` passes, parse type from the deposit filename prefix and title from the plan text's `# Title` line, call `mint_and_claim(...)`, then perform the SINGLE rename deposit-name → `in-progress-<type>-<id>.md` (replacing the current `in-progress-{base_filename}` construction at ~:411 for the claim site ONLY — the downstream lifecycle prefix renames at :449/:540/:636/:667/:677-685/:398/:1442/:1498 already operate on `base_filename` and need no edit since `base_filename` is now `<type>-<id>.md`); `_write_shadow` with the new canonical name. **(3) Substring fixes:** at ~:1424 replace `plan_slug in pname` with exact comparison (strip `verdict-pending-` prefix from `pname`, derive its slug via `verdict.slug_from_path`, compare `==`); at ~:1521-1538 same conversion for both the Done/ scan and the `halted-` scan. Both fixes must hold for legacy AND id-only slugs. **(4) Commit-message id-tagging:** bootstrap prompts at ~:431-438 and ~:553 gain the tag instruction with the actual minted id; auto-stage commit message at ~:943-953 gains `[<id>]`; `--no-ff` merge at ~:1049-1051 switches from `--no-edit` to an explicit message `Merge branch '<branch_name>' [<id>]`; thread `plan_id` through `run_plan()` and `_teardown_worktree()` signatures as needed. **(5) Startup recovery:** wire `recover_half_claimed()` into daemon startup after `init_lifecycle_db()`; log each recovery action. **(6) Tests — `tests/test_lifecycle.py` (NEW):** mint monotonicity (sequential mints return consecutive ids), transaction atomicity (failed plans-INSERT does not consume an id), `recover_half_claimed` both branches (placeholder present → re-rename; absent → `abandoned`), DB path resolves under the canonical root. **Regression tests:** the two substring fixes get explicit cases proving `142` does NOT match `1423` plans in verdict consumption and stale-scan, AND legacy slugs still match exactly; claim-rename test proving `diagnostic-draft-143022.md` and `diagnostic-foo-bar-2026-06-10.md` BOTH claim to `in-progress-diagnostic-<id>.md`; existing tests asserting legacy claim naming updated to the new contract (list every such edit in your dev log with file:line and why). **(7) FULL suite:** run `pytest tests/ --tb=line` to a pass/fail result, write the complete output to `bellows/knowledge/development/pytest_full_id_threading.txt`, and read the TAIL yourself — do not infer green from a collect count; report exact pass/fail/pre-existing counts in your dev log. Self-verify: in a scratch python session against a temp DB, demonstrate two sequential `mint_and_claim` calls returning consecutive ids and the plans rows landing. Write a dev log covering every work item with file:line of each change. Use `with open()` for deposits; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/id-threading-lifecycle-mint-dev-log-2026-06-11.md`
> - `bellows/knowledge/development/pytest_full_id_threading.txt`

---
---

## STEP 2 — Bellows QA

---

> **MANDATORY — READ THIS FIRST. This step is gate-enforced by the daemon's `rule_20_self_check` gate.** The gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` followed by `PASSED — SELF-CHECK PASSED`. Your verification table does NOT satisfy this gate — the banner must appear verbatim in the report. Before finishing, self-grep your deposited report for the exact string `Rule 20 — QA Self-Check Results` and confirm it is present; if it is not, you have not finished. **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file, the blueprint `bellows/knowledge/architecture/lifecycle-db-id-threading-blueprint-2026-06-11.md`, and the DEV deposits `bellows/knowledge/development/id-threading-lifecycle-mint-dev-log-2026-06-11.md` + `bellows/knowledge/development/pytest_full_id_threading.txt`. **Verify, each with executed evidence (commands + output captured into the report):** (1) **Mint atomicity + monotonicity** — in a scratch python session against a TEMP copy of the DB schema (never the live `lifecycle.db`), run 3 sequential `mint_and_claim` calls: ids are consecutive integers; force a plans-INSERT failure mid-transaction and prove `next_id` did not advance. (2) **Claim-rename contract** — unit-level: both `diagnostic-draft-143022.md` and `diagnostic-legacy-name-2026-06-10.md` shapes resolve to `in-progress-diagnostic-<id>.md`; confirm the downstream prefix renames (verdict-pending, Done/, halted-) operate correctly on the id-canonical `base_filename` by running the existing lifecycle-rename tests. (3) **Substring fixes** — run the new regression tests proving `142` does not match `1423` in verdict consumption (bellows.py:~1424 site) and the Done/halted stale scan (~1521-1538 site), and that legacy slugs still match exactly. (4) **Dual-format tolerance** — `is_runnable_plan`, `slug_from_path`, `slug_for`, and the verdict/orphan-guard regexes accept both `diagnostic-142.md`-style and legacy `diagnostic-foo-2026-06-10.md`-style names (cite the test or run a 6-case scratch check covering both formats through each function). (5) **Commit-tag plumbing** — grep `bellows.py` for the prompt-injection tag instruction, the `[`-bearing auto-stage message, and the explicit `--no-ff` merge message; confirm `plan_id` threads through `run_plan` → `_teardown_worktree` signatures. (6) **Recovery scan** — run the `recover_half_claimed` tests (both branches). (7) **FULL suite under a wall-clock bound** — run `timeout` (or `gtimeout`) wrapped `pytest tests/ --tb=line`, watched to completion; write output to `bellows/knowledge/qa/pytest_full_id_threading_qa.txt`; read the TAIL; report exact counts and reconcile against the DEV run's counts — any NEW failure vs the DEV run is a FAIL finding. (8) **Executable-B boundary** — confirm `lifecycle.db` schema contains ONLY `id_sequence` and `plans` (no steps/commits/deposits/verdicts/gate_events tables) — A must not pre-build B. Write the QA report with a verification table (item | method | evidence | PASS/FAIL), the reconciled suite counts, and the Rule 20 self-check banner block. **Deposits:**
> - `bellows/knowledge/qa/id-threading-lifecycle-mint-qa-report-2026-06-11.md`
> - `bellows/knowledge/qa/pytest_full_id_threading_qa.txt`
