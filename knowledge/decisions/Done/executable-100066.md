# bellows — THE ALIGN HOOK INJECTS `STANDING_CONSTRAINTS.md` AS TEXT AT EVERY SessionStart: the nine constraints under a `## Standing constraints` heading, capped at 40 lines, fail-open to the doctrine pointer when the file is absent — a prompt for the session's middle, never a gate on the reading (thread 267)

**Date:** 2026-09-10 | **Project:** bellows | **Tier:** Small (one hook gains a reader and a pure composer, one test file gains nine tests, one mutation manifest, one dev-log) | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** full-suite (Rule 21 — the hook shares `_common.py` with four others and its tests run under two interpreters; `pytest tests/ -q`) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 267

**auto_close:** false

**Post-close:** no restart (the hook is loaded by the harness at every SessionStart through the `~/.claude/eluvian` symlink into the bellows checkout — live at the merge the daemon performs after the QA step's gates, right before `awaiting_verdict` (`bellows.py:1255–1295`; MACHINE_SETUP v1.8's rule). One governance line for the CEO's word at close: MACHINE_SETUP.md's align-hook sentence (line 66's family) gains "and injects `STANDING_CONSTRAINTS.md`'s text under `## Standing constraints`" as v1.9 — the 248/v2.35 pattern. STANDING_CONSTRAINTS.md itself is v1.0 and is NOT edited by this plan; a rider edits the file, never the hook (the CEO's shape ruling, thread 267).

**Depends on:** the CEO's ruling of 2026-09-10 (session d04ebd33): "Approve the nine", "One governance file, every SessionStart", the order 265 → 266 → 267 → 264; `STANDING_CONSTRAINTS.md` v1.0 (governance `bfe41d9a`); bellows #100062 (Done 2026-09-10, thread 259 — the five hooks share `hooks/eluvian/_common.py`; `emit` and `hooklog` are its; the harness-interpreter canary is its QA's). No W-W with the plans in flight or waiting (#100064 and the 266 draft write `scripts/` and `tests/test_lens_commit.py`; this plan writes the hook and `tests/test_align_hook_sync.py`). Clone origin by kind: `Done/executable-100062.md` (the same hook family, the same test discipline under `/usr/bin/python3`); by layout: the closed 270 draft.

**Tier computed (§1):** **T1** — T-3 fires (the hook runs under the harness interpreter `/usr/bin/python3` 3.9.6 through a symlink, never under the venv the tests use — #100062's E-seat measured the split) and T-1 fires (a hook and a test file); §1's tier lines make T-1/T-3 a T1 — T2 is earned only by T-5, T-6 or the every-row clause, none of which fires (T-6 does not: the hook injects a prompt, no gate reads it, and the constraints file is doctrine the CEO already ruled). The v0 draft derived T2 from T-3 in error (scout S5); the cold SCOUT seat convened at walk 0 under that error stands as a declared act — its nine findings folded before lens 1 — and no further seat is owed.

## CEO Context

The session's entry and exit are mechanical (the align and debt hooks at SessionStart, the wrap lock at Stop); the daemon gates every dispatched plan; the drafting close and the lens commits run through tools. The session's MIDDLE rests on the Planner reading PLANNER_TEMPLATE and DRAFTING_CYCLE, and doctrine forbids turning that reading into a gate (DC v2.18/v2.20 — "never turn lenses into gates" is the CEO's gloss on those rows, not a DC sentence; scout S8). On 2026-09-10 the CEO ruled what can be done without a gate: the nine standing constraints, approved verbatim, live in ONE governance file, `STANDING_CONSTRAINTS.md` (v1.0, committed), and the align hook — which today injects a doctrine POINTER, the daemon's status, the parked count and the sync line (P2) — injects that file's text at every SessionStart, so a session that never opens the doctrine still meets the rules in its first context. This plan is the hook half: a reader for the file under the governance root the hook already resolves, a `## Standing constraints` section capped at 40 lines, fail-open to the pointer when the file is absent or unreadable (a hook must never crash a session — the hook's own `__main__` guard, P2), and tests that load the module by path under BOTH interpreters (P3). Not a gate: nothing checks that a walk passed because the text was injected — the line thread 267 says this plan must not cross.

## What this changes

1. **`hooks/eluvian/eluvian_align_hook.py`** — two additions and one seam. (a) `_STANDING_FILE = "STANDING_CONSTRAINTS.md"` and `_STANDING_MAX_LINES = 40` beside the other module constants (`:22`'s neighbourhood). (b) `def _standing_constraints(root)` returning a `str` or `None` — UNANNOTATED (the module imports nothing from `typing`; its `from __future__ import annotations` is present, but the plain signature is the 3.9-safe choice, P4): reads `root / _STANDING_FILE` with `encoding="utf-8"`; returns `None` on any `OSError`/`UnicodeDecodeError` (absent, unreadable, a directory — one `except Exception:  # fail-open (standing constraints)`, the hook's fail-open idiom, the comment making the line a unique mutant anchor); otherwise takes the lines AFTER the first line that equals `## Standing constraints` (the file's own heading, P5) up to the end of file or the next `## `, strips leading and trailing blank lines, and returns `"## Standing constraints\n" + "\n".join(lines[:_STANDING_MAX_LINES])`, appending `\n… ({n} more lines not shown)` when the section exceeds the cap; a file without the heading returns `None`. (c) `def _compose_context(doctrine, daemon, parked, sync_line, constraints)` → `str`, the PURE composer of what `main()` builds today (P2 `:172–190`): the doctrine line, the daemon line, the parked line when non-zero, the sync line, then — when `constraints` is not `None` — a blank line and the constraints text, else the line `Standing constraints: STANDING_CONSTRAINTS.md not found under the governance root — read PLANNER_TEMPLATE and DRAFTING_CYCLE before acting`, then `Type /eluvian for the full alignment pass.`; `main()` computes `constraints = _standing_constraints(_GOV_ROOT)`, calls the composer, logs `constraints=<line count or absent>` in its `hooklog` line, and `emit`s — the module's `__main__` fail-open guard and every other function untouched.
2. **`tests/test_align_hook_sync.py`** — nine tests, failing-first, loading the module by path the way `tests/test_hook_default_root.py` does (`importlib.util.spec_from_file_location` on `hooks/eluvian/eluvian_align_hook.py` with `hooks/eluvian` on `sys.path` so `from _common import …` resolves — #100062's D-seat lesson S1): **a1** a tmp root holding a SYNTHETIC nine-line file in v1.0's exact shape (a literal in the test: the title line, the `**Version:**` line, a blank, `## Standing constraints`, a blank, nine numbered lines the first of which begins `1. One commit per lens`) → `_standing_constraints(root)` starts with `## Standing constraints\n`, contains that first line, and has no `… (` truncation line; **a1b** the REAL file when present (`$ELUVIAN_WRAP_ROOT/STANDING_CONSTRAINTS.md`, else `_common._default_root()`'s; skipped with a reason otherwise — a bellows test must not require a governance file) → the same three asserts; **a2** an empty tmp root → `None`, and a root where the path is a DIRECTORY → `None` (fail-open, no raise); **a3** a synthetic file whose section is 60 numbered lines → exactly 40 numbered lines plus the line `… (20 more lines not shown)`; **a4** `_compose_context("D", "S", 0, "Sync: core repos current.", "## Standing constraints\n1. x")` → the doctrine line first, the constraints text present, `Type /eluvian` last, no `Parked` line; **a5** `_compose_context(…, constraints=None)` → the `not found under the governance root` line present, `## Standing constraints` absent. **a6** (the harness interpreter, #100062's canary): `subprocess.run(["/usr/bin/python3", "-c", "<load the module by path; print(_standing_constraints(Path(root)) is not None)>"])` on the a1 root → `True` (skipped with a reason when `/usr/bin/python3` is absent). **a7 / a8** (the INJECTED context, thread 267's own ask — scout S4; #100062's s5 shape): subprocess-run the hook file under `sys.executable` with `{}` on stdin and the isolation env (`ELUVIAN_WRAP_ROOT` → a `tmp_path` root holding a copy of the nine-line file and a `bellows/status.py` stub that prints one line; `ELUVIAN_WRAP_LESSONS_FORGE`, `ELUVIAN_WRAP_TUYERE` → `git init`ed tmp dirs; `ELUVIAN_WRAP_MEMORY` → a tmp dir; `ELUVIAN_HOOKS_LOG` → `tmp_path/hooks.log`) — the stub and the dirs must exist BEFORE the subprocess starts — `_STATUS_PY` and `_GOV_ROOT` are computed at import time (`:22–32`); a7: `json.loads(stdout)["hookSpecificOutput"]["additionalContext"]` contains `## Standing constraints` and `1. One commit per lens`; a8: the file removed → the context contains `not found under the governance root` and not the heading; both rc 0, and `tmp_path/hooks.log` exists (the run logged there, nowhere else). The six existing sync tests stay green (they touch `_repo_sync` only).
3. **`knowledge/mutants/align-hook-standing-constraints.json`** — at least six mutants (`target: hooks/eluvian/eluvian_align_hook.py`; every `expect_fail` a plain `tests/test_align_hook_sync.py::<test>` node id; every anchor UNIQUE in the file — the reader's `except Exception:` carries the comment `# fail-open (standing constraints)` on its line so the anchor is count-1, scout S7): the cap removed (a3); the `except` re-raising (a2); the heading line dropped from the return (a1); the composer dropping the constraints argument (a4); the absent-file line dropped (a5); `constraints = None` at `main()`'s call site (a7).
4. **Not changed:** `_common.py`, the other four hooks, `STANDING_CONSTRAINTS.md`, `~/.claude/settings.json` (the hook is already wired at SessionStart, P6), the sync logic, the `__main__` guard. The diff touches two files plus the manifest, the run file and the dev-log.

## Why this exists

The CEO's question was whether the middle of a session can be ordered without a gate. The answer ruled: a prompt, from one file the CEO owns, in the first context of every session — the hook that already runs there carries it. Every constraint on the list was learned by breaking it (LESSONS 2026-08-08 through 2026-09-10); the file is the codified form and the hook is its delivery. A hook that crashed on a missing file would take the session's entry with it, so absence degrades to the pointer the hook already gives.

## What this does NOT do

- Does not check, count, or gate anything against the injected text — no verdict, no lint, no wrap step reads it (thread 267's line).
- Does not edit `STANDING_CONSTRAINTS.md` — riders edit the file (the CEO's), never the hook; the cap is a display bound, not an edit.
- Does not touch the other four hooks, `_common.py`, or the harness wiring.

## MUST-PRESERVE

- ⛔ **Fail-open.** No path through `_standing_constraints` or the composer raises; an absent or unreadable file yields the pointer line; the `__main__` guard stays.
- ⛔ **The file is the source.** The hook injects the file's text verbatim after its heading, up to the cap, never a copy of the list.
- ⛔ **Suite green at both commits;** nothing outside the two files moves.

## Numbers discipline — measured 2026-09-10 by the Planner (bellows `de7dd4f`, governance `ba3818d5`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the ruling | thread 267 (2026-09-10 13:10): the nine constraints, one file, the hook injects the text under `## Standing constraints` capped at ~40 lines, fail-open to the pointer, a test in `tests/test_align_hook_sync.py`; the CEO's answers in session d04ebd33: "Approve the nine", "One governance file, every SessionStart", "265 → 266 → 267 → 264" | `python -m tuyere.threads show 267` |
| P2 | ⛔ the hook as it stands | `hooks/eluvian/eluvian_align_hook.py` (205 lines, sha `208f29b8a6d2`, last writer `7afd777` #100062): `_GOV_ROOT = Path(os.environ.get("ELUVIAN_WRAP_ROOT") or _default_root())` `:22`; `_resolve_sibling` `:40`, `_sync_repos` `:63`, `_repo_sync` `:98`, `_daemon_status` `:127`, `_parked_count` `:146`; `main()` `:165–196` builds `parts` = the doctrine line `f"Eluvian doctrine: {_DOCTRINE}"`, `f"Daemon: {daemon}"`, `Parked arcs: N` when non-zero, the sync line (`⚠️ Sync: …` or `Sync: core repos current…`), `Type /eluvian for the full alignment pass.`, then `hooklog("SessionStart-align", …)` and `emit("\n".join(parts))`; the `__main__` guard `:199–205` prints `⚠️ eluvian_align_hook: internal error (FAIL-OPEN, continuing)` then `{}` and exits 0 on any exception; `_common.emit` `:53–63` wraps the text as `hookSpecificOutput.additionalContext` for `SessionStart` | `sed -n '22p;165,205p' hooks/eluvian/eluvian_align_hook.py`; `sed -n 53,63p hooks/eluvian/_common.py` |
| P3 | ⛔ the tests as they stand | `tests/test_align_hook_sync.py` (sha `551de3d3c762`): six tests on a bare-origin/clone `repo_pair` fixture (`test_current`, `test_behind`, `test_ahead`, `test_diverged`, `test_no_upstream`, `test_fetch_failed`), all through `_repo_sync` — none runs `main()` or reads the emitted context; `tests/test_hook_default_root.py` is the by-path loading pattern (`spec_from_file_location`, `hooks/eluvian` on `sys.path`); `tests/test_wrap_hooks.py:28` `_run_hook(script_name, payload, env_overrides)` runs a hook as a subprocess under `sys.executable` | `grep -n 'def test_\|def _' tests/test_align_hook_sync.py tests/test_hook_default_root.py` |
| P4 | ⛔ the two interpreters | the harness runs the hook as `/usr/bin/python3 /Users/marklehn/.claude/eluvian/eluvian_align_hook.py` (`~/.claude/settings.json` `hooks.SessionStart[1]`, timeout 30; `~/.claude/eluvian` → `bellows/hooks/eluvian`); `/usr/bin/python3` is 3.9.6 — runtime-evaluated syntax stays 3.9 (`Optional[...]` in the helper's signature is safe either way: the hook already carries `from __future__ import annotations`, scout S9; no `match`); the suite runs under the venv's 3.12; `~/.claude/settings.json`'s `env` block SETS `ELUVIAN_WRAP_ROOT` for the harness (scout, P4) — the unset-variable canary exercises the fallback, the harness the variable; #100062's canary: `env -u ELUVIAN_WRAP_ROOT /usr/bin/python3 -c "…spec_from_file_location…"` | `python3 -c "import json;print(json.load(open('$HOME/.claude/settings.json'))['hooks']['SessionStart'])"`; `/usr/bin/python3 --version` |
| P5 | ⛔ the file | `$ELUVIAN_WRAP_ROOT/STANDING_CONSTRAINTS.md` v1.0: 15 lines — a title line, a `**Version:** 1.0 (2026-09-10)` line, the heading `## Standing constraints`, nine numbered lines `1.`–`9.` each ending in a parenthesised source; line 1 begins `1. One commit per lens, the subject naming the lens`; committed at governance `bfe41d9a` | `wc -l STANDING_CONSTRAINTS.md`; `grep -n '^## \|^[1-9]\.' STANDING_CONSTRAINTS.md` |
| P6 | the wiring and the class | `~/.claude/settings.json` `SessionStart` runs `wrap_debt_hook.py` then `eluvian_align_hook.py` (P4); `hooks/README.md` does not mention the align hook by name (`grep -ic align hooks/README.md` → 0 — left as is); this plan writes `hooks/` and `tests/` → **shop-infra** (HOLDS, the CEO releases); in flight at drafting: #100064 (thread 265) — different files, no W-W | `python3 status.py` |
| P7 | the mutation contract | `tools/mutation_check.py`: manifest-driven, KILLED = exit 1 only, sandbox = `git archive HEAD`; run file `knowledge/mutants/<slug>.run.txt` ending `MUTATION: N killed, 0 survived, 0 error`; the hook mutants run the by-path tests only | `sed -n 1,22p tools/mutation_check.py` |

## ACID, stated

- **Atomicity:** after DEV's first commit (three files: the hook, the test file, the manifest) the tree is green; the hook goes live for every NEW session on this machine at the merge, which follows the QA step (not before it — the symlink reads the checkout); the second commit adds the run file and dev-log; QA adds two evidence files. A halt after the merge and before the verdict leaves the injection live — the intended end state, and fail-open by construction.
- **Consistency:** the file is the source (MUST-PRESERVE 2); the cap is a display bound (a3); absence degrades to the pointer (a2, a5); no consumer reads the injected text (What this does NOT do).
- **Isolation:** two steps, one worktree; no other plan writes the hook or its test file (P6); the QA runs the WORKTREE's file under the harness interpreter (the merge follows the QA step — scout S1), with the symlink's old code as its control; the harness reads the live checkout only after the merge.
- **Durability:** the run file, the dev-log with P2's cell, the QA receipt with the block's PASSED line and the canary's JSON, the redirected suite file.

## Drafting Cycle

**Tier:** **T1** — T-3, T-1 fire (v0 said T2 — corrected at the scout fold). **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-align-hook-standing-constraints-2026-09-10.md
**Walks:** walk 0 pinned (P1–P7 measured on bellows `de7dd4f`; clone-diff against `Done/executable-100062.md` (kind) and the closed 270 draft (layout) run: FACTS, ARTEFACTS, STRUCTURE; the standing-rules diff against bellows `CLAUDE.md`, STANDING_CONSTRAINTS v1.0 and DC v2.35; the cold SCOUT seat convened before lens 1 per §2.0); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py` — folding lenses by hand only while its gate refuses CONTINUE (thread 266's plan, closed and waiting), declared per commit; no lens or walk number in any `--desc` (thread 271).

**Cold scout (walk 0):** convened under the v0 draft's T2 derivation, corrected to T1 at its fold (seat-scout.md: 9 findings — 3 HIGH, 2 MED, 4 LOW; no direction-class; all nine folded before lens 1; ~155k tokens). No panel seats: T1.

- Weak spots:          w1 4 folded — instruction 3 / record 1 (the helper unannotated; a1 synthetic with a1b on the real file; MUST-PRESERVE 2's cap clause; the counts)
- Weak spots:          w2 dry
- Destruction:         w1 dry — three conditions measured and recorded (the stub path is the hook's first candidate; the log line has no consumer; the context size has no known limit, the cap bounds it)
- Destruction:         w2 dry
- Vulnerabilities:     w1 1 folded — instruction 1 / record 0 (import-time resolution: the isolation root is built before the subprocess)
- Vulnerabilities:     w2 dry
- Integration-record:  w1 dry (MACHINE_SETUP v1.9 for the CEO already in Post-close; hooks/README's silence on the align hook left as is; #100062's LESSON on ~/.claude carried into both steps)
- Integration-record:  w2 dry
- ACID:                w1 1 folded — instruction 0 / record 1 (the Isolation line's merged-tree claim, stale after S1)
- ACID:                w2 dry

**Closing:** WARM close after walk 2 — BAR MET (T1), thread 267's plan. Two walks: 6 (walk 1: the helper unannotated, a synthetic a1 with a1b on the real file, the cap clause, import-time resolution, the Isolation line; three Destruction conditions recorded) → 0 (walk 2 fully dry). Walk 0 carried a cold scout convened under the v0 draft's T2 derivation — corrected to T1 at its fold (§1: T-1/T-3 → T1), its nine findings folded before lens 1 (the merge follows QA, so the QA runs the worktree's file; #100062's isolation set restored; two tests of the injected context). Record form: walk 1 by hand (declared — `lens_commit.py` refuses CONTINUE until thread 266's plan ships), walk 2 by the tool under `--dry`; every commit touches the register; the tool run bare, its exit read. Deposit via `ready-` now (no write conflict with the plan in flight); HOLDS on class shop-infra, the CEO releases. Close acts: the CEO's MACHINE_SETUP v1.9 line (Post-close); the deposit-side observer run on a scratch copy before the receipt.

## Cycle Manifest
tier: T1
target: hooks/eluvian/eluvian_align_hook.py
class: shop-infra
reads: hooks/eluvian/eluvian_align_hook.py, hooks/eluvian/_common.py, tests/test_align_hook_sync.py, tests/test_hook_default_root.py, tests/test_wrap_hooks.py, tools/mutation_check.py, knowledge/decisions/Done/executable-100062.md, STANDING_CONSTRAINTS.md, MACHINE_SETUP.md, DRAFTING_CYCLE.md
writes: hooks/eluvian/eluvian_align_hook.py, tests/test_align_hook_sync.py, knowledge/mutants/align-hook-standing-constraints.json, knowledge/mutants/align-hook-standing-constraints.run.txt, knowledge/development/dev-log-align-hook-standing-constraints-2026-09-10.md, knowledge/qa/evidence/align-hook-standing-constraints-qa-receipt-2026-09-10.md, knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt
mutants: knowledge/mutants/align-hook-standing-constraints.json
open_forks: 1. the debt hook and the align hook sharing one composer (open fork 3 of #100062) — not this plan; 2. a per-machine constraints overlay — rejected at drafting: one file, the CEO's ruling; 3. thread 264 (`check_deposit.py`'s stage-aware flag) — next in the order
walks: 2
yields: 4, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:27
coherence: 2/2 body walks named in the register (16 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-align-hook-standing-constraints.md.foldcheck.json

---

## STEP 1 — DEV (one hook, nine tests; the mutation run; two commits)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** — except where an item names `/usr/bin/python3`, the harness interpreter. ⛔ Never run the daemon, `run_plan`, a claim; never import `lifecycle`. ⛔ Every deposit is written in THIS worktree (`"$(git rev-parse --show-toplevel)"`), never in the canonical checkout; the plan's own lane file lives in the canonical checkout at `/Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md` (`<id>` from the prompt). ⛔ Never write under `~/.claude` (the symlink reads the checkout; the sandbox denies it anyway — #100062's LESSON).
>
> **Scope:**
> - `hooks/eluvian/eluvian_align_hook.py`
> - `tests/test_align_hook_sync.py`
> - `knowledge/mutants/align-hook-standing-constraints.json`
> - `knowledge/mutants/align-hook-standing-constraints.run.txt`
> - `knowledge/development/dev-log-align-hook-standing-constraints-2026-09-10.md`
>
> **Item 1 — re-derive P2, P3, P4 and P5 and HALT on a mechanism mismatch** (P1, P6, P7 are records; a line-number drift is not a mismatch, a changed `main()` shape, a missing `## Standing constraints` heading in the file, or a harness interpreter other than 3.9 is). Paste `main()`, the `__main__` guard, `_common.emit`, the test file's six names, `/usr/bin/python3 --version`, and the file's heading and first numbered line, under the first declared heading.
> **Item 2 — write the failing tests FIRST:** *What this changes* 2 — a1–a6. Run the file: a1, a1b, a2–a6 red (the module has no `_standing_constraints`/`_compose_context` — `AttributeError`, say so), a7 red (no heading in the context), a8 GREEN before the edit (today's context lacks the heading; it pins the fail-open shape — say so), the six sync tests green. Paste under the second declared heading.
> **Item 3 — the edit** as *What this changes* 1 — 3.9-compatible syntax only (`Optional[str]`, no `match`, no `X | None`); then the file green (15 tests), then the FULL suite green (`N passed, 1 skipped` — `test_gate_watcher` skips in a worktree; a skip is not a failure). Then the canary under the HARNESS interpreter, from this worktree's own file: `env -u ELUVIAN_WRAP_ROOT /usr/bin/python3 -c "import importlib.util,sys,pathlib; sys.path.insert(0,'hooks/eluvian'); s=importlib.util.spec_from_file_location('h','hooks/eluvian/eluvian_align_hook.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); r=m._standing_constraints(m._GOV_ROOT); print(type(r).__name__, len((r or '').splitlines()))"` → `str 10` (the heading plus nine lines — `splitlines`, not a newline count, which is 9; scout S3); paste it.
> **Item 4 — first commit**, gated on the FULL suite AND the pre-check, path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && ( /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" 2>&1 || true ) | grep -E '^(FAIL|PRECHECK)' && git add hooks/eluvian/eluvian_align_hook.py tests/test_align_hook_sync.py knowledge/mutants/align-hook-standing-constraints.json && git commit -F <msg-file> -- hooks/eluvian/eluvian_align_hook.py tests/test_align_hook_sync.py knowledge/mutants/align-hook-standing-constraints.json` — three files (the manifest rides this commit so the mutation run's `HEAD:` archive contains it); the message tagged with the plan id and `thread 267`. (The dev-log and the run file do not exist yet: the pre-check reports `rule_22_verification` and `dev_log_declared_text` failures — expected at the FIRST commit; the paste MUST contain the `PRECHECK:` line; thread 264 is the stage-aware flag the tool lacks.)
> **Item 5 — the mutation run, REDIRECTED into `knowledge/mutants/align-hook-standing-constraints.run.txt` (a Deposit):** `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/align-hook-standing-constraints.json > knowledge/mutants/align-hook-standing-constraints.run.txt 2>&1`; the last line must read `MUTATION: <n> killed, 0 survived, 0 error` with n ≥ 6 — a survivor is a test to write, not a mutant to delete.
> **Item 6 — the dev-log** `knowledge/development/dev-log-align-hook-standing-constraints-2026-09-10.md` under the four headings declared below; `## Pins re-derived (P2, P3, P4, P5)` opens with P2's value cell pasted verbatim, whole, to its last character — ⛔ the VALUE cell (third column) ends with the words `` `hookSpecificOutput.additionalContext` for `SessionStart` ``; paste through those words, then stop. **Second commit**, gated on the FULL suite AND the run file AND the pre-check, path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && grep -Eq 'MUTATION: ([6-9]|[1-9][0-9]+) killed, 0 survived, 0 error' knowledge/mutants/align-hook-standing-constraints.run.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/mutants/align-hook-standing-constraints.run.txt knowledge/development/dev-log-align-hook-standing-constraints-2026-09-10.md && git commit -F <msg-file> -- knowledge/mutants/align-hook-standing-constraints.run.txt knowledge/development/dev-log-align-hook-standing-constraints-2026-09-10.md` — two files.
> **Headings:** `## Pins re-derived (P2, P3, P4, P5)`; `## Failing-first (eight red, one pinned, then green)`; `## The harness-interpreter canary (Item 3)`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P2, P3, P4, P5)` ← P2
>
> **Deposits:**
> - `hooks/eluvian/eluvian_align_hook.py`
> - `tests/test_align_hook_sync.py`
> - `knowledge/mutants/align-hook-standing-constraints.json`
> - `knowledge/mutants/align-hook-standing-constraints.run.txt`
> - `knowledge/development/dev-log-align-hook-standing-constraints-2026-09-10.md`
>
> **Post-conditions:** nine new tests green and the six prior green; the canary under `/usr/bin/python3` prints `str 10`; the mutation run's last line `n ≥ 6 killed, 0 survived, 0 error`; `git diff --stat main...HEAD` (three-dot) names exactly the five Scope files; the four declared headings present with P2's cell whole; `_common.py` and the other four hooks unchanged.

## STEP 2 — QA (full suite + the hook RUN live through the symlinked path under the harness interpreter, present and absent)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter ABSOLUTE except where `/usr/bin/python3` is named; read-only beyond the two evidence files. ⛔ Never write under `~/.claude`.
>
> **Item 1 — full suite, REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt 2>&1`; the summary line quoted in the receipt as `N passed` with the skip named as `one skip (test_gate_watcher, live-DB)` — never the word the Rule 20 block scans for.
> **Item 2 — the hook RUN under the harness interpreter, present (the worktree's file — the daemon merges AFTER this step's gates, right before `awaiting_verdict`, `bellows.py:1255–1295`, so the symlink still holds the OLD code during QA; scout S1):** build the isolation root #100062's QA used, so the run touches nothing live: `T=$(mktemp -d); mkdir -p $T/root/bellows $T/root/lf $T/root/mem $T/root/tuyere; cp "$ELUVIAN_WRAP_ROOT/STANDING_CONSTRAINTS.md" $T/root/; printf 'print("● stub")\n' > $T/root/bellows/status.py; git -C $T/root/lf init -q; git -C $T/root/tuyere init -q; ISO="ELUVIAN_WRAP_ROOT=$T/root ELUVIAN_WRAP_LESSONS_FORGE=$T/root/lf ELUVIAN_WRAP_TUYERE=$T/root/tuyere ELUVIAN_WRAP_MEMORY=$T/root/mem ELUVIAN_HOOKS_LOG=$T/hooks.log"`; then `echo '{}' | env $ISO /usr/bin/python3 hooks/eluvian/eluvian_align_hook.py > $T/present.json; echo "rc=$?"` → rc 0; `/usr/bin/python3 -c "import json;d=json.load(open('$T/present.json'));c=d['hookSpecificOutput']['additionalContext'];print('## Standing constraints' in c, '1. One commit per lens' in c, len(c.splitlines()))"` → `True True <n>`; paste the three values and the first constraint line as printed. Then the pre-merge CONTROL through the symlink, same `$ISO`: `echo '{}' | env $ISO /usr/bin/python3 /Users/marklehn/.claude/eluvian/eluvian_align_hook.py > $T/control.json` → rc 0 and `'## Standing constraints' in c` → `False` (the old code — #100062's QA labelled its canonical run the same way); paste.
> **Item 3 — the hook RUN, absent (fail-open):** `rm $T/root/STANDING_CONSTRAINTS.md`; `echo '{}' | env $ISO /usr/bin/python3 hooks/eluvian/eluvian_align_hook.py > $T/absent.json; echo "rc=$?"` → rc 0, valid JSON, the context carrying `not found under the governance root` and NOT `## Standing constraints`; `wc -l $T/hooks.log` → the two runs logged THERE and nowhere else (scout S2: without the isolation set the empty-root run resolves the CANONICAL `status.py`, fetches the canonical `bellows` and `forge_lessons`, and appends to `~/.claude/eluvian/hooks.log`); paste.
> **Item 4 — production writes, stated exactly:** none outside the two evidence files; nothing under `~/.claude` (`ls -la --time-style=full-iso /Users/marklehn/.claude/eluvian/hooks.log` before and after Items 2–3 — same mtime, pasted); no real repo fetched (the `$ISO` set); no lane file, no lifecycle row, no lifecycle import; every scratch dir removed (`[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`).
> **Item 5 — receipt** `knowledge/qa/evidence/align-hook-standing-constraints-qa-receipt-2026-09-10.md`: `numstat` over the DEV commits (`<base>..<dev>`, five files); a `## Verification` table with one row per Item 1–4, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`$ELUVIAN_WRAP_ROOT/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: align-hook-standing-constraints-2026-09-10`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["align-hook-standing-constraints-suite-2026-09-10.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the closing `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 6 — commit**, path-scoped and gated: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/align-hook-standing-constraints-qa-receipt-2026-09-10.md knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt && git commit -F <msg-file> -- knowledge/qa/evidence/align-hook-standing-constraints-qa-receipt-2026-09-10.md knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt`. ⛔ A QA step that finds it must change production code STOPS and requests a verdict (thread 262) rather than committing the change.
>
> **Deposits:**
> - `knowledge/qa/evidence/align-hook-standing-constraints-qa-receipt-2026-09-10.md`
> - `knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/align-hook-standing-constraints-qa-receipt-2026-09-10.md`
> - `knowledge/qa/evidence/align-hook-standing-constraints-suite-2026-09-10.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 1 skipped` with N ≥ the DEV's count and no `failed`; Item 2's present run pasted (`True True`, the first constraint line) beside the symlink control's `False`; Item 3's absent run pasted (rc 0, the pointer line, no heading) with the scratch log's line count; the live log's mtime unchanged; the receipt's `## Verification` table has four rows and closes with the block's own PASSED line; one QA commit carrying the two evidence files.
