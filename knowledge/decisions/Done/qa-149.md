# Bellows — QA re-verification: session-limit park + auto-resume (Rule 20 banner)
**Date:** 2026-07-09 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** full | **Execution:** Step 1 (QA) | **qa_steps:** 1 | **pause_for_verdict:** always

## CEO Context

The session-limit **park + auto-resume** feature (originally plan 148) shipped its code across two DEV steps — both passed clean gates and Planner Rule 22(b) review, and are committed on `main`:
- `f8d701b` — `runner.py`: `_check_session_limit` + `_parse_session_reset` (detect session-limit 429 result event on stdout, parse resets-at, distinct from the transient-retry guard).
- `da86b6a` — `bellows.py`: `_maybe_park_session_limit` at both run_step consumption sites, `parked_steps` DB table + `record_park`/`clear_park`, `_resume_parked` invoked from BOTH `_rescan` and the startup scan (restart-safe), `parked-` scan exclusions, deduped CEO notification. 26 tests total (`tests/test_session_limit_park.py`).

Plan 148's **Step 3 QA gate FAILED on `rule_20_self_check`** — the QA report omitted the mandatory `Rule 20 — QA Self-Check Results` banner. The QA *substance* was complete (6 checklist items PASS, full suite 788 passed / 0 regressions), but the report was non-conformant, so 148 was STOPPED (its DEV commits remain on main — nothing lost). This plan re-runs the QA verification and **requires the Rule 20 self-check banner explicitly** (the root-cause fix: 148's QA step never named it).

**Scope note:** this is verification-only — NO code changes. The code under test is already committed on `main`; QA reads it there.

**Deposit-once discipline:** this file was deposited exactly once. If a second copy appears, that is a claim-dedup bug — do not double-claim.

## How to Run This Plan

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/qa-session-limit-park-reverify-2026-07-09.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation.
```

---
---

## STEP 1 — QA

---

> **FIRST — before any reads or work: post a short visible message to chat (1-2 sentences) confirming you are starting this plan and your immediate next action.** Do NOT rename the plan file.
>
> You are Bellows QA. Read `bellows/agents/BELLOWS_QA.md` if present (skip with a note if absent). All commands run from `/Users/marklehn/Developer/GitHub/bellows`. **QA is verification + reporting only — no product-code changes.** If you find a blocker, STOP and report it (do not fix forward).
>
> **Scope:**
> - `knowledge/qa/session-limit-park-autoresume-qa-reverify-2026-07-09.md`
>
> **Verify the already-committed session-limit feature (commits `f8d701b` + `da86b6a` on `main`):**
> 1. **Detection precision.** `runner._check_session_limit` parks ONLY on `is_error` + `api_error_status==429` + ("session limit"|"usage limit") in the result text; a transient stderr `429`/`rate limit` still takes the once-only retry path unchanged; a `429` session-limit result with `num_turns>1` / cost>0 / output_tokens>0 does NOT park (no-progress guard). Confirm the tests in `tests/test_session_limit_park.py` cover all three.
> 2. **resets-at parse + tz.** `runner._parse_session_reset` maps `11:50pm (America/Chicago)` to the correct next-future epoch; alternate time forms parse; unparseable string falls back to ~now+5h with a WARN (not a crash, not a lost step).
> 3. **Restart safety.** Park is persisted in BOTH `bellows.db` (`parked_steps`) AND on disk (`parked-` prefix), and `_resume_parked` is invoked from BOTH `_rescan` (~bellows.py:1958) AND the startup scan (~bellows.py:2287). Confirm the resume threads `resume_step` through `handle_new_plan -> _run_tracked -> run_plan` (dispatches the parked step, not step 1).
> 4. **No-progress safety.** The "park only when the step made no progress" guard is present and tested.
> 5. **Scan exclusion.** `is_runnable_plan` excludes `parked-`; slug-strip lists include it; the startup sweep does not delete parked files.
> 6. **Notification.** CEO is paged once per park (dedup via `_NOTIFIED_PARKED`), message names the resume step + resets_at_raw.
>
> **Run the FULL suite** `python3 -m pytest tests/ -v` (`python3 -m pytest`, NOT the `timeout` binary — unavailable on macOS). Read the tail; record exact pass/fail counts and confirm 0 regressions. Apply mechanical Rule 20 / Rule 22 gates.
>
> **MANDATORY — Rule 20 self-check banner.** Your QA deposit MUST contain, verbatim, a section headed exactly:
> `## Rule 20 — QA Self-Check Results`
> followed (anywhere below it) by a line reading exactly:
> `**PASSED — SELF-CHECK PASSED**`
> This is the self-attestation the `rule_20_self_check` gate requires (its omission is why plan 148 Step 3 failed). Include it in addition to your per-item PASS/FAIL table and evidence.
>
> **Deposit:** `bellows/knowledge/qa/session-limit-park-autoresume-qa-reverify-2026-07-09.md` — per-checklist-item verdict (PASS/FAIL + evidence), the full-suite tail with counts, the mandatory Rule 20 self-check banner + PASSED line above, and an Output Receipt with status. Canonical Python file-write pattern — no heredoc. In `### Ledger Updates` include `#### Prompt Feedback` (daemon-owned; do NOT edit any feedback file directly). Update `bellows/PROJECT_STATUS.md` to record the session-limit feature as QA-verified (note DAEMON RESTART REQUIRED for it to go live).
>
> **Deposits:**
> - `bellows/knowledge/qa/session-limit-park-autoresume-qa-reverify-2026-07-09.md`
>
> **STOP. Wait for CEO verdict.**
