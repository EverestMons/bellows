# Bellows — `_gate_rule_20_self_check` False-Positive Root Cause Audit
**Date:** 2026-05-11 | **Tier:** Small (diagnostic) | **Test Scope:** targeted | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation. Per Rule 22, the Planner reads the deposited findings file directly before authorizing any follow-up work.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-rule-20-gate-false-positive-root-cause-2026-05-11.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further. Do NOT move the plan to Done.
```

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-rule-20-gate-false-positive-root-cause-2026-05-11.md", "bellows/knowledge/decisions/in-progress-diagnostic-rule-20-gate-false-positive-root-cause-2026-05-11.md")`. You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. Skip the domain glossary read — this is a code-tracing audit task. **Context.** The Step 2 (QA) verdict for `executable-fence-strip-plan-text-parsers-2026-05-11` tripped `_gate_rule_20_self_check` with failure message `no QA deposit contains Rule 20 self-check banner`. The QA report at `bellows/knowledge/qa/fence-strip-plan-text-parsers-qa-2026-05-11.md` DOES contain the canonical banner `Rule 20 — QA Self-Check Results` immediately followed by `PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.` — Planner Rule 22 (e) verification passed cleanly. The CEO overrode the gate with a continue verdict; the plan is now in `Done/`. This diagnostic investigates the root cause class of the false positive so a structural fix can be authored. **This is the second false-positive class on `_gate_rule_20_self_check`** — the first was the deposit-path resolution issue closed 2026-04-30 (commit `e609ad3` added `isdir()` to resolution strategies). The Planner-side guardrail explicitly bars quick fixes for recurring bug classes; this diagnostic targets the systemic cause, not a point patch. **Investigate only; do NOT propose code changes inside the findings file beyond what Q5 explicitly asks for. Do not write source code changes. Do not run tests beyond REPL fixtures captured inline in findings.** **Q0 — Has any fix shipped since 2026-05-05?** The Rule 20 gate shipped 2026-05-05. Run `git --no-pager log --since=2026-05-05 --oneline -- bellows.py gates.py verdict.py parser.py` from inside the `bellows/` directory. For every commit returned, run `git --no-pager show --stat <sha>` and check whether the commit touches `_gate_rule_20_self_check`, `_resolve_deposit_path`, or any deposit-enumeration logic in the QA-step gate path. If a fix has shipped that would have prevented the 2026-05-11 false positive, stop the diagnostic and report — write a one-paragraph findings file that says so and cite the commit SHA, then jump straight to the Output Receipt. If no shipped fix exists, proceed to Q1. **Q1 — Locate and capture `_gate_rule_20_self_check` source.** Read `bellows/gates.py` and locate `_gate_rule_20_self_check`. Capture its exact source (function signature + body) and line range in the findings (cite as `gates.py:NNN-MMM`). Also capture: (a) which fields from the step context/verdict request the gate uses to determine which files to inspect, (b) how the gate enumerates QA deposits (singular `Deposit:` field vs plural `**Deposits:**` block vs hardcoded path vs directory scan), (c) what the gate does when its enumeration returns zero files. Be precise about the data flow from "step completes" → "gate runs" → "gate decides which files to scan for the banner string." **Q2 — Confirm the actual filesystem state at the time of the 2026-05-11 false positive.** The deposited QA report exists at `bellows/knowledge/qa/fence-strip-plan-text-parsers-qa-2026-05-11.md`. The DEV log exists at `bellows/knowledge/development/fence-strip-plan-text-parsers-2026-05-11.md`. The verdict request at `bellows/verdicts/pending/verdict-request-fence-strip-plan-text-parsers-2026-05-11-step-2.md` shows `Deposit: bellows/knowledge/development/fence-strip-plan-text-parsers-2026-05-11.md` (singular field, points to DEV log not QA report). Confirm each of these three states by direct filesystem read. Then check: does `_gate_rule_20_self_check` (per Q1 findings) inspect the QA report file? If yes, classify the false positive as a banner-detection bug (gate reads the right file but misses the banner). If no, classify as a deposit-enumeration bug (gate reads the wrong file or no file). State the classification explicitly. **Q3 — Trace the singular-vs-plural deposit parsing.** Per the 2026-04-18 Rule 26 entry, `extract_primary_deposit` in `verdict.py` reads the singular `**Deposit:**` form and does NOT recognize the plural `**Deposits:**` block. Confirm this is still the case by reading `bellows/verdict.py` and locating `extract_primary_deposit`. Capture its source and line range. Then read the Step 2 (QA) block of `bellows/knowledge/decisions/Done/executable-fence-strip-plan-text-parsers-2026-05-11.md` and confirm the step declares a plural `**Deposits:**` block listing the QA report path. The gap is: plan declares plural deposits → `extract_primary_deposit` parses only singular → verdict request `Deposit:` field gets some fallback or stale value → `_gate_rule_20_self_check` looks for the banner in the singular Deposit file → false positive. Trace this gap end-to-end and state whether the gate consumes `extract_primary_deposit`'s output directly, or whether it does its own enumeration. **Q4 — Population audit of Rule 20 gate failures.** Audit all `bellows/verdicts/processed/` and `bellows/verdicts/resolved/` verdict files from 2026-05-05 (Rule 20 gate ship date) forward. Run `grep -lE "rule_20_self_check" bellows/verdicts/processed/* bellows/verdicts/resolved/* bellows/verdicts/pending/* 2>/dev/null` (use `find` if grep doesn't recurse). For each match: (a) read the verdict file, (b) extract the plan slug and step number, (c) check whether the associated QA report exists at `bellows/knowledge/qa/<slug>-qa-*.md` or similar (use `Filesystem:list_directory` on `bellows/knowledge/qa/`), (d) if the QA report exists, grep it for the canonical banner `Rule 20 — QA Self-Check Results`. Classify each occurrence as: (i) TRUE-POSITIVE (gate correctly fired, banner missing from QA report), (ii) FALSE-POSITIVE (gate fired, banner present in QA report), (iii) UNRESOLVABLE (QA report not located, classification deferred). Report counts per class. State whether the false-positive count is 1 (just this session) or higher (recurring class confirmed). **Q5 — Root cause classification and fix shape recommendation.** Synthesize Q1–Q4 into a root cause class. Possible classes: (a) `extract_primary_deposit` parser gap (singular-only parsing of Rule 26 plural-deposit plans → gate inspects wrong file), (b) gate file-enumeration scope bug (gate reads the right field but skips QA reports in non-default locations), (c) banner-detection bug (gate reads the right file but the banner-match regex/substring fails on valid banners), (d) some other class. For the identified class, recommend a fix shape with one paragraph of reasoning. Possible fix shapes include: (a) extend `extract_primary_deposit` to recognize `**Deposits:**` blocks (the long-deferred Rule 26 enforcement parser), (b) change `_gate_rule_20_self_check` to enumerate the plural `**Deposits:**` block directly rather than consume `extract_primary_deposit`, (c) change `_gate_rule_20_self_check` to scan all files under `bellows/knowledge/qa/` for the banner regardless of declared deposits, (d) some other shape. State the recommended shape, estimated LOC, test surface (new tests + tests at risk of breakage), and any dependencies on other open BACKLOG items. **Q6 — Confirm consistency with the recurring-bug-class guardrail.** The 2026-04-03 Lesson says: do NOT write quick fixes for recurring bug classes. Check the BACKLOG and recent QA reports for prior fix attempts at `_gate_rule_20_self_check`. Run `grep -lE "rule_20|_gate_rule_20|self_check" bellows/knowledge/BACKLOG.md bellows/knowledge/decisions/Done/*.md` and enumerate prior closed items that touched this gate or its support functions. For each prior fix, state whether it would have prevented the 2026-05-11 false positive. If 2+ prior fixes exist in this gate's support path, the recommended fix shape MUST be structural (parser refactor, gate refactor, or shared utility) — not another point patch. State this constraint explicitly in the Q5 recommendation if applicable. **Findings file structure.** Deposit a single findings file at the path declared in **Deposits** below. Structure: top-level sections per Q0–Q6. Use markdown tables for Q4's population audit (columns: verdict file, plan slug, step, QA report path, banner present yes/no, classification) and any Q1 data-flow enumerations. Include all REPL fixtures and grep outputs in fenced blocks. Cite line ranges as `gates.py:NNN-MMM`, `verdict.py:NNN-MMM` format. End the findings with a one-paragraph "Planner-facing summary" stating: (i) whether a fix has shipped (Q0), (ii) the false-positive count from Q4, (iii) the classified root cause class (Q5), (iv) the recommended fix shape, and (v) whether prior fix attempts make this a structural-fix-required case. Be precise — the Planner uses this summary to scope the executable. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/research/rule-20-gate-false-positive-root-cause-2026-05-11.md`
>
> **STOP. Do NOT proceed to any further step. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
