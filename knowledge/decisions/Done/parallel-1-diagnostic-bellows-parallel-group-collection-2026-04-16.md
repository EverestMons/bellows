# Bellows — Parallel Group Collection Bug Diagnostic
**Date:** 2026-04-16 | **Type:** diagnostic (investigation only, no fixes)

## How to Run This Plan

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bellows-parallel-group-collection-2026-04-16.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further or move the plan to Done.
```

---
---

## STEP 1 — INVESTIGATION AGENT (bellows developer)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-parallel-group-collection-2026-04-16.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-parallel-group-collection-2026-04-16.md")`. Skip specialist file and glossary reads — pure code-tracing. **Context:** Today's session deposited two `parallel-1-` plans into forge's decisions dir simultaneously. Bellows logged `parallel group parallel-1 — 1 plans` TWICE instead of `parallel group parallel-1 — 2 plans` once. Both plans ran and completed, but as sequential singles in separate threads rather than as a collected group dispatched together via `handle_parallel_group`. **Q1 — Trace `extract_parallel_group`.** Read the function in `bellows.py`. Show its regex or parsing logic. Given filename `parallel-1-executable-forge-evaluation-annotation-2026-04-16.md`, what does it return? Given `parallel-1-executable-forge-budget-correction-2026-04-16.md`, what does it return? Do both return the same group identifier? **Q2 — Trace `collect_group` in `PlanHandler`.** Show the function. It receives a `decisions_path` and a `group` identifier. How does it find siblings — does it scan the directory for filenames starting with `{group}-`? What does it check: `fname.startswith(group + "-")`? If `group = "parallel-1"`, does it match `parallel-1-executable-forge-evaluation-annotation-2026-04-16.md`? Show the literal matching logic. **Q3 — Trace the `_handle` dispatch path for parallel plans.** In `PlanHandler._handle`, when `extract_parallel_group` returns a group: (a) does it call `collect_group` to find ALL siblings before dispatching? (b) does it add ALL siblings to `self._seen`? (c) or does it process them one at a time as the watcher fires `on_created` for each file? **Q4 — Identify the root cause.** The two files were moved into the decisions directory as two separate `Filesystem:move_file` calls (atomic moves from staging). The watchdog observer fires `on_created` for each file independently. If `_handle` is called for file A before file B lands on disk: (a) `collect_group` scans the directory and finds only file A (file B doesn't exist yet), (b) dispatches group with 1 plan, (c) then `on_created` fires for file B, `_handle` is called again, `collect_group` finds only file B (file A is already in `_seen`), (d) dispatches group with 1 plan again. Is this the race? Or is there a different cause? Trace the exact sequence and confirm or deny this hypothesis. **Q5 — Is there also a rescan-path issue?** `_rescan` calls `_handle` for each file found in the directory. If both parallel files are present during a rescan, does `_handle` correctly collect both into one group on that path? Or does the same one-at-a-time problem apply? **Q6 — Propose the fix.** The core issue (if Q4 confirms) is that `collect_group` only finds siblings already on disk at the moment it runs. Options: (a) add a short delay (e.g., 2 seconds) after detecting a parallel-prefixed file before collecting — gives time for siblings to land, (b) in `_handle`, if a parallel group is detected, defer dispatch to the next rescan cycle rather than dispatching immediately from `on_created` — rescan is guaranteed to see all files, (c) collect into a pending-group buffer and dispatch on the next rescan tick if the group hasn't grown. Evaluate each on reliability and complexity. **Deposit:** write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/parallel-group-collection-diagnostic-2026-04-16.md`. Verify you answered ALL SIX questions before marking Status=Complete. **Commit:** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/parallel-group-collection-diagnostic-2026-04-16.md && git commit -m "docs: diagnostic — parallel group collection bug"`. **Feedback:** standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed. Do NOT move the plan to Done. Report completion and wait for CEO confirmation.**
