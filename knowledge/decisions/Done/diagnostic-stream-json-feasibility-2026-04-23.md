# Bellows — Stream-JSON Feasibility Diagnostic
**Date:** 2026-04-23 | **Tier:** Diagnostic | **Test Scope:** targeted | **Execution:** Step 1 (SA) — single-step per Rule 22 v4.19

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The SA investigates and deposits findings. After Step 1 completes, the Planner reads the deposited findings file via Rule 22 verification before authorizing housekeeping.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-stream-json-feasibility-2026-04-23.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT move the plan to Done.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-stream-json-feasibility-2026-04-23.md", "bellows/knowledge/decisions/in-progress-diagnostic-stream-json-feasibility-2026-04-23.md")`. You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. Skip the domain glossary — this is code-tracing and tooling investigation. **Context.** The previous diagnostic (`permission-prompt-substrate-2026-04-23`) found that Bellows invokes `claude -p --output-format json --allowedTools Read,Edit,Write,Bash`, which receives a single JSON lump at the end of each step. Tool calls, tool results, and intermediate events are NOT visible to Bellows — only the final `result` text and the `git diff` delta. The SA flagged that `--output-format stream-json` is a supported flag that would emit structured NDJSON events in real time. Before deciding whether to proceed with a Failure 3 fix (gate-after-move-to-Done ordering, top BACKLOG item) via reorder/block/defer, we want to know whether switching to stream-json would make that fix unnecessary by changing the gate substrate from post-hoc to real-time. **This is a feasibility diagnostic, NOT a design diagnostic.** You are answering three specific questions and estimating scope. You are NOT designing a new gate model, NOT proposing which gates to switch first, NOT comparing stream-json to alternative architectures, NOT discussing the downstream "verdict-as-structured-corpus" CEO vision — that is explicitly out of scope for this diagnostic. **This diagnostic is single-step per Rule 22 v4.19. Investigate, deposit findings, report Complete. The Planner verifies via Rule 22 and handles housekeeping directly.** **Q1 — What does stream-json actually emit?** Run `claude -p --output-format stream-json --include-partial-messages --verbose --model claude-sonnet-4-20250514 "list three colors"` and capture the raw NDJSON output to `/tmp/stream-json-sample.ndjson`. The `--verbose` flag may be required — if the command errors without it, include it. If `--include-partial-messages` errors, try without it. If `claude-sonnet-4-20250514` is not a current model alias, substitute whatever model name your own context uses for dispatched agents. The test prompt ("list three colors") is deliberately trivial — cost ~$0.05-0.10. Do NOT use Bellows to run this; invoke `claude -p` directly from your shell. Parse the captured NDJSON and identify every distinct event type. For each event type, capture: (i) the `type` field value, (ii) the structure of the event payload (top-level keys, nested structure shape), (iii) whether tool calls appear as structured events (name + arguments visible), (iv) whether tool results appear as structured events, (v) whether the final `result` text is reconstructible from the stream, (vi) whether session metadata (session_id, cost, stop_reason) appears in the stream or only in a terminal message. If the trivial prompt doesn't exercise tool use, run a second test: `claude -p --output-format stream-json --verbose --model <model> --allowedTools Bash "run: echo hello"` — this forces a tool call. Capture to `/tmp/stream-json-sample-tool.ndjson`. Deliverable: a table of event types observed, one example line (redacted if needed) per type, and a three-sentence summary of what the stream makes visible that the current single-JSON format does not. **Q2 — Blast radius of switching runner.py from json to stream-json.** Read `bellows/runner.py` and `bellows/parser.py` in full. Identify every call site, function, or code path that assumes the current single-JSON-object model. For each assumption, classify the change required to adapt it as: (a) MECHANICAL — swap `json.loads(text)` for streaming NDJSON reader, no logic change beyond the parse call, (b) LOGIC — something that currently waits for completion must now handle partial/incremental state (e.g., receipt inference, cost tracking, stop_reason handling), (c) INVARIANT — something that assumes a single final result must fundamentally adapt (e.g., Gate 4's `permission_denials` array check, parser's VERDICT_REQUESTED detection, any single-pass post-hoc logic). Deliverable: a table with columns `File:Line`, `Function/Block`, `Current Assumption`, `Change Class (a/b/c)`, `One-Line Justification`. Include every site found — do not spot-check. Do NOT write any replacement code. Do NOT design the new parser. Just identify what has to change and how disruptive each change is. **Q3 — Per-gate reclassification under stream-json.** Read `bellows/gates.py` in full. For EVERY gate function (`_gate_*` or however they're named), classify as: (a) BECOMES PER-EVENT — gate logic can evaluate incrementally as events stream in, likely becoming more precise or more responsive (example: scope_check could reject a Write event the moment it fires rather than diffing git post-hoc); (b) STAYS POST-HOC — gate inherently needs end-state and streaming doesn't help (example: test regression count, cumulative cost threshold); (c) BECOMES UNNECESSARY — the signal the gate synthesizes would be directly available as a stream event and the gate itself is redundant (example: if permission-denied events stream in as structured events, the `permission_denials` array check may be redundant with direct event observation). For each gate, also note: (i) any gate whose reclassification depends on Q2's INVARIANT-class changes (chain dependencies), (ii) any gate whose current implementation has a known false-positive issue in BACKLOG (scope_check git-range, deposit_exists prose-directory, no_permission_denials Grep-denial) and whether stream-json would incidentally fix it. Deliverable: gate-by-gate classification table with (a/b/c) and chain notes. **Scope estimate.** After Q1-Q3, write a short "Scope Estimate" section: in broad strokes, is the stream-json switch a day of work, a week, or multi-week? Estimate by code surface, not by hours — "X files touched, Y functions to rewrite, Z tests to update, N new tests to add." Include the riskiest unknown in one sentence. This estimate is the decision input: if small, we proceed with stream-json and Failure 3 falls out of scope; if large, we fall back to reorder/block/defer for Failure 3 and treat stream-json as a longer-term direction. **Out of scope — do NOT include these in the findings.** No design for the new parser or gate model. No proposed sequencing of which gates to switch first. No comparison of stream-json to hypothetical alternative architectures. No discussion of the verdict-as-corpus vision. No recommendation on whether to proceed — the CEO makes that call from the scope estimate. Investigation and classification only. **Deliverable format.** Deposit findings to `bellows/knowledge/research/stream-json-feasibility-2026-04-23.md`. Structure: (1) Executive Summary — 3 sentences: does stream-json emit structured tool-call events, rough blast radius class, rough scope class. (2) Q1 findings — event-type table + sample lines + three-sentence summary. (3) Q2 findings — blast radius table covering every assumption site in runner.py and parser.py. (4) Q3 findings — gate classification table with chain notes. (5) Scope Estimate — files/functions/tests count + one-sentence biggest unknown. (6) Follow-up questions — anything that surfaced warranting a separate diagnostic. No design sections. No recommendations. No corpus discussion. **Canonical file write pattern.** Use `Filesystem:write_file` or `Desktop Commander:write_file` directly for the findings file. If Python: `content = """..."""` then `with open("/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/stream-json-feasibility-2026-04-23.md", "w") as f: f.write(content)`. No heredoc. No `python3 -c "..."` with embedded triple quotes. **No git commits during this diagnostic.** Investigation only. The `/tmp/stream-json-sample*.ndjson` files are evidence — reference them in the findings but do NOT copy their contents verbatim into the findings file (too long). Quote 1-3 representative lines per event type only. **Prompt feedback.** Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
>
> - `bellows/knowledge/research/stream-json-feasibility-2026-04-23.md`
>
> **STOP. Do NOT proceed further. The Planner will verify the findings file via Rule 22 and perform housekeeping directly — this is a single-step diagnostic.**
