# Bellows — Silent Deposit-Loss at Header-Pause Teardown (Diagnostic)
**Date:** 2026-06-10 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## Context (Rule 27)
BACKLOG #1 (2026-06-08, severity medium): a deposit written to a non-auto-staged directory is silently discarded at a header-pause teardown while every gate reads green — `deposit_exists` PASSes (it checks the worktree), `files_changed=0`, the worktree is then removed, and a completed step's output is lost unrecoverably. Reproduced on `executable-anvil-lab-project-scope-fix-2026-06-08` (v1) Step 1: the sole deposit `knowledge/architecture/lab-project-scope-blueprint-2026-06-08.md` was written UNTRACKED in the worktree and discarded at the `after_step_1` pause; by contrast `knowledge/development|research|qa` deposits survived (bellows cycle 1, same pause). Reference: stop verdict `verdicts/resolved/processed-verdict-anvil-lab-project-scope-fix-2026-06-08-step-1.md`. **The entry's "auto-staged dirs" framing is a HYPOTHESIS, not proven — and it must be pinned, because it did not reproduce this session:** the daemon-archetype and scope_check-FP diagnostics both deposited to `knowledge/architecture/` and SURVIVED their `after_step_1` pauses. So the real discriminator (dir-based auto-staging? committed-vs-uncommitted? a copy-back that covers only committed changes? continuous-run vs teardown-pause?) is unknown and is the core question of this diagnostic. This is a design diagnostic only — no fix code; the implementing executable is authored after CEO review. **Severity rationale:** unlike the scope_check FP (loud — halts and is overridden), this is silent success masking data loss, which is the worse failure mode. **Self-protection:** this diagnostic's own SA deposit goes to `knowledge/architecture/` — the implicated dir class — so the SA MUST explicitly `git add` + `git commit` its deposit before finishing, so the diagnostic cannot fall victim to the bug it is investigating.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file first, then the source: `bellows.py` — specifically `_teardown_worktree` (the copy-back / land / removal sequence), `_capture_git_diff` and `_parse_diff_stat`, the worktree-creation/removal path, and the step-completion path that runs between an agent finishing and a header pause; also read the deposit/staging logic if any auto-staging exists. Read the BACKLOG #1 entry and the referenced stop verdict `verdicts/resolved/processed-verdict-anvil-lab-project-scope-fix-2026-06-08-step-1.md`. **Post a 1-line "Read X." after each file read and a 1-line "Drafting Section N." at the start of each section** (keeps the inactivity timer warm). This is a design diagnostic — author NO fix code, no `bellows.py` edits (the implementing executable comes after CEO review). Ground every claim in current code, not the BACKLOG's prior framing. **Section 1 — Pin the real discriminator (core question).** Trace exactly what happens to a deposit FILE in a worktree between agent completion and a mid-plan / header-pause teardown. Determine the EXACT condition under which the file is preserved vs lost: is it dir-based auto-staging (confirm/refute against current code — is there ANY code that stages `development|research|qa` but not `architecture`?), committed-vs-uncommitted state, a copy-back step that covers only committed (or only staged) changes, or the continuous-run-vs-teardown-pause distinction (worktree persists across steps in a continuous run, but is removed at a header pause)? **Reconcile the contradiction directly:** v1 lab-project-scope-fix LOST its `knowledge/architecture/` deposit at an `after_step_1` pause, yet this session's two diagnostics PRESERVED their `knowledge/architecture/` deposits at `after_step_1` pauses — what differs between those cases (did the surviving ones commit? did teardown land them differently? did a 2026-06-0x teardown-hardening change the copy-back)? State the precise preserved-vs-lost predicate. **Section 2 — Locate the loss in code.** Identify the exact line(s) in the teardown sequence where an uncommitted-and-unstaged (or otherwise-uncovered) deposit is dropped, and what the copy-back/land step actually covers (committed only? staged? working-tree?). **Section 3 — Fix design.** Evaluate the two BACKLOG shapes and recommend: (a) PRESERVE — auto-stage-and-include all declared deposits at step end before teardown (the real fix — keeps the work), and/or (b) FAIL-LOUD — make a gate FAIL (not `deposit_exists`-PASS) when a declared deposit is untracked/uncovered at a teardown pause (surfaces the loss instead of silently succeeding). Weigh whether the fix should be (a), (b), or (a)+(b) as a safety net; recommend with reasoning. **CRITICAL — do not regress the hardened teardown/resume family:** the fix MUST NOT alter the `_teardown_worktree` land-or-raise contract, the Gap-1b/1c guards, or the stranded-cleanup preserve-branch logic; state explicitly how the proposed change interacts with each and confirm it is additive. Keep the fix strictly Layer-1 mechanical (staging/diff/gate logic), no judgment. **Section 4 — Test surface.** Enumerate the `tests/` additions: positive (a deposit in the previously-lost dir/condition now survives teardown OR the gate fires) AND negative/regression (existing teardown landing tests still pass; a genuinely-missing declared deposit still fails the gate). **Section 5 — Layer Impact (required) + executable hand-off.** State affected layers (expect Layer 1), the exact files the executable will touch, and the daemon-restart + tight-scope self-fix constraints. Produce a Gap Assessment table `| Gap | Current State (file:line) | Proposed State | Change Required |`. **Section 6 — `### Verification Blocks` (Rule 39)** with `(claim, query, expected_output)` triples for the load-bearing current-state facts (the preserved-vs-lost predicate; what the copy-back covers) so the executable can re-verify at edit time. **BEFORE FINISHING — self-protection: explicitly `git add` your deposit file and `git commit` it** (the dir you are depositing into is the one implicated in this bug; do not rely on auto-staging). Use `with open()` for the deposit; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/architecture/silent-deposit-loss-teardown-fix-design-2026-06-10.md`
