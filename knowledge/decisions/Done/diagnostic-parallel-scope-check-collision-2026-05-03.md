# Bellows — Parallel-Plan scope_check Collision (Reproduction + Candidate Walk-Through)
**Date:** 2026-05-03 | **Tier:** Diagnostic | **Test Scope:** targeted | **Execution:** Step 1 (BELLOWS DEVELOPER) → Step 2 (BELLOWS SYSTEMS ANALYST)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation ("ok") before proceeding to Step 2. The agent must never skip steps, auto-chain to the next step, or move the plan to Done — the Planner performs the terminal move after Rule 22 verification.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-parallel-scope-check-collision-2026-05-03.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

**Background context for the agents (read before Step 1):** This is the third diagnostic for the parallel-plan scope_check collision class. Two prior attempts failed: the 2026-05-01 diagnostic (`bellows/knowledge/research/parallel-plan-scope-check-collision-diagnosis-2026-05-01.md`) correctly characterized the contamination vector in Q5(e) ("if sibling B's `claude -p` modifies file X on disk during plan A's step, those files appear in A's `post_diff`") but then recommended file-checksum snapshot (variant c1) without verifying that snapshots solve THE SAME vector. The shipped fix (commit `9b20d94`) was reverted via `executable-revert-snapshot-fix-and-reopen-backlog-2026-05-01` after Planner Rule 22 verification caught that file-checksum snapshots read the SAME shared working tree as `git diff --stat` — a sibling editing file X on disk changes X's checksum, so X appears in plan A's post-snapshot regardless of which plan modified it. The DEV's own regression test asserted as much. The recurring failure mode: candidates evaluated against a categorization of the bug rather than against the actual incident. This diagnostic structurally prevents that by requiring each candidate be walked end-to-end through a specific reproduction, with a literal answer for "what files does the candidate report for plan A's step." Categorical reasoning is rejected.

---
---

## STEP 1 — BELLOWS DEVELOPER

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-parallel-scope-check-collision-2026-05-03.md", "bellows/knowledge/decisions/in-progress-diagnostic-parallel-scope-check-collision-2026-05-03.md")`.
>
> You are the Bellows Developer. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first. Skip glossary read — Bellows has no domain glossary; this is code-path tracing. **Task: produce a literal incident reconstruction of the 2026-04-30 parallel-plan scope_check collision, then deposit a fresh canary that reproduces the same collision under controlled conditions.** The output anchors Step 2's candidate evaluation in literal evidence rather than categorical reasoning.
>
> **Phase 1A — 2026-04-30 incident reconstruction.** The incident involved three plans: `parallel-1-executable-deposit-exists-directory-paths-2026-04-30`, `parallel-1-executable-backlog-hygiene-sweep-2026-04-30` (note: BACKLOG entry says `executable-backlog-hygiene-sweep-2026-04-30` — verify the actual filename in `bellows/knowledge/decisions/Done/`), and `parallel-1-executable-ledger-pause-reason-code-2026-04-30`. For each of the three plans, retrieve from git history (`git --no-pager log --all --pretty=format:"%H %ai %s" --name-status` filtered to commits made by each plan's DEV/QA agents during the parallel window): (1) the commit SHAs and exact UTC timestamps of every commit made during dispatch, (2) the file paths touched by each commit. Cross-reference with `bellows.db` `runs` table (read via Python sqlite3) to find the dispatch start/end timestamps for each plan's steps. Build a single chronological timeline table: `| Time (UTC) | Event | Plan | Files | Notes |` listing every commit AND every Bellows step start/end across all three plans, sorted by time. Identify the EXACT moments where one plan's step execution window overlapped another plan's commit (this is the contamination window). Deposit the timeline to `bellows/knowledge/research/parallel-collision-incident-timeline-2026-05-03.md`.
>
> **Phase 1B — code-path trace anchored to the incident.** Open `bellows/bellows.py`. For one specific contaminated step pair from Phase 1A's timeline (e.g., "deposit-exists-directory-paths Step 1, dispatched at T1, post_diff captured at T2; ledger-pause-reason-code committed file X at T1+Δ where T1 < T1+Δ < T2"), trace the literal code path for that contaminated capture: (a) line where `pre_diff` is captured (call out exact line number), (b) line where `runner.run_step()` is called, (c) line where `post_diff` is captured, (d) the argv passed to `_capture_git_diff` and the cwd it runs in, (e) what `git --no-pager diff --stat --relative -- .` would have returned at T2 (working-tree state including any sibling-uncommitted changes AND excluding any sibling-just-committed changes), (f) what `_parse_diff_stat(post, pre, project_path)` would compute for `files_changed`, (g) what `_gate_scope_check` would do with that list. Write this as a literal walk-through with line numbers and the specific file paths involved — not abstract description. Append to the timeline file.
>
> **Phase 1C — fresh canary reproduction.** Author and dispatch a controlled reproduction of the collision. Two minimal canary plans, both prefixed `parallel-99-`, both targeting the bellows project: `parallel-99-executable-collision-canary-A-2026-05-03` and `parallel-99-executable-collision-canary-B-2026-05-03`. Each plan has a single DEV step. Plan A's step writes a file `bellows/knowledge/research/canary-A-marker.txt` containing a single line, then sleeps 30 seconds, then commits the marker (single commit, file in scope). Plan B's step writes a different file `bellows/knowledge/research/canary-B-marker.txt` containing a single line, then commits immediately, then sleeps 30 seconds. The 30-second windows overlap deliberately: B commits during A's sleep, ensuring B's commit lands during A's step execution window — exactly the scenario 2 vector. Both plans declare ONLY their own marker file in the `**Deposits:**` block. Deposit both canary plans to `bellows/knowledge/decisions/` atomically (write to `/tmp/` first, move into watched directory). Each canary plan must claim itself on entry per Rule 24 and per the standard plan template. After both plans complete (whether passing or failing scope_check), capture: (1) the verdict request files Bellows produced for each, (2) the `files_changed` list from each verdict request, (3) whether scope_check tripped on either plan, (4) which sibling's files appeared in which plan's diff. Deposit canary results as a section appended to the timeline file.
>
> **CRITICAL — canary safety:** The canary plans are dispatched by Bellows under their own lifecycle. Use `parallel-99-` (not `parallel-1-`) to avoid colliding with any naming convention currently in use. The canaries must be self-cleaning — after both complete, the agent moves both canary plans to `bellows/knowledge/decisions/Done/` and deletes the marker files via `rm` (since cleanup of test artifacts is acceptable here). Do NOT commit the marker files to long-term history beyond the canary's own commit; instead, produce a final cleanup commit removing both markers after the canary completes.
>
> **Phase 1D — observed-vector summary.** End the deposit with a single short section titled "Observed Contamination Vector" stating in 3-5 sentences: (1) the literal mechanism by which sibling files appeared in plan A's diff (cite specific timestamps and lines from Phase 1A and 1B), (2) whether the canary reproduced the same vector or a variant, (3) the specific property a candidate fix must have to prevent this vector. This section is the ground-truth specification Step 2's candidate evaluation must answer to.
>
> **Constraints:** Do NOT propose fix candidates. Do NOT recommend a solution. This step is reconstruction and reproduction only — Step 2 evaluates candidates. Do NOT modify `bellows.py` or any production code. Do NOT introduce regression tests or unit tests. The Phase 1C canary plans are diagnostic instruments, not fixes. Use `git --no-pager` for all git commands. If the 2026-04-30 commit history is incomplete or any timestamp cannot be reliably established, document the gap explicitly rather than inferring — categorical reasoning is the failure mode this diagnostic is designed to prevent.
>
> Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/parallel-collision-incident-timeline-2026-05-03.md`
> - `bellows/knowledge/decisions/Done/parallel-99-executable-collision-canary-A-2026-05-03.md`
> - `bellows/knowledge/decisions/Done/parallel-99-executable-collision-canary-B-2026-05-03.md`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — BELLOWS SYSTEMS ANALYST

---

> Before starting, read `bellows/knowledge/research/parallel-collision-incident-timeline-2026-05-03.md` and check the Phase 1D "Observed Contamination Vector" section. If the timeline is incomplete, the canary did not reproduce the vector, or any Phase 1A/1B/1C output is missing, stop and report the issue to the CEO before proceeding.
>
> You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. Skip glossary read. **Task: walk each surviving fix-shape candidate end-to-end through Phase 1's reconstructed contamination vector and deposit a recommendation.** This step's deliverable answers, for each candidate, the literal question: "what does the candidate report for plan A's `files_changed` in the Phase 1A timeline."
>
> **Phase 2 — candidate walk-through.** The four surviving candidates from the 2026-04-30 BACKLOG entry (don't-fix is excluded by CEO direction): **(a) timestamp-bound git range** — capture step_start_ts before runner.run_step(), use `git log --since=<ts> --stat --relative -- .` instead of `git diff --stat` for post-step capture; **(b) plan-slug commit-message scoping** — filter `git log` to commits whose message contains the plan slug, requires PLANNER_TEMPLATE convention adoption (current rate 0%); **(d) per-plan git worktree** — `git worktree add` per parallel plan, each `claude -p` runs in its own working tree with isolated index, commits merged back at plan close; **(e) per-process file-touch tracking via fsevents/inotify filtered by claude-p PID** — register an OS-level file watcher scoped to the claude-p subprocess PID, accumulate the file-touch list as the step's authoritative `files_changed`. For each candidate, produce these answers as a bulleted analysis: (1) the literal mechanism the candidate replaces or augments in the current code (which lines of `bellows.py` change, which functions, which call sites); (2) given the Phase 1A timeline (sibling B commits file X at T1+Δ during A's step window T1→T2), what does the candidate's mechanism return for plan A's `files_changed`?; (3) does the answer to (2) include any of B's files? If yes, the candidate is structurally insufficient — say so explicitly and stop the analysis for that candidate; (4) given Phase 1C's canary results, does the candidate's mechanism behave the same way on the canary as it does on the 2026-04-30 incident? Differences between canary and incident behavior are red flags worth surfacing; (5) what new failure modes does the candidate introduce? Specifically: agent UX (worktrees may break absolute path expectations), platform-specificity (fsevents is Mac-only, inotify is Linux-only), git history shape (commit merging, worktree commit attribution), interaction with existing gates (does Gate 7 file_change_audit still produce useful output?), interaction with `.bellows-cache` shadow cache (worktrees may need their own cache); (6) cost: estimated LOC, surface area of code change (single function vs. multiple files), test surface needed.
>
> **Phase 3 — recommendation.** From the surviving candidates (those that passed (3) above — the literal answer to "does it return B's files for A?" is no), produce a ranked recommendation with explicit reasoning. The recommendation must (a) name the recommended candidate, (b) cite the literal Phase 2 evidence that supports the choice, (c) enumerate the specific fix shape (function signatures, where in `bellows.py` the change lands, what the new test coverage looks like at the test-file level — do NOT write the actual tests), (d) identify what new failure modes the recommendation introduces and how each is mitigated. If no candidate passes (3), the recommendation is "all four are structurally insufficient; the next diagnostic must explore [specific direction]" — this is a valid outcome, not a punt. **Cost-vs-friction context paragraph (last item before deposit):** as the closing paragraph of the recommendation, include a one-paragraph cost-vs-friction comparison: estimated full-LOC + test-surface cost of the recommended fix, vs. the workaround cost of "treat parallel-N- as read-only-diagnostics-only and use sequential plans for committing work." This paragraph is not a candidate — the CEO has excluded don't-fix from the candidate list — but it provides decision context. Do not draw a recommendation conclusion from this paragraph; just state both costs.
>
> **Constraints:** Do NOT write a fix prompt. Do NOT recommend tests beyond the test-file-level surface (the actual tests are written in a separate executable plan that the Planner authors after CEO reviews this recommendation). Do NOT modify `bellows.py` or any production code. The diagnostic ends with the recommendation deposit — implementation is a downstream plan. Read the Phase 1 deposit BEFORE evaluating any candidate; reasoning that does not cite Phase 1 evidence is rejected.
>
> Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/parallel-collision-candidate-evaluation-2026-05-03.md`
>
> **STOP.** Do NOT move this plan to Done/. Per the disable-auto-close model, the Planner performs the terminal move after Rule 22 verification passes.
