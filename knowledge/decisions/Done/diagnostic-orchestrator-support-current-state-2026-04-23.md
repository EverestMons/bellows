# Bellows Orchestrator Support — Current State Diagnostic
**Date:** 2026-04-23 | **Tier:** Small | **Test Scope:** none | **Execution:** Step 1 (Bellows Developer)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the plan file and executes Step 1. This is a single-step diagnostic per Rule 22 — after the Bellows Developer reports Complete, the Planner reads the deposited findings file directly, performs Rule 22 (a)–(e) verification, and handles housekeeping in the Project conversation.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-orchestrator-support-current-state-2026-04-23.md. Execute Step 1.
```

---
---

## STEP 1 — BELLOWS DEVELOPER

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-orchestrator-support-current-state-2026-04-23.md", "bellows/knowledge/decisions/in-progress-diagnostic-orchestrator-support-current-state-2026-04-23.md")`. Skip specialist file and glossary reads — this is a code-tracing and architectural-audit task with no domain interpretation required. **Scope:** Read-only audit of Bellows's current dispatch architecture to inform ADR-003 (unified Forge cycle orchestrator). Do NOT propose fixes, changes, or implementations. Findings only. **Q1 — Dispatch state machine.** (a) List every plan-file state Bellows recognizes, keyed by filename prefix pattern. For each state, report: what code sets it, what code reads it, what transition rules move a file out of that state. Expected states include `executable-*`, `diagnostic-*`, `in-progress-*`, `verdict-pending-*`, `halted-*`, `parallel-N-*`, files in `Done/`. Report findings as a table: state | filename pattern | setter (file:line) | reader (file:line) | exit transitions. (b) For each state, identify which component owns the transition in practice (Bellows daemon code, Planner via Filesystem:move_file, or CEO via manual `rm` / shell). Report as a second table: state | owner | documented or inferred. (c) Quote the filesystem watcher setup verbatim from `bellows.py` — specifically the `is_runnable_plan` check (or equivalent), the allowlist regex, and any dispatch-dedup logic that prevents re-claiming. **Q2 — Gate evaluation boundary.** (a) List every gate name in `gates.py` and quote each gate's signature verbatim (function name, parameters, return type). (b) When does each gate run relative to the step execution lifecycle — before agent dispatch, after agent completion, both, per-step, per-plan? Quote the code that calls gates (likely in `bellows.py` or `runner.py`). (c) What is the data model of a gate result? Quote the return-type definition (dataclass, TypedDict, or dict shape). (d) Where do gate failures persist? Report: verdict file fields, DB tables, log files — whichever apply. (e) Are gates independent or sequential? If gate N fails, do subsequent gates still run? **Q3 — Agent dispatch mechanics.** (a) Quote the `claude -p` invocation site verbatim (expected in `runner.py` or similar). What arguments does Bellows pass to the agent session? What working directory? What environment? (b) How does Bellows detect agent completion? (Process exit? Log marker? Output file? Other?) Quote the detection code. (c) What data flows back from the agent to Bellows besides exit code? Stdout capture? Structured output parsing? Files on disk that Bellows reads? Quote the relevant parsing logic. (d) How is the agent's step progress communicated back — is there a notion of "I finished step 1, waiting for verdict" vs "I finished all steps, moved to Done" that Bellows observes, or is it all inferred from filesystem state after agent exits? **Q4 — Extension points for orchestrator plans.** (a) Is there any existing mechanism by which a plan references other plans? Examples to check: `**Depends on:**` field parsing (documented in PLANNER_TEMPLATE), `parallel-N-` group coordination (do plans in the same parallel group share any dispatch state?), `before-*`/`after-*` relationships. Quote whatever plan-to-plan references currently exist. (b) How are parallel-group plans dispatched? Quote the code path that handles `parallel-N-` files. Does Bellows run them concurrently, sequentially, or does the Planner manually stagger them? (c) Is there any part of the dispatch code that could structurally support a "parent plan spawns child plans" primitive — e.g., could a plan step invoke `shutil.move` on a child plan into the executable queue, and would Bellows pick it up? Is there anything preventing this today? Report what would work, what would fail, and why. (d) Where in the codebase would an orchestrator-plan abstraction naturally land — new module, extension to `bellows.py`, new gate type, or cross-cutting changes? Identify the smallest change surface that would enable orchestrator-plan support. (e) Are there known Bellows backlog items that touch this area? Scan `bellows/knowledge/BACKLOG.md` for items related to plan-to-plan dispatch, cross-plan coordination, or orchestrator concepts. Report match or no-match per scan term; quote any matching items. **Output format.** Deposit findings to `bellows/knowledge/research/orchestrator-support-current-state-2026-04-23.md`. Structure as four Q sections, each with subsections (a)–(e) as specified, each subsection containing literal code quotes with file:line citations, direct answers, or explicit "no match found" when applicable. Do NOT summarize or interpret — just report evidence. End with Output Receipt per standard template: Agent, Step (1), Status (Complete / Partial / Blocked), What Was Done, Files Deposited, Files Created or Modified (Code) (None), Decisions Made (None), Flags for CEO, Flags for Next Step. **Deposits:** - `bellows/knowledge/research/orchestrator-support-current-state-2026-04-23.md` **Feedback:** Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **End-of-step:** Report completion in conversation. Do NOT move the plan to Done — Planner handles housekeeping per Rule 22 after reading findings.
