# Bellows — Daemon-Owned Ledgers Phase 1: Feedback→DB Mechanism (Dormant, Coexistence-Safe)
**Date:** 2026-06-13 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 42, Phase 1 of the phased migration (CEO fork verdicts in processed-verdict-42-step-1): relocate the highest-frequency append-only ledger — `agent-prompt-feedback.md` — OUT of the worktree merge path into `lifecycle.db`, with the `.md` becoming a generated artifact. Phase 1 ships the daemon-side MECHANISM only, deliberately DORMANT and coexistence-safe: while the current feedback protocol still has agents write `agent-prompt-feedback.md` in their worktree, the daemon detects that (via `files_changed`) and skips its own write — so nothing double-writes and nothing breaks. The mechanism activates only when the SEPARATE governance follow-on flips agents to emit feedback via the Output Receipt channel instead of writing the file (that follow-on also freezes the existing 309KB file as an archive and makes generation the live producer). Therefore Phase 1 does NOT yet close FORWARD rows 4/5/13 — the conflict is fixed when governance activates the mechanism; Phase 1's job is to land and prove the daemon-side capability. The design doc `knowledge/research/daemon-owned-ledgers-design-2026-06-13.md` is AUTHORITATIVE for all specifics — Section 3 File 2 (feedback disposition), Section 4 (the `### Ledger Updates` Output Receipt channel; parser mirrors the existing `### Flags for CEO` extraction at parser.py:30–37), Section 6 Phase 1 (coexistence via files_changed). Re-verify its Verification Blocks at edit time; halt-and-report on divergence. Anchors (re-verify by grep): `parser.py` `def parse` (~:6) and the `ceo_flags`/`### Flags for CEO` extraction (~:30–37); teardown call sites in bellows.py (~:622 / ~:731); the lifecycle.py migration mechanism. DAEMON RESTART REQUIRED after close to load the new code + migration (flag in receipt).

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, then the design doc Sections 3 (File 2), 4, and 6 (Phase 1) IN FULL. **Scope is exactly: `lifecycle.py`, `parser.py`, `bellows.py`, `tests/test_lifecycle.py`, `tests/test_parser.py`, `tests/test_bellows.py` (create any that do not exist).**
> - **Migration + table:** add a `prompt_feedback` table to lifecycle.db via the existing migration mechanism (idempotent; update the schema CREATE block for fresh DBs). Columns per the design (Section 3 File 2): at minimum `(id, plan_id, step_number, agent, project, entry_text, created_at)` — finalize against the design doc. Add a writer `record_prompt_feedback(...)` (log-and-continue semantics like all lifecycle writes).
> - **Channel parse:** in `parser.py`, extend `parse` to extract a `### Ledger Updates` section from the agent Output Receipt — specifically its `feedback` subsection — mirroring the existing `### Flags for CEO` → `ceo_flags` extraction pattern (parser.py:~30–37). Return the extracted feedback text in the parsed dict (e.g. `parsed["ledger_updates"]["feedback"]`); absent section → None/empty, no error.
> - **Apply (dormant + coexistence):** add `_apply_ledger_updates(parsed, project_path, plan_id, files_changed)` and call it in bellows.py AFTER successful `_teardown_worktree()` at the teardown site(s) (~:622 / ~:731). For Phase 1 it handles ONLY feedback: if `agent-prompt-feedback.md` appears in `files_changed` (agent wrote it old-style) → SKIP (coexistence — log a debug line, do nothing); else if `parsed["ledger_updates"]["feedback"]` is present → call `record_prompt_feedback(...)`. Never write the feedback FILE in this function. Wrap in log-and-continue.
> - **Generation function (tested, not yet live producer):** add a `generate_feedback_md(project, db_path=None)` that returns the `agent-prompt-feedback.md` content from `SELECT ... FROM prompt_feedback WHERE project=? ORDER BY created_at DESC` (newest-first, matching current convention), read-only. It is exercised by tests but not wired to write any file in Phase 1 (the governance follow-on makes it the live producer).
> - Do NOT touch the feedback protocol rules, do NOT freeze or modify the existing `agent-prompt-feedback.md`, do NOT change PROJECT_STATUS/FORWARD handling — those are later phases / the governance follow-on.
>
> **Tests:** (a) migration adds `prompt_feedback` idempotently; (b) `record_prompt_feedback` + read back; (c) parser extracts `### Ledger Updates`→feedback (and returns empty when absent); (d) `_apply_ledger_updates` SKIPS when agent-prompt-feedback.md is in files_changed (coexistence), and WRITES the DB row when it is not and the receipt carries feedback; (e) `generate_feedback_md` renders rows newest-first. Then run the FULL suite (`python3 -m pytest tests/`) to an explicit pass/fail and READ THE TAIL. Write the dev log to `bellows/knowledge/development/daemon-ledgers-phase1-feedback-dev-log-2026-06-13.md`. Use `with open()`; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md` (yes — keep using the current protocol; Phase 1 is dormant). **Deposits:**
> - `bellows/knowledge/development/daemon-ledgers-phase1-feedback-dev-log-2026-06-13.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, and design doc Sections 4 + 6. **Verify, each with executed evidence (files into `knowledge/qa/evidence/daemon-ledgers-phase1-2026-06-13/`):** (1) **Full suite** — final 15 lines, zero failures, new-test count matches dev log; output to `full_suite_tail.txt`. (2) **Table + migration** — `sqlite3 "file:/Users/marklehn/Developer/GitHub/bellows/lifecycle.db?mode=ro" "PRAGMA table_info(prompt_feedback)"` shows the columns; re-running migration is a no-op; output to `table_migration.txt`. (3) **Parser channel** — grep + a unit invocation showing `### Ledger Updates`→feedback extraction mirrors the ceo_flags pattern; output to `parser_channel.txt`. (4) **Dormancy/coexistence proof (the load-bearing check)** — demonstrate via the tests that `_apply_ledger_updates` SKIPS the DB write when `agent-prompt-feedback.md` is in files_changed; confirm by grep that the function NEVER writes the feedback file and that the existing `agent-prompt-feedback.md` is untouched by this plan (`git -C /Users/marklehn/Developer/GitHub/bellows log --oneline -1 -- knowledge/research/agent-prompt-feedback.md` predates this plan); output to `dormancy_proof.txt`. (5) **Generation** — `generate_feedback_md` renders fixture rows newest-first, read-only; output to `generation_check.txt`. (6) **No scope bleed** — `git diff HEAD~1 --stat` shows only the in-scope files (+ deposits); PROJECT_STATUS/FORWARD handling and the feedback protocol rules are untouched; output to `scope_check.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `daemon-ledgers-phase1-2026-06-13`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/daemon-ledgers-phase1-qa-report-2026-06-13.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/daemon-ledgers-phase1-2026-06-13/`; `required_evidence_files`: `[full_suite_tail.txt, table_migration.txt, parser_channel.txt, dormancy_proof.txt, generation_check.txt, scope_check.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table and the Rule 20 banner block to `bellows/knowledge/qa/daemon-ledgers-phase1-qa-report-2026-06-13.md`. **Receipt Flags for CEO must include:** (1) DAEMON RESTART REQUIRED — loads new code + migration; (2) Phase 1 is DORMANT (coexistence) — it does NOT close FORWARD rows 4/5/13 yet; the governance follow-on activates it; (3) next up per the phased plan: Phase 2 (PROJECT_STATUS→daemon-post-merge) and the governance activation follow-on. Use `with open()`; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/daemon-ledgers-phase1-qa-report-2026-06-13.md`
> - `bellows/knowledge/qa/evidence/daemon-ledgers-phase1-2026-06-13/` (six evidence files per Rule 20 self-check)
