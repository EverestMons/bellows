# Bellows — scope_check Union Fix Implementation
**Date:** 2026-06-10 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** full-suite | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step

## Context (Rule 27)
Implements the approved design `bellows/knowledge/architecture/scope-check-false-positive-fix-design-2026-06-09.md` (diagnostic in Done/). CEO disposition: ship the union-of-all-step-texts fix; DECLINE the blueprint-follow parser (#9 residue handled by Planner discipline). Closes FP #2 (continuous-run cross-step leakage) in code; partially addresses #9 (authorizes the blueprint file, not files listed inside it). Purely additive change to `_gate_scope_check` — only moves files from out-of-scope to in-scope, never the reverse; no `bellows.py` change (gates.check already receives full plan_text). **Self-fix constraints:** daemon restart required to activate (gates.py loads at startup); BOTH steps explicitly name `gates.py` and `tests/test_gates.py` so the pre-fix daemon's own scope_check authorizes the cumulative diff at the QA terminal step.

---
---

## STEP 1 — Bellows Developer

---

> Before starting, read `bellows/knowledge/architecture/scope-check-false-positive-fix-design-2026-06-09.md` and check the Output Receipt status; if not Complete, halt and report to the CEO. **Pre-edit verification (Rule 39):** re-run the design's Section 6 Verification Blocks (V1-V6) against current code before editing; if any actual output differs materially from the recorded output, STOP — do not edit — and deposit a verification-mismatch report to `bellows/knowledge/flags/verification-mismatch-bellows-scope-check-fp-fix-impl-2026-06-10-step-1.md` for Planner triage. **Then** read your specialist file. Implement the union fix in `gates.py` per design Section 2 — reference it as your checklist, do not restate (Rule 11): in `_gate_scope_check` (around gates.py:666), replace the single-step `step_text = _extract_step_text(plan_text, step_number)` extraction with a union built by iterating `_extract_step_text(plan_text, s)` for s in `range(1, step_number + 1)`, collecting non-empty results, and joining them into `union_text`; use `union_text` as the haystack in the path/basename check (gates.py:677) and the directory-mention check (gates.py:691); retain the current step's `step_text` SOLELY for the evidence-display context at gates.py:700. The change is purely additive — it must not alter the allowlist checks, the directory-mention depth guard, the failure-append, or any other clause; it only widens the text haystack. Add the 4 tests from design Section 4 to `tests/test_gates.py`: 2 positive (`test_scope_check_union_authorizes_earlier_step_file`, `test_scope_check_union_authorizes_file_from_step_1_at_step_2`) and 2 NEGATIVE proving teeth (`test_scope_check_union_still_rejects_unmentioned_file`, `test_scope_check_union_does_not_blanket_authorize_across_dirs`). Run `pytest tests/` to a pass/fail result and read the tail (do not infer green from a subset). **Scope (scope_check pre-empt):** modify ONLY `gates.py` and `tests/test_gates.py` — both named here so the pre-fix daemon's own scope_check authorizes them; if an edit needs any other file, halt and report rather than expanding scope. All file writes use `with open()`; no bash heredocs. Do NOT `git push`. Write the dev log, then commit (`git add` + `git commit`, no push). Standard prompt feedback → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/scope-check-fp-fix-impl-2026-06-10.md`

---
---

## STEP 2 — Bellows QA

---

> **MANDATORY — Rule 20 self-check gate (do this, not just the table).** The `rule_20_self_check` gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` immediately followed by a line beginning `PASSED — SELF-CHECK PASSED`. The verification table does NOT satisfy this gate. After writing the report, run the canonical block and self-grep your own report for that banner before finishing — if it is absent the plan halts on re-dispatch. **Then:** Before starting, read `bellows/knowledge/development/scope-check-fp-fix-impl-2026-06-10.md` and check the Output Receipt status; if not Complete, halt and report. Read your specialist file. **FIRST — Deliverable Verification (Rule 17).** Verify `gates.py` holds the union construction in `_gate_scope_check` (grep for the `range(1, step_number + 1)` iteration and `union_text`), and `tests/test_gates.py` holds the 4 new tests by name. Produce `| Deliverable | Expected | Status (✅/❌) | Evidence |`; any ❌ → fix or halt, do NOT close. **Checks (deposit raw output to evidence per Rule 18; never ✅ with hedging per Rule 19):** (1) **Union positive** — the 2 positive tests in `tests/test_gates.py` pass (earlier-step / step-1-at-step-2 files authorized) → `union_positive.txt`. (2) **Teeth (load-bearing)** — the 2 negative tests pass AND an explicit ad-hoc check shows a genuinely-unmentioned file still produces a `scope_check` failure → `teeth.txt`. (3) **Additive-only regression** — the pre-existing single-step `scope_check` tests (`test_scope_check_passes_when_files_in_plan`, `test_scope_check_fails_when_file_not_in_plan`) still pass unchanged → include in `pytest_full.txt`. (4) **Full suite** — `pytest tests/`, read the tail, green (>= prior baseline + 4 new) → `pytest_full.txt`. Then run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root with `plan_slug`: `bellows-scope-check-fp-fix-impl-2026-06-10`, `qa_report_path`: `<absolute path to this QA report>`, `evidence_dir`: `<absolute path to the evidence dir>`, `required_evidence_files`: `[union_positive.txt, teeth.txt, pytest_full.txt, deliverable_grep.txt]`; include literal stdout; if FAILED, halt and report. **Final (Rule 8/23 order):** deliverable verification → QA deposit → update `PROJECT_STATUS.md` (union scope_check fix shipped in `gates.py`/`tests/test_gates.py`; closes BACKLOG #2 fully, #9 cross-step partial; DAEMON RESTART REQUIRED to activate) → feedback append → final commit (`git add` + `git commit`, no push). **STOP — do NOT move this plan to Done;** the close path is owned by Bellows on the Planner's continue verdict. (Note: `gates.py` and `tests/test_gates.py` are named in this step so the pre-fix daemon's terminal-step scope_check authorizes the cumulative diff.) **Deposits:**
> - `bellows/knowledge/qa/scope-check-fp-fix-impl-2026-06-10-qa.md`
> - `bellows/knowledge/qa/evidence/bellows-scope-check-fp-fix-impl-2026-06-10/` (multiple files per Rule 20 self-check)
