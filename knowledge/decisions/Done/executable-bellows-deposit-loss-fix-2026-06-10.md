# Bellows — Deposit-Loss Fix: Auto-Commit Declared Deposits + Fail-Loud Gate
**Date:** 2026-06-10 | **Tier:** Medium | **Dispatch Mode:** bellows | **Test Scope:** full-suite | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step

## Context (Rule 27)
Implements the approved design `bellows/knowledge/architecture/silent-deposit-loss-teardown-fix-design-2026-06-10.md` (diagnostic in Done/). Root cause (proven): a deposit survives teardown iff committed to the worktree branch; teardown merges committed changes only, untracked/unstaged files are destroyed at worktree removal while `deposit_exists` reads green and `files_changed=0`. CEO disposition: ship (a)+(b) with a PLANNER ORDERING CORRECTION. **(a) PRESERVE:** Bellows auto-commits the plan-declared deposit paths (path-scoped `git add`, NOT `git add -A`) immediately BEFORE `gates.check()` at step completion, so `deposit_exists` evaluates post-commit state and the teardown merge lands them. **(b) FAIL-LOUD:** extend `_gate_deposit_exists` to fail when a declared deposit exists on disk but is still uncommitted — the residue net for declared deposits (a) could not resolve/commit. Layer-1 mechanical (plan-declared paths only, no content judgment). **CRITICAL — additive only:** must NOT alter `_teardown_worktree` land-or-raise, the Gap 1b/1c guards, or the stranded-cleanup preserve-branch logic. Scoped to declared deposits — uncommitted non-declared changes (e.g., source) are out of scope (DEV-commit discipline covers; "fully loss-proof worktree" is a candidate follow-on). **Daemon restart required to activate.** **Self-fix note:** this plan runs under the PRE-FIX daemon, which still loses uncommitted deposits — so BOTH agents MUST commit all their work (code + dev log + QA report) before finishing or it will be lost at teardown. `bellows.py`/`gates.py`/`tests/test_worktree.py`/`tests/test_gates.py` are named in both steps so the (now-live union) scope_check authorizes the cumulative diff.

---
---

## STEP 1 — Bellows Developer

---

> Before starting, read `bellows/knowledge/architecture/silent-deposit-loss-teardown-fix-design-2026-06-10.md` and check the Output Receipt status; if not Complete, halt and report to the CEO. **Pre-edit verification (Rule 39):** re-run the design's Section 6 Verification Blocks (1-6) against current code before editing; if any actual output differs materially from the recorded output, STOP — do not edit — and deposit a verification-mismatch report to `bellows/knowledge/flags/verification-mismatch-bellows-deposit-loss-fix-2026-06-10-step-1.md` for Planner triage. **Then** read your specialist file. Implement (a)+(b) per design Section 3 — reference it as your checklist (Rule 11) — WITH THE ORDERING CORRECTION below, which OVERRIDES the design's "before `_teardown_worktree`" placement. **(a) PRESERVE:** add a helper `_auto_stage_deposits(plan_text, plan_header, project_path, wt_path, slug)` to `bellows.py` that (1) extracts the plan-declared deposit paths via `gates._extract_plan_required_deposits(plan_text)` plus `plan_header.get("deposits")`, (2) resolves each against the worktree with `_resolve_deposit_path(path, project_path, wt_path=wt_path)`, (3) for each resolved path that exists but is untracked/unstaged (`git status --porcelain -- <path>`), runs a path-scoped `git add <path>` (NEVER `git add -A`), (4) if anything was staged, creates one commit `bellows: auto-stage declared deposits before teardown` with a visible log line, (5) is a clean no-op when all declared deposits are already committed. **Call `_auto_stage_deposits(...)` immediately BEFORE each `gates.check()` invocation at step completion** (so `deposit_exists` evaluates the post-commit state and `files_changed` captures the now-committed deposits) — re-derive the exact call site(s) from current `bellows.py` rather than trusting line numbers. **(b) FAIL-LOUD:** extend `gates._gate_deposit_exists` so that, after confirming a declared deposit exists on disk (current behavior preserved), it additionally runs `git status --porcelain -- <path>` in the worktree and, if the path is untracked/unstaged, appends a gate failure with evidence `deposit_uncommitted: <path> exists on disk but is not committed — will be lost at teardown`. **CRITICAL — additive only:** do NOT modify `_teardown_worktree` itself, the land-or-raise contract, the Gap 1b/1c guards, or the stranded-cleanup preserve-branch logic; state in your dev log how (a) interacts with each and confirm additivity. Add the new tests from design Section 4 to `tests/test_worktree.py` (`test_auto_stage_preserves_untracked_deposit_on_teardown`, `test_auto_stage_handles_multiple_deposits`, `test_auto_stage_noop_when_all_committed`) and `tests/test_gates.py` (`test_gate_fails_on_uncommitted_deposit`, `test_gate_deposit_uncommitted_evidence_message`, `test_gate_passes_when_deposit_committed`). Run `pytest tests/` to a pass/fail result and read the tail (do not infer green from a subset). **Scope (scope_check):** modify ONLY `bellows.py`, `gates.py`, `tests/test_worktree.py`, `tests/test_gates.py` — all named here; if an edit needs any other file, halt and report. All file writes use `with open()`; no bash heredocs. **COMMIT EVERYTHING (code + dev log) with `git add` + `git commit` before finishing — the pre-fix daemon does NOT preserve uncommitted files, so anything left uncommitted is lost at teardown (the very bug).** Do NOT `git push`. Standard prompt feedback → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/deposit-loss-fix-2026-06-10.md`

---
---

## STEP 2 — Bellows QA

---

> **MANDATORY — Rule 20 self-check gate (do this, not just the table).** The `rule_20_self_check` gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` immediately followed by a line beginning `PASSED — SELF-CHECK PASSED`. The verification table does NOT satisfy this gate. After writing the report, run the canonical block and self-grep your own report for that banner before finishing — if it is absent the plan halts on re-dispatch. **Then:** Before starting, read `bellows/knowledge/development/deposit-loss-fix-2026-06-10.md` and check the Output Receipt status; if not Complete, halt and report. Read your specialist file. **FIRST — Deliverable Verification (Rule 17).** Verify `bellows.py` has `_auto_stage_deposits` and calls it immediately before `gates.check()` (grep the call site ordering — it must precede the gate, not the teardown), and `gates.py` `_gate_deposit_exists` has the `git status --porcelain` uncommitted check; confirm the 6 new tests exist by name. Produce `| Deliverable | Expected | Status (✅/❌) | Evidence |`; any ❌ → fix or halt, do NOT close. **Checks (deposit raw output to evidence per Rule 18; never ✅ with hedging per Rule 19):** (1) **Preserve (load-bearing)** — `test_auto_stage_preserves_untracked_deposit_on_teardown` + `test_auto_stage_handles_multiple_deposits` pass, proving an untracked declared deposit now survives teardown → `preserve.txt`. (2) **Fail-loud residue** — `test_gate_fails_on_uncommitted_deposit` + evidence-message test pass → `fail_loud.txt`. (3) **No-op + ordering** — `test_auto_stage_noop_when_all_committed` passes AND confirm via grep that `_auto_stage_deposits` precedes `gates.check()` (ordering correction in place, so (b) sees post-commit state) → `ordering_noop.txt`. (4) **Hardened-teardown regression (CRITICAL)** — `test_teardown_merges_commits`, `test_teardown_proceeds_on_empty_commit_list`, `test_landing_tolerates_dirty_main_invariant`, `test_teardown_raises_on_git_log_exception` all still pass unchanged → `teardown_regression.txt`. (5) **Full suite** — `pytest tests/`, read tail, green (≥ prior baseline + 6 new) → `pytest_full.txt`. Then run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root with `plan_slug`: `bellows-deposit-loss-fix-2026-06-10`, `qa_report_path`: `<absolute path to this QA report>`, `evidence_dir`: `<absolute path to the evidence dir>`, `required_evidence_files`: `[preserve.txt, fail_loud.txt, ordering_noop.txt, teardown_regression.txt, pytest_full.txt, deliverable_grep.txt]`; include literal stdout; if FAILED, halt and report. **Final (Rule 8/23 order):** deliverable verification → QA deposit → update `PROJECT_STATUS.md` (deposit-loss fix shipped; auto-commit declared deposits + fail-loud gate; DAEMON RESTART REQUIRED) and update the BACKLOG #1 entry status in `knowledge/BACKLOG.md` to reference this plan → feedback append → final commit (`git add` + `git commit`, no push). **COMMIT ALL your work before finishing — the pre-fix daemon does not preserve uncommitted files.** **STOP — do NOT move this plan to Done;** the close path is owned by Bellows on the Planner's continue verdict. (`bellows.py`, `gates.py`, `tests/test_worktree.py`, `tests/test_gates.py`, `PROJECT_STATUS.md`, `knowledge/BACKLOG.md` are named here so the union scope_check authorizes the cumulative diff.) **Deposits:**
> - `bellows/knowledge/qa/deposit-loss-fix-2026-06-10-qa.md`
> - `bellows/knowledge/qa/evidence/bellows-deposit-loss-fix-2026-06-10/` (multiple files per Rule 20 self-check)
