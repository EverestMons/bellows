# Bellows — Deposit-Path Format Survey Diagnostic
**Date:** 2026-04-18 | **Tier:** Small (Diagnostic) | **Test Scope:** n/a (diagnostic — no code changes) | **Execution:** Step 1 (Investigation Agent) → [Planner verification per Rule 22]

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 (investigation only — no code changes). After completing Step 1, the agent deposits findings and reports Complete via Output Receipt. The Planner then reads the deposited findings file (Rule 22 verification) and performs housekeeping directly. No Step 2.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bellows-deposit-path-formats-2026-04-18.md. Execute Step 1. After depositing findings and reporting Complete, STOP — do NOT move the plan to Done. The Planner will verify findings and handle housekeeping.
```

---
---

## STEP 1 — INVESTIGATION AGENT

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-bellows-deposit-path-formats-2026-04-18.md", "bellows/knowledge/decisions/in-progress-diagnostic-bellows-deposit-path-formats-2026-04-18.md")`. **Skip specialist file and glossary reads — this is a text-pattern survey task across a file corpus, no domain interpretation required.** Characterize the formats that plan-step text uses to indicate PRIMARY deposit paths (the main knowledge artifact a step produces — research findings file for diagnostics, QA report for executable QA steps, dev log for executable DEV steps). This survey exists to support Plan B for verdict.py — adding a `**Deposit:**` field to verdict request files populated by a parser that extracts the primary deposit path from the plan-step text at verdict-posting time. The parser lives in `bellows.py`, not `verdict.py`. **Survey scope — Bellows-watched projects only, recent sample:** survey the eight projects Bellows currently watches per `bellows/config.json`: `invoice-pulse`, `BrewBuddy`, `study`, `ai-career-digest`, `freight-kb`, `forge`, `anvil`, `bellows`. For each project: (1) list ALL files currently in `[project]/knowledge/decisions/` (NOT in Done — these are active or pending templates), and (2) list the 12 most recently modified files in `[project]/knowledge/decisions/Done/` by mtime. Combined target: approximately 100 plan files across the eight projects. Report the actual per-project file count in your findings — if any project has fewer than 12 in Done, note that and sample all available. **Scope boundary — PRIMARY deposit only:** for each step within each plan, identify the single primary deposit — the main knowledge file the step produces. IGNORE evidence files (`knowledge/qa/evidence/<plan-slug>/*.txt`), PROJECT_STATUS.md updates, feedback log appends, commit hashes, and any secondary artifacts. The Planner's Rule 22 use case needs ONE deposit path per step — the one to open and read to verify the step's primary outcome. If a step has no primary deposit (housekeeping-only steps, gate-setup steps), note that and move on — don't invent a primary. **Investigation phases — two passes:** (Pass 1 — shallow inventory) for every file in the corpus, grep or scan for all lines containing any of: `**Deposit:**`, `Deposit:`, `deposit `, `write findings to`, `write the QA report to`, `write a dev log to`, `deposited to`, `deposit findings to`, `Output location:`, `Save to`, `save to`, `save output to`, `save the QA report to`, `save the dev log to`, and any other phrase that appears to introduce a deposit. For each match, capture (i) the project, (ii) the plan filename, (iii) the step number (parse from `## STEP N` headers), (iv) the exact line or phrase matched, (v) whether the path that follows is backticked / bolded / plain / bracketed-placeholder (e.g., `[project]/knowledge/...`), (vi) whether there is a label preceding the path or the path is inline in prose. Deposit this raw inventory as a table — one row per match — in the findings file under a section `## Pass 1 — Raw Inventory`. **Pass 2 — format clustering and frequency analysis:** group the Pass 1 matches into distinct format patterns. Example cluster names (feel free to introduce your own as the data warrants): `labeled-bolded-backticked` (`**Deposit:** write findings to \`path\``), `labeled-bolded-plain` (`**Deposit:** write findings to path`), `labeled-plain` (`Deposit: path`), `inline-prose-backticked` (`write the QA report to \`path\``), `inline-prose-plain` (`write findings to path`), `bracketed-placeholder` (`write findings to \`[project]/knowledge/...\``), `no-label-inline` (`deposited to \`path\` at the end of the step`), and so on. For each cluster, report: (a) cluster name, (b) canonical example (one representative line, verbatim), (c) count of occurrences across the corpus, (d) list of project+plan references for 3 example matches, (e) whether the path is reliably extractable via a single regex pattern (and if so, what pattern), (f) edge cases observed (multi-line, embedded in tables, split across sentences). Deposit this as a table in a section `## Pass 2 — Format Clusters`. **Parser strategy evaluation:** evaluate three parser strategies against the Pass 2 frequency data. For each strategy, report: (i) description, (ii) percent of corpus deposits it captures, (iii) false positive risk, (iv) complexity (lines of code estimate), (v) whether it requires changes to PLANNER_TEMPLATE conventions or existing plans. **Strategy A — Strict labeled field.** Require every plan step to have an explicit `**Deposits:**` field as a new convention; parser extracts only from that field. Change to convention: yes. **Strategy B — Opportunistic regex against existing formats.** Parser handles the top N format clusters from Pass 2 via regex; lower-frequency clusters are missed. Change to convention: no. **Strategy C — Hybrid.** Parser prefers the strict `**Deposits:**` field if present; falls back to opportunistic regex if absent. Captures existing corpus AND allows new plans to opt into the strict path. Change to convention: yes (additive). Deposit this as a section `## Parser Strategy Evaluation`. **Proposed implementation:** based on the Pass 2 data, recommend which strategy Plan B should pursue. Include: (a) the regex patterns the parser should implement (in Python raw-string form, tested against examples from Pass 1 if possible), (b) the expected false-positive rate, (c) handling for steps with no primary deposit (return None? empty list? skip the Deposit field entirely?), (d) handling for the bracketed-placeholder case (paths like `[project]/knowledge/...` — should the parser expand them using the plan's project context or treat them as unresolved?). Deposit this as a section `## Proposed Implementation`. **Gap Assessment (same shape as the previous diagnostic):** produce a table with columns `| Gap | Current State | Proposed State | Change Required in bellows.py |`. Enumerate every distinct issue that would need a code change — the parser regex itself, the caller site in `post_verdict_request`, any helper functions, tests. This table is what Plan B's executable will cite directly, so make it specific. Deposit as section `## Gap Assessment`. **Do NOT write any code changes in this plan.** If a regex is proposed, it is a proposal — not a committed implementation. Plan B will be the executable that implements the recommendation. **Deposit:** write findings to `bellows/knowledge/research/deposit-path-formats-2026-04-18.md` using canonical Python file write pattern: define the full findings content as a triple-quoted string in a variable, then `with open("bellows/knowledge/research/deposit-path-formats-2026-04-18.md", "w") as f: f.write(content)`. Do NOT use heredoc syntax or `python3 -c` — both are banned per PLANNER_TEMPLATE Compression Principles. Include an Output Receipt at the end of the findings file per the template format. Within the Output Receipt, cite the actual per-project file counts surveyed (e.g., "invoice-pulse: 14 files surveyed (2 active + 12 Done), BrewBuddy: 13 files (1 active + 12 Done), ...") — this is the completeness evidence the Planner checks against during Rule 22 verification. **Feedback:** standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT move the plan to Done. The Planner performs Rule 22 verification on the findings file and handles housekeeping directly.**
