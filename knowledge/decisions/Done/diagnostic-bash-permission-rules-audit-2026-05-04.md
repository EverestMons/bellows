# Bellows — Bash Permission Rules Audit Across Watched Projects
**Date:** 2026-05-04 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1 (SA)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and reports findings. The Planner reads the deposited findings file directly per Rule 22 and decides next steps.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bash-permission-rules-audit-2026-05-04.md. Execute Step 1.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-bash-permission-rules-audit-2026-05-04.md", "bellows/knowledge/decisions/in-progress-diagnostic-bash-permission-rules-audit-2026-05-04.md")`. Skip specialist file and glossary reads — this is a settings-file audit task, mechanical inventory. **Background:** the prior diagnostic (`diagnostic-claude-p-prompt-suppression-2026-05-04`) found that Bash, Edit, Write are pre-authorized in Bellows dispatches via `--allowedTools` so they never prompt. The remaining safety net for destructive Bash commands is per-command rules from each project's `.claude/settings.local.json` (or `.claude/settings.json`). Bellows has zero visibility into what rules exist where. This audit produces the inventory. **Investigation scope — read-only audit, do NOT modify any settings file:** **(1) Enumerate the 8 watched projects** from `bellows/config.json` — they are: `invoice-pulse`, `BrewBuddy`, `study`, `ai-career-digest`, `freight-kb`, `forge`, `anvil`, `bellows`, all under `/Users/marklehn/Desktop/GitHub/`. Also include the governance root itself (`/Users/marklehn/Desktop/GitHub/`) since `bellows/` lives inside it and inherits its settings — that's 9 audit targets total. **(2) For each of the 9 targets, check for the following files in this order:** (a) `.claude/settings.local.json` (project-local user settings, gitignored), (b) `.claude/settings.json` (project-shared settings, committed), (c) `.claude/settings/` directory if it exists. Use `os.path.exists` for each path. Record presence/absence per target. **(3) For every settings file that exists, read it in full and parse the JSON.** Record: (a) the `permissions` block if present, (b) specifically the `allow`, `deny`, `defaultMode`, and `additionalDirectories` keys within `permissions`, (c) any `Bash(...)` patterns in allow/deny lists — quote each pattern verbatim, (d) any other permission-shaped keys (hooks, mcpServers permission overrides, etc.). **(4) Cross-reference with user-level settings.** Check `~/.claude/settings.json` and `~/.claude/settings.local.json` (CEO's home directory). User-level settings apply to ALL Claude Code sessions including `claude -p` dispatches. Document presence/absence and any Bash permission patterns found there. **(5) Build the per-project Bash rules table.** Columns: (a) Project name; (b) settings.local.json present (yes/no); (c) settings.json present (yes/no); (d) Bash deny patterns (verbatim list, or "none"); (e) Bash allow patterns (count + 2-3 representative examples, or "none"); (f) defaultMode for permissions ("default"/"acceptEdits"/"plan"/"bypassPermissions"/absent); (g) Risk classification: 🔴 No deny rules AND no defaultMode tightening (broad approval surface), 🟡 Allow patterns exist but no deny rules (specific approvals, no destructive guards), 🟢 Deny patterns block destructive commands (e.g., `rm -rf`, `git push --force`, `git reset --hard`), 🔵 Settings file absent (relies entirely on user-level + Bellows allowedTools). **(6) Identify high-risk gaps.** For any project classified 🔴 or 🔵, list specifically what destructive Bash commands could currently auto-approve under Bellows dispatch. Be concrete: "in project X, no deny rules exist for `rm`, `git push --force`, `git reset --hard`, or `chmod`, so an agent invoking any of these would get pre-approved by --allowedTools and execute without per-command CEO review." **(7) Cross-check against actual Bellows behavior.** From the prior diagnostic's log evidence (Sample 3 in `bellows/knowledge/research/claude-p-prompt-suppression-inventory-2026-05-04.md`), a 2026-04-17 Bash denial was observed against a `rm -f .git/index.lock 2>/dev/null; git add ...` command. Determine which settings file produced that denial — was it project-local invoice-pulse, user-level, or governance-root settings? Quote the matching deny pattern if findable. This validates that the per-command rule layer is actually firing in production. **(8) Recommendations section — minimal, no implementation.** List 3-5 concrete recommendations the CEO could adopt to harden the per-command Bash safety net across all watched projects. Do NOT propose a fix plan yet — just enumerate options. Examples might include: a shared `.claude/settings.json` template with deny rules for destructive commands, a Bellows-side gate that warns when a watched project lacks settings.json, or a documentation entry in PLANNER_TEMPLATE on minimum settings hygiene. Rank by leverage and effort. **Deposit:** write findings to `bellows/knowledge/research/bash-permission-rules-audit-2026-05-04.md` using `Filesystem:write_file` (or canonical Python `with open(...) as f: f.write(content)` pattern if MCP write isn't reachable). The file structure: (a) one-paragraph executive summary, (b) Critical Findings section if any (severity-coded 🔴/🟡/🔵 per Security & Testing convention), (c) Per-project Bash rules table (the main deliverable), (d) User-level settings summary, (e) High-risk gaps section, (f) Production behavior validation (the Sample 3 cross-check), (g) Recommendations (ranked), (h) Output Receipt. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/bash-permission-rules-audit-2026-05-04.md`
>
> **STOP. Report findings. Do NOT propose or implement fixes — this is investigation only. The Planner will read the findings file directly and decide what (if anything) needs to ship.**
