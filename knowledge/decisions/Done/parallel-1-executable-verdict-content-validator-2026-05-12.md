# bellows — Verdict Content Schema Validator
**Date:** 2026-05-12 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1 (BELLOWS_DEVELOPER) → Step 2 (BELLOWS_QA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation ("ok") before proceeding to Step 2.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/parallel-1-executable-verdict-content-validator-2026-05-12.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

## CEO Context

Verdict content schema validator. Implements Artifact 1 / Response A+C from `bellows/knowledge/research/planner-authored-contract-validation-step-2-evaluation.md`. Transforms the silent-skip failure mode in `check_verdict()` (verdict.py:160-181) into a loud failure: when a verdict file exists, is non-empty, but the first line doesn't match the expected pattern, emit a WARN log with the actual first line and expected format, and post a Pushover notification deduped per (filename, failure-type) for the daemon lifetime. Plus observability: log every file scanned in `verdicts/resolved/` with parse outcome.

The current silent-skip behavior has caused 3 known incidents in 12 days (2026-05-01, 2026-05-09, 2026-05-12) where correctly-intended verdicts were silently discarded because the format didn't match the regex. Plans stranded for hours each time.

Parallel-1 with `parallel-1-executable-verdict-directory-validator-2026-05-12.md`. Different production files (`verdict.py` vs `bellows.py`), different test files — no conflicts.

---
---

## STEP 1 — BELLOWS_DEVELOPER

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/parallel-1-executable-verdict-content-validator-2026-05-12.md", "bellows/knowledge/decisions/in-progress-parallel-1-executable-verdict-content-validator-2026-05-12.md")`. You are the Bellows Developer. Read your specialist file and domain glossary first. Read `bellows/knowledge/research/planner-authored-contract-validation-step-2-evaluation.md` (Part 3, Artifact 1 section) and `bellows/knowledge/qa/evidence/planner-authored-contract-validation-2026-05-12/consumer-code-grep-transcripts.md` (Artifact 1 section) for the exact target code. **Target:** `verdict.py::check_verdict()` (currently lines 160-181). **Change 1 — schema validator:** between the empty-lines check and the regex match, when the file exists and is non-empty, if `re.match(r"^(?:verdict:\s*)?(continue|stop)$", first_line, re.IGNORECASE)` returns None, log a WARN line via the existing logging mechanism (use the same logger pattern as elsewhere in `verdict.py`; if no logger exists in the module, use `print` to stderr with timestamp prefix matching other Bellows stderr output) with the literal message format: `verdict file malformed: <filepath> — first line: <first_line!r> — expected pattern: 'continue', 'stop', 'verdict: continue', or 'verdict: stop' (case-insensitive)`. Then call a new helper `_notify_malformed_verdict(filepath, first_line)` that posts a Pushover notification with title "Bellows — Malformed Verdict" and body `<filename> — first line: <first_line>` deduped via a module-level `_NOTIFIED_MALFORMED: set[tuple[str, str]] = set()` set keyed by `(str(filepath), "malformed_content")`. Skip Pushover if the key is already in the set; add the key after a successful notify. Use `notifier.push(title, body)` for the Pushover call (import at top of file if not already imported). Wrap the Pushover call in try/except to swallow notification failures — they must never break verdict checking. Preserve the existing return contract: return `{"found": False}` regardless of whether the warning fired. **Change 2 — observability logging:** at the top of `check_verdict`, after computing `filepath`, log a DEBUG-level (or stderr if no DEBUG level exists; gate behind a module-level constant `VERDICT_PARSE_LOG_VERBOSE = False` so it can be flipped on for diagnosis without code change) line of the form `verdict scan: <filepath> — exists=<bool> — outcome=<outcome>` where `outcome` is one of `not_found`, `empty`, `malformed`, `parsed_<verdict>`. This is additive — `outcome` is computed at each early-return point. Do NOT change any return value or call signature. **Tests:** add at minimum these tests to `tests/test_verdict.py` (create the file if absent — but it exists per the consumer-code-grep evidence) — (a) `test_check_verdict_logs_warning_on_malformed_first_line` — write a verdict file with first line `not a verdict`, assert capsys captures the WARN with the expected format and the file path, assert return value is `{"found": False}`. (b) `test_check_verdict_pushover_deduped_per_file_per_daemon_lifetime` — write the same malformed file, call `check_verdict` twice in succession, monkeypatch `notifier.push` to track call count, assert called exactly once. (c) `test_check_verdict_no_warning_on_empty_file` — write an empty file (or whitespace-only), assert no WARN fires, assert return is `{"found": False}`. (d) `test_check_verdict_no_warning_on_missing_file` — don't create the file, assert no WARN fires, assert return is `{"found": False}`. (e) `test_check_verdict_well_formed_verdict_continue` — write a file with first line `continue`, assert return is `{"found": True, "verdict": "continue", "reason": ""}`, assert no WARN. (f) `test_check_verdict_well_formed_verdict_stop_with_reason` — write a file with `verdict: stop\nrationale text`, assert return contract is preserved. (g) `test_check_verdict_pushover_failure_swallowed` — monkeypatch `notifier.push` to raise, write a malformed file, assert `check_verdict` returns normally with `{"found": False}` (the try/except guard works). **Use pytest fixtures with tmp_path; reset `_NOTIFIED_MALFORMED.clear()` in setup of dedupe-sensitive tests.** Run targeted `pytest tests/test_verdict.py -v` and confirm all pass plus no regressions in pre-existing tests in that file. Run full suite `pytest -x --tb=short` and confirm only the pre-existing `test_run_step_timeout` failure remains (no other regressions). Commit with message: `feat(verdict): schema validator + observability for check_verdict — closes silent-skip on malformed verdict files`. After your output receipt, read `bellows/knowledge/research/agent-prompt-feedback.md` and append a dated entry per the standard protocol. Commit the feedback entry with message: `docs: prompt feedback — DEV verdict content validator`. **Deposits:** - `bellows/knowledge/development/verdict-content-validator-2026-05-12.md` - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — BELLOWS_QA

---

> Before starting, read `bellows/knowledge/development/verdict-content-validator-2026-05-12.md` and check the Output Receipt status field. If status is not Complete or Partial with known acceptable scope, stop and report the issue to the CEO before proceeding. You are the Bellows QA Analyst. Read your specialist file and domain glossary first. Read the deposited dev log to understand what shipped. **Verify deliverables per Rule 17:** (1) `verdict.py::check_verdict()` has been modified — read the current code at the function and confirm the schema validator logic (regex check → WARN log + Pushover-with-dedupe) is in place; confirm the observability logging line is in place at the top with the `VERDICT_PARSE_LOG_VERBOSE` gate constant. (2) Module-level `_NOTIFIED_MALFORMED` set is declared. (3) `notifier.push` is imported. (4) The 7 new tests are present in `tests/test_verdict.py` with the exact function names listed in the Step 1 prompt; read each test to confirm the assertions match the test name's claim. (5) Run targeted `pytest tests/test_verdict.py -v` — confirm all 7 new tests pass plus all pre-existing tests pass. (6) Run full suite `pytest --tb=short` — confirm only `test_run_step_timeout` (pre-existing) fails. (7) Behavioral spot-check: write a Python REPL one-liner that imports `check_verdict`, creates a temp file with malformed content `garbage line\n`, calls `check_verdict(slug, step)` against it (slug and step chosen to construct the temp path), and confirms (a) return value is `{"found": False}`, (b) stderr captured the expected WARN. Run it and capture the output to `bellows/knowledge/qa/evidence/verdict-content-validator-2026-05-12/repl-malformed-spot-check.txt`. (8) Behavioral spot-check #2: same setup but with well-formed content `verdict: continue\nlooks good`, confirm return is `{"found": True, "verdict": "continue", "reason": "looks good"}`. Capture to `bellows/knowledge/qa/evidence/verdict-content-validator-2026-05-12/repl-wellformed-spot-check.txt`. **Rule 20 self-check:** run the canonical Python block from `/Users/marklehn/Desktop/GitHub/RULE_20_SELF_CHECK_BLOCK.md` against the evidence directory `bellows/knowledge/qa/evidence/verdict-content-validator-2026-05-12/`. **PROJECT_STATUS.md update:** append a 2026-05-12 entry covering this ship (commit SHA, files changed, test delta, REMINDER: restart Bellows daemon to load fix). **Deposit QA report at `bellows/knowledge/qa/verdict-content-validator-qa-2026-05-12.md`** with all 8 checks + Rule 20 self-check banner + Output Receipt Complete. Commit with message: `qa: verdict content validator — 7 new tests + 2 behavioral spot-checks pass; daemon restart required`. After your output receipt, read `bellows/knowledge/research/agent-prompt-feedback.md` and append a dated entry per the standard protocol. Commit the feedback entry with message: `docs: prompt feedback — QA verdict content validator`. **Deposits:** - `bellows/knowledge/qa/verdict-content-validator-qa-2026-05-12.md` - `bellows/knowledge/qa/evidence/verdict-content-validator-2026-05-12/` - `bellows/PROJECT_STATUS.md` - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT move the plan to Done — that is the Planner's responsibility after Rule 22 verification.**
