---
type: executable
project: bellows
created: 2026-05-12
auto_close: false
pause_for_verdict: after_step_1
---

# Executable — Intermediate Decision Surfacing in Verdict Requests + Vexp Read-Class Exemption

**Execution Map:** Step 1 (BELLOWS_DEVELOPER) → Step 2 (BELLOWS_DEVELOPER) → Step 3 (BELLOWS_QA)

**Context:** The 2026-05-12 capture audit found 2.7% Output Receipt capture rate. The 2026-05-12 detection-design diagnostic confirmed phrase-grep at F1=0.706 with perfect recall against the audit's 33-phrase list. This plan ships the Bellows-side fix that surfaces phrase-matched intermediate `assistant`-event text into a new optional section of the verdict request file. It also closes a gate-exemption gap exposed by the same detection-design diagnostic: `mcp__vexp__run_pipeline` is a code-exploration tool (read-equivalent) that tripped `no_permission_denials` as a blocking gate. Both fixes are deterministic Bellows-side changes; they ship as two DEV commits + one QA step.

**Reference deposits:**
- Audit findings: `bellows/knowledge/research/output-receipt-capture-audit-2026-05-12.md`
- Design diagnostic: `bellows/knowledge/research/intermediate-decision-detection-design-2026-05-12.md`
- Hand-classified ground truth: `bellows/knowledge/qa/evidence/intermediate-decision-detection-2026-05-12/labeled-blocks.jsonl`

---

## STEP 1 — BELLOWS_DEVELOPER

Read your specialist file and domain glossary first. Read the design diagnostic at `bellows/knowledge/research/intermediate-decision-detection-design-2026-05-12.md` for context — specifically Part 6's Bellows-side implementation sketch. **Do NOT trust the sketch's specific line numbers** — verify current code state before any edit. **Goal:** add phrase-based intermediate-decision detection to Bellows. Flagged blocks surface as a new `## Intermediate Decisions Detected` section in the verdict request file. **Part A — phrase-list file creation.** Create a new governance-root file at `/Users/marklehn/Desktop/GitHub/INTERMEDIATE_DECISION_PHRASES.md`. The file is the single source of truth for the phrase list, matching the single-source pattern established by `RULE_20_SELF_CHECK_BLOCK.md`. Header explains purpose: this file lists phrases used by Bellows's intermediate-decision detector to flag `assistant`-event text blocks in the NDJSON stream that likely represent substantive agent decisions narrated mid-step. Match is case-insensitive substring. The list is the 33 phrases from the 2026-05-12 audit's final extended grep list, available at `bellows/knowledge/research/output-receipt-capture-audit-2026-05-12.md` (Final Extended Grep List section). Copy those 33 phrases verbatim, one per line, prefixed with `- ` (markdown bullet). Include a closing paragraph stating that changes to this file take effect on next Bellows daemon restart. **Part B — detector module.** Add a new module `bellows/decisions.py` exposing `load_phrases() -> list[str]` (reads `INTERMEDIATE_DECISION_PHRASES.md` from governance root, parses bullet lines, returns lowercased list — cache the result at module load) and `extract_decision_blocks(raw_output: str | list, phrases: list[str]) -> list[dict]` (parses NDJSON stream OR accepts pre-parsed event list, iterates `assistant` events, extracts `content[].type == "text"` blocks, returns list of `{"event_idx": int, "text": str, "matched_phrases": list[str]}` for blocks matching at least one phrase). Edge cases: handle missing/malformed NDJSON gracefully (return empty list, log warning), handle empty raw_output (return empty list), handle assistant events with multiple content items. Truncate stored text to 500 chars per block (longer than Part 6's 200-char estimate — substantive decision context benefits from more context). **Part C — verdict request integration.** Locate the verdict request rendering function in `verdict.py` (likely `post_verdict_request()` or similar — find it by grep, do not trust the diagnostic's line number). Add a new optional section to the rendered markdown: `## Intermediate Decisions Detected\n\n<count> phrase-matched blocks. Review for agent decisions narrated mid-step:\n\n` followed by each block as a bullet with format `- **Event <idx>:** <text> _(matched: <phrase-list>)_`. If no blocks match, omit the section entirely (do not render an empty section). Locate the call site in `bellows.py` that calls the verdict-request function; pass the extracted decisions in. **Part D — `runner.py` integration.** Locate where `_write_log()` is called (or where raw_output finishes parsing) in `runner.py`. After parsing completes, call `decisions.extract_decision_blocks(raw_output, decisions.load_phrases())` and include the result in the returned dict under key `intermediate_decisions`. **Part E — tests.** Add unit tests in `tests/test_decisions.py`. Cover at minimum: (1) `load_phrases()` reads the file correctly and lowercases entries, (2) `extract_decision_blocks()` matches each of the 6 S-class blocks from the diagnostic's ground truth (read from `bellows/knowledge/qa/evidence/intermediate-decision-detection-2026-05-12/labeled-blocks.jsonl`; iterate the S-labeled entries and assert each matches at least one phrase), (3) empty-input edge cases, (4) malformed-NDJSON edge case, (5) multiple content items per assistant event. Add an integration test in `tests/test_verdict.py` for the new section rendering — both populated and omitted cases. **Run the full test suite.** Report total pass/fail counts (note the pre-existing `test_run_step_timeout` failure if it appears — that's unrelated). **Commit message:** `feat(bellows): surface intermediate decisions in verdict requests via phrase-grep detector`. Deposit a dev log at `bellows/knowledge/development/intermediate-decision-detector-dev-log-2026-05-12.md` covering: files modified with brief description, test count delta, any deviations from this plan, any decisions made on initiative.

**Deposits:**
- `/Users/marklehn/Desktop/GitHub/INTERMEDIATE_DECISION_PHRASES.md`
- `bellows/decisions.py`
- `bellows/verdict.py`
- `bellows/runner.py`
- `bellows/bellows.py`
- `bellows/tests/test_decisions.py`
- `bellows/tests/test_verdict.py`
- `bellows/knowledge/development/intermediate-decision-detector-dev-log-2026-05-12.md`

---

## STEP 2 — BELLOWS_DEVELOPER

Read your specialist file first. **Goal:** add Vexp code-exploration tools to the `READ_CLASS_TOOLS` exemption in `bellows/gates.py` to close the gate-exemption gap exposed when `mcp__vexp__run_pipeline` tripped `no_permission_denials` as a blocking gate on the 2026-05-12 design diagnostic. **Part A — locate the exemption list.** Grep `bellows/gates.py` for `READ_CLASS_TOOLS`. Read the current contents — the diagnostic referenced `{"Grep", "Glob", "Read"}` but the current set may have evolved. Report what you found. **Part B — add Vexp tools.** Extend the set to include `mcp__vexp__run_pipeline`. If grep reveals other Vexp read-class tools in the same MCP namespace (e.g., `mcp__vexp__search`, `mcp__vexp__inspect`, or similar — check by reading the running daemon's permission-denial log entries from today's session at `bellows/logs/` if accessible, or check Vexp tool listings via direct read of the Vexp config if present at known paths), add those too. If only `run_pipeline` is observed today, add only that one — do not speculatively add tools that aren't confirmed Vexp read-class. State explicitly which tools you added and why. **Part C — tests.** Add a regression test in `tests/test_gates.py` (find the existing `no_permission_denials` tests by grep) asserting that `mcp__vexp__run_pipeline` denial does NOT trip the gate. Lock the test to the literal tool name. **Run the full test suite.** **Commit message:** `fix(gates): exempt Vexp code-exploration tools from no_permission_denials`. Append to the dev log from Step 1 (`bellows/knowledge/development/intermediate-decision-detector-dev-log-2026-05-12.md`) a new section "Step 2 — Vexp exemption" with: tools added with brief justification, test count delta, any deviations.

**Deposits:**
- `bellows/gates.py`
- `bellows/tests/test_gates.py`
- `bellows/knowledge/development/intermediate-decision-detector-dev-log-2026-05-12.md`

---

## STEP 3 — BELLOWS_QA

Read your specialist file and the Rule 20 self-check canonical block at `/Users/marklehn/Desktop/GitHub/RULE_20_SELF_CHECK_BLOCK.md` first. **FIRST — Deliverable Verification.** Read both DEV steps' Output Receipt "Files Created or Modified" lists. For EVERY listed file: verify it exists on disk and contains the described change (grep for key content). Produce a verification table: `| Deliverable | Expected | Status (✅/❌) | Evidence |`. If ANY item is ❌, attempt to fix before proceeding; if unfixable, stop and report to CEO — do NOT move plan to Done. Specific deliverable-verification anchors: (1) `INTERMEDIATE_DECISION_PHRASES.md` exists at governance root with 33 bulleted phrase lines, (2) `bellows/decisions.py` exists with `load_phrases()` and `extract_decision_blocks()` functions, (3) `bellows/verdict.py` was modified to render the new section (grep for `Intermediate Decisions Detected`), (4) `bellows/runner.py` was modified to call `extract_decision_blocks()` (grep for the call), (5) `bellows/gates.py` was modified to include `mcp__vexp__run_pipeline` in `READ_CLASS_TOOLS` (grep for the literal tool name), (6) `tests/test_decisions.py` exists with at minimum the 6-S-block ground-truth test, (7) `tests/test_gates.py` was modified with the Vexp exemption regression test. **Behavioral verification — Part A (detector against ground truth).** Read `bellows/knowledge/qa/evidence/intermediate-decision-detection-2026-05-12/labeled-blocks.jsonl`. Filter to the 6 S-class entries. For each, invoke `decisions.extract_decision_blocks()` with a minimal synthetic NDJSON stream containing just that text as a single assistant event. Verify each S-block is flagged. Then filter to a sample of T-class entries (pick 5 randomly) and verify they are NOT all flagged — some will be (per the F1=0.706 false positive rate) but if all 5 T-class samples flag, something is wrong. Document results in the QA report. **Behavioral verification — Part B (verdict request integration).** Construct a synthetic verdict-request scenario (or run a canary diagnostic plan if simpler) and verify the new `## Intermediate Decisions Detected` section renders correctly when blocks are present and is omitted when no blocks match. **Behavioral verification — Part C (Vexp exemption).** Construct a synthetic `claude -p` output containing a single `mcp__vexp__run_pipeline` permission denial and no other issues. Run the `no_permission_denials` gate against it and verify it returns pass (not failure). **Test suite.** Run the full test suite. Report pass/fail counts. Note the pre-existing `test_run_step_timeout` failure if it appears. **Live verification of the running daemon (post-restart caveat).** Note in the QA report that the running Bellows daemon will not load these changes until restart — the new detector code, the new verdict-request section, and the Vexp exemption will appear in plans dispatched AFTER the next daemon restart. Do NOT attempt to verify the changes are live in the current daemon. Run a Rule 20 self-check per the canonical block. Deposit the QA report at `bellows/knowledge/qa/intermediate-decision-detector-qa-2026-05-12.md`. Evidence files at `bellows/knowledge/qa/evidence/intermediate-decision-detector-qa-2026-05-12/`: deliverable-verification grep output, detector-against-ground-truth output, verdict-request-rendering output, Vexp-exemption test output. **Final:** Update `bellows/PROJECT_STATUS.md` — add a completed milestone entry summarizing both DEV steps' changes (new detector module, new verdict request section, Vexp exemption). Append to `bellows/feedback.log`: `2026-05-12 — intermediate decision detector shipped — detector module + verdict section + Vexp exemption, test count <pre>→<post>`. Final commit message: `docs: PROJECT_STATUS + feedback for intermediate decision detector ship`. **STOP.** Do NOT move this plan to Done/. Per the disable-auto-close model, the Planner performs the terminal move after Rule 22 verification.

**Deposits:**
- `bellows/knowledge/qa/intermediate-decision-detector-qa-2026-05-12.md`
- `bellows/knowledge/qa/evidence/intermediate-decision-detector-qa-2026-05-12/`
- `bellows/PROJECT_STATUS.md`
- `bellows/feedback.log`
