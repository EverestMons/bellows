# Bellows — Resume Path + Pending Cleanup Diagnostic
**Date:** 2026-04-16 | **Type:** diagnostic (investigation only, no fixes)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation ("ok") before proceeding. For this single-step diagnostic, the agent reports completion and the Planner performs Rule 22 verification of the deposited findings before moving the plan to Done.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-resume-path-pending-cleanup-2026-04-16.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further or move the plan to Done.
```

---
---

## STEP 1 — INVESTIGATION AGENT (bellows developer)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-resume-path-pending-cleanup-2026-04-16.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-resume-path-pending-cleanup-2026-04-16.md")`. Skip specialist file and glossary reads — bellows has no specialist files and this is pure code-tracing. You are investigating two bugs in Bellows' verdict-resume path so the Planner can write an executable fix. Do NOT propose fixes, write code, or modify anything — investigate and deposit findings only. **Bug 1 — Resume-from-Step-1.** When `_consume_verdicts()` in `bellows.py` processes a `continue` verdict for a mid-plan step (step N < total_steps), it calls `self.handle_new_plan(inprogress_path)`. The Planner believes this re-enters `run_plan()` from scratch, which always starts with the Step 1 bootstrap prompt, meaning Step N+1 never executes. **Q1:** Confirm or deny this. Trace the exact code path from `_consume_verdicts` → `handle_new_plan` → `_run_tracked` → `run_plan`. Does `run_plan` receive any information about which step to resume from? Show the literal bootstrap prompt that gets built on resume — is it always Step 1? **Q2:** When a plan resumes and re-runs Step 1, what happens to the code changes the agent already committed in the original Step 1? Does the agent re-commit (creating a duplicate commit), no-op (detecting nothing to change), or fail (merge conflict)? Show the relevant lines. **Q3:** Propose the minimal code change to make resume work. The Planner's hypothesis is Option B: instead of calling `handle_new_plan`, build a resume bootstrap prompt (`f"Read the plan at {path}. Execute Step {step_number + 1}."`) and pass it to a new method `handle_resume(path, prompt)` that threads the custom prompt through `_run_tracked` → `run_plan`. Confirm whether this approach is viable by checking: (a) does `run_plan` accept a custom bootstrap prompt or is it hardcoded? (b) does `runner.run_step` accept a custom prompt? (c) what's the minimal signature change to thread a resume prompt through? If the Planner's Option B doesn't work, propose an alternative and explain why. **Bug 2 — Pending file accumulation.** `post_verdict_request()` in `verdict.py` writes a file to `verdicts/pending/`. Nothing ever removes these files after the verdict is consumed. **Q4:** Confirm that `_consume_verdicts` does NOT delete or move the pending request file — it only moves the resolved verdict to `processed-`. Show the relevant lines. **Q5:** Identify the cleanest place to add the cleanup. Options: (a) inside `_consume_verdicts` after successfully processing a verdict — delete the corresponding pending file, (b) inside `check_verdict` — delete the pending file when a resolved verdict is found, (c) a separate cleanup sweep. The Planner's preference is (a) since `_consume_verdicts` already knows the plan slug and step number. Confirm whether the pending filename can be reconstructed from `plan_slug` + `step_number` reliably (i.e., is the pending filename always `verdict-request-{slug}-step-{N}.md`?). **Q6:** Are there any edge cases where deleting the pending file too early could cause problems? For example: does any other code read from `verdicts/pending/` besides the Planner? Does the Planner's `check_verdict` ever reference pending files? Does the notifier read them? **Deposit:** write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/resume-path-pending-cleanup-diagnostic-2026-04-16.md` using the canonical pattern. Structure with sections Q1–Q6 and include an Output Receipt. Verify you answered ALL SIX questions before marking Status=Complete. **Commit:** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/resume-path-pending-cleanup-diagnostic-2026-04-16.md && git commit -m "docs: diagnostic — resume path + pending cleanup"`. **Feedback:** standard prompt feedback protocol → `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed. Do NOT move the plan to Done. Report completion and wait for CEO confirmation — the Planner will perform Rule 22 verification of the findings before authorizing housekeeping.**
