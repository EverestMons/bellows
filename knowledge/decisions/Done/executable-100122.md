# bellows — executable: DOCTRINE — Gate 2, thread 336, PT tranche six (the execution model's remaining rules — the LAST PT tranche): proposals 444, 474, 489, 513, 518, 538, 567, 587 onto PLANNER_TEMPLATE.md v4.118 by a committed builder (six extensions — Rules 56, 84 and 35, Workaround 11, the self-repair exemption, the Lifecycle DB Read Protocol; two new Rules 119 and 120 — a single-home store's check carries its home, a failure the operator must act on is written where a gate looks), then the eight flip `implemented` — 17 accepted rows remain for the DC/PST tranche after it

**Date:** 2026-09-15 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full (the bellows suite is UNCHANGED by this plan and is run once by QA so the test gate reads a real summary line; the gate reads FAILURES against `known_failures`, never the pass count — P7 is the expectation, and bellows `main` moves as plans merge) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 2

**auto_close:** false

**Slug:** `gate2-pt-w30-f-2026-09-16`

**Depends on:** the CEO's rulings of 2026-09-14 (thread 336 — one Gate 2 over the 86 accepted proposals, W=29 and W=30 together) and the delegation of 2026-09-15 ("Start on gate 2 and you have my delegation to release at the proper time"; "No I am good with moving forward on these" — the seven-tranche partition, `gate2-packet-2026-09-15.md` §2, its partition row "PT six — the execution model's remaining rules"); the non-author Gate 1 packets — W=29's `gate1-packet-2026-09-02.md` (444) and W=30's `gate1-packet-2026-09-13-a.md` (reader A: 474, 489), `-b.md` (reader B: 513, 518, 538) and `-c.md` (reader C: 567, 587); **tranche five** — `Done/executable-100121.md` in bellows (`gate2-pt-w30-e-2026-09-16`, the clone origin and the newest same-class plan: doctrine prose onto `PLANNER_TEMPLATE.md` by a committed builder, then a scoped CAS flip; its register `walk-register-gate2-pt-w30-e-2026-09-16.md` — walk 0, the scout's fifteen folds, walk 1, the EXECUTION and CAPSTONE seats, walk 2) and `Done/executable-100120.md` (tranche four, the grandparent form); the LIVE `PLANNER_TEMPLATE.md` is v4.117 (tranche five's act, governance `0f5c34bd`), this tranche's pre-edit version, so every pin below is measured on it directly; the builder `governance/knowledge/decisions/drafts/build-gate2-pt-w30-f-2026-09-15.py` (committed `59c2ec28` — the walk-0 re-pin after the scout's s2 and s9 — digest `a03e802789dd5462`; proven on the v4.117 candidate chain at authoring 2026-09-15, its E7 anchor re-pinned to Rule 118's landed Source line with tranche four's walk-0 re-pin of builder e, re-proven on the LIVE v4.117 at walk 0 and again after the re-pin). Walk register: `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-f-2026-09-16.md`.

**Tier computed, not judged (§1):** **T-6 fires** — `PLANNER_TEMPLATE.md` is the template → **T2**. T-2 fires (eight canonical rows flip; caps at T1). T-1 fires (two repos, plus the DB). T-8 does NOT fire — a structure-for-structure clone of tranche five (#100121) carrying 540's extension form and tranche one's new-rule form; ten edits against seven, two new rules against one, no span, is scale, not shape. **Panel form — the magnitude proxy, measured at walk 0 (all three must hold): (i) structure-clone** — the parent is #100121 (closed 2026-09-16 14:27, the newest PT tranche), and both walk-0 clone-diffs ran (against its final text and its register); **(ii) mechanized edits** — every template mutation flows through the committed builder, whose ten anchors are count-1-asserted BY EXECUTION and whose 47 post-conditions are asserted on the output before any write, with the line and byte deltas asserted against pinned constants (proven at authoring on the v4.117 candidate and at walk 0 on the LIVE v4.117: a byte-identical rebuild, a `31	2` numstat, the byte pin firing on an off-by-one copy of the constant, the three refusals); the flip is one CAS statement under a structural pre-check; **(iii) scripted probes** — every new token measured 0 against the pre-edit file, the invariant tokens at their MEASURED pre-edit counts. **→ SMALL form: the walk-0 scout, then EXECUTION, then CAPSTONE — the seats on the Air when a session is live there (the CEO's direction of 2026-09-15, thread 369), else local subagents.** The CEO may call the full form.

## Why this exists

Gate 1 routed 86 proposals `accepted|codify`; tranches one to five landed sixty-one of them (v4.112 by #100109, v4.113 by #100114, v4.115 by #100119, v4.116 by #100120, v4.117 by #100121). This tranche is the packet's sixth row and the LAST PLANNER_TEMPLATE tranche — the PT route empties here: the eight rows about the EXECUTION MODEL's remaining rules — a precondition read before the re-entry ladder never depending on state the plan's own steps move, a QA step that assembles pinned fixtures and never designs them, a divergence sized by exact-file overlap, the unshipped artifact as the instrument repair's fixture, a per-step table only as fine as its write site, a copy of live state that includes the process that took it, a single-home store's check carrying its home, and a failure the operator must act on written where a gate already looks. Their homes are the packets', re-verified against the live v4.117 at authoring (every probe `/usr/bin/grep -c -F`, counting LINES; the live count then the build's): `before the re-entry ladder never depends` 0 → 1; `ASSEMBLES pinned fixtures` 0 → 1; `exact-file intersection` 0 → 1; `names the real artifact` 0 → 1; `as fine as the context that wrote it` 0 → 1; `includes the process that took it` 0 → 2; `carries the home in its contract` 0 → 1; `is a property of WHERE it is written` 0 → 1. **Homes:** SIX extension paragraphs appended after the home's existing last line (540's form) — Rule 56 (474), Rule 84 (538), Workaround 11 (489), the self-repair exemption (513), the Lifecycle DB Read Protocol (567), Rule 35's config-act clause (587) — each opening in bold with its proposal, entry and `codified 2026-09-16 — Gate 2, thread 336, PT tranche six`; TWO new rules (`### 119.` kin of Rules 99 and 105, `### 120.` kin of Rule 99 and Workaround 15, each with its Source line carrying `codified 2026-09-16 (Gate 2, thread 336 — PT tranche six)` and its `---`), placed after Rule 118's separator and before the Lifecycle DB Read Protocol; the version, the `Last Updated` line and the History row. Numbering append-only: Rules 119 and 120 follow Rule 118. The Rule 19 keyword scan of the builder's added lines at authoring (word-bounded, the canonical list): none.

## What this plan does

**In the governance checkout, by absolute path (`GOV=/Users/marklehn/Developer/eluvian-governance`), ten anchored edits to `PLANNER_TEMPLATE.md` by the committed builder (never by hand):**
- **E1** — Rule 56: after its last paragraph (anchor: the blank line and Rule 57's heading, re-emitted), the 474 paragraph. **E2** — Rule 84: after its last paragraph (anchor: the blank line and Rule 85's heading), the 538 paragraph. **E3** — Workaround 11: after its last paragraph (anchor: the blank line and Workaround 12's heading `#### 12. Final-step gate_failure recovery checklist`), the 489 paragraph. **E4** — the self-repair exemption: after its last paragraph (anchor: the blank line and the `### pause_for_verdict Header Field` heading), the 513 paragraph. **E5** — the Lifecycle DB Read Protocol: after its authority model (anchor: the blank line and `**Canonical queries.**`), the 567 paragraph. **E6** — Rule 35's config-act clause: after it (anchor: the blank line and `**Net-zero-cost failures still warrant correction.**`), the 587 paragraph.
- **E7** — the TWO NEW rules: the anchor is Rule 118's Source line `*Source: proposals 576 and 581, entries 568 and 573 (2026-09-11, 2026-09-12), codified 2026-09-16 (Gate 2, thread 336 — PT tranche five)*`, its `---` and the `## Lifecycle DB Read Protocol (Planner)` heading (count 1); the replacement re-emits them around the new rules — `### 119. A check on a single-home store carries the home in its contract` (kin of Rules 99 and 105; 444) and `### 120. The visibility of a bookkeeping failure is a property of WHERE it is written` (kin of Rule 99 and Workaround 15; 518), each with its Source line and its `---` (the Rules 112–118 form).
- **E8** — `**Version:** 4.117` → `**Version:** 4.118`. **E9** — `**Last Updated:** 2026-09-16 (v4.117)` → `**Last Updated:** 2026-09-16 (v4.118)`. **E10** — the History row inserted directly under the `| Date | Lesson |` header (the v4.117, v4.116, v4.115, v4.114, v4.113 and v4.112 rows survive below it).

The builder reads the file as bytes, refuses CRLF, refuses an already-built input, asserts each anchor's count is 1, applies the ten edits, asserts 47 post-conditions on the OUTPUT (the builder's `POST` list: the Rule 119 and Rule 120 headings once each; their History-row fragments once each; the codified strings at 6 and 2; the six openers once each; the v4.118 tokens 1 and the v4.117 tokens 0; the new History row 1 and the six rows below it 1 each; the section headings surviving at their MEASURED pre-edit counts — `### 35. `, `### 56. `, `### 57. `, `### 84. `, `### 85. `, `### 99. `, `### 105. `, `### 118. `, `#### 11. `, `#### 12. `, `#### 15. ` at 1, `### 14. ` 3, `### 17. ` 2, `### 121. ` and `#### 16. ` 0 (numbering append-only), `**Canonical queries.**`, `### pause_for_verdict Header Field`, `**Net-zero-cost failures still warrant correction.**`, `### The self-repair exemption` and the Lifecycle DB Read Protocol heading 1 each; Rule 118's Source line and Rule 35's `Source: proposal 78, lesson 2026-05-27` 1 each), asserts the line and byte deltas against PINNED constants (`EXPECTED_LINES = 29`, `EXPECTED_BYTES = 14485` — a post-condition that can fail, proven at authoring on an off-by-one copy of the constant) and only then writes — to a SCRATCH path it refuses to let be the live file or anything under the governance root or the input's git toplevel. Apply = copy the proven scratch output over the live file, then re-measure the live file.

**In the lessons DB (`/Users/marklehn/Developer/forge_lessons/lessons-forge.db`, the mini's one live copy), AFTER the doctrine commit:** one scoped CAS flip — `id IN (444,474,489,513,518,538,567,587) AND status='accepted' AND route='codify'` → `implemented`, `status_updated_by='ceo'` (the delegation's record), a fresh UTC stamp — under a structural pre-check (a TEMP-TABLE `CHECK(x=8)` that aborts the transaction before the UPDATE if the eight are not all `accepted|codify`), with the capture of every row with `id <= 590` — the whole table at authoring — `(id, status, route, category, status_updated_by, status_updated_at)` to a SCRATCH file BEFORE the UPDATE (copied into the worktree's evidence dir afterwards — never written into the canonical checkout), and the sentinels by name: `PRE_F=8`, `CHANGES_F=8`, `EXCL_F=8` (the eight rows now `implemented` with a stamp that is NOT one of the TWO vintage stamps — 444 carries W=29's `2026-09-02T23:54:37Z`, the seven W=30's `2026-09-13T18:27:07Z`), `ACC_POST=17`, `IMPL_POST=402`. The flip SQL is a committed file in governance; a backup of the DB precedes it.

**In the bellows worktree:** the dev log and the QA evidence.

## What this plan does NOT do

- Does not touch the seventeen `accepted|codify` rows of the DC/PST tranche (their homes are named in the packet — each is its own plan), DRAFTING_CYCLE.md, PANEL_SEAT_TEMPLATE.md, COMPANY.md, MACHINE_SETUP.md, GLOSSARY.md, code, or threads. Does not push (the Planner pushes after the pause; the Planner's Rule 22 (b) read at the pause takes the canonical block's stdout and the daemon's gate row as the receipt's evidence, never the QA's `PRECHECK: 0 failure(s)` line — at a QA step `tools/check_deposit.py` verifies presence alone, thread 362). Does not project the `LESSONS.md` markers (the Planner's act after the close — `forge_lessons/scripts/project_status_markers.py --apply`, the Rule 112 instrument). Does not close thread 336 (the Planner's `threads done` with the seventh tranche).
- ⚠️ **Runs after tranche five (#100121, 2026-09-16 — its v4.117 is this plan's pre-edit version, LIVE) and BEFORE any W=31 ingest or another tranche** — A0's ladder asserts the accepted population (25 before this plan's flip, 17 after) with every accepted row at one of the two vintage stamps; another tranche landing first moves it, and a W=31 ingest could stale an accepted row (`accepted` is not terminal — `forge_lessons/src/lessons_forge.py:31`). The 74 `[status: pending]` headings written since W=30's ingest wait (the marker projection at governance `539710a1`, 2026-09-16 14:28, is the newest; its dry run at this derivation counted `unmatched (no DB row): 74` — re-read at the freeze).
- ⚠️ **PLANNER_TEMPLATE's version is moved by the CEO's doctrine acts at other plans' closes** (tranche five's act is this plan's pre-edit version, live since 2026-09-16 14:11 (`0f5c34bd`; #100121 Done 14:27); tranche one moved twice under its own draft, tranches two to five not at all): the re-pin surface is the PIN TABLE and its restatements — P1 (sha, lines, bytes); P2 (every line number, which a doctrine act above the anchors shifts, and its `**Version:**` anchor); P4 (the builder's digest, after its two constants `OLD_VERSION`/`NEW_VERSION` are re-committed); P5 (`wc -l`, `wc -c`; the BUILT line's deltas hold unless an act lands inside one of the seven homes, which an anchor count or the byte pin refuses); Task C's four version tokens; the `wc -l` restated in A2, A3, both RE-ENTRY arms and QA Item 1 and the `wc -c` in A2; every literal of the new version (the title, the E8/E9 lines, the POST description, MUST-PRESERVE, P3, the RE-ENTRY text, Task C, the THREE commit subjects) and the pre-edit-state literals (E7's, the POST description's, P1's, P2's anchor, P3's, Task C's zero probe); the register's Targets line and title — re-pinned TOGETHER at the freeze and before the release by ONE scripted pass over those sites (tranche one's CAPSTONE re-pin is the form), recorded in the register with each literal's count before and after, and Task C's probes re-run on a scratch build of the new version, and the Why-line dedup probes re-run against the new pre-edit version with their `live v4.NNN` label and counts re-pinned with them (the CAPSTONE seat's S3-1); the historical mentions keep their versions; a moved version at A0 is a NONE-MATCH halt and a redeposit, never a guess.

## MUST-PRESERVE

- ⚠️ **TWO REPOSITORIES AND A DB, ONE STEP.** Governance by absolute path, `git -C "$GOV"` never `cd`; porcelain EMPTY and sha = P1 before touching `PLANNER_TEMPLATE.md`; commit by explicit pathspec with the message on stdin; no push. The DB by absolute path; `sqlite3` with `-bail`; a backup (`pre-gate2-pt-w30-f-<HHMMSS>.db` at the forge root, untracked) BEFORE the flip.
- **The builder is the only editor of the template.** Its committed blob digest must equal the on-disk file (P4) before it runs; it runs scratch→scratch; the live file is replaced only by `cp` of a scratch output whose post-conditions the builder already asserted, and the live file is then re-measured (Task C).
- **Doctrine before DB (540's C6):** the flip runs only after the governance commit has landed; a HALT between leaves v4.118 committed-unpushed and the flip owed — the RE-ENTRY arm in A0 resumes at the flip.
- ⛔ **Never run any `checkout --` on an A0 halt except the ONE recipe named there** (a pre-commit apply that died, discriminated by the HEAD numstat, the porcelain and the `### 119. ` token); a pin mismatch otherwise means the file carries changes that are not this plan's.
- ⚠️ **NO landed text carries a Rule 20 hedging keyword** — the scan of the builder's added lines against the canonical block's list (at authoring, on the live build) returned nothing; still, NO receipt row quotes a landed paragraph's body: Task C pins heading fragments and opener fragments only, and the receipt cites counts
- **`known_failures: 0`.** The suite runs in the worktree under the canonical venv; the QA reads its own run's failure count, and a bellows `main` that gained tests since P7 is stated in `probes-raw.txt` under Item 4 (the probe's command and the hashes it printed; the summary line stays in the suite file), never in the receipt's row and never halted on. Any failure is a HALT/Critical.
- ⛔ **`probes-raw.txt` carries no summary-shaped line** — no `N passed` restatement of P7, no pasted suite line, no commit log beyond the path-scoped drift probe's: the suite gate (`gates._select_summary_txt`, bellows `91354d2a`) chooses, among the `.txt` deposits that carry a pytest summary, a basename carrying `suite` or `full`, then the largest `passed` count, then the first match — so a summary-shaped line in the raw file is the one read whenever the suite file crashed without a summary or lost its name (thread 363's defect; its "first `.txt`" wording is narrower than the live gate, measured 2026-09-15 on `71c6bfc3`), and it is forbidden here by name so the selection never rests on that tie-break (tranche one's QA wrote "P7 baseline: 2395 passed, 2 skipped" as a header there — harmless beside a named suite file, forbidden here). P7 is cited as "P7" in the raw file, never restated with its numbers.
- ⛔ **Every commit in the bellows worktree chains `tools/check_deposit.py` before `git add`** (Rule 23; the step IS bound — the commit binding was installed by bellows `4bcde8a9` (#100105, thread 333 — `hooks/git/pre-commit` and the daemon's `BELLOWS_PRECHECK_*` env), and tranches one's to five's chains ran under it), and puts its message on stdin (`git commit -F -` with a quoted heredoc), never in a file.

## Numbers discipline — the pins DEV re-derives (measured 2026-09-16 by the Planner's derivation script on the LIVE PLANNER_TEMPLATE v4.117 at governance `0f5c34bd`, bellows main `7fdc06cb`, forge_lessons `a1ebf45`; re-measured at the freeze)

| pin | what | value | how |
|---|---|---|---|
| P1 | **`PT_SHA`**, lines, bytes — pre-edit (the LIVE v4.117, tranche five's act at #100121's DEV, governance `0f5c34bd`) | `fa7f800a63abe875`; 2639 lines; 591,416 bytes (= v4.116 `23969632693f592c` / 2624 / 578,618 + tranche five's fifteen lines, +12,798 bytes) | `shasum -a 256 "$GOV/PLANNER_TEMPLATE.md" \| cut -c1-16`; `wc -l`; `wc -c` |
| P2 | **`ANCHORS`** — the builder's ten anchors | each count 1: Rule 57's heading with its preceding blank line (E1, the heading at 1153 of the live v4.117) · Rule 85's (E2, 1367) · Workaround 12's heading `#### 12. Final-step gate_failure recovery checklist` (E3, 2125) · the `### pause_for_verdict Header Field` heading (E4, 1980) · `**Canonical queries.**` (E5, 1627) · `**Net-zero-cost failures still warrant correction.**` (E6, 926) · Rule 118's Source line `*Source: proposals 576 and 581, entries 568 and 573 (2026-09-11, 2026-09-12), codified 2026-09-16 (Gate 2, thread 336 — PT tranche five)*` with its `---` and the `## Lifecycle DB Read Protocol (Planner)` heading (E7, 1615) · `**Version:** 4.117` (5) · `**Last Updated:** 2026-09-16 (v4.117)` (6) · the History header pair (2418–2419; tranche five's v4.117 row directly under it) — the line numbers measured on the live v4.117, re-measured at the freeze | `/usr/bin/grep -cF` (the multi-line anchors by the builder's own assertion) |
| P3 | **`TOKENS`** 0 before, their counts after | `### 119. ` · `### 120. ` · the six openers (`**A precondition read before the re-entry ladder never depends on state the plan's own steps move`, `**A QA step ASSEMBLES pinned fixtures — it never designs them`, `**Size a divergence by the exact-file intersection of the two `--name-only` sets`, `**The evidence an instrument repair owes names the real artifact whose verdict the repair should move`, `**A table's key is only as fine as the context that wrote it`, `**A copy of live state includes the process that took it`) · `**Rule 119 — a single-home store's check carries the home**` · `**Rule 120 — a failure the operator must act on is written where a gate looks**` · `codified 2026-09-16 — Gate 2, thread 336, PT tranche six` · `codified 2026-09-16 (Gate 2, thread 336 — PT tranche six)` · `**Version:** 4.118` · `\| 2026-09-16 \| v4.118: Gate 2, thread 336` — each 0 before, 1 after but the two codified strings (6 and 2); retired after: `**Version:** 4.117`, `**Last Updated:** 2026-09-16 (v4.117)` — each 1 before, 0 after; invariants: `### 35. ` 1, `### 56. ` 1, `### 57. ` 1, `### 84. ` 1, `### 85. ` 1, `### 99. ` 1, `### 105. ` 1, `### 118. ` 1, `#### 11. ` 1, `#### 12. ` 1, `#### 15. ` 1, `### 14. ` 3, `### 17. ` 2, `### 121. ` 0, `#### 16. ` 0, Rule 118's Source line 1, `Source: proposal 78, lesson 2026-05-27` 1, the six surviving History rows 1 each (unchanged by this plan) | `/usr/bin/grep -cF` |
| P4 | **`BUILDER`** — on-disk digest = the blob at its OWN last commit | `a03e802789dd5462` (governance `59c2ec28` — the builder's own last commit: the walk-0 re-pin after the scout's s2 and s9; `db049c3f` the chain proof's re-pin, `08b2b6de` its first) | `shasum -a 256 <builder> \| cut -c1-16`; `BC=$(git -C "$GOV" log -1 --format=%H -- governance/knowledge/decisions/drafts/build-gate2-pt-w30-f-2026-09-15.py); git -C "$GOV" show "$BC:governance/knowledge/decisions/drafts/build-gate2-pt-w30-f-2026-09-15.py" \| shasum -a 256 \| cut -c1-16` |
| P5 | **`DRY_RUN`** — the success line, numstat, sizes | `BUILT: <out> edits=10 blocks=1 lines+29 bytes+14485 post=47/47 deltas=pinned`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" <out>` → `31	2` (exit 1 is the expected differing state — read the numbers, never the exit); `wc -l <out>` → 2668; `wc -c <out>` → 605,901 (the live v4.117's; the output `c5ff01269a6ea517`) | as stated, scratch-only |
| P6 | **`FLIP_PRE`** — the eight rows | 444 `accepted\|codify\|planner\|2026-09-02T23:54:37Z`; 474, 489, 513, 518, 538, 567, 587 each `accepted\|codify\|planner\|2026-09-13T18:27:07Z`; `SELECT COUNT(*) … WHERE status='accepted'` → 25; `implemented` → 394; `SELECT status_updated_at, COUNT(*) … WHERE status='accepted' GROUP BY 1` → exactly two rows, `2026-09-02T23:54:37Z\|8` and `2026-09-13T18:27:07Z\|17`; `SELECT MAX(id), COUNT(*) FROM lesson_proposals` → `590|590` | `sqlite3 -readonly /Users/marklehn/Developer/forge_lessons/lessons-forge.db` |
| P7 | **`SUITE`** — the expectation | `2524 passed, 2 skipped`, exit 0 — bellows main `7fdc06cb` (the newest QA's commit, 2026-09-16), the last suite file on main (`full-suite-gate2-pt-w30-e.txt`) — the drift probe is anchored at `7fdc06cb`; re-read at the freeze; the gate reads failures, and `main` moves | `BPY -m pytest tests -q -p no:cacheprovider` |
| P8 | **`NO_INGEST_NO_TRANCHE`** (precondition, in two halves) | UNCONDITIONAL (before the ladder): `SELECT COUNT(*) FROM lesson_entries` → 580 (no W=31 ingest) AND `ls /Users/marklehn/Developer/forge_lessons/knowledge/decisions/ \| /usr/bin/grep -cF -- 'forge-cycle-w31'` → 0 (a zero-match count prints 0 and exits 1 — read the number). ARM-DEPENDENT (ladder step (5)): the accepted population with its at-the-vintages count — `25\|25` on FRESH and flip-owed; `17\|17` on complete — the eight at their vintages on FRESH and flip-owed (ladder (4)) | as stated |

## Drafting Cycle

**Tier:** T2 — T-6 (the template), T-2 (eight canonical rows, capped), T-1 fire; T-8 does not (a structure clone). **Panel: SMALL form** (the magnitude proxy measured at walk 0, all three conditions holding) — the walk-0 scout, then EXECUTION, then CAPSTONE, on the Air's session by cross-session message when one is live there, a local fresh-context seat at once when the Air does not take the work up within a few minutes (thread 382); the CEO may call the full form.
**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-gate2-pt-w30-f-2026-09-16.md
**Walk 0 (context pin, measured):** the v0 derived by script from tranche five's closed text (`executable-gate2-pt-w30-e.md` at `5d45ca77`; #100121 Done 14:27:50), the script proven first on the v4.117 candidate — an explicit site list with the builder's basename, the parent's names and the bare parent slug's five sites set by site, every number measured on the LIVE v4.117 at run time, the leftover and bare-slug scans read, every named path checked, three sites corrected at the live run by the script's own count assert (M1); the ten anchors each count 1 at their measured lines (M2); the eight entries and their Gate 1 rows read, the dedup probes 0 on the live file and at their Why-line counts on the build (M3); the live v4.117 `fa7f800a63abe875` / 2639 / 591,416 and the builder `a03e802789dd5462` = its blob at `59c2ec28` (M4; `a8aecb9978e3f4c2` at `db049c3f` before the scout's s2 and s9); every home's last writer by blame (M5); the dry run on the live file `lines+29 bytes+14485 post=47/47`, the output `c5ff01269a6ea517` / 2668 / 605,901, numstat `31 2`, BYTE_IDENTICAL on a rebuild, the three refusals, the off-by-one constant refused, the pre-act v4.116 refused on the Source-line anchor, CRLF refused, the Rule 19 scan none (M6); the flip rehearsal owed to the scout (M7); the DB `8|17` of 25 accepted, 394 implemented, 590 rows, 580 entries (M8); the suite `2524 passed` at `7fdc06cb`, the drift probe empty (M9); the consumers under pytest `WRITES 8`, `ASSIGNED shop-infra` (M10); `plan_lint` 0 FAIL, the WARN set read, the baseline armed after this register existed (signals=13), `propagation_check` 146 all detector (1) (M11). Written 14:31 from the printed output. Verdict at walk 0: the magnitude proxy holds → SMALL form; the scout convened on the v0.

- Weak spots:          w1 dry; w2 dry; w3 dry
- Destruction:         w1 dry; w2 dry; w3 dry
- Vulnerabilities:     w1 dry; w2 1 folded — instruction 1 / record 0; w3 dry
- Integration-record:  w1 dry; w2 dry; w3 dry
- ACID:                w1 dry; w2 dry; w3 dry
**Cold panel (SMALL form):** _not yet convened — the meter opens at the scout's dispatch._
**Closing:** ✅ **BAR MET — walk 3 dry on every lens after walk 2's one fold (instruction 1 — the Air's EXECUTION seat's S2-1, the embedded-backtick probe annotated at A3) and walk 1 dry ×5 after the scout's nine walk-0 folds (two of them in the builder, re-pinned at `59c2ec28`). The content — six extensions on Rules 56, 84 and 35, Workaround 11, the self-repair exemption and the Lifecycle DB Read Protocol, two new Rules 119 and 120, the eight rows' flip — is the text the walk-0 scout, the EXECUTION seat (the Air, `c2754dad`) and the CAPSTONE seat (the Air, `48bf327d`, one record finding already corrected) read, its freeze checklist MATCHING on the LIVE v4.117 at every seat (P1–P8, Task C 47/47), the flip rehearsed by the scout on its own backup with the missing-dir abort under `-bail`. With this plan the PLANNER_TEMPLATE route empties: seventeen accepted rows remain, all the DC/PST tranche's. Residue: none. Direction verdict after walk 1: PROCEED. Released under the CEO's delegation of 2026-09-15 ("release at the proper time"), after #100121's Done and the v4.117 act; never two bellows plans concurrently.**

## Cycle Manifest
tier: T2
target: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md
class: shop-infra
reads: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/build-gate2-pt-w30-f-2026-09-15.py, /Users/marklehn/Developer/eluvian-governance/gate2-packet-2026-09-15.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-02.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-a.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-b.md, /Users/marklehn/Developer/eluvian-governance/gate1-packet-2026-09-13-c.md, /Users/marklehn/Developer/eluvian-governance/LESSONS.md, knowledge/decisions/Done/executable-100121.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/Done/executable-540.md, /Users/marklehn/Developer/forge_lessons/lessons-forge.db
writes: /Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30f-flip.sql, /Users/marklehn/Developer/forge_lessons/lessons-forge.db, knowledge/development/dev-log-gate2-pt-w30-f-2026-09-16.md, knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt, knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/qa-receipt.md, knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/probes-raw.txt, knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/full-suite-gate2-pt-w30-f.txt
open_forks: 1. the seventeen accepted rows of the DC/PST tranche (its own plan, the packet's seventh row — the PT route empties with this plan); 2. Rules 119 and 120 follow Rule 118 (numbering append-only) — the packet's "beside Rule 99 / Workaround 15" placements are carried by the kin clauses that open their bodies, not by position; 3. the `LESSONS.md` markers are projected after the close by the Planner, not by this plan; 4. P7's suite count moves with bellows main and is re-pinned at the freeze
walks: 3
yields: 0, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:147
coherence: 3/3 body walks named in the register (10 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-gate2-pt-w30-f.md.foldcheck.json

---

## STEP 1 — DEV

> **FIRST — post a short visible chat message (1–2 sentences).** Do NOT rename this file. You are the bellows Developer. ⚠️ Governance runs IN PLACE at `$GOV` (every operand ABSOLUTE); commits there by explicit pathspec; ONE action per compound. ⛔ Every commit's message goes to `git commit -F -` on stdin — a quoted heredoc ending the commit's own command: `<<'MSG'`, the message on the lines after it, `MSG` alone on the last — so no file is written for it. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** (`BPY`); the builder runs under `python3` as tranche one's did (it imports only the stdlib — `os`, `sys`, and `subprocess` for the toplevel guard).
>
> ⛔ **A0 — roots, the precondition, then the branch ladder (catch-all LAST):** `cd "$(git rev-parse --show-toplevel)" && [ -f bellows.py ] && [ -d tests ] && echo TREE_OK` — HALT unless TREE_OK; `GOV=/Users/marklehn/Developer/eluvian-governance; PT="$GOV/PLANNER_TEMPLATE.md"; B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-f-2026-09-15.py"; DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db; [ -f "$PT" ] && [ -f "$B" ] && [ -f "$DB" ] && echo GOV_OK` — HALT unless GOV_OK. Re-derive `GOV`, `PT`, `B`, `DB` in every compound. `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python`. **P8's UNCONDITIONAL half → both values, else HALT** (`lesson_entries` → 580 and the `forge-cycle-w31` count → 0; on a mismatch: `a W=31 cycle has started — re-pin P6/P8 and re-deposit`). **P8's accepted-population half is read INSIDE the ladder, never before it:** it changes when THIS plan's flip lands. Then the ladder — **(1)** P1's sha matches; **(2)** `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30f-flip.sql` (state it; EMPTY on FRESH); **(3)** `git -C "$GOV" log -1 --format=%s -- PLANNER_TEMPLATE.md` — does the subject carry `gate2-pt-w30-f`?; **(4)** `sqlite3 -readonly "$DB" "SELECT id||'|'||status||'|'||COALESCE(route,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id IN (444,474,489,513,518,538,567,587) ORDER BY id;"`; **(5)** `sqlite3 -readonly "$DB" "SELECT (SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted')||'|'||(SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z'));"` — the accepted population and how many of it sit at the two vintage stamps (every accepted row does, on EVERY arm).
> - **FRESH** = (1) match AND (2) empty AND (3) no AND (4) the eight `accepted|codify` at their vintages (444 at `2026-09-02T23:54:37Z`, the seven at `2026-09-13T18:27:07Z`) AND (5) `25|25` → A1.
> - **RE-ENTRY (doctrine landed, flip owed)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` (a dirty template on re-entry is a FOREIGN edit — HALT untouched, quoting the porcelain) AND (4) the eight still `accepted|codify` at their vintages AND (5) `25|25` → verify the COMMITTED state (the Task C counts on the live file, `wc -l` 2668, and `C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show --stat --format= "$C" -- PLANNER_TEMPLATE.md` → 31 insertions, 2 deletions — the PATH-SCOPED commit, never `HEAD`: once A4's SQL commit or any later governance commit lands, `HEAD` no longer names the template's commit and a `HEAD`-scoped show prints NOTHING with exit 0), then A4 (the flip) and A5 only. If (2) shows `g2ptw30f-flip.sql` untracked or modified with the flip NOT applied, that is this arm too — A4 rewrites the file from the exact text, which is idempotent.
> - **RE-ENTRY (complete)** = (3) yes AND (2) EMPTY for `PLANNER_TEMPLATE.md` AND (4) the eight `implemented|codify` with every stamp NOT one of the two vintages AND (5) `17|17` → verify the COMMITTED state (the Task C counts, `wc -l`, and the path-scoped show → 31/2); then the two idempotent tails of A4: the SQL file — if `git -C "$GOV" log -1 --format=%s -- governance/knowledge/development/g2ptw30f-flip.sql` does not carry `gate2-pt-w30-f`, write it from the exact text and commit it; the capture — if `/tmp/g2ptw30f/flip-capture.txt` exists copy it into the worktree, else reconstruct the worktree copy from the newest pre-flip backup — `BK=$(ls -t /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-f-*.db | head -1)` (each A4 attempt mints a second-stamped file; the newest is the one whose flip landed), then `[ -f "$BK" ] && echo "BK=$BK"` and `sqlite3 -readonly "file:$BK?immutable=1" "SELECT COUNT(*) FROM lesson_proposals WHERE status='accepted'"` → 25 (THIS plan's pre-flip population; any other number is a HALT quoting every `pre-gate2-pt-w30-f-*.db` with its accepted count — a `-f-` backup reading 17 was minted by a second A4 attempt after the flip landed, and the capture is reconstructed from the newest backup reading 25, named in the HALT for the Planner, never chosen by the DEV) — no file is a HALT (`no pre-flip backup found — the capture cannot be reconstructed; re-deposit`), never an empty `$BK` handed to sqlite, which would open a scratch database and fail on the table — `sqlite3 -readonly "file:$BK?immutable=1" "<the capture SELECT>"` → the same 590 lines, labelled `RECONSTRUCTED (post-commit re-entry)` in the dev log); then A4's read-back (no flip) and A5 with the dev log marked `re-derived on re-entry`.
> - **NONE-MATCH** = anything else → HALT quoting every measurement — and when (5)'s SECOND number ≠ its first on ANY arm, the HALT reads `an accepted row carries a stamp outside the two vintages — an ingest or a foreign flip has moved the population; re-pin P6/P8 and re-deposit`; when (3) = no and (4) reads the eight `implemented` with (5) `17|17`, it reads `this plan's flip has landed and a later act moved PLANNER_TEMPLATE.md after its commit — find the template's commit with git -C "$GOV" log --format=%h --grep=gate2-pt-w30-f -- PLANNER_TEMPLATE.md, verify it as the complete arm does (its show --stat → 31/2), then run the complete arm's idempotent tails`; when (3) = no and (5)'s first number ≠ 25 with the two numbers equal and (4) still reads `accepted`, it reads `the accepted set moved — another Gate 2 tranche has landed first, or an ingest staled an accepted row (accepted is not terminal, forge_lessons/src/lessons_forge.py:31); re-pin P6/P8 and re-deposit`; when (1) mismatches and (3) = no and the template's porcelain is EMPTY, it reads `PLANNER_TEMPLATE.md moved under this plan — a doctrine act landed after the freeze; re-pin P1 and the builder's version constants, and re-deposit`. The ONE recipe: (1) mismatch + (3) no + (4) the eight `accepted|codify` at their vintages + (5) `25|25` = a pre-commit apply that died — BUT ONLY IF the dirty file is THIS plan's build: `git -C "$GOV" diff HEAD --numstat -- PLANNER_TEMPLATE.md` → `31	2` (against HEAD, never the bare index-relative `diff`, which prints NOTHING in the staged half-state this recipe exists for) AND `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md` → ` M` or `M ` (never `MM`: a staged foreign edit beneath an unstaged build would pass the HEAD numstat and be discarded) AND `/usr/bin/grep -cF -- '### 119. ' "$PT"` → 1 (a foreign hand edit to the template presents the SAME (1)+(3)+(4)+(5) shape and must never be discarded; any other numstat or porcelain → HALT untouched, quoting it) → `git -C "$GOV" checkout HEAD -- PLANNER_TEMPLATE.md` (`HEAD`, not the index — a death between `add` and `commit` leaves v4.118 STAGED), re-assert P1, then A1. Every other shape HALTs untouched.
>
> ⛔ **A1 — re-derive P1–P6; state each; a mismatch is a HALT quoting both.** (P4 with the builder's OWN commit, never `HEAD:`.)
>
> **A2 — dry-run, scratch→scratch (LITERAL paths, no variables across compounds):** `rm -rf /tmp/g2ptw30f; mkdir -p /tmp/g2ptw30f; python3 "$B" "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30f/PT-out.md; echo "builder_exit=$?"` → P5's `BUILT:` line verbatim, `builder_exit=0`; `git diff --no-index --numstat -- "$GOV/PLANNER_TEMPLATE.md" /tmp/g2ptw30f/PT-out.md` → `31	2`; `wc -l /tmp/g2ptw30f/PT-out.md` → 2668; `wc -c` → 605901. Refusals (each `BUILDER REFUSED`, nonzero; quote each): `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/PLANNER_TEMPLATE.md"` (out == in); `python3 "$B" "$GOV/PLANNER_TEMPLATE.md" "$GOV/x.md"` (under a forbidden root — the literal governance root, or the input's git toplevel when the input is inside a repository, as the live template is, so both bases are active here; no `x.md` is written. The EXECUTION seat's S2-1: on a NON-git input — the scratch candidate — only the literal root refuses, so a reproduction build sends its output to `/tmp`, never under a clone); `python3 "$B" /tmp/g2ptw30f/PT-out.md /tmp/g2ptw30f/PT-out2.md` (already built).
>
> **A3 — apply and measure the LIVE file (Task C), then commit:** `cp /tmp/g2ptw30f/PT-out.md "$GOV/PLANNER_TEMPLATE.md"`; then from the live file, counts read never exit codes (`/usr/bin/grep -cF -- '<token>' "$PT"`, each token as one single-quoted literal): `### 119. A check on a single-home store carries the home in its contract` 1 · `### 120. The visibility of a bookkeeping failure is a property of WHERE it is written` 1 · `**Rule 119 — a single-home store's check carries the home**` 1 · `**Rule 120 — a failure the operator must act on is written where a gate looks**` 1 · `codified 2026-09-16 (Gate 2, thread 336 — PT tranche six)` 2 · `codified 2026-09-16 — Gate 2, thread 336, PT tranche six` 6 · `**A precondition read before the re-entry ladder never depends on state the plan's own steps move` 1 · `**A QA step ASSEMBLES pinned fixtures — it never designs them` 1 · `**Size a divergence by the exact-file intersection of the two `--name-only` sets` 1 (ONE token — grep the whole phrase; the embedded `--name-only` backticks are in the landed text, and the fragment ` sets` alone counts 7) · `**The evidence an instrument repair owes names the real artifact whose verdict the repair should move` 1 · `**A table's key is only as fine as the context that wrote it` 1 · `**A copy of live state includes the process that took it` 1 · `**Version:** 4.118` 1 · `**Version:** 4.117` 0 · `**Last Updated:** 2026-09-16 (v4.118)` 1 · `**Last Updated:** 2026-09-16 (v4.117)` 0 · `| 2026-09-16 | v4.118: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.117: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.116: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.115: Gate 2, thread 336` 1 · `| 2026-09-16 | v4.114: CEO act at the close of bellows plan 100118` 1 · `| 2026-09-15 | v4.113: Gate 2, thread 336` 1 · `| 2026-09-15 | v4.112: Gate 2, thread 336` 1 · `## Lifecycle DB Read Protocol (Planner)` 1 · `**Canonical queries.**` 1 · `### pause_for_verdict Header Field` 1 · `**Net-zero-cost failures still warrant correction.**` 1 · `### The self-repair exemption` 1 · `### 35. ` 1 · `### 56. ` 1 · `### 57. ` 1 · `### 84. ` 1 · `### 85. ` 1 · `### 99. ` 1 · `### 105. ` 1 · `### 118. ` 1 · `### 119. ` 1 · `### 120. ` 1 · `### 121. ` 0 · `#### 11. ` 1 · `#### 12. ` 1 · `#### 15. ` 1 · `#### 16. ` 0 · `### 14. ` 3 · `### 17. ` 2 · `Source: proposal 78, lesson 2026-05-27` 1 · `*Source: proposals 576 and 581, entries 568 and 573 (2026-09-11, 2026-09-12), codified 2026-09-16 (Gate 2, thread 336 — PT tranche five)*` 1 (the six rows below the new one survive; the headings unchanged); `wc -l` 2668; `cmp /tmp/g2ptw30f/PT-out.md "$GOV/PLANNER_TEMPLATE.md"` silent; `git -C "$GOV" diff --stat -- PLANNER_TEMPLATE.md` → 31 insertions, 2 deletions. Any failing → FRESH: `git -C "$GOV" checkout -- PLANNER_TEMPLATE.md` + HALT; RE-ENTRY: HALT, no restore. Commit, the message on stdin: `git -C "$GOV" add PLANNER_TEMPLATE.md && git -C "$GOV" commit -F - -- PLANNER_TEMPLATE.md <<'MSG'` with the subject `[<id from your plan filename>] gate2-pt-w30-f: PLANNER_TEMPLATE v4.118 — six extensions (474, 538, 489, 513, 567, 587), Rules 119 (444) and 120 (518) — the last PT tranche` and `MSG` closing it; `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` → that commit. Do NOT push.
>
> **A4 — the flip (doctrine before DB):** backup first, by sqlite's own copy — the DB is WAL-journaled, so a filesystem `cp` can lag a non-checkpointed write while sizes match: `sqlite3 "$DB" ".backup /Users/marklehn/Developer/forge_lessons/pre-gate2-pt-w30-f-$(date +%H%M%S).db"`; then on the backup — ⚠️ through `sqlite3 -readonly "file:<backup>?immutable=1"`, never a bare `-readonly <path>`: a `.backup` output of a WAL database has no `-shm` sidecar and a bare read-only open fails `unable to open database file (14)` — `PRAGMA integrity_check` → `ok`; `SELECT COUNT(*) FROM lesson_proposals` → equal to the live count (state both); `SELECT COUNT(*) FROM lesson_proposals WHERE id IN (444,474,489,513,518,538,567,587) AND status='accepted'` → 8 (`BK=8`). Write `$GOV/governance/knowledge/development/g2ptw30f-flip.sql` with a QUOTED heredoc, EXACTLY:
> ```
> .timeout 5000
> BEGIN IMMEDIATE;
> CREATE TEMP TABLE g_pre(x INTEGER CHECK(x=8));
> INSERT INTO g_pre VALUES((SELECT COUNT(*) FROM lesson_proposals WHERE id IN (444,474,489,513,518,538,567,587) AND status='accepted' AND route='codify'));
> SELECT 'PRE_F='||x FROM g_pre;
> .output /tmp/g2ptw30f/flip-capture.txt
> SELECT COALESCE(id,'')||'|'||COALESCE(status,'')||'|'||COALESCE(route,'')||'|'||COALESCE(category,'')||'|'||COALESCE(status_updated_by,'')||'|'||COALESCE(status_updated_at,'') FROM lesson_proposals WHERE id <= 590 ORDER BY id;
> .output stdout
> UPDATE lesson_proposals SET status='implemented', status_updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now'), status_updated_by='ceo' WHERE id IN (444,474,489,513,518,538,567,587) AND status='accepted' AND route='codify';
> SELECT 'CHANGES_F='||changes();
> SELECT 'EXCL_F='||COUNT(*) FROM lesson_proposals WHERE id IN (444,474,489,513,518,538,567,587) AND status='implemented' AND status_updated_at NOT IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z');
> SELECT 'ACC_POST='||COUNT(*) FROM lesson_proposals WHERE status='accepted';
> SELECT 'IMPL_POST='||COUNT(*) FROM lesson_proposals WHERE status='implemented';
> COMMIT;
> ```
> ⚠️ The `.output` path is the LITERAL scratch dir A2 created (`sqlite3` dot-commands take no variables) — never a path inside the canonical bellows checkout. Run: `[ -d /tmp/g2ptw30f ] || mkdir -p /tmp/g2ptw30f; sqlite3 -bail "$DB" < "$GOV/governance/knowledge/development/g2ptw30f-flip.sql"` (⚠️ `-bail` is LOAD-BEARING twice: with the capture dir missing it stops at `.output` and the transaction never commits; and the CHECK pre-check aborts the flip only under it — without `-bail` sqlite skips the failed statement or dot-command and the flip COMMITS, with no capture file or with the eight not all accepted) → the five sentinel lines BY NAME: `PRE_F=8`, `CHANGES_F=8`, `EXCL_F=8`, `ACC_POST=17`, `IMPL_POST=402` (a `CHECK` abort prints a line beginning `Runtime error near line 4: CHECK constraint failed: x=8` and writes nothing — HALT on that MESSAGE; never read the exit code through a pipe, it is the last command's). **Read-back on a FRESH read-only connection** (durability is proven post-commit, never by in-transaction sentinels): the eight rows `implemented|codify|ceo|<stamp>` with every stamp outside the two vintages; `accepted` 17; `implemented` 402; the capture file has 590 lines and 25 rows containing `|accepted|codify|`. Copy the capture into the worktree: `mkdir -p knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16 && cp /tmp/g2ptw30f/flip-capture.txt knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt` (A5 commits it; QA verifies it). Commit the SQL in governance, the message on stdin: `git -C "$GOV" add governance/knowledge/development/g2ptw30f-flip.sql && git -C "$GOV" commit -F - -- governance/knowledge/development/g2ptw30f-flip.sql <<'MSG'` with the subject `[<id>] gate2-pt-w30-f: the flip SQL (444, 474, 489, 513, 518, 538, 567, 587 -> implemented)`.
>
> **A5 — dev log + commit by explicit pathspec, the pre-check chained first.** `knowledge/development/dev-log-gate2-pt-w30-f-2026-09-16.md`: the A0 determination, A1's pins, A2's lines and the three refusals, A3's counts and the governance commit hash, A4's backup lines, sentinels, read-back and the SQL commit hash. Then, as ONE command: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/development/dev-log-gate2-pt-w30-f-2026-09-16.md knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt && git commit -F - -- knowledge/development/dev-log-gate2-pt-w30-f-2026-09-16.md knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt <<'MSG'` with the subject `[<id>] gate2-pt-w30-f: PT v4.118 + flip landed (dev log, flip capture)` and `MSG` closing it. `git status --short` → empty. STOP.
>
> **Deposits:**
> - `knowledge/development/dev-log-gate2-pt-w30-f-2026-09-16.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30f-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt`
>
> **Scope:**
> - `knowledge/development/dev-log-gate2-pt-w30-f-2026-09-16.md`
> - `/Users/marklehn/Developer/eluvian-governance/PLANNER_TEMPLATE.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/development/g2ptw30f-flip.sql`
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt`

## STEP 2 — QA

> **Step 1's Receipt status must be `Status: Complete`.** Anything else → HALT. **FIRST — post a short visible chat message.** You are the bellows QA agent. `cd "$(git rev-parse --show-toplevel)"`; `GOV=/Users/marklehn/Developer/eluvian-governance`; `B="$GOV/governance/knowledge/decisions/drafts/build-gate2-pt-w30-f-2026-09-15.py"`; `DB=/Users/marklehn/Developer/forge_lessons/lessons-forge.db`; `BPY=/Users/marklehn/Developer/bellows/.venv/bin/python` — re-derive per compound. Read-only beyond the three evidence files; the commit's message goes on stdin, as STEP 1's header states.
>
> **(A) Rule 20 self-check** — the canonical block at the path the dispatcher's mandate names (this machine's `RULE_20_SELF_CHECK_BLOCK.md`; quote the path you were handed). Run with:
> - `plan_slug`: `gate2-pt-w30-f-2026-09-16`
> - `qa_report_path`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/qa-receipt.md"`
> - `evidence_dir`: `"$(pwd)/knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16"`
> - `required_evidence_files`: `["probes-raw.txt", "full-suite-gate2-pt-w30-f.txt", "flip-capture.txt"]` (the capture committed by DEV in A5; QA verifies it is the committed file — `git log -1 --format=%s -- knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt` carries the plan's id)
>
> **(B) Items — raw output appended to `probes-raw.txt`** (⛔ the raw file carries NO summary-shaped line: P7 is named as "P7", never restated with its numbers; the suite's summary line stays in the suite file; no commit log is pasted beyond Item 4's path-scoped probe — MUST-PRESERVE's raw-file rule, thread 363):
> - **Item 1 — the live template:** `git -C "$GOV" log --oneline -1 -- PLANNER_TEMPLATE.md` (the `[<id>]` commit); every Task C count re-run (47 probes); `wc -l` 2668; `git -C "$GOV" status --porcelain -- PLANNER_TEMPLATE.md governance/knowledge/development/g2ptw30f-flip.sql` → EMPTY.
> - **Item 2 — the builder, by a second pair of hands (byte-identity):** `rm -rf /tmp/g2ptw30f-qa; mkdir -p /tmp/g2ptw30f-qa; C=$(git -C "$GOV" log -1 --format=%H -- PLANNER_TEMPLATE.md); git -C "$GOV" show "$C^:PLANNER_TEMPLATE.md" > /tmp/g2ptw30f-qa/PT-pre.md; python3 "$B" /tmp/g2ptw30f-qa/PT-pre.md /tmp/g2ptw30f-qa/PT-out.md; echo "builder_exit=$?"; cmp /tmp/g2ptw30f-qa/PT-out.md "$GOV/PLANNER_TEMPLATE.md" && echo BYTE_IDENTICAL` → the `BUILT:` line, `builder_exit=0`, `BYTE_IDENTICAL`; P4 with the builder's own commit; the three refusals reproduced.
> - **Item 3 — the flip, read back on a FRESH read-only connection:** the eight rows `implemented|codify|ceo|<stamp outside the two vintages>`; `accepted` 17 — and the seventeen by count, each still at one of the two vintages (`SELECT COUNT(*) … WHERE status='accepted' AND status_updated_at IN ('2026-09-02T23:54:37Z','2026-09-13T18:27:07Z')` → 17); `implemented` 402; `wc -l knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/flip-capture.txt` → 590; the capture's eight rows for the flipped ids read `accepted|codify` (the pre-flip state, captured before the UPDATE).
> - **Item 4 — the full-suite file:** `"$BPY" -m pytest tests -q -p no:cacheprovider > knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/full-suite-gate2-pt-w30-f.txt 2>&1; echo "exit=$?" >> knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/full-suite-gate2-pt-w30-f.txt` → `exit=0` and a summary line with NO `failed`/`error` count — P7's counts are the expectation, but the gate (`_gate_qa_test_result`) reads FAILURES against `known_failures`, never the pass count: if bellows `main` moved since P7 was measured (`git log --format=%h <P7's commit>..HEAD -- tests` non-empty — P7's commit is the anchor — `7fdc06cb` at the derivation, the newest QA's; re-read at the freeze), the drift is stated in `probes-raw.txt` under Item 4 as that probe's command and the HASHES it printed (`--format=%h`, never `--oneline` — a bellows QA commit's subject carries `N passed`, the summary shape the raw file must not hold), and nothing broader; the receipt's suite row stays the FILE and `exit=0`, never a note: a drift note reaches for `not run`, `skipped` or `pending`, and the canonical block fails a positive row carrying any of them. A different pass count with zero failures is not a halt. ⛔ A suite file WITHOUT a summary line (a crashed run — a traceback, a killed process) is a HALT, never a re-run note and never a second attempt written over it: with the named suite file summary-less the gate falls to the next `.txt` carrying one, and the raw-file rule above is the only thing standing between that and a stale count (the EXECUTION seat measured both configurations, S2-1).
>
> **(C) The report** `qa-receipt.md`: the verification table — status cells carry the glyph only, and NO positive row quotes a probe token or an output line carrying a Rule 20 hedging keyword (`hedging_keywords` in the canonical block): this plan has TWO such sources — the suite summary line and the agent's OWN prose (a drift note, a re-run note: `not run`, `skipped` and `pending` are the words such a note reaches for, and the block fails a positive row carrying any of its `hedging_keywords` whoever wrote it) — so the suite row names the FILE and `exit=0` with the drift left in `probes-raw.txt`, and Item 1's row reads `47/47 counts as pinned in A3, see probes-raw.txt` (the raw file may carry the tokens; the receipt's positive rows may not — a row quoting any of them FAILS the self-check) — the follow-ups (the DC/PST tranche after this one — the PT route now empty; the Planner pushes governance and projects the markers; thread 336 at the keyboard), the Rule 20 stdout APPENDED. Commit, as ONE command with the pre-check chained and the message on stdin: `"$BPY" tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/ && git commit -F - -- knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/ <<'MSG'` with the subject `[<id>] QA: PT v4.118 rebuilt byte-identical from the pre-edit blob; flip read back (8 implemented, 17 accepted remain)` and `MSG` closing it. STOP.
>
> **Deposits:**
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/full-suite-gate2-pt-w30-f.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/qa-receipt.md`
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/probes-raw.txt`
> - `knowledge/qa/evidence/gate2-pt-w30-f-2026-09-16/full-suite-gate2-pt-w30-f.txt`

Rule 20 banner (byte-exact, produced by RUNNING the canonical block — never hand-authored):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
