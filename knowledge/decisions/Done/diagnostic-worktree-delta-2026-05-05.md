# Bellows — Worktree Recommendation Delta Check

**Date:** 2026-05-05 | **Tier:** Small | **Test Scope:** targeted (no test execution — read-only diagnostic) | **Execution:** Step 1 (Bellows SA)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS. The Planner reads the deposited findings file via Rule 22 verification and authorizes housekeeping directly.

**Bootstrap:**

```
Read the plan at bellows/knowledge/decisions/diagnostic-worktree-delta-2026-05-05.md. Execute Step 1 ONLY. After completing Step 1, STOP.
```

---
---

## STEP 1 — Bellows SA

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-worktree-delta-2026-05-05.md", "bellows/knowledge/decisions/in-progress-diagnostic-worktree-delta-2026-05-05.md")`. **You are the Bellows Systems Analyst.** Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md`. **Skip domain glossary read — bellows has no glossary; this is a code-tracing + recommendation-delta task.** **Context (read these BEFORE investigating, in order):** (1) `bellows/knowledge/research/worktree-cost-coverage-recommendation-2026-05-03.md` — the existing SA recommendation you are checking for staleness; specifically the surface map citations (Phase A1, A2, A6, A7), the 12-dimension cost-coverage matrix, and the Step 1E two-plan implementation structure. (2) `bellows/knowledge/research/worktree-implementation-surface-2026-05-03.md` — the line-number surface map being delta-checked. Phase A1 cites the 4 `_capture_git_diff` call sites and `run_plan` insertion points. (3) `bellows/knowledge/research/worktree-candidate-designs-2026-05-03.md` — D2/D3/D5/D6/D7 sections referenced from the recommendation. (4) The 2026-05-04 monorepo-worktree-fix close in `bellows/knowledge/BACKLOG.md` (search for "Closed 2026-05-04: `2026-05-04: monorepo-worktree-at-governance-root structural fix`") — describes the detect-and-skip implementation that introduced `_create_worktree` and `_teardown_worktree` to `bellows.py`. **Task — answer two specific questions and ONLY these two questions. Do not re-evaluate (a)/(b)/(d)/(e) fix shapes; the SA already ranked them. Do not re-derive coverage analysis; the matrix is locked. Do not propose new candidates.** **Question 1 — Code interaction with the 2026-05-04 detect-and-skip fix.** The 2026-05-04 close shipped `_create_worktree(project_path, slug)` and `_teardown_worktree(project_path, wt_path, slug)` as no-ops for projects without their own `.git`. Read these two functions in current `bellows.py` HEAD. Determine: (a) where exactly are they called from in `run_plan`? (b) under what conditions are they called today — every plan dispatch, or only specific plan types (e.g., parallel-N-, executable, all)? (c) do their current signatures and bodies match what the 2026-05-03 recommendation's Step 1E proposes for parallel-isolation use, or do they need restructuring (parameters added, bodies extended, call sites changed)? (d) specifically: when a real-`.git` project (e.g., invoice-pulse) currently dispatches a plan, does `_create_worktree` already create a worktree for it today (per always-worktree scope locked 2026-05-03), or is the always-worktree behavior not yet wired up and the 2026-05-04 close was scoped only to bellows-self detect-and-skip? Report the answer with file:line citations. **Question 2 — Surface map staleness check.** The 2026-05-03 surface map cites specific line numbers in `bellows.py` (e.g., `_capture_git_diff` at lines 404-420, `run_plan` insertion points at lines 240/265/281/321/339, the 4 `_capture_git_diff` call sites). Verify: (a) are these line numbers still accurate at HEAD today? (b) have any new state, new functions, or new call sites been added to `run_plan` between 2026-05-03 and today (commits in the 2026-05-03 → 2026-05-05 window) that the surface map does not reflect? (c) are the 4 `_capture_git_diff` call sites still 4, or have any been added/removed? (d) has anything in `_create_worktree` or `_teardown_worktree` (which the 2026-05-03 recommendation does NOT cite because they did not exist on 2026-05-03) created a new code-modification site the executable will need to address? Report any drift with old-line/new-line/diff-summary citations. **Hard scope rule.** If your investigation surfaces material that suggests the 2026-05-03 recommendation needs RE-EVALUATION (not just delta updates) — for example, if the 2026-05-04 close materially changed the cost analysis or the call-site topology has shifted enough that Step 1E's two-plan structure no longer fits — flag this in "Flags for CEO" and stop. Do NOT re-derive the recommendation. The Planner will decide whether a fresh SA pass is needed. **Deliverable.** Write findings to `bellows/knowledge/research/worktree-delta-check-2026-05-05.md`. Structure: (i) Q1 answer with file:line citations, (ii) Q2 answer with file:line citations and any line-drift table, (iii) "Surface map updates required for executable" — a short list of specific edits (e.g., "replace `bellows.py:265` with `bellows.py:N`") OR "no updates required, surface map current as of HEAD," (iv) "Recommendation impact" — one of: "no change to 2026-05-03 recommendation," "additive update required (specify)," or "full re-evaluation required (flag to CEO)." **Standard prompt feedback protocol** → `bellows/knowledge/research/agent-prompt-feedback.md`. **Final commit message:** `docs: worktree-delta-check findings 2026-05-05`. **Deposits:**
> - `bellows/knowledge/research/worktree-delta-check-2026-05-05.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to any other step. Do NOT move the plan to Done. The Planner performs Rule 22 verification on the deposited findings file and the move-to-Done.**
