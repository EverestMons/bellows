# bellows — diagnostic: THE ACTION CENSUS — every act the shop performs, enumerated from the surfaces that perform it (the per-machine acts MACHINE_SETUP names, bellows' tools and scripts by their own `--help`, the daemon's verbs and event sites, the dashboard's keys, the depositor's admission, tuyere's five commands and its watcher's handler map, the MCP server's nine tools, and the Planner's acts as doctrine names them), each classified as MODEL-RUN or DETERMINISTIC by a stated test, priced from the lifecycle DB where a price exists and from the logs where a duration does, and marked with how it can be reached from another machine today — the evidence the hub's button set (thread 292) is ruled on; no recommendation (thread 293)

**Date:** 2026-09-11 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic; writes one research doc, one TSV and a dev-log) | **Execution:** Step 1 (DIAGNOSTIC) | **pause_for_verdict:** after_step_1

**auto_close:** false

**Post-close:** no restart, no doctrine. The doc's inventory, its two shares and its per-act reachability are the input to thread 292's three decisions (which acts become the CEO's buttons; the kill switch; auth) and to the pricing the CEO asked for; the hub, its buttons and any handler are drafted after this doc, by plans of their own. This plan recommends nothing. Close acts: the Planner commits the two governance deposits at the wrap; `tuyere.threads done 293` after the CEO has read the doc (a work thread — no commit required).

**Depends on:** thread 293 (CEO direction 2026-09-11, session d04ebd33: "an open diagnostic about what this hub could control via buttons and code which doesn't rely on an ai model. It would be worth pricing out the actions that use ai and which do not.") and thread 292 (the hub umbrella, whose decisions wait on this). Clone origin by kind and layout: `Done/diagnostic-100071.md` (the QA-scope census, this morning): one DIAGNOSTIC step, numbered questions, doc + TSV as Planner-commit deposits, the census script in the doc's appendix, no recommendation; the surfaces are READ through their own entry points (`--help`, the code's own tables) and never restated from memory.

**Tier computed (§1):** **T1** — T-1 fires (a read across bellows, tuyere, the lifecycle DB and the daemon logs); T-6 does not (no doctrine edit); T-2 does not (no source, hook, tool or lane change; no write-bearing act is executed to time it).

## CEO Context

The hub (292) is a page of buttons over the machines; before any button exists you asked which acts a button could perform with plain code and which need a model, and what each costs. Nobody holds that list today: MACHINE_SETUP §6 names six operator acts; bellows carries 20 tools and 13 scripts with their own flags, two daemon verbs, three dashboard keys and a depositor that admits, holds and clears; tuyere carries five commands (`claims`, `enqueue`, `threads`, `wraps`, `control`), a watcher whose handler map holds one entry (`noop_echo`, thread 78) and an MCP server with nine tools; the daemon runs three kinds of model step and the Planner performs acts doctrine names (the drafting cycle, the (b) read, the verdict, the wrap, the release request, the restart). The lifecycle DB prices the model steps — 137 of 143 step rows carry `cost_usd` (P5) — and prices nothing else; the deterministic acts have durations in the daemon log and none in the DB; the Planner's own session cost is recorded nowhere. This diagnostic builds the list from the surfaces themselves, applies one test to every row — an act is DETERMINISTIC when its inputs are files and DB rows and its output is fixed by them; it is MODEL-RUN when it reads prose for meaning or writes prose — prices what has a price, times what can be timed without performing a write-bearing act, and states for each row how another machine can reach it today (ssh since this morning; a tuyere intent for `noop_echo` alone; the MCP read tools behind the Funnel). The numbers you asked for — the share of the acts, and of a plan's cost, that needs a model at all — close the doc; the button set is your ruling on 292.

## Numbers discipline — measured 2026-09-11 by the Planner (bellows `d3456b9`, governance `9bd8d91d`, tuyere at `/Users/marklehn/Developer/tuyere`, daemon pid 60199 under the agent)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the per-machine acts doctrine names | `MACHINE_SETUP.md` §6 "The acts an operator performs per machine (each is a tool, never a bare rename)": six bullets — Deposit (`tools/deposit_receipt.py <draft> <session-id>` BEFORE the `ready-` copy), Migrate to the agent (`bellows.py stop` then `bash scripts/install-daemon-agent.sh`; v1.11), Release a class hold (`tools/clear_plan.py <hold-file> --release-class-hold`), Verdicts (`tools/issue_verdict.py <id> <step> continue\|stop --reason`; overrides `clear_plan.py --override-gate`), Hooks (the harness loads the repo's files), Every act lands in the shared git namespaces; §7 lists nine post-setup asserts (repos current, daemon running, requirements importable, `config.json` consistent, power, Tailscale, `id_sequence`, tuyere liveness, hooks registered, a T0 canary) | `sed -n 62,82p MACHINE_SETUP.md` under the governance root |
| P2 | ⛔ bellows' surfaces, counted from the tree | `tools/*.py` 20 files, 15 with `argparse`; `scripts/*.py` 13 files, 6 with `argparse`; `scripts/*.sh` 2 (`bootstrap.sh`, `install-daemon-agent.sh`); `bellows.py` verbs `sys.argv[1] in ("stop", "restart")` (the guarded stop, then `_perform_restart` — `launchctl kickstart -k` when the agent is loaded); `dashboard.py` keys `q`, `r`, `y` (quit, restart, confirm); `depositor.py` admission: `_assign_class(writes, project_root)`, `_hold(path, reason, details)`, `_clear(path, assigned_class)`; the daemon's own acts: 22 `_log("EVENT", …)` sites in `bellows.py` (detected plan, RUNNING, started, gates step N, paused, merged, closed, queue empty — the DEV reads each site's message); `hooks/commands/` two slash commands (`wrap.md`, `eluvian.md`) | `ls tools/*.py \| wc -l`; `grep -l argparse tools/*.py \| wc -l`; the same for `scripts/`; `grep -cF '_log("EVENT"' bellows.py`; `grep -oE 'ord\("[a-z]"\)' dashboard.py \| sort -u` |
| P3 | ⛔ tuyere's surfaces, by their own `--help` | `tuyere/tuyere/`: `claims.py {claim,release,status}`, `enqueue.py {intent,note,status}`, `threads.py {add,list,show,done,drop,claim,release,supersede,retype}`, `wraps.py {record,list}`, `control.py` ("Keyboard-only control operations (mutate scope — never voice-reachable)": `pause`, `resume`), `watcher.py` (the launchd watcher; its handler map in `handlers.py` — `noop_echo` the only handler, thread 78), `db.py`; the MCP server `server/tools.py`: nine `@mcp.tool()` functions — `capture_note`, `capture_thread`, `enqueue_intent`, `get_intent`, `list_actions`, `list_machines`, `list_threads`, `queue_status`, `why_pending` — served by `server/app.py` behind the Tailscale Funnel with OAuth | `.venv/bin/python -m tuyere.<m> --help` from the tuyere checkout for each of the five; `grep -cF '@mcp.tool' server/tools.py`; `grep -n noop_echo tuyere/handlers.py` |
| P4 | ⛔ the Planner's acts as doctrine names them | PLANNER_TEMPLATE v4.109 and DRAFTING_CYCLE v2.36: the drafting cycle (walk 0's pins and consumer dry-run, the lens commits through `scripts/lens_commit.py`, the close through `scripts/close_cycle.py`, the checks `plan_lint`, `fold_check`, `walk_register_lint`, `lens_order_check`, `cycle_check`, `propagation_check`); the deposit (P1's first act); the (b) read at a pause and the verdict (`tools/issue_verdict.py`); the wrap (`/wrap`: the queue check, the lock, fetch, the lessons sweep with `tools/lessons_guard.py pin/verify`, the glossary, the baton, the threads, the wrap record `tuyere.wraps record`); the release REQUEST (the command handed to the CEO); the restart after a daemon-loaded merge; STANDING_CONSTRAINTS v1.1's nine lines govern them — the DEV lists each act with the doctrine line that names it | `grep -n` over the three files for `lens_commit`, `close_cycle`, `issue_verdict`, `deposit_receipt`, `lessons_guard`, `wraps record`, `restart` |
| P5 | ⛔ what the lifecycle DB prices (`mode=ro`) | `steps` 143 rows: 137 carry `cost_usd`, 139 carry `duration_s`; by `step_number` (n, mean `cost_usd`, mean minutes): 1 → 80, $2.631, 17.2; 2 → 55, $1.395, 8.7; 3 → 2, $0.938, 5.2; per plan (sum of its steps): `executable` 65 plans mean $3.949, `diagnostic` 15 plans mean $2.156; `plans` by state: closed 71, halted 11, abandoned 3, in_progress 1; `verdicts` 141 rows by `decided_by`: `verdict_file` 121, `gate_auto` 18, `gate_recheck` 2; `gate_events` 1289 rows; 14 tables (`clearances, commits, deposits, derivations, diagnostic_meta, executable_meta, gate_events, id_sequence, ledger_writes, plans, prompt_feedback, steps, step_files, verdicts`) — NO table prices a Planner session, a wrap, a release or a restart | the six queries against `file:lifecycle.db?mode=ro` |
| P6 | ⛔ what the logs time (deterministic acts, from today's log — never re-performed by this plan) | `logs/terminal/bellows-2026-09-11.log`: a guarded restart under the agent — `SIGTERM received` 16:32:39 → `auth preflight OK` 16:32:48 (9 s of which the `claude -p` preflight is 5–7 s); a class-hold release → claim: `notifier: class_hold … paged` 15:57:37 → `minted id` 16:01:24 with three declined rescans between (the stranded claim, thread 296) and 16:37:13 → 16:37:14 on the clean run; a deposit's admission: the `ready-` copy → the `hold-` rename within one watcher poll (≤ 12 s in this session's three deposits); the full bellows suite `2276 passed in 106.64s`; anvil's `274 passed in 1.60s`; a lens commit through `lens_commit.py` ≈ 20–40 s (the battery); a `close_cycle.py` run ≈ 60–90 s | the log's timestamps; the suite lines in this session's receipts |
| P7 | ⛔ reachability from another machine, today | ssh from the mini to the Air (`ssh marklehn@100.79.204.47`, Remote Login + key, since 2026-09-11 morning) and the reverse not yet proven; a tuyere intent reaches a machine's watcher, whose handler map executes `noop_echo` only (P3); the MCP server's nine tools are reads plus `enqueue_intent`, `capture_note`, `capture_thread` (writes to the shared tables), behind the Funnel's OAuth; `tuyere.control pause` is keyboard-only by GOVERNANCE; the dashboard's keys are local to the machine's TUI; the daemon's launchd agent restarts on `launchctl kickstart -k` from a local shell (thread 297: no idle guard on that path) | MACHINE_SETUP §4/§7; `tuyere/GOVERNANCE.md` `control` lines; this session's ssh acts |
| P8 | in-flight; class; interpreter | #100083 in flight at drafting; two closed drafts wait ahead of this one (commits-per-step, thread 275; what-works callers, thread 283) — this plan deposits after #100083 closes and both daemons restart, and its class auto-clears; writes: a bellows dev-log and two governance research files → **app-feature** (`_assign_class`, walk 0; the depositor's parse recorded at deposit); the bellows venv (Python 3.12.14) for bellows and the DB, `/Users/marklehn/Developer/tuyere/.venv/bin/python` for tuyere's `--help` and read verbs; no anvil, no forge | `.venv/bin/python status.py`; `_assign_class` |

## MUST-PRESERVE

- ⛔ **Read-only, and no act is PERFORMED to be measured.** The inventory is read from `--help` output, the code's own tables and the doctrine's lines; the prices from the DB (`mode=ro`) and the durations from the logs; NO release, restart, deposit, kill, pause, verdict, intent or push is executed by this step — a row whose duration the logs do not carry says `not timed`.
- ⛔ **Enumerated, never recalled.** Every row names the surface it was read from (a file and a line, a `--help` line, a DB query); a surface the DEV cannot read is listed as `unread` with the reason, never filled from memory.
- ⛔ **One test, quoted on every row.** DETERMINISTIC: inputs are files and DB rows and the output is fixed by them (a release, a restart, a pull, a lint, a gate, a reconcile, a `--help`); MODEL-RUN: the act reads prose for meaning or writes prose (a DEV/QA/DIAGNOSTIC step, a (b) read, a verdict's reason, a draft, a lessons sweep, a baton). A row that fits neither cleanly is `mixed` with the two halves named (a wrap: its checks are deterministic, its sweeps are model-run).
- ⛔ **No recommendation.** The doc answers Q1–Q5 and ends with "What this does not establish"; which acts become buttons, the kill switch and auth are 292's decisions, the CEO's.
- ⛔ **`lifecycle` is never imported;** the DB is read with `sqlite3` directly; tuyere is invoked only through `--help` and its read verbs (`threads list`, `claims status`, `wraps list`, `enqueue status`), never `control`, `enqueue intent`, `claims claim/release`, `threads add/done/drop`.

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-action-census-2026-09-11.md
**Walks:** walk 0 pinned (P1–P8 measured on bellows `d3456b9`, governance `9bd8d91d`; clone-diff against `Done/diagnostic-100071.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py`; the tool run bare, its exit read; no lens or walk number in any `--desc`; `lens_order_check` after every walk.

- Weak spots:          w1 2 folded — instruction 2 / record 0; w2 1 folded — instruction 0 / record 1
- Destruction:          w1 dry; w2 dry
- Vulnerabilities:      w1 dry; w2 dry
- Integration-record:   w1 dry; w2 dry
- ACID:                 w1 dry; w2 dry
**Closing:** WARM close after walk 2 — BAR MET (T1), thread 293's measurement before the hub. Two walks: 2 → 0 (walk 1 made the census read every tool as a subprocess and as text so the script never imports `lifecycle`, and made the thread-count post-condition read the full listing; walk 2 removed a drafting placeholder from the record and was dry on every lens). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-` AFTER #100083 closes and both daemons restart (P8); class app-feature auto-clears; the daemon claims and the verdict is the Planner's own call after the (b) read. Close acts: the Planner commits the two governance deposits at the wrap; 292's three decisions are the CEO's, on this doc.

## Cycle Manifest
tier: T1
target: knowledge/development/dev-log-action-census-2026-09-11.md
class: app-feature
reads: bellows.py, dashboard.py, depositor.py, tools/*.py, scripts/*.py, scripts/*.sh, hooks/commands/wrap.md, hooks/commands/eluvian.md, lifecycle.db, logs/terminal/bellows-2026-09-11.log, /Users/marklehn/Developer/tuyere/tuyere/*.py, /Users/marklehn/Developer/tuyere/server/tools.py, /Users/marklehn/Developer/tuyere/GOVERNANCE.md, MACHINE_SETUP.md, PLANNER_TEMPLATE.md, DRAFTING_CYCLE.md, STANDING_CONSTRAINTS.md, knowledge/decisions/Done/diagnostic-100071.md
writes: knowledge/development/dev-log-action-census-2026-09-11.md, governance/knowledge/research/action-census-2026-09-11.md, governance/knowledge/research/action-census-2026-09-11.tsv
open_forks: 1. the button set — which of the CEO-keyboard rows become intents with a handler (292's first decision); 2. a price for the Planner's own acts — the session's tokens are recorded nowhere; a `session_cost` column or a wrap-time capture would be a plan of its own; 3. the reverse ssh (Air → mini) and the Air's Remote Login, if a hub on the mini must reach the Air's keyboard acts; 4. the handler map beyond `noop_echo` (thread 78) — the first real handler is the first button
walks: 2
yields: 2, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:11
coherence: 2/2 body walks named in the register (3 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.diagnostic-bellows-action-census.md.foldcheck.json

---

## STEP 1 — DIAGNOSTIC: five questions over every surface that performs an act, one row per act

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE; tuyere's is `/Users/marklehn/Developer/tuyere/.venv/bin/python`, ABSOLUTE, run with `cd /Users/marklehn/Developer/tuyere` (its modules import by the cwd).** ⛔ **Read-only:** never run the daemon, `run_plan`, a claim, `bellows.py stop|restart`, `launchctl`, any `tools/*.py` beyond `--help`, `tuyere.control`, `enqueue intent`, `claims claim|release`, `threads add|done|drop|supersede|retype`; never import `lifecycle`; the DB is opened `sqlite3.connect("file:/Users/marklehn/Developer/bellows/lifecycle.db?mode=ro", uri=True)` and never written; the daemon log is READ at `/Users/marklehn/Developer/bellows/logs/terminal/bellows-2026-09-11.log`; the doctrine is read under `/Users/marklehn/Developer/eluvian-governance/`; `T=$(mktemp -d /tmp/action-census.XXXXXX)` holds the script and scratch output; nothing else is written outside the three deposits.
>
> **Scope:**
> - `knowledge/development/dev-log-action-census-2026-09-11.md`
>
> **Item 1 — re-derive P2, P3 and P5 and HALT on a mechanism mismatch** (a count that moved because a plan closed since drafting is reported as both figures; a `tools/*.py` without `argparse` that nevertheless takes arguments is listed as `unread-surface` and read by its docstring — not a halt). Write the census script to `$T/census.py` — verbatim in the doc's appendix — that builds the inventory MECHANICALLY where a surface is machine-readable: (a) for every `tools/*.py` and `scripts/*.py` with `argparse`, run `<interpreter> <file> --help` AS A SUBPROCESS (`subprocess.run([...], capture_output=True, timeout=60)` — several tools import `lifecycle` in their own process, which is their design; the census script itself never imports `lifecycle`, `bellows`, `depositor` or `dashboard` — it reads those four as TEXT; the bellows venv from the worktree root; `scripts/plan_lint.py`, `scripts/walk_register_lint.py` and `scripts/cycle_check.py` take no `--help` — list them by their docstring's first line and the usage their `sys.argv` read implies) and record `prog`, the positional and optional arguments and the subcommands; (b) for the two `.sh` scripts, the header comment lines; (c) for `bellows.py`, the two verbs and the 22 `_log("EVENT", …)` sites with their message text (`grep -nF '_log("EVENT"' bellows.py`); (d) for `dashboard.py`, the three keys and the method each dispatches to; (e) for `depositor.py`, `_assign_class`, `_hold`, `_clear` with their docstrings; (f) for tuyere, `--help` of the five commands under tuyere's venv, `server/tools.py`'s nine `@mcp.tool()` names with their docstrings' first lines, `handlers.py`'s map; (g) for the doctrine, `grep -n` over PLANNER_TEMPLATE, DRAFTING_CYCLE, STANDING_CONSTRAINTS and MACHINE_SETUP §6 for the act names P4 lists, each with its line; (h) the two slash commands' step lists (`hooks/commands/wrap.md`'s numbered steps, `eluvian.md`'s five). Paste each pin's re-derived count beside it.
> **Item 2 — Q1: the inventory.** One row per act, from Item 1's output and nothing else: columns `surface` (machine-setup / bellows-tool / bellows-script / daemon-verb / daemon-event / dashboard-key / depositor / slash-command / tuyere-cmd / tuyere-mcp / watcher-handler / doctrine-planner-act / model-step), `act` (one line), `invocation` (the command or key or file:line), `performer` today (CEO-keyboard / Planner-session / daemon / watcher / hook / harness), `source` (file:line or the `--help` line). A daemon EVENT site is a row when it marks an act the daemon performs (a claim, a gate run, a merge, a pause, a close) and a note when it only reports (a heartbeat); the DEV states which. The `model-step` rows are the three step kinds the daemon runs through `runner.run_step` (DEV, QA, DIAGNOSTIC) and the Planner's four prose acts (draft, (b) read + verdict reason, lessons sweep, baton). Count the rows by surface and by performer, one table each.
> **Item 3 — Q2: the classification.** Every row gets `kind` ∈ {`deterministic`, `model-run`, `mixed`} by the MUST-PRESERVE test, with the one-line reason in `test_note` ("inputs: the hold file and the DB; output: a rename and a clearance row" / "reads the dev-log for meaning"); `mixed` rows name both halves (a wrap: nine deterministic checks and two model-run sweeps; a lens commit: the battery deterministic, the fold model-run; the release: deterministic, but the DECISION to release is the CEO's reading — the act and the decision are two rows). Counts by kind, and by kind × performer.
> **Item 4 — Q3: the price.** For every `model-run` and `mixed` row that the lifecycle DB prices: the DB's figures (P5 — `cost_usd` and `duration_s` by step kind, the DEV separating DEV/QA/DIAGNOSTIC by the plan's `type` and the step's number, since `steps.role` is NULL), quoted as mean, median and the count they rest on; for the Planner's prose acts: `not priced — no table records a Planner session` (P5), with the ONE proxy the record holds — the count of lens commits and closes in today's governance log (`git -C /Users/marklehn/Developer/eluvian-governance log --since=2026-09-11 --oneline \| grep -cE 'lens [1-5]|close —'`) and the wall-clock between the first and last of a draft's commits, stated as a time bound, not a dollar figure. For every `deterministic` row: the duration the LOG or a receipt carries (P6 — quoted with its timestamps), `not timed` otherwise; a `--help` run's own wall-clock is recorded for the rows Item 1 ran (the tool's start-up cost, the only measurement this step takes), and no other deterministic act is performed. A dollar column that is empty is empty; the doc never estimates.
> **Item 5 — Q4: reachability today.** Every row gets `reachable_from` ⊆ {`keyboard`, `ssh`, `intent`, `mcp`, `none`} from P7's facts and the row's own invocation: a local TUI key is `keyboard`; a CLI tool is `keyboard` + `ssh`; an act the watcher's handler map names is `intent` (today: `noop_echo` alone, so no row but that one); an MCP tool is `mcp`; `tuyere.control` is `keyboard` by GOVERNANCE, quoted. Then `hub_button_needs`: for a row to become a button — the local tool it would wrap, the handler that would run it (existing or absent), the eligibility check the daemon or watcher would apply, and whether the act is one GOVERNANCE forbids remotely; the DEV fills the four cells from the row's source and P7, never from a design. Counts by `reachable_from`.
> **Item 6 — Q5: the two shares.** (a) `model-run` + `mixed` rows ÷ all rows, and `model-run` rows alone ÷ all rows; (b) of a plan's recorded cost (P5: the mean `executable` plan $3.949, the mean `diagnostic` $2.156), the share that is model-run — by construction every `cost_usd` dollar is a model step, so the share of the DB's recorded dollars is 100 % model-run and the deterministic acts' dollar cost is zero; the doc states that as the fact it is and puts the deterministic acts' price in SECONDS beside it (the sum of P6's timed durations for one plan's life: deposit admission + claim + N steps' gates + merge + release + restart — with `not timed` gaps named); (c) the same two shares restricted to the CEO-keyboard rows — the acts a button would replace. Three tables, the counts they rest on beside each figure.
> **Item 7 — write the research doc** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/action-census-2026-09-11.md` answering Q1–Q5 in order, each section opening with the script's raw output line it rests on, the MUST-PRESERVE test quoted once at Q2, the census script as a code block in an appendix (⛔ the WHOLE script, every line — never a header with a pointer to `$T`; the appendix is the instrument's only surviving copy), and the TSV `action-census-2026-09-11.tsv` — one row per act, columns `surface, act, invocation, performer, kind, test_note, price_usd, seconds, price_source, reachable_from, hub_button_needs, source`; end with "What this does not establish" (at least: the Planner's own token cost; the CEO's time per act; the hub's auth model; whether any act SHOULD be a button — 292's ruling; a duration read from one day's log is one sample; the Air's rows are the mini's rows by symmetry (MACHINE_SETUP) and were not read on the Air).
> **Item 8 — dev-log** with the headings declared below (the `dev_log_declared_text` gate reads them at the pause); `## Pins re-derived (P2, P3, P5)` opens with P5's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `a release or a restart`; paste through those words, then stop. **Commit** the dev-log alone (message tagged with the plan id and `thread 293`), gated on the FULL suite (`/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q` from the worktree root — nothing under bellows changed but the rule is the rule), path-scoped to the Scope list; the two governance files are Planner-commit deposits. Last act, after the commit: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P2, P3, P5)`; `## The two shares (Q5)`; `## Q1–Q5 in one table`; `## What the doc does not establish`
> **Verbatim:** `## Pins re-derived (P2, P3, P5)` ← P5
>
> **Deposits:**
> - `knowledge/development/dev-log-action-census-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/action-census-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/action-census-2026-09-11.tsv`
>
> **Post-conditions:** the doc answers five questions, every row with its surface, invocation, performer, kind, test note, price or `not priced`, seconds or `not timed`, reachability and source, and ends without a recommendation; Q5 states the two shares in the three forms named with their counts; the appendix holds the whole script (`grep -c '^import\|^def ' <doc>` ≥ the script's own count of those lines, pasted beside it); the TSV has one row per act and the same row count the doc's Q1 table states; the dev-log's four headings present as full lines with P5's cell whole; one bellows commit carrying the dev-log only; no gate, tool, script, lane file, DB row, daemon or tuyere row changed (`git -C /Users/marklehn/Developer/eluvian-governance status --porcelain -- governance/knowledge/research/` lists the two research files, any other entry named as pre-existing; `git status --porcelain` in the worktree shows nothing after the commit; `PRAGMA data_version` on the DB read before and after the census is equal, or the doc names the daemon write that moved it; `tuyere.threads list --all --limit 1000 --json` — its trailing `meta` line's `returned` — equal before and after); nothing under `$T` that survives the step.
