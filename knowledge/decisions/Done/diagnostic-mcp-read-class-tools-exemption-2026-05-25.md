# Bellows — MCP Tool Denials READ_CLASS_TOOLS Exemption Characterization

**Date:** 2026-05-25 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** none (read-only diagnostic) | **Execution:** Step 1 (SA) | **pause_for_verdict:** always

## Context

The 2026-05-22 BACKLOG open entry reports that `mcp__vexp__run_pipeline` and `mcp__vexp__get_context_capsule` denials fire the `no_permission_denials` gate (5 log-level denial events / 2 gate-level failures over 30 days). The recommended resolution shape is to extend `READ_CLASS_TOOLS` in `gates.py` to filter these MCP tool denials from gate evaluation, following the architectural precedent shipped 2026-04-28 (`Closed 2026-04-28: BACKLOG #2 ... added READ_CLASS_TOOLS = {"Grep", "Glob", "Read"} to gates.py and modified _gate_no_permission_denials to filter read-class denials`).

A 2026-05-12 plan claims to have added `mcp__vexp__run_pipeline` to READ_CLASS_TOOLS (`Closed 2026-05-12: Intermediate decision detector + Vexp exemption shipped. ... Vexp code-exploration tool mcp__vexp__run_pipeline added to READ_CLASS_TOOLS exemption in gates.py to close gate-exemption gap`). If true, the 2026-05-22 audit observed denials in spite of that exemption — meaning either (a) the exemption is literal-string and doesn't cover `mcp__vexp__get_context_capsule`, or (b) the 2026-05-12 add landed in a different place than READ_CLASS_TOOLS itself, or (c) the filter mechanism reads tool names differently than expected.

The BACKLOG entry's open question is exactly this: extend READ_CLASS_TOOLS with specific tool literals (treating MCP read tools as a known list), or generalize the exemption mechanism (any tool name matching `mcp__*_read*` or similar pattern)? This diagnostic answers that question before the executable ships.

## How to Run This Plan

```
Read the plan at bellows/knowledge/decisions/diagnostic-mcp-read-class-tools-exemption-2026-05-25.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed.
```

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-mcp-read-class-tools-exemption-2026-05-25.md", "bellows/knowledge/decisions/in-progress-diagnostic-mcp-read-class-tools-exemption-2026-05-25.md")`. You are the Bellows Systems Analyst. Read your specialist file and domain glossary first. This is a read-only investigation. Do NOT modify any code, tests, or governance. **Task:** characterize the current state of MCP tool exemption in `bellows/gates.py` and recommend a resolution shape (literal-list extend vs. pattern-match generalize) for the open BACKLOG entry on MCP tool denials. **Investigation questions, answer all six in your deposit:** (Q1) What is the current literal value of `READ_CLASS_TOOLS` in `gates.py`? Quote verbatim with line number. Is `mcp__vexp__run_pipeline` actually present (validating the 2026-05-12 close) or absent (invalidating it)? (Q2) How is the set used in `_gate_no_permission_denials`? Quote the filter code verbatim with line numbers. Does it match by string equality, prefix, or pattern? (Q3) Enumerate every `mcp__*` tool name observed in the 30-day denial log corpus (read `bellows/logs/terminal/*.log` and search for `permission denied` or `Permission denied` or denial-event patterns from `runner.py` output). Report each distinct tool name with its denial count and the dates of occurrence. This includes but is not limited to `mcp__vexp__run_pipeline`, `mcp__vexp__get_context_capsule` — find all of them. (Q4) For the tools enumerated in Q3, classify each as (i) read-class (read-only operations, no side effects — safe to exempt), (ii) write-class (modifies state, MUST NOT be exempted because denials carry real signal), or (iii) ambiguous (requires CEO judgment). Provide the basis for each classification (read the MCP tool's documentation, naming convention, or behavior description if available; fall back to naming-convention inference noting it as such). (Q5) Compare two resolution shapes: **Shape A** — extend READ_CLASS_TOOLS as a literal set with each read-class MCP tool enumerated by exact name (e.g., `READ_CLASS_TOOLS = {"Grep", "Glob", "Read", "mcp__vexp__run_pipeline", "mcp__vexp__get_context_capsule", ...}`); **Shape B** — generalize the filter to match `mcp__*` patterns or `mcp__*_read*`/`mcp__*_get*`/`mcp__*_query*` patterns indicating read-class behavior by name convention. For each shape: (a) LOC estimate (production + test), (b) risk of over-exemption (false-negatives — legitimate denials filtered as exempt), (c) risk of under-exemption (false-positives — read-class tools still firing gate), (d) maintenance cost when new MCP tools appear in the corpus. Make a recommendation between A and B with reasoning. If a third shape emerges from the code reading (e.g., a different filter site, a config-driven allowlist), describe it. (Q6) Confirm or refute the BACKLOG-entry hypothesis that 5 log-level denials produced 2 gate-level failures over 30 days. Provide the actual counts from log scan. If the ratio differs significantly, hypothesize why (e.g., the filter is partially working). **Read access scope:** `bellows/gates.py` (full read), `bellows/runner.py` (search for denial-event emission patterns), `bellows/logs/terminal/*.log` (30-day window: 2026-04-25 to 2026-05-25), `bellows/knowledge/BACKLOG.md` (for the entry citation context), `bellows/knowledge/decisions/Done/*vexp*` if any exist (for the 2026-05-12 close history). Do NOT read tests unless a Q3 enumeration needs to cross-check log evidence against test fixtures. **Deposit findings** at `bellows/knowledge/research/mcp-read-class-tools-exemption-2026-05-25.md` with sections: (1) Current state — Q1 + Q2 verbatim quotes with line numbers; (2) MCP tool corpus — Q3 enumeration table; (3) Classification — Q4 read/write/ambiguous table with basis; (4) Resolution shapes — Q5 A-vs-B comparison; (5) Population sanity check — Q6 actual counts vs BACKLOG hypothesis; (6) Recommendation — single paragraph naming the recommended shape and LOC estimate; (7) Output Receipt with Status: Complete. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/research/mcp-read-class-tools-exemption-2026-05-25.md`
>
> **STOP. Do NOT proceed. Wait for CEO confirmation. This is a single-step diagnostic — the Planner reviews the findings and authors the executable as a separate plan.**
