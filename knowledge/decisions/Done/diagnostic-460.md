# bellows — cycle_check.py format census + assert-input Gap Assessment
**Date:** 2026-08-19 | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic; Q6 only DESIGNS cycle_check's contract for the downstream executable) | **Execution:** Step 1 (DIAG) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

## Context

The CEO routed `proposal-cycle-automation-depositor-2026-08-18.md` on 2026-08-19 and directed `cycle_check.py` be built first (standalone validator; the depositor is a separate, in-bellows build with a dashboard UI). Before authoring that executable, this read-only diagnostic pins the Cycle Log formats `cycle_check` must parse and produces a Rule 27 Gap Assessment the executable implements without re-verification (T-7).

**Load-bearing premise correction (Planner walk-0 finding, to be confirmed by this diagnostic):** the proposal §3 asserts `cycle_yields.py` "already has lens regexes and folded/dry matchers… what is missing is a validator." That is only half true. `cycle_yields.py` reuses cleanly for block extraction (`extract_dc_blocks`), lens prefixes (`LENS_PREFIXES`), and the folded/dry/origin matchers — but it has **NO parser for the instruction-vs-record class split** (`— instruction 1 / record 0`), which is the axis assert #1 (internal arithmetic) rests on. `cycle_yields`'s origin axis (pre-existing / fold-introduced) is a DIFFERENT axis. Assert #1 needs a new regex.

**Measured format variance (Planner walk-0, to confirm):** of 37 Done plans carrying a `## Drafting Cycle` block, only **5** carry `— instruction N / record M` class-split lines; the other ~32 use the older `wN K folded (prose)` form with no class split. So the class-split arithmetic assert is **N/A on the majority** and present-and-checkable only on the newer form. `cycle_check` must return N/A (not FAIL) when the split is absent — a false-fail on 32/37 of the corpus would make it unusable.

**This is a read-only diagnostic.** It reads the Done/ corpus and `bellows/scripts/`, changes no code, and creates no files outside `knowledge/research/`. It confirms the premise correction, enumerates every format variant the three asserts must parse, maps each assert's input to the script that already provides it (or the gap), confirms the `plan_lint` check (f) defect the executable will supersede, and specifies `cycle_check`'s CONTINUE/BAR_MET/ESCALATE contract per the routed answers.

## Drafting Cycle
**Tier:** T1 — triggers fired: T-7 (the cycle_check executable will build on this diagnostic's Gap Assessment per Rule 27), T-8 (novel). No T-5/T-6 for a read-only diagnostic → no cold panel.
**Walk 0 (context pin):** targets `bellows/scripts/cycle_yields.py` (block/lens/folded/dry/origin parsers — reused), `bellows/scripts/plan_lint.py` (check (f), to be superseded), `bellows/scripts/fold_check.py` + `bellows/scripts/walk_register_lint.py` (assert #2/#3 input providers). Census scope measured: **37** Done plans carry a `## Drafting Cycle` block; **5** carry `— instruction N / record M` class-split lines (form: `- Weak spots: w1 1 folded — instruction 1 / record 0 (…)`); the STATUS aggregate class-count form is a second variant to census. Reuse boundary measured: `cycle_yields` parses folded COUNT + origin axis, NOT the instruction/record CLASS axis. Routed answers pinned: plateau=3, no cost-escalate, ESCALATE=pause-resume. Clone-diff: closest same-class `diagnostic-455` (structure-borrowed read-only bellows-infra diagnostic; no origin drift).
**Direction verdict (after walk 1):** **PROCEED** — the angle (census the Cycle Log formats + gap-assess cycle_check's asserts and contract) is sound; no forcing finding. The walk-0 premise correction (cycle_yields lacks the class-split axis; 5/37 blocks carry it) is a scope refinement, not a direction kill.
**Walks:** 4 (bar MET — walk 4 dry, zero findings, no restructuring fold). Yields 8 → 2 → 2 → 0.
- Weak spots (1.4):     w1 3 folded — instruction 2 / record 1 (W1: cycle_check is STATELESS — pause/resume is a caller contract, not an exit-code the script holds; W2: the census must cover the PARTIAL live block, not only Done/ complete blocks; record: Priority 2→1). w2 dry. w3 1 folded — instruction 1 / record 0 (W4: Q5's Rule 27 table is the executable's complete build contract but was scoped to (a)–(e); expanded to (f) decision function, (g) fail-closed, (h) partial-block, (i) degenerate verdicts). w4 dry.
- Destruction (2.4):    w1 1 folded — instruction 1 / record 0 (D1: the diagnostic does NOT authorize removing plan_lint check (f) — supersede-then-retire-behind-evidence; removal earns its own diff review downstream). w2 dry. w3 dry. w4 dry.
- Vulnerabilities (3.1): w1 2 folded — instruction 2 / record 0 (V1: fail-closed — an unparseable block must NEVER resolve to BAR_MET; V2: enumerate the degenerate inputs — zero-walk, mid-walk, multiple-DC-block, all-unparseable — with the correct verdict for each). w2 1 folded — instruction 1 / record 0 (V3: the census count is load-bearing — require the agent to state its exact parse method and cross-check two ways, guarding a silent false-absence). w3 dry (candidate weighed and rejected: a structural-vs-date discriminator nudge — Q3 already asks the agent to recommend one, folding it would manufacture yield). w4 dry.
- Integration-record:   w1 1 folded — instruction 1 / record 0 (I1: name the invocation context — the Planner runs it per-walk in-session, the in-bellows depositor RE-RUNS it at deposit; the contract serves both). w2 1 folded — instruction 1 / record 0 (I2: the plan cites proposal §3/§5 but the agent runs in bellows/ — give it the root-relative proposal path as required reading). w3 1 folded — instruction 1 / record 0 (I3: asserts #2/#3 read git, so cycle_check must run AFTER the walk is committed — name the precondition + the un-committed verdict). w4 dry.
- ACID (5.2):           w1 1 folded — instruction 1 / record 0 (A1: specify the decision function that combines the 3 asserts + walk yield + plateau counter into one verdict, incl. how an N/A assert participates without blocking BAR_MET). w2 dry. w3 dry. w4 dry.
**Walk 1 STATUS:** 8 folded — instruction 7 / record 1 — NOT dry.
**Walk 2 STATUS:** 2 folded — instruction 2 / record 0 — NOT dry (yield 8→2, falling).
**Walk 3 STATUS:** 2 folded — instruction 2 / record 0 — NOT dry (yield flat; folds only in Weak-spots + Integration-record).
**Walk 4 STATUS:** 0 folded — full dry walk across all five lenses, no restructuring fold. §2 class bar MET (DC:40 — zero instruction-class, not a falling count).
**Conflicts:** none.
**§5 Conformance:** `plan_lint` run at shape-stability (walk 4) → **0 FAIL**. Two WARN, both benign: the no-Closing WARN cleared with this block; the (o2) Deposits-path WARN is the known bellows-in-tree relative-path false positive — the single Deposits entry (below) resolves under the bellows repo the daemon runs in, same class `diagnostic-455` cleared as worktree-runtime-proven. STEP header count asserted = 1 (`grep -c '^## STEP '`).
**Closing:** full walk 4 dry — 0 findings, no restructuring fold; §5 conformance clean (0 FAIL); closing-record re-read run (this block), dry; cycle CLOSED. Deposit exactly once (pending CEO go).

---
---

## STEP 1 — BELLOWS FORMAT-CENSUS AGENT

---

> **Identity:** You are characterizing the Cycle Log formats that a new validator (`cycle_check.py`) must parse. Read-only: change NO code; create NO files outside `knowledge/research/`. Deposit findings there.
> **Required reading (cited below):** the routed proposal is at `../proposal-cycle-automation-depositor-2026-08-18.md` — repo root, ONE level above the bellows tree you run in (or an absolute path resolved from it). Read §3 (the three asserts) and §5 (the depositor re-runs) before Q4/Q6; do not reconstruct them from memory.
>
> **Q1 — Census every Cycle Log block format across the Done/ corpus.** Over all `bellows/knowledge/decisions/Done/*.md` carrying a `## Drafting Cycle` block (Planner measured 37; confirm the count): enumerate the distinct forms of the per-lens result line. Report at minimum: (a) the newer class-split form `- Weak spots: w1 1 folded — instruction 1 / record 0 (…)` — Planner measured 5 files; confirm and list them — and **state the exact command/method used to produce each count so it is reproducible.** The count is load-bearing for assert #1's N/A boundary; a silent false-absence (e.g. a non-`-F` ugrep exiting 1 on a present line) would mis-scope the parser. Cross-check the count two ways; (b) the older form `- <Lens>: w1 2 folded (prose)` with no class split; (c) the compact/aggregate STATUS line form (e.g. `STATUS: CYCLE COMPLETE — walk N dry` and any aggregate class count). For each form give a real specimen (file + line) and note dash variance (`—` vs `-` vs none), lens-name variance (`Integration` vs `Integration-record`), and pass-token variance (`w1`, `c1`, `Closing`, `Confirming`). Do NOT assume uniformity — the parser fails silently on an unseen representation.
> **The Done/ corpus is COMPLETE blocks; cycle_check runs on a PARTIAL, live block (walk 1).** The Done/ census establishes the closed-cycle grammar, but the validator's actual input mid-cycle is a Cycle Log with walks `0..N` present and the Closing/STATUS lines ABSENT. Characterize that partial form explicitly — where a live draft carries its DC block during authoring (scratchpad/ or the deposit path), and confirm the per-lens/register lines are byte-identical to their Done/ form so the census transfers. Name any line that exists ONLY in a completed block (Closing, aggregate STATUS) so cycle_check does not treat its absence mid-cycle as a defect.
>
> **Q2 — Map each of cycle_check's three asserts to its input provider.** For each assert in proposal §3, state exactly which existing script (or gap) provides its data: **Assert #1 (internal arithmetic — per-lens `instruction + record` = stated total; STATUS aggregate = sum of per-lens):** confirm `cycle_yields.py` does NOT parse the instruction/record axis (Planner finding) and specify the NEW regex needed and the N/A rule when the split is absent. **Assert #2 (evidence exists — walk-register section + per-phase commit per walk):** does `walk_register_lint.py` already assert register-row-per-walk? Does anything assert a per-phase commit per walk? Name the gap. **Assert #3 (fold actually happened — fold_check baseline advanced for that walk):** confirm `fold_check.py`'s baseline mechanism and how cycle_check would invoke/read it per walk. Produce a table: `| Assert | Data needed | Provided by | Gap |`.
>
> **Q3 — The class-split N/A boundary and the STATUS aggregate form.** Given only 5/37 blocks carry the class split, specify precisely when assert #1 runs vs returns N/A. Determine whether a block can be identified as "current-format" (should carry the split → WARN if missing) vs "legacy" (no split expected → N/A silently) — e.g. by date, by plan id range, or by presence of the STATUS aggregate line. Recommend the discriminator. Also census the STATUS aggregate class-count form: does one exist consistently, and can assert #1's cross-check (STATUS = sum of per-lens) actually run, or is it N/A on most blocks too? **Enumerate the degenerate inputs cycle_check must survive without a false verdict:** a zero-walk block (walk 0 only, no lens lines yet), a partial block mid-walk, a block with MULTIPLE `## Drafting Cycle` headings (cycle_yields flags `MULTIPLE_BLOCKS` — state the intended behavior), and a block whose lens lines are entirely unparseable. State the correct verdict for each.
>
> **Q4 — Confirm the plan_lint check (f) defect the executable supersedes.** Proposal §3 states `plan_lint` check (f) reads the LAST lens result line rather than the class split, so its false-clean rate RISES as a cycle converges (ACID/Integration go dry first while Weak-spots keeps folding). Locate check (f) in `plan_lint.py`, quote the lines, confirm the mechanism, and state exactly how `cycle_check` reading the aggregate split supersedes it. Recommend whether check (f) should eventually be REMOVED from plan_lint or left as a redundant weaker check — but **do NOT authorize removal from this diagnostic.** Removing a live lint check is a subtractive change that earns its own diff review in the downstream executable, and only AFTER cycle_check is demonstrably running and catching the cases check (f) misses; a premature removal drops coverage between the two. Frame this as "supersede, then retire behind evidence," not "delete now."
>
> **Q5 — Gap Assessment (Rule 27 — mandatory table, exact columns).** `| Gap | Current State | Proposed State | Change Required |`. Cover: (a) the missing class-split regex; (b) the per-phase-commit-per-walk assertion gap; (c) fold_check per-walk invocation; (d) the check-(f) supersession; (e) the N/A/WARN discriminator; (f) the decision function that aggregates the 3 asserts + yield + plateau into one verdict (Q6); (g) fail-closed behavior on an unparseable/multiple-block input; (h) the partial live-block form the validator actually runs on (Q1); (i) the degenerate-input verdicts (Q3). **The table is the executable's complete build contract — it implements from this WITHOUT re-verification, so every load-bearing behavior Q1–Q6 raise must appear as a row.** Make each row build-ready.
>
> **Q6 — cycle_check's output contract (DESIGN only, no code).** Specify the three-verdict contract per the routed CEO answers: emits exactly one of `CONTINUE` · `BAR_MET` · `ESCALATE:<reason>`. **Plateau threshold = 3** (3 consecutive walks at a flat instruction-class count with no new finding class → `ESCALATE:plateau`). **No cost/token escalation** (dropped per routing).
> **cycle_check is a STATELESS validator; the pause/resume lives in the caller.** Like `fold_check`/`plan_lint` it reads inputs and emits a verdict in one shot — it does not itself hold a paused cycle. "ESCALATE is a pause, not a stop" is a CONTRACT ON THE CALLER: emitting `ESCALATE:<reason>` halts advance until a one-word answer resumes it. Design the reason vocabulary as a small closed resumable set (direction-class finding · new-CEO-decision · yield-rising · restructuring-fold · plateau · battery-failure). Because it is stateless, the plateau counter (3 flat walks) is DERIVED from the Cycle Log's own walk history each run, not held in memory — say how.
> **Invocation context + a git precondition.** Name who runs it: the Planner runs cycle_check after each walk in-session (replacing the CEO's "walk N" gate), and the in-bellows depositor RE-RUNS it at deposit rather than trusting the written verdict (proposal §5). The contract must serve both callers. **State the precondition asserts #2/#3 impose:** both read git (a per-phase commit exists for the walk; fold_check's baseline advanced), so cycle_check must run AFTER the walk is committed — define its verdict when run on an un-committed draft (e.g. `ESCALATE:uncommitted-walk`, never `BAR_MET`), and confirm the live drafting workflow does commit per walk (the git log shows per-walk `drafting(...)` commits).
> **The decision function (assert aggregation).** Specify precisely how the three asserts + the walk's yield + the plateau counter COMBINE into one verdict — this is the load-bearing logic. E.g.: any assert FAIL → `ESCALATE`; a dry walk with all asserts PASS/N/A and no restructuring fold → `BAR_MET`; otherwise `CONTINUE`; with plateau/yield-rising/restructuring-fold overriding to `ESCALATE`. State how an assert returning **N/A** (class split absent — the 32/37 case) participates without blocking `BAR_MET`.
> **Fail closed, never false-pass.** An input cycle_check CANNOT parse (unparseable lens lines, missing register, multiple DC blocks) must NEVER resolve to `BAR_MET` — it emits `ESCALATE:unparseable` (or a FAIL exit), mirroring cycle_yields' `UNPARSEABLE`/`MULTIPLE_BLOCKS` states. A silent-clean on an unrecognized representation is the exact failure this validator exists to prevent.
> State the exit-code convention and the stdout format both callers will read. Do NOT write cycle_check.py — only its contract.
>
> **Constraints:** Read-only; no code edits; findings file only. If a format variant is undeterminable or a script's behavior is ambiguous, report it as such — do not paper over. Every claimed specimen must cite file + line so the executable author can re-read it (Rule: read the cited rule, don't recall it).
>
> **Deposit:** `knowledge/research/cycle-check-format-census-2026-08-19.md` — Q1–Q6 with the format census, the assert-input map table, and the Rule 27 Gap Assessment table. End with an Output Receipt (Status). Standard prompt-feedback protocol.
>
> **Deposits:**
> - `knowledge/research/cycle-check-format-census-2026-08-19.md`
