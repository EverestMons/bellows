# bellows — diagnostic: 'WHAT WORKS' DEFINED BEFORE BUILT — three candidate definitions of a load-bearing function (import-verified inbound; survived; role-weighted) scored against a reference set built from the shop's own records, hubs kept, so anvil's glossary half is built on a definition that finds the exemplars or is parked on the number that says it cannot (thread 263, the CEO's ruling of 2026-09-10 on 255)

**Date:** 2026-09-11 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic; writes one research doc, one TSV and a dev-log) | **Execution:** Step 1 (DIAGNOSTIC) | **pause_for_verdict:** after_step_1

**auto_close:** false

**Post-close:** no restart, no doctrine. The doc's three scores are the input to the CEO's next act on thread 263 (build the glossary half on the definition that scores, or park it); the entry shapes in Q6 are facts for that judgement. This plan recommends nothing.

**Depends on:** the CEO's ruling of 2026-09-10 on thread 255 (after #100057): the dependency-graph half PARKS, the clone half STAYS (thread 259), the 'what works' half needs a definition BEFORE any build — "no build until one definition scores", hubs kept. Clone origin by kind and layout: `Done/diagnostic-100057.md` (the anvil re-price, 2026-09-09): one DIAGNOSTIC step, numbered questions, a scratch anvil DB under `mktemp -d` built by the four stage functions, doc + TSV as Planner-commit deposits, no recommendation. Its Q5 is this plan's walk 0: the stable/bound/reused definition found 2 of 14 reference names because its volatility filter excluded the hubs. This plan reuses #100057's build instrument (read) and deposits nothing under anvil.

**Tier computed (§1):** **T1** — T-1 fires (a read across bellows' source and history, anvil's archetype and scratch build, governance's glossary and lessons); T-6 does not (no doctrine edit); T-2 does not (a scratch SQLite under a temp dir is not real data; no source, hook or tool changes).

## CEO Context

Thread 263 asks for the definition before the build, tested three ways against "a proper exemplar set". Walk 0 measured how thin the shop's exemplar sources are: bellows `CLAUDE.md` names three callables and no `file.py::name` at all; the 73 glossary entries tagged `[project: bellows]` name eight, two of them stdlib; the daemon archetype names functions only through 45 regex rules that map a name to one of 13 roles; LESSONS.md names 46 distinct callables, the most-mentioned four times. The record names rules and lanes, not functions — that is a fact about the shop's records, and it bounds what any precision/recall over this set can say. The three candidates each fix one thing #100057 found: (a) verifies every call edge by the caller's imports, because the graph's resolver takes the first project-wide chunk of a bare name; (b) scores survival from git rather than the graph, so a hub every plan touches is not excluded for being touched; (c) weights by the archetype's role, which the archetype names but does not weight. The doc scores all three against the same set, keeps the hubs, and shows the entry the top five would carry.

## Numbers discipline — measured 2026-09-11 by the Planner (bellows `cc58328`, governance `c16b6f2c`, anvil `a1910df`, daemon pid 70398)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the reference sources, counted | bellows `CLAUDE.md`: `file.py::name` tokens 0, backticked callables 3 (`depositor.evaluate(`, `bellows_root.resolve_projects_parent(`, `_parse_plan(`); `GLOSSARY.md` entries whose heading carries `[project: bellows`: 73, `::` tokens 0, callables 8 (`resolve_projects_parent`, `resolve_governance_root`, `is_runnable_plan`, `gates.check`, `bellows_root.resolve_projects_parent`, `bellows_root.resolve_governance_root`, and two stdlib — `os.path.basename`, `os.listdir`); `LESSONS.md`: 46 distinct callables, the top by mention — `init_db(` 4, `gates.check(` 3, `_assign_class(` 3, `datetime.now(` 3 (stdlib), `open(` 3 (builtin), `run_full_lessons_cycle(` 2 (forge, not bellows), `is_runnable_plan(` 2, `_retire_receipts(` 2; the daemon archetype `anvil/src/archetypes/daemon.py`: `NAME_RULES` 45 regex → role pairs (`^run_plan$` → plan_dispatcher, `^run_step$` → agent_lifecycle, `^_?(?:create\|teardown)_worktree$` → worktree_manager, `^notify_\w+` → notifier, `^(?:check_verdict\|post_verdict_request\|log_to_ledger)$` and `^_consume_verdicts$` → verdict_handler, `^_gate_` → gate_checker, …), `FILE_PATH_RULES` at `:75`, `DECORATOR_RULES` empty. The token rule: a backticked `name(` or `mod.name(`; a name JOINS the set only if it resolves to a def in the bellows checkout at HEAD (P6) — by bare name, or by module and name when dotted — and every unresolved token is listed with its reason (stdlib, builtin, another project, no def) | `grep -oE '\`[A-Za-z_][A-Za-z0-9_.]*\(' <file> \| sort \| uniq -c`; `awk` over the glossary headings; `grep -oE 're.compile\(r"[^"]+"\), "[a-z_]+"' daemon.py` |
| P2 | ⛔ the #100057 control set, and the ruling | `anvil-reprice-2026-09-09.md` Q5: "Reference set … 14 names total", eight from the archetype's name rules — `run_plan`, `run_step`, `_teardown_worktree`, `notify_*`, `check_verdict`, `_consume_verdicts`, `validate_at_claim`, `extract_decision_blocks`; the walk-0 definition found 2 of 14 (`runner.py::run_step`, `parser.py::parse`); excluded by its volatility filter: `bellows.py::run_plan`, `gates.py::check`, `lifecycle.py::init_lifecycle_db`, `depositor.py::_assign_class`; the CEO's ruling: hubs kept | the research doc Q5; thread 263's body |
| P3 | ⛔ the scratch DB (built 2026-09-09 by the walk-0 instrument over bellows `1365acc`, the session scratchpad's `anvil_pin/anvil-scratch.db`) | `code_chunks` 8029 rows (module rows included; #100057 P1: function 744, method 56, class 221, test_case 1994); `chunk_dependencies` 6890 — `call` 6571 (cross_file 4076, within_file 2495), `import` 319; `chunk_symbol_bindings` — `calls` 23744, `defines` 3037, `imports` 1350, `tests` 3188 of which 1207 carry a `target_chunk_id`; `code_chunks.functional_role` over function+method: utility 506, cli_script 107, plan_dispatcher 60, gate_checker 39, notifier 27, verdict_handler 14, agent_lifecycle 14, plan_validator 11, plan_parser 7, worktree_manager 5, config_loader 4, cache_manager 4, data_model 2; `functional_roles.scoring_weights` is EMPTY on all 13 rows; `ROLE_THRESHOLDS` (daemon.py:151) carry `complexity_threshold` / `coupling_hotspot_threshold` only — the archetype names roles, it does not weight them | `sqlite3 -readonly <db>`; Item 0 rebuilds the DB and pastes each count beside these |
| P4 | ⛔ the resolver's rule, quoted (#100057 P2) | `candidates = by_name.get(callee_name, [])` then `break  # Take first match` (`anvil/src/extractor.py`, the "Resolve call dependencies" block): a call edge is the FIRST project-wide chunk of that bare name, whatever the caller imported; #100057 Q3 measured 0–45% precision on the top-inbound chunks — the reason definition (a) verifies each edge against the caller's imports | `grep -nF 'Take first match' anvil/src/extractor.py` |
| P5 | ⛔ the history query's cost, and its form | `git log -L ':_gate_scope_check:gates.py' --format=%h` → 5 commits, 0.02 s; `':check:gates.py'` → 7 commits, 0.02 s; but `':_assign_class:depositor.py'` (a METHOD, `depositor.py:409–470` at HEAD) → `fatal: -L parameter '_assign_class' starting at line 1: no match` — no `.gitattributes`, no `diff.python.xfuncname`, so the heading regex is git's default; the RANGE form `-L 409,470:depositor.py --format='%h %ad' main` → 4 commits, 0.02 s, oldest `481a800` 2026-08-20, five `[-+]` def lines across its hunks (the birth's `+` and two signature changes); 588 defs (P6) at that cost ≈ 12 s | `time git log -L '<lineno>,<end_lineno>:<file>' --format=%h main` (the range from `ast`; the `:<name>:` form only to reproduce the `no match`) |
| P6 | ⛔ the production defs at HEAD | `git ls-files -- '*.py'` 170 files; production files 55 by the rule "not under `tests/`, `knowledge/`, `logs/`, `receipts/`, `.claude/`"; `FunctionDef` + `AsyncFunctionDef` nodes over them (`ast.walk`, methods included) 588 | the 12-line stdlib script, in the doc |
| P7 | the instrument reused, read-only | `anvil/scripts/reprice_bellows_build.py` at anvil `a1910df` (`--checkout`, `--tmp`; asserts the DB path is under a temp dir; enters `scan_project`, `extract_project`, `classify_project`, `score_project` — never `run_cycle`; its docstring names the writers it never calls: `run_cycle`, `run_lab`, `write_intent_audit`, `write_cycle_report`, `ingest_test_results`); the scoring script is NEW and lives in `$T` and the doc's appendix — nothing is written under anvil | `git -C anvil log -1 -- scripts/reprice_bellows_build.py`; the script's head |
| P8 | in-flight; class; interpreter | #100071 (the QA-scope census) in flight at drafting — this plan deposits after it closes; writes: a bellows dev-log and two governance research files → **app-feature** (auto-clears; the depositor's parse recorded at deposit); anvil has NO venv on the mini and bellows' venv lacks `datasketch` — Item 0 builds one under `mktemp -d` (`datasketch==2.0.0`; anvil's own suite is not run here, so no `pytest`), the #100057 shape; Python 3.9 stdlib plus that venv | `python3 status.py`; `_assign_class`; `ls anvil/.venv` |

## MUST-PRESERVE

- ⛔ **Read-only over bellows, anvil and governance.** No lifecycle row, no lane file, no run of the daemon, `run_plan`, a claim, or anvil's `run_cycle` / `run_lab` / `ingest_test_results` (P7); `lifecycle` never imported; the scratch DB and venv live under `mktemp -d`; the only files written are the three deposits; the bellows checkout analysed is the CANONICAL one at `main`, read through `git -C … show` and `git -C … log`, never this worktree.
- ⛔ **Hubs kept.** No candidate definition filters by volatility or by inbound count; the four hubs P2 names appear in every ranking with their rank stated.
- ⛔ **No recommendation.** The doc answers Q1–Q6 and ends with "What this does not establish"; which definition the glossary half is built on, or whether it is parked, is the CEO's act on thread 263.
- ⛔ **Every score carries its formula and its denominators**, the reference set is listed member by member with the source that named it, and every graph edge that counts was verified against the caller's import lines, never taken from the graph alone (P4).

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-what-works-definition-2026-09-11.md
**Walks:** walk 0 pinned (P1–P8 measured on bellows `cc58328`, governance `c16b6f2c`, anvil `a1910df`; clone-diff against `Done/diagnostic-100057.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py`; the tool run bare, its exit read; no lens or walk number in any `--desc`; `lens_order_check` after every walk.

- Weak spots:          w1 2 folded — instruction 2 / record 0; w2 1 folded — instruction 1 / record 0; w3 dry
- Destruction:         w1 1 folded — instruction 1 / record 0; w2 dry; w3 dry
- Vulnerabilities:     w1 2 folded — instruction 1 / record 1; w2 dry; w3 dry
- Integration-record:  w1 1 folded — instruction 1 / record 0; w2 dry; w3 dry
- ACID:                w1 2 folded — instruction 1 / record 1; w2 dry; w3 dry
**Closing:** WARM close after walk 3 — BAR MET (T1), thread 263's definition test before any build. Three walks: 6 → 1 → 0 (walk 1 folded the history trace to a line range after the name form failed on methods, and reconciled two cross-fold contradictions at ACID; walk 2 folded the survived formula's own consequence; walk 3 dry on every lens). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-` after #100071 closes; class app-feature auto-clears; the daemon claims and the verdict is issued under the CEO's 2026-09-11 overnight delegation (continue on all-pass with the (b) check, the verdict its own call). Close acts: the Planner commits the two governance deposits at the wrap; the build or the park is the CEO's act on thread 263.

## Cycle Manifest
tier: T1
target: knowledge/development/dev-log-what-works-definition-2026-09-11.md
class: app-feature
reads: CLAUDE.md, GLOSSARY.md, LESSONS.md, anvil/src/archetypes/daemon.py, anvil/src/extractor.py, anvil/scripts/reprice_bellows_build.py, governance/knowledge/research/anvil-reprice-2026-09-09.md, knowledge/decisions/Done/diagnostic-100057.md
writes: knowledge/development/dev-log-what-works-definition-2026-09-11.md, governance/knowledge/research/what-works-definition-2026-09-11.md, governance/knowledge/research/what-works-definition-2026-09-11.tsv
open_forks: 1. the glossary half of anvil built on the definition that scores (Q5's best), its entry shape from Q6, as anvil's own T1 with `src/config.py` de-hardcoded first; 2. an import-aware resolver in `extractor.py` (P4) if (a)'s verified/total ratio is the number that moves the score; 3. park the glossary half to a pointer if no definition reaches the reference set — the CEO's act
walks: 3
yields: 6, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:26
coherence: 3/3 body walks named in the register (13 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.diagnostic-bellows-what-works-definition.md.foldcheck.json

---

## STEP 1 — DIAGNOSTIC: six questions — one reference set, three definitions scored on it, the entry the top five would carry

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **Read-only:** never run the daemon, `run_plan`, a claim, anvil's `run_cycle`, `run_lab`, `write_intent_audit`, `write_cycle_report` or `ingest_test_results` (P7); never import `lifecycle`; the anvil checkout is `/Users/marklehn/Developer/anvil` (read only); the bellows checkout under analysis is the CANONICAL checkout `/Users/marklehn/Developer/bellows` at `main` (files via `git -C … show main:<path>`, history via `git -C … log`) — never this worktree, whose HEAD is the plan branch and whose tree carries the plan's own files; the governance files are read at `/Users/marklehn/Developer/eluvian-governance`; `T=$(mktemp -d /tmp/what-works.XXXXXX)` holds the venv, the scratch DB, the script and scratch output; nothing else is written outside the three deposits.
>
> **Scope:**
> - `knowledge/development/dev-log-what-works-definition-2026-09-11.md`
>
> **Item 0 — the interpreter and the scratch DB.** `python3 -m venv "$T/venv" && "$T/venv/bin/pip" -q install datasketch==2.0.0` — record `$T` in the dev-log; then `"$T/venv/bin/python" /Users/marklehn/Developer/anvil/scripts/reprice_bellows_build.py --checkout /Users/marklehn/Developer/bellows --tmp "$T"` (P7: the four stage functions, the temp-dir assert) → `$T/anvil-scratch.db`; paste the build's stage summaries and, beside each P3 count, the rebuilt value (chunks by type, dependencies by type and scope, bindings by type with and without target, roles over function+method, and `SELECT count(*) FROM functional_roles WHERE scoring_weights IS NULL OR scoring_weights=''`). A count that moved because bellows moved since `1365acc` is reported as both figures; a count that moved for any other reason is a mechanism mismatch — HALT.
> **Item 1 — Q1: the reference set.** Write `$T/define.py` — verbatim in the doc's appendix. Re-derive P1 with its token rule over the four sources, resolve every token to `(file, name)` at `main` by AST over P6's production files (bare name → every def of that name, each listed; dotted `mod.name` → the def in `mod.py` or the class's method), and list every unresolved token with its reason. The set R = the resolved members, each with the source(s) that named it and the mention count; then R+ = R ∪ P2's control set (its `notify_*` expanded by the archetype's regex over P6's defs), with the overlap counted. State |R|, |R+|, and the four hubs' membership. If |R+| < 10 the doc says precision and recall at this size are not computable as rates and reports per-member ranks only — that is an answer, not a failure.
> **Item 2 — Q2: definition (a), import-verified inbound.** The edges are the PARKED graph's (the ruling on 255 parked it as a query surface at 0–45% precision); this definition uses them only after verification, and the doc says so in Q2's first line, with verified/total as the number that ruling measured. From `chunk_dependencies` (`dependency_type='call'`, `scope='cross_file'`) joined to `code_chunks` on both ends, keep edges whose target is a production function or method (P6's path rule); an edge is VERIFIED iff the caller's file, parsed by AST at `main`, carries `import <mod>`, `import <mod> as …`, or `from <mod> import <name>` / `from <mod> import *` where `<mod>` is the target file's module (`gates` for `gates.py`, `tools.deposit_receipt` for `tools/deposit_receipt.py`; a relative `from .x import` resolved against the caller's package) and, for the `from … import <name>` form, `<name>` is the target's name or its class; the rule is stated in the doc beside P4's resolver line. A caller that loads the target by PATH (`importlib.util.spec_from_file_location`, `runpy`) carries no import line — its edges stay unverified and are counted apart as `path-loaded` (the caller's file contains one of those two names), so the doc can say how much of the unverified mass is that class and how much is P4's name collision. `score_a` = verified inbound, hubs kept (no filter). Report verified/total pooled and for the top-12 targets by total; rank every P6 def by `score_a`, ties broken by `file::name` ascending (the one tie rule for all three rankings, stated once); precision@K and recall@K for K = |R+| and K = 20, and the rank of every R+ member (`—` for zero verified inbound).
> **Item 3 — Q3: definition (b), survived.** For every P6 def: `git -C /Users/marklehn/Developer/bellows log -L '<lineno>,<end_lineno>:<file>' --format='%H %ad' --date=short main` with the range from the def's AST node at `main` (`lineno`, `end_lineno`) — the `:<name>:<file>` form is NOT used: bellows has no `.gitattributes` Python diff driver, so git's default heading regex sees unindented `def` lines only and every METHOD fails with `no match` (P5); `age_days` = today minus the oldest commit's date in the trace (the birth); `stable_commits` = commits in the trace OTHER than the birth whose hunks carry no `-` or `+` line matching `^[-+]\s*(async )?def <name>\(` (the signature line unchanged — the birth always carries a `+` def line and is excluded by name); `tests_bound` = `chunk_symbol_bindings` rows with `binding_type='tests'` and `target_chunk_id` = the def's chunk (0 when the chunk is absent); `score_b` = `age_days × (1 + tests_bound) × stable_commits` — the thread's three factors as a product, the formula printed. The formula's own consequence is reported, not hidden: a def whose trace is its birth alone has `stable_commits = 0` and scores 0 however old and tested — the doc counts those defs and lists the R+ members among them; beside (b) the doc reports the variant (b′) `age_days × (1 + tests_bound) × (1 + stable_commits)` as a labelled row, so the CEO sees what the thread's product does to the untouched, not a fourth candidate. A trace git refuses (a `fatal:` on the range) scores `age_days = 0, stable_commits = 0` and is listed with git's line, never a halt. Wall-clock of the 588 traces beside P5. Rank, precision@K, recall@K and per-member ranks as Item 2.
> **Item 4 — Q4: definition (c), role-weighted.** Role from `code_chunks.functional_role` (P3's distribution, re-derived at Item 0). The archetype carries no numeric weights (P3), so the table is the PLANNER'S and the doc says so in its first line: 2.0 for the seven daemon-core roles (plan_dispatcher, gate_checker, verdict_handler, agent_lifecycle, worktree_manager, plan_validator, plan_parser), 1.5 for the four infrastructure roles (notifier, config_loader, data_model, cache_manager), 1.0 for utility, cli_script and unclassified; `score_c` = `score_a × weight`. Rank, precision@K, recall@K and per-member ranks as Item 2; then the sensitivity in one line — with every weight 1.0 the ranking is (a)'s, so state how many of the top-20 positions the table moved and which R+ members entered or left the top 20 because of it.
> **Item 5 — Q5: the three side by side.** One table: definition, P@|R+|, R@|R+|, P@20, R@20, the R+ members in its top 20 by name, the rank of each of P2's four hubs; (b′) as a labelled variant row under (b). The best = highest R@|R+| among (a), (b), (c), ties by P@|R+|, a remaining tie reported as a tie; if (b′) would rank above (b) the doc says so in one line and leaves the choice to the CEO. Beside it, #100057's walk-0 definition (P2: 2 of 14) as the row the three are measured against.
> **Item 6 — Q6: the entry the top five would carry.** For the top five under the best definition (all five of a tie's leaders if fewer than five separate): `file::name`; role; signature (the `def` line from `ast.get_source_segment`, or `ast.unparse(node.args)`); verified callers — count and the first three `file::name`; the docstring's first line, or `no docstring`; tests bound — count and the first two test node names from the binding sources. Facts only, one row each; whether such an entry is 'what works, applied elsewhere' is the CEO's judgement.
> **Item 7 — write the research doc** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-definition-2026-09-11.md` answering Q1–Q6 in order, each section opening with the script's raw output line it rests on, the three formulas printed once each, `define.py` as a code block in an appendix, and the TSV `what-works-definition-2026-09-11.tsv` — one row per P6 def, columns `file, name, role, inbound_total, inbound_verified, inbound_path_loaded, age_days, tests_bound, stable_commits, score_a, score_b, score_c, rank_a, rank_b, rank_c, in_reference, named_by`; end with "What this does not establish" (at least: a reference set of this size scores coarsely and the sources name rules and lanes, not functions, by design; the role weights are the Planner's, not the archetype's; `-L` does not follow a rename, so `age_days` is bounded by the file's history under its current path; import verification passes a same-named function in an imported module; a scratch DB built once over one repo in one language; the control set is #100057's, built by the same Planner; the BEST of three is a ranking, not a pass — the recall a build would need is the CEO's threshold, not set here).
> **Item 8 — dev-log** with the headings declared below (the `dev_log_declared_text` gate reads them at the pause); `## Pins re-derived (P2, P3)` opens with P2's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `the CEO's ruling: hubs kept`; paste through those words, then stop. **Commit** the dev-log alone (message tagged with the plan id and `thread 263`), gated on the FULL suite (`.venv/bin/python -m pytest -q` from the worktree root — nothing under bellows changed but the rule is the rule), path-scoped to the Scope list; the two governance files are Planner-commit deposits. Last act, after the commit: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P2, P3)`; `## The reference set (Q1)`; `## Q1–Q6 in one table`; `## What the doc does not establish`
> **Verbatim:** `## Pins re-derived (P2, P3)` ← P2
>
> **Deposits:**
> - `knowledge/development/dev-log-what-works-definition-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-definition-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-definition-2026-09-11.tsv`
>
> **Post-conditions:** the doc answers six questions, every score with its formula and denominators, the reference set listed member by member with its source, the hubs' ranks stated under every definition, and ends without a recommendation; the TSV has one row per P6 def; the dev-log's four headings present as full lines with P2's cell whole; one bellows commit carrying the dev-log only; nothing written under anvil, under any lane, or under `$T` that survives the step (`git -C /Users/marklehn/Developer/anvil status --porcelain` empty; `git -C /Users/marklehn/Developer/eluvian-governance status --porcelain` shows only the two research files; `git status --porcelain` in the worktree shows nothing after the commit).
