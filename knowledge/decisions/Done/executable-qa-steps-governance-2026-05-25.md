# Governance — `qa_steps` Header Field Documentation in PLANNER_TEMPLATE.md

**Date:** 2026-05-25 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** none (documentation-only) | **Execution:** Step 1 (DOC) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always

## Context

The code half of the `qa_steps` work shipped earlier today (`executable-qa-steps-header-field-code-2026-05-25`, in Done/). `bellows/gates.py::_gate_is_qa_step` now reads `qa_steps` as the authoritative source with keyword fallback for backward compatibility. This plan ships the governance half: documenting the field in PLANNER_TEMPLATE.md so future Planner-authored plans declare it.

Three insertion points per SA fix-shape at `bellows/knowledge/research/qa-step-detection-fix-shape-2026-05-22.md` Section 4: (1) header example in Plan File Structure section, (2) new definitional paragraph after the "Execution map" paragraph, (3) Gate 6 table description in "The Eight Gates" section. Plus a version bump (4.49 → 4.50) and a new Lessons Learned row.

**Scope:** governance-root file only. No bellows code changes. The code change shipped earlier today already handles plans both with and without the field.

**Sources:** SA fix-shape Section 4 has exact proposed wording for all three insertion points. Use that text verbatim where specified.

## How to Run This Plan

```
Read the plan at bellows/knowledge/decisions/executable-qa-steps-governance-2026-05-25.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

---
---

## STEP 1 — Bellows Documentation Analyst

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-qa-steps-governance-2026-05-25.md", "bellows/knowledge/decisions/in-progress-executable-qa-steps-governance-2026-05-25.md")`. You are the Bellows Documentation Analyst. Read your specialist file and domain glossary first. **Task:** make five edits to `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md` to document the `qa_steps` header field. **Read first** the SA fix-shape Section 4 at `bellows/knowledge/research/qa-step-detection-fix-shape-2026-05-22.md` for the exact proposed wording. **All edits use `Filesystem:edit_file` with anchor-based `oldText`/`newText` pairs.** Line numbers in the SA spec are stale (2026-05-22 state); locate each anchor by its unique surrounding context, not by line number. **Edit 1 — Version bump.** Locate the header lines `**Version:** 4.49` and `**Last Updated:** 2026-05-21 (v4.49)` near the top of the file. Replace with `**Version:** 4.50` and `**Last Updated:** 2026-05-25 (v4.50)`. **Edit 2 — Header example in Plan File Structure section.** Locate the example header line that starts with `**Date:** YYYY-MM-DD | **Tier:** [tier]` and includes `**Dispatch Mode:**`, `**Test Scope:**`, `**Execution:**`, and `**pause_for_verdict:**` separated by ` | `. Insert ` | **qa_steps:** [comma-separated step numbers]` between `**Execution:**` (and its full value with arrow notation) and `**pause_for_verdict:** after_step_1` so the final line reads: `**Date:** YYYY-MM-DD | **Tier:** [tier] | **Dispatch Mode:** [bellows | manual_bootstrap] | **Test Scope:** [targeted | full-suite | both] | **Execution:** Step 1 (AGENT) → Step 2 (AGENT) or Step 2A (AGENT) ∥ Step 2B (AGENT) → Step 3 (AGENT) | **qa_steps:** [comma-separated step numbers] | **pause_for_verdict:** after_step_1`. Anchor `oldText` on the full existing line including newlines on either side so the match is unique. **Edit 3 — Definitional paragraph.** Locate the "Execution map" paragraph (it explains the `→` for sequential and `∥` for parallel in the header line) and the immediately-following paragraph beginning "Every agent prompt must include the domain glossary read". Insert a new paragraph BETWEEN them with this exact wording: `**\`qa_steps\` header field.** Every executable plan with one or more QA steps MUST declare a \`qa_steps\` field in the plan header listing the step numbers that are QA-gated, as a comma-separated list of integers (e.g., \`qa_steps: 2\` or \`qa_steps: 2,4\`). Bellows's \`_gate_is_qa_step\` reads this field to determine which steps trigger Rule 20 self-check verification, Rule 22 QA-specific checks (c)+(d), and \`qa_checkpoint\` auto-pause. Without the field, Bellows falls back to keyword detection on the \`## STEP N\` header line — checking whether the header contains the substring "qa" (case-insensitive). This fallback misclassifies QA steps whose specialist role name does not contain "qa" (e.g., "Invoice Security & Testing Analyst", "Bellows Security & Testing") or whose header uses a descriptive label instead of a role name (e.g., "Canary verification" with \`**Agent:** QA\` in the body). The 2026-05-22 population audit found 14 leaked QA steps across 139 total (10.1% overall; concentrated in invoice-pulse) — every instance of "Invoice Security & Testing Analyst" leaked, every use of a descriptive header label leaked. The \`qa_steps\` field is the authoritative source; the keyword fallback is a backward-compatibility path for legacy plans. Diagnostic plans and plans with no QA steps omit the field entirely — omission means "no QA steps" when no keyword match is found.` Anchor `oldText` to include the closing sentence of the "Execution map" paragraph and the opening sentence of the "domain glossary read" paragraph so the insertion point is unambiguous. **Edit 4 — Gate 6 row in "The Eight Gates" table.** Locate the table row whose first cell is `| 6 |` and second cell is `is_qa_step`. The current description reads (approximately): `Heuristic detection based on the step header containing "QA" (case-insensitive). When true, triggers a qa_checkpoint pause reason even if all blocking gates pass.` Replace the description cell with: `QA step detection: reads the plan header's qa_steps field (authoritative, comma-separated step numbers) or falls back to keyword detection on the ## STEP N header line (case-insensitive "qa" substring). When true, triggers qa_checkpoint pause reason, Rule 20 self-check verification, and Rule 22 QA-specific checks.` Anchor `oldText` on the full row including the leading `| 6 |` and the trailing `|` plus the surrounding row separators if needed for uniqueness. **Edit 5 — Lessons Learned row.** Locate the Lessons Learned table at the bottom of the file. Append a new row at the end (after the most recent existing row, which should be a 2026-05-21 entry). The new row: `| 2026-05-25 | qa_steps header field documentation. The code half (gates.py) shipped earlier same day via executable-qa-steps-header-field-code-2026-05-25 with keyword fallback preserving backward compatibility. This governance edit documents the field in PLANNER_TEMPLATE.md so future Planner-authored plans declare it. Three insertion points: header example, new definitional paragraph after Execution map, Gate 6 table row. Closes the post-Rule-25 blind spot where vacuous \`is_qa_step = False\` produced PASS results that the Planner's substance-only routing under Rule 25 treated as clean, silently suppressing manual verification of misclassified QA steps. The 2026-05-22 population audit measured 10.1% leak rate (14/139 QA steps); 6 patterns identified, all closed by the new field. **Pattern:** when shipping mechanization that creates a new authoritative data source, the same plan or immediate-next plan ships the governance edit that obligates the Planner to write the new field. The code change alone is half-shipped; the Planner won't author the field without the template prompt. |`. Anchor `oldText` on the full text of the most recent existing Lessons row (the 2026-05-21 row about "Once Bellows mechanizes a Rule 22 sub-check, the Planner must stop re-running it" or the row that immediately follows it) plus a trailing newline so the new row appends cleanly after it. **Pre-edit verification:** before issuing any `edit_file` call, read the relevant section of `PLANNER_TEMPLATE.md` to confirm the anchor text matches byte-for-byte. If an anchor doesn't match (file has drifted since the SA spec), STOP and report — do NOT improvise. **Dry-run first:** for each `edit_file` call, use `dryRun: true` first to confirm the diff looks right, then issue the real edit. **Deposit a dev log** at `bellows/knowledge/development/qa-steps-governance-2026-05-25.md` with: (a) the five edit anchors used (verbatim oldText snippets, with newlines and whitespace shown), (b) confirmation each edit landed by reading back the file at each insertion point, (c) any deviations from the SA spec wording and why, (d) Output Receipt with Status: Complete. **Commit** at the governance root (`/Users/marklehn/Developer/GitHub/`) with message `docs: PLANNER_TEMPLATE v4.50 — qa_steps header field documentation`. The bellows dev log file commits separately to bellows. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/qa-steps-governance-2026-05-25.md`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — Bellows QA

---

> Before starting, read `bellows/knowledge/development/qa-steps-governance-2026-05-25.md` and check the Output Receipt status field. If status is not Complete, stop and report the blocker before proceeding. You are the Bellows QA agent. Read your specialist file and domain glossary first. **FIRST — Deliverable Verification.** Build a verification table with these checks, each row including the absolute path checked, line numbers (current after edits), and a verbatim quote excerpt: (1) Version header in PLANNER_TEMPLATE.md is `**Version:** 4.50` and `**Last Updated:** 2026-05-25 (v4.50)`; (2) Header example line in Plan File Structure contains `**qa_steps:** [comma-separated step numbers]` between `**Execution:** ...` and `**pause_for_verdict:** after_step_1`; (3) New definitional paragraph exists, begins with `**\`qa_steps\` header field.**`, contains the phrase "10.1% overall", and is positioned between the "Execution map" paragraph and the "Every agent prompt must include the domain glossary read" paragraph; (4) Gate 6 row in "The Eight Gates" table description starts with `QA step detection: reads the plan header's qa_steps field`; (5) Lessons Learned table has a new 2026-05-25 row whose content begins with `qa_steps header field documentation.` Mark each row ✅ or ❌ with evidence. If any ❌, stop and report. **Then — structural compliance check.** Run `git --no-pager show --stat HEAD` at `/Users/marklehn/Developer/GitHub/` and confirm the commit touched exactly one file: `PLANNER_TEMPLATE.md`. Capture to `bellows/knowledge/qa/evidence/executable-qa-steps-governance-2026-05-25/governance_commit.txt`. Run `git --no-pager diff HEAD~1 PLANNER_TEMPLATE.md` and confirm the diff matches the five intended edits: version bump (1 line), header example (1 line), definitional paragraph (~12 lines added), Gate 6 row (1 line replaced), Lessons row (1 line added). No other sections modified. Capture to `evidence/governance_diff.txt`. **Then — bellows-side compliance.** Run `git --no-pager show --stat HEAD` at `/Users/marklehn/Developer/GitHub/bellows/` and confirm a separate commit touched only `knowledge/development/qa-steps-governance-2026-05-25.md`. Capture to `evidence/bellows_dev_log_commit.txt`. **Then — Rule 20 self-check.** Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `executable-qa-steps-governance-2026-05-25`; `qa_report_path`: `<absolute-path-to-knowledge/qa/executable-qa-steps-governance-2026-05-25.md>`; `evidence_dir`: `<absolute-path-to-knowledge/qa/evidence/executable-qa-steps-governance-2026-05-25/>`; `required_evidence_files`: list every evidence file you wrote during this step. Include the literal stdout output of the block in the QA report under an `**Output:**` heading. If FAILED, halt and report to CEO. **Then — QA report deposit.** Write `knowledge/qa/executable-qa-steps-governance-2026-05-25.md` with the verification table, structural compliance results, Rule 20 self-check stdout, and Output Receipt with Status: Complete. **DEPOSITS RULE:** declare ONLY the QA report file and the evidence directory in the **Deposits** block below. Do NOT list PROJECT_STATUS.md as a deposit even though you will update it — listing multiple .md files triggers the BACKLOG-documented hash-dependent `md_paths[0]` false-positive in `_gate_rule_22_verification`. PROJECT_STATUS.md is updated as a side effect, not a primary deposit. **Final:** Update `bellows/PROJECT_STATUS.md` — prepend a new Completed entry under today's date (2026-05-25) summarizing: PLANNER_TEMPLATE.md bumped to v4.50 with `qa_steps` header field documented in three places (header example, definitional paragraph, Gate 6 row); pairs with this morning's code half to close the 2026-05-22 BACKLOG entry; no daemon restart required (governance-only). Use `Filesystem:edit_file` with verbatim anchor on the existing top entry. **STOP.** Do NOT move this plan to Done/. Standard prompt feedback protocol. **Deposits:**
> - `bellows/knowledge/qa/executable-qa-steps-governance-2026-05-25.md`
> - `bellows/knowledge/qa/evidence/executable-qa-steps-governance-2026-05-25/`
>
> **STOP. Do NOT move the plan to Done. Wait for CEO confirmation. Per Rule 22, the Planner performs the move-to-Done after verification.**
