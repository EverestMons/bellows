# Bellows — Daemon-Owned Ledgers ACTIVATION Slice 1: Feedback→DB Go-Live (Live Canary)
**Date:** 2026-06-14 | **Tier:** Large | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 42 — the ACTIVATION of the feedback ledger (CEO chose feedback-first as the live canary, 2026-06-14). Phases 1–3 (plans 43/44/45) built the daemon-side mechanism DORMANT; this plan FLIPS feedback live shop-wide. The activation principle: once agents stop writing `agent-prompt-feedback.md` (governance change below), the daemon's existing coexistence check (`skip if file in files_changed`) self-passes and the daemon takes over — so activation = governance flip + wire generation as the live producer + freeze the existing files + idempotency + drop the gate allowlist entry. Feedback disposition is (b) DB-stored + generated (design Section 3 File 2): the `.md` becomes a generated artifact from the `prompt_feedback` table, fixing both the merge-conflict class (FORWARD rows 4/5/13 — the feedback share) AND the unbounded growth (bellows 316K, invoice-pulse 1.1M, forge 168K, study 76K, anvil/governance 20K each). The design doc `knowledge/research/daemon-owned-ledgers-design-2026-06-13.md` is AUTHORITATIVE (Sections 3 File 2, 4, 5 governance sites, 6 migration). Re-verify Verification Blocks at edit time; halt-and-report on divergence. Recon (2026-06-14): `record_prompt_feedback(plan_id, step_number, agent, project, entry_text, ...)` and `generate_feedback_md(project, db_path)` are project-agnostic (lifecycle.py:522/541); NO `ledger_writes` idempotency table exists yet; the `_apply_ledger_updates` feedback handler records to DB but does NOT yet generate the `.md`. SCOPE NOTE — this slice activates feedback ONLY; PROJECT_STATUS and FORWARD stay dormant (their handlers keep skipping because agents still write those files) and activate in later slices. CROSS-REPO: edits span the bellows worktree (code + bellows freeze), the governance root (PLANNER_TEMPLATE), and 5 other repos (forge, anvil, invoice-pulse, governance, study — feedback-file freeze) via absolute paths (Bash `python3 -c`/`with open()`/`os.rename`; Edit tool is worktree-scoped). DAEMON RESTART REQUIRED after close — the live canary is the FIRST bellows plan dispatched after restart (its feedback must land in `prompt_feedback` and regenerate the `.md`; nothing hand-appends the file).

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, then the design doc Sections 3 File 2, 4, 5, 6, then the EXISTING Phase 1 code (`_apply_ledger_updates` feedback branch in bellows.py; `record_prompt_feedback`/`generate_feedback_md` in lifecycle.py; `### Ledger Updates`→feedback parse in parser.py). **Scope (exactly):** bellows code `bellows.py`, `lifecycle.py`, `gates.py`, `tests/`; governance root `/Users/marklehn/Developer/GitHub/PLANNER_TEMPLATE.md`; feedback-file freeze in the 6 projects listed.
> - **(1) Idempotency:** add a `ledger_writes` table to lifecycle.db (per design: `step_id, ledger_file, content_hash, applied_at`) via the existing migration mechanism (idempotent). Before applying a feedback write, skip if a row with the same `(step_id, ledger_file, content_hash)` exists — so a teardown retry (reattempt-on-continue-resume precedent) never double-writes.
> - **(2) Wire generation as live producer:** extend the `_apply_ledger_updates` feedback branch so that, after `record_prompt_feedback(...)` (when NOT skipped by coexistence and after the idempotency check), it calls `generate_feedback_md(project)` and writes the result to `<project_path>/knowledge/research/agent-prompt-feedback.md` on main, then `git add` + commit. Log-and-continue; never crash teardown.
> - **(3) Gate allowlist:** remove `agent-prompt-feedback.md` from `gates.py` `SCOPE_ALLOWLIST` (it must no longer appear in agents' `files_changed`). Leave `PROJECT_STATUS.md` in the allowlist (its activation is a later slice).
> - **(4) Freeze existing feedback files (6 projects):** for each of `bellows, forge, anvil, invoice-pulse, governance, study` that has `knowledge/research/agent-prompt-feedback.md`, rename it to `knowledge/research/agent-prompt-feedback-ARCHIVE.md` (preserve full history; prepend a one-line frozen-header noting it is the pre-activation archive, the live log is now DB-generated). Then write a fresh generated `agent-prompt-feedback.md` for each via `generate_feedback_md(project)` (initially just the generated header + any existing DB rows for that project — likely empty/near-empty, with a pointer to the ARCHIVE). bellows is in your worktree; the other 5 are edited via absolute path.
> - **(5) Governance flip (PLANNER_TEMPLATE.md, per design Section 5):** feedback protocol — diagnostic (~L265–274), fix (~L308–317), compressed (~L341): replace "append a dated entry to agent-prompt-feedback.md" with "emit prompt feedback in the Output Receipt `### Ledger Updates` → `#### Prompt Feedback` section; do NOT write agent-prompt-feedback.md directly". Rule 23 ordering (~L648–660): remove `feedback →` from the agent's final-step ordering. Session-wrap (~L1504): change "read agent-prompt-feedback.md" to "query the `prompt_feedback` table in lifecycle.db (read-only) and read the per-project archive for pre-activation history". Add a short `### Ledger Updates` channel-format spec near the feedback protocol so agents know the `#### Prompt Feedback` subsection format. Bump `**Version:**` to the next minor and add a changelog row. Locate every site by ANCHOR/grep; zero or multiple matches → halt and report. Do NOT touch PROJECT_STATUS (Rule 8) or FORWARD (Rule 42) instructions — later slices.
>
> **Tests:** (a) `ledger_writes` migration idempotent; (b) idempotency guard blocks a duplicate `(step_id, ledger_file, content_hash)`; (c) the feedback handler, when coexistence allows (file NOT in files_changed) and feedback present, records to DB AND writes the generated `.md` AND commits; (d) `generate_feedback_md` output is what gets written; (e) coexistence still SKIPS when the file IS in files_changed (back-compat during transition). Run the FULL suite (`python3 -m pytest tests/`) to explicit pass/fail and READ THE TAIL. Write the dev log (all 5 parts, the 6 freezes confirmed, governance anchors before/after, suite tail) to `bellows/knowledge/development/feedback-activation-dev-log-2026-06-14.md`. Use `with open()`/`os.rename`; no heredocs. **Standard prompt feedback for THIS step: still append to the (now archived/regenerated) file the old way is fine — coexistence covers the transition.** **Deposits:**
> - `bellows/knowledge/development/feedback-activation-dev-log-2026-06-14.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, and design doc Sections 4/5/6. **Verify, each with executed evidence (files into `knowledge/qa/evidence/feedback-activation-2026-06-14/`):** (1) **Full suite** — final 15 lines, zero failures, new-test count matches dev log; `full_suite_tail.txt`. (2) **Idempotency** — `ledger_writes` table present (PRAGMA), and the duplicate-guard test passes; `idempotency.txt`. (3) **Generation wired** — the feedback handler writes the generated `.md` (not a raw append); grep + the handler test; `generation_wired.txt`. (4) **Allowlist** — `agent-prompt-feedback.md` removed from `SCOPE_ALLOWLIST`, `PROJECT_STATUS.md` retained; `allowlist.txt`. (5) **6 freezes** — each project has `agent-prompt-feedback-ARCHIVE.md` (full history preserved — line count ≈ pre-freeze) and a fresh generated `agent-prompt-feedback.md`; list all 6; `freezes.txt`. (6) **Governance flip** — the 3 feedback-protocol sites, Rule 23 ordering, L1504, and the channel-format spec reflect the new contract; PROJECT_STATUS/FORWARD instructions untouched; version bumped; `governance.txt`. (7) **Coexistence intact** — the skip-when-in-files_changed test still passes (transition safety); `coexistence.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `feedback-activation-2026-06-14`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/feedback-activation-qa-report-2026-06-14.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/feedback-activation-2026-06-14/`; `required_evidence_files`: `[full_suite_tail.txt, idempotency.txt, generation_wired.txt, allowlist.txt, freezes.txt, governance.txt, coexistence.txt]`. Include the literal stdout of the block in the QA report. If it prints `FAILED`, halt and report to CEO. Write the QA report (verification table + Rule 20 banner) to `bellows/knowledge/qa/feedback-activation-qa-report-2026-06-14.md`. **Receipt Flags for CEO MUST include:** (1) DAEMON RESTART REQUIRED to load the activation; (2) **LIVE CANARY** — the FIRST bellows plan dispatched after restart is the live test: its feedback must appear in the `prompt_feedback` table and regenerate `agent-prompt-feedback.md`, with NO hand-append; watch it; (3) feedback is now activated, PROJECT_STATUS + FORWARD remain dormant (later slices); (4) FORWARD rows 4/5/13 do NOT fully close until all three ledgers are activated — note partial progress. Use `with open()`; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/feedback-activation-qa-report-2026-06-14.md`
> - `bellows/knowledge/qa/evidence/feedback-activation-2026-06-14/` (seven evidence files per Rule 20 self-check)
