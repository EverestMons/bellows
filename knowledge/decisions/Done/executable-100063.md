# bellows — EVERY LENS COMMIT TOUCHES THE REGISTER, AND THE DEPOSITED COPY READS THE DRAFT'S LOG: `lens_commit.py` takes `--dry`, writes the DRY line itself when the draft moved only its cycle line, refuses a fold without a row, and commits the draft its own spec named; `lens_order_check.py` resolves the drafting log through `fold_baseline:` so a deposited plan gets the verdict its draft already gets — the hold that stopped #265's deposit on a record the drafting side read as OK (thread 270)

**Date:** 2026-09-10 | **Project:** bellows | **Tier:** Small (two scripts edited by a few lines each, two test files extended, one mutation manifest, one dev-log) | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full-suite (Rule 21 — `lens_order_check` is subprocess-run by the depositor at every `ready-` and by both loop tools; `pytest tests/ -q`) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 270

**auto_close:** false

**Post-close:** no restart (both instruments are subprocess-read: the depositor runs `lens_order_check.py` fresh at every `ready-` — its own comment names the 31-hour stale-gate split as the reason). No doctrine edit in this plan's diff: DRAFTING_CYCLE:196 already says the DRAFT is committed after each lens's fold and `lens_order_check`'s docstring already says the register is the record and a dry lens appends its row; this plan makes the tool do what both say. ONE doctrine act for the CEO's word at close (the v2.34 precedent — the CEO's act at #100061's close), touching TWO lines of DRAFTING_CYCLE §2.7 (panel C6): line 221's tool sentence (`scripts/lens_commit.py` — baseline, register lint, `cycle_check`, the NAME assert, the yield-rising WARN) gains `--dry` and the record rule (every lens commit touches the register; a fold without a row refuses) so the doctrine describes the shipped tool; and line 196's clause *a dry lens leaves no plan commit but still appends its register row* loses its first half, which the record contradicts (scout S1: every dry commit changes the draft's cycle line). Then the Planner's act this plan unblocks: re-pin thread 265's closed draft (its P3/P4 name `lens_commit.py` lines and a five-test file this plan moves — a pin refresh walk, then its dry walk), receipt, `ready-` — the deposit that held on `lens_order:2_breach` on 2026-09-10 re-reads OK through this plan's second half.

**Depends on:** the CEO's ruling of 2026-09-10 (session d04ebd33, "Option 2"): the tool fix precedes 265's redeposit and 266. Clone origin by kind: `Done/executable-100061.md` (Done 2026-09-10 — the two loop tools with their test siblings, a mutation run, and a QA that replays real artefacts); the observer's own five commits (`efcf7bd`, `458955a`, `8a3ef73`, `30bde52`, `f988d06`, 2026-09-06/07 — Planner direct fixes, no plan id; **measured: no shipped plan has touched `lens_order_check.py`**, so the newest same-class is 100061 by tool family). By layout: this session's closed draft `executable-bellows-lint-warn-to-fail.md` (thread 265; the plan this one unblocks).

**Tier computed (§1):** **T2** — T-6 FIRES: `lens_order_check` is the instrument the depositor's admission gate reads (`depositor.py:753–766`: exit 1 holds the deposit), so changing what it reads changes what is admitted; T-1 fires (two scripts, two test files). Magnitude proxy (§2.6): (i) holds (100061 named with its Done date; both walk-0 clone-diffs run); (ii) FAILS — the edits are hand edits by the DEV agent, no builder; → **full form**: scout at walk 0, DISCOVERY / EXECUTION / CAPSTONE seats after the warm walks, each seat a read-only mandate writing one file under `governance/knowledge/research/panel-lens-record-visibility-2026-09-10/`.

## CEO Context

Thread 265's plan closed at BAR_MET after four walks, twenty lens commits, `lens_order_check.py <draft>` → `LENS-ORDER OK — 20 lens commit(s)`. Deposited to bellows, the depositor held it on `lens_order:2_breach`: read from the deposited copy the same observer saw nine lens commits and called walks 2 and 3 INCOMPLETE. Two causes, one per side. The author's: eleven DRY lens commits changed only the draft's Drafting-Cycle line, so the register — "THE RECORD" in the observer's own docstring — got no line for them; `git add` of an unchanged file records nothing, and `scripts/lens_commit.py` would not have helped: it adds the register and the baseline and never the draft (its own plan, #100061, specified all three), and writes nothing to the register on a dry lens. The observer's: for a deposited copy the "plan" source is the deposited path's own log in the project repo, empty by construction, so the draft's commits — the record DRAFTING_CYCLE:196 names — are invisible and only the register log counts; the drafting-side reading (`commit_record_paths`, one git log over draft + register) is exactly the reading the deposit needed and could not reach. Appending the missing register lines now is not a repair (`analyse` builds the sequence in commit-time order → OUT-OF-ORDER), and the release tool refuses non-class holds by design. The CEO chose the tool fix over a history rewrite. This plan is that fix: the tool makes every lens commit touch the register (and refuses a fold that has no row), and the observer, given a deposited copy, finds the draft through the pointer the manifest already carries (`fold_baseline:`) and reads the drafting repo's one log.

## What this changes

1. **`scripts/lens_commit.py` — the record rule, with an explicit `--dry`.** A new flag `--dry` (the author's attestation that this lens folded nothing). A new **step 0**, right after the three file checks and BEFORE step 1 (`repo_dir = draft_path.resolve().parent` moves up from step 5 to serve it, and every git call in step 0 takes the RESOLVED absolute paths, as step 5's `to_add` already does — with a relative draft path `ls-files --error-unmatch` reads a tracked file as untracked and `diff --quiet` reads a changed file as unchanged, panel D3, measured; both files are assumed to live in that repo, as the existing commit step already assumes) — so no later write by the tool itself can mask the author's state (the `--allow-yield-rising` override appends a WARN line to the register inside step 3, `:110–113`; scout S4): (i) both files must be IN HEAD — with `root = git -C <repo_dir> rev-parse --show-toplevel` computed once, `git -C <root> cat-file -e HEAD:<os.path.relpath(resolved path, root)>` exit 0 for the draft and the register (the `HEAD:<path>` form takes a ROOT-relative path and nothing else — panel C2: from the drafts dir it exits 128; every other step-0 call takes the resolved absolute path), else `LENS-COMMIT: record FAIL — <path> is not committed; commit the walk-0 state first` (an untracked path reads as "unchanged" to `git diff`, scout S8; `ls-files` answers the INDEX, so a register `git add`ed and never committed would pass it and then read as changed — panel E6); (ii) `draft_changed`, `register_changed` via `git -C <repo_dir> diff --quiet HEAD -- <resolved path>` (exit 1 = changed, exit 0 = unchanged; ANY other exit — 128 on a bad HEAD or no repo — is `LENS-COMMIT: record FAIL — git diff --quiet exit <n> on <path>`, never read as unchanged; likewise `rev-parse --show-toplevel` failing → `record FAIL — no git repo above <draft>`); (iii) `draft_cycle_only`: every changed line of the diff read with explicit plumbing so no git config shapes it — `git -C <repo_dir> -c color.ui=false -c diff.external= diff --no-color --no-ext-diff --no-prefix -U0 HEAD -- <draft>` (panel E3: under `color.ui=always` or `diff.external` the porcelain diff carried no `+`/`-` lines and the guard passed VACUOUSLY on a real body fold; under `diff.noprefix` the header regex refused every dry lens) — a line starting `+` or `-` that is not a file header (`^(\+\+\+|---) ` with the space; a deleted `---` rule line reads `----` and is content — panel D5) — is, after its sign, blank or one of the three shapes a dry lens writes on the record (panel D1's census of the CLASS: 235 dry-subject lens commits in governance `drafts/`, of which 44 — every walk-closing ACID dry lens in the #100061 and #100062 cycles — also write the per-walk summary line and bump the walk count): a lens line `^- (Weak spots|Destruction|Vulnerabilities|Integration-record|ACID):`, the Drafting Cycle block's per-walk summary line `^\*\*Walk \d+ — `, or the manifest's `^walks: \d+$`. (Scout S1: every real dry commit changes the draft — the `; wN dry` token or a new lens line — so "nothing changed" never describes a dry lens.) The refusal names the first line that failed the guard. (iv) the VACUITY check that gives `draft_changed` its job (panel E3/E10): `draft_changed` and not one content line parsed → `LENS-COMMIT: record FAIL — the draft changed but its diff could not be read`. (v) the duplicate guard (panel E7): under `--dry`, if the register already carries a `**Walk {walk_n} lens {lens_n} —` line or HEAD's subject already starts `draft({slug}): walk {walk_n} lens {lens_n}` (scoped to THIS draft's slug — governance is a multi-draft repo, panel C4) → `LENS-COMMIT: record FAIL — walk {walk_n} lens {lens_n} is already on the record` (a second `--dry` run otherwise wrote a second DRY line and a REPEATED lens). ⛔ **The step-0 refusals fire in this ORDER, and the first failing fact is the one quoted: (i) in-HEAD → (v) duplicate → (iv) vacuity → the record rule (cycle-only under `--dry`, no-row without it)** (panel C4). ⛔ **Every `record FAIL` fires AT STEP 0** — the facts are complete there, and a refusal after step 1 leaves a re-saved baseline in the worktree (panel E11); only the DRY append waits for step 5. **Step 5 then applies the rule from step 0's facts:** with `--dry` — refuse (at step 0) unless `draft_cycle_only` (`LENS-COMMIT: record FAIL — --dry, but the draft changed beyond its cycle record: <the offending line after its sign, first 80 chars>`); if the register is unchanged, append (UTF-8, `encoding="utf-8"`, the way the step-3 WARN write does — the line carries an em-dash) `**Walk {walk_n} lens {lens_n} — {LENS_NAMES[lens_n]} — DRY, basis:** {desc}` (#100062's exact form, `706a0f4b`; the tool's authorship goes in `desc` if the author wants it — scout S7), else the register already carries the lens's rows and nothing is written; without `--dry` — refuse (at step 0) unless `register_changed` (`LENS-COMMIT: record FAIL — the register carries no row for walk {walk_n} lens {lens_n}; write the fold row (§2.7), or pass --dry for a dry lens`). Combinations, stated: `--dry` with the register already changed (a dry lens whose author wrote considered-no-fold rows) → proceed, nothing appended; `--dry` with `--allow-yield-rising` → step 0 saw the register unchanged, step 3 appends the WARN line, step 5 appends the DRY line after it — both lines land; a draft unchanged AND register unchanged without `--dry` → refuses (the author must say which it is). In every proceeding case `to_add` is the DRAFT, the register and the baseline — #100061 item 2 specified "the draft, register and baseline" and DRAFTING_CYCLE:196 says the draft is committed after each lens's fold; the shipped tool dropped the draft against its own spec (P3; scout S3). The docstring's step list gains `0. record — in HEAD, diff facts (draft / register / cycle-only / vacuity / duplicate); every record FAIL fires here` and step 5 becomes `commit — git add draft + register + baseline; --dry writes the DRY line when the register is unchanged; a fold without a register row refuses`. Every lens commit therefore touches the register.
2. **`scripts/lens_order_check.py` main() — the draft through `fold_baseline:`.** A new helper `_draft_from_fold_baseline(plan_path) -> (draft_or_None, note_or_None)`: `bl, _declared = cycle_check.resolve_fold_baseline(plan_path)` (the ONE resolver — it routes through `_resolve_register_ref`, whose governance-root step resolves the governance-relative `fold_baseline:` from a bellows path on the mini — measured, scout S5; inside a governance clone the git-root step resolves that relative pointer to the clone's baseline, while the `**Walk register:**` ref, absolute on every plan, resolves to the LIVE register from anywhere — panel E8); if `bl` is a file and its name matches `^\.(.+)\.foldcheck\.json$`, the draft is `bl.parent / <group 1>`; return `(draft, None)` when `draft.is_file()`, `(None, "draft <path> not used (missing)")` when it is not a file (a directory at the path is "missing" — panel E5: `.exists()` there raised `IsADirectoryError` on the read), `(None, None)` when nothing resolved or the name does not match — the helper returns the FINISHED note text and `main` prints `NOTE: fold_baseline resolved; {note}` (panel E10). In the cross-repo branch (today: `rows = _merge_by_time(rows, reg_rows)`): `draft, note = _draft_from_fold_baseline(plan_path)`; if `draft` and `_repo_of(draft)` resolves to `reg_repo` AND the identity tie holds — the derived draft's own `**Walk register:**` ref (read with `cycle_check.parse_block` on its Drafting Cycle block, the same parser `register_commit_record` uses; string equality on the parsed ref — every real record's ref is absolute and the copy is a byte copy, 12/12 Done plans tie, panel E9) equals the deposited copy's — the helper call, the draft read and the tie all inside one `try/except OSError` whose except yields `note = "draft <path> not used (unreadable)"` — `<path>` the derived draft, or `<unresolved>` if the helper raised before deriving one — and the fallback (panel E5: a traceback inside the observer is a subprocess exit 1, which the depositor holds as `lens_order:0_breach`) — then `rows = commit_record_paths(reg_repo, [draft, reg_path])` and `record_src = f"draft+register, one log via fold_baseline ({draft.name})"`; otherwise the existing time-merge and its existing `record_src`, unchanged — and when `bl` resolved but the derived draft is missing, lives in another repo than the register, or names a different register (a STALE or FOREIGN pointer — the clone practice leaves the parent's `fold_baseline:` in a copied draft until walk 0 rewrites it; without the tie the one log would union ANOTHER plan's lens commits and manufacture REPEATED/OUT-OF-ORDER where today reads INCOMPLETE or OK — panel D4, measured with the prototype), ONE line to stderr: `NOTE: fold_baseline resolved; draft <path> not used (<missing|other repo|register ref differs>)` — no verdict change, the reader learns why the hold stands (scout S9: every miss otherwise falls silently to the exact hold this plan fixes). The same-repo branch is untouched. The docstring gains one paragraph naming the deposited-copy case and thread 270.
3. **`tests/test_lens_commit.py`** — nine tests, failing-first, on `_make_lens_fixture`, plus `--dry` added to the four existing calls that run on an unedited fixture (l1, BOTH calls of l3, l4 — under the record rule an unedited fixture without `--dry` refuses at step 0 with the no-row line, so l3's first call would refuse for the wrong reason; their asserts are unchanged): **l6** (dry): change the fixture's `- Destruction: w1 dry.` line IN PLACE to `- Destruction: w1 dry; w2 dry.` (a Cycle-Log-only change, inside the block `cycle_check` parses), register untouched, `--dry` → rc 0; the register's text, `rstrip()`ed, ends with `**Walk 1 lens 2 — Destruction — DRY, basis:** dry`; `git show --name-only --format= HEAD`, split on newlines, contains `plan.md`, `register.md` and `.plan.md.foldcheck.json` as WHOLE names (panel E1: a substring test on `--stat` is satisfied by the baseline's own name, so l8 was green before the edit and the draft-dropped mutant survived). **l7** (fold without a row): append `\nextra fold line\n` to `plan.md`, register untouched, no `--dry` → rc 1, stdout carries `LENS-COMMIT: record FAIL`, `git rev-list --count HEAD` unchanged. **l7b** (`--dry` over a real fold): the same edit WITH `--dry` → rc 1, `record FAIL — --dry, but the draft changed beyond` and the offending line `extra fold line` quoted. **l6b** (the walk-closing shape, panel D1): FIRST rewrite the fixture's `walks: <declare>` to `walks: 0` and commit that as the walk-0 state (panel E2: every first close on the record moves `walks: 0` → `1`; `<declare>` → `1` is the fixture's artifact and no admitted shape deletes it), THEN the l6 edit PLUS a new line `**Walk 1 — dry.**` inside the Drafting Cycle block and `walks: 0` → `walks: 1`, `--walk 1 --lens 5 --dry` (the walk-closing ACID lens the census names) → rc 0 (the 44-commit shape admitted). **l7c** (the environment, panel E3): `git config color.ui always` in the fixture repo, the l7 body edit, `--dry` → rc 1 and `record FAIL — the draft changed but its diff could not be read` is NOT what prints — the plumbing flags read the diff regardless, so the refusal is l7b's `beyond its cycle record`; and a second variant with `diff.external` set to `/usr/bin/true` → the same l7b refusal (the DEV asserts the l7b text under both configs). **l11** (the vacuity refusal, panel C5): `git config core.filemode true` in the fixture repo (the macOS default; stated so the mode change is a change to git), then `chmod +x plan.md` and nothing else — `diff --quiet` reads changed (mode), the `-U0` diff has no content line — `--dry` → rc 1, `record FAIL — the draft changed but its diff could not be read`. **l9** (panel E6/C1): a fixture whose `initial` commit holds the plan only — the register written and `git add`ed AFTER it, never committed (the `_make_lens_fixture` seed commits both, so l9 builds its own two-step seed) → rc 1, `record FAIL — … is not committed`. **l10** (panel E7): the l6 run twice → the second rc 1, `record FAIL — walk 1 lens 2 is already on the record`, the register carrying ONE DRY line. **l8** (fold with a row): the l7 edit AND a row `| f1 | 1 | 2 | q | v0 | finding | text | folded |` appended to `register.md` (SHAPE-OK — scout S8) → rc 0 and `git show --name-only --format= HEAD` lists all three names. The existing l2 refuses at step 4 and l5 at step 2 as today.
4. **`tests/test_lens_order_check.py`** — seven tests, failing-first, on a NEW helper `_two_repos(tmp_path, steps, with_fold_baseline=True, foreign=False)`: repo A (drafting) built like `_repo_with_register` but with the register ref written ABSOLUTE (`**Walk register:** <A>/knowledge/research/walk-register-fixture.md` — the relative form resolves from repo B's git root to nothing and every o-test reads NO-RECORD, scout S2, measured) plus a committed baseline file `knowledge/decisions/drafts/.executable-fixture.md.foldcheck.json` (any JSON — the observer never opens it) and a `## Cycle Manifest` stanza in the plan text in `tests/test_close_cycle.py`'s `_MANIFEST_BLOCK` shape with `fold_baseline:` set to the baseline's ABSOLUTE path (absolute → `_resolve_register_ref` returns it directly, so the fixture does not depend on the machine's governance root); repo B (project) holding a copy of A's plan text at `knowledge/decisions/executable-fixture.md`, one commit. Steps in A, the #265 shape: `w1 l1` plan+reg, `w1 l2` plan only (dry — the DC line moved), `w1 l3` plan+reg, `w1 l4` plan only, `w1 l5` plan only, `w2 l1` plan+reg. **o1**: `_run(B's copy, B)` → rc 0, `LENS-ORDER OK`, `source=draft+register, one log via fold_baseline (executable-fixture.md)`. **o2** (negative control): the copy with the `fold_baseline:` line removed → rc 1, `INCOMPLETE: walk 1`, `merged by time` in the BASIS line — today's reading preserved where the pointer is absent. **o3**: `fold_baseline:` naming a file that does not exist → rc 1, `INCOMPLETE`, `merged by time`, no traceback (the fallback, not a crash); **o4**: the baseline resolves but the derived draft was deleted from A after the baseline was committed → rc 1, `merged by time`, and stderr carries `NOTE: fold_baseline resolved;`; **o4b** (panel E5): the derived path is a DIRECTORY → rc 1, `merged by time`, the NOTE, and `Traceback` absent from stderr. **o4c** (panel C3): the derived draft exists but is UNREADABLE (`chmod 000`, restored in a `finally`) → rc 1, `merged by time`, the NOTE `(unreadable)`, no `Traceback` — the case only the `try/except OSError` can pass (a directory never reaches the read under `is_file()`, so o4b cannot kill that mutant). **o5** (the foreign pointer, panel D4): repo A also holds a second, complete cycle (`executable-other.md` with its own register and baseline); the copy's `fold_baseline:` names `.executable-other.md.foldcheck.json` → rc 1, `INCOMPLETE: walk 1` (the copy's own register, `merged by time`), stderr `register ref differs` — never `REPEATED` (`_run` captures stderr — `capture_output=True`). ⚠️ The o-tests' ABSOLUTE pointer exercises `_resolve_register_ref`'s first step only; the governance-root step that the real deposit relies on is exercised by QA Item 2 on the real machine and was proved at walk 0 (register) — stated so nobody reads the fixtures as covering it.
5. **`knowledge/mutants/lens-record-visibility.json`** — at least fourteen mutants (top-level `target: scripts/lens_commit.py`; the observer mutants carry their own `target`; every `expect_fail` a plain `tests/<file>.py::<test>` node id): the DRY append removed (l6); the cycle-only guard removed (l7b); the no-row refusal removed (l7); the draft dropped from `to_add` (l8); `_draft_from_fold_baseline` returning `None` always (o1); the leading dot not stripped from the baseline name (o1); the NOTE removed (o4); the identity tie removed (o5 — the foreign cycle's rows would union in and read REPEATED); the duplicate guard removed (l10); the in-HEAD check reverted to `ls-files` (l9); the `try/except OSError` removed (o4c — NOT o4b, panel C3); the vacuity refusal removed (l11); the plumbing flags dropped from the diff call (l7c — under `color.ui always` the diff then reads no content line, the vacuity refusal fires where l7b's refusal is asserted); the `**Walk` shape dropped from the accepted set (l6b) — panel C5. The seat's dry run of the manifest as first specified: `7 killed, 1 survived` — the draft-dropped mutant survived l8's substring stat until E1's `--name-only`; with it `8 killed, 0 survived`.
6. **Not changed:** `depositor.py` (its arm reads the exit; nothing daemon-loaded moves), `cycle_check.py` (its resolvers are called, not changed), the same-repo branch, `_merge_by_time`, `analyse`, every verdict word, the four functions `substrate_check.py` imports from the observer (`_repo_of`, `tier_of`, `declared_walks`, `commit_record` — P9), every other test; the two census tools that run the observer keep their verdicts on the whole pointer-carrying corpus (P9: 12 plans, 0 changes). Thread 266 (`lens_commit` refusing CONTINUE at step 3) is NOT this plan: it is the next in the CEO's order, and its edit sits at `:116`, above step 5. The diff touches four files plus the manifest, the run file and the dev-log.

## Why this exists

The observer was built so sequential execution is provable from the record; the record is the register (threads 163/165); a deposited plan lives in another repo (thread 163's measurement: 28 commits in governance, 0 in the project). The 265 hold is the residue of that design meeting a tool that did not write the record it presumes and a reading that could not reach the record it names. Both halves are in doctrine already — DRAFTING_CYCLE:196 (commit the draft after each lens), the observer's docstring (a dry lens still appends its row) — and neither was mechanised. GLOSSARY's *walk register (as the record)* entry states the record side; the pointer the observer needs is already in every manifest since DC v2.28 (`fold_baseline:`, thread 185: "a pointer the plan carries, resolved through the ONE resolver that already finds the register"). Same-class precedent: #481's `_assign_class` and the fold-baseline carrier — a value the deposit side cannot reach by position is carried as a field and resolved once.

## What this does NOT do

- Does not rewrite governance history for #265; its redeposit reads OK through item 2 with its record as committed.
- Does not change the same-repo reading, the time-merge, `analyse`'s verdicts, or the depositor's arm; a deposited copy WITHOUT `fold_baseline:` (every plan before DC v2.28) reads exactly as today.
- Does not make `lens_commit.py` accept CONTINUE (thread 266) or run `plan_lint` (thread 265's plan, which re-pins after this ships).
- Does not write the DRY line for a hand commit — an author committing by hand still owes the register its line; the tool is where the rule is enforced, and the standing rule stays in words until the tool is the only path.

## MUST-PRESERVE

- ⛔ **The register stays the record.** Item 2 reads the drafting repo's ONE git log over the draft and the register — the reading the draft already gets — in place of the deposited copy's own log (which carries 0 lens rows on every one of the 12 pointer-carrying Done plans, panel D6) and never as a substitute for the register: the branch runs only when the register has at least one lens row (it sits inside `if reg_rows:`), a plan with no resolvable register still reads `plan only`, and the draft is admitted only when it names the same register (the identity tie).
- ⛔ **A fold without a row refuses; a dry lens is attested, then checked.** The tool never writes a DRY line without `--dry`, and never when the draft changed beyond its cycle record (the lens lines, the per-walk summary lines, `walks:`).
- ⛔ **No pointer, no change; a wrong pointer, no change.** Every deposited copy without `fold_baseline:` reads through the time-merge as today (o2); one whose pointer names another plan's draft reads as today with a NOTE (o5).
- ⛔ **Suite green at both commits;** nothing outside the four files moves.

## Numbers discipline — measured 2026-09-10 by the Planner (bellows `8ec3815`, governance `6bdafaf5`; P1–P8 at walk 0, P9 at walk 1 lens 2)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the hold, verbatim | `knowledge/decisions/hold-executable-bellows-lint-warn-to-fail.hold.json` (withdrawn to the session scratchpad `withdrawn/2nd/`): `"hold_reason": "lens_order:2_breach"`, `"cycle_check": "BAR_MET"`, `"plan_lint": "exit_0"`, `"lens_order": "breach_2"`; the observer on the held copy: `BASIS: source=plan+register, merged by time (walk-register-lint-warn-to-fail-2026-09-10.md) tier=T1 declared_walks=[1, 2, 3, 4] lens_commits=9 walks_with_lens_commits=[1, 2, 3, 4]`, `INCOMPLETE: walk 2 — closed walk proves lenses [1, 3]; T1 requires [1, 2, 3, 4, 5] — missing [2, 4, 5]`, `INCOMPLETE: walk 3 — closed walk proves lenses [1] …`; on the draft: `BASIS: source=plan+register, one log (…) … lens_commits=20 walks_with_lens_commits=[1, 2, 3, 4]`, `LENS-ORDER OK — 20 lens commit(s) across walks [1, 2, 3, 4]` | `scripts/lens_order_check.py <governance draft>` vs the same on a `cp` of the draft placed under `$T/knowledge/decisions/` inside a scratch `git init` (NEVER a real `knowledge/decisions/`) |
| P2 | ⛔ the observer's seams | `scripts/lens_order_check.py` (re-pinned by its own command after scout S6): `def commit_record` `:92` (`git log --follow --reverse` over one path); `def commit_record_paths` `:125` (ONE log over several paths, git's order, source tag `plan+register`); `def _merge_by_time` `:157`; `def _repo_of` `:174`; `def register_commit_record` `:185` (docstring `:186–202`: THE REGISTER IS THE RECORD … a dry lens still appends its row to it) → `(rows, resolved register path)` via `cycle_check._resolve_register_ref`; `def main` `:305`; the branch: `commit_record_paths(repo, [plan_path, reg_path])` `:337` (same repo → `record_src = "plan+register, one log (<name>)"`), `rows = _merge_by_time(rows, reg_rows)` `:339` (else → `"plan+register, merged by time (<name>)"`); `walks = declared_walks(text)` follows; exits 0/1/2 as the docstring states | `grep -n 'def \|_merge_by_time(rows\|commit_record_paths(repo' scripts/lens_order_check.py` |
| P3 | ⛔ the tool's commit step | `scripts/lens_commit.py:129–155`: `repo_dir = draft_path.parent`; `to_add = [str(register_path.resolve())]`; `bpath = _baseline_path(draft_path)` appended when a file; `git -C repo_dir add <to_add>`; `git commit -m <subject>`; the DRAFT is never added; nothing is written to the register; the docstring `:9–13` lists five steps with `5. commit — git add register + baseline, commit with composed subject`; `:110–113` the `--allow-yield-rising` WARN line appended to the register INSIDE step 3; `Done/executable-100061.md` item 2 specified `committing the draft, register and baseline` and its P6 hand shape was `git add <draft> <register> <baseline>` — the shipped tool dropped the draft against its own spec, uncaught by l1–l5 (none reads `git show --stat`; scout S3); `LENS_NAMES` `:24–30` | `sed -n '9,13p;129,155p' scripts/lens_commit.py` |
| P4 | ⛔ the resolver this reuses | `scripts/cycle_check.py:818–846` `resolve_fold_baseline(plan_path, manifest=None) -> (path_or_None, declared)`: reads `fold_baseline:` from `parse_manifest_stanza`, resolves through `_resolve_register_ref` (`:771–816`: absolute → as-is; then git root; then governance root — the step that resolves a governance-relative ref from a bellows path on the mini, measured; then the shop root), requires `.is_file()`; a declared ref that does not resolve returns `(None, True)` with no beside-the-plan fallback; the baseline name convention `.<draft name>.foldcheck.json` (`lens_commit._baseline_path` `:53–56`, `close_cycle._baseline_path` `:108–111`, `resolve_fold_baseline`'s beside-the-plan branch); four STALE beside-the-plan baselines sit in `bellows/knowledge/decisions/` (Aug 24, `.executable-eluvian-e2-…`) — for a deposited copy without a declared pointer they derive the copy itself, whose repo ≠ the register's → the fallback (scout S9) | `sed -n 818,846p scripts/cycle_check.py`; `grep -nF 'foldcheck.json' scripts/*.py` |
| P5 | ⛔ the tests as they stand | `tests/test_lens_order_check.py`: 17 tests; helpers `_git` `:22`, `_plan_text(tier, walks)` `:26` (the REAL Cycle Log shape — lens-keyed lines with `wN` tokens; no manifest stanza), `_repo(tmp_path, subjects, …)` `:52`, `_run(plan, repo)` `:66` (subprocess with `--repo`), `_repo_with_register(tmp_path, steps, walks, same_second)` `:208–251` (plan + register committed SEPARATELY, explicit increasing commit dates — `%cI` is second-resolution; its register ref is RELATIVE, `knowledge/research/walk-register-fixture.md`, which from a second repo resolves to nothing — scout S2); `test_a_dry_lens_is_visible_through_the_register` `:254` is the same-repo precedent for o1; `tests/test_lens_commit.py`: 5 tests, `_make_lens_fixture` `:68–88` (plan = `_INITIAL_PLAN`, register = `_REGISTER`, one `initial` commit, `user.name` set), l1 asserts the subject via `git log --format=%s -1`, l3/l4 monkeypatch `run_checker` for `cycle_check.py` only and read the register's `**WARN (walk 4):**` line; `tests/test_depositor_lens_order_gate.py`: 6 tests, all through a stubbed subprocess — untouched | `grep -n 'def test_\|def _' tests/test_lens_order_check.py tests/test_lens_commit.py tests/test_depositor_lens_order_gate.py` |
| P6 | the consumers | `depositor.py:753–773` (`_run_validation`'s lens-order arm: exit 1 → hold `lens_order:<n>_breach`; exit 2 → `no_record`, no hold; exception → recorded, no hold); `scripts/close_cycle.py:147–149` (battery: `LENS-ORDER OK` or `N/A` required); `tools/clear_plan.py:97–107` (`--release-class-hold` allows `class:` and `held_pending_ceo_release` only — a lens-order hold has no release arm) | `grep -nF 'lens_order' depositor.py scripts/close_cycle.py tools/clear_plan.py` |
| P7 | the record convention | DRAFTING_CYCLE:196 — *Commit the draft after every phase … and after each lens's fold inside a walk*; the observer's docstring `:186–202` — *THE REGISTER IS THE RECORD … a dry lens still appends its row to it*; #100062's dry-lens commit `706a0f4b` (governance): draft `+1` (the cycle line) + register `+2` (`**Walk 5 lens 2 — Destruction — DRY, basis:** unchanged`) + the baseline; the 265 cycle's twelve dry commits: eleven are draft `2 +-` (the `; wN dry` token) + baseline with the register untouched (e.g. governance `9079ce2f`, `0649a6a4`, `c0e0a925`), one (`5a3b236b`, walk 4 lens 1) also opened the register's walk-4 heading (panel D8) — ⛔ a dry lens CHANGES THE DRAFT (scout S1); the observer's docstring sentence "a dry lens leaves no plan commit" is contradicted by both records; the CLASS (panel D1): 235 dry-subject lens commits in governance `drafts/` — 224 touched a register, 5 touched no draft, 44 write the per-walk summary line and/or `walks:` (every walk-closing ACID dry lens of #100061 and #100062) | `git -C <governance> show --stat --format= 706a0f4b 9079ce2f`; the census: `git -C <governance> log --format='%H %s' -- governance/knowledge/decisions/drafts/ \| grep -E 'lens [1-5] — [A-Za-z-]+: dry'` → the subjects, then `git show -U0 --format= <sha> -- <draft>` per commit against the three shapes (the seat's `dry_census.py` did exactly this) |
| P9 | ⛔ the change measured on the corpus (walk 1 lens 2) | a scratch prototype of item 2 (the helper + the branch, in the session scratchpad — NOT the DEV's source; the DEV writes from item 2) run on deposit-shaped copies (a scratch `git init`, never a real lane) of every `Done/executable-*.md` carrying `fold_baseline:` — **12 plans, 0 verdict changes** (each already OK: their cycles wrote the register at every lens); on the 265 copy: `BASIS: source=draft+register, one log via fold_baseline (executable-bellows-lint-warn-to-fail.md) tier=T1 declared_walks=[1, 2, 3, 4] lens_commits=20 walks_with_lens_commits=[1, 2, 3, 4]` then `LENS-ORDER OK — 20 lens commit(s) across walks [1, 2, 3, 4]; …` — the deposit-side OK the hold lacked; callers of the observer beyond the depositor: `scripts/substrate_check.py` imports `_repo_of`, `tier_of`, `declared_walks`, `commit_record` (none changed), `tools/battery_census.py`, `tools/cycle_log_projection_census.py`, `tests/test_register_coverage.py` run the script (verdict class unchanged on the corpus); callers of `lens_commit.py`: its tests only (doctrine names it in prose) | `for f in Done/executable-*.md: grep -q '^fold_baseline:'` → copy into a scratch repo → run both observers → diff the verdict word |
| P8 | the mutation contract; class; in-flight | `tools/mutation_check.py`: manifest-driven, per-mutant `target` override (`:20`), KILLED = exit 1 only, sandbox = `git archive HEAD`; run file `knowledge/mutants/<slug>.run.txt` ending `MUTATION: N killed, 0 survived, 0 error`; this plan writes `scripts/` and `tests/` → **shop-infra** (HOLDS, the CEO releases); in-flight none (daemon pid 70398, HEAD `8ec3815`); five `gate_watcher.py` processes were spawned across the cycle (two armed by the withdrawn 265 deposits, one by a test inside the walk-1 scratch archive run, two by the EXECUTION seat's archive runs — `tests/test_notifier_coverage.py::test_11_gate_watcher` spawns a real one); four alive at the capstone fold — reporters with a 120-minute timeout, never writers, left to expire (panel D8, C11); thread 265's draft is CLOSED and WITHDRAWN (its lane is empty) — no W-W on `scripts/lens_commit.py` or `tests/test_lens_commit.py` until this plan is Done and 265 re-pins | `sed -n 1,22p tools/mutation_check.py`; `python3 status.py`; `ls knowledge/decisions \| grep -c lint-warn` → 0 |

## ACID, stated

- **Atomicity:** after DEV's first commit (five files: two scripts, two tests, the manifest) the worktree is coherent and green; after the second, the run file and dev-log ride; QA adds two evidence files; nothing reaches `main` until the daemon merges at the QA pause. A halt after the merge and before the verdict leaves both instruments live for every deposit and every lens commit — the intended end state.
- **Consistency:** the register stays the record (item 2 adds a path to the drafting repo's one log; MUST-PRESERVE 1); the DRY line is written only under `--dry` with a cycle-record-only diff and an unchanged register (item 1); the no-row refusal closes the one breach the observer cannot see from the register (item 1); the pointer-absent reading is byte-identical to today (o2).
- **Isolation:** two steps, one worktree. Cross-plan: thread 265's plan writes `lens_commit.py` and `test_lens_commit.py` — withdrawn, not in flight (P8); it re-pins and redeposits only after this plan's Done. Thread 266 edits the step-3 gate (`lens_commit.py:116` today; the line moves once step 0 lands) — after 265.
- **Durability:** the run file, the dev-log with P3's cell, the QA receipt with the block's own PASSED line, the redirected suite file, and the QA's replay of the real #265 case (Item 2) and rehearsal of the tool on a clone (Item 3) — the deposit-side OK on the very record that held.

## Drafting Cycle

**Tier:** **T2** — T-6, T-1 fire; full form (proxy leg (ii) fails: hand edits). **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-lens-record-visibility-2026-09-10.md
**Walks:** walk 0 pinned (P1–P8 measured on bellows `8ec3815`, P9 at walk 1 lens 2; clone-diff against `Done/executable-100061.md` (kind) and the closed 265 draft (layout) run: FACTS, ARTEFACTS, STRUCTURE; the standing-rules diff against bellows `CLAUDE.md` and STANDING_CONSTRAINTS v1.0; the cold SCOUT seat convened before lens 1 per §2.0); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit.

**Cold panel (T2, full form):** scout at walk 0 (seat-scout.md: 9 findings — S1 DIRECTION-class, the dry rule re-modelled on the record; all nine folded before lens 1; ~175k tokens); DISCOVERY at walk 3 lenses 1–2 (seat-discovery.md: 8 findings, ~245k), EXECUTION at lens 3 (seat-execution.md: 11 findings, ~250k), CAPSTONE at lenses 4–5 (seat-capstone.md: 11 findings, ~341k). ⚠️ Declared deviation (panel C7): the panel was convened after walk 2, which was NOT dry (6 folds) — §2.6 licenses the convene on a dry walk, the lens-4 signal, or the CEO's call; the author read #100062's sequence as the precedent and misread it (its walk 2 WAS dry). Consequence honoured: the panel's folds re-open the walk — warm walk 4 on the folded text, then a fully dry walk, before the close. Second declared deviation: every lens commit of this cycle is by hand (`lens_commit.py` refuses CONTINUE — thread 266, open fork 2); each touches the register, the dry lenses by their DRY line.

- Weak spots:          w1 7 folded — instruction 5 / record 2, 1 recorded (stale sentences after the scout's redesign; blank diff lines; the helper's return shape; the one-repo assumption); w2 5 folded — instruction 3 / record 2 (label drifts, all fold-introduced)
- Destruction:         w1 3 folded — instruction 1 / record 2 (the corpus census as P9 — 12 plans, 0 changes; the callers; the DC §2.7 sentence for the CEO); w2 dry
- Vulnerabilities:     w1 3 folded — instruction 2 / record 1 (UTF-8 on the DRY write; flag combinations; the fixtures' absolute pointer named as not covering the governance-root step); w2 1 folded — instruction 1 / record 0
- Integration-record:  w1 3 folded — instruction 0 / record 3 (DC:196's contradicted clause joins the CEO's sentence; GLOSSARY's record entry cited; substrate leg 2 as open fork 5); w2 dry
- ACID:                w1 1 folded — instruction 0 / record 1 (the Durability line's item reference); w2 dry
- Weak spots:          w3 5 folded — instruction 4 / record 1 (panel D1: the guard widened to the record's three dry shapes on a 235-commit census, l6b; D2: the rehearsal edits first; D3: resolved paths in step 0; D5: header exclusion, rstrip); w4 7 folded — instruction 0 / record 7 (wording drifts, all fold-introduced but one); w5 10 folded — instruction 1 / record 9 (the re-run's deviations); w6 dry
- Destruction:         w3 4 folded — instruction 2 / record 2 (panel D4: the identity tie, o5, a mutant; D6: MUST-PRESERVE 1 restated on the measured 0 own-log rows; D7: open fork 6; D8: P7/P8 counts and the watchers); w4 dry; w5 dry; w6 dry
- Vulnerabilities:     w3 10 folded — instruction 9 / record 1 (EXECUTION: E1 name-only stats; E2 l6b's walk-0 seed; E3 plumbing flags + vacuity; E4 QA text; E5 is_file + OSError; E6 in-HEAD check; E7 duplicate guard; E8 the live register in a clone; E10 the note's form; E11 refusals at step 0; E9 recorded); w4 dry; w5 dry; w6 dry
- Integration-record:  w3 4 folded — instruction 0 / record 4, 1 recorded (CAPSTONE: C6 two DC lines; C7 the convene and the hand commits declared; C8/C9/C11 the record sites; C10 recorded); w4 dry; w5 dry; w6 dry
- ACID:                w3 5 folded — instruction 5 / record 0 (CAPSTONE: C1 l9's own seed; C2 root-relative `HEAD:` path; C3 o4c; C4 refusal order, slug-scoped guard, QA cases on lens 2; C5 l11 and three mutants); w4 dry; w5 dry; w6 dry

**Closing:** WARM close after walk 6 — BAR MET (T2, full form), thread 270's plan. Six walks: 18 → 6 → 30 (the panel's fold pass: DISCOVERY 9, EXECUTION 10 + 1 recorded, CAPSTONE 9 + 1 recorded) → 7 (wording, the post-panel read C7 owed) → 10 (the §2.6 scoped execution re-run's deviations, one instruction clause) → 0 (walk 6 fully dry). Scout at walk 0 (9), three seats at walk 3, the re-run before the close: the two tools and all sixteen tests built from the spec in scratch — 39 passed, `MUTATION: 14 killed, 0 survived, 0 error`, QA Items 2 and 3 byte-identical to the plan's expected lines. Declared deviations: the panel convened after a non-dry walk (the author misread #100062's yields); every lens commit by hand (`lens_commit.py` refuses CONTINUE — thread 266); one lens-commit subject carried `QA lens 2` in its description and read as a batched lens — reworded by the CEO's history rewrite of that one subject (path 1, 2026-09-10), thread 271 filed for the tool guard. Deposit via `ready-`; HOLDS on class shop-infra (writes under `scripts/` and `tests/`), the CEO releases. Close acts: the CEO's two-line DC §2.7 act (Post-close); the depositor's class parse re-run at this close (9 writes, shop-infra, step 2 declared QA); the deposit-side observer run on a scratch copy BEFORE the receipt (the lesson 265 taught).

## Cycle Manifest
tier: T2
target: scripts/lens_order_check.py
class: shop-infra
reads: scripts/lens_order_check.py, scripts/lens_commit.py, scripts/cycle_check.py, scripts/close_cycle.py, depositor.py, tools/clear_plan.py, tools/mutation_check.py, tests/test_lens_order_check.py, tests/test_lens_commit.py, tests/test_depositor_lens_order_gate.py, tests/test_close_cycle.py, knowledge/decisions/Done/executable-100061.md, knowledge/decisions/Done/executable-100062.md, DRAFTING_CYCLE.md, STANDING_CONSTRAINTS.md
writes: scripts/lens_order_check.py, scripts/lens_commit.py, tests/test_lens_order_check.py, tests/test_lens_commit.py, knowledge/mutants/lens-record-visibility.json, knowledge/mutants/lens-record-visibility.run.txt, knowledge/development/dev-log-lens-record-visibility-2026-09-10.md, knowledge/qa/evidence/lens-record-visibility-qa-receipt-2026-09-10.md, knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt
mutants: knowledge/mutants/lens-record-visibility.json
open_forks: 1. `close_cycle.py` splicing `walks:` (thread 269) — its own small plan; 2. thread 266 (`lens_commit.py` accepts CONTINUE) — next after 265; 3. `lens_commit.py` deriving `--dry` from the diff alone (no flag) — rejected at walk 0: the flag is the author's attestation, the diff is its check; 4. `lens_order_check` reading the draft's log for a plan whose manifest predates `fold_baseline:` (DC v2.28) via a `draft:` field — no such plan is in a lane today; 5. `substrate_check` leg 2 (`:84–95`, per-walk coverage from `commit_record` ∪ register rows) resolving the draft the same way — inert once item 1 holds (every walk then has a register commit), so not this plan; 6. `commit_record`'s `git log --follow --reverse` (`:92`) — copy-detection can empty a byte-identical-seeded register's history (panel D7, pre-existing; item 2's one log takes no `--follow`)
walks: 6
yields: 8, 4, 20, 0, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:65
coherence: 6/6 body walks named in the register (81 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-lens-record-visibility.md.foldcheck.json

---

## STEP 1 — DEV (two scripts by a few lines, two test files extended; the mutation run; two commits)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ Never run the daemon, `run_plan`, a claim; never import `lifecycle`. ⛔ Every deposit is written in THIS worktree (`"$(git rev-parse --show-toplevel)"`), never in the canonical checkout; the plan's own lane file lives in the canonical checkout at `/Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md` (`<id>` from the prompt). ⛔ Every scratch git repo the tests or you build lives under pytest's `tmp_path` or `$T` — never a plan-shaped file under a real `knowledge/decisions/`.
>
> **Scope:**
> - `scripts/lens_order_check.py`
> - `scripts/lens_commit.py`
> - `tests/test_lens_order_check.py`
> - `tests/test_lens_commit.py`
> - `knowledge/mutants/lens-record-visibility.json`
> - `knowledge/mutants/lens-record-visibility.run.txt`
> - `knowledge/development/dev-log-lens-record-visibility-2026-09-10.md`
>
> **Item 1 — re-derive P2, P3, P4 and P5 and HALT on a mechanism mismatch** (P1, P6–P9 are records; a line-number drift is not a mismatch, a changed function shape, a changed `record_src` string or a changed baseline-name convention is). Paste `main()`'s cross-repo branch, the tool's step 5, `resolve_fold_baseline`'s signature and return contract, the two test files' helper signatures and test counts, under the first declared heading. Then confirm the pointer resolves from a PROJECT path the way item 2 needs: a five-line harness that copies `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/executable-bellows-lint-warn-to-fail.md` into `$T/x.md` and prints `cycle_check.resolve_fold_baseline(Path("$T/x.md"))` — expected a governance path ending `.executable-bellows-lint-warn-to-fail.md.foldcheck.json` and `True`; a `(None, True)` HALTS (the governance-root step did not reach the file — item 2's premise is false on this machine).
> **Item 2 — write the failing tests FIRST:** *What this changes* 3 and 4 — l6, l6b, l7, l7b, l7c, l8, l9, l10, l11 (and `--dry` on l1/l3/l4 — before the edit every `--dry` test, these three included, dies at `argparse` with `SystemExit` 2, which pytest reports as a FAILED test carrying `SystemExit: 2`, not the reasons below; the DEV says so and reads the reasons below at the FIRST run after `--dry` is parsed, panel C9) and the `_two_repos` helper with o1–o5, o4b and o4c. Run the two files: the new tests red for the right reason (l6/l6b: no DRY line and `plan.md` absent from `--name-only`; l7/l7b/l7c/l9/l10/l11: rc 0 and a commit where refusal was expected; l8: `plan.md` absent from `--name-only`; o1: INCOMPLETE where OK was expected; o4/o4b/o4c: no NOTE; o2 and o3 GREEN before the edit — they pin today's reading; o5 RED on its NOTE assert only, its four verdict asserts green — and the DEV says so in the paste), every other existing test in the two files still green. Paste under the second declared heading.
> **Item 3 — the two edits** as *What this changes* 1–2; then the two files green, then the FULL suite green (`N passed, 1 skipped` — the `test_gate_watcher` live-DB test skips in a worktree; a skip is not a failure; a scratch copy under `/tmp`, `/private/tmp` or `/var/folders` fails the two `clear_plan` durable-ref tests by LOCATION — `clear_plan.py:178` — not a defect, and the plan's worktree under `.bellows-worktrees/` is not under a temp prefix).
> **Item 4 — first commit**, gated on the FULL suite AND the pre-check, path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && ( /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" 2>&1 || true ) | grep -E '^(FAIL|PRECHECK)' && git add scripts/lens_order_check.py scripts/lens_commit.py tests/test_lens_order_check.py tests/test_lens_commit.py knowledge/mutants/lens-record-visibility.json && git commit -F <msg-file> -- scripts/lens_order_check.py scripts/lens_commit.py tests/test_lens_order_check.py tests/test_lens_commit.py knowledge/mutants/lens-record-visibility.json` — five files (the manifest rides this commit so the mutation run's `HEAD:` archive contains it); the message tagged with the plan id and `thread 270`. (At this commit the dev-log and the run file do not exist yet: the pre-check reports failures under `rule_22_verification` and `dev_log_declared_text` — expected at the FIRST commit; the subshell keeps the suite gate and the `grep` exits 1 only when neither a `FAIL` nor a `PRECHECK:` line was produced; the paste MUST contain the `PRECHECK:` line; thread 264 is the stage-aware flag the tool lacks.)
> **Item 5 — the mutation run, REDIRECTED into `knowledge/mutants/lens-record-visibility.run.txt` (a Deposit):** the manifest committed at Item 4 (*What this changes* 5, at least fourteen mutants); `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/lens-record-visibility.json > knowledge/mutants/lens-record-visibility.run.txt 2>&1`; the last line must read `MUTATION: <n> killed, 0 survived, 0 error` with n ≥ 14 — a survivor is a test to write, not a mutant to delete.
> **Item 6 — the dev-log** `knowledge/development/dev-log-lens-record-visibility-2026-09-10.md` under the four headings declared below; `## Pins re-derived (P2, P3, P4, P5)` opens with P3's value cell pasted verbatim, whole, to its last character — ⛔ the VALUE cell (third column) ends with the words `` `LENS_NAMES` `:24–30` ``; paste through those words, then stop. **Second commit**, gated on the FULL suite AND the run file AND the pre-check, path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && grep -Eq 'MUTATION: (1[4-9]|[2-9][0-9]+|[1-9][0-9]{2,}) killed, 0 survived, 0 error' knowledge/mutants/lens-record-visibility.run.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/mutants/lens-record-visibility.run.txt knowledge/development/dev-log-lens-record-visibility-2026-09-10.md && git commit -F <msg-file> -- knowledge/mutants/lens-record-visibility.run.txt knowledge/development/dev-log-lens-record-visibility-2026-09-10.md` — two files.
> **Headings:** `## Pins re-derived (P2, P3, P4, P5)`; `## Failing-first (red or pinned, then green)`; `## The pointer resolves from a project path (Item 1)`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P2, P3, P4, P5)` ← P3
>
> **Deposits:**
> - `scripts/lens_order_check.py`
> - `scripts/lens_commit.py`
> - `tests/test_lens_order_check.py`
> - `tests/test_lens_commit.py`
> - `knowledge/mutants/lens-record-visibility.json`
> - `knowledge/mutants/lens-record-visibility.run.txt`
> - `knowledge/development/dev-log-lens-record-visibility-2026-09-10.md`
>
> **Post-conditions:** sixteen new tests green (seventeen collected items — l7c is parametrised over `color.ui` and `diff.external`, so the suite count moves by seventeen) and every prior test in the two files green; `git show --name-only` of a `--dry` fixture commit lists the draft, the register and the baseline; a fold without a row refuses with `record FAIL`; `--dry` over a body change refuses and quotes the line; the walk-closing shape (per-walk line + `walks:`) is admitted; a deposited copy with `fold_baseline:` reads `LENS-ORDER OK` with `via fold_baseline` in its BASIS; without the pointer, `merged by time`; the mutation run's last line `n ≥ 14 killed, 0 survived, 0 error`; `git diff --stat main...HEAD` (three-dot) names exactly the seven Scope files; the four declared headings present with P3's cell whole; `depositor.py` and `cycle_check.py` unchanged.

## STEP 2 — QA (full suite + the real #265 record read from a deposit-shaped copy + the tool rehearsed on a scratch clone)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter ABSOLUTE; read-only beyond the two evidence files. ⛔ THE INCIDENT MANDATE: a deposit-shaped copy goes ONLY under `$T` — never under any real `knowledge/decisions/`; the daemon claims and dispatches within one second.
>
> **Item 1 — full suite, REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt 2>&1`; the summary line quoted in the receipt as `N passed` with the skip named as `one skip (test_gate_watcher, live-DB)` — never the word the Rule 20 block scans for.
> **Item 2 — the real record, read from a deposit-shaped copy:** `T=$(mktemp -d)`; `git init -q $T/proj && mkdir -p $T/proj/knowledge/decisions`; `cp /Users/marklehn/Developer/eluvian-governance/governance/knowledge/decisions/drafts/executable-bellows-lint-warn-to-fail.md $T/proj/knowledge/decisions/executable-bellows-lint-warn-to-fail.md`; commit it there once; then `scripts/lens_order_check.py $T/proj/knowledge/decisions/executable-bellows-lint-warn-to-fail.md` — expected rc 0 and, verbatim (P9's prototype line): `BASIS: source=draft+register, one log via fold_baseline (executable-bellows-lint-warn-to-fail.md) tier=T1 declared_walks=[1, 2, 3, 4] lens_commits=20 walks_with_lens_commits=[1, 2, 3, 4]` then `LENS-ORDER OK — 20 lens commit(s) across walks [1, 2, 3, 4]; each names one lens, ascending, and every closed walk carries its tier's full set`; then the negative control: the same copy with its `fold_baseline:` line deleted → rc 1, `merged by time`, `INCOMPLETE: walk 2`, `INCOMPLETE: walk 3` — the 2026-09-10 hold reproduced byte-for-byte in its verdict lines (P1). Paste both.
> **Item 3 — the tool rehearsed:** `git clone -q /Users/marklehn/Developer/eluvian-governance $T/gov` (a clone, never the live repo); in the clone, `scripts/lens_commit.py $T/gov/governance/knowledge/decisions/drafts/executable-bellows-lint-warn-to-fail.md --register $T/gov/governance/knowledge/research/walk-register-lint-warn-to-fail-2026-09-10.md --walk 5 --lens 1 --desc "qa rehearsal"` with `--dry`, AFTER appending `; w5 dry` to the clone's draft's LAST `- Weak spots:` line (the walk-4 line `- Weak spots:          w4 dry` — the 265 draft carries one such line per walk, panel E4; the real dry shape — an unchanged draft never appears in a commit's stat, panel D2) → expected: `LENS-COMMIT: commit OK — draft(executable-bellows-lint-warn-to-fail): walk 5 lens 1 — Weak spots: qa rehearsal`, the DRY line appended to the clone's register, and `git -C $T/gov show --name-only --format= HEAD` listing the draft, the register and the baseline (the scout ran the rehearsal on the shipped tool: the clone's draft reads BAR_MET and the tool reached step 5 — a baseline-only commit today; the three names are this plan's change). ⚠️ The clone's draft names the LIVE register by absolute ref, so the tool's step 3 (`cycle_check`) reads the live register while step 2 lints and step 5 writes the clone's — a mixed-repo rehearsal that writes nothing live (panel E8); say so in the receipt. Then the fold-without-row case, with `--walk 5 --lens 2` (lens 1 is now on the record — the duplicate guard would refuse it first, panel C4): append one line to the clone's draft body, register untouched, no `--dry` → `LENS-COMMIT: record FAIL — the register carries no row for walk 5 lens 2`, no commit; and `--dry --walk 5 --lens 2` over that same edit → `record FAIL — --dry, but the draft changed beyond its cycle record:` followed by the appended line. Paste all three.
> **Item 4 — production writes, stated exactly:** none outside the two evidence files; no lane file, no lifecycle row, no lifecycle import; the clone and every scratch dir removed (`[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`).
> **Item 5 — receipt** `knowledge/qa/evidence/lens-record-visibility-qa-receipt-2026-09-10.md`: `numstat` over the DEV commits (`<base>..<dev>`, seven files); a `## Verification` table with one row per Item 1–4, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`$ELUVIAN_WRAP_ROOT/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: lens-record-visibility-2026-09-10`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["lens-record-visibility-suite-2026-09-10.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the closing `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 6 — commit**, path-scoped and gated: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/lens-record-visibility-qa-receipt-2026-09-10.md knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt && git commit -F <msg-file> -- knowledge/qa/evidence/lens-record-visibility-qa-receipt-2026-09-10.md knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt`. ⛔ A QA step that finds it must change production code STOPS and requests a verdict (thread 262) rather than committing the change.
>
> **Deposits:**
> - `knowledge/qa/evidence/lens-record-visibility-qa-receipt-2026-09-10.md`
> - `knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/lens-record-visibility-qa-receipt-2026-09-10.md`
> - `knowledge/qa/evidence/lens-record-visibility-suite-2026-09-10.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 1 skipped` with N ≥ the DEV's count and no `failed`; Item 2's two runs pasted (OK with `via fold_baseline`; the reproduced hold without it); Item 3's three rehearsals pasted; the receipt's `## Verification` table has four rows and closes with the block's own PASSED line; one QA commit carrying the two evidence files.
