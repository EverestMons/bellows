# Bellows — Step-State Resume Design (BACKLOG #4 Phase 2)
**Date:** 2026-04-28 | **Tier:** Small | **Test Scope:** none | **Execution:** Step 1 (Bellows Systems Analyst)
**Priority:** 5

## Context

BACKLOG #4 Phase 1 diagnostic at `bellows/knowledge/research/step-state-persistence-map-2026-04-28.md` mapped the current state machine. Findings: the verdict-consumer path correctly tracks step state via filename parsing; the manual CEO-rename path (verdict-pending → executable, halted → executable) bypasses the consumer and dispatches Step 1 from scratch. The `runs` table records 473 rows of per-step history but is write-only.

Phase 1 enumerated three fix options (A: DB query in run_plan; B: watcher-side query; C: procedural elimination of manual rename) and 7 risk-surface unknowns spanning plan-hash drift, deposit verification, slug ambiguity, crash recovery, race semantics, halted-vs-complete distinction, and DB query reliability.

Phase 2 (this plan) is design-only. The SA evaluates each option against each unknown, recommends one path with scoped Phase 3 implementation effort, and surfaces any decisions that require CEO judgment before Phase 3 begins. NO code changes. The deliverable is an architecture document.

Test Scope: none — pure design.

## How to Run This Plan

Bellows watcher claims this plan automatically. Step 1 (Bellows Systems Analyst) reads Phase 1 findings + relevant source code, produces a design comparison document at `knowledge/architecture/`. Per disable-auto-close, terminal step pauses for Planner verdict. Single-step diagnostic per Rule 22.

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-step-state-resume-design-2026-04-28.md", "bellows/knowledge/decisions/in-progress-diagnostic-step-state-resume-design-2026-04-28.md")`. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. **Skip the domain glossary** — design and architecture work, no Bellows-specific domain vocabulary. **Mandatory first read:** `bellows/knowledge/research/step-state-persistence-map-2026-04-28.md` — this is the Phase 1 diagnostic this plan builds on. Treat its findings as canonical: do NOT re-investigate code paths it already mapped (`run_plan`, `_consume_verdicts`, `is_runnable_plan`, the `runs` table schema, the verdict filename parse). Re-read source files only if Phase 1's citation is insufficient for a design decision. **Investigation scope: produce a design comparison document.** Address the following sections in order. **Section 1 — Option Comparison Matrix.** For each of the three options Phase 1 enumerated (A: DB query in `run_plan()`; B: watcher-side query in `PlanHandler._handle()`; C: procedural elimination of manual rename), produce a row in a comparison table with columns: Coverage (which scenarios it handles — verdict-pending resume, halted resume, crash recovery, plan edited between steps), Code Cost (LOC estimate, files touched, schema changes if any), Procedural Cost (what discipline the CEO/Planner must adopt), and Failure Modes (what can go wrong in production). Be honest — the SA's Phase 1 deposit said Option A is "10 lines confined to run_plan" but the slug-ambiguity Q6 unknown implies a schema column addition; restate the LOC realistically. **Section 2 — Q6 Unknown Handling Per Option.** Take each of the 7 Q6 unknowns from Phase 1 verbatim (plan-hash drift, prior step deposit verification, multiple verdict-pending for same plan, orphaned in-progress crash recovery, DB query slug ambiguity, race condition on concurrent re-claim, halted-vs-complete distinction). For each unknown, state how each of A/B/C addresses it (or doesn't). Some unknowns may be option-independent (orphan recovery is its own bug regardless of which option is chosen). Flag those explicitly. **Section 3 — Hybrid options.** Phase 1 listed A, B, C as discrete. In practice, hybrids are possible: Option C as a near-term ship (cost zero) while Option A is being designed; Option A + a halted-distinction guard; Option C plus a separate "orphan recovery on startup" plan. Enumerate at least 2 plausible hybrids with their tradeoffs. **Section 4 — Recommendation.** Recommend ONE path forward. State the chosen option (or hybrid) explicitly. Justify the choice against the comparison matrix and the Q6 unknowns. State which Q6 unknowns are deferred to follow-up plans (with named follow-up plan slugs the Planner should track) versus which are addressed inline in Phase 3. **Section 5 — Phase 3 Scope.** Concrete implementation outline for the recommendation: which files change, which functions change (cite line numbers from Phase 1), what new tests are needed, what schema changes if any, what migration steps if any, how Bellows restart fits in. Estimate Phase 3 effort in hours and Phase 3 plan tier (Small/Medium/Large). **Section 6 — CEO decisions required before Phase 3.** Surface any choice that the Planner cannot resolve from this design alone — e.g., "should the runs table get a canonical slug column or should we accept slug-LIKE query fragility for now?", "should plan-hash mismatch on re-claim halt the plan or restart from step 1 with a warning?". Frame each as a binary or short-list choice with stated tradeoffs. The Planner takes Section 6 to the CEO between Phase 2 close and Phase 3 plan-writing. **Deliverable.** Use `Filesystem:write_file` to deposit the design at `bellows/knowledge/architecture/step-state-resume-design-2026-04-28.md`. Structure follows Sections 1–6 above. Include a final "Open Questions" section if anything emerged during design that wasn't covered by Phase 1. **Do NOT write any code. Do NOT modify any source files. Do NOT commit anything to git** — design document only. **Standard prompt feedback protocol** → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/architecture/step-state-resume-design-2026-04-28.md`
