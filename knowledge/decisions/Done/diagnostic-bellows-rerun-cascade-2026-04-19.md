# bellows — Diagnostic: Re-Run Cascade Characterization
**Date:** 2026-04-19 | **Tier:** Small (investigation) | **Test Scope:** n/a (diagnostic) | **Execution:** Step 1 (SA)
**Priority:** 1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS. The Planner reads the deposited findings file via direct tool call and performs Rule 22 verification.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bellows-rerun-cascade-2026-04-19.md. Execute Step 1 ONLY. After completing Step 1, STOP.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-bellows-rerun-cascade-2026-04-19.md", "bellows/knowledge/decisions/in-progress-diagnostic-bellows-rerun-cascade-2026-04-19.md")`. **Read your specialist file and skip glossary reads — this is a daemon-state and event-trace investigation.** At 18:14:32 on 2026-04-19, Bellows re-claimed and re-executed `diagnostic-verdict-lifecycle-coupling-2026-04-19.md` approximately 8 minutes AFTER the Planner had moved it from `decisions/` to `decisions/Done/`. The re-run cost $1.07, re-deposited identical findings, and failed gates with `passed=False, failures=2`. Separately, the file that ended up in `Done/` is named `verdict-pending-diagnostic-verdict-lifecycle-coupling-2026-04-19.md` (retaining the `verdict-pending-` prefix) instead of the intended `diagnostic-*` name. The Planner's move command was `source=decisions/verdict-pending-diagnostic-verdict-lifecycle-coupling-2026-04-19.md`, `destination=decisions/Done/diagnostic-verdict-lifecycle-coupling-2026-04-19.md` — the tool reported success. Five other `verdict-pending-diagnostic-*` plans are currently in `decisions/` and may be at the same risk. **The CEO will restart Bellows BEFORE this diagnostic runs to load the post-fix code from today's watcher-reliability and deposit-parser closed items; answer question (1) against the post-restart state.** Your job is to characterize what happened, not fix anything. **Investigation questions, in order:** **(1) Post-restart code state.** Read `bellows.py` and confirm the `PlanHandler` class has an `on_moved` method (closed BACKLOG #4, shipped 2026-04-19). Cite line range. Read `verdict.py` and confirm `extract_primary_deposit` has block-aware parsing that reads the `**Deposits:**` block form (closed BACKLOG #6, shipped 2026-04-19). Cite line range. Read `gates.py` (or wherever `_extract_plan_required_deposits` lives) and confirm the same — block-form preference over legacy prose regex. Cite line range. State explicitly: "post-fix code is loaded" or "pre-fix code is still loaded" for each of the three modules. **(2) Current filesystem state.** Enumerate `decisions/` (non-recursive) and `decisions/Done/` (non-recursive, names only, no need for full content). For `decisions/`, report every file name. For `decisions/Done/`, filter to any file whose name contains `verdict-lifecycle-coupling` and report full filename + size + mtime. Also enumerate `verdicts/pending/` and report any files. **(3) Trace the 18:14:32 re-run trigger.** Read the Bellows log file `logs/20260419-181432-step.json` (the run output) and any earlier log files from 18:06-18:15 window that mention `verdict-lifecycle-coupling`. What was the filename Bellows saw in `decisions/` when it claimed the plan for re-execution? What prefix did it have? Was the plan_path it invoked matching `executable-*`, `diagnostic-*`, `in-progress-*`, or `verdict-pending-*`? Cite specific log lines. If no 18:06-18:15 logs mention this slug, state that explicitly. **(4) is_runnable_plan audit.** Read `is_runnable_plan()` in `bellows.py` (function name should match; locate via grep) and report the exact prefix-rejection logic. Does it reject `verdict-pending-*` prefixed files? Does it reject files already inside a `Done/` subdirectory? Does it reject `in-progress-*` files? Quote the relevant conditions (up to 5 lines). Then cross-reference with question (3): given the filename Bellows saw when it re-claimed, why did `is_runnable_plan` return True for it? **(5) Rename-path audit.** If the Planner's move landed `verdict-pending-diagnostic-verdict-lifecycle-coupling-2026-04-19.md` in `Done/` (per question 2), but the PLANNER intended to land `diagnostic-verdict-lifecycle-coupling-2026-04-19.md`, trace the possible causes: (a) the Planner's move tool did exactly what was specified but Bellows then renamed the file back, or (b) Bellows ignored the Planner's move and performed its own move during the re-run using the pre-renamed filename, or (c) a race between the Planner's move and Bellows's own Done-rename logic. Cite any log entries, timestamps, or filesystem ordering evidence that supports one hypothesis over another. **(6) Risk assessment for the 5 other verdict-pending plans.** The 5 plans currently in `decisions/` are: `verdict-pending-diagnostic-bellows-deposit-path-formats-2026-04-18.md`, `verdict-pending-diagnostic-bellows-verdict-file-schema-2026-04-18.md`, `verdict-pending-diagnostic-extract-primary-deposit-shape-2026-04-19.md`, `verdict-pending-diagnostic-gates-deposit-parser-current-state-2026-04-19.md`, `verdict-pending-diagnostic-rule-26-gate-smoke-test-2026-04-19.md`. For each, check `verdicts/resolved/` for any corresponding `processed-verdict-*` or non-processed `verdict-*` files. If any have pending non-processed verdict files in `resolved/`, those plans are in the consumption queue and will be processed on next `_consume_verdicts` tick — which may be imminent post-restart. Report the list with per-plan classification: "stuck (no verdict in queue)" vs "will-process (verdict in resolved/)". **(7) Gate failure signature.** The re-run reported `failures=2`. Read the 18:14:32 log and any accompanying verdict-request file in `logs/` or `verdicts/resolved/` (if one was written) that surfaces which two gates failed and with what reasoning. Report verbatim gate-failure strings. **(8) Cheap sanity check on hypothesis.** State your best-guess causal chain in 3-5 sentences: what sequence of events between 18:06 and 18:14:32 led to the re-run. Identify the single strongest piece of evidence supporting the chain, and the single strongest piece of evidence AGAINST it. Do NOT recommend a fix. **Deposit format:** write findings to `bellows/knowledge/research/bellows-rerun-cascade-2026-04-19.md` as an 8-section report, one section per question. Each section answers the question with concrete code or log citations (cite line numbers and file paths; quote at most 5 lines per excerpt). End with a "Hypothesis Summary" subsection (question 8's content). **Write the deposit using the canonical Python file-write pattern:** `with open("bellows/knowledge/research/bellows-rerun-cascade-2026-04-19.md", "w") as f: f.write(content)` where `content` is a triple-quoted Python string defined before the open call. Do NOT use heredoc. Do NOT use `python3 -c`. Commit with message: `"docs: diagnostic — bellows re-run cascade characterization"`. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/bellows-rerun-cascade-2026-04-19.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to write any fix. Do NOT move the plan to Done. The Planner will read your findings and handle housekeeping per Rule 22.**
