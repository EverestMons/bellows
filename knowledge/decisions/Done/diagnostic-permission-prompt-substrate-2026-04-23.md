# Bellows — Permission Prompt Substrate Diagnostic
**Date:** 2026-04-23 | **Tier:** Diagnostic | **Test Scope:** targeted | **Execution:** Step 1 (SA) — single-step per Rule 22 v4.19

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The SA investigates and deposits findings. After Step 1 completes, the Planner reads the deposited findings file via Rule 22 verification before authorizing housekeeping.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-permission-prompt-substrate-2026-04-23.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT move the plan to Done.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-permission-prompt-substrate-2026-04-23.md", "bellows/knowledge/decisions/in-progress-diagnostic-permission-prompt-substrate-2026-04-23.md")`. You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. Skip the domain glossary — this is code-tracing and tooling investigation, no Bellows-specific domain vocabulary required. **Context.** The CEO has observed that permission prompts which routinely interrupt manual `claude` (interactive) sessions no longer appear to interrupt Bellows-dispatched `claude -p` sessions. Plans ship with less friction than manual bootstraps did. Three possible explanations: (1) Bellows auto-approves permission prompts via flag/config, (2) Bellows suppresses the prompts by swallowing output, (3) `claude -p` non-interactive mode handles permissions differently than interactive `claude` (auto-approve, auto-deny, or fail). We need to know which is true before deciding what to do about Failure 3 (move-to-Done gate ordering, BACKLOG top item) — because if permission prompts are being silently auto-approved, the verdict system is a lossy substitute for a signal that used to be real-time, and the correct fix may be restoring real-time visibility rather than reordering gates. **This diagnostic is single-step per Rule 22 v4.19 — no Step 2 consolidation. You investigate, deposit findings, report Complete. The Planner verifies via Rule 22 and handles housekeeping directly.** **Q5a — What does `claude -p` do with permission prompts? Reading-first, empirical branch if needed.** Start by reading: (i) Claude Code CLI documentation at https://docs.claude.com/en/docs/claude-code if reachable, or locally-cached docs, or the `claude --help` output captured via `claude --help 2>&1 | head -100`; (ii) any permission-mode or permission-related flags documented for `claude -p`. Identify: does `claude -p` have an explicit flag like `--permission-mode`, `--dangerously-skip-permissions`, `--allowed-tools`, or similar? What is the documented default behavior for permission prompts in non-interactive mode — auto-approve, auto-deny, fail-on-prompt, or something else? If documentation is clear and Q5b (below) reveals Bellows passes an explicit permission flag matching the documented behavior, STOP at reading — no empirical test needed. If documentation is ambiguous OR Bellows passes no explicit flag (relying on defaults), proceed to empirical branch: construct a minimal test plan file at `/tmp/permission-prompt-test-plan.md` containing a single step that attempts ONE of the following (pick whichever is lowest-risk and most-likely-to-prompt in interactive mode): (a) `Bash(curl https://example.com)` — network request, (b) `Write` to a path outside the current working directory like `/tmp/permission-test-marker.txt`, (c) a tool call to an MCP connector not in the agent's allowed tool set. Invoke `claude -p` directly (bypassing Bellows) on that plan: `claude -p --input /tmp/permission-prompt-test-plan.md 2>&1 | tee /tmp/claude-p-direct-output.txt` (or whatever the correct invocation flags are per step (i) above). Capture: did a permission prompt fire? Did it auto-approve, auto-deny, or fail? Did the action succeed or fail? Then run the same test plan THROUGH Bellows by depositing it to `bellows/knowledge/decisions/` with the proper prefix and letting Bellows dispatch it — capture `/tmp/claude-p-via-bellows-output.txt` from the Bellows runner log or terminal. Compare behavior. **Q5b — How does Bellows invoke `claude -p`?** Read `bellows/bellows.py` and any runner modules it imports (likely `bellows/runner.py` if it exists, or the subprocess invocation block in `bellows.py` itself). Identify: (i) the exact `claude -p` command-line Bellows constructs — all flags, all arguments, environment variables set, working directory; (ii) whether any permission-related flag is passed; (iii) whether Bellows writes a permission config file (e.g., `.claude/permissions.json` or similar) before invoking; (iv) what stdin, stdout, stderr handling Bellows uses — pipe, capture, tee, discard. Quote the exact subprocess call and surrounding context (10 lines above and below). If there is NO explicit permission flag and Bellows relies on `claude -p` defaults, note this explicitly — it means Q5a's empirical branch is required. **Q5c — What signals does Bellows see from `claude -p` output?** In the same code files, identify: (i) does Bellows parse `claude -p` stdout as text, or does it consume structured events (JSON lines, NDJSON)? (ii) are there any flags to `claude -p` that would emit machine-readable events (something like `--output-format json` or `--events`)? Check the Claude Code CLI docs for this. (iii) if structured events are available but Bellows is not consuming them, what would it take to switch — a flag change plus a parser, or a larger refactor? (iv) for each signal class — tool requests, permission prompts (granted), permission prompts (denied), judgment calls/pauses, commit events, file writes — classify as: OBSERVED AND LOGGED (Bellows sees and records), OBSERVED NOT LOGGED (Bellows sees but discards), NOT OBSERVED (signal does not reach Bellows), UNKNOWN (needs investigation). **Deliverable.** Deposit findings to `bellows/knowledge/research/permission-prompt-substrate-2026-04-23.md`. Structure the file as: (1) Executive Summary — 3-5 sentences answering "why did permission prompts go quiet under Bellows"; (2) Q5a findings — what `claude -p` does with permission prompts, with evidence (doc quotes, CLI help output, empirical test results if run); (3) Q5b findings — Bellows's exact invocation of `claude -p` with quoted code; (4) Q5c findings — signal observability table with 6 rows (one per signal class) and 4 columns (OBSERVED+LOGGED / OBSERVED-NOT-LOGGED / NOT-OBSERVED / UNKNOWN); (5) Implications — short section naming which of the three possible explanations (auto-approve, suppression, non-interactive defaults) is confirmed by the evidence, and what that implies for the verdict-as-structured-log direction the CEO described; (6) Follow-up questions — anything that surfaced during investigation that warrants a separate diagnostic. Do NOT propose fixes. Do NOT design a verdict-as-log system. Investigation and scoping only. **Canonical file write pattern.** Use `Filesystem:write_file` or `Desktop Commander:write_file` directly when writing the findings file. If using Python: `content = """..."""` then `with open("/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/permission-prompt-substrate-2026-04-23.md", "w") as f: f.write(content)`. No heredoc, no `python3 -c "..."` with embedded triple quotes. **Test scope: targeted.** No test suite run is part of this diagnostic. If the empirical branch is triggered and you deposit a test plan to Bellows's watched directory, that is itself a live test — clean up afterward by moving the test plan to `bellows/knowledge/decisions/Done/` manually. **Do NOT commit anything to git during this diagnostic.** Investigation only. **Prompt feedback.** Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
>
> - `bellows/knowledge/research/permission-prompt-substrate-2026-04-23.md`
>
> **STOP. Do NOT proceed further. The Planner will verify the findings file via Rule 22 and perform housekeeping directly — this is a single-step diagnostic.**
