# bellows — THE PER-STEP SCOPE ARM, WARN FIRST: `scope_check` keeps its union rule and gains a second reading that names every changed file declared only by an EARLIER step — a `scope_step` row in the verdict request (WARN, never a failure), a `scope_step` row in the gate ledger carrying the paths, so the FAIL threshold is decided from a rate the ledger now records; the template sentence for a QA step that must touch production is the CEO's doctrine act (thread 262, after #100071's census)

**Date:** 2026-09-11 | **Project:** bellows | **Tier:** Small (one gate arm, one verdict row, one ledger row, one test file plus one list edit, one mutation manifest, one dev-log) | **Dispatch Mode:** bellows | **Test Scope:** full-suite (Rule 21 — `gates.py`, `verdict.py` and `lifecycle.py` are imported by most of the suite; `pytest tests/ -q`) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 262 | **cycle_tier:** T1

**auto_close:** false

**Post-close:** daemon restart owed — `gates.py`, `verdict.py` and `lifecycle.py` are daemon-loaded; the CEO restarts after the merge. Doctrine, the CEO's act, one sentence in PLANNER_TEMPLATE §8 (the QA closeout rules, `:521`): "A QA step that must change production code STOPS — it commits nothing under the DEV's Scope, states the change it needs in its Output Receipt, and pauses for a verdict; the DEV's record (dev-log, mutation run) is the one that has to move (thread 262, #100061)." The WARN → FAIL threshold is 262's third act, decided from the ledger's `scope_step` rows once they cover a stated number of plans — not here.

**Depends on:** thread 262 and its rule "evidence-first: the corpus measurement before the arm"; the measurement is `Done/diagnostic-100071.md` (2026-09-11): over the gate's own per-step diff record, ONE of 37 QA steps committed a genuine edit to a file only an earlier step declared (#100061 step 2, `scripts/close_cycle.py`), residue 0, the ledger's six `scope_check` fails all on `tests/` files; the per-step arm's would-be WARN count over both arms (18 of 95) is inflated by the `commits` table's attribution (thread 275) and is not the number this plan rests on. Clone origin by kind and layout: `Done/executable-100073.md` (this morning): a two-commit DEV with the manifest riding the first, failing-first, the mutation run redirected, `check_deposit.py --expect-missing` at the first commit, the one-token status cell, the thread-262 stop; STEP 2 a full-suite QA with a receipt.

**Tier computed (§1):** **T1** — T-1 fires (a gate, the verdict request's table, the lifecycle ledger); T-6 does not (the doctrine sentence is the CEO's, after close); T-2 does not (no schema change — the ledger row uses the existing `pass` result with a `reason_code`).

## CEO Context

Thread 262 named the mechanism and asked for the rate before the arm; #100071 gave the rate: one genuine instance in the whole recorded corpus, and every other thing a per-step arm would flag is a QA step appending the DEV's own evidence files — files the DEV step declared and the QA step is told to write. A FAIL arm today would block the shop's standard QA shape on every plan for one real case. So the arm ships as a READING, not a gate: `scope_check` keeps its union rule and its pass/fail verdict unchanged, and beside it the gate names, for the step just run, every changed file that clears the union only through an earlier step's declaration. The verdict request shows the list as a WARN row the Planner reads at (b); the ledger records the paths on a `scope_step` row for every step, so the next census is one query and the FAIL threshold is a ruling on a number. The one design choice this plan makes for you: the `gate_events.result` column admits only `pass` and `fail` (a CHECK constraint, P3), and a schema change is a migration; the ledger row is written as `pass` with the paths in `reason_code` — the row's meaning is "the gate passed; these files were declared by an earlier step only" — and a `warn` result value is left as an open fork.

## What this changes

1. **`gates.py`** — `_gate_scope_check(plan_text, step_number, files_changed, failures, project_path=None, warnings=None)`: after the union verdict (unchanged, `:1133–1228`), when `declared` is true and `step_number > 1`, build the OWN-step declared set the same way the union is built (P1: `_extract_plan_scope` and `_extract_deposits_block_paths` over `_extract_step_text(plan_text, step_number)` alone, the P14 absolute-path normalization applied) and, for every `fpath` that cleared the union and is not allowlisted, test it against the own-step set with the SAME match (`d == fpath or ("/" in d and d.split("/", 1)[1] == fpath)`, the prefix arm) — a file that clears the union but not the own-step set is appended to `warnings` as `{"gate": "scope_step", "evidence": "<fpath>"}` (one entry per file). A file the union FAILS is never warned (the fail already names it). `check()` creates `warnings = []`, passes it, and returns it as a new key `"warnings"` in the result dict (P2) — `"passed"` is computed from `failures` alone, untouched. Step 1 never warns (own = union by construction); an undeclared plan (legacy prose arm) never warns. A later step that declares NO Scope and NO Deposits of its own while an earlier step declared (`_extract_step_text` present, both extractors empty) has an EMPTY own-step set, so every union-cleared file it changed is warned — the reading is correct (nothing of its own was declared) and t12 pins it, so the behaviour is a fact of the arm, not a surprise at a verdict.
2. **`verdict.py`** — `_build_verification_results_table`: one new row directly after `scope_check` in `_KNOWN_GATES`, `("scope_step", None, None)`, handled like the two display-only rows (`qa_step_detection`, `file_change_audit`, P2): `| scope_step | WARN | N file(s) declared by an earlier step only: a, b |` when `gate_result.get("warnings")` carries `scope_step` entries, else `| scope_step | PASS | Every changed file is in this step's own Scope or Deposits |`. The row never reads FAIL and never touches `passed`.
3. **`lifecycle.py`** — `record_gate_events`: `"scope_step"` appended to `standard_gates` (P3) so every step gets a row; the row is `pass` with `reason_code = "earlier-step-only: a, b"` when the result's `warnings` carry `scope_step` entries, else `pass` with `reason_code NULL` — the existing CHECK admits it, no schema change. The census query for the rate becomes `SELECT count(*) FROM gate_events WHERE gate_name='scope_step' AND reason_code IS NOT NULL`.
4. **`tests/test_scope_step.py`** — new, one file, a declared-mode two-step fixture (`STEP 1` Scope `gates.py`, `verdict.py`; `STEP 2` Scope `knowledge/qa/evidence/r.md`; Deposits blocks as the real plans carry them): (t1) step 2 changing `knowledge/qa/evidence/r.md` → `passed True`, `warnings == []`; (t2) step 2 changing `gates.py` → `passed True`, no `scope_check` failure, `warnings == [{"gate": "scope_step", "evidence": "gates.py"}]`; (t3) step 2 changing `totally_unexpected.py` → `scope_check` FAIL and `warnings == []` (fail dominates); (t4) step 1 changing `gates.py` → `warnings == []`; (t5) step 2 changing `.gitkeep` → neither; (t6) the legacy fixture `PLAN_TEXT` of `tests/test_gates.py` (no Scope block, P4) at step 2 → `warnings == []`; (t7) `_build_verification_results_table` with a `scope_step` warning → the WARN row with the file name, `scope_check` still PASS; (t8) without → the PASS row; (t9) `record_gate_events` with the warning → a `scope_step` row `('pass', 'earlier-step-only: gates.py')`; (t10) without → `('pass', None)`; (t11) `record_gate_events` with a `gate_result` that has NO `warnings` key at all (an old-shaped result) → the `scope_step` row `('pass', None)`, no raise; (t12) a fixture whose STEP 2 carries no Scope and no Deposits block while STEP 1 declares `gates.py` — step 2 changing `gates.py` → `passed True`, `warnings == [{"gate": "scope_step", "evidence": "gates.py"}]` (the empty own-step set).
5. **`tests/test_gate_transaction_mechanization.py`** — `STANDARD_GATES` gains `"scope_step"` after `"scope_check"` (its two assertions count rows against the list, P4).
6. **`knowledge/mutants/scope-step-warn.json`** — five mutants: (m1) the arm appends to `failures` instead of `warnings` → t2 (`passed` would be False); (m2) the own-step set built from ALL steps (the arm can never fire) → t2; (m3) the verdict row always PASS → t7; (m4) `record_gate_events` writes `reason_code NULL` regardless → t9; (m5) the fail-dominance check removed (a union-failed file also warned) → t3.
7. **Not changed:** the union rule and its verdict; `SCOPE_ALLOWLIST`; `bellows.py` (it already passes `gate_result` whole to `post_verdict_request` and `record_gate_events`, P6); the `gate_events` schema; `status.py`; every other gate.

## Why this exists

The census showed the arm's cost and its yield: one real case, many evidence-file appends. A reading the Planner sees at every verdict and a ledger row the next census can count is what "WARN first" means; the FAIL is a later ruling on a recorded rate, not a guess.

## What this does NOT do

- Fail any step. `passed` is unchanged; the union rule stands.
- Change the `gate_events` schema (a `warn` result value is an open fork).
- Write the PLANNER_TEMPLATE sentence (the CEO's act, quoted in Post-close).
- Decide the FAIL threshold.

## MUST-PRESERVE

- ⛔ **`passed` is computed from `failures` alone.** Nothing the arm produces reaches `failures`; a test asserts `passed is True` beside a non-empty `warnings`.
- ⛔ **The own-step set is built by the gate's own extractors and matched by the gate's own rule** — no second matcher; the arm's code sits beside the union's and reuses its helpers.
- ⛔ **Fail dominates:** a file the union fails is named by the failure only.
- ⛔ **No schema change; no live DB write from the plan** (every test under `conftest.isolate_lifecycle_db`).
- ⛔ **Every `expect_fail` is a class-qualified node id where the test is a method**, and the mutation run is redirected into its deposit, never piped.
- ⛔ **The thread-262 stop:** a QA step that must change production code STOPS and requests a verdict rather than committing.

## Numbers discipline — measured 2026-09-11 by the Planner (bellows `c1ba53f`, governance `00c00fd0`, daemon pid 30156)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the gate's rule, quoted | `gates.py:1127` `def _gate_scope_check(plan_text, step_number, files_changed, failures, project_path=None):`; `:1133` `for s in range(1, step_number + 1):`; `:1145–1147` the Scope union; `:1152–1158` `declared_files` ∪ Deposits (`_extract_deposits_block_paths`); `:1161–1170` the P14 normalization of absolute declared entries against `project_path`; `:1180–1182` `SCOPE_ALLOWLIST` / `SCOPE_ALLOWLIST_PREFIXES`; `:1189` `d == fpath or ("/" in d and d.split("/", 1)[1] == fpath)`; `:1193–1198` the prefix arm; `:1203–1219` the legacy prose/directory arms (undeclared plans); `:1223–1228` `failures.append({"gate": "scope_check", "evidence": f"out-of-scope files: … | plan step context: …{scope_note}"})`; the extractors `_extract_step_text` :565, `_extract_plan_scope` :643, `_extract_deposits_block_paths` :1103 | `sed -n 1127,1230p gates.py` |
| P2 | ⛔ the result dict and the table | `gates.check` (`:270`) returns `{"passed": len(failures) == 0, "failures", "is_qa_step", "files_changed", "plan_header", "verdict_requested"}` (`:322–329`); `_gate_scope_check` is Gate 8 at `:310`; `verdict._build_verification_results_table(gate_result, parsed, step_number, total_steps, intermediate_decisions=None)` (`:105`) with `_KNOWN_GATES` (`:112–128`, 15 tuples, `scope_check` the 8th) and two display-only rows (`qa_step_detection`, `file_change_audit`, `failure_gate None`), the `intermediate_decisions` INFORMATIONAL row last (`:177–179`); `bellows.py` passes `gate_result` whole to `verdict.post_verdict_request` (`:1286`, `:1422`, `:1463`) | `sed -n 270,330p gates.py`; `sed -n 105,180p verdict.py` |
| P3 | ⛔ the ledger | `lifecycle.record_gate_events(step_id, gate_result, db_path=None)`: `fail` rows from `failures`, then `pass` rows for the 12 `standard_gates` not in the failure set (`receipt_status`, `no_errors`, `no_permission_denials`, `deposit_exists`, `scope_check`, `rule_20_self_check`, `rule_22_verification`, `qa_test_result`, `quoted_test_nodes_exist`, `mutation_result`, `qa_nodes_match_suite`, `dev_log_declared_text`); `gate_events` schema `:130–138`: `result TEXT NOT NULL CHECK (result IN ('pass', 'fail'))`, `reason_code TEXT`; the one migration precedent is `ALTER TABLE plans ADD COLUMN plan_doc_ref TEXT` (`:57`) — a CHECK cannot be altered in SQLite without a table rebuild; the live ledger: `scope_check` pass 117 / fail 6 at #100071 | `grep -nF 'def record_gate_events' -A40 lifecycle.py`; `sed -n 130,138p lifecycle.py` |
| P4 | ⛔ the tests to clone and the lists to extend | `tests/test_gate_transaction_mechanization.py:15–28` `STANDARD_GATES` (12 names) with `assert len(rows) == len(STANDARD_GATES)` (`:74`) and `assert total == len(STANDARD_GATES)` (`:108`); `tests/test_gates.py:22–33` `PLAN_TEXT` — two steps, NO Scope block (legacy prose arm; `test_scope_check_passes_when_files_in_plan` `:176` relies on the file names appearing in the prose); `tests/test_lifecycle.py` `TestRecordGateEvents.test_records_pass_and_fail` (`:445`, the row-reading shape `SELECT gate_name, result, reason_code FROM gate_events WHERE step_id = ?`); `conftest.isolate_lifecycle_db` `:31`; no test today calls `_build_verification_results_table` directly (`grep -ln` over `tests/` → `test_gates_verdict_pause.py`, `test_gates_evidence_correspondence.py`, `test_verdict.py` name the table's strings, none the function) | the files at those lines |
| P5 | ⛔ the measurement this plan rests on | `Done/diagnostic-100071.md` and `governance/knowledge/research/qa-scope-census-2026-09-11.md`: the gate's own per-step record (`step_files`, 157 rows, 35 steps, 20 plans) — own 153, earlier-step-only 1 (`100061` step 2 `scripts/close_cycle.py`, `d6da69b`), undeclared 3 (the ledger's fails, overridden); Q3 rate **1 of 37** QA steps; residue 0; the ledger's six `scope_check` fails all `tests/` files; Q5's 18 of 95 rests on the `commits` arm and is inflated by thread 275's attribution — not used here | the doc's Q1, Q3, Q4 |
| P6 | ⛔ the call sites the arm rides | `lifecycle.record_gate_events(_lc_step_id, gate_result)` at `bellows.py:1220` and `:1379`, immediately after `record_step_end`; `verdict.post_verdict_request(…, gate_result, …)` at `:1286`, `:1422`, `:1463` — `gate_result` passes whole, so a new key reaches both consumers with no daemon edit | `grep -n 'record_gate_events\|post_verdict_request' bellows.py` |
| P7 | in-flight; class; interpreter | queue empty at drafting (#100073 Done at 08:2x, the daemon restarted on `c1ba53f`, pid 30156); writes: `gates.py`, `verdict.py`, `lifecycle.py`, two test files, the manifest, the run file, the dev-log, two QA evidence files → **shop-infra** (`_assign_class`, walk 0) — HOLDS at admission, the CEO releases; Python 3.9.6, the bellows venv | `python3 status.py`; `_assign_class` |

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-scope-step-warn-2026-09-11.md
**Walks:** walk 0 pinned (P1–P7 measured on bellows `c1ba53f`; clone-diff against `Done/executable-100073.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py`; the tool run bare, its exit read; no lens or walk number in any `--desc`; `lens_order_check` after every walk.

- Weak spots:          w1 1 folded — instruction 1 / record 0; w2 dry
- Destruction:         w1 dry; w2 dry
- Vulnerabilities:     w1 dry; w2 dry
- Integration-record:  w1 dry; w2 dry
- ACID:                w1 dry; w2 dry
**Closing:** WARM close after walk 2 — BAR MET (T1), thread 262's arm after its census. Two walks: 1 → 0 (walk 1 stated the empty own-step case and pinned it; walk 2 dry on every lens). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-`; HOLDS on class shop-infra, the CEO releases; the daemon restart after the merge and the PLANNER_TEMPLATE sentence are the CEO's acts; the FAIL threshold is 262's third act, from the ledger's `scope_step` rows.

## Cycle Manifest
tier: T1
target: gates.py
class: shop-infra
reads: gates.py, verdict.py, lifecycle.py, bellows.py, tests/test_gates.py, tests/test_gate_transaction_mechanization.py, tests/test_lifecycle.py, tests/conftest.py, knowledge/decisions/Done/executable-100073.md, knowledge/decisions/Done/diagnostic-100071.md
writes: gates.py, verdict.py, lifecycle.py, tests/test_scope_step.py, tests/test_gate_transaction_mechanization.py, knowledge/mutants/scope-step-warn.json, knowledge/mutants/scope-step-warn.run.txt, knowledge/development/dev-log-scope-step-warn-2026-09-11.md, knowledge/qa/evidence/scope-step-warn-qa-receipt-2026-09-11.md, knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt
mutants: knowledge/mutants/scope-step-warn.json
open_forks: 1. the FAIL threshold — a ruling on `SELECT count(*) FROM gate_events WHERE gate_name='scope_step' AND reason_code IS NOT NULL` once the rows cover a stated number of plans (thread 262's third act); 2. a `warn` value in `gate_events.result` (a table rebuild — SQLite cannot alter a CHECK) if the `pass`-with-`reason_code` form proves unreadable; 3. the QA-shape exception — a QA step's writes to `knowledge/mutants/` and the DEV's dev-log declared by the DEV step are the arm's expected WARNs (#100071 Q5); whether the arm exempts `knowledge/` by rule is decided with the threshold, from the rows
walks: 2
yields: 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:13
coherence: 2/2 body walks named in the register (6 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-scope-step-warn.md.foldcheck.json

---

## STEP 1 — DEV (one gate arm, one verdict row, one ledger row, twelve tests; the mutation run; two commits)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ Never run the daemon, `run_plan`, a claim; the live `lifecycle.db` is never opened by this step; `T=$(mktemp -d /tmp/scope-step.XXXXXX)` for scratch, removed at the end.
>
> **Scope:**
> - `gates.py`
> - `verdict.py`
> - `lifecycle.py`
> - `tests/test_scope_step.py`
> - `tests/test_gate_transaction_mechanization.py`
> - `knowledge/mutants/scope-step-warn.json`
> - `knowledge/mutants/scope-step-warn.run.txt`
> - `knowledge/development/dev-log-scope-step-warn-2026-09-11.md`
>
> **Item 1 — re-derive P1, P2, P3 and P4 and HALT on a mechanism mismatch** (a line-number drift is not a mismatch; a `warnings` key already in `check()`'s result, a `scope_step` name already in `_KNOWN_GATES` or `standard_gates`, or a `result` CHECK that already admits `warn`, IS one — someone shipped it first). Paste each pin's re-derived line beside it.
> **Item 2 — write the failing tests FIRST:** `tests/test_scope_step.py` t1–t12 as *What this changes* 4 (red: t2 `KeyError: 'warnings'` or `AttributeError` on the missing row; t7/t8 no `scope_step` row in the table; t9–t11 no `scope_step` row in `gate_events`); the `STANDARD_GATES` edit of *What this changes* 5 (red: `len(rows) == 13` against 12 rows written). Run the two files; paste the red summary lines.
> **Item 3 — the edits** as *What this changes* 1–3; then the two files green (t1–t12 plus the mechanization tests), then the FULL suite green (`N passed, 1 skipped` — `test_gate_watcher`'s live-DB skip is the one).
> **Item 4 — first commit**, gated on the FULL suite AND the pre-check with the stage flag (#100067), path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" --expect-missing knowledge/development/dev-log-scope-step-warn-2026-09-11.md knowledge/mutants/scope-step-warn.run.txt && git add gates.py verdict.py lifecycle.py tests/test_scope_step.py tests/test_gate_transaction_mechanization.py knowledge/mutants/scope-step-warn.json && git commit -F <msg-file> -- gates.py verdict.py lifecycle.py tests/test_scope_step.py tests/test_gate_transaction_mechanization.py knowledge/mutants/scope-step-warn.json` — six files (the manifest rides this commit so the mutation run's `HEAD:` archive contains it); the message tagged with the plan id and `thread 262`.
> **Item 5 — the mutation run, REDIRECTED into `knowledge/mutants/scope-step-warn.run.txt` (a Deposit):** `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/scope-step-warn.json > knowledge/mutants/scope-step-warn.run.txt 2>&1`; the last line must read `MUTATION: 5 killed, 0 survived, 0 error` — a survivor is a test to write, not a mutant to delete.
> **Item 6 — the dev-log** `knowledge/development/dev-log-scope-step-warn-2026-09-11.md` under the four headings declared below; `## Pins re-derived (P1, P2, P3, P4)` opens with P5's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `not used here`; paste through those words, then stop. Under `## Failing-first (red, then green)` the red lines and the green line; under `## The arm observed (t2, t7, t9)` the three values — the `warnings` list, the table row, the ledger row — pasted from the tests' output; under `## Mutation run` the run file's last line. **Second commit**, gated on the FULL suite and the pre-check bare (`… tools/check_deposit.py … 1 --wt "$(git rev-parse --show-toplevel)"`, no stage flag — the run file and dev-log now exist), path-scoped to the run file and the dev-log. Last act: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P1, P2, P3, P4)`; `## Failing-first (red, then green)`; `## The arm observed (t2, t7, t9)`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P2, P3, P4)` ← P5
>
> **Deposits:**
> - `knowledge/mutants/scope-step-warn.json`
> - `knowledge/mutants/scope-step-warn.run.txt`
> - `knowledge/development/dev-log-scope-step-warn-2026-09-11.md`
>
> **Post-conditions:** t1–t12 green and the mechanization tests green with 13 standard gates; the full suite `N passed, 1 skipped` with no `failed`; the mutation run's last line `MUTATION: 5 killed, 0 survived, 0 error`; two DEV commits — the first carrying the six files, the second the run file and the dev-log; the dev-log's four headings present as full lines with P5's cell whole; `git status --porcelain` empty after the second commit; no code path in this step opens the live DB (every test runs under `conftest.isolate_lifecycle_db`).

## STEP 2 — QA (full suite; the arm read on THIS plan's own step-1 gate; the receipt)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter ABSOLUTE; read-only beyond the two evidence files; `T=$(mktemp -d /tmp/scope-step-qa.XXXXXX)`.
>
> **Item 1 — the full suite REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt 2>&1`; the summary line quoted in the receipt as `N passed` with the skip named as `one skip (test_gate_watcher, live-DB)` — never the word the Rule 20 block scans for.
> **Item 2 — the arm, read from outside pytest on THIS plan:** the daemon that ran STEP 1 loaded the OLD `gates.py` (the restart is owed after close), so STEP 1's verdict request carries no `scope_step` row — state that, then produce the row the NEW code would write: `"$T"` holds a copy of this plan's own text (`cp "$(git rev-parse --show-toplevel)/knowledge/decisions/in-progress-executable-<id>.md" "$T/plan.md"` — the in-progress file is the plan's own text, read only) and the STEP 1 file list from `git diff --name-only <base>..<dev>`; run, in-process from the worktree root, `gates.check(<a clean parsed dict as tests/test_gates.py's _clean_parsed builds it>, open("$T/plan.md").read(), 2, "<worktree root>", files_changed=<that list>)` and paste `result["warnings"]` and `result["passed"]` — on this plan the STEP 1 files (`gates.py`, `verdict.py`, `lifecycle.py`, the tests, the manifest) are STEP 1's Scope, so read at step 2 they are the arm's own first WARN list: every one of them `earlier-step-only`, `passed True`. Then `verdict._build_verification_results_table(result, {}, 2, 2)` and paste its `scope_step` row. This is the plan reading itself through the arm it ships — a call into the module under test, not a run of the daemon.
> **Item 3 — production writes, stated exactly:** none outside the two evidence files; `$T` removed; no lane file, no live lifecycle row, no daemon.
> **Item 4 — receipt** `knowledge/qa/evidence/scope-step-warn-qa-receipt-2026-09-11.md`: `numstat` over the DEV commits (`<base>..<dev>`, eight files); a `## Verification` table with one row per Item 1–3, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`$ELUVIAN_WRAP_ROOT/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: scope-step-warn-2026-09-11`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["scope-step-warn-suite-2026-09-11.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the closing `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 5 — commit**, path-scoped and gated: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/scope-step-warn-qa-receipt-2026-09-11.md knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt && git commit -F <msg-file> -- knowledge/qa/evidence/scope-step-warn-qa-receipt-2026-09-11.md knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt`. ⛔ A QA step that must change production code STOPS and requests a verdict (thread 262).
>
> **Deposits:**
> - `knowledge/qa/evidence/scope-step-warn-qa-receipt-2026-09-11.md`
> - `knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/scope-step-warn-qa-receipt-2026-09-11.md`
> - `knowledge/qa/evidence/scope-step-warn-suite-2026-09-11.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 1 skipped` with N ≥ the DEV's count and no `failed`; Item 2's `warnings` list names every STEP 1 file, `passed True`, and the table's `scope_step` row reads WARN with their count; the receipt's `## Verification` table has three rows and closes with the block's own PASSED line; one QA commit carrying the two evidence files.
