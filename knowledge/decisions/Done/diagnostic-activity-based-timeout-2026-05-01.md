# Bellows — Activity-Based Timeout Diagnostic
**Date:** 2026-05-01 | **Tier:** Diagnostic (single-step) | **Test Scope:** targeted | **Execution:** Step 1 (SA)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS — this is a single-step diagnostic plan, the Planner performs Rule 22 verification on the deposited findings file and authorizes the move-to-Done.

Bootstrap prompt:

```
Read the plan at bellows/knowledge/decisions/diagnostic-activity-based-timeout-2026-05-01.md. Execute Step 1 ONLY. After completing Step 1, STOP. Do not move the plan to Done — the Planner does that after verification.
```

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-activity-based-timeout-2026-05-01.md", "bellows/knowledge/decisions/in-progress-diagnostic-activity-based-timeout-2026-05-01.md")`. **You are the Bellows Systems Analyst. Skip specialist file and glossary reads — this is a code-tracing + empirical analysis task.** **Background.** BACKLOG entry `2026-04-17: activity-based timeout` — runner.py uses a fixed 1200s timeout that kills agents mid-work on large plans even when they're actively committing. The fix needs to be inactivity-based: monitor `claude -p` stdout, reset a rolling inactivity timer on each new event, only timeout on true silence. The CEO has deferred two design decisions (hard wall-clock ceiling y/n and value; whether `rate_limit_event` resets the timer) pending data from this diagnostic. Stream-JSON shipped 2026-04-23 — agents now emit NDJSON events continuously, making per-event activity tracking feasible. **Q1 — Map the current runner.py timeout mechanism.** Read `bellows/runner.py` in full. Document: (a) the function signature and call surface of `run_step()` — what does `bellows.py` pass in, what does it get back; (b) the subprocess invocation pattern — is it `subprocess.run` with `timeout=`, `subprocess.Popen` + `communicate(timeout=)`, or a manual polling loop; (c) where exactly the 1200s timeout is enforced (line numbers, variable names, exception handling on timeout); (d) what state is captured/lost when timeout fires (cost, partial output, log file, return dict shape); (e) whether stdout is currently read line-by-line (NDJSON-aware) or as one blob at end of process; (f) any existing inactivity-related variables (`last_output_time`, heartbeat, etc.) — Phase 6 work mentioned a "60s heartbeat" canary in `bellows.py` but confirm whether `runner.py` itself has any inactivity awareness. **Q2 — Empirical event-cadence analysis (post-stream-JSON only).** Logs at `bellows/logs/*.json` — only files with timestamps `>= 20260423` are post-stream-JSON. Pre-2026-04-23 logs are single-blob JSON and irrelevant for this analysis. Sample the **20 longest-running successful post-stream-JSON step logs** (criterion: largest file size as a proxy for duration and event count, AND `is_error=false` in the terminal `result` event AND `stop_reason=end_turn`). For each sampled log: (a) count total NDJSON events; (b) identify event types present (system/init, assistant, user, rate_limit_event, result); (c) compute inter-event intervals using each event's timestamp field if present, else fall back to the log file's mtime as the upper bound for total duration and report that limitation; (d) report the max inter-event gap observed within each log; (e) tabulate `rate_limit_event` occurrences — count per log, duration of each rate-limit pause if inferrable, and whether other events interleave during a rate-limit pause or whether the agent goes silent. **Output format for Q2:** one summary table with columns `| log_filename | total_events | duration_estimate | max_inter_event_gap_s | rate_limit_event_count | rate_limit_max_pause_s | notes |`. Then a histogram (text-based, e.g. `0-10s: ████████, 10-30s: ████, 30-60s: █, 60s+: 0`) of inter-event gap distribution across all sampled logs combined. **Q3 — Find the wall-clock data point.** Among the 20 sampled logs, what is the longest legitimate run duration (lower-bound estimate using mtime if event timestamps unavailable)? This is the data point for the "do we need a hard ceiling, and at what value" decision. Also note: any log in the entire post-2026-04-23 corpus where the run was killed by the 1200s timeout — search for `timeout` in the log content or `stop_reason` values that indicate forced termination. **Q4 — Design recommendation.** Based on Q2 data, propose: (a) inactivity threshold value (with empirical justification — e.g., "P99 inter-event gap is X seconds, recommend threshold of 2X"); (b) whether to keep a hard wall-clock ceiling — if yes, propose a value with justification (e.g., "longest legitimate run was X minutes, propose ceiling at 2X"); if no, justify why purely inactivity-based is safe; (c) whether `rate_limit_event` should reset the inactivity timer — answer based on Q2(e) data on whether agents go silent during rate limits (if yes, rate_limit_event is the only signal of "still alive but throttled" and SHOULD reset; if other events interleave, agent isn't silent and rate_limit_event treatment doesn't matter); (d) sketch of code changes needed in runner.py — pseudocode shape, NOT line-by-line implementation. Include explicit "what stays the same" notes (e.g., "log file path, return dict shape, cost reporting on terminate"). **Q5 — Risks and edge cases.** Enumerate what could go wrong with the proposed design: (a) agent stuck in a tool call that produces no NDJSON output for >threshold but is legitimately working — does this happen, and how would we detect it; (b) network hang vs agent hang — can we distinguish, does it matter; (c) interaction with existing 1200s call sites — is the timeout passed in by `bellows.py`, and if so does the new design replace that parameter or layer alongside it; (d) any other failure modes the empirical data suggests. **Output.** Deposit findings to `bellows/knowledge/research/activity-based-timeout-diagnosis-2026-05-01.md`. Use the canonical Python file write pattern (`with open(...) as f: f.write(content)` after defining `content` as a triple-quoted string variable) — NO heredoc. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/research/activity-based-timeout-diagnosis-2026-05-01.md`
>
> **STOP.** Do NOT move the plan to Done. The Planner reads the deposited findings file, performs Rule 22 verification, and authorizes the move-to-Done.
