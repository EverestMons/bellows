# bellows — Cycle Manifest stanza design + §3 amendment gap-assessment
**Date:** 2026-08-19 | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only design diagnostic; produces the grammar + Rule 27 gap table an executable implements) | **Execution:** Step 1 (DIAG) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_step_1

## Context

Component 2 of the routed cycle-automation proposal (§4). The CEO ratified the `## Cycle Manifest` stanza FORMAT on 2026-08-19: a fixed `key: value` block emitted at BAR_MET carrying `tier · target · class · reads · writes · open_forks · walks · yields · validation · coherence`, where `reads:`/`writes:` are explicit path lists (enabling the depositor's collision checks), `class:` (`read-only | governed-tooling | register-writing`) drives auto-deposit vs HOLD, `validation:` is COMPUTED (the depositor re-runs it), and the stanza is compacting (replaces overrun Closing/STATUS prose). This read-only diagnostic characterizes the current §3 Cycle Log stanza across the corpus, finalizes the exact grammar (edge cases), and produces the Rule 27 Gap Assessment the executable implements to amend `DRAFTING_CYCLE.md §3` and wire the emitter/validator — WITHOUT re-verification (T-7).

**This is a read-only diagnostic.** It reads the ROOT doctrine file `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md §3` (absolute path — governance-class file, outside the bellows tree), the `bellows/knowledge/decisions/Done/*.md` corpus, and `bellows/scripts/cycle_check.py` + `plan_lint.py`. It changes no code and deposits one findings file. The `DRAFTING_CYCLE.md` amendment itself is the DOWNSTREAM executable's job (governance in-place dispatch, absolute operands).

## Drafting Cycle
**Tier:** T1 — triggers: T-7 (the §3-amendment executable builds on this diagnostic's grammar + gap table), T-8 (novel). Read-only diagnostic → no cold panel; the doctrine amendment it DESIGNS is a T-5/T-6 surface and earns its own cold-panel consideration downstream.
**Adoption note:** cycle_check.py (component 1) is dogfooded on THIS plan's drafting cycle — `python3 bellows/scripts/cycle_check.py <this plan>` is run after each walk and its verdict recorded in the register, replacing the manual "walk N" gate.
**Walk 0 (context pin):** targets `/Users/marklehn/Developer/GitHub/DRAFTING_CYCLE.md` §3 (the stanza doctrine to amend, ~111KB file — read §3 specifically), the Done/ corpus Closing/STATUS forms (the prose being compacted — census diagnostic-460 measured 37 DC blocks, 2 with aggregate STATUS), `bellows/scripts/cycle_check.py` (consumes the block today; may gain a manifest check) and `plan_lint.py` (§3 conformance checks). Ratified stanza fields fixed (see Context). Clone-diff: structural sibling `diagnostic-460` (read-only bellows design diagnostic, Rule 27 gap-table output; no origin drift). The proposal §4 evidence: this class of block reached 47,959 bytes of record vs 47,715 of instruction — the compaction is the point.
**Direction verdict (after walk 1):** **PROCEED** — the angle (characterize §3 + finalize the ratified grammar + gap-assess the amendment) is sound; no forcing finding. Walk 1's folds sharpen the design, not the premise.
**Walks:** in progress — walk 1 (4) + walk 2 (2) + walk 3 (2) + walk 4 (1) + walk 5 (1) + walk 6 (0), walk 6 CLASS-DRY. Instruction yields 4 → 2 → 2 → 1 → 1 → 0.
- Weak spots (1.4):     w1 dry. w2 dry. w3 dry. w4 dry. w5 dry. w6 dry.
- Destruction (2.4):    w1 1 folded — instruction 1 / record 0 (D1: this is DOCTRINE — the subtractive §3 cut must prove each removed line SUBSUMED by a stanza field or obsolete; never drop a live mandate for compaction). w2 dry. w3 dry. w4 dry. w5 1 folded — instruction 1 / record 0 (D2: `--emit-manifest` (w4 I4) must write STDOUT ONLY and preserve component 1's strictly-read-only invariant — a validator that mutated the plan to insert its own manifest would break the safety property it shipped). w6 dry.
- Vulnerabilities (3.1): w1 1 folded — instruction 1 / record 0 (V1: `reads:` is author-declared and its completeness is mechanically UNVERIFIABLE — an undeclared read gives false collision-safety (the plan-436 case); cross-check vs declared targets, and state the limitation plainly). w2 1 folded — instruction 1 / record 0 (V2: give the per-field TRUST TAXONOMY — computed vs authored-verified vs authored-declaration; maximize the computed set). w3 1 folded — instruction 1 / record 0 (V3: define `class: read-only` PRECISELY — writes only ephemeral/research artifacts; the auto-deposit test is blast radius, not literal zero-writes). w4 dry. w5 dry. w6 dry.
- Integration-record:   w1 1 folded — instruction 1 / record 0 (I1: reconcile the ratified 10-field set against proposal §4 — `validation:` subsumes §4's plan_lint-exit + battery-results, `walks`/`yields` added). w2 1 folded — instruction 1 / record 0 (I2: §3 is DENSE + spread throughout the file — reconcile the stanza against each existing §3 rule; esp. **`yields:` may violate the ~line-194 "no running fold-count" prohibition**; a real conflict = supersede a live §3 rule = the CEO's call). w3 1 folded — instruction 1 / record 0 (I3: CORRECTS Q4 — the stanza-presence check is a DEPOSIT-TIME check (plan_lint / depositor), NOT per-walk cycle_check, since the stanza is emitted AFTER BAR_MET). w4 1 folded — instruction 1 / record 0 (I4: the EMITTER is a new `cycle_check --emit-manifest` mode (extends shipped component 1, preserve its 27 tests) that COMPUTES validation/coherence/walks/yields and merges the authored declarations — the Planner does NOT hand-type the computed fields; follows from V2). w5 dry. w6 dry.
- ACID (5.2):           w1 1 folded — instruction 1 / record 0 (A1: `validation:` AND `coherence:` must be COMPUTED (emitted by cycle_check), not authored). w2 dry. w3 dry. w4 dry. w5 dry. w6 dry.
**Walk 1 STATUS:** 4 folded — instruction 4 / record 0 — NOT dry.
**cycle_check (dogfood) after walk 1:** `CONTINUE` (exit 0) — agrees. Manual "walk N" gate retired for this cycle.
**Walk 2 STATUS:** 2 folded — instruction 2 / record 0 — NOT dry (yield 4→2, falling).
**cycle_check (dogfood) after walk 2:** `CONTINUE` (exit 0) — agrees.
**Walk 3 STATUS:** 2 folded — instruction 2 / record 0 — NOT dry (yield 4→2→2, flat).
**cycle_check (dogfood) after walk 3:** `CONTINUE` (exit 0) — agrees, correctly NO `plateau` at 2 flat walks (threshold 3).
**Walk 4 STATUS:** 1 folded — instruction 1 / record 0 — NOT dry (yield 4→2→2→1, resumed falling; the emitter model, Integration only — a NEW region, not a plateau).
**cycle_check (dogfood) after walk 4:** `CONTINUE` (exit 0) — agrees (instruction 1, yield fell 2→1 so no plateau; not dry).
**Walk 5 STATUS:** 1 folded — instruction 1 / record 0 — NOT dry (yield 4→2→2→1→1; the read-only-invariant catch, Destruction only — a new region/class, not a plateau).
**cycle_check (dogfood) after walk 5:** `CONTINUE` (exit 0) — agrees; no plateau despite flat count, because a previously-dry lens (Destruction) folded this walk (the "no new finding class" plateau condition is not met).
**Walk 6 STATUS:** 0 folded — instruction 0 / record 0 — **CLASS-DRY** across all five lenses, no restructuring fold. §2 bar MET.
**cycle_check (dogfood) after walk 6:** **`BAR_MET`** (exit 0) — the tool signals the close: class-dry walk, asserts #1 PASS / #2 #3 N/A (scratchpad draft, no register/baseline — fail-safe), no restructuring, no plateau. The manual "walk N" gate is fully replaced for this cycle.
**Conflicts:** none.
**§5 Conformance:** `plan_lint` at shape-stability (walk 6) → **0 FAIL**; STEP count = 1. Benign residual WARNs are the location-dependent bellows-in-tree class (deposit path resolves at `bellows/…`). cycle_check confirms `BAR_MET` (dogfood), and re-confirms it with the Closing line present (step-8 anti-fabrication does not fire — the claim is earned).
**Closing:** full walk 6 class-dry across all five lenses, no restructuring fold; cycle_check `BAR_MET`; §5 conformance 0 FAIL; closing-record re-read run (this block), dry; cycle CLOSED. Deposit exactly once (pending CEO go). ⚠️ Carries an OPEN FORK for the CEO: the `yields:`-vs-§3-line-194 "no running fold-count" conflict (walk-2 I2) — if the diagnostic confirms it, keeping `yields:` means superseding a live §3 rule (the CEO's call).

---
---

## STEP 1 — BELLOWS DOCTRINE-DESIGN AGENT

---

> **Identity:** You are finalizing the `## Cycle Manifest` stanza grammar and producing the Rule 27 Gap Assessment for amending `DRAFTING_CYCLE.md §3`. Read-only: change NO code or doctrine; create NO files outside `knowledge/research/`. The stanza FORMAT is already CEO-ratified (see Q0) — do NOT relitigate the field set; finalize the grammar's edge cases and the amendment plan.
>
> **Q0 — The ratified format (fixed, do not change).** `## Cycle Manifest`, a fixed `key: value` block emitted at BAR_MET. Fields: `tier · target · class · reads · writes · open_forks · walks · yields · validation · coherence`. `reads:`/`writes:` = explicit path lists; `class:` ∈ `{read-only, governed-tooling, register-writing}` drives auto-deposit; `validation:` is COMPUTED (re-run by the depositor). Restate it verbatim so the executable clones the ratified shape. **Reconcile against proposal §4's field list** so nothing is dropped or double-added: §4's separate `plan_lint exit` and `battery results` items are SUBSUMED into `validation:` (as `checker=verdict` pairs), and `walks`/`yields` were ADDED for the plateau/convergence record — the ratified 10-field set is complete; the executable must not re-introduce §4's items as separate fields.
>
> **Q1 — Characterize the current §3 rules in doctrine and in the corpus.** §3 is "record the Cycle Log" (`DRAFTING_CYCLE.md:7`), and its rules are SPREAD THROUGHOUT the file, not in one block — locate the §3 section header AND gather every inline `§3` cross-reference. Quote at minimum: the per-class number form (`instruction 0 / record 5`, ~line 40), the ONE bounded compact-form exception for judged-stop residue (~line 50), and the **"§3 forbids a running fold-count in the log and mandates the compact per-lens form" prohibition (~line 194)**. Then census the Done/ corpus: what do real `**Closing:**` / STATUS / §5-Conformance lines carry today, and their byte weight (sample the largest)? Establish the compaction evidence (proposal §4's 47,959-vs-47,715). Distinguish AUTHORED prose (drifts) from COMPUTABLE facts.
> **⚠️ RECONCILE the ratified stanza against EACH existing §3 rule — this is the load-bearing integration.** The amendment is NOT a blank-slate addition to a dense, heavily-codified section. Flag every conflict: **(i)** does `yields:` (a per-walk instruction-class series) violate §3's "forbids a running fold-count in the log" prohibition (~line 194)? **(ii)** does the stanza DUPLICATE the existing per-class number form (~line 40)? **(iii)** does adding a computed `validation:`/`coherence:` create a second compact-form exception against the ~line-50 "ONE bounded exception" clause? A genuine conflict means the amendment must either reconcile the stanza to the rule OR explicitly supersede a live §3 rule — which RAISES the doctrine stakes and is the CEO's call. Surface each as a design question; do NOT silently resolve one by quietly overriding doctrine.
>
> **Q2 — Finalize the stanza grammar (edge cases the executable must implement).** Give the exact grammar: one specimen (use a real closed plan, e.g. diagnostic-460 or executable-464, populated from its actual data). Resolve: (a) multi-line value handling for `reads:`/`writes:` path lists (continuation-line convention); (b) path expression — relative to the plan's repo vs absolute, and cross-repo reads (the governance-repo case from the cycle_check census); (c) the `validation:` value form (`checker=verdict` pairs) and that it is re-run, never trusted; (d) `yields:` as the per-walk instruction-class series; (e) where exactly the stanza sits (at Closing / BAR_MET) and its relation to the existing register lines cycle_check parses; (f) **`validation:` AND `coherence:` are both COMPUTED, not authored** — `coherence:` (e.g. "6/6 walks have register rows") must be emitted from cycle_check's own walk/register check, not typed by the Planner, or it reintroduces exactly the authored-prose drift the stanza exists to eliminate; (g) **give the per-field TRUST TAXONOMY** — classify each of the 10 fields as COMPUTED (emitted by cycle_check: `validation`, `coherence`, and — extend the principle — `walks`, `yields`, all derivable from the register), AUTHORED-BUT-DEPOSITOR-VERIFIED (`reads`, `writes` via collision; `class` drives deposit), or AUTHORED DECLARATION (`tier`, `target`, `open_forks`). Maximizing the computed set is what gives the stanza its drift-resistance; state which fields remain irreducibly authored and why.
>
> **Q3 — reads:/writes: collision computation (the depositor's dependency).** Specify how `reads:`/`writes:` are compared against in-flight plans' declared Scope/Deposits: `writes ∩ writes` → HARD HOLD; `reads ∩ writes` → HOLD AND REPORT (proposal §6, the plan-436 moving-target case). State the exact query (the `plans` table + each in-flight plan's Scope block) and path-normalization rules so intersection is sound across repos. **Name the load-bearing limitation:** `reads:` is AUTHOR-DECLARED and its completeness cannot be mechanically verified — an undeclared read gives false collision-safety (the plan-436 memory-repo case would have gone undetected had that read not been declared). Recommend a partial mitigation: cross-check `reads:` against the plan's own declared `targets`/required-reading so an obviously-incomplete `reads:` (writes a file it never lists reading, or omits a declared target) is at least flagged — but state plainly that the depositor's `reads∩writes` safety is only as good as the declared `reads:`.
>
> **Q4 — WHERE the stanza is validated (a deposit-time check, NOT a per-walk one).** Design (no code) the check that the `## Cycle Manifest` stanza is PRESENT + well-formed, its `validation:` verdicts re-runnable (not merely asserted), and `reads:`/`writes:` non-empty for a writing plan. **Crucial ordering:** the stanza is emitted AFTER cycle_check returns `BAR_MET` — so it CANNOT be a per-walk cycle_check check (cycle_check would be validating its own not-yet-emitted output). It is a **DEPOSIT-TIME** check: `plan_lint` at deposit-shape and/or the depositor's re-run (component 3). cycle_check's role stays the per-walk verdict that SIGNALS close; the stanza check is strictly downstream of `BAR_MET`. Recommend which of plan_lint vs depositor owns it (or both, at different moments).
>
> **Q5 — Rule 27 Gap Assessment for the §3 amendment (mandatory table).** `| Gap | Current State | Proposed State | Change Required |`. Cover: the §3 doctrine text change (compacting — name the prose the stanza REPLACES vs adds), the `reads:` field introduction, **the emitter — a new `cycle_check --emit-manifest` mode** (this EXTENDS component 1's already-shipped `cycle_check.py`, so the component-2 executable touches tested code and must preserve its 27 tests) that COMPUTES `validation`/`coherence`/`walks`/`yields` from the block and merges the authored declarations (`tier`/`target`/`class`/`reads`/`writes`/`open_forks`) from the plan header — the Planner does NOT hand-type the computed fields (V2's drift-resistance depends on the machine emitting them). ⚠️ **`--emit-manifest` writes to STDOUT ONLY — it MUST preserve cycle_check's strictly-read-only invariant** (component 1: writes nothing, mutates no file, never `--save-baseline`). The emitted stanza is placed into the plan by the Planner/close step, never by cycle_check writing into the plan — a read-only validator that mutated the plan to insert its own manifest would break the very safety property component 1 shipped. The validator (Q4), and the depositor's read of it (component 3). Each row build-ready. **Compaction discipline:** the amendment must be net-neutral-or-shrinking on §3 (proposal §4) — quantify the expected byte delta. **Subtractive-cut safety (this is doctrine, not scratch):** for EVERY §3 line the amendment removes, prove it is either SUBSUMED by a specific stanza field or explicitly obsolete — never drop a live §3 mandate in the name of compaction. Present the removed lines as a table mapping each to its subsuming field (or "obsolete, because…"). The downstream executable's doctrine edit earns its own diff review; this diagnostic supplies the subsumption proof it will verify against.
>
> **Q6 — class: taxonomy → auto-deposit mapping.** Confirm the mapping: `read-only` → AUTO-DEPOSIT; `governed-tooling` / `register-writing` → HOLD FOR CEO (proposal §7). Give 2-3 worked examples from real plans (e.g. this arc's diagnostic-460 = read-only → AUTO; executable-464 = governed-tooling → HOLD; a forge-register append = register-writing → HOLD). Name the boundary case: a bellows-scripts write (governed-tooling) is HOLD, not AUTO. **Define `read-only` PRECISELY so the auto-deposit boundary is unambiguous:** it means the plan writes ONLY ephemeral/research artifacts (`knowledge/research/`, scratch) — NO governed register, production code, schema, or doc-of-record. A diagnostic that deposits a findings file is STILL `read-only` by this definition; the test is blast radius (a bad auto-deposit costs one wasted run), not literal zero-writes. State the rule the depositor applies to assign the class from a plan's `writes:` paths.
>
> **Constraints:** Read-only; no code/doctrine edits; findings file only. Quote `DRAFTING_CYCLE.md §3` by line so the executable author re-reads it (read the cited rule, don't recall it). If a grammar edge case is genuinely open, present the options with a recommendation rather than inventing a silent default.
>
> **Deposit:** `knowledge/research/cycle-manifest-stanza-design-2026-08-19.md` — Q0–Q6 with the finalized grammar specimen, the collision-computation spec, and the Rule 27 Gap Assessment table. End with an Output Receipt (Status). Standard prompt-feedback protocol.
>
> **Deposits:**
> - `knowledge/research/cycle-manifest-stanza-design-2026-08-19.md`
