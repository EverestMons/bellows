# Bellows — expected-keys warning fix shape resolution (bellows.py:416-419)
**Date:** 2026-05-21 | **Tier:** Diagnostic | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1 | **auto_close:** false

## Context

The 2026-05-21 expected-keys-warning diagnostic (in `bellows/knowledge/decisions/Done/diagnostic-bellows-expected-keys-warning-2026-05-21.md`, findings at `bellows/knowledge/research/bellows-expected-keys-warning-2026-05-21.md`) established that:

- Only `pause_for_verdict` is safety-critical among the 5 keys in `expected_keys` at bellows.py:416
- Resolution shape (1) "narrow the warning" is recommended; shape (2) "fix templates" rejected
- One subtlety surfaced during Planner review of the findings: after `_apply_defensive_header_defaults` runs at bellows.py:381, `pause_for_verdict` is always in the header dict when the sparse-header trigger fires (`total_steps > 1 and len(header) < 3`). A simple post-defensive-default check `pause_for_verdict not in header` is therefore uninformative

The Planner identified three candidate implementation shapes for the narrowed warning. This second diagnostic exists to resolve the choice between them with code-trace evidence, NOT planner intuition. The Planner does not read source; the SA does.

**Three candidate shapes (lifted verbatim from Planner's analysis):**

Shape A: **Modify `_apply_defensive_header_defaults` to return a flag** indicating whether it fired. The warning then checks both whether `pause_for_verdict` is in the dict AND whether the defensive default supplied it. Adds a return value to a function that currently returns the mutated dict.

Shape B: **Capture `original_header_keys = set(header.keys())` before calling `_apply_defensive_header_defaults`** and check that snapshot in the warning. Introduces a parallel variable that has to be kept in sync with the dict.

Shape C: **Two warnings, consolidated.** The existing warning at bellows.py:382-383 already logs "sparse header (N keys) for M-step plan — defaulting pause_for_verdict to after_step_1 (safe-pause)" when the defensive default fires. Narrow the line-416 warning to fire ONLY when `pause_for_verdict not in header AND total_steps > 1 AND defensive default did NOT fire` (i.e., the dangerous case where header had ≥ 3 keys but `pause_for_verdict` was nevertheless absent, so the plan would auto-advance without pausing). The two warnings then cover distinct cases without overlap.

**Scope:** read-only investigation of `bellows.py` lines 317-420 only, plus a handful of recent Done plans to validate behavior against real cases. No fixes, no code changes. Findings deposit drives the Planner's executable.

---
---

## STEP 1 — Bellows Systems Analyst

---

> You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. **Skip glossary read.** **Pre-investigation read:** the prior diagnostic findings at `bellows/knowledge/research/bellows-expected-keys-warning-2026-05-21.md` — specifically Sections 1 (warning code), 2 (per-key audit, `pause_for_verdict` entry), and 6 (Gap Assessment row e). Treat those findings as established; do not re-derive. **Task:** resolve which of Shapes A / B / C is the cleanest implementation for narrowing the expected-keys warning to `pause_for_verdict` only. Output is a recommendation with code-evidence rationale. **Investigation questions:** (1) **Re-read `_apply_defensive_header_defaults` at bellows.py:317-326 and the call site at bellows.py:381.** Quote both verbatim with 3 lines of surrounding context. Confirm the function's current return contract (returns the mutated dict, callers ignore the return — quote both call sites in `bellows.py` to verify). Quote the existing line-382-383 warning that fires when the defensive default activates. (2) **Trace the actual dispatch outcomes for each of the four logical cases** by reading bellows.py:380-420 and downstream pause logic at bellows.py:495-515 + 586-606. For each case, report what Bellows does today (pauses where, auto-advances when, emits which warnings):<br>&nbsp;&nbsp;&nbsp;&nbsp;Case 1: `pause_for_verdict` present in original header, header had ≥ 3 keys → ?<br>&nbsp;&nbsp;&nbsp;&nbsp;Case 2: `pause_for_verdict` present in original header, header had < 3 keys → ?<br>&nbsp;&nbsp;&nbsp;&nbsp;Case 3: `pause_for_verdict` absent from original header, header had < 3 keys (defensive default fires) → ?<br>&nbsp;&nbsp;&nbsp;&nbsp;Case 4: `pause_for_verdict` absent from original header, header had ≥ 3 keys (defensive default does NOT fire) → ?<br>&nbsp;&nbsp;&nbsp;&nbsp;Build a table: `| Case | pv present originally? | header keys count | defensive default fires? | runtime behavior | warning emitted today | desired warning behavior (post-fix) |`. The "desired warning behavior" column is your judgment based on whether the case is safe or dangerous. (3) **Evaluate each of Shapes A, B, C against the four cases.** For each shape, write a short code-sketch (5-15 LOC) of how the implementation would look. Specifically: under Shape A, what does `_apply_defensive_header_defaults` return and how does the warning read it? Under Shape B, where in the function flow does the snapshot get captured? Under Shape C, what is the exact boolean expression that gates the line-416 warning? For each shape, evaluate: does it emit a warning for Case 4 and only Case 4 (assuming defensive default is the safety net for Case 3 and the existing line-382-383 warning covers Case 3 reporting)? Does it preserve the existing line-382-383 sparse-header warning unchanged? What is the LOC delta? Are there hidden side effects (e.g., other callers of `_apply_defensive_header_defaults` if Shape A changes its return)? (4) **Audit `_apply_defensive_header_defaults` callers.** Grep `bellows.py` for `_apply_defensive_header_defaults` and report every call site with 2 lines of surrounding context. Confirm whether changing the function's return type (Shape A) would require changes at any other call site. Repeat for `gates.py`, `verdict.py`, `parser.py`, `validators.py`, `runner.py` to be thorough. (5) **Recent-plan reality check.** Read the 10 most-recently-modified plans in `bellows/knowledge/decisions/Done/` (sort by mtime desc). For each, count header keys and check whether `pause_for_verdict` is present in the parsed header. Categorize each into Cases 1-4 from question 2. Report the distribution. This grounds the choice in actual plan patterns rather than theory. (6) **Recommendation.** Produce a Gap Assessment row with columns `| Shape | LOC delta | Reads cleanly? (yes/no + 1 sentence) | Hidden side effects? | Covers all 4 cases correctly? | Recommendation |` for each of A, B, C. End with a one-paragraph "Recommended Shape" verdict naming exactly one shape and citing the strongest 1-2 facts from the audit. **Read depth:** bellows.py:317-420 + 495-515 + 586-606 full; grep for `_apply_defensive_header_defaults` across all modules; 10 recent Done plans sampled. **Output format:** structured markdown deposit at `bellows/knowledge/research/bellows-expected-keys-shape-choice-2026-05-21.md` with sections matching question numbers 1-6. Quote code verbatim with line numbers. **Do NOT propose the warning's final string or the executable's edit_block anchors** — those come after the shape is chosen. Just resolve the shape. **Standard prompt feedback protocol** → append entry to `bellows/knowledge/research/agent-prompt-feedback.md`. **Commit:** stage findings file and feedback log; commit message `docs: SA findings — bellows expected-keys fix-shape choice`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/knowledge/research/bellows-expected-keys-shape-choice-2026-05-21.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
