# bellows — THE FIVE SESSION HOOKS SHARE ONE MODULE: `hooks/eluvian/_common.py` carries `_default_root`, `_log_path`, `hooklog`, `emit`, `_validate_session_id` and the log-path constant once; each hook imports them through its own directory — plan 100015's by-design duplication retired on the premise MACHINE_SETUP v1.1a falsified (the hooks are not copies, the directory is a symlink into this repo), its identity test replaced by an import-identity test (thread 259)

**Date:** 2026-09-10 | **Project:** bellows | **Tier:** Small (one new module, five hooks edited by extraction, two tests edited (test (d) replaced; one helper line in `test_wrap_sentinel.py`), one test sibling added, one mutation manifest, one dev-log) | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full-suite (Rule 21 — the hooks are the wrap gate; the eight hook test files first, then `pytest tests/ -q`) + the four harness hooks imported and RUN under `/usr/bin/python3` (3.9.6, the harness interpreter — T-3, the 100015 precedent) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 259

**auto_close:** false

**Post-close:** no restart (hooks load per event from the symlinked directory — the next SessionStart/Stop runs the new files). ⚠️ The merge that makes them live is the daemon's teardown at the QA PAUSE — the hooks are live BEFORE the verdict, for every session on this machine including the CEO's; the first commit's eleven files land together in one ref update, so no event loads a hook without `_common.py`; a reject verdict is a `git revert` of the three landed commits on main, live on the next event by the same symlink (panel C6). No doctrine. MACHINE_SETUP §6's hooks line already says the harness loads the repo's files (v1.1a); the `hooks/README.md` first two paragraphs still say "copy" and "LIVE location" and are corrected by this plan (*What this changes* 6).

**Depends on:** the CEO's rulings of 2026-09-10 (session d04ebd33): "Draft as T2", then, with the ratified design surfaced, "Proceed as T2, premise named" (note 16 on thread 259). The premise: `Done/executable-100015.md` line 17 — *"the hooks are STANDALONE files: no bellows module on their path, and the harness runs COPIES from `~/.claude/eluvian/` … so a shared helper module would be a sixth file to install and a new way for the copies to drift"* — pinned by `tests/test_hook_default_root.py::test_d_four_bodies_identical`. The correction: MACHINE_SETUP v1.1a (the same night, 2026-09-02): *"the live hook directory is a symlink into the repo on the mini and the shop's settings name the repo path"* — measured again at walk 0 (P4). A sibling module in `hooks/eluvian/` ships with the hooks on every machine by the same symlink; nothing is installed. Clone origin by kind: `Done/executable-100015.md` (the hooks work: five files, harness-interpreter QA, the per-hook anchor discipline); by layout: `Done/executable-100055.md`. Measurement basis: thread 259's note 10 (helpers byte-identical; 4 of 19 hook commits since 2026-08-01 carried helper-definition lines across files — the propagation cost the shared module removes).

**Tier computed (§1):** **T2** — **T-6 fires:** the hooks ARE the wrap gate (the Stop hook blocks a turn until `wrap_check` passes; the SessionStart hooks inject doctrine context) — a governance surface under §1's words, so the cold panel convenes. **T-3 fires:** the hooks run under `/usr/bin/python3` 3.9.6, not the bellows venv (100015's precedent) — QA imports and runs every hook under it. **T-1 fires** (six files, one subsystem). The magnitude proxy (§2.6): (i) a shipped same-class parent is named with its Done date (100015, 2026-09-02) and both walk-0 clone-diffs run; (ii) FAILS by construction — a new module is novel machinery, and the five hook edits are extractions, not anchored substitutions through a committed builder; (iii) the post-conditions are scripted. One leg failing → **FULL form: walk-0 scout + DISCOVERY, EXECUTION, CAPSTONE at the freeze.**

## CEO Context

Anvil's clone detector, priced on 2026-09-09 (#100057 Q6), found one thing a name search cannot: four helpers copied byte-for-byte across the five hook files under `hooks/eluvian/` — `_default_root` in four, `hooklog` in four, `emit` in two, `_validate_session_id` in two — plus one hardcoded log path in four (P1). The duplication was deliberate: plan 100015 wrote the resolver into four hooks "verbatim, by design" and pinned the four bodies identical with a test, because it measured the harness running COPIES of the hooks from `~/.claude/eluvian/`. That measurement was wrong the same night: MACHINE_SETUP v1.1a re-measured the directory as a symlink into this repo on the mini, and the shop's settings name the repo path directly (P4 re-measures both today). Since then a change to a helper has been a multi-file edit by hand — three commits since August carried helper-definition lines in two or more files, one of them a mirrored edit of one function in three files (P8) — and the identity test is the only thing that catches a missed fifth site. The CEO's direction on 2026-09-09: anvil's refactor half keeps the findings its clone detector produces; this is the first one. The refactor is an extraction with no behaviour change: every hook keeps the same functions under the same names, imported instead of pasted, and the identity test becomes an import-identity test that fails if any hook grows its own copy again.

## What this changes

1. **NEW `hooks/eluvian/_common.py`** — the five helpers and the constant, each moved VERBATIM from its current body (P1's shas pin the bodies; the DEV extracts by copying the text, never by retyping): `_DEFAULT_LOG = Path("/Users/marklehn/.claude/eluvian/hooks.log")` (unchanged literal — de-hardcoding it is open fork 1, not this plan), `_default_root()` (its docstring's last two sentences rewritten to the truth — the first, the semantics sentence, stays: *"Shared by the hooks through `_common.py` since plan hooks-shared-common (2026-09-10); the hooks load from `hooks/eluvian/` on every machine — a symlink on the mini, the repo path in the shop's settings (MACHINE_SETUP v1.1a). Plan 100015 duplicated this body on the copies premise; `test_hook_default_root` (d) now asserts the four hooks that resolve a root bind THIS function."*), `_log_path()`, `hooklog(event, detail="")`, `emit(context)`, `_validate_session_id(raw_id)`. Module docstring: what it holds, who imports it, and the one rule — *a hook that needs a helper adds it here, never inline*. Stdlib only (`datetime`, `json`, `os`, `re`, `sys`, `pathlib`).
2. **Each of the five hooks** gains, immediately after its stdlib imports, the two lines `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` and `from _common import <exactly the names that hook used>` (P1's inventory: arm — `_default_root, _log_path, hooklog, _validate_session_id`; stop — `_default_root, _log_path, hooklog, _validate_session_id`; check — `_default_root`; align — `_default_root, _log_path, hooklog, emit`; debt — `_log_path, hooklog, emit, _VALID_SESSION_ID`), and LOSES the corresponding `def` blocks and its `_DEFAULT_LOG =` line — nothing else in any hook moves (P2 pins each hook's line count before; the diff of each hook is deletions plus the two lines — a separating blank line is allowed, so `--stat` may show 3 insertions — panel E6). `wrap_stop_hook.py` keeps its own `allow()` and `block()` (single-site; not shared). `_VALID_SESSION_ID` (the regex constant) moves with `_validate_session_id` into `_common`; arm and stop referenced it only through the function, and `wrap_debt_hook.py:84` matches it DIRECTLY (its own copy at `:29`) — the debt hook's import line becomes `_log_path, hooklog, emit, _VALID_SESSION_ID` and its constant line is deleted, so the regex lives once (scout S5).
3. **`tests/test_hook_default_root.py`** — test (d) `test_d_four_bodies_identical` is REPLACED by `test_d_four_hooks_bind_the_shared_helper`: obtain `common` the way the hooks do — `sys.path.insert(0, str(HOOKS_DIR)); common = importlib.import_module("_common")` (a `spec_from_file_location` load is a DIFFERENT module object and would make the identity assertion red — scout S1, demonstrated); both lines live INSIDE test (d)'s body, never at module scope, because mutant 5's kill needs a process in which no test but (d) has put `hooks/eluvian` on `sys.path` (panel D3) — then load each of the four hooks by path (the existing `_load_hook`), and assert `mod._default_root is common._default_root` (identity, not source equality) AND `"def _default_root" not in Path(hook).read_text()` for each hook (no private copy may reappear). Tests (a)–(c), (e)–(g) unchanged and green. `tests/test_wrap_sentinel.py::_make_passcheck_dir` (lines 55–67) gains ONE line — `shutil.copy2(HOOKS_DIR / "_common.py", d / "_common.py")` — so the six tests that run a COPY of the stop hook from a foreign directory (`script_dir=hook_dir`, lines 94–210) find the sibling; a helper edit, no assertion touched (panel D1).
4. **NEW `tests/test_hooks_shared_common.py`** — (s1) every name in each hook's `from _common import` line exists in `_common` and no hook defines any of the seven shared names locally (a grep over the five files: `^def (_default_root|_log_path|hooklog|emit|_validate_session_id)\(`, `^_DEFAULT_LOG` and `^_VALID_SESSION_ID` → 0 each); (s2) `_common` imports under `/usr/bin/python3` (subprocess `-c "import importlib.util, …"`, skipped with a stated reason if that interpreter is absent); (s3) `hooklog` writes the `ts\tevent\tdetail` line to `ELUVIAN_HOOKS_LOG` and swallows an unwritable path (the by-design `except Exception: pass`); (s4) `emit` prints the `hookSpecificOutput` JSON with `SessionStart` for a context and `{}` for none, and exits 0; (s5) each of the four harness hooks RUN as a subprocess (the `test_wrap_hooks._run_hook` idiom: `sys.executable`, piped stdin JSON, `ELUVIAN_WRAP_ROOT` and `ELUVIAN_HOOKS_LOG` under `tmp_path`; `BELLOWS_DISPATCH` DELETED via `monkeypatch.delenv` as `_isolate_wrap_env` does, or the stop and debt hooks take their `daemon-exempt` branch under the daemon — panel D5; the scratch root carries a one-line `bellows/status.py` stub and `ELUVIAN_WRAP_LESSONS_FORGE`, `ELUVIAN_WRAP_TUYERE` and `ELUVIAN_WRAP_MEMORY` point at empty scratch dirs (`monkeypatch.setenv`), so the align hook's `_sync_repos`/`_daemon_status` resolve inside `tmp_path` and never `git fetch` a canonical checkout, and the debt hook's `wrap_check` never launches the canonical tuyere CLI — panels D6, E2) exits 0, prints one JSON line and writes its NON-exempt log line — the live shape; it pins behaviour the hooks already have and is GREEN before the extraction (panel D10).
5. **`knowledge/mutants/hooks-shared-common.json`** — at least six mutants (P7's contract): a hook re-growing a local `_default_root` (s1), `_common._default_root`'s FALLBACK returning the GitHub home (`tests/test_hook_default_root.py::test_c_no_marker`; a candidate-order swap is not killable by tests a–c, which plant at most one marker — scout S2), `hooklog` raising on an unwritable path (s3), `emit` naming `Stop` (s4), the `sys.path.insert` line removed from `wrap_arm_hook.py` (`tests/test_hook_default_root.py::test_c_no_marker` — a path-loaded hook has no directory on `sys.path`; a script run has, so (s5) cannot kill it — scout S3; the insert exists for path- and package-loaded consumers, not for the harness), `_validate_session_id` accepting `.` and `/` (replacement regex `^[A-Za-z0-9./-]+$`; `expect_fail` `tests/test_wrap_sentinel.py::TestSessionIdFallback::test_invalid_charset_arms_bare`, which sends `../etc/passwd` — both characters are needed for the widened regex to admit it, and the node id is the class method's, a bare `::test_…` collects nothing (exit 4) — panel D2).
6. **`hooks/README.md`** first two paragraphs (this item): "Canonical copy … `~/.claude/eluvian/` is the LIVE location the Claude Code harness loads hooks from" → the v1.1a truth in two sentences (the repo directory IS what the harness loads: a symlink on the mini, the repo path in the shop's settings; `_common.py` is imported by every hook from this directory). Nothing else in the README moves.
7. **Not changed:** any hook's behaviour, argument, output, exit code or log line; `hooks/commands/*.md`; `~/.claude/settings.json`; the harness wiring; every test outside the two named.

## Why this exists

A helper pasted into four files is edited four times or three (note 10: four propagation commits since August; the identity test catches the third-of-four case only after the fact). The reason for the paste was measured false the night it was written, and the correction (v1.1a) has stood for eight days without the code following it. The refactor is the smallest one that makes the next helper edit a one-site edit, and it replaces a test that guards a symptom (four equal bodies) with one that guards the cause (one body, four bindings).

## What this does NOT do

- Does not change what any hook does, prints, logs or returns — an extraction, proven by the eight hook test files unchanged in their assertions (141 tests, P5) and by the live subprocess runs (s5, QA Item 3).
- Does not de-hardcode `_DEFAULT_LOG` (open fork 1) and does not touch `wrap_check.py`'s other helpers (`_resolve_bellows`, `_tuyere_checkout`) — single-site, not shared.
- Leaves four further byte-identical two-site duplicates in place, by decision: `_wrap_root()` (arm `:64`, stop `:57`), `_DEFAULT_ROOT = _default_root()` (arm `:50`, stop `:48` — an import-time call each hook binds itself), `_BELLOWS_DISPATCH_ALLOW` (stop `:52`, debt `:28`) and `CHECK = Path(__file__).with_name("wrap_check.py")` (stop `:49`, debt `:25` — `__file__`-relative, meaningful only in its owner). The criterion this plan moves by is thread 259's note 10 (the four helpers anvil's clone detector surfaced) plus the `_log_path`/`_DEFAULT_LOG` pair they call; `_wrap_root` and `_BELLOWS_DISPATCH_ALLOW` are the next candidates and are open fork 4 (panel D7).
- Does not install anything anywhere; does not touch `~/.claude/`.
- Does not edit doctrine, the command files, or the harness settings.

## MUST-PRESERVE

- ⛔ **Byte-behaviour.** Every hook's stdout, exit code and `hooks.log` line for every payload the eight test files send are unchanged; the eight files' assertions are not edited (only test (d) is replaced, and it asserts a stronger property; `_make_passcheck_dir` gains one copy line, an assertion-free helper).
- ⛔ **A hook never raises on import or on a missing root** — `_common` must import under `/usr/bin/python3` 3.9 (no 3.10+ syntax: no `match`, no `X | Y` type unions at runtime; `from __future__ import annotations` stays at the top of every file that had it) and `_default_root` keeps its quiet fallback — given `_common.py` beside the hook, which the symlink and the shop's settings path guarantee; a hook COPIED without its sibling tracebacks by design, with no silent fallback to a private body (panel D12).
- ⛔ **Extraction, not authorship.** The DEV moves text; a helper whose moved body differs from its source by anything but the `_default_root` docstring rewrite named in *What this changes* 1 is a defect (QA diffs each body against P1's pinned source).
- ⛔ **The symlink is read, never written.** `ls -la ~/.claude/eluvian` is quoted; nothing under `~/.claude/` is touched.

## Numbers discipline — measured 2026-09-10 by the Planner (bellows `2d317f0`, governance `08eb7542`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the duplicated definitions, by file:line | `_default_root`: `wrap_arm_hook.py:36`, `wrap_stop_hook.py:34`, `wrap_check.py:43`, `eluvian_align_hook.py:21` (12 lines each, one distinct body — AST-body hash `8533d82b`); `hooklog`: arm `:90`, stop `:65`, align `:55`, debt `:36` (7 lines, `64b697f8`); `emit`: align `:64`, debt `:45` (11 lines, `0fe193c7`; the two bodies md5-equal `e2448129`); `_validate_session_id`: arm `:72`, stop `:84` (8 lines, `a5bda96a`); `_log_path`: arm `:68`, stop `:61`, align `:51`, debt `:32`; `_DEFAULT_LOG = Path("/Users/marklehn/.claude/eluvian/hooks.log")`: arm `:51`, stop `:50`, align `:46`, debt `:26`; `_VALID_SESSION_ID = re.compile(...)`: arm `:61`, stop `:53`, debt `:29` (debt matches it directly at `:84`); `wrap_check.py` has `_default_root` only (its `ROOT` derives from it) | `grep -n '^def _default_root\|^def hooklog\|^def emit\|^def _validate_session_id\|^def _log_path\|^_DEFAULT_LOG' hooks/eluvian/*.py`; the AST hash script in thread 259's note 10 |
| P2 | ⛔ the five files, pre-edit | `wrap_arm_hook.py` sha256[:16] `cd405b04b47f990b` 143 lines; `wrap_stop_hook.py` `4171db3a0bfa4ada` 268; `wrap_check.py` `c3cdb1ff193fcac2` 580; `eluvian_align_hook.py` `06fd96aa71d6ef3f` 245; `wrap_debt_hook.py` `55cb3d0815d3bde1` 114; last writers: arm/stop/align `c50efcf` (100015, 2026-09-02), check `f1fed67` (2026-09-02), debt `cf4c694` (2026-08-25, plan 520) | `shasum -a 256`; `git log -1 -- <file>` |
| P3 | ⛔ the ratified design and its premise | `Done/executable-100015.md:17` (quoted in *Depends on*); the helper's docstring (all four sites): *"Duplicated verbatim in the four hooks by design: they are standalone files copied into ~/.claude/eluvian/, and a shared module would be one more file to install (test_hook_default_root asserts the four bodies stay identical)"*; `tests/test_hook_default_root.py:64–74` `test_d_four_bodies_identical` compares `inspect.getsource(mod._default_root)` across `HOOK_NAMES = ["wrap_arm_hook", "wrap_stop_hook", "wrap_check", "eluvian_align_hook"]` | `sed -n 64,74p tests/test_hook_default_root.py` |
| P4 | ⛔ the premise's correction, re-measured today | `ls -la ~/.claude/` → `eluvian -> /Users/marklehn/Developer/bellows/hooks/eluvian` (a symlink, dated Aug 24); `~/.claude/settings.json` hooks: `UserPromptSubmit → /usr/bin/python3 /Users/marklehn/.claude/eluvian/wrap_arm_hook.py`, `Stop → … wrap_stop_hook.py`, `SessionStart → … wrap_debt_hook.py` and `… eluvian_align_hook.py` (four entries; `wrap_check.py` runs as the Stop hook's subprocess and by hand); MACHINE_SETUP v1.7 §6: *"the harness loads the REPO's files, not copies — on the shop `~/.claude/settings.json` names `…/bellows/hooks/eluvian/` directly (plan 496's repoint, 2026-08-21); on the mini `~/.claude/eluvian` is a SYMLINK"*; v1.1a's row: *"the live hook directory is a symlink into the repo on the mini and the shop's settings name the repo path"*; `/usr/bin/python3 --version` → `Python 3.9.6` | the three commands, quoted |
| P5 | ⛔ the hook suite and its loading idioms | eight files, `141 passed in 10.68s` (`tests/test_wrap_hooks.py`, `test_align_hook_sync.py`, `test_wrap_sentinel.py`, `test_wrap_receipts.py`, `test_wrap_3b_keyed.py`, `test_wrap_memory_class_gate.py`, `test_wrap_r2_registry.py`, `test_hook_default_root.py`); idioms: by path with `importlib.util.spec_from_file_location` (`test_hook_default_root.py`, `test_wrap_sentinel.py`), as a subprocess with piped stdin (`test_wrap_hooks.py`, `test_wrap_memory_class_gate.py`, `test_wrap_sentinel.py`, `test_align_hook_sync.py`), by `sys.path.insert(0, HOOKS_DIR)` + `import` (`test_wrap_memory_class_gate.py`, `test_align_hook_sync.py`), and as a package import `import hooks.eluvian.wrap_check` from the repo root (`test_wrap_receipts.py:112`) — all four resolve a sibling import once the hook inserts its own directory (a spec-loaded module has `__file__`); a FIFTH idiom, a COPY of the stop hook run from a foreign `tmp_path` directory (`test_wrap_sentinel.py::_make_passcheck_dir`, six tests), resolves only if the copy's directory also holds `_common.py` — the one-line helper edit in *What this changes* 3 (panel D1) | the pytest line; `grep -l` per idiom |
| P6 | the clone-diff targets | by kind `Done/executable-100015.md` (Done 2026-09-02, the newest hooks plan — measured: `git log --format=%ad -- hooks/eluvian/` newest plan-tagged commit `c50efcf`); `clone-origin` threads naming 100015: none open (`threads list --component clone-origin` → no open threads); the parent's walk register `walk-register-hooks-de-hardcode-2026-09-02.md` carries its findings as `w1-n` table rows and its RESOLVED paragraph (the symlink discovery) as prose — the register diff reads both | `threads list --component clone-origin`; the register file |
| P7 | the mutation contract; class; in-flight | `tools/mutation_check.py`: manifest-driven, `expect_fail` a pytest node id, KILLED = exit 1 only, run file `knowledge/mutants/<slug>.run.txt`; this plan writes `hooks/eluvian/`, `tests/`, `knowledge/mutants/` → **shop-infra** (HOLDS, the CEO releases); at drafting nothing is in flight (daemon pid 70398, HEAD `2d317f0`) | `python3 status.py`; the depositor's parse at walk 0 |
| P8 | the propagation cost the module removes | note 10 on thread 259: 19 commits touched `hooks/eluvian/` since 2026-08-01; 7 touched two or more hook files; 4 carried helper-DEFINITION lines in two or more files (`c50efcf`, `c42ab49`, `384fffa`, `322e0c9`), of which `c42ab49` is the four-file vendoring CREATION and `52965f5` (excluded) the align hook's one-file creation; under a creation-excluding criterion the count is 3 — `c50efcf` (100015: `_default_root` inserted ×4), `322e0c9` (497: `_validate_session_id` ×2) and `384fffa` (496 step 2: the one MIRRORED EDIT, `hooklog`'s signature changed in three files) — panel D8 | `git log --since=2026-08-01 -- hooks/eluvian/` with the per-commit `--name-only` and diff grep in the note |

## Drafting Cycle

**Tier:** **T2** — T-6, T-3, T-1 fire; full form (proxy leg (ii) fails: novel module). **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-hooks-shared-common-2026-09-10.md
**Walks:** walk 0 pinned (P1–P8 measured on bellows `2d317f0`; clone-diff against `Done/executable-100015.md` run: FACTS, ARTEFACTS, STRUCTURE; the standing-rules diff against bellows `CLAUDE.md`; the parent's register read as prose; the cold SCOUT seat convened before lens 1 per §2.0); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit.

**Cold panel (T2, full form):** scout at walk 0 (seat-scout.md: 14 findings, 19 rows folded before lens 1); DISCOVERY at walk 3 lenses 1–2 (seat-discovery.md: 15 findings, 27 rows; D4/D11 held to lens 4); EXECUTION at walk 3 lens 3 (seat-execution.md: 7 findings, 12 rows; 43 rehearsal rows, 37 MATCH — every post-condition reached in a scratch clone); CAPSTONE at walk 3 lenses 4–5 (seat-capstone.md: 11 findings + the two held, folded at lenses 4–5).

**Closing:** WARM close after walk 5 — thread 259's plan, a judged stop after the full-form cold panel. Five walks: instruction 19 (scout, at walk 0) → 2 → 0 (the freeze) → 46 (the panel's fold pass: DISCOVERY, EXECUTION, CAPSTONE, seat by seat in lens order) → 1 → 0. T2: scout + three seats convened; the CAPSTONE read the fold set; walk 5 fully dry (§2.6). Deviations declared: walk 4 committed by hand (`lens_commit.py` refuses CONTINUE — thread 266). Deposit via `ready-`; HOLDS on class shop-infra (writes under `hooks/eluvian/` and `tests/`), the CEO releases; the hooks go LIVE at the daemon's teardown at the QA pause, before the verdict (Post-close). Close acts: none beyond the release; the depositor's parse re-run at this close (15 writes, shop-infra, step 2 a QA step).
- Weak spots:          w1 2 folded — instruction 2 / record 0
- Destruction:         w1 dry
- Vulnerabilities:     w1 dry
- Integration-record:  w1 dry
- ACID:                w1 dry
- Weak spots:          w2 dry
- Destruction:         w2 dry
- Vulnerabilities:     w2 dry
- Integration-record:  w2 dry
- ACID:                w2 dry
- Weak spots:          w3 24 folded — instruction 18 / record 6
- Destruction:         w3 3 folded — instruction 1 / record 2
- Vulnerabilities:     w3 12 folded — instruction 9 / record 3
- Integration-record:  w3 12 folded — instruction 7 / record 5
- ACID:                w3 5 folded — instruction 4 / record 1
- Weak spots:          w4 1 folded — instruction 1 / record 0
- Destruction:         w4 dry
- Vulnerabilities:     w4 dry
- Integration-record:  w4 dry
- ACID:                w4 dry
- Weak spots:          w5 dry
- Destruction:         w5 dry
- Vulnerabilities:     w5 dry
- Integration-record:  w5 dry
- ACID:                w5 dry
**Walk 5 — DRY, all five lenses, one commit per lens. Instruction 0 on a full pass after the panel: the close meets the bar (§2, §2.6) — a judged stop (66 findings over the cycle: scout 14, warm 2, panel 33 folded as 56 rows, walk 4 one; yield 19 → 2 → 0 → 56 → 1 → 0). T2: the full-form panel convened and its fold set read by the CAPSTONE.**
**Walk 4 — one fold at lens 1 (instruction 1 / record 0), four lenses dry. Instruction non-zero: walk 5 owed (§2.6: a folding panel closes only on a FULLY dry walk).**
**Walk 3 — the cold panel's fold pass, seat by seat: 24 + 3 + 12 + 12 + 5 = 56 rows (instruction 46 / record 10) across the five lenses; the CAPSTONE read the fold set; walk 4 is the FULLY dry walk §2.6 requires after an early-fired or folding panel.**
**Walk 2 — DRY, all five lenses, one commit per lens: the warm walks converge; the FREEZE — the full-form cold panel (DISCOVERY, EXECUTION, CAPSTONE) convenes on this text per §2.6, its findings folded as walk-3 rows, then a final dry walk closes.**
**Walk 1 — two folds at lens 1 (instruction 2 / record 0, both fold-introduced by the scout's s5 set), four lenses dry; one commit per lens.**

## Cycle Manifest
tier: T2
target: hooks/eluvian/_common.py
class: shop-infra
reads: hooks/eluvian/wrap_arm_hook.py, hooks/eluvian/wrap_stop_hook.py, hooks/eluvian/wrap_check.py, hooks/eluvian/eluvian_align_hook.py, hooks/eluvian/wrap_debt_hook.py, hooks/README.md, tests/test_hook_default_root.py, tests/test_wrap_hooks.py, tests/test_align_hook_sync.py, tests/test_wrap_sentinel.py, tests/test_wrap_receipts.py, tests/test_wrap_memory_class_gate.py, tools/mutation_check.py, knowledge/decisions/Done/executable-100015.md, knowledge/decisions/Done/executable-100055.md, eluvian-governance/MACHINE_SETUP.md
writes: hooks/eluvian/_common.py, hooks/eluvian/wrap_arm_hook.py, hooks/eluvian/wrap_stop_hook.py, hooks/eluvian/wrap_check.py, hooks/eluvian/eluvian_align_hook.py, hooks/eluvian/wrap_debt_hook.py, hooks/README.md, tests/test_hook_default_root.py, tests/test_wrap_sentinel.py, tests/test_hooks_shared_common.py, knowledge/mutants/hooks-shared-common.json, knowledge/mutants/hooks-shared-common.run.txt, knowledge/development/dev-log-hooks-shared-common-2026-09-10.md, knowledge/qa/evidence/hooks-shared-common-qa-evidence-2026-09-10.md, knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt
open_forks: 1. `_DEFAULT_LOG` de-hardcoded to `Path.home() / ".claude" / "eluvian" / "hooks.log"` (a behaviour change on any machine whose user is not `marklehn` — its own T1 with MACHINE_SETUP's word); 2. `wrap_check.py`'s `_resolve_bellows`/`_tuyere_checkout` twins with `plan_claim` (thread 100015's twin contract) moved to a shared home — a different subsystem, not this plan; 3. the align hook's SessionStart context and the debt hook's share one `emit` — a future `emit(context, event)` if a non-SessionStart hook ever needs it (none does today); 4. `_wrap_root` and `_BELLOWS_DISPATCH_ALLOW` into `_common` (two-site each; left here by decision — panel D7)
walks: 5
yields: 2, 0, 39, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:49
coherence: 5/5 body walks named in the register (81 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-hooks-shared-common.md.foldcheck.json

---

## STEP 1 — DEV (one module by extraction, five hooks re-pointed, one test replaced, one sibling added; the mutation run; two commits)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter for tests is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE; the harness interpreter for the import and run checks is `/usr/bin/python3` (3.9.6).** ⛔ Never run the daemon, `run_plan`, a claim; never import `lifecycle`; never touch `~/.claude/` (read `ls -la ~/.claude/eluvian` once, quote it, and use the REPO path `hooks/eluvian/` for everything). ⛔ Every deposit is written in THIS worktree (`"$(git rev-parse --show-toplevel)"`), never in the canonical checkout.
>
> **Scope:**
> - `hooks/eluvian/_common.py`
> - `hooks/eluvian/wrap_arm_hook.py`
> - `hooks/eluvian/wrap_stop_hook.py`
> - `hooks/eluvian/wrap_check.py`
> - `hooks/eluvian/eluvian_align_hook.py`
> - `hooks/eluvian/wrap_debt_hook.py`
> - `hooks/README.md`
> - `tests/test_hook_default_root.py`
> - `tests/test_wrap_sentinel.py`
> - `tests/test_hooks_shared_common.py`
> - `knowledge/mutants/hooks-shared-common.json`
> - `knowledge/mutants/hooks-shared-common.run.txt`
> - `knowledge/development/dev-log-hooks-shared-common-2026-09-10.md`
>
> **Item 1 — re-derive P1, P2, P4 and P5 and HALT on a mechanism mismatch** (P3, P6–P8 are records; a line-number drift is not a mismatch; a changed body hash, a missing symlink, a settings entry that names a copy, or a hook suite count below 141 is). Paste the `grep -n` inventory, the five shas, the `ls -la ~/.claude/` line, the four settings entries, `/usr/bin/python3 --version` and the 141 line under the first declared heading.
> **Item 2 — write the failing tests FIRST.** Replace test (d) in `tests/test_hook_default_root.py` as *What this changes* 3 (it is red now: no `_common.py` to load) and write `tests/test_hooks_shared_common.py` (s1)–(s5) as *What this changes* 4 — run the two files: (d) and (s1)–(s4) red for the right reason (`FileNotFoundError`/`ModuleNotFoundError` on `_common`, or (s1)'s grep finding the local `def`s), (s5) GREEN before and after (it pins behaviour the hooks already have — panel D10), the other six `test_hook_default_root` tests still GREEN (they read the hooks as they are). Paste the run under the second declared heading.
> **Item 3 — the extraction.** Create `_common.py` by COPYING each body from the site P1 names first for it (arm for `_default_root`, `_log_path`, `hooklog`, `_validate_session_id` and the regex constant; align for `emit`; the `_DEFAULT_LOG` line from arm), with the one docstring rewrite named in *What this changes* 1; then, per hook, insert the two import lines after its stdlib imports (after the `from __future__` line where present — that line must stay first) and delete the moved `def` blocks, the `_DEFAULT_LOG` line and, in arm, stop and debt, the `_VALID_SESSION_ID` constant; rewrite `hooks/README.md`'s first two paragraphs as *What this changes* 6; add the one copy line to `tests/test_wrap_sentinel.py::_make_passcheck_dir` (*What this changes* 3); write the mutation manifest as *What this changes* 5 (it rides the FIRST commit — panel D14). Then: the eight hook test files plus the sibling (`146 passed` over the nine: 141, test (d) replaced one-for-one, s1–s5 added as five test FUNCTIONS — loops inside, no `parametrize`; a parametrized shape states its own count and why — panel D13), then the FULL suite, green — `N passed, 1 skipped` (the `test_gate_watcher` live-DB test skips wherever `lifecycle.db` is absent beside the tree, as in a worktree; a skip is not a failure — panel E7).
> **Item 4 — the harness-interpreter check, pasted:** `for h in wrap_arm_hook wrap_stop_hook wrap_check eluvian_align_hook wrap_debt_hook _common; do /usr/bin/python3 -c "import importlib.util,sys; sys.path.insert(0,'hooks/eluvian'); s=importlib.util.spec_from_file_location('$h','hooks/eluvian/$h.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print('$h imports OK')"; done` — six `imports OK` lines, under 3.9, with `ELUVIAN_WRAP_ROOT` set to the worktree root for the run; then the canary, the variable REMOVED — `env -u ELUVIAN_WRAP_ROOT /usr/bin/python3 -c "import importlib.util; s=importlib.util.spec_from_file_location('h','hooks/eluvian/wrap_arm_hook.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print(m._default_root())"` → exactly `/Users/marklehn/Developer/eluvian-governance` (the fallback resolved THROUGH `from _common import _default_root` under 3.9 — MACHINE_SETUP §6's canary; 100015 A2; panel D11).
> **Item 5 — first commit**, gated on the FULL suite AND the pre-check, path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && ( /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" 2>&1 || true ) | grep -E '^(FAIL|PRECHECK)' && git add hooks/eluvian/_common.py hooks/eluvian/wrap_arm_hook.py hooks/eluvian/wrap_stop_hook.py hooks/eluvian/wrap_check.py hooks/eluvian/eluvian_align_hook.py hooks/eluvian/wrap_debt_hook.py hooks/README.md tests/test_hook_default_root.py tests/test_wrap_sentinel.py tests/test_hooks_shared_common.py knowledge/mutants/hooks-shared-common.json && git commit -F <msg-file> -- hooks/eluvian/_common.py hooks/eluvian/wrap_arm_hook.py hooks/eluvian/wrap_stop_hook.py hooks/eluvian/wrap_check.py hooks/eluvian/eluvian_align_hook.py hooks/eluvian/wrap_debt_hook.py hooks/README.md tests/test_hook_default_root.py tests/test_wrap_sentinel.py tests/test_hooks_shared_common.py knowledge/mutants/hooks-shared-common.json` — eleven files (the manifest rides this commit so the mutation run's `HEAD:` archive contains it — panel D14; the sentinel test file rides it for panel D1); the message tagged with the plan id and `thread 259`. (The pre-check reads this plan's Deposits block: at this commit the dev-log and the run file do not exist yet — the pre-check reports SEVEN failures: two `rule_22_verification` (the two missing deposits) and five `dev_log_declared_text` (the four declared headings and the P2 cell live in the missing dev-log); that is expected at the FIRST commit (panel E3) and the chain therefore wraps the pre-check as `( … 2>&1 || true ) | grep -E '^(FAIL|PRECHECK)'` ONLY here — a subshell, never a bare `; true`, which would end the `&&` chain and detach the suite gate from the commit (scout S6); the `grep` prints the paste and exits 1 when neither a `FAIL` nor a `PRECHECK:` line was produced, so a pre-check that CRASHED (a typo'd `<id>`, a wrong directory) refuses the chain instead of passing silently (panel E1; on a refusal, re-run the pre-check bare to read the traceback — the subshell's `2>&1` feeds the grep, not the paste — panel C7) — the paste MUST contain the `PRECHECK: 7 failure(s)` line; the second commit's chain has no such allowance; the stage-aware flag the tool lacks is thread 264.)
> **Item 6 — the mutation run, REDIRECTED into `knowledge/mutants/hooks-shared-common.run.txt` (a Deposit):** the manifest committed at Item 5 (*What this changes* 5); `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/hooks-shared-common.json > knowledge/mutants/hooks-shared-common.run.txt 2>&1`; the last line must read `MUTATION: <n> killed, 0 survived, 0 error` with n ≥ 6 — a survivor is a test to write, not a mutant to delete.
> **Item 7 — the dev-log** `knowledge/development/dev-log-hooks-shared-common-2026-09-10.md` under the four headings declared below; `## Pins re-derived (P1, P2, P4, P5)` opens with P2's value cell pasted verbatim, whole, to its last character — ⛔ the VALUE cell (third column) ends with the words `(2026-08-25, plan 520)`; paste through those words, then stop. `## Extraction diff` carries, per hook, `git diff --stat main -- hooks/eluvian/<hook>.py` and the count of deleted `def` lines. **Second commit**, gated on the FULL suite AND the run file AND the pre-check, path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && grep -Eq 'MUTATION: ([6-9]|[1-9][0-9]+) killed, 0 survived, 0 error' knowledge/mutants/hooks-shared-common.run.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/mutants/hooks-shared-common.run.txt knowledge/development/dev-log-hooks-shared-common-2026-09-10.md && git commit -F <msg-file> -- knowledge/mutants/hooks-shared-common.run.txt knowledge/development/dev-log-hooks-shared-common-2026-09-10.md` — two files.
> **Headings:** `## Pins re-derived (P1, P2, P4, P5)`; `## Failing-first (test (d) and s1–s4 red, s5 and six green, then all green)`; `## Extraction diff`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P2, P4, P5)` ← P2
>
> **Deposits:**
> - `hooks/eluvian/_common.py`
> - `hooks/eluvian/wrap_arm_hook.py`
> - `hooks/eluvian/wrap_stop_hook.py`
> - `hooks/eluvian/wrap_check.py`
> - `hooks/eluvian/eluvian_align_hook.py`
> - `hooks/eluvian/wrap_debt_hook.py`
> - `hooks/README.md`
> - `tests/test_hook_default_root.py`
> - `tests/test_wrap_sentinel.py`
> - `tests/test_hooks_shared_common.py`
> - `knowledge/mutants/hooks-shared-common.json`
> - `knowledge/mutants/hooks-shared-common.run.txt`
> - `knowledge/development/dev-log-hooks-shared-common-2026-09-10.md`
>
> **Post-conditions:** `grep -c '^def _default_root\|^def hooklog\|^def emit\|^def _validate_session_id\|^def _log_path\|^_DEFAULT_LOG\|^_VALID_SESSION_ID' hooks/eluvian/<each hook>.py` → 0 for all five and `7` for `_common.py`; each hook carries exactly one `from _common import` line; the eight hook test files green with test (d) replaced and (s1)–(s5) added; six `imports OK` under `/usr/bin/python3` plus the canary's one line; the mutation run's last line `n ≥ 6 killed, 0 survived, 0 error`; `git diff --stat main...HEAD` (three-dot — the branch's own changes; two-dot would count commits main gained during the run, scout S7) names exactly the thirteen Scope files; the four declared headings present with P2's cell whole; nothing under `~/.claude/` touched (`ls -la ~/.claude/eluvian` unchanged and quoted).

## STEP 2 — QA (full suite + the extraction proven body-for-body + the four harness hooks RUN live through the symlinked path under 3.9)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK (the literal, not a reference — panel E4). Interpreters ABSOLUTE; read-only beyond the two evidence files; `~/.claude/` read, never written.
>
> **Item 1 — full suite, REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt 2>&1`; the summary line quoted in the receipt; the nine hook test files' own summary (`-q` over the eight plus the sibling: `146 passed`) quoted beside it.
> **Item 2 — the extraction proven body-for-body:** for each shared helper, `git show main:hooks/eluvian/<origin hook>.py` into `$T`, extract the `def` block by name (AST `lineno`–`end_lineno`), extract the same from `_common.py`, and diff — expected: empty for `_log_path`, `hooklog`, `emit`, `_validate_session_id`; for `_default_root` the diff is exactly the docstring lines *What this changes* 1 rewrites and nothing in the body; paste each diff (or `IDENTICAL`).
> **Item 3 — the four harness hooks RUN live, under the harness interpreter, through the symlinked path:** with `ELUVIAN_WRAP_ROOT=$T/root` (a scratch dir holding `COMPANY.md` AND a one-line `bellows/status.py` stub), `ELUVIAN_WRAP_LESSONS_FORGE=$T/lf`, `ELUVIAN_WRAP_TUYERE=$T/lf` and `ELUVIAN_WRAP_MEMORY=$T/mem` (empty scratch dirs — the last so `wrap_check`'s `[4/memory]` arm reads nothing of the CEO's live memory directory, panel C11; an explicit tuyere override with no `.venv` makes `wrap_check._tuyere_checkout` return `None`, so the debt hook's `_session_wraps_today` never launches the canonical tuyere CLI — panel E2) and `ELUVIAN_HOOKS_LOG=$T/hooks.log` — so the align hook's `_sync_repos` and `_daemon_status` resolve inside `$T` and never `git fetch` the canonical checkouts or run the canonical `status.py` (panel D6), run each of `wrap_arm_hook`, `wrap_stop_hook`, `wrap_debt_hook`, `eluvian_align_hook` as `env -u BELLOWS_DISPATCH /usr/bin/python3 /Users/marklehn/.claude/eluvian/<hook>.py` (the daemon sets `BELLOWS_DISPATCH=1` in every agent's environment and the stop and debt hooks take their `daemon-exempt` branch under it — panel D5) with the stdin payload `{"session_id": "qa-<uuid>", "hook_event_name": "<the hook's event>", "cwd": "$T"}` — ⚠️ the symlinked path resolves to the CANONICAL checkout's `hooks/eluvian/`, which at QA time still holds the PRE-merge hooks; therefore run the same four commands a second time with the path replaced by THIS worktree's `hooks/eluvian/<hook>.py`, and paste both — expected on the worktree run: exit 0, one JSON line each (`{}` or a `hookSpecificOutput`), `$T/hooks.log` carrying each hook's NON-exempt line (`unarmed-allow` or `ignored` for stop, `clean` or `DEBT-injected` for debt, the align hook's `parked=… sync=…` line, the arm hook's line) and never `daemon-exempt`; on the canonical run: the same shape from the old code (the control). State that the live wiring picks up the new files at the merge, by the symlink, with no act. The canary again, on the worktree hook: Item 4's `env -u ELUVIAN_WRAP_ROOT /usr/bin/python3 -c "…"` line with the path `$(git rev-parse --show-toplevel)/hooks/eluvian/wrap_arm_hook.py` → `/Users/marklehn/Developer/eluvian-governance`, pasted (panel D11).
> **Item 4 — the identity test proven earnable, then the regression it guards:** `pytest tests/test_hook_default_root.py -q` green; then, with `_common` imported from `hooks/eluvian` FIRST (so the copy's own `from _common import` resolves through `sys.modules`), a copy of `wrap_arm_hook.py` in `$T` with a local `def _default_root` pasted in AFTER its import line → the copy's `_default_root is not common._default_root` (a five-line probe, pasted; the order is the point — panel D15) — the property test (d) now guards.
> **Item 5 — production writes, stated exactly:** none outside the two evidence files; no `git fetch` of any canonical checkout, no run of the canonical `status.py`, no launch of the canonical tuyere CLI and no read of the CEO's memory directory (the align hook's sync and status, and `wrap_check`'s tuyere and memory resolution, were pointed inside `$T` — Item 3); no lane file, no lifecycle row, no lifecycle import; `~/.claude/` untouched (`ls -la ~/.claude/eluvian` quoted, unchanged); every scratch dir removed (`[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`).
> **Item 6 — receipt** `knowledge/qa/evidence/hooks-shared-common-qa-evidence-2026-09-10.md`: `numstat` over the DEV commits (`<base>..<dev>`, thirteen files); a `## Verification` table with one row per Item 1–5, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and nothing else, and no positive row's text carries a hedging keyword: quote the suite line as `2186 passed` and name the skip as `one skip (test_gate_watcher, live-DB — panel E7)`, never the word the block scans for (panel C1; 100055 Item 8's clause); the quoted node ids limited to the two test files' methods. Then RUN the canonical Rule 20 block (`$ELUVIAN_WRAP_ROOT/RULE_20_SELF_CHECK_BLOCK.md`, the path the dispatcher's mandate names) with the four plan-side values — `plan_slug: hooks-shared-common-2026-09-10`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["hooks-shared-common-suite-2026-09-10.txt"]` — and APPEND its stdout to the receipt: the banner `Rule 20 — QA Self-Check Results` and the closing `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored (100015's (A) hunk; panel C2); a `FAILED` stdout goes into the receipt as written and the step STOPS and requests a verdict (thread 262).
> **Item 7 — commit**, path-scoped and gated: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/hooks-shared-common-qa-evidence-2026-09-10.md knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt && git commit -F <msg-file> -- knowledge/qa/evidence/hooks-shared-common-qa-evidence-2026-09-10.md knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt`. ⛔ A QA step that finds it must change production code STOPS and requests a verdict (thread 262) rather than committing the change.
>
> **Deposits:**
> - `knowledge/qa/evidence/hooks-shared-common-qa-evidence-2026-09-10.md`
> - `knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/hooks-shared-common-qa-evidence-2026-09-10.md`
> - `knowledge/qa/evidence/hooks-shared-common-suite-2026-09-10.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 1 skipped` with N ≥ the DEV's count and no `failed` (panel E7); Item 2's five diffs pasted (four empty, one docstring-only); Item 3's eight runs pasted (four worktree, four canonical control) with exit codes; Item 4's probe pasted; the receipt's `## Verification` table has five rows; one QA commit carrying the two evidence files; `~/.claude/` untouched.
