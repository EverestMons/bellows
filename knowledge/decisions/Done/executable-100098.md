# bellows — executable: THE ABANDONED-RUNNER-CLOSE RECORD, WRITTEN FROM ITS OWN RUN, AND THE QA IT NEVER REACHED — plan 100097's dev-log written under the four headings it declared, from the halted run's transcript and runs made here; the stray dev-log removed; then 100097's own QA over its merged code (thread 306)

**Date:** 2026-09-14 | **Project:** bellows | **Tier:** Small (one record written, one removed, the halted plan's QA) | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** full-suite (Rule 21 — 100097's merged `bellows.py` and `lifecycle.py` are imported by most of the suite; `pytest tests/ -q` gates the DEV commit and is QA Item 1) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Discharges:** thread 306

**auto_close:** false

**Post-close:** ⚠️ **RESTART OWED** — 100097's merged `bellows.py`, `lifecycle.py` and `notifier.py` are daemon-loaded; the restart was deferred from 100097's halt to this close. The CEO restarts with `bellows.py restart`, and that first startup runs the steps-table migration on the live `lifecycle.db`, its backup landing beside it as `lifecycle.db.pre-steps-abandoned-<UTC stamp>.bak`. Then two Planner acts, once each, from the canonical checkout: `.venv/bin/python tools/reconcile_steps.py` (LIST) and then `--apply` — the census's three running phantoms to `abandoned`, the printed rows pasted into the baton; and `tuyere.threads done 306 --commit bellows:8b52344` (the DEV commit that touched `lifecycle.py`; component `lifecycle`). The doctrine follow-ups are the halted plan's Post-close's (`knowledge/decisions/halted-executable-100097.md:7`), each a CEO apply act after this close.

**Depends on:** the CEO's halt of 100097 (2026-09-14 01:16, STEP 1: `deposit_exists`, `rule_22_verification`, `scope_check` and five `dev_log_declared_text` rows — the dev-log written as `…-2026-09-13.md` under generic headings, none of the four declared and no P3 cell, and the run's four commits made without the plan's gated chains, its transcript holding no `check_deposit` call) and the ruling that the record is written FROM the run, never from memory (LESSONS 2026-09-09, the #100054 and #100055 shape). The merged code: bellows `8b52344`, `a4bb61d`, `aa9a9ed` and `8f836ac` over `06b545f`, on origin (P1). Clone origin BY KIND: `Done/executable-100055.md` (2026-09-09, a halted plan's record written from its transcript, then the QA it owed; T1); the QA step is the halted plan's own STEP 2 (`halted-executable-100097.md:141–159`), carried unchanged but for its numstat range and its two references to the halted plan's item 1.

**Tier computed (§1):** **T1** — T-8 fires: the record half clones #100055, but the QA half is 100097's STEP 2, which has never executed, and §1's if-unsure rule reads it as novel. T-2 does not (the QA's migration and reconcile run on a backup-API copy; the live database is only read); T-5 does not (the stray record's removal is a tracked `git rm`, cleanly revertible); T-1 does not (records only, no source file); T-6 does not.

## CEO Context

306's code shipped — merged on main and on origin — but its DEV wrote its record from memory, under the wrong name and headings, and committed without the pre-checks, so you halted it before its QA. This plan finishes 306 without touching its code: it writes the record the halted plan declared, from the run's own transcript and from runs made in this step, removes the stray record, and runs the QA 306 never reached — the full suite, and the migration and the reconcile tool on a copy of the live database. Its writes are records only, so the depositor classes it app-feature and it clears without a release; the daemon restart 306 owes waits for this close.

## What this changes

⛔ **One record written, one removed, the halted plan's QA run; no source file moves.**

1. **`knowledge/development/dev-log-abandoned-runner-close-2026-09-12.md`** — WRITTEN under the four headings 100097 declared (P2), every number quoting the halted run's transcript (P4) or a run made in this step, each section saying which: `## Pins re-derived (P1, P2, P3, P4, P7, P10)` OPENS with THIS plan's P3 value cell pasted verbatim (the gate compares against this plan's pin table; the cell is the halted plan's P3, unchanged), then the halted plan's P1, P2, P4, P7 and P10 (`halted-executable-100097.md:65–74`) re-derived against `06b545f`, the code they describe, and its P3 from the live database read `mode=ro`, then the DEV's own P3 read, marked `(from the 100097 transcript)`; `## Failing-first (four red, then green)` carries the four red lines of a run made in this step — the four new test files, as merged, over an archive of `06b545f` in scratch, after a control run of the three that exist there came back green — and the green line of this step's full suite, the transcript's red runs named as mid-implementation, not failing-first; `## The close observed (c-t1 to c-t4)` the four PASSED lines of a `-v` run made in this step (the halted run made none); `## Mutation run` the run file's last line as committed at `8f836ac`; and `## The halt, recorded` — the four commits where the halted plan asked for two, the two fix commits' own messages, and the transcript's absent `check_deposit` call.
2. **`knowledge/development/dev-log-abandoned-runner-close-2026-09-13.md`** — REMOVED (`git rm`): the halted run's record, written from memory under generic headings (P2); it stays in history at `8f836ac`.
3. **STEP 2** — the halted plan's QA, unchanged but for its numstat range and its two references to the halted plan's item 1; its deposits keep the halted plan's declared names, so 306's record completes under them.

## Why this exists

Plan 100097 shipped its code and failed its record. The DEV step's gate read the plan's `**Headings:**` and `**Verbatim:**` lines and found the declared dev-log missing and, in its place, a file under the next day's date with three generic headings and no P3 cell; the run had committed four times without once calling the pre-check that would have refused it. The code — 30 mutants killed, the suite green by the run's own report — is merged and pushed. Overriding the gate would have accepted a record written from memory, the defect the gate exists to catch; the CEO halted. What is owed is the record, written from the run that made it, and the QA the halted plan never reached.

## What this does NOT do

- ⛔ **It does not change 100097's code** — no source, test or manifest file moves; the run file stands as committed.
- ⛔ **It does not invent the record** — every section quotes the transcript or a run made in this step and says which; a section with no source in the transcript is measured fresh and says so.
- **It does not re-run the mutation run** — no source moved since `8f836ac`, so the committed run file is the run of the code under QA.
- **It does not restart the daemon or migrate the live database** — the restart is the CEO's, after this close; the QA migrates a copy.
- **It does not touch the halted plan's lane file or its `awaiting_verdict` step row** — the halt route leaves both, as it has for the seven other halted plans' steps.

## MUST-PRESERVE

- ⛔ **No source file moves.** The DEV commit carries the two dev-log paths and nothing else; QA's commit, the two evidence files.
- ⛔ **The four declared headings are lines of the dev-log, and `## Pins re-derived (P1, P2, P3, P4, P7, P10)` opens with P3's cell whole.**
- ⛔ **Nothing in the record is written from memory** — each number names its transcript event or the command run here.
- ⛔ **No live database write, and no act on the canonical checkout's worktrees or branches:** the live `lifecycle.db` is read only `mode=ro`; the failing-first run lives in an archive under `/tmp`, never a registered worktree.
- ⛔ **The thread-262 stop:** a step that must change production code STOPS and requests a verdict.

## Numbers discipline — measured 2026-09-14 by the Planner (bellows `8f836ac`, governance `a4b3bb94`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the merged code | bellows `main` = `origin/main` at `8f836ac` over `06b545f`: `8b52344` (9 files, +3074/−84: `bellows.py`, `lifecycle.py`, `notifier.py`, `tools/reconcile_steps.py`, the four test files, `knowledge/mutants/abandoned-runner-close.json`), `a4bb61d` (the manifest and the close tests), `aa9a9ed` (the close tests), `8f836ac` (the run file and the stray dev-log); eleven files net over `06b545f..8f836ac`; `init_lifecycle_db` lifecycle.py:24, `_migrate_steps_abandoned` :203, `record_step_start` :623, `mark_step_abandoned` :696; `_check_idle` bellows.py:285, `_live_processes_in` :1029, `_preserve_and_remove_stranded_worktree` :1108, `_close_abandoned_runner` :1243, `_run_startup_recovery` :1510; the run file's last line `MUTATION: 30 killed, 0 survived, 0 error` | `git log --format='%h %s' 06b545f..main`; `git show --numstat --format= <sha>`; `grep -n '^def '` |
| P2 | ⛔ the halted record and plan | `knowledge/development/dev-log-abandoned-runner-close-2026-09-13.md` (106 lines; headings `## What was built` :3, `## Key decisions` :44, `## Testing notes` :69 — none of the four declared, no P3 cell); the halted plan `knowledge/decisions/halted-executable-100097.md`, byte-identical to governance `75344dd2`'s draft (their sha-256 digests equal): its `**Headings:**` and `**Verbatim:**` lines :131–132, its pin table :65–74, its STEP 2 :141–159, its Post-close :7 | `shasum -a 256`; `grep -n` |
| P3 | ⛔ the steps table and the live DB | `steps` lifecycle.py:83–97, `CHECK (status IN ('pending','running','awaiting_verdict','complete'))` :89, `UNIQUE(plan_id, step_number)` :96; four tables reference `steps(id)` — `commits` :102, `deposits` :111, `gate_events` :132, `step_files` :143 — with 207, 592, 1601 and 312 rows, none referring to a missing step; steps 166 rows (max id 166): `complete` 156, `awaiting_verdict` 7, `running` 3 — ids 90 (plan 100051 step 2), 141 (100081 step 1) and 142 (100082 step 1), each on an `abandoned` plan; plans `closed` 84, `halted` 11, `abandoned` 3, none `in_progress`; `PRAGMA foreign_keys` 0 on a fresh connection; the one index on `steps` is `sqlite_autoindex_steps_1`; no trigger and no view | the venv interpreter's `sqlite3.connect('file:<db>?mode=ro', uri=True)` and the queries — never the system `sqlite3` CLI, whose SQLite 3.43.2 refuses a read-only open of a WAL database no writer holds |
| P4 | ⛔ the transcript | `logs/20260913-174007-step.json` (7,631,384 bytes; keys `parsed`, `raw_output`, `stderr`, `success`, the last true); `raw_output` 6,954,540 characters, 2,716 parsed events; the full suite `2364 passed, 2 skipped, 9 warnings in 230.09s (0:03:50)` at event 1671 and `… in 226.44s (0:03:46)` at event 2304; red runs mid-implementation at events 976–1399 (`16 failed, 9 passed` at 1206); no `check_deposit` call; four `git commit` commands; no `-v` run of c-t1..c-t4; the DEV's P3 read at event 50, after a first attempt from the worktree path failed at event 46 | a `python -c` that parses the file, importing no bellows module |
| P5 | the close tests | `tests/test_abandoned_runner_close.py`: c-t1 `test_preserved_branch_and_worktree_removed` :173, c-t2 `test_lane_and_lifecycle_state_after_close` :211, c-t3 `test_claim_released_and_page_sent` :256, c-t4 `test_live_process_refused_then_closed` :310 — module-level functions, so each node id is `tests/test_abandoned_runner_close.py::<name>` | `grep -n '^def test_'` |
| P6 | in flight; class; the daemon | none in flight at drafting; `halted-executable-100097.md` in the lane, its claim released and its receipt archived; the daemon pid 61599 running the code it loaded at start, before 100097 merged (the restart deferred to this close); 306's two QA deposit paths free on `main`; the writes → `_assign_class` → **app-feature** (read at drafting: every write under bellows `knowledge/`, none read-only and no register pattern — it clears without a release) | `status.py`; `ls`; the depositor at deposit |

DEV re-derives P1, P2, P5 and P6 in Item 1 against `main` and HALTs on a mechanism mismatch; P3 and P4 are re-derived in Items 1 and 2.

## Drafting Cycle

**Tier:** **T1** — T-8 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-abandoned-runner-close-record-2026-09-14.md
**Walks:** walk 0 pinned (P1–P6 measured 2026-09-14 on bellows `8f836ac` and the halted plan; clone-diff against `Done/executable-100055.md` run: FACTS, ARTEFACTS, STRUCTURE — its premise that the verbatim gate reads this plan's own pin table re-measured by a positive and a negative control on a scratch mirror; the parent's register and bellows' standing rules diffed; the deposit gates dry-run on a scratch copy; one fold at v0, f0); `fold_check --save-baseline` ARMED on v0 before any walk fold; re-saved after every intended edit; no walk-0 scout (T1, the Planner's call: the QA half passed 100097's full T2 panel); every lens commit through `/Users/marklehn/Developer/bellows/scripts/lens_commit.py`, the tool run bare, its exit read; `lens_order_check` after every lens commit, and before the receipt on a copy of the draft in a scratch project-shaped repository with history.

- Weak spots:          w1 5 folded — instruction 5 / record 0; w2 1 folded — instruction 1 / record 0; w3 dry
- Destruction:          w1 dry; w2 dry; w3 dry
- Vulnerabilities:      w1 1 folded — instruction 1 / record 0; w2 dry; w3 dry
- Integration-record:   w1 dry; w2 dry; w3 dry
- ACID:                 w1 dry; w2 dry; w3 dry
**Closing:** WARM close after walk 3 — BAR MET (T1), the record-and-QA follow-up to the halted 100097 (thread 306), on a dry walk. Three walks, findings by class — walk 1: 6 (instruction 6 / record 0), walk 2: 1 (instruction 1 / record 0), walk 3: 0; with f0, one record-class fold at v0. Walk 1 executed the failing-first item before folding it, in scratch: the archive named so its reds are the plan's, and a control run of the base's own tests to prove it; walk 2 moved the record's two sources, an untracked lane file and a gitignored transcript, to the canonical checkout's absolute paths. No panel (T1). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-`; the class app-feature, as the manifest declares, the depositor's admission deciding — it clears without a release; both step pauses are the Planner's verdicts; after this close the CEO restarts the daemon, then the Planner runs the reconcile tool's LIST and its apply once and closes thread 306.

## Cycle Manifest
tier: T1
target: knowledge/development/dev-log-abandoned-runner-close-2026-09-12.md
class: app-feature
reads: knowledge/decisions/halted-executable-100097.md, logs/20260913-174007-step.json, lifecycle.py, bellows.py, notifier.py, tools/reconcile_steps.py, tools/check_deposit.py, tests/test_lifecycle.py, tests/test_abandoned_runner_close.py, tests/test_reconcile_steps.py, tests/test_stop_path.py, tests/conftest.py, knowledge/mutants/abandoned-runner-close.run.txt, knowledge/decisions/Done/executable-100055.md
writes: knowledge/development/dev-log-abandoned-runner-close-2026-09-12.md, knowledge/development/dev-log-abandoned-runner-close-2026-09-13.md, knowledge/qa/evidence/abandoned-runner-close-qa-receipt-2026-09-12.md, knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt
open_forks: 1. a DEV commit made outside its plan's gated chain is caught only at the pause — the halted run committed four times with no pre-check, and nothing refused it at the desk; 2. the record-only follow-up is now a recurring shape (100055, this plan) — the halted plan's pin table, headings and transcript could generate it
walks: 3
yields: 6, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:61
coherence: 3/3 body walks named in the register (8 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-abandoned-runner-close-record.md.foldcheck.json

---

## STEP 1 — DEV (the record from the run; one commit)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ Never run the daemon, `run_plan`, a claim, `_run_startup_recovery`, `_close_abandoned_runner`, `stop_daemon` or `tools/reconcile_steps.py --apply` outside a test; the live `lifecycle.db` is opened only read-only (`mode=ro`), for Item 1's P3 counts. ⛔ No source, test or manifest file changes in this step: the two dev-log paths in Scope are its only writes. ⛔ **The record is written FROM `/Users/marklehn/Developer/bellows/logs/20260913-174007-step.json` (`raw_output`) or from a run made in this step — never from memory**; each section names its source, the transcript's event number or the command run here. ⛔ Test code runs under pytest or not at all (LESSONS 2026-09-09): no test helper is called from `python -c` — the only `python -c` runs this step makes are Item 1's read-only `sqlite3` query for P3 and Item 2's transcript read, neither importing a bellows module. ⛔ The failing-first run lives in scratch: `T=$(mktemp -d /tmp/abandoned-close-record.XXXXXX)`, never a registered worktree; the canonical checkout's `.bellows-worktrees/`, branches and worktree list stay as they are.
>
> **Scope:**
> - `knowledge/development/dev-log-abandoned-runner-close-2026-09-12.md`
> - `knowledge/development/dev-log-abandoned-runner-close-2026-09-13.md`
>
> **Item 1 — the pins, re-derived**: this plan's P1, P2, P5 and P6 against the worktree's HEAD (cut from `main`), HALT on a mechanism mismatch — `8f836ac` not an ancestor of HEAD, or one of P1's functions absent; a line-number drift is not one, and a later commit on `main` is a record, its files named; then the halted plan's P1, P2, P4, P7 and P10 (`/Users/marklehn/Developer/bellows/knowledge/decisions/halted-executable-100097.md:65–74` — the canonical checkout's lane file, untracked, so absent from this step's worktree) against `06b545f`, the code they describe — `git show 06b545f:<file> | sed -n '<range>p'` over each bellows line they cite, and tuyere's lines from its checkout, `/Users/marklehn/Developer/tuyere` — and its P3 from the live database, read-only: `/Users/marklehn/Developer/bellows/.venv/bin/python -c` over `sqlite3.connect('file:/Users/marklehn/Developer/bellows/lifecycle.db?mode=ro', uri=True)` with P3's queries. Each line is pasted beside the pin it re-derives; a count that moved is a record, pasted.
> **Item 2 — the transcript**: re-measure P4 with a `python -c` that parses `/Users/marklehn/Developer/bellows/logs/20260913-174007-step.json` (the canonical checkout's — `logs/` is gitignored, so absent from this step's worktree; key `raw_output`, one event per line) and imports no bellows module — its size, its keys, the two full-suite lines, the red runs, the `git commit` and `check_deposit` counts, and the DEV's P3 read; the event numbers count the lines of `raw_output` that parse as JSON objects, from 0; paste; HALT if a line P4 quotes is absent from the transcript.
> **Item 3 — failing-first, run here**: `mkdir -p "$T/bellows" && git archive 06b545f | tar -x -C "$T/bellows"` — the directory named `bellows`, since an unchanged test asserts that the default `lifecycle.db` path lies under one, and under any other name fails for the archive's location; first the control: from `$T/bellows`, run the three test files that exist at `06b545f` (`tests/test_lifecycle.py`, `tests/test_reconcile_steps.py`, `tests/test_stop_path.py`) as they stand there, each green (rehearsed at drafting: `104 passed`, `3 passed`, `7 passed`) — HALT if one is not, since the archive is then failing tests of its own; then `git show HEAD:tests/<file> > "$T/bellows/tests/<file>"` for the four test files (those three and `tests/test_abandoned_runner_close.py`, which 100097 added), and run each with the absolute interpreter (`-m pytest tests/<file> -q -rf --tb=line -p no:cacheprovider`), capturing its summary line and its `FAILED` lines — four red lines, since `06b545f` has neither the updater, the new CHECK, the discriminator, the close functions, the `--apply` pass nor the pid guard (rehearsed at drafting: `17 failed, 105 passed`, `25 failed`, `4 failed, 3 passed`, `5 failed, 5 passed` — the run supersedes it). HALT if any file runs green there. A red the control did not show is the plan's, even on a test 100097 left unchanged: such a test fails through a helper 100097 changed — the stop-path tests through `_make_lifecycle_db`, which now writes `daemon_pid`.
> **Item 4 — the close observed, run here**: in the worktree, `-m pytest -v` over P5's four node ids → four PASSED lines.
> **Item 5 — the green line, run here**: the FULL suite in the worktree (`-m pytest tests/ -q`), its summary line.
> **Item 6 — the record** as change 1, under the four headings declared below and `## The halt, recorded`. `## Pins re-derived (P1, P2, P3, P4, P7, P10)` opens with THIS plan's P3 value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `no trigger and no view`; paste through those words, then stop — then Items 1 and 2's lines; `## Failing-first (four red, then green)` carries Item 3's three control lines and four red lines and Item 5's green line, and names the transcript's red runs (P4) as mid-implementation; `## The close observed (c-t1 to c-t4)` Item 4's four lines; `## Mutation run` the run file's last line, `git show 8f836ac:knowledge/mutants/abandoned-runner-close.run.txt | tail -1`; `## The halt, recorded` the four commits (P1) where the halted plan asked for two, the two fix commits' own messages, and the transcript's absent `check_deposit` call (Item 2).
> **Item 7 — the stray record removed** as change 2: `git rm -q knowledge/development/dev-log-abandoned-runner-close-2026-09-13.md`.
> **Item 8 — the commit**, gated on the FULL suite AND the pre-check bare, path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/development/dev-log-abandoned-runner-close-2026-09-12.md && git commit -F <msg-file> -- knowledge/development/dev-log-abandoned-runner-close-2026-09-12.md knowledge/development/dev-log-abandoned-runner-close-2026-09-13.md` — the removal staged by Item 7; the message tagged with the plan id and `thread 306`. ⛔ The commit is made only through this chain: a refused pre-check is a record to fix, never a commit to make. Last act: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"`.
> **Headings:** `## Pins re-derived (P1, P2, P3, P4, P7, P10)`; `## Failing-first (four red, then green)`; `## The close observed (c-t1 to c-t4)`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P2, P3, P4, P7, P10)` ← P3
>
> **Deposits:**
> - `knowledge/development/dev-log-abandoned-runner-close-2026-09-12.md`
>
> **Post-conditions:** the four headings present as full lines, P3's cell whole; `ls knowledge/development | grep -F abandoned-runner-close` prints only the `-2026-09-12` dev-log; one DEV commit, `git show --format= --name-status HEAD` printing the dev-log added and the stray record deleted and nothing else; the full suite green; `git status --porcelain` empty after the commit; `git worktree list` naming no path under `$T` — the archive never registered; `$T` removed.

## STEP 2 — QA (full suite; the migration and the reconcile on a backup-API copy of the live DB; the receipt)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter ABSOLUTE; read-only beyond the two evidence files; `T=$(mktemp -d /tmp/abandoned-close-qa.XXXXXX)`.
>
> **Item 1 — the full suite REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt 2>&1`; the summary line quoted in the receipt as `N passed` with the skips named as `two skips (test_gate_watcher live-DB; test_fallback_live_wal_window version gate)` — never the word the Rule 20 block scans for.
> **Item 2 — the migration and the reconcile on a COPY, never the live file:** the backup API from the venv interpreter — `/Users/marklehn/Developer/bellows/.venv/bin/python` opening `file:/Users/marklehn/Developer/bellows/lifecycle.db?mode=ro` (`uri=True`) and calling its `backup()` into `$T/lc.db`, no bellows module imported (a plain `cp` of a live WAL database can miss pages the daemon has not checkpointed, and the system `sqlite3` CLI, SQLite 3.43.2, refuses a read-only open of a WAL database no writer holds — the daemon opens and closes its connections, so none may hold it: LESSONS 2026-09-11, MACHINE_SETUP §2); then, on the COPY only, print `SELECT id, lifecycle_state FROM plans WHERE lifecycle_state IN ('in_progress','awaiting_verdict')` into the receipt and run `sqlite3 "$T/lc.db" "UPDATE plans SET lifecycle_state = 'closed' WHERE lifecycle_state IN ('in_progress','awaiting_verdict')"` — this plan is in flight while its QA runs, another may be too, and `--apply` refuses while any plan is (`_check_no_inflight`; #100073's QA met the same refusal); record `SELECT count(*), max(id) FROM steps`, the four referencing tables' counts and the five `foreign_key_check` results (the five tables of the halted plan's item 1) from the copy; run `/Users/marklehn/Developer/bellows/.venv/bin/python tools/reconcile_steps.py --db "$T/lc.db"` (LIST) and paste it; run the same with `--apply` and paste its rows; then, on the copy: the `steps` DDL carries `'abandoned'` and `daemon_pid` (`SELECT sql FROM sqlite_master WHERE name = 'steps'`), the counts equal the ones recorded, the five `foreign_key_check`s print the rows recorded before (none on the mini, 2026-09-13 — the migration's own rule, the halted plan's item 1), steps 90, 141 and 142 read `abandoned`, and one `lc.db.pre-steps-abandoned-*.bak` sits beside the copy; run LIST once more and paste it. The live DB is opened once, read-only, by that backup, and the receipt quotes that one command.
> **Item 3 — production writes, stated exactly:** none outside the two evidence files; `$T` removed; no lane file, no live lifecycle row, no daemon act, no branch in the canonical checkout.
> **Item 4 — receipt** `knowledge/qa/evidence/abandoned-runner-close-qa-receipt-2026-09-12.md`: `numstat` over the DEV commits (`06b545f..<dev>`, `<dev>` this plan's STEP 1 commit — eleven files: 100097's nine source, test and manifest files and its run file, and this plan's dev-log; the stray record added at `8f836ac` and removed at `<dev>` nets to no line); a `## Verification` table with one row per Item 1–3, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`/Users/marklehn/Developer/eluvian-governance/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: abandoned-runner-close-2026-09-12`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["abandoned-runner-close-suite-2026-09-12.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the closing `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 5 — commit**, path-scoped and gated: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/abandoned-runner-close-qa-receipt-2026-09-12.md knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt && git commit -F <msg-file> -- knowledge/qa/evidence/abandoned-runner-close-qa-receipt-2026-09-12.md knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt`. ⛔ A QA step that must change production code STOPS and requests a verdict (thread 262).
>
> **Deposits:**
> - `knowledge/qa/evidence/abandoned-runner-close-qa-receipt-2026-09-12.md`
> - `knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/abandoned-runner-close-qa-receipt-2026-09-12.md`
> - `knowledge/qa/evidence/abandoned-runner-close-suite-2026-09-12.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 2 skipped` with N at least the DEV's count and no `failed`; Item 2's LIST, apply and LIST-again outputs pasted, with the counts before and after; the receipt's `## Verification` table has three rows and closes with the block's own PASSED line; one QA commit carrying the two evidence files; the live DB opened once, read-only, by Item 2's backup — `grep -c -F 'lifecycle.db?mode=ro' <receipt>` prints 1: that quoted command, and nothing else.
