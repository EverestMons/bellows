# bellows — diagnostic: RE-PRICE ANVIL AS A GOVERNANCE QUERY SURFACE — over the last ten bellows DEV commits and one positive control, does the structural graph name the dependents a DEV left untouched, the tests a touched function has, and the code the shop already treats as its exemplars — before any build (thread 255)

**Date:** 2026-09-09 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** none (read-only diagnostic; writes one research doc, one TSV, two instrument scripts under anvil and a dev-log) | **Execution:** Step 1 (DIAGNOSTIC) | **pause_for_verdict:** after_step_1 | **Discharges:** none

**auto_close:** false

**Post-close:** no restart, no doctrine. The research doc is the input to the CEO's ruling on thread 255 (build the query surface and chain it into thread 253's pre-check, or park anvil to a pointer); this plan recommends nothing.

**Depends on:** the CEO's ruling of 2026-09-09 (session d04ebd33): re-price before building — the same ruling that produced `Done/diagnostic-100056.md` for thread 81, which is this plan's clone origin BY KIND and BY LAYOUT (one DIAGNOSTIC step, numbered questions, one research doc plus one TSV as Planner-commit deposits, no recommendation). The Planner's walk-0 measurement (`build.py` / `analyze.py` in the session scratchpad, re-deposited by Item 2 as anvil scripts) is the instrument this plan re-runs in the worktree; its numbers are pins, not results.

**Tier computed (§1):** **T1** — T-1 fires (a read across three repos: anvil's source and knowledge, bellows' git history and QA records, governance's glossary); T-6 does not (no doctrine, gate or instrument behaviour changes — two new read-only scripts under anvil, imported by nothing); T-2 does not (a scratch SQLite under a temp dir is not real data).

## CEO Context

Anvil (`~/Developer/anvil`, "Code Forge") was built in March as the shop's structural memory: a per-function dependency graph, function-level test bindings, MinHash clones, co-change pairs and a five-dimension health score, purely static, Python only. Its PRODUCT (cycle reports; constraints `coverage_required` / `verify_dependents`) has never been consumed by a bellows plan (zero Done plans reference either name), and its June runs against bellows were not validated (cycle 1 leaked invoice-pulse chunks; cycle 2 reported zero coverage gaps for a repo the walk-0 build scores as 405 of 591 production functions unbound). The CEO's direction on 2026-09-09 (thread 255) widens the question: anvil is meant to ABSORB each project's code and produce a code glossary (what works here, applied elsewhere) and a refactor engine. The one governance hook already in motion is thread 253 (the DEV-side pre-check): if the graph can name, at the DEV's desk, the cross-file dependents a diff left untouched, that is the integration; if it cannot, anvil parks. This plan measures, on bellows, whether the tables answer three questions well enough to be worth de-hardcoding and wiring. The lessons corpus mentions "consumer" 70 times and "sibling" 67; the known live instance of the class is commit `751ab87` (P4).

## Numbers discipline — measured 2026-09-09 by the Planner (bellows `1365acc`, anvil `caf7efc`, governance `b5f94ab7`, scratch venv `datasketch==2.0.0`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the build, on the mini | `scan_project` 2.2 s: files_total 5014, git_commits_ingested 972 (4-week window); `extract_project` 38.9 s: files_processed 162, chunks 3015 (function 744, method 56, class 221, test_case 1994, module 5014), dependencies 6890 (call cross_file 4076, call within_file 2495, import cross_file 319), similarities 153; `classify_project` 1021; `score_project` 0.5 s, avg composite 0.2743; `tests` bindings 3188, with target 1207; coverage_score over production function/method: 0.2 → 119, 0.5 → 67, 1.0 → 405; roles: None 7008, utility 720, cli_script 108, plan_dispatcher 63, gate_checker 39, notifier 28; anvil's own suite on the mini: `262 passed in 1.33s` | Item 1: `scripts/reprice_bellows_build.py` (Item 2) with the config patched to the checkout and a temp dir; the four stage functions, never `run_cycle` (its Stage 4 writes into the target repo) |
| P2 | ⛔ the resolver's rule, verbatim | `candidates = by_name.get(callee_name, [])` then `break  # Take first match` (`src/extractor.py`, the "Resolve call dependencies" block): a call edge is the FIRST project-wide chunk of that bare name, whatever module the caller imported; symptom in the build: cross-file inbound `dashboard.py::run` 510, `tools/cycle_log_signal_census.py::a` 222, `knowledge/qa/evidence/pt-dc-census-2026-09-02/census.py::read` 82, beside `gates.py::check` 126 and `lifecycle.py::init_lifecycle_db` 119 | Item 5: quote the block; precision sample |
| P3 | ⛔ the ten-commit table (sha files prod_defs chunks deps untouched later_touched unbound) | `53e7146 2 9 9 0 0 0 9` / `cfb968e 4 2 2 3 1 0 0` / `f5c82a4 8 26 26 128 111 0 10` / `99d24c1 1 0 0 0 0 0 0` / `df6acd3 2 0 0 0 0 0 0` / `410bcd0 10 6 6 29 2 2 3` / `54e8b85 4 1 1 78 28 0 0` / `91354d2 9 15 13 193 160 1 2` / `492f50d 3 8 8 100 86 1 1` / `9ffb96c 4 4 4 48 42 4 2`; later-touched hits: `bellows.py::run_plan` ×4 (a hub every plan touches), `tools/battery_census.py::get_coverage` by `53e7146` (planned by 100056), `tests/test_notifier_coverage.py::*` by `f5c82a4` (new tests, not fixes) | Item 3: `scripts/reprice_bellows_analyze.py`; sample rule: newest ten `main` commits whose subject matches `^(feat\|fix)\(` and carries `[1000`, excluding `dev-log`, `qa(`, `wrap` |
| P4 | ⛔ the positive control | `751ab87 fix(tests+dashboard): the consumers of threads 210/104 that the direct-edit lane missed — STANDARD_GATES in the mechanization tests carries the three PASS-row names (7 → 10), the dashboard passes HEAD through and its test reads the labelled header` (3 files: `dashboard.py`, `tests/test_dashboard.py`, `tests/test_gate_transaction_mechanization.py`); its causes `601f5b9` (lifecycle PASS rows, thread 210) and `34eeab2` (status header, thread 104) | Item 4: run the analysis on the two cause commits; state whether `STANDARD_GATES` (a module-level constant) is a node in `code_chunks` at all |
| P5 | ⛔ the stable/bound/reused ranking, first eight | `knowledge/qa/evidence/pt-dc-census-2026-09-02/census.py::read` (utility, vol 0.13, inbound 82); `parser.py::parse` (agent_lifecycle, 0.09, 51); `runner.py::run_step` (agent_lifecycle, 0.06, 30); `scripts/cycle_yields.py::parse_lens_line` (cli_script, 0.13, 24); `scripts/cycle_yields.py::extract_dc_blocks` (18); `scripts/cycle_yields.py::collect` (13); `runner.py::_check_session_limit` (10); `scripts/fold_check.py::is_signal` (8); production clone pairs 19 | Item 7: the query (production function/method/class, volatility ≤ 0.2, coverage_score < 1.0, ordered by cross-file inbound) before and after excluding non-source dirs |
| P6 | the records | anvil `knowledge/qa/findings-quality-assessment-2026-03-30.md`: 1212 findings / ~307 signal / 25% overall, coverage gaps 81%, clones 5%; `knowledge/research/cycle-1-findings-2026-06-08.md` (bellows cycle 1: the leaked `engines/validator.py::gate_9_accessorials` row) and `cycle-2-findings-2026-06-08.md` ("Coverage Gaps (0 findings)", `verify_dependents` naming `bellows.py::run_plan` 117 inbound + 306 outbound); `knowledge/FORWARD.md` items 5 (project_id scoping lint), 6 (exclude non-source dirs), 7 (bellows roles); `grep -rn 'verify_dependents\|coverage_required' <bellows>/knowledge/decisions/Done/*.md` → 0 | quoted, not re-measured |
| P7 | in-flight; class; interpreter | none; daemon pid 26364; the plan writes no bellows tool, but the repo-aware assigner reads the two `anvil/scripts/` writes as **shop-infra** (`_assign_class` on the manifest's five writes, walk 0) — HOLDS at admission, the CEO releases; anvil has NO venv on the mini and bellows' venv lacks `datasketch` — the step builds its own under `mktemp -d` (Item 0), which is the class of gap thread 84 closed for bellows | `python3 status.py`; `dep._parse_plan` / `_assign_class` per bellows `CLAUDE.md` |
| P8 | the writers, by name | `src/lab.py::run_lab` (writes `<target>/knowledge/anvil/audit-findings-<date>.md` via `write_intent_audit`, and the cycle report under anvil), `src/cycle.py::run_cycle` (calls it), `src/scorer.py::ingest_test_results` (runs pytest), `src/scanner.py::scan_project` (copies the DB to `ANVIL_ROOT/backups`, patched to the temp dir); config hardcodes `/Users/marklehn/Developer/GitHub/anvil` and `.../GitHub/bellows` (`src/config.py:6,10,19`) | forbidden by name in Item 0 |

## MUST-PRESERVE

- ⛔ **Read-only over bellows, anvil and governance.** No lifecycle row, no lane file, no run of the daemon, `run_plan`, a claim, or anvil's `run_cycle` / `run_lab` / `ingest_test_results`; the scratch DB and venv live under `mktemp -d`; the only files written are the five deposits.
- ⛔ **No recommendation.** The doc answers Q1–Q7 and ends with "What this does not establish"; the ruling on thread 255 is the CEO's.
- ⛔ **Every rate carries its denominator and its sample rule**, and every graph-derived claim is checked against git (a later commit, an import line), never against the graph alone — P2 says the graph's edges are name-matched.

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-anvil-reprice-2026-09-09.md
**Walks:** walk 0 pinned (P1–P8 measured on bellows `1365acc`, anvil `caf7efc`; clone-diff against `Done/diagnostic-100056.md` run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit.

**Closing:** WARM close after walk 5 — thread 255's first act, a judged stop. The first close (after walk 3) was reopened at the deposit hold: the walk-0 instrument committed to anvil failed the post-condition as written (f10); walk 4's yield-rising escalate was continued under the CEO's under-seven-walks ruling (register WARN). Five walks: instruction 6 → 2 → 0 → 1 → 0. T1, no panel. Deposit via `ready-`; HOLDS on class shop-infra (two writes under `anvil/scripts/`), the CEO releases. Close acts: the Planner commits the two governance research deposits at the wrap (the anvil scripts are already committed, `183e85c`, and the DEV extends them in place); the CEO's ruling on thread 255 reads the doc.
- Weak spots:          w1 3 folded — instruction 3 / record 0
- Destruction:         w1 1 folded — instruction 1 / record 0
- Vulnerabilities:     w1 2 folded — instruction 1 / record 1
- Integration-record:  w1 1 folded — instruction 1 / record 0
- ACID:                w1 dry
- Weak spots:          w2 2 folded — instruction 2 / record 0
- Destruction:         w2 dry
- Vulnerabilities:     w2 dry
- Integration-record:  w2 dry
- ACID:                w2 dry
- Weak spots:          w3 dry
- Destruction:         w3 dry
- Vulnerabilities:     w3 dry
- Integration-record:  w3 dry
- ACID:                w3 dry
- Weak spots:          w4 1 folded — instruction 1 / record 0
- Destruction:         w4 dry
- Vulnerabilities:     w4 dry
- Integration-record:  w4 dry
- ACID:                w4 dry
- Weak spots:          w5 dry
- Destruction:         w5 dry
- Vulnerabilities:     w5 dry
- Integration-record:  w5 dry
- ACID:                w5 dry
**Walk 5 — DRY, all five lenses, one commit per lens. Instruction 0 on a full pass: the WARM close meets the bar (§2) — a judged stop (10 findings over four warm walks: instruction 9 / record 1 plus f0; yield 7 → 2 → 0 → 1 → 0). T1: no panel owed.**
**Walk 4 — one fold at lens 1 (instruction 1 / record 0), four lenses dry; reopened after the hold. Instruction non-zero: walk 5 owed.**
**Walk 3 — DRY, all five lenses, one commit per lens. Instruction 0 on a full pass: the first WARM close (REOPENED at the deposit hold — f10, walk 4) — a judged stop (9 findings over two warm walks: instruction 8 / record 1 plus f0; yield 7 → 2 → 0). T1: no panel owed.**
**Walk 2 — two folds at lens 1 (instruction 2 / record 0), four lenses dry; one commit per lens. Instruction non-zero: the bar is not met — walk 3 owed.**
**Walk 1 — seven folds across four lenses (instruction 6 / record 1), ACID dry; one commit per lens.**

## Cycle Manifest
tier: T1
target: knowledge/development/dev-log-anvil-reprice-2026-09-09.md
class: shop-infra
reads: anvil/src/*.py, anvil/knowledge/qa/findings-quality-assessment-2026-03-30.md, anvil/knowledge/research/cycle-1-findings-2026-06-08.md, anvil/knowledge/research/cycle-2-findings-2026-06-08.md, anvil/knowledge/FORWARD.md, knowledge/decisions/Done/diagnostic-100056.md, knowledge/decisions/Done/*.md, knowledge/qa/*.md, verdicts/resolved/override-*.md, CLAUDE.md, GLOSSARY.md
writes: knowledge/development/dev-log-anvil-reprice-2026-09-09.md, anvil/scripts/reprice_bellows_build.py, anvil/scripts/reprice_bellows_analyze.py, governance/knowledge/research/anvil-reprice-2026-09-09.md, governance/knowledge/research/anvil-reprice-2026-09-09.tsv
open_forks: 1. the build (de-hardcode `src/config.py`, a read-only query CLI `dependents` / `tests-for`, chained into thread 253's pre-check as a WARN line) if Q1–Q2 show a yield the QA records do not already carry; 2. an import-aware resolver (P2) as anvil's own T1 if Q3's precision is the limiting number; 3. park anvil to a pointer, forge-style, if Q1–Q5 show nothing the shop's existing records lack
walks: 5
yields: 6, 2, 0, 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:17
coherence: 5/5 body walks named in the register (11 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.diagnostic-bellows-anvil-reprice.md.foldcheck.json

---

## STEP 1 — DIAGNOSTIC: seven questions on bellows, with the graph checked against git at every step

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **Read-only:** never run the daemon, `run_plan`, a claim, anvil's `run_cycle`, `run_lab`, `write_intent_audit`, `write_cycle_report` or `ingest_test_results` (P8); never import `lifecycle`; the anvil checkout is `/Users/marklehn/Developer/anvil` (read it, write only the two scripts Item 2 names); the bellows checkout under analysis is the CANONICAL checkout `/Users/marklehn/Developer/bellows` (its `main` history via `git -C /Users/marklehn/Developer/bellows log main`, files via `git show` only) — never this worktree, whose HEAD is the plan branch and whose tree carries the plan's own files.
>
> **Scope:**
> - `knowledge/development/dev-log-anvil-reprice-2026-09-09.md`
>
> **Item 0 — the interpreter.** `T=$(mktemp -d /tmp/anvil-reprice.XXXXXX) && python3 -m venv "$T/venv" && "$T/venv/bin/pip" -q install datasketch==2.0.0 pytest` — record `$T` in the dev-log; every anvil call below uses `"$T/venv/bin/python"`; the scratch DB is `$T/anvil-scratch.db`. Then `cd /Users/marklehn/Developer/anvil && "$T/venv/bin/python" -m pytest -q -p no:cacheprovider` and paste the summary line beside P1's `262 passed`.
> **Item 1 — re-derive P1 and state both figures on any mismatch.** Run the build (Item 2's script) over `/Users/marklehn/Developer/bellows` with `src.config.ANVIL_ROOT`, `ANVIL_DB_PATH` and `SCAN_TARGETS["bellows"]["path"]` patched in-process to `$T` and the checkout (also `src.scanner.ANVIL_ROOT` / `ANVIL_DB_PATH`, imported by name); enter at `scan_project`, `extract_project`, `classify_project`, `score_project` — never `run_cycle` (P8). Paste the stage summaries and every count P1 lists, in P1's order, each beside its pinned value.
> **Item 2 — deposit the instrument under anvil, read-only.** `/Users/marklehn/Developer/anvil/scripts/reprice_bellows_build.py` (the build of Item 1, taking `--checkout`, `--tmp`) and `/Users/marklehn/Developer/anvil/scripts/reprice_bellows_analyze.py` (Items 3–8, taking `--checkout`, `--db`, `--tsv`, `--control <sha>...`): each names in its docstring the writers it must never call (P8) and asserts on start that the DB path is under `/tmp` or `$TMPDIR`; neither imports `run_lab` or `run_cycle`. The Planner commits them at the wrap (a Planner-commit deposit, the 100056 shape).
> **Item 3 — Q1: dependents-untouched yield over the ten commits (P3's sample rule).** For each commit: files changed; production defs touched (post-image AST at that sha intersected with `-U0` hunks); chunks matched by (file, name); cross-file dependents from `chunk_dependencies`; UNTOUCHED = dependents whose file is not in the commit; LATER-TOUCHED = an untouched dependent whose (file, name) a NEWER `main` commit touched. Then the column the graph cannot fill: for every LATER-TOUCHED row, read the newer commit's subject and the plan's own record — `Done/executable-<id>.md` STEP 2, its QA receipt `knowledge/qa/evidence/<slug>-qa-evidence-<date>.md`, `verdicts/resolved/override-<id>-*.md`, and the step transcript `logs/<ts>-step.json` (`raw_output`) when the receipt is silent and classify it `hub` (the dependent is touched by most plans — `run_plan`), `planned` (the newer commit's own plan named it), `new-test`, or `MISS` (the newer commit's subject or record says the earlier change left this consumer behind). Q1's number is MISS over ten, with the denominators of untouched rows and later-touched rows stated.
> **Item 4 — Q2: the positive control (P4).** Run Item 3's analysis on `601f5b9` and `34eeab2`. State whether the graph lists `tests/test_gate_transaction_mechanization.py` and `dashboard.py` / `tests/test_dashboard.py` among their untouched dependents, and whether `STANDARD_GATES` exists as a chunk (`SELECT ... FROM code_chunks WHERE name='STANDARD_GATES'`). If the constant is not a node, say in one sentence what the graph would have needed (a `defines` binding for module-level names) — that is a fact about the class of miss the shop actually had, not a recommendation.
> **Item 5 — Q3: edge precision under P2.** Quote the resolver block. For the top-12 chunks by cross-file inbound, take every inbound edge (or a fixed 40 per chunk if more, the rule stated) and check with `git grep -n` at `main` whether the CALLER's file imports the TARGET's module (`import <mod>` / `from <mod> import`); precision = edges with an import over edges checked, per chunk and pooled. Separately state the scan inflation: files_total 5014 vs the count of `git ls-files -- '*.py'` (162 at the pin), and which registered paths are not source (P6 item 6).
> **Item 6 — Q4: tests-for against the QA record.** For the production defs the ten commits touched: the count of `tests` bindings with `target_chunk_id` on each, beside the tests the plan's QA receipt actually named (the `knowledge/qa/evidence/<slug>-qa-evidence-<date>.md` node list the `quoted_test_nodes_exist` gate reads; the deposit receipts under `receipts/` carry no test nodes) — agreement = the QA-named test file is among the binding sources. Then explain, from `find_coverage_gaps`'s predicate (`coverage_score >= 0.8 AND composite_score >= 0.7`), how June's bellows cycle 2 reported zero gaps while P1 scores 405 of 591 production functions unbound.
> **Item 7 — Q5: does the stable/bound/reused ranking land on the shop's exemplars?** Build the reference set from bellows `CLAUDE.md`, the central `GLOSSARY.md` entries tagged `[project: bellows]`, and the daemon archetype's role map (`src/archetypes/daemon.py`): every `file.py::name` they mention — matched by file AND name where the source gives both, by name alone otherwise, and the doc states the count of each kind. Rank per P5 twice — as built, and with non-source dirs excluded (`knowledge/`, `logs/`, `receipts/`, `.claude/`, `tools/*census*`) — and report top-20 ∩ reference for both, plus the roles distribution (P1: utility 720 of 1021 classified).
> **Item 8 — Q6/Q7: the refactor half, and cost.** List the 19 production clone pairs (P5) with a one-word class each (`real` / `fixture` / `generated` / `wrapper`); the top-10 complexity hotspots beside the functions LESSONS.md names most (count `run_plan`, `_teardown_worktree`, `gates.check` mentions). Cost: the build's three stage times (P1) and the per-commit analysis time, then the shape of a pre-check call (one build per merge, one query per commit) in seconds.
> **Item 9 — write the research doc** `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/anvil-reprice-2026-09-09.md` answering Q1–Q7 in order, each section opening with the tool's or the script's raw line it rests on, and the TSV `anvil-reprice-2026-09-09.tsv` (one row per commit × untouched dependent, columns `sha, file, name, dependent_file, dependent_name, later_sha, class, evidence` — `evidence` is the newer commit's subject line or the record line the `class` rests on, quoted, so a `hub` or `MISS` call can be checked without the DB); end with "What this does not establish" (at least: one repo, one language, name-matched edges, the analyzer's own (file, name) chunk match — a method shares its name with every same-named method in the file, so Q1's dependents are an upper bound — and the sample rule's exclusions). No recommendation.
> **Item 10 — dev-log** with the headings declared below (the `dev_log_declared_text` gate reads them); `## Pins re-derived (P1, P3)` opens with P3's value cell pasted verbatim, whole, to its last character. **Commit** the dev-log alone (message tagged with the plan id and `thread 255`), gated on the FULL suite (`.venv/bin/python -m pytest -q` — nothing under bellows changed but the rule is the rule), path-scoped to the Scope list; the four other deposits are Planner-commit deposits outside this repo. Last act, after the commit: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"` — the guard is the point (with `$T` unset the same line is `rm -rf`-of-nothing-shaped only by luck); the post-condition reads the guard, not the absence.
> **Headings:** `## Pins re-derived (P1, P3)`; `## Instrument deposited (two scripts, writers forbidden by name)`; `## Q1–Q7 in one table`; `## What the doc does not establish`
> **Verbatim:** `## Pins re-derived (P1, P3)` ← P3
>
> **Deposits:**
> - `knowledge/development/dev-log-anvil-reprice-2026-09-09.md`
> - `/Users/marklehn/Developer/anvil/scripts/reprice_bellows_build.py`
> - `/Users/marklehn/Developer/anvil/scripts/reprice_bellows_analyze.py`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/anvil-reprice-2026-09-09.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/anvil-reprice-2026-09-09.tsv`
>
> **Post-conditions:** the research doc answers seven questions with denominators and sample rules and ends without a recommendation; the TSV has one row per untouched dependent across the ten commits and the two control commits; the two anvil scripts import neither `run_lab` nor `run_cycle` (`grep -cE '^(from|import) .*\b(run_lab|run_cycle)\b'` = 0 each — the docstrings NAME them, as Item 2 requires, so a bare `grep -c` would count the naming) and refuse a DB path outside a temp dir; the dev-log's four headings present with P3's cell whole; one bellows commit carrying the dev-log only; nothing written under any lane, under anvil beyond `scripts/`, or under `$T` that survives the step.
