# Bellows — Diagnostic Phase 1: Timeline of Recent Changes
**Date:** 2026-04-15 | **Type:** Read-only investigation | **Source:** Persistent strand pattern (3 strands in 4 plans on 2026-04-14, plus a 4-phase sanity check stranded on 2026-04-15) | **Single-step:** Yes (diagnostic-prefixed) | **Run mode:** Manual bootstrap in Claude Code (Bellows is paused for this investigation)

## Context

Bellows has been correctly stranding plans because agents report "step complete" before the move-to-Done has happened. Yesterday this was attributed to plan complexity. Today's 4-phase sanity check stranded after only one substantive action (writing a marker file), invalidating that hypothesis. The agent appears to be terminating after the first deposit-style action regardless of plan length.

This diagnostic is **Phase 1 of 4** in a structured Bellows reliability investigation. Phase 1 establishes the timeline: when did Bellows last work reliably, what changed since, and what is the most likely delta. Phase 2 will read the agent dispatch path. Phase 3 will reproduce the failure. Phase 4 will fix.

Phase 1 is read-only. No source code edits, no Bellows config changes, no commits other than the findings deposit.

**Bellows is paused for this investigation.** This plan is run via manual Claude Code bootstrap, not via Bellows dispatch. The agent should NOT attempt to start or interact with the Bellows process.

## How to Run This Plan (manual bootstrap)

In a Claude Code session, paste:

```
Read the plan at /Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-strand-timeline-2026-04-15.md and execute Step 1. Claim the plan first by renaming it to in-progress-, then complete Step 1 and move it to Done.
```

---
---

## STEP 1 — Investigation Agent

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-strand-timeline-2026-04-15.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-strand-timeline-2026-04-15.md")`. Skip specialist file and glossary reads — this is a code-archaeology investigation. Working directory is `/Users/marklehn/Desktop/GitHub/bellows`. **READ-ONLY: do not modify any source file, configuration file, or database. Do not start, stop, or interact with any running Bellows process.** Investigation has six numbered questions (Q1–Q6). Answer each in the deposited findings file under its own section header. **Q1 — Inventory the Bellows source tree.** Run `ls -la` and `ls -la tests/`. Report all Python files, config files, database files, and the tests directory contents. The Planner needs the full file inventory to know what subsequent phases should read. Note any files that look load-bearing for the agent dispatch loop (planner.py, runner.py, parser.py are the suspected candidates — confirm or expand). **Q2 — When did Bellows last ship a strand-free plan?** Bellows tracks runs in `bellows.db:runs`. Query: `python3 -c "import sqlite3; conn = sqlite3.connect('bellows.db'); cur = conn.execute('SELECT timestamp, plan_path, project, step, status FROM runs ORDER BY timestamp DESC LIMIT 50'); rows = cur.fetchall(); print(f'{\"timestamp\":<25} {\"status\":<12} {\"step\":<6} {\"plan\":<60}'); print('-' * 110); [print(f'{r[0]:<25} {r[1]:<12} {str(r[3]):<6} {r[1][:60] if isinstance(r[1], str) else str(r[1])[:60]}') for r in rows]"`. If the column order is different or the table has different columns, run `PRAGMA table_info(runs)` first and adjust. Report the literal stdout. The Planner needs to see which plans completed cleanly vs stranded, with timestamps, to identify the transition point. **Q3 — What does git log say about recent Bellows commits?** Run `git --no-pager log --oneline -30` and report. Run `git --no-pager log --since="7 days ago" --stat` and report. The Planner needs to see what code changes have landed in Bellows in the past week, with file-level diff stats, to identify candidate cause commits. **Q4 — Check for very recent uncommitted changes.** Run `git --no-pager status --short` and `git --no-pager diff HEAD --stat`. Report any uncommitted edits. If there are uncommitted changes to bellows.py or the dispatch path files, those are immediate suspects. **Q5 — Identify the most recent commit BEFORE the strand pattern started.** From Q2 (run history) we know which plans were the last clean ones. Find the git commit that was current at that time. Use `git --no-pager log --oneline --until="2026-04-14 12:00:00"` (adjust the cutoff if Q2 shows the transition was earlier or later) and report the most recent commit at that point. The Planner needs the git SHA of the last-known-good Bellows version so Phase 2 can diff against it. **Q6 — Best-guess timeline reconstruction.** Based on Q1-Q5, write a one-paragraph reconstruction: when was the last clean run, what commits have landed since, and what is the most likely candidate-cause commit (or set of commits). If multiple commits look suspect, list them in priority order with reasoning. Do NOT speculate about root cause yet — Phase 2 reads the actual code. Just rank what to read first. **Step A — Findings deposit.** Write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/bellows-strand-timeline-2026-04-15.md`. Use sections Q1–Q6. Include a standard Output Receipt with Status=Complete, Files Deposited, Files Created or Modified (Code) = `[]` (read-only), Decisions Made = the Q6 reconstruction, Flags for CEO = the recommended commit(s) for Phase 2 to read first. **Step B — Feedback append.** Append a feedback entry to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/agent-prompt-feedback.md` (create the file if it doesn't exist) with the standard prompt feedback format. **Step C — Commit.** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/bellows-strand-timeline-2026-04-15.md knowledge/research/agent-prompt-feedback.md && git commit -m "docs: phase 1 diagnostic — bellows strand timeline"`. **Step D — Move-to-Done as the absolute last operation:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-strand-timeline-2026-04-15.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/Done/diagnostic-bellows-strand-timeline-2026-04-15.md")`. **Steps A → B → C → D in strict order. The move-to-Done must be the last thing done.**
