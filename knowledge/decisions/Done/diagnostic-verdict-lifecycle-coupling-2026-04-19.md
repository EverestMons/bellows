# bellows — Diagnostic: Verdict Lifecycle Coupling
**Date:** 2026-04-19 | **Tier:** Large (architecture investigation) | **Test Scope:** n/a (diagnostic) | **Execution:** Step 1 (SA)
**Priority:** 1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS. The Planner reads the deposited findings file via direct tool call and performs Rule 22 verification. If all checks pass, the Planner moves the plan to Done and a follow-up executable is written based on the findings.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-verdict-lifecycle-coupling-2026-04-19.md. Execute Step 1 ONLY. After completing Step 1, STOP.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-verdict-lifecycle-coupling-2026-04-19.md", "bellows/knowledge/decisions/in-progress-diagnostic-verdict-lifecycle-coupling-2026-04-19.md")`. **Read your specialist file and skip glossary reads — this is a code-tracing and lifecycle-mapping architecture investigation.** You are investigating the verdict lifecycle coupling gap identified as BACKLOG #9: when a plan reaches terminal state (Done or halted), `verdict-request-<slug>-*.md` files matching the plan's slug are NOT cleaned up from `bellows/verdicts/pending/`. Stranded files accumulate (33 observed 2026-04-18, 5 currently visible in `bellows/knowledge/decisions/` as `verdict-pending-*` prefixes) and cannot be distinguished from legitimately-awaiting verdicts. Your job is to map the current lifecycle so the Planner can write an executable that adds the cleanup sweep safely. **Investigate and report only — do NOT write code, do NOT modify any file outside your deposit target.** **Investigation questions, in order:** **(1) Rename paths in `bellows.py`.** Identify the exact function(s) and line range(s) that perform the two terminal-state renames: (a) move-to-Done when a plan completes its final step (likely in a `_handle_plan_completion` or `_consume_verdicts` path), (b) the halted-* rename path when a plan is explicitly halted. Report the function name, line range, and the exact `shutil.move()` (or equivalent) call signature for each. **(2) Plan slug extraction rule.** Determine the canonical rule for extracting a plan's slug from its filename. Given example filenames `executable-create-forge-developer-specialist-2026-04-18.md`, `diagnostic-verdict-lifecycle-coupling-2026-04-19.md`, `parallel-1-executable-foo-2026-04-19.md`, and `in-progress-executable-bar-2026-04-19.md` — what substring does Bellows treat as "the slug"? Is there an existing utility function for this? If so, cite it (function name, line range). If not, note that the sweep will need to implement slug extraction. **(3) Verdict request filename shape.** Read `verdict.py` and identify how `verdict-request-*` filenames are constructed when a verdict is written. Report the exact format string (e.g., `f"verdict-request-{slug}-step-{n}.md"`). Confirm whether the slug portion of the verdict filename matches the slug extraction rule from (2) — if they differ, the sweep's glob pattern must account for the transform. **(4) Current state of `verdicts/pending/`.** Enumerate every file currently in `bellows/verdicts/pending/`. For each, extract: filename, age (mtime), parsed slug, and whether a matching plan file exists in `bellows/knowledge/decisions/` OR `bellows/knowledge/decisions/Done/`. Classify each file into one of: (a) legitimately pending (plan is active, verdict awaits resolution), (b) stranded after Done (plan is in Done/ but verdict was never consumed), (c) stranded after halted (plan has `halted-` prefix but verdict was never consumed), (d) orphaned (no matching plan file found anywhere — plan was manually deleted or never existed). Report counts per category and a representative sample from each (up to 3 files per category, full filename + age + classification reasoning). **(5) Race window analysis.** Trace the ordering of operations during move-to-Done. Specifically: can a new `verdict-request-*` file be written to `verdicts/pending/` by Bellows (via `_consume_verdicts`, scope_check, or any other path) AFTER the sweep runs but BEFORE the plan's rename-to-Done completes? If yes, describe the window and what triggers a late write. This is the concern raised by BACKLOG #1 (scope_check race) — confirm whether the same race affects the sweep. Also answer: is there any scenario where a verdict request arriving AFTER move-to-Done is supposed to be processed, not deleted? If so, what scenario and who processes it? **(6) Resolved/ folder interaction.** The 2026-04-18 verdict schema diagnostic noted a `processed-verdict-*` file in `resolved/` with a matching file still stranded in `pending/`. Read the current `resolved/` enumeration. For each pending-side stranded file, check whether a corresponding `resolved/` file exists. If `resolved/` is append-only and strictly tracks consumption, the sweep should NOT touch resolved/. If resolved/ has its own stranded-file pattern, note it. Report the finding explicitly. **(7) Sweep call-site proposal.** Given findings from (1)-(6), propose WHERE in `bellows.py` the cleanup sweep should be invoked — exact function and line, before-or-after which existing operation. Justify placement against the race window from (5). Propose the sweep function signature (e.g., `_cleanup_verdicts_for_slug(slug: str) -> int` returning count of files removed). Do NOT write the function — just propose the shape. **(8) Edge cases.** Enumerate edge cases the executable will need to handle: (a) multiple pending verdicts for the same slug (one plan may have multiple steps each with their own verdict request), (b) plan moved to Done while a verdict write is in flight, (c) halted plans that already had verdicts resolved (no-op sweep — confirm harmless), (d) plans whose slug is a prefix of another plan's slug (e.g., `foo-2026-04-19` and `foo-bar-2026-04-19` — does the sweep accidentally clean up the wrong files?), (e) any other edge case the investigation surfaces. **Deposit format:** write findings to `bellows/knowledge/research/verdict-lifecycle-coupling-2026-04-19.md` as an 8-section report, one section per question. Each section answers the question with concrete code citations (function name + line range + relevant excerpt — quote selectively, no more than 5 lines per quote), lifecycle observations, and explicit status/classification. End with a "Recommended Executable Shape" subsection summarizing what the follow-up plan needs to implement, where it lives in the code, and what tests will verify correctness. **Write the deposit using the canonical Python file-write pattern:** `with open("bellows/knowledge/research/verdict-lifecycle-coupling-2026-04-19.md", "w") as f: f.write(content)` where `content` is a triple-quoted Python string defined before the open call. Do NOT use heredoc syntax. Do NOT use `python3 -c`. Commit with message: `"docs: diagnostic — verdict lifecycle coupling (BACKLOG #9)"`. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **Deposits:**
> - `bellows/knowledge/research/verdict-lifecycle-coupling-2026-04-19.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
>
> **STOP. Do NOT proceed to write any fix. Do NOT move the plan to Done. The Planner will read your findings and handle housekeeping per Rule 22.**
