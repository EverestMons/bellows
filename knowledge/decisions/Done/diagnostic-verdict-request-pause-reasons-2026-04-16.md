# Bellows — Verdict Request Pause Reasons Audit
**Date:** 2026-04-16 | **Type:** diagnostic (investigation only, no fixes)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation ("ok") before proceeding. For this single-step diagnostic, the agent reports completion and the Planner performs Rule 22 verification of the deposited findings before moving the plan to Done.

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-verdict-request-pause-reasons-2026-04-16.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further or move the plan to Done.
```

---
---

## STEP 1 — INVESTIGATION AGENT (bellows developer)

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-verdict-request-pause-reasons-2026-04-16.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-verdict-request-pause-reasons-2026-04-16.md")`.
>
> Skip specialist file and glossary reads — bellows has no specialist files yet and this is a pure code-tracing task. You are investigating how `post_verdict_request` is invoked across the Bellows dispatcher so the Planner can design a fix that threads a `pause_reason` through to the verdict request template. Do NOT propose fixes, write code, or modify anything — investigate and deposit findings only. **Background:** `verdict.py` currently writes `Gate Failures: - None (QA checkpoint — all gates passed)` as the fallback string whenever its caller passes an empty `failures` list (see `verdict.py:39`). This is misleading because some pause conditions aren't QA checkpoints at all — for example, a diagnostic's final step pausing because `not effective_auto_close` evaluated true. The caller knows the real reason but has no way to pass it through today. **Q1 — Enumerate every call site of `post_verdict_request`.** Grep the repo: `grep -rn "post_verdict_request" /Users/marklehn/Desktop/GitHub/bellows --include="*.py"`. For each call site, report: (a) file:line, (b) the enclosing function name, (c) the literal ~10 lines of surrounding code so the Planner can see the call in context, (d) whether the call passes a populated `failures` list or an empty/default one. **Q2 — Identify the pause condition for each call site.** For each call site found in Q1, trace upward in the same function to identify the condition that triggered the pause. Mark mentioned there are five pause conditions; verify whether that count is accurate. For each condition, report: (a) a one-sentence plain-English description of what triggers the pause ("QA checkpoint failed gates", "final diagnostic step with `not effective_auto_close`", etc.), (b) a short token suitable for a `pause_reason` enum value (e.g., `qa_gate_failure`, `diagnostic_no_auto_close`, `scope_flag`, etc.), (c) whether the condition currently produces a meaningful `failures` list or whether it falls through to the misleading default. **Q3 — Recommended threading mechanism.** Based on Q1 and Q2, recommend ONE of the following approaches and justify it — do not punt with "either works": **(A)** Add a `pause_reason` field to the `gate_result` dict that every call site populates before invoking `post_verdict_request`, and update `verdict.py` to use it when rendering the "Gate Failures" section header. **(B)** Add a new required `pause_reason` parameter to `post_verdict_request`'s signature, so every call site must explicitly provide it, and update `verdict.py` to branch the template on it. **(C)** Something else you identify during the audit. Justify your recommendation on: call-site ergonomics (how many lines change at each site), backward compatibility (is there external code calling `post_verdict_request`?), and template clarity (does the fix let `verdict.py` render a distinct section for non-gate pauses, e.g., `## Pause Reason\n\n[reason]\n` instead of the misleading "Gate Failures" header). **Q4 — Template design sketch.** Write a 15-line markdown sketch of what the verdict request file should look like for EACH of the five (or however many) pause conditions found in Q2. Specifically show how the section structure differs between a QA-gate-failure pause (where the current "Gate Failures" listing is appropriate) and a non-gate pause (where a different section makes sense). Do NOT implement — just sketch the target output. **Q5 — Caveats and gotchas.** Flag anything the Planner should know before writing the fix executable: (a) are any of the call sites passing `planner_py_decision` (legacy) in a way that complicates the fix? (b) does any test in `tests/` currently assert on the literal string "None (QA checkpoint — all gates passed)"? (c) are there any in-flight verdict requests in `verdicts/pending/` that would render differently under the fix? **Deposit:** write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/verdict-request-pause-reasons-audit-2026-04-16.md` using the canonical pattern: `with open("/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/verdict-request-pause-reasons-audit-2026-04-16.md", "w") as f: f.write(content)`. Structure the file with sections Q1–Q5 and include an Output Receipt at the bottom with Status=Complete, Files Deposited, Files Created or Modified (Code) = `[]`, Decisions Made = your Q3 recommendation, Flags for CEO = Q5 caveats. **Commit:** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/verdict-request-pause-reasons-audit-2026-04-16.md && git commit -m "docs: audit verdict request pause reasons"`. **Feedback:** standard prompt feedback protocol → `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed. Do NOT move the plan to Done. Report completion and wait for CEO confirmation — the Planner will perform Rule 22 verification of the findings before authorizing housekeeping.**
