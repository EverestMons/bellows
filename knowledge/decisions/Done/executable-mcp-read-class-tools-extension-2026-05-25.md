# Bellows — MCP READ_CLASS_TOOLS Extension (Shape A)

**Date:** 2026-05-25 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** full-suite | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_step_1

## Context

The 2026-05-25 SA diagnostic (`diagnostic-mcp-read-class-tools-exemption-2026-05-25`, in Done/) characterized the current state of MCP tool exemption in `gates.py` and recommended Shape A (literal set extension). Key findings: `READ_CLASS_TOOLS = {"Grep", "Glob", "Read", "mcp__vexp__run_pipeline"}` at `gates.py:35`; filter uses exact string equality via set membership at `gates.py:221-244`. All 7 vexp tools were classified via authoritative MCP schema lookup: 6 read-class (`run_pipeline`, `get_context_capsule`, `get_session_context`, `get_skeleton`, `index_status`, `search_memory`), 1 write-class (`save_observation` — modifies state; denials MUST continue firing the gate).

This plan ships Shape A: add the 5 missing read-class vexp tools to READ_CLASS_TOOLS as exact-name literals. Closes the 2026-05-22 BACKLOG entry on MCP tool denials and pre-empts gate failures from the 4 vexp read-class tools not yet observed in the denial corpus.

**Sources:** SA findings at `bellows/knowledge/research/mcp-read-class-tools-exemption-2026-05-25.md` (sections 1, 3, 4, 6); existing test pattern at `tests/test_gates.py:874` (`test_permission_denials_vexp_run_pipeline_exempt`).

**Pre-existing test failures expected (5 carry-overs, not regressions):** 4 × `test_decisions.py` worktree artifacts, 1 × `test_run_step_timeout`.

## How to Run This Plan

```
Read the plan at bellows/knowledge/decisions/executable-mcp-read-class-tools-extension-2026-05-25.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-mcp-read-class-tools-extension-2026-05-25.md", "bellows/knowledge/decisions/in-progress-executable-mcp-read-class-tools-extension-2026-05-25.md")`. You are the Bellows Developer. Read your specialist file and domain glossary first. **Task:** extend `READ_CLASS_TOOLS` in `bellows/gates.py` with the 5 missing read-class vexp tools identified by the 2026-05-25 SA diagnostic, and add 6 regression tests (5 positive + 1 critical negative). **Read first** the SA findings at `bellows/knowledge/research/mcp-read-class-tools-exemption-2026-05-25.md` Section 3 (Classification table) and Section 6 (Recommendation) for the exact tool list and rationale. Also read the existing test at `tests/test_gates.py:874` (`test_permission_denials_vexp_run_pipeline_exempt`) — this is the pattern template for the new tests. **Edit 1 — `READ_CLASS_TOOLS` set in `gates.py`:** locate the current definition (currently at `gates.py:35`: `READ_CLASS_TOOLS = {"Grep", "Glob", "Read", "mcp__vexp__run_pipeline"}`) and extend it to include 5 more entries: `mcp__vexp__get_context_capsule`, `mcp__vexp__get_session_context`, `mcp__vexp__get_skeleton`, `mcp__vexp__index_status`, `mcp__vexp__search_memory`. Format as a multi-line set literal for readability. **Do NOT add `mcp__vexp__save_observation`** — it is write-class and its denials must continue firing the gate. **Edit 2 — Tests in `tests/test_gates.py`:** add 6 new test functions following the pattern of the existing `test_permission_denials_vexp_run_pipeline_exempt`. Five positive tests (one per newly-added tool name) asserting that a denial of that tool name is filtered (gate does NOT fire). One critical negative test, `test_permission_denials_vexp_save_observation_is_not_exempted`, asserting that a denial of `mcp__vexp__save_observation` DOES fire the gate (this is the safety guarantee that prevents over-exemption — write-class denials carry real signal and must not be silenced). Use the same fixture shape as the existing test: construct a `parsed` dict with a `permission_denials` list, call `_gate_no_permission_denials(parsed, failures)`, assert the appropriate state of `failures`. **Verification before committing:** run `python3 -m pytest tests/test_gates.py -v -k 'vexp or save_observation or read_class'` to confirm the 6 new tests plus any existing READ_CLASS-related tests pass; then run `python3 -m pytest` for the full suite — expect 5 pre-existing failures (4 × test_decisions.py worktree artifact, 1 × test_run_step_timeout) and zero new regressions. **Deposit a dev log** at `bellows/knowledge/development/mcp-read-class-tools-extension-2026-05-25.md` with sections: (a) verbatim diff of the `READ_CLASS_TOOLS` change in `gates.py`, (b) the 6 new test function names with one-line descriptions each, (c) full-suite test counts (collected/passed/failed/warnings), (d) confirmation that `save_observation` denial test fails the filter (i.e., still appears in `failures`), (e) any deviations from the SA Section 3 tool list and why, (f) Output Receipt with Status: Complete. **Commit** with message `fix(gates): extend READ_CLASS_TOOLS with 5 vexp read-class tools — closes BACKLOG mcp_tool_denials` and a brief body listing the three files changed. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/mcp-read-class-tools-extension-2026-05-25.md`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — Bellows QA

---

> Before starting, read `bellows/knowledge/development/mcp-read-class-tools-extension-2026-05-25.md` and check the Output Receipt status field. If status is not Complete, stop and report the blocker before proceeding. You are the Bellows QA agent. Read your specialist file and domain glossary first. **FIRST — Deliverable Verification.** Build a verification table with these checks, each row including line-number evidence and a verbatim quote excerpt: (1) `READ_CLASS_TOOLS` in `gates.py` contains all 9 expected entries (`Grep`, `Glob`, `Read`, `mcp__vexp__run_pipeline`, `mcp__vexp__get_context_capsule`, `mcp__vexp__get_session_context`, `mcp__vexp__get_skeleton`, `mcp__vexp__index_status`, `mcp__vexp__search_memory`) and explicitly does NOT contain `mcp__vexp__save_observation`; (2) `_gate_no_permission_denials` filter mechanism unchanged — still uses `tool_name in READ_CLASS_TOOLS` exact set membership (no new pattern matching added); (3) 5 positive test functions present in `tests/test_gates.py` for the 5 newly-added tools — grep `def test_permission_denials_vexp_` and confirm count went from 1 to 6; (4) 1 negative test `test_permission_denials_vexp_save_observation_is_not_exempted` present and asserts the gate DOES fire on save_observation denial; (5) Dev log has all 6 sections (a-f) + Output Receipt Complete. Mark each row ✅ or ❌ with evidence. If any ❌, stop and report. **Then — full pytest suite.** Run `python3 -m pytest` and capture output to `evidence/pytest_full.txt`. Expected: 5 pre-existing failures, 0 new regressions, all 6 new tests PASSED. Verify by grepping the pytest output for `test_permission_denials_vexp_` and `test_permission_denials_vexp_save_observation_is_not_exempted` and confirming all show PASSED. **Then — structural compliance check.** Run `git --no-pager show --stat HEAD` and confirm the DEV commit touched exactly 3 files: `gates.py`, `tests/test_gates.py`, `knowledge/development/mcp-read-class-tools-extension-2026-05-25.md`. Capture to `evidence/dev_commit.txt`. Run `git --no-pager diff HEAD~1 gates.py` and confirm the diff is bounded to the `READ_CLASS_TOOLS` set literal — no other functions or constants modified. Capture to `evidence/diff_gates.txt`. **Then — Rule 20 self-check.** Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values: `plan_slug`: `executable-mcp-read-class-tools-extension-2026-05-25`; `qa_report_path`: `<absolute-path-to-knowledge/qa/executable-mcp-read-class-tools-extension-2026-05-25.md>`; `evidence_dir`: `<absolute-path-to-knowledge/qa/evidence/executable-mcp-read-class-tools-extension-2026-05-25/>`; `required_evidence_files`: list every evidence file you wrote during this step. Include the literal stdout output of the block in the QA report under an `**Output:**` heading. If FAILED, halt and report to CEO. **Then — QA report deposit.** Write `knowledge/qa/executable-mcp-read-class-tools-extension-2026-05-25.md` with the verification table, full-suite test summary, structural compliance results, Rule 20 self-check stdout, and Output Receipt with Status: Complete. **DEPOSITS RULE:** declare ONLY the QA report file and the evidence directory in the **Deposits** block below. Do NOT list PROJECT_STATUS.md as a deposit even though you will update it — listing multiple .md files triggers the BACKLOG-documented hash-dependent `md_paths[0]` false-positive. **Final:** Update `bellows/PROJECT_STATUS.md` — prepend a new Completed entry under today's date (2026-05-25) summarizing: `READ_CLASS_TOOLS` extended with 5 read-class vexp tools (`get_context_capsule`, `get_session_context`, `get_skeleton`, `index_status`, `search_memory`); `save_observation` deliberately excluded as write-class; closes 2026-05-22 BACKLOG entry on MCP tool denials; 6 new regression tests (5 positive + 1 negative). Include the **REMINDER: restart Bellows daemon** line. Use `Filesystem:edit_file` with verbatim anchor on the existing top entry. **STOP.** Do NOT move this plan to Done/. Standard prompt feedback protocol. **Deposits:**
> - `bellows/knowledge/qa/executable-mcp-read-class-tools-extension-2026-05-25.md`
> - `bellows/knowledge/qa/evidence/executable-mcp-read-class-tools-extension-2026-05-25/`
>
> **STOP. Do NOT move the plan to Done. Wait for CEO confirmation. Per Rule 22, the Planner performs the move-to-Done after verification.**
