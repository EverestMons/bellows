# Bellows — expected-keys warning misleading on single-line headers (bellows.py:419)
**Date:** 2026-05-21 | **Tier:** Diagnostic | **Dispatch Mode:** bellows | **Test Scope:** targeted | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1 | **auto_close:** false

## Context

`bellows/knowledge/BACKLOG.md` open item: `bellows.py:419` emits a WARN like `missing: ['author', 'project', 'total_steps']` even when the safety-critical `pause_for_verdict` field IS parsed successfully. The current expected-keys set is `{"project", "date", "author", "total_steps", "pause_for_verdict"}` — five keys treated equally. The proposal is to either (1) narrow the warning to fire only when safety-critical keys are missing, OR (2) update Planner plan-header templates to always emit the full key set. The choice depends on which keys are actually safety-critical at runtime vs cosmetic metadata.

**Scope:** read-only investigation of `bellows.py`, `gates.py`, `verdict.py`, `parser.py`, `validators.py`, `runner.py`, and `PLANNER_TEMPLATE.md` to characterize each expected-key's runtime role. No fixes, no code changes. Findings deposit goes to `bellows/knowledge/research/`. The Planner reads findings and authors a separate executable based on the resolution shape (1) or (2).

---
---

## STEP 1 — Bellows Systems Analyst

---

> You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` first. **Skip glossary read — this is code-tracing task.** **Task:** characterize each of the 5 keys in the `expected_keys` set at `bellows.py:419` (`project`, `date`, `author`, `total_steps`, `pause_for_verdict`) as safety-critical or cosmetic at runtime. Then audit current Planner plan-header templates to identify which keys are reliably emitted and which are commonly omitted. The diagnostic output drives the choice between resolution shape (1) narrow the warning, or (2) fix the templates upstream. **Investigation questions (answer each in the findings deposit):** (1) **Confirm the warning code.** Read `bellows.py` and quote the exact code at and around line 419. Report the `expected_keys` set membership, the WARN log format string, and the trigger condition. Note any related code (defensive defaults, downstream consumers of the parsed header). (2) **Per-key runtime audit.** For each of the 5 keys in `expected_keys`, trace where the key's value is consumed by `run_plan()` and other call sites. For each key, classify: (a) **Safety-critical** — value affects control flow (routing, pause decisions, validation outcomes, dispatch rejections). Quote the consumption site. (b) **Cosmetic** — value is logged, included in metadata, or otherwise non-load-bearing. Quote the consumption site if any; if no consumption site exists, note this explicitly. For `pause_for_verdict` specifically, note that the defensive default at `_apply_defensive_header_defaults` already handles its absence — what does "missing" actually mean at runtime if the defensive default fires? (3) **Dispatch-mode and other keys NOT in expected_keys.** The plan header parser produces a dict that may include `dispatch_mode`, `tier`, `test_scope`, `auto_close`, `Model`/`model`, and other keys. Which of THESE keys are safety-critical at runtime? Are any currently NOT in `expected_keys` despite being safety-critical? Quote consumption sites for `dispatch_mode` (from `validators.validate_at_claim`) and `auto_close` (from the orchestration loop) specifically. (4) **Planner template audit.** Read `PLANNER_TEMPLATE.md` sections that describe plan-header format. Identify the documented header format. Then read the most recent 5-10 plans in `bellows/knowledge/decisions/Done/` (sort by mtime, take the most recent) and count how many emit each of the 5 expected-keys fields. Report: per-key emission rate as a fraction (e.g., "project: 8/10, date: 2/10, author: 0/10"). This characterizes whether the warning fires because of legitimate template gaps or because of expected-keys overreach. (5) **Single-line header impact.** The BACKLOG entry calls out "single-line headers" specifically. The plan header format in PLANNER_TEMPLATE.md uses a pipe-separated single-line format like `**Date:** ... | **Tier:** ... | **Dispatch Mode:** ...`. Read `parser.py` (specifically the header-parse function imported by `gates._parse_plan_header`) and identify which key names the parser RECOGNIZES from this format. Are there keys in the expected_keys set that the parser cannot extract from single-line format even when they're present in the plan? Quote the parser regex or extraction logic. (6) **Recommendation — Gap Assessment.** Produce a table with columns `| Gap | Current State | Proposed State | Change Required | Recommended | Rationale |`. Rows: (a) `project` key handling, (b) `date` key handling, (c) `author` key handling, (d) `total_steps` key handling, (e) `pause_for_verdict` key handling, (f) any safety-critical key NOT currently in expected_keys (e.g., `dispatch_mode`, `auto_close`), (g) the overall resolution shape — (1) narrow the warning or (2) fix the templates, with rationale. For each row, specify yes/no/defer with one-sentence rationale. **Read depth:** `bellows.py`, `parser.py`, `validators.py`, `PLANNER_TEMPLATE.md` (header sections only), and 5-10 recent Done plans full read required. `gates.py`, `verdict.py`, `runner.py` are on-demand only if data-flow trace surfaces dependencies. **Output format:** structured markdown deposit at `bellows/knowledge/research/bellows-expected-keys-warning-2026-05-21.md` with sections matching the question numbers above (1-6). Quote code verbatim with line numbers. Quote plan-header lines verbatim from sampled Done plans (e.g., `**Date:** 2026-05-21 | **Tier:** Small | ...`). Cite anchor lines for any future executable's edit_block to use. **Do NOT propose code edits in this deposit.** The Planner authors the executable based on findings. **Standard prompt feedback protocol** → append entry to `bellows/knowledge/research/agent-prompt-feedback.md`. **Commit:** stage findings file and feedback log; commit message `docs: SA findings — bellows expected-keys warning diagnostic`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/knowledge/research/bellows-expected-keys-warning-2026-05-21.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
