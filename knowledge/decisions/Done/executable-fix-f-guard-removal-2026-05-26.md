# Fix F isinstance Guard Removal + Test Fixture Conformance
**Project:** bellows
**Date:** 2026-05-26
**Author:** Planner
**Dispatch Mode:** bellows
**Total Steps:** 2
**pause_for_verdict:** after_step_1
**auto_close:** false
**qa_steps:** 2

---

## CEO Context

The Fix F defensive guards at `bellows.py:495` and `bellows.py:587` are contract-pollution defense against a non-conformant test fixture. Production code's `gate_result["failures"]` contract is `list[dict]` with shape `{"gate": str, "evidence": str}` (verified by SA Q1(b) in the 2026-05-26 gate-result preservation diagnostic, and by the 21-site enumeration in the 2026-05-21 isinstance-asymmetry diagnostic). The pre-existing test `test_run_plan_inprogress_entry_renames_to_verdict_pending` in `tests/test_bellows.py` uses `"failures": ["scope_check"]` (string list, not dict list) as a simplified fixture. Without the `isinstance(f, dict)` guard, Fix F's `f["gate"]` crashes on the string.

The 2026-05-26 verdict-ledger-gate-result-preservation DEV agent added the guard defensively when shipping E.4 + Fix F; QA approved it. Production behavior is unchanged for the conformant dict format. The BACKLOG entry filed 2026-05-26 captured this as a contract-uniformity cleanup item.

**Resolution:** update the test fixture to use the production dict shape `[{"gate": "scope_check", "evidence": "<evidence>"}]`, then remove the `isinstance(f, dict)` guard from both Fix F sites. After this lands, all `gate_result["failures"]` entries in test fixtures match the production contract, and the defensive guards are no longer needed.

This is a contract-uniformity cleanup, not a function-signature change. The production contract is unchanged. The fixture is brought into compliance with the existing contract, and the dead defensive code is removed. Rule 21's contract-change carve-out does NOT apply (no function signature, parameter type, or semantic contract changes). However, `bellows.py` pause-reason discriminator blocks are exercised by multiple integration tests, so test scope is `full-suite` out of caution.

**Test scope justification:** `full-suite`. The guards at `bellows.py:495` and `bellows.py:587` are inside the pause-reason discriminator blocks. While the BACKLOG entry only names one test as touching the non-conformant fixture, the surrounding code (Fix F's terminal log expansion + the pause-reason classification logic) is exercised by integration tests across `test_bellows.py`, `test_consume_verdicts.py`, `test_verdict.py`, and possibly `test_misplaced_verdicts.py`. Pre-flight `grep -rn "gate_result\[.failures.\]" tests/` will confirm the test exposure surface.

Pre-edit baseline: 407 collected / 407 passed (from main checkout; worktree shows 4 known carry-over failures + 1 long-standing `test_run_step_timeout`). Post-edit expectation: same pass count, zero new failures, zero regressions.

---
---

## STEP 1 — DEV

---

> You are the Bellows Developer. Read your specialist file at `agents/BELLOWS_DEVELOPER.md` and `governance/GUARDRAILS.md` first. **Skip glossary read — domain terminology is fully covered in the specialist file.** Note: worktree root IS the bellows project root — strip the `bellows/` prefix from any path references in this prompt when running commands. **Reads (mandatory, in order):** (1) `agents/BELLOWS_DEVELOPER.md` — your specialist file; (2) `knowledge/development/verdict-ledger-gate-result-preservation-2026-05-26.md` — the 2026-05-26 dev log "Anchor-Drift Surprises" section that documents why the guards were added defensively; (3) `bellows.py` lines 480-540 — the first Fix F site (intermediate-step pause block at line ~495); (4) `bellows.py` lines 570-630 — the second Fix F site (final-step pause block at line ~587); (5) `tests/test_bellows.py` — locate `test_run_plan_inprogress_entry_renames_to_verdict_pending` and capture the non-conformant fixture. **Pre-flight scope check:** run `grep -rn 'gate_result\[.failures.\]' tests/` to confirm the test exposure surface. List every test file that references the failures list. Production contract is `list[dict]` with shape `{"gate": str, "evidence": str}` per the 2026-05-26 SA findings. **Task:** Three changes, in order. **Change 1 — Update fixture in `tests/test_bellows.py`.** Locate `test_run_plan_inprogress_entry_renames_to_verdict_pending`. Update the `gate_result["failures"]` value from `["scope_check"]` (string list) to `[{"gate": "scope_check", "evidence": "<evidence text matching test intent>"}]` (dict list matching production contract). The `evidence` text should describe whatever the test is asserting — read the test body to infer the right evidence string. If the test's assertions reference the failure structure beyond `"scope_check"` membership, update assertions to match the dict shape. If the test only asserts that a failure exists (not its content), the evidence string can be a brief descriptive placeholder like `"test fixture for scope_check failure"`. **Change 2 — Remove the `isinstance(f, dict)` guard at `bellows.py:495`.** The current code at `bellows.py:495` includes `isinstance(f, dict) and` in the `all(...)` predicate that classifies pause reasons. Remove the `isinstance(f, dict) and` clause, leaving `all(f.get("gate") == "rule_22_verification" for f in gate_result["failures"])` (or whatever the simplified form is). The `f.get("gate")` form is preserved (safer than `f["gate"]`) because it's part of the production contract pattern, not the defensive guard. **Change 3 — Remove the `isinstance(f, dict)` guard at `bellows.py:587`.** Same change as Change 2, applied to the second Fix F site at line ~587. The two sites should be byte-equivalent on the discriminator check after removal. **Test execution.** Run full test suite: `python3 -m pytest tests/ -v 2>&1 | tail -60`. Capture pass count, failure count, and confirm zero regressions. Pre-edit baseline: 407 passed / 5 carry-over failures (from main checkout; worktree shows 4 worktree-context + 1 long-standing). Post-edit expectation: 407 passed / 5 carry-over failures (or 411 / 1 if running from main). **Anchor verification before commit:** before staging, grep `bellows.py` for `isinstance(f, dict)` — expected: zero matches in the two Fix F blocks (both removed). One match may remain at the `_gate_rule_22_verification`-related site at `bellows.py:505` if that one is structurally distinct (the 2026-05-21 isinstance-asymmetry diagnostic verified both Block 1 at line 505 and Block 2 at line 594 use the symmetric pattern — those are different sites from the Fix F guards). Confirm which sites are Fix-F-related (added 2026-05-26 per the dev log) versus structurally-needed (added 2026-05-21 for Block 1/Block 2 symmetry). Remove only the Fix-F-added guards; leave the 2026-05-21 symmetric pattern intact. **If unclear which guard is which, halt and report to CEO rather than guessing.** **Deposit:** Author a dev log to `bellows/knowledge/development/fix-f-guard-removal-2026-05-26.md` documenting: the three changes with before/after snippets (1-3 lines of context plus the changed lines), the pre-flight grep results, the test counts (before/after), confirmation that the 2026-05-21 isinstance symmetry pattern is preserved, and the Output Receipt per your specialist file. **Commit:** stage code change, test fixture update, and dev log with message `fix(bellows): remove Fix F isinstance guard at lines 495 and 587 after test fixture conformance update`. **DO NOT push to origin** — the Planner handles session-wrap commits. **Standard prompt feedback protocol** → append entry to `knowledge/research/agent-prompt-feedback.md`. Second commit for feedback: `docs: prompt feedback — bellows DEV Fix F guard removal`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/knowledge/development/fix-f-guard-removal-2026-05-26.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
> - `bellows/bellows.py` (modified)
> - `bellows/tests/test_bellows.py` (modified)

---
---

## STEP 2 — QA

---

> You are the Bellows QA Agent. Read your specialist file at `agents/BELLOWS_QA.md` and `governance/GUARDRAILS.md` first. **Skip glossary read — domain terminology is fully covered in the specialist file.** Note: worktree root IS the bellows project root — strip the `bellows/` prefix from any path references in this prompt when running commands. **Before starting, read `knowledge/development/fix-f-guard-removal-2026-05-26.md` (DEV's Step 1 deposit) and check its Output Receipt status; if not Complete, stop and report the blocker.** **FIRST — Deliverable Verification (Rule 17).** Read DEV's Output Receipt "Files Created or Modified (Code)" list. For each entry, verify the file exists and the declared changes are present. Produce verification table `| Deliverable | Expected | Status (✅/❌) | Evidence |`. Specifically: (1) `bellows.py:495` block no longer contains `isinstance(f, dict)` — grep with context and confirm only the 2026-05-21 symmetric pattern guards remain at the structurally-distinct sites; (2) `bellows.py:587` block no longer contains `isinstance(f, dict)` — same grep; (3) `tests/test_bellows.py::test_run_plan_inprogress_entry_renames_to_verdict_pending` fixture uses dict shape `{"gate": ..., "evidence": ...}` — grep the test function and capture the fixture lines; (4) the 2026-05-21 Block 1 / Block 2 symmetric isinstance pattern is intact at its own sites (separate from Fix F sites) — confirm via grep with line numbers; (5) `knowledge/development/fix-f-guard-removal-2026-05-26.md` exists and documents the three changes. Capture each grep output to evidence: `knowledge/qa/evidence/executable-fix-f-guard-removal-2026-05-26/bellows_495_grep.txt`, `bellows_587_grep.txt`, `test_fixture_grep.txt`, `isinstance_pattern_grep.txt`, `dev_log_check.txt`. Any ❌ blocks plan close — report to CEO. **Test execution.** Run full test suite: `python3 -m pytest tests/ -v 2>&1 | tail -80`. Capture to `knowledge/qa/evidence/executable-fix-f-guard-removal-2026-05-26/pytest_full.txt`. Verify: (a) pass count matches DEV's reported number (expect 407 or similar with the 5 known carry-over failures); (b) zero new failures; (c) zero regressions; (d) the test that previously used the non-conformant fixture (`test_run_plan_inprogress_entry_renames_to_verdict_pending`) still passes with the dict fixture. **Structural-compliance checks.** (a) Confirm Fix F guards removed only at the two Fix F sites, not at the 2026-05-21 symmetric pattern sites. Capture both site lists side-by-side to `evidence/.../guard_site_classification.txt`. (b) Confirm production contract `list[dict]` is consistent: every `gate_result["failures"]` assignment in source code uses dict shape (none use string lists). Capture to `evidence/.../contract_consistency.txt` via `grep -n 'gate_result\["failures"\]' bellows.py gates.py verdict.py`. (c) Confirm no other test fixture uses the non-conformant string-list shape — pre-flight grep `tests/` for `"failures": \[` and verify all matches are dict lists. Capture to `evidence/.../fixture_audit.txt`. **Rule 20 self-check** — run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at governance root. Use these values: `plan_slug` = `executable-fix-f-guard-removal-2026-05-26`; `qa_report_path` = `bellows/knowledge/qa/executable-fix-f-guard-removal-2026-05-26.md`; `evidence_dir` = `bellows/knowledge/qa/evidence/executable-fix-f-guard-removal-2026-05-26/`; `required_evidence_files` = `["bellows_495_grep.txt", "bellows_587_grep.txt", "test_fixture_grep.txt", "isinstance_pattern_grep.txt", "dev_log_check.txt", "pytest_full.txt", "guard_site_classification.txt", "contract_consistency.txt", "fixture_audit.txt"]`. Include literal stdout in QA report. If FAILED, halt — report to CEO. **Final:** Update `bellows/PROJECT_STATUS.md` — prepend a 2026-05-26 entry under Completed for "Fix F isinstance guard removal + test fixture conformance" with a one-paragraph summary using `Filesystem:edit_file` (find the existing topmost Completed entry as the anchor and insert immediately before it). **DAEMON RESTART REMINDER goes in the QA deposit** under Flags for CEO: "REMINDER: restart Bellows daemon to load the updated pause-reason discriminator code. The running daemon executed this plan with pre-edit code; the simplified discriminator (without isinstance guards) activates on next plan dispatched after restart." **Commit:** stage QA report, evidence files, and PROJECT_STATUS update with message `qa(bellows): Fix F guard removal verified, test fixture conformance verified`. **DO NOT push to origin** — Planner handles session-wrap commits. **Standard prompt feedback protocol** → append entry to `bellows/knowledge/research/agent-prompt-feedback.md`. Second commit: `docs: prompt feedback — bellows QA Fix F guard removal`. **DO NOT push.**
>
> **Deposits:**
> - `bellows/knowledge/qa/executable-fix-f-guard-removal-2026-05-26.md`
> - `bellows/knowledge/qa/evidence/executable-fix-f-guard-removal-2026-05-26/` (9 evidence files per Rule 20 self-check list)
> - `bellows/PROJECT_STATUS.md` (modified)
> - `bellows/knowledge/research/agent-prompt-feedback.md`
