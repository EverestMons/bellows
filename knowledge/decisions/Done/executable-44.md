# Bellows — Daemon-Owned Ledgers Phase 2: PROJECT_STATUS→Daemon-Post-Merge (Dormant, Coexistence-Safe)
**Date:** 2026-06-13 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** always | **qa_steps:** 2 | **Test Scope:** full suite

## Context (Rule 27)
This executable implements diagnostic 42, Phase 2 of the phased migration (CEO fork verdicts, processed-verdict-42-step-1): relocate `PROJECT_STATUS.md` writes OUT of the worktree merge path via daemon-post-merge append. It REUSES the Phase 1 framework (plan 43, Done): the `### Ledger Updates` Output Receipt channel, the `_apply_ledger_updates()` function, and the `files_changed` coexistence pattern — Phase 2 only EXTENDS them with a project_status handler. Disposition is (a) daemon-written post-merge, NOT DB-generated: PROJECT_STATUS entries are rich agent narratives (cross-refs to dev logs, QA reports, SHAs) that the daemon should preserve verbatim, not synthesize. After a successful code merge, the daemon extracts the milestone text from the receipt's `### Ledger Updates` (a `#### Project Status` subsection) and appends it to `PROJECT_STATUS.md` on main at a CANONICAL position (after the first `## Completed` heading per the design's positioning mitigation), then commits — conflict-free because the daemon operates on main, never a worktree. Phase 2 stays DORMANT/coexistence-safe: if `PROJECT_STATUS.md` is in `files_changed` (agent wrote it old-style per current Rule 8) → skip the daemon write. Like Phase 1, Phase 2 does NOT close FORWARD rows 4/5/13 — the governance follow-on activates all phases at once. The design doc `knowledge/research/daemon-owned-ledgers-design-2026-06-13.md` is AUTHORITATIVE — Section 3 File 1 (PROJECT_STATUS disposition + canonical-append positioning), Section 4 (channel), Section 6 Phase 2. Re-verify Verification Blocks at edit time; halt-and-report on divergence. Anchors (re-verify by grep): `_apply_ledger_updates(parsed, project_path, plan_id, files_changed)` and the `### Ledger Updates` parse extraction (both from plan 43). DAEMON RESTART REQUIRED after close (flag in receipt).

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_DEVELOPER.md`, then the design doc Section 3 File 1 + Section 4, then the EXISTING Phase 1 code in `parser.py` (the `### Ledger Updates` extraction) and `bellows.py` (`_apply_ledger_updates`) so you extend them consistently. **Scope is exactly: `parser.py`, `bellows.py`, `tests/test_parser.py`, `tests/test_bellows.py`.**
> - **Channel parse extension:** in `parser.py`, extend the existing `### Ledger Updates` extraction to also capture a `#### Project Status` subsection into `parsed["ledger_updates"]["project_status"]` (same pattern as the Phase 1 `#### Prompt Feedback` → feedback). Absent → None, no error.
> - **Apply extension (daemon-post-merge + coexistence):** extend `_apply_ledger_updates` to handle project_status: if `PROJECT_STATUS.md` appears in `files_changed` → SKIP (coexistence: agent wrote old-style). Else if `parsed["ledger_updates"]["project_status"]` is present → append the milestone text to `<project_path>/PROJECT_STATUS.md` at the canonical position (after the first `## Completed` heading; if no such heading exists, append at EOF — handle both, log which path taken), then `git -C <project_path> add PROJECT_STATUS.md && git commit` with a clear message (e.g. `docs(status): PROJECT_STATUS milestone for plan <id> (daemon-post-merge)`). Daemon operates on the real project root (main), NOT a worktree. Log-and-continue: a failure here must not crash teardown. Do NOT alter the Phase 1 feedback path.
> - Do NOT touch Rule 8 / the feedback protocol / FORWARD handling — governance is the separate follow-on; FORWARD is Phase 3.
>
> **Tests:** (a) parser extracts `#### Project Status` (and Phase 1 feedback still extracts — regression); (b) `_apply_ledger_updates` SKIPS project_status when PROJECT_STATUS.md in files_changed (coexistence); (c) WRITES (append at canonical position) when not in files_changed and receipt carries project_status — assert the entry lands after the first `## Completed` heading in a fixture file, and the no-`## Completed` EOF-fallback path; (d) the daemon commits the change (or test the append function in isolation against a temp git repo per repo conventions). Then run the FULL suite (`python3 -m pytest tests/`) to an explicit pass/fail and READ THE TAIL. Write the dev log to `bellows/knowledge/development/daemon-ledgers-phase2-projectstatus-dev-log-2026-06-13.md`. Use `with open()`; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/daemon-ledgers-phase2-projectstatus-dev-log-2026-06-13.md`

---
---

## STEP 2 — Bellows QA

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/BELLOWS_QA.md`, the dev log, and design doc Section 3 File 1. **Verify, each with executed evidence (files into `knowledge/qa/evidence/daemon-ledgers-phase2-2026-06-13/`):** (1) **Full suite** — final 15 lines, zero failures, new-test count matches dev log; output to `full_suite_tail.txt`. (2) **Parser extension** — `#### Project Status` extracts into `parsed["ledger_updates"]["project_status"]`, AND Phase 1 `#### Prompt Feedback` still works (no regression); output to `parser_extension.txt`. (3) **Canonical-append behavior** — the append-after-first-`## Completed` and the EOF-fallback are both demonstrated by the tests; output to `canonical_append.txt`. (4) **Dormancy/coexistence (load-bearing)** — `_apply_ledger_updates` SKIPS the project_status write when PROJECT_STATUS.md is in files_changed; grep confirms it does not write PROJECT_STATUS inside any worktree; confirm no real PROJECT_STATUS.md was modified by this plan; output to `dormancy_proof.txt`. (5) **Phase 1 intact** — the feedback path and `prompt_feedback` table are unchanged (`git diff` shows only additive extension); output to `phase1_intact.txt`. (6) **No scope bleed** — `git diff HEAD~1 --stat` shows only in-scope files; FORWARD handling and Rule 8 untouched; output to `scope_check.txt`.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `daemon-ledgers-phase2-2026-06-13`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/daemon-ledgers-phase2-qa-report-2026-06-13.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/daemon-ledgers-phase2-2026-06-13/`; `required_evidence_files`: `[full_suite_tail.txt, parser_extension.txt, canonical_append.txt, dormancy_proof.txt, phase1_intact.txt, scope_check.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table and the Rule 20 banner block to `bellows/knowledge/qa/daemon-ledgers-phase2-qa-report-2026-06-13.md`. **Receipt Flags for CEO:** (1) DAEMON RESTART REQUIRED; (2) Phase 2 DORMANT — does not close FORWARD rows yet; (3) next: Phase 3 (FORWARD new-rows) then the governance activation follow-on. Use `with open()`; no heredocs. **Deposits:**
> - `bellows/knowledge/qa/daemon-ledgers-phase2-qa-report-2026-06-13.md`
> - `bellows/knowledge/qa/evidence/daemon-ledgers-phase2-2026-06-13/` (six evidence files per Rule 20 self-check)
