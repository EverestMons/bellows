# Bellows — Fence-Stripping Utility for Plan-Text Parsers
**Date:** 2026-05-11 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1 (DEV) → Step 2 (QA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation. Test Scope: targeted — fix touches 3 files (`bellows.py`, `gates.py`, `verdict.py`) with new shared utility + 4 call sites; targeted tests for the affected parsers and the new utility are sufficient. Live integration smoke test is OUT OF SCOPE for this plan per the 2026-04-19 Lesson — Bellows does not hot-reload, so a Bellows-side fix cannot be live-tested by the plan that ships it. Code-level QA only (unit tests, grep, baselines).

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/executable-fence-strip-plan-text-parsers-2026-05-11.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to Step 2 or move the plan to Done.
```

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-fence-strip-plan-text-parsers-2026-05-11.md", "bellows/knowledge/decisions/in-progress-executable-fence-strip-plan-text-parsers-2026-05-11.md")`. You are the Bellows Developer. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first. Skip the domain glossary read — this is a code-tracing fix task. **Context.** The diagnostic at `bellows/knowledge/research/extract-total-steps-fence-class-audit-2026-05-11.md` confirmed 4 BUG-CONFIRMED parsers that match `## STEP N` patterns without stripping fenced code blocks first: (1) `extract_total_steps` at `bellows.py:98-103` over-counts plan steps when example headers appear inside code fences (causing spurious dispatches), (2) `_extract_step_text` at `gates.py:243-251` extracts wrong step text when in-fence headers precede the real ones (corrupts `_gate_scope_check`, `_gate_deposit_exists`, and `_extract_plan_required_deposits`), (3) `_gate_is_qa_step` at `gates.py:340-345` misclassifies steps based on in-fence example headers (can apply Rule 20 to non-QA steps or skip it on real QA steps), and (4) `_extract_step_text_from_plan` at `verdict.py:23-31` (explicit duplicate of gates.py one, kept in sync per documented circular-import avoidance) has the same bug and corrupts verdict request deposit paths. **Recommended fix shape** from the diagnostic Q6: shape (a) — a single shared `strip_fenced_code_blocks(text)` utility applied to `plan_text` at the call site before parsing, NOT inside each parser. The diagnostic confirmed no existing fence-stripper exists in the codebase. **Implementation.** **Part A — Add the shared utility to `bellows/bellows.py`.** Add a module-level function near `extract_total_steps` (around `bellows.py:98`): `def strip_fenced_code_blocks(text: str) -> str: """Remove fenced code blocks (\`\`\` ... \`\`\`) from text, preserving line structure. Used by plan-text parsers to prevent example/fixture headers inside code fences from being parsed as structural elements."""` The body uses `re.sub` with a regex that matches a triple-backtick fence opener at line-start, any optional language label on the same line, then everything (including newlines) up to a closing triple-backtick fence at line-start — substituting with empty string. Use `re.MULTILINE | re.DOTALL` flags so `^` anchors to line-starts and `.` matches newlines. The closing fence pattern should also anchor to line-start to avoid greedy over-matching across multiple fenced blocks. Test the regex against the Q1 REPL fixture from the diagnostic before committing. This matches triple-backtick lines at line-start, which is the standard markdown convention. **Part B — Apply the utility at the 4 call sites.** Call-site change at each parser entry point: wrap `plan_text` with `strip_fenced_code_blocks()` before the regex executes. Specific changes: (1) In `extract_total_steps` (`bellows.py:98-103`), apply the strip at the top of the function before the `re.findall` calls — `plan_text = strip_fenced_code_blocks(plan_text)`. (2) In `_extract_step_text` (`gates.py:243-251`), apply the strip at the top of the function before the `re.search` call. Import the utility at the top of `gates.py`: `from bellows import strip_fenced_code_blocks`. If circular-import issues surface (gates.py is imported by bellows.py), duplicate the function in gates.py with a matching "keep in sync" comment pointing at bellows.py — same pattern as the existing `_extract_step_text` duplication. Document which path you took in the dev log. (3) In `_gate_is_qa_step` (`gates.py:340-345`), apply the strip at the top of the function before the `re.search` call. Uses the same import as (2). (4) In `_extract_step_text_from_plan` (`verdict.py:23-31`), apply the strip at the top of the function. Same import-or-duplicate decision as (2). The function already has a "keep in sync" comment with gates.py per `bellows/agents/BELLOWS_DEVELOPER.md` Quality Standards — if you duplicate the utility there too, add the same sync comment. **Part C — Tests.** Add tests in the existing test files matching the parser's home module. (1) `bellows/tests/test_bellows.py` — add `test_extract_total_steps_ignores_in_fence_headers` near the existing `test_extract_total_steps_*` cluster around `test_bellows.py:1813-1838`. Fixture: 2 real `## STEP N` headers + a fenced block containing 2 in-fence `## STEP N` patterns at line-start; assert returns 2. Also add `test_strip_fenced_code_blocks_basic` (utility unit test) — fixture with fence + non-fence content, assert fence content stripped while non-fence preserved including blank lines. (2) `bellows/tests/test_gates.py` — add `test_extract_step_text_ignores_in_fence_headers` (fixture with in-fence `## STEP 2` before real `## STEP 2`, assert returns the REAL step body not the fence content). Add `test_gate_is_qa_step_ignores_in_fence_headers` (fixture where in-fence `## STEP 2 — Bellows QA` precedes real `## STEP 2 — Developer`, assert returns False — `_gate_is_qa_step` is not currently tested at all per the diagnostic Q7, so this is also the baseline test for that function). (3) `bellows/tests/test_verdict.py` — add `test_extract_step_text_from_plan_ignores_in_fence_headers` (same shape as gates.py test, but exercises verdict.py — diagnostic Q7 confirmed zero existing tests for this function, this is the baseline + regression test combined). **Implementation constraints.** Do NOT deduplicate `_extract_step_text` and `_extract_step_text_from_plan` — the duplication is documented as intentional for circular-import avoidance (per `executable-bellows-verdict-schema-plan-b-2026-04-18` lessons). Patch both with matching changes and keep the "keep in sync" comment. Do NOT modify Rule 26 or any governance files. Do NOT add or remove any other gates. Scope is strictly: utility function + 4 call-site changes + 5 tests. **Run tests.** Run `cd bellows && python3 -m pytest tests/test_bellows.py tests/test_gates.py tests/test_verdict.py -x` from the repo root. All new tests must pass. No regressions in existing tests. Deposit the full test output to evidence file (path declared in Deposits). **Commit.** Single commit with message `fix(parsers): strip fenced code blocks before parsing plan_text — 4 parsers (extract_total_steps, _extract_step_text, _gate_is_qa_step, _extract_step_text_from_plan)`. Body cites the diagnostic findings file path. **Deposit dev log.** Deposit a development log file at the path in Deposits documenting: the import-vs-duplicate decision (Part B circular-import path you chose), exact LOC delta per file, test count delta, and any decisions made within specialist authority. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/development/fence-strip-plan-text-parsers-2026-05-11.md`
> - `bellows/knowledge/qa/evidence/executable-fence-strip-plan-text-parsers-2026-05-11/`
>
> **STOP. Do NOT proceed to Step 2. Do NOT move the plan to Done. Wait for CEO confirmation before continuing.**

---
---

## STEP 2 — Bellows QA

---

> **Before starting, read `bellows/knowledge/development/fence-strip-plan-text-parsers-2026-05-11.md` and check the Output Receipt status field. If status is not Complete, stop and report the issue to the CEO before proceeding.** You are the Bellows QA agent. Read your specialist file at `bellows/agents/BELLOWS_QA.md` first. Skip the domain glossary read — this is a code-level QA task. **FIRST — Deliverable Verification.** Read the prior DEV step's Output Receipt "Files Created or Modified (Code)" list. For EVERY listed file: verify it exists on disk and contains the described change. Verification commands: (a) `grep -n "def strip_fenced_code_blocks" bellows/bellows.py` — must return exactly one match (or one match in gates.py + one match in verdict.py if the DEV chose the duplicate path; verify against the DEV's documented decision). (b) `grep -n "strip_fenced_code_blocks(plan_text)" bellows/bellows.py bellows/gates.py bellows/verdict.py` — verify the call-site application exists at all 4 functions documented in the diagnostic (`extract_total_steps`, `_extract_step_text`, `_gate_is_qa_step`, `_extract_step_text_from_plan`). (c) `grep -nE "def test_(extract_total_steps_ignores_in_fence|strip_fenced_code_blocks_basic|extract_step_text_ignores_in_fence|gate_is_qa_step_ignores_in_fence|extract_step_text_from_plan_ignores_in_fence)" bellows/tests/test_bellows.py bellows/tests/test_gates.py bellows/tests/test_verdict.py` — verify all 5 new tests exist. (d) `git --no-pager log -1 --stat` — verify single commit, all expected files present in the diff. Produce a verification table: `| Deliverable | Expected | Status (✅/❌) | Evidence |` with one row per deliverable. If ANY item is ❌, attempt to fix (re-commit, re-create) before proceeding. If unfixable, stop and report to CEO. **Test verification.** Run the targeted test suite: `cd bellows && python3 -m pytest tests/test_bellows.py tests/test_gates.py tests/test_verdict.py -v 2>&1 | tee bellows/knowledge/qa/evidence/executable-fence-strip-plan-text-parsers-2026-05-11/pytest_targeted.txt`. All 5 new tests must pass. No new failures in existing tests. The known pre-existing `test_run_step_timeout` failure may persist — if it's the only failure and it was failing before this plan, document it explicitly in the QA report as a pre-existing failure (not a regression). **Behavioral regression check.** Run the original BUG-CONFIRMED REPL fixture from the diagnostic Q1 against the current code (do this as inline Python, capture output to evidence file `pytest_repl_extract_total_steps.txt`): construct a string with 2 real `## STEP N` headers + a fenced block containing 2 in-fence patterns, call `extract_total_steps(fixture)`, assert returns 2. Same shape for the other 3 parsers from diagnostic Q3 — each fixture, each call, each result, all captured to separate evidence files (`pytest_repl_extract_step_text.txt`, `pytest_repl_gate_is_qa_step.txt`, `pytest_repl_extract_step_text_from_plan.txt`). All 4 must demonstrate the bug is fixed (returns the correct value instead of the documented buggy value). **No live integration smoke test.** Per the 2026-04-19 Lesson on Bellows hot-reload absence, do NOT dispatch a canary plan to verify live behavior in this QA step — the running Bellows daemon was started before this fix and would still run pre-fix code. The CEO performs the post-restart canary as a separate plan. **QA report deposit.** Write the QA report at the path declared in Deposits. Structure: top-level Deliverable Verification table (Rule 17), Test Results section with summary + pre-existing failure note, Behavioral Regression Verification table (4 parsers × buggy-vs-fixed values), and Rule 20 Self-Check Results at the end. **Rule 20 Self-Check.** Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `executable-fence-strip-plan-text-parsers-2026-05-11`, `qa_report_path`: `/Users/marklehn/Desktop/GitHub/bellows/knowledge/qa/fence-strip-plan-text-parsers-qa-2026-05-11.md`, `evidence_dir`: `/Users/marklehn/Desktop/GitHub/bellows/knowledge/qa/evidence/executable-fence-strip-plan-text-parsers-2026-05-11/`, `required_evidence_files`: `["pytest_targeted.txt", "pytest_repl_extract_total_steps.txt", "pytest_repl_extract_step_text.txt", "pytest_repl_gate_is_qa_step.txt", "pytest_repl_extract_step_text_from_plan.txt"]`. Include the literal stdout of the block in the QA report. If FAILED, halt and report to CEO — do not proceed with PROJECT_STATUS update. **Final.** Update `bellows/PROJECT_STATUS.md` — add a completed milestone entry summarizing all changes (utility function added, 4 parsers patched, 5 tests added, diagnostic findings reference). Commit with descriptive message. **STOP.** Do NOT move this plan to Done. Per the disable-auto-close model, the Planner performs the terminal move after Rule 22 verification passes. Order of final operations (per Rule 23): QA report deposit → behavioral evidence files → Rule 20 self-check → PROJECT_STATUS.md update → feedback append → final commit. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `bellows/knowledge/qa/fence-strip-plan-text-parsers-qa-2026-05-11.md`
> - `bellows/knowledge/qa/evidence/executable-fence-strip-plan-text-parsers-2026-05-11/`

---
