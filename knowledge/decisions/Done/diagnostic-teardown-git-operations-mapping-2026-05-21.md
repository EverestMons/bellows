# bellows — Diagnostic: Teardown Git Operations Mapping
**Date:** 2026-05-21 | **Tier:** Small | **Dispatch Mode:** bellows | **Test Scope:** none (read-only diagnostic) | **Execution:** Step 1 (SA) | **pause_for_verdict:** always

## Context

The 2026-05-21 session (mis-dated as 2026-05-27 in earlier session artifacts; see PLANNER_TEMPLATE Rule 40 entry) observed four parallel-SHA local-vs-origin commit pairs:
- `eeaedcb` (local) ≡ `4294706` (origin) — normalize fix
- `289df0c` (local) ≡ `8469c44` (origin) — normalize QA
- `3075764` (local) ≡ `80ca915` (origin) — normalize diagnostic
- `9acd499` (local recovery) ≡ `7f0c6f2` (origin) — verdict-enrichment diagnostic

All four pairs were content-identical (`git diff` returned empty in every case). Local was 3 commits ahead of origin and origin was 4 commits ahead of local at session end; reconciled via `git reset --hard origin/main`.

The baton in `bellows/NEXT_SESSION.md` Priority 2 hypothesized the mechanism is "Bellows's worktree teardown pushes the agent's commit from the worktree directly to origin." A pre-diagnostic grep of `bellows/*.py` for `git push` and `subprocess.*push` returned **zero hits** — Bellows code has no push operations anywhere. The baton's hypothesis is mechanically false at the Bellows-code level.

What's actually happening is unknown. The dispatched agent (running in a worktree) may be pushing to origin as part of Rule 23 housekeeping; Bellows then cherry-picks the agent's commit onto local main (producing a content-identical commit with a different SHA due to different timestamp or parent); the local push to origin never happens within Bellows's run cycle, leaving local ahead with a stale parallel-SHA chain until a manual reconciliation.

This diagnostic maps the actual git operation timeline from agent dispatch through teardown through next-session-start, with the goal of explaining (and naming a corrective response to) the parallel-SHA divergence pattern. The diagnostic is sequenced BEFORE the Priority 1 verdict-enrichment executable because that plan is 229 LOC and routes through the teardown path multiple times during its own dispatch — shipping it against an unverified teardown bug could compound the same divergence at higher volume.

This is a read-only diagnostic — no code changes, no tests, no production behavior modified.

---
---

## STEP 1 — SA

---

> You are the Bellows Systems Analyst. Read your specialist file at `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` and `governance/GUARDRAILS.md` first. **Skip glossary read — this is a code-tracing diagnostic, no domain terminology dependency beyond what your specialist file already covers.** **Task:** Map the actual git operation timeline from agent dispatch through teardown through next-session-start, and explain the parallel-SHA local-vs-origin divergence pattern observed in the 2026-05-21 session. **Reads (mandatory, in order):** (1) `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md` — your specialist file; (2) `bellows/bellows.py` — full file (you must trace through the call sites at lines 514, 602, 628 calling `_teardown_worktree` and the function body at line 757); (3) `bellows/runner.py` — full file (the agent-dispatch path; look for any git operations performed pre- or post-subprocess); (4) `bellows/agents/BELLOWS_QA.md` and `bellows/agents/BELLOWS_DEVELOPER.md` — these specialist files document what the agent is REQUIRED to do as housekeeping (specifically Rule 23 `git add → git commit → git push` sequence); (5) `bellows/knowledge/research/teardown-worktree-reliability-2026-05-10.md` — the most recent teardown audit (note: it does NOT cover push behavior, which is the gap this diagnostic addresses); (6) `bellows/knowledge/research/worktree-teardown-current-state-findings-2026-05-05.md` — earlier state findings for cross-reference; (7) `PLANNER_TEMPLATE.md` Rule 23 (the housekeeping order: feedback → commit) and any rules covering agent-side git push behavior; (8) the git log for one of the four observed commit pairs — run `git log --all --oneline -20` at `/Users/marklehn/Developer/GitHub/bellows/` to see the divergent SHAs in context, then `git show eeaedcb` and `git show 4294706` to confirm content-identical-different-SHA, and `git log eeaedcb..HEAD --oneline` plus `git log 4294706..origin/main --oneline` to see what surrounds each. **Investigation questions (the diagnostic MUST answer each):** **Q1. Does the dispatched agent execute `git push` as part of standard housekeeping?** Look in two places: (a) the BELLOWS_QA.md, BELLOWS_DEVELOPER.md, and BELLOWS_DOCUMENTATION_ANALYST.md specialist files for any prescribed git workflow; (b) recent dev-logs or QA reports for evidence of `git push` calls made by the agent (e.g., grep `bellows/knowledge/dev-logs/` and `bellows/knowledge/qa/` for `git push`, `pushed to`, `origin/main`, with file:line hits and surrounding context for the top 5–10 hits). State definitively: does the agent push, or does it not? **Q2. What `git` subprocess calls does Bellows itself make, anywhere in the codebase?** Grep `bellows/*.py` for `subprocess.run` and `subprocess.Popen` calls where the first argument starts with `git` or contains `git`. Enumerate each call site with file:line, the git subcommand, and what triggers it. Confirm the pre-diagnostic claim that Bellows performs NO push operations — or refute it by naming the call site. **Q3. Trace one specific divergence case end-to-end.** Pick the `eeaedcb` (local) ≡ `4294706` (origin) pair from the normalize-fix plan (the path-form-normalization fix that shipped earlier 2026-05-21 / mis-dated 2026-05-27). Walk through the timeline: (a) what commit produced `4294706` on origin — agent-side push or Bellows-side push? (Use `git reflog` if available, or infer from timestamps and committer fields via `git show --format=fuller 4294706` and `git show --format=fuller eeaedcb`.) (b) What commit produced `eeaedcb` on local — Bellows's cherry-pick during teardown? (Trace through `_teardown_worktree` and check the timestamps.) (c) Why are the SHAs different despite identical content — parent commit difference, committer-date difference, or both? (`git show --pretty=raw 4294706` and `git show --pretty=raw eeaedcb` will show the parent SHA and committer date directly.) (d) After teardown, what does Bellows do with local main? Does it push? Reset? Nothing? (e) What is the divergence state at session end — local ahead by N, origin ahead by M? **Q4. Identify the actual mechanism producing the divergence.** Synthesize Q1–Q3. State the mechanism in one paragraph, naming concrete code paths and operation order. If the mechanism implicates a Bellows bug (e.g., teardown should push the cherry-picked commit to origin but doesn't), name the bug location with file:line. If the mechanism implicates an agent-discipline-vs-Bellows-architecture mismatch (e.g., the agent pushes from worktree per Rule 23, but Bellows's teardown then introduces local divergence that the agent didn't anticipate), name that too. If the mechanism is benign (e.g., it's the expected outcome of an architectural choice and the only fix is operational), say that. **Q5. Recovery surface.** Today's session used `git fetch origin && git reset --hard origin/main` to reconcile. (a) Is this safe in all cases — when local has commits NOT yet pushed to origin (independent of the parallel-SHA pairs)? (b) Should Bellows perform this reconciliation automatically post-teardown, or does that create new failure modes (e.g., overwriting Planner work-in-progress)? (c) Are there other observable artifacts that would help operators detect the divergence faster (e.g., a startup-sweep log line, a heartbeat field, a notification)? **Q6. Implications for Priority 1.** The Priority 1 verdict-enrichment executable will route through the teardown path multiple times during its dispatch (5 deposits, each producing an agent commit + teardown). State whether the divergence pattern would compound at higher volume (e.g., 5 parallel-SHA pairs instead of 1) or whether it's effectively rate-limited per-plan (i.e., one divergence per session regardless of plan count). If it compounds, recommend a sequencing or operational change before Priority 1 ships. If it doesn't compound, state that Priority 1 can proceed without modification. **Constraints:** **Read-only — do NOT modify any code, run any plans, push any commits, or alter any production state.** Do NOT propose architectural changes beyond what's needed to answer Q5 and Q6. Do NOT redesign teardown. The diagnostic's only job is to map the actual mechanism and surface the implications. **Output:** Deposit findings to `bellows/knowledge/research/teardown-git-operations-mapping-2026-05-21.md` following your specialist file's Output Format. Required sections: **Q1 Answer** (with file:line citations and grep evidence); **Q2 Answer** (enumerated subprocess.run call sites with git operations); **Q3 Answer** (one-case timeline trace with `git show` output snippets); **Q4 Answer** (one-paragraph mechanism statement); **Q5 Answer** (recovery procedure safety analysis + observability recommendations); **Q6 Answer** (sequencing implication for Priority 1 — proceed or defer); **Claim Verification Blocks** per SPECIALIST_TEMPLATE convention for every load-bearing population claim (e.g., "Bellows codebase contains zero `git push` calls" needs the grep command as the query and its output as the recorded result). **Standard prompt feedback protocol** → append entry to `bellows/knowledge/research/agent-prompt-feedback.md`; update Patterns Identified if relevant. **Commit:** stage your diagnostic deposit and the feedback log to the bellows repo with message `feat(bellows): SA diagnostic mapping teardown git operations and parallel-SHA divergence`. Second commit for feedback: `docs: prompt feedback — bellows SA teardown git ops diagnostic`. Do NOT push — the Planner will handle session-wrap commits and any required reconciliation.
>
> **Deposits:**
> - `bellows/knowledge/research/teardown-git-operations-mapping-2026-05-21.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`

---
