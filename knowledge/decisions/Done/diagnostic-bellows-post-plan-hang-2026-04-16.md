# Bellows — Post-Plan Hang Diagnostic
**Date:** 2026-04-16 | **Type:** diagnostic (investigation only, no fixes)

## How to Run This Plan

**Bootstrap:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-bellows-post-plan-hang-2026-04-16.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed further or move the plan to Done.
```

---
---

## STEP 1 — INVESTIGATION AGENT (bellows developer)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-post-plan-hang-2026-04-16.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-post-plan-hang-2026-04-16.md")`. Skip specialist file and glossary reads — pure code-tracing. **Context:** Bellows froze today after successfully completing a plan. The last step log (`logs/20260416-131311-step.json`) shows `success: true, stop_reason: end_turn` at 1:13 PM. The terminal became unresponsive ~40 minutes later. The plan moved to Done, the QA report was deposited, but Bellows never printed the "Queue empty" message and the terminal froze (required force-quit). The freeze is in Bellows' post-plan processing, not in the agent execution. **Q1 — Trace the post-final-step code path in `run_plan`.** After the final `runner.run_step` returns, `run_plan` runs: gate check, verdict request posting (if conditions met), record_run, print statements, and return. Show the exact sequence of operations between "step completed" and "function returns." Identify every operation that could block: (a) network calls (notifier.push, notifier.notify_verdict_request — do these make HTTP requests? Are they synchronous? Do they have timeouts?), (b) file I/O (verdict.post_verdict_request, verdict.log_to_ledger, shutil.move — any of these block on disk?), (c) subprocess calls (git diff for scope_check — does `_capture_git_diff` use subprocess? What timeout does it have?). **Q2 — Trace what happens AFTER `run_plan` returns.** `run_plan` is called from `_run_tracked`. After `run_plan` returns, `_run_tracked` decrements `_active_count` and calls `_check_queue_drain`. Then the `_rescan` timer fires every 30 seconds, calling `_consume_verdicts`. Trace this path: (a) does `_check_queue_drain` do anything that could block? (b) does `_consume_verdicts` do anything that could block (file I/O, network, subprocess)? (c) does `_rescan` itself block? Is it running in the main thread or a timer thread? **Q3 — Check the notifier for blocking calls.** Read `notifier.py` in full. (a) Does `notifier.push` make an HTTP request? (b) What timeout does it use? (c) If the Pushover API is unreachable or slow, does the call block indefinitely? (d) Is `notifier.push` called synchronously from `run_plan`? (e) How many `notifier.push` or `notifier.notify_*` calls happen in the post-final-step path — could multiple sequential blocking network calls compound? **Q4 — Check `_capture_git_diff` for subprocess hangs.** Read the function. (a) Does it use `subprocess.run` or `subprocess.Popen`? (b) What timeout does it set? (c) Could `git diff --stat` hang on a large repo or a locked index file? (d) Is it called AFTER the final step (for the post-step scope check)? **Q5 — Check the verdict consumption loop.** The `_rescan` method calls `_consume_verdicts`. If a verdict file is written by the Planner (via MCP tools) while Bellows is in the middle of `_consume_verdicts`, could there be a race condition or deadlock? (a) Does `_consume_verdicts` hold any locks? (b) Does `shutil.move` inside `_consume_verdicts` block if the destination exists? (c) Could the main thread's `time.sleep(1)` loop interact badly with the rescan timer? **Q6 — Propose the fix.** Based on Q1-Q5, identify the most likely hang point and propose a fix. The fix should: (a) add timeouts to any blocking calls that don't have them, (b) wrap network calls in try/except with a timeout so a single Pushover failure doesn't freeze Bellows, (c) add a watchdog or heartbeat print so the CEO can tell if Bellows is alive. If multiple potential hang points exist, rank them by likelihood. **Deposit:** write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/post-plan-hang-diagnostic-2026-04-16.md`. Verify you answered ALL SIX questions before marking Status=Complete. **Commit:** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/post-plan-hang-diagnostic-2026-04-16.md && git commit -m "docs: diagnostic — post-plan hang investigation"`. **Feedback:** standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed. Do NOT move the plan to Done. Report completion and wait for CEO confirmation.**
