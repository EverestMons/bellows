# bellows — ANVIL'S 'WHAT WORKS' ENTRY GENERATOR, READ-ONLY: `src/config.py` de-hardcoded (the roots derived from the checkout's own location, env-overridable), a library `src/what_works.py` that scores every production def under the three definitions #100072 measured and renders the top-K entries (role, signature, verified callers, docstring, tests bound), a CLI `scripts/what_works.py` that writes a research doc and a TSV and REFUSES the glossary as a target; run once on bellows, its entries deposited under governance research for the CEO to read before 'applied elsewhere' is claimed (thread 263, the CEO's ruling of 2026-09-11)

**Date:** 2026-09-11 | **Project:** bellows | **Tier:** Small (one config edit, one library, one CLI, one test file with a fixture, one mutation manifest, one dev-log; two research deposits) | **Dispatch Mode:** bellows | **Test Scope:** full-suite (anvil's suite under the step's venv — `pytest -q` from the anvil checkout — AND the bellows suite at each bellows commit, Rule 21) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 263 | **cycle_tier:** T1

**auto_close:** false

**Post-close:** no restart (nothing bellows loads changes), no doctrine. The anvil files are Planner-commit deposits: the Planner commits them on anvil `main` at the wrap (the #100057 shape, `a1910df`) and commits the two governance research files. Whether the entries are 'what works, applied elsewhere' — a glossary product — is the CEO's reading of the deposited doc; this plan writes nothing to `GLOSSARY.md` and claims nothing.

**Depends on:** the CEO's ruling of 2026-09-11 on thread 263 (session d04ebd33, after #100072): build the read-only entry generator on definition (c), never writing the glossary, 'applied elsewhere' unclaimed until the entries have been used once. Clone origin by kind and layout: `Done/diagnostic-100057.md` (a bellows-lane plan whose code deposits live under anvil, committed by the Planner; the venv under `mktemp -d` with `datasketch==2.0.0`; the four stage functions, never `run_cycle`) and `Done/executable-100074.md` (this morning: the two-commit DEV, failing-first, the mutation run redirected, the QA that reads the plan's own product). The spec of the three definitions is `governance/knowledge/research/what-works-definition-2026-09-11.md` (#100072) — its formulas, its tie rule, its weight table and its 33-member reference set; its appendix is ABRIDGED (P4), so the generator is built from the doc's stated rules, not from a script.

**Tier computed (§1):** **T1** — T-1 fires (anvil's config, a new library and CLI, a read across bellows' history); T-6 does not (no doctrine); T-2 does not (a scratch DB under a temp dir; no live store).

## CEO Context

#100072 scored three definitions of a load-bearing function against 33 names your records name; (c) role-weighted found 13 of them in its top 33 and ranked `gates.py::check` first with the hubs kept. You ruled: build the smallest product — a generator that emits the entry a function would carry, read-only, to a research doc — and claim nothing until the entries have been used. Two things stand in the way and this plan removes both. `src/config.py` hardcodes the shop machine's roots (`/Users/marklehn/Developer/GitHub/…`, P1), so every anvil run on this machine patches the module in-process; the fix is to derive the roots from the checkout's own location with an env override, the fork #100057 named first. And the instrument that produced #100072's numbers is gone — the doc's appendix carries a header and "see T/define.py" where the plan asked for the script verbatim, and `$T` was removed at the step's end (P4); the doc's stated formulas are the surviving spec, so the library is written from them and proven against the doc's own numbers (13 of 33 under (c)) before it emits an entry.

## What this changes

1. **`anvil/src/config.py`** — `ANVIL_ROOT = os.environ.get("ANVIL_ROOT", ANVIL_RUNTIME_ROOT)`; `PROJECTS_PARENT = os.environ.get("ANVIL_PROJECTS_PARENT", os.path.dirname(ANVIL_RUNTIME_ROOT))`; `SCAN_TARGETS[…]["path"]` = `os.path.join(PROJECTS_PARENT, "invoice-pulse")` and `os.path.join(PROJECTS_PARENT, "bellows")`; `ANVIL_DB_PATH` unchanged in form (`os.path.join(ANVIL_ROOT, "anvil.db")`). On the mini the parent is `/Users/marklehn/Developer`; on the Air `~/Developer/GitHub` — both correct without an edit. No other line of `config.py` moves; every consumer (P1's list) reads the same names.
2. **`anvil/src/what_works.py`** — new, read-only, stdlib + `sqlite3`: `production_defs(checkout) -> list[Def]` (the #100072 P6 rule: `git ls-files -- '*.py'` at `main`, not under `tests/`, `knowledge/`, `logs/`, `receipts/`, `.claude/`; `FunctionDef` and `AsyncFunctionDef` with `lineno`, `end_lineno`, the def line, the docstring's first line; a file that fails to parse is listed, not fatal); `verified_inbound(conn, checkout, defs)` (the doc's Q2 rule: cross-file `call` edges from `chunk_dependencies` whose caller's file, parsed at `main`, imports the target's module — `import m`, `import m as x`, `from m import name|*`, a relative form resolved against the caller's package — with `path_loaded` counted apart where the caller's file names `spec_from_file_location` or `runpy`); `survived(checkout, defs, today)` (Q3: `git log -L '<lineno>,<end_lineno>:<file>' --format='%H %ad' --date=short main`; `age_days` from the birth; `stable_commits` = commits other than the birth with no `^[-+]\s*(async )?def <name>\(` hunk line; `tests_bound` from `chunk_symbol_bindings` `binding_type='tests'`; `score_b = age_days × (1 + tests_bound) × stable_commits`; a refused trace scores zero and is listed); `ROLE_WEIGHTS` — a module constant with a docstring saying it is the PLANNER'S table from #100072, not the archetype's: 2.0 for plan_dispatcher, gate_checker, verdict_handler, agent_lifecycle, worktree_manager, plan_validator, plan_parser; 1.5 for notifier, config_loader, data_model, cache_manager; 1.0 otherwise; `score_c = score_a × ROLE_WEIGHTS[role]`; `rank(rows, definition)` — ties broken by `file::name` ascending, one rule; `precision_recall(ranked, reference, k)`; `entry(conn, checkout, row) -> dict` (role, the def line as signature, verified callers — count and the first three `file::name` —, the docstring's first line or `no docstring`, tests bound — count and the first two test node names from the binding sources); `render_markdown(entries, meta) -> str`, `render_tsv(rows) -> str` (the doc's 17 columns). No function writes a file; no function imports `run_cycle`, `run_lab`, `write_intent_audit`, `write_cycle_report` or `ingest_test_results`.
3. **`anvil/scripts/reprice_bellows_build.py`** — its `main()` body becomes `build_scratch_db(checkout, tmp_root) -> str` (returns the DB path; the temp-dir assert kept; `main()` parses the two flags and calls it) so the generator reuses the build instead of copying it. Behaviour of the CLI unchanged.
4. **`anvil/scripts/what_works.py`** — new CLI, argparse: `--checkout` (default the bellows path from `SCAN_TARGETS`), `--tmp` (required; must be under a temp dir — the same assert), `--definition {a,b,c}` (default `c`), `--top K` (default 20), `--out-md PATH`, `--out-tsv PATH`, `--reference FILE` (one `file::name` per line; prints `P@|ref| R@|ref| P@20 R@20` for the chosen definition and lists the members found). It builds the scratch DB with `build_scratch_db`, scores, ranks, renders. ⛔ **The glossary guard:** it REFUSES (exit 2, nothing written) any `--out-*` path whose basename is `GLOSSARY.md`, or that lies under a `knowledge/decisions` directory, or that is not under a directory named `research` or under `--tmp`; the refusal prints the rule. Read-only over the checkout; the writers of P1 are never imported.
5. **`anvil/tests/test_what_works.py`** — new (anvil's fixture shape: an in-memory `init_db` connection, `tmp_path` git repos as `test_cycle.py` builds them): (t1) `config.ANVIL_ROOT` equals the checkout when `ANVIL_ROOT` is unset and equals the env value when set (monkeypatch `os.environ`, `importlib.reload(src.config)` inside the test, and a `finally` that clears the variables and reloads again — a reloaded module is process-global, and `test_classifier.py`'s `monkeypatch.setitem(SCAN_TARGETS, …)` binds to whichever object is current); (t2) `SCAN_TARGETS["bellows"]["path"]` ends with `/bellows` under `PROJECTS_PARENT`; (t3) `production_defs` on a `tmp_path` repo with a top-level def, a method and a def under `tests/` → two defs, the method with its range; (t4) `survived` on a `tmp_path` repo with three commits — birth, a body change, a signature change — → `stable_commits == 1`, `age_days` from the first commit's date; (t5) the birth-only def → `score_b == 0` (the formula's consequence, pinned); (t6) `verified_inbound` on a two-file fixture where `a.py` imports `b` and calls `b.f`, and `c.py` calls `f` without an import → verified 1 of 2 for `b.py::f`; (t7) `rank` with two equal scores orders by `file::name`; (t8) `score_c` differs from `score_a` for a `gate_checker` def and equals it for a `utility` def; (t9) `precision_recall` on a hand-built ranking and reference → the expected fractions; (t10) the CLI refuses `--out-md <tmp>/GLOSSARY.md` and `--out-md <tmp>/knowledge/decisions/x.md` (exit 2, no file), accepts `<tmp>/research/x.md`; (t11) `render_markdown` on one entry carries the six fields as table rows; (t12) `build_scratch_db` is importable from `scripts.reprice_bellows_build` and `main()` still parses `--checkout --tmp`. **Fixture file** `anvil/tests/fixtures/what-works-reference-bellows-2026-09-11.txt` — the 33 R+ members from the doc's Q1 table, one `file::name` per line (the regression's reference).
6. **`knowledge/mutants/what-works-generator.json`** (bellows) — five mutants with `target` paths relative to the anvil repo root, run with `--repo-root` on a scratch clone (Item 5): (m1) the birth counted as a stable commit → t4; (m2) the tie rule dropped (sort by score alone) → t7; (m3) the glossary guard's basename check removed → t10; (m4) `ROLE_WEIGHTS` all 1.0 → t8; (m5) `PROJECTS_PARENT` hardcoded back to `/Users/marklehn/Developer/GitHub` → t2.
7. **Not changed:** every other anvil module; `GLOSSARY.md`; the daemon's config (anvil stays outside the lanes — this plan runs in the bellows lane and its anvil writes are the DEV's direct edits to the canonical checkout, committed by the Planner, P5); bellows code.

## Why this exists

The CEO ruled the glossary half is built on the definition that scored — as the smallest product that can be read and used before anything is claimed. The generator is that product; the config fix is the one thing every anvil run on this machine has patched around since March.

## What this does NOT do

- Write `GLOSSARY.md`, or any file under a `knowledge/decisions` directory — refused by the CLI, tested.
- Add anvil to the daemon's lanes, or run `run_cycle` / `run_lab`.
- Claim 'applied elsewhere' — the Post-close leaves that to the CEO's reading.
- Change the definitions: the formulas, the tie rule and the weight table are #100072's, quoted; the weight table stays labelled the Planner's.

## MUST-PRESERVE

- ⛔ **Read-only over bellows, anvil's data and governance.** The scratch DB and venv live under `mktemp -d`; the only anvil files touched are the five *What this changes* names; the only governance files written are the two research deposits; the writers of P1 are never imported.
- ⛔ **The glossary guard is code and test, not prose.** t10 fails if `GLOSSARY.md` or a `knowledge/decisions` path is accepted.
- ⛔ **The library reproduces #100072 before it emits.** QA Item 2 runs the CLI with `--reference` = the 33-member fixture and reads `P@33 R@33` under (c) beside the doc's 0.394; a difference is stated with the HEAD that moved, never silently accepted.
- ⛔ **The canonical anvil checkout is never committed by the DEV or the QA** — the Planner commits at the wrap; the mutation run uses a scratch clone (Item 5).
- ⛔ **Every `expect_fail` is a class-qualified node id where the test is a method**, and the mutation run is redirected into its deposit, never piped.
- ⛔ **The thread-262 stop** (PT v4.108 §8): a QA step that must change production code STOPS and pauses for a verdict.

## Numbers discipline — measured 2026-09-11 by the Planner (bellows `a7ba78c`, governance `efc534ef`, anvil `a1910df`, daemon pid 63362)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the hardcode and its consumers | `anvil/src/config.py:6` `ANVIL_ROOT = "/Users/marklehn/Developer/GitHub/anvil"`; `:8` `ANVIL_RUNTIME_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` (already the checkout); `:10` `ANVIL_DB_PATH = os.path.join(ANVIL_ROOT, "anvil.db")`; `:14` and `:19` the two `SCAN_TARGETS` paths under `/Users/marklehn/Developer/GitHub/`; consumers by import: `classifier.py:14,73`, `cycle.py:15,65`, `extractor.py:18,42`, `scorer.py:18,359`, `lab.py:19,27,66,109` (writes a report under `ANVIL_ROOT`), `scanner.py:21–27,37,40,128,132–133` (backs the DB up under `ANVIL_ROOT/backups`); the writers forbidden by name (#100057 P8): `run_cycle`, `run_lab`, `write_intent_audit`, `write_cycle_report`, `ingest_test_results` | `sed -n 1,25p anvil/src/config.py`; `grep -n 'ANVIL_ROOT\|ANVIL_DB_PATH\|SCAN_TARGETS' anvil/src/*.py` |
| P2 | ⛔ anvil's suite and fixtures | 15 test files, 279 `def test_` functions; no `conftest.py`; the DB fixture is per file — `test_classifier.py:18` `conn()`: `sqlite3.connect(":memory:")`, `init_db(c)`, `PRAGMA foreign_keys=ON`; `test_cycle.py:31` builds a `tmp_path` git repo with `main.py` and `utils.py`; `262 passed in 1.33s` on the mini under `datasketch==2.0.0` (#100057 P1, 2026-09-09); `requirements.txt`: `datasketch>=1.6.0`, `pytest>=7.0.0`; anvil has NO venv on the mini | `ls anvil/tests`; `grep -cF 'def test_'`; `sed -n 18,26p anvil/tests/test_classifier.py` |
| P3 | ⛔ the spec — #100072's rules, by line | `what-works-definition-2026-09-11.md`: Q2 formula `:108` (cross-file call edges whose caller imports the target's module), the tie rule `:179`, Q3 `:194–198` (`score_b`, the birth, the signature-line regex), the formula's consequence `:203–205` (412 of 581 defs birth-only), (c) `:260–265` (`score_c = score_a × weight(role)`; the weight table the Planner's; `P@33=0.394 R@33=0.394 P@20=0.450 R@20=0.273 hits@20=9`), the sensitivity `:322` (all weights 1.0 → (a); 4 top-20 positions moved); R+ = 33 members, the Q1 table `:49–103` (37 table rows including the header and R's 12); hubs under (c): `gates.py::check` 1, `run_plan` 3, `init_lifecycle_db` 4, `_assign_class` 10 | the doc at those lines |
| P4 | ⛔ the lost instrument | the doc's `## Appendix — define.py` (`:437–463`) says "Verbatim copy" and carries a 12-line header ending `# (full script: 281 lines; see T/define.py for complete listing)`; the plan's Item 8 removed `$T` after the commit — the script is gone; the Planner's (b) read of #100072 did not reach the appendix's last line (LESSONS at the wrap) | `sed -n 437,463p` the doc |
| P5 | ⛔ the lane and the deposit shape | the daemon watches `tuyere`, `bellows`, `forge_lessons` (`config.json` `watched_projects`) — anvil is NOT a lane; #100057 ran in the bellows lane and its two `anvil/scripts/` writes were the DEV's direct edits to `/Users/marklehn/Developer/anvil`, committed by the Planner (`183e85c` the walk-0 instrument, `a1910df` "extended by diagnostic #100057 … Planner-commit deposit"); `scope_check` sees the bellows worktree only, so the anvil writes are verified by the QA's `git -C anvil status --porcelain` against the declared list; anvil `main` at `a1910df`, tree clean | `python3 -c "import json; print(json.load(open('config.json'))['watched_projects'])"`; `git -C anvil log -3`; `git -C anvil status --porcelain` |
| P6 | ⛔ the build instrument and the mutation tool | `anvil/scripts/reprice_bellows_build.py`: one `main()` (`:14`) that patches `cfg.ANVIL_ROOT`, `cfg.ANVIL_DB_PATH`, `cfg.SCAN_TARGETS["bellows"]["path"]`, `sc.ANVIL_ROOT`, `sc.ANVIL_DB_PATH` in-process, removes an existing DB, `init_db`, `seed_archetype_data(conn, get_archetype("daemon"))`, then `scan_project`, `extract_project(conn, "bellows", 1)`, `classify_project`, `score_project(conn, "bellows", 1, arch)`; the temp-dir assert allows `/tmp`, `$TMPDIR`, `/private/tmp`; bellows `tools/mutation_check.py <manifest> [--repo-root] [--keep-sandbox] [--python] [--timeout]` builds its sandbox from the repo root's `HEAD:` archive — uncommitted files are absent from it, hence the scratch clone in Item 5 | the script's head; `mutation_check.py --help` |
| P7 | the scratch DB's shape (#100072, built over bellows `cc58328`) | `code_chunks` (function 744, method 56, class 221 …), `chunk_dependencies` `call` 6571 (cross_file 4076) / `import` 319, `chunk_symbol_bindings` `tests` 3188 (1207 targeted), `functional_role` over function+method: utility 506, cli_script 107, plan_dispatcher 60, gate_checker 39, notifier 27, verdict_handler 14, agent_lifecycle 14, plan_validator 11, plan_parser 7, worktree_manager 5, config_loader 4, cache_manager 4, data_model 2; `functional_roles.scoring_weights` empty on all 13 rows | #100072 P3; Item 0 rebuilds |
| P8 | in-flight; class; interpreter | queue empty at drafting (#100074 Done, daemon restarted pid 63362); writes: five anvil files (`src/config.py`, `src/what_works.py`, `scripts/reprice_bellows_build.py`, `scripts/what_works.py`, `tests/test_what_works.py` + the fixture), the bellows manifest, run file, dev-log, two QA evidence files, two governance research files → **shop-infra** (`_assign_class`, walk 0 — the `anvil/` writes, as #100057) — HOLDS at admission, the CEO releases; Python 3.9.6; the step's venv under `$T` with `datasketch==2.0.0` and `pytest` (anvil's suite runs here) | `python3 status.py`; `_assign_class` |

## Drafting Cycle

**Tier:** **T1** — T-1 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-what-works-generator-2026-09-11.md
**Walks:** walk 0 pinned (P1–P8 measured on bellows `a7ba78c`, anvil `a1910df`; clone-diff against `Done/diagnostic-100057.md` (lane and deposit shape) and `Done/executable-100074.md` (the two-commit DEV) run: FACTS, ARTEFACTS, STRUCTURE); `fold_check --save-baseline` ARMED on v0 before any fold; re-saved after every intended edit; every lens commit through `scripts/lens_commit.py`; the tool run bare, its exit read; no lens or walk number in any `--desc`; `lens_order_check` after every walk.

- Weak spots:          w1 3 folded — instruction 3 / record 0; w2 dry
- Destruction:         w1 dry; w2 dry
- Vulnerabilities:     w1 1 folded — instruction 1 / record 0; w2 dry
- Integration-record:  w1 dry; w2 dry
- ACID:                w1 dry; w2 dry
**Closing:** WARM close after walk 2 — BAR MET (T1), thread 263's build after the CEO's ruling. Two walks: 4 → 0 (walk 1 made the scratch-clone copy absolute, restored the config reload, gave the venv pin its supersede, and moved anvil's suite run into its own checkout; walk 2 dry on every lens). Every lens commit through `lens_commit.py`, the tool run bare, its exit read; the close through `close_cycle.py`. Deposit via `ready-`; HOLDS on class shop-infra, the CEO releases; the anvil files and the two research files are Planner-commit deposits at the wrap; 'applied elsewhere' is the CEO's reading of the deposited entries.

## Cycle Manifest
tier: T1
target: anvil/src/what_works.py
class: shop-infra
reads: anvil/src/config.py, anvil/src/*.py, anvil/scripts/reprice_bellows_build.py, anvil/tests/test_classifier.py, anvil/tests/test_cycle.py, anvil/requirements.txt, governance/knowledge/research/what-works-definition-2026-09-11.md, knowledge/decisions/Done/diagnostic-100057.md, knowledge/decisions/Done/executable-100074.md, config.json
writes: anvil/src/config.py, anvil/src/what_works.py, anvil/scripts/reprice_bellows_build.py, anvil/scripts/what_works.py, anvil/tests/test_what_works.py, anvil/tests/fixtures/what-works-reference-bellows-2026-09-11.txt, knowledge/mutants/what-works-generator.json, knowledge/mutants/what-works-generator.run.txt, knowledge/development/dev-log-what-works-generator-2026-09-11.md, knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md, knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt, governance/knowledge/research/what-works-entries-bellows-2026-09-11.md, governance/knowledge/research/what-works-entries-bellows-2026-09-11.tsv
mutants: knowledge/mutants/what-works-generator.json
open_forks: 1. anvil as a daemon lane (`config.json` `watched_projects` + a restart — the CEO's), so an anvil plan's writes meet the gates instead of the QA's `git status`; 2. role weights from the archetype's own thresholds instead of the Planner's table, if the entries are used; 3. a second project (invoice-pulse) through the generator — 'applied elsewhere' measured, not claimed; 4. the `commits`-arm findings of #100071 (thread 275) and the scratch-DB non-source exclusion (anvil FORWARD item 6) if the entries surface census noise
walks: 2
yields: 4, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:118
coherence: 2/2 body walks named in the register (8 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-anvil-what-works-generator.md.foldcheck.json

---

## STEP 1 — DEV (the config fix, the library, the CLI, twelve tests and a fixture; the mutation run on a scratch clone; two bellows commits; the anvil files left for the Planner)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **Read-only over bellows** (the checkout under analysis is the CANONICAL `/Users/marklehn/Developer/bellows` at `main`, read through `git -C … show` and `git -C … log`; never this worktree); never run the daemon, `run_plan`, a claim, anvil's `run_cycle`, `run_lab`, `write_intent_audit`, `write_cycle_report` or `ingest_test_results` (P1); never import `lifecycle`; ⛔ the anvil checkout `/Users/marklehn/Developer/anvil` is EDITED in place for the five files *What this changes* names and NEVER committed by this step (P5 — the Planner commits at the wrap); `T=$(mktemp -d /tmp/what-works-gen.XXXXXX)` holds the venv, the scratch DB and the scratch clone.
>
> **Scope:**
> - `knowledge/mutants/what-works-generator.json`
> - `knowledge/mutants/what-works-generator.run.txt`
> - `knowledge/development/dev-log-what-works-generator-2026-09-11.md`
>
> **Item 0 — the interpreter.** `python3 -m venv "$T/venv" && "$T/venv/bin/pip" -q install datasketch==2.0.0 pytest` (the pin is #100057's, installed here on 2026-09-09; if pip refuses it, the newest 2.x that installs is RECORDED in the dev-log and supersedes the pin) — record `$T` in the dev-log; every anvil call below uses `"$T/venv/bin/python"`. Then `cd /Users/marklehn/Developer/anvil && "$T/venv/bin/python" -m pytest -q -p no:cacheprovider` and paste the summary line beside P2's `262 passed` — the baseline before any edit; HALT on a `failed`.
> **Item 1 — re-derive P1, P2, P5 and P6 and HALT on a mechanism mismatch** (a `PROJECTS_PARENT` already in `config.py`, a `what_works` module already present, a `build_scratch_db` already factored, or a dirty anvil tree, IS one). Paste each pin's re-derived line beside it.
> **Item 2 — write the failing tests FIRST:** `anvil/tests/test_what_works.py` t1–t12 and the fixture file as *What this changes* 5 (red: `ModuleNotFoundError: src.what_works`, `AttributeError: PROJECTS_PARENT`, `ImportError: build_scratch_db`). Run the file from the anvil checkout with the venv; paste the red summary line.
> **Item 3 — the edits** as *What this changes* 1–4; then the file green (t1–t12), then anvil's FULL suite green (`N passed` with N ≥ Item 0's recorded baseline + 12 — 274 at drafting), then — nothing under bellows changed yet — no bellows run is owed here.
> **Item 4 — the mutation run on a SCRATCH CLONE, redirected into `knowledge/mutants/what-works-generator.run.txt` (a Deposit):** `A=/Users/marklehn/Developer/anvil; git clone -q "$A" "$T/anvil-mut" && for f in src/config.py src/what_works.py scripts/reprice_bellows_build.py scripts/what_works.py tests/test_what_works.py tests/fixtures/what-works-reference-bellows-2026-09-11.txt; do mkdir -p "$T/anvil-mut/$(dirname $f)" && cp "$A/$f" "$T/anvil-mut/$f"; done` (every source path ABSOLUTE under `$A` — this worktree is bellows, and a relative `anvil/…` resolves to nothing here) && git -C "$T/anvil-mut" add -A && git -C "$T/anvil-mut" -c user.email=dev@scratch -c user.name=dev commit -q -m "scratch: what-works generator for the mutation run"` (the commit is in the clone under `$T`, never in the canonical anvil); write `knowledge/mutants/what-works-generator.json` (*What this changes* 6; `target` paths relative to the clone's root); then `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/what-works-generator.json --repo-root "$T/anvil-mut" --python "$T/venv/bin/python" > knowledge/mutants/what-works-generator.run.txt 2>&1`; the last line must read `MUTATION: 5 killed, 0 survived, 0 error` — a survivor is a test to write, not a mutant to delete.
> **Item 5 — first bellows commit**, gated on the bellows FULL suite AND the pre-check with the stage flag (#100067), path-scoped: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" --expect-missing knowledge/development/dev-log-what-works-generator-2026-09-11.md && git add knowledge/mutants/what-works-generator.json knowledge/mutants/what-works-generator.run.txt && git commit -F <msg-file> -- knowledge/mutants/what-works-generator.json knowledge/mutants/what-works-generator.run.txt` — two files (the manifest and its run; the code lives in anvil, uncommitted, P5); the message tagged with the plan id and `thread 263`.
> **Item 6 — the dev-log** `knowledge/development/dev-log-what-works-generator-2026-09-11.md` under the four headings declared below; `## Pins re-derived (P1, P2, P5, P6)` opens with P3's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `` `_assign_class` 10 ``; paste through those words, then stop. Under `## Failing-first (red, then green)` the red line, the green line and anvil's suite line; under `## The anvil tree left for the Planner` the output of `git -C /Users/marklehn/Developer/anvil status --porcelain` — exactly the six declared paths (five files and the fixture), nothing else; under `## Mutation run` the run file's last line. **Second bellows commit**, gated on the bellows FULL suite and the pre-check bare (`… tools/check_deposit.py … 1 --wt "$(git rev-parse --show-toplevel)"`), path-scoped to the dev-log. Last act: `[ -n "$T" ] && [ -d "$T" ] && rm -rf "$T"` — the venv goes with it; the QA builds its own.
> **Headings:** `## Pins re-derived (P1, P2, P5, P6)`; `## Failing-first (red, then green)`; `## The anvil tree left for the Planner`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P2, P5, P6)` ← P3
>
> **Deposits:**
> - `knowledge/mutants/what-works-generator.json`
> - `knowledge/mutants/what-works-generator.run.txt`
> - `knowledge/development/dev-log-what-works-generator-2026-09-11.md`
>
> **Post-conditions:** t1–t12 green; anvil's full suite `N passed` with N ≥ 274 and no `failed` (274 = P2's 262 plus twelve; Item 0 MEASURES the baseline before any edit and RECORDS it in the dev-log, and the recorded baseline plus twelve SUPERSEDES 274 if anvil moved); the mutation run's last line `MUTATION: 5 killed, 0 survived, 0 error`; two bellows commits — the first carrying the manifest and the run, the second the dev-log; the dev-log's four headings present as full lines with P3's cell whole; `git status --porcelain` empty in this worktree after the second commit; `git -C /Users/marklehn/Developer/anvil status --porcelain` lists exactly the six declared paths and `git -C /Users/marklehn/Developer/anvil log -1 --format=%h` is still `a1910df` (nothing committed there); nothing under `$T` survives.

## STEP 2 — QA (anvil's suite; the generator run once on bellows against #100072's numbers; the entries deposited; the receipt)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter: a fresh venv `T=$(mktemp -d /tmp/what-works-qa.XXXXXX) && python3 -m venv "$T/venv" && "$T/venv/bin/pip" -q install datasketch==2.0.0 pytest` (the pin is #100057's, installed on this machine on 2026-09-09; if pip refuses it, the newest 2.x that installs is RECORDED in the receipt and supersedes the pin); read-only beyond the two bellows evidence files and the two governance research deposits; the anvil checkout is READ (its working tree carries the DEV's edits) and never committed.
>
> **Item 1 — anvil's full suite REDIRECTED not piped** (the suite file is a BELLOWS deposit, so the output path is absolute under THIS worktree): `WT=$(git rev-parse --show-toplevel); cd /Users/marklehn/Developer/anvil && "$T/venv/bin/python" -m pytest -q -p no:cacheprovider > "$WT/knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt" 2>&1; cd "$WT"` — run FROM the anvil checkout (its tests import `src.…` by the cwd, and it has no `conftest.py` to put the root on `sys.path`), the output path captured absolute before the `cd`; the summary line quoted in the receipt as `N passed` — never the word the Rule 20 block scans for.
> **Item 2 — the generator run once, on bellows, against the doc's numbers:** `cd /Users/marklehn/Developer/anvil && "$T/venv/bin/python" scripts/what_works.py --checkout /Users/marklehn/Developer/bellows --tmp "$T" --definition c --top 20 --reference tests/fixtures/what-works-reference-bellows-2026-09-11.txt --out-md /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11.md --out-tsv /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11.tsv`; paste the printed `P@33 R@33 P@20 R@20` beside #100072's `0.394 0.394 0.450 0.273` (P3) — equal, or the difference stated with `git -C /Users/marklehn/Developer/bellows rev-parse --short main` beside `cc58328` (the doc's HEAD; the checkout has moved since, so a member's rank may move by one — both figures, never a silent acceptance); paste the top-5 `file::name` beside the doc's Q6 five; then the glossary guard exercised for the record: the same command with `--out-md /Users/marklehn/Developer/eluvian-governance/GLOSSARY.md` → exit 2, the refusal line pasted, `git -C /Users/marklehn/Developer/eluvian-governance status --porcelain` showing only the two new research files.
> **Item 3 — production writes, stated exactly:** none outside the two bellows evidence files and the two governance research files; `$T` removed; `git -C /Users/marklehn/Developer/anvil status --porcelain` still exactly the six declared paths, `log -1` still `a1910df`; no lane file, no live lifecycle row, no daemon.
> **Item 4 — receipt** `knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md`: `numstat` over the DEV's two bellows commits AND `git -C /Users/marklehn/Developer/anvil diff --numstat` for the anvil tree (the six paths); a `## Verification` table with one row per Item 1–3, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`$ELUVIAN_WRAP_ROOT/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: what-works-generator-2026-09-11`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["what-works-generator-suite-2026-09-11.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the closing `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (PT v4.108 §8).
> **Item 5 — commit**, path-scoped and gated: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt && git commit -F <msg-file> -- knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt`. ⛔ A QA step that must change production code STOPS and pauses for a verdict (PT v4.108 §8).
>
> **Deposits:**
> - `knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md`
> - `knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11.md`
> - `/Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/what-works-entries-bellows-2026-09-11.tsv`
>
> **Scope:**
> - `knowledge/qa/evidence/what-works-generator-qa-receipt-2026-09-11.md`
> - `knowledge/qa/evidence/what-works-generator-suite-2026-09-11.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed` with N ≥ the DEV's recorded baseline plus twelve (274 at drafting; the dev-log's recorded figure supersedes it) and no `failed`; Item 2's four numbers pasted beside the doc's four with the HEAD comparison, the top-5 beside Q6's, the guard's refusal line and the governance status showing only the two research files; the receipt's `## Verification` table has three rows and closes with the block's own PASSED line; one QA commit carrying the two evidence files; the anvil tree unchanged since the DEV (six paths, `a1910df`); `GLOSSARY.md` untouched.
