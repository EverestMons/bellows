# Bellows — ADR: Structured Plan Metadata for Gate Consumption
**Date:** 2026-05-20 | **Tier:** Architecture | **Test Scope:** none (ADR diagnostic) | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1 ONLY. After completing Step 1, the agent STOPS and waits for CEO confirmation ("ok") before the Planner closes the plan. This is a single-step diagnostic; there is no Step 2.

Bootstrap prompt:
```
Read the plan at bellows/knowledge/decisions/executable-adr-structured-plan-metadata-2026-05-20.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT move the plan to Done.
```

---
---

## STEP 1 — BELLOWS SYSTEMS ANALYST

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/executable-adr-structured-plan-metadata-2026-05-20.md", "bellows/knowledge/decisions/in-progress-executable-adr-structured-plan-metadata-2026-05-20.md")`. **Identity:** You are the Bellows Systems Analyst. **Reads (in order):** `bellows/agents/BELLOWS_SYSTEMS_ANALYST.md`, the Bellows domain glossary if one exists at `bellows/knowledge/glossary.md` or `bellows/knowledge/research/glossary.md`, the current `bellows/gates.py` source (focus: `_extract_plan_required_deposits`, `_parse_plan_header`, `_gate_deposit_exists`, `_gate_rule_20_self_check`, `_gate_scope_check`, `strip_fenced_code_blocks`, `_extract_step_text`, `READ_CLASS_TOOLS` allowlist, `_filter_transient_paths`), the current `bellows/verdict.py` source (focus: `extract_primary_deposit`, `DEPOSITS_BLOCK_RE`, `slug_from_path`, the verdict request schema fields and `post_verdict_request`), and the current `bellows/bellows.py` source (focus: `_parse_plan_header` callers, header_says_pause, `extract_total_steps`, the auto_close model). Then read these four LESSONS.md entries verbatim from `/Users/marklehn/Developer/GitHub/LESSONS.md` for the strike history this ADR is responding to: 2026-05-18 strike 4 (deposit_exists on `_staging_*` filenames in Deposits prose), 2026-05-18 strike 5 (Rule 20 banner inside fenced block with `===` decoration), 2026-05-17 strike 3 (Rule 20 banner with shell-prompt prefix), and the 2026-05-06 BACKLOG hygiene wrap entry capturing the original `deposit_exists` false-positive class on newly-created-file paths. Also read PROJECT_STATUS.md for the 2026-05-19 Gate 2c entry to understand what `_filter_transient_paths` and the tolerant Rule 20 regex already do.
>
> **Task:** Author an Architecture Decision Record (ADR) at `bellows/knowledge/architecture/ADR-structured-plan-metadata-2026-05-20.md` proposing the introduction of YAML frontmatter on plan files as the authoritative source of structured metadata for Bellows gates, replacing prose-pattern extraction as the primary source for the four parser-fragility gate false positives (strikes 1, 3, 4, 5 — `deposit_exists` on newly-created-file paths, Rule 20 banner under shell-prompt prefix, `deposit_exists` on `_staging_*` filenames, Rule 20 banner inside fenced blocks with decoration). Scope is the parser-fragility class ONLY. Strike 2 (`no_permission_denials` cross-repo, already closed via `READ_CLASS_TOOLS` allowlist 2026-04-28) is acknowledged out of scope. The 2026-05-19 `claude -p exit code 1` auth-failure misrouting is acknowledged out of scope and named explicitly as a separate pause-reason taxonomy change that should ship as its own SA/DEV plan (the ADR must include a sentence pointing readers to that separate work — do not bury it).
>
> **ADR structure (required sections, in this order):** **(1) Status:** Proposed (this ADR). **(2) Context:** Summarize the parser-fragility pattern in two paragraphs maximum. State the five strikes by date, gate, and root cause shape; classify the four in scope as one architectural problem (gates consume human-readable prose as authoritative spec) and explicitly exclude strike 2 and the 2026-05-19 auth-failure as different problem classes. Cite the verbatim BACKLOG/LESSONS entries you read. Quantify: how many production code lines today implement prose-pattern extraction across `gates.py` and `verdict.py` (count the regex constants and the helper functions that scope-strip fenced blocks, anchor headers, etc.); how many Rule 22 overrides has the Planner issued in response to these gates over the past 5 weeks (cite specific dates from PROJECT_STATUS). **(3) Decision:** Propose YAML frontmatter at the head of every plan file, fenced by `---` lines, parsed BEFORE any markdown processing. Define the exact schema: which fields are required vs optional, what types each field holds, what each field means semantically, how each field maps to a specific gate. At minimum the schema must include: `deposits` (list of final-artifact paths, one per terminal/QA step that produces files; replaces prose `**Deposits:**` extraction); `qa` block with `self_check_required: bool` and optional `evidence_dir: path` (replaces Rule 20 banner grep). Decide and justify whether the schema also includes `pause_for_verdict`, `auto_close`, `total_steps`, `tier`, `test_scope` — these are currently in the prose header line — or whether the prose header stays as the source for those and frontmatter is scoped strictly to gate-consumed fields. Lean toward smaller schema; justify any inclusion. Provide a complete YAML example for a hypothetical 2-step DEV→QA executable plan. **(4) Layer Impact:** Layer 1 (Bellows): which functions change, which constants become obsolete, which stay as legacy fallback. Layer 2 (agents): does agent prompt construction change, do specialist files need updating, does the Rule 20 self-check block format change. Layer 3 (Planner): does PLANNER_TEMPLATE.md need a new rule for frontmatter authoring, does Rule 22 verification change, does Rule 26 `**Deposits:**` block stay or get deprecated. Be explicit about which responsibilities shift between layers, if any. **(5) Per-strike Coverage Table:** A table with one row per in-scope strike (strikes 1, 3, 4, 5). Columns: Strike #, Date, Gate, Root cause shape, How frontmatter eliminates it, Residual risk after fix. Be honest about residual risk — if a strike could still trip under frontmatter (e.g., agent forgets to author frontmatter), name it. **(6) Migration Strategy:** Propose the dual-mode policy: how long does prose extraction stay as fallback, what signal flips the cutover, what telemetry confirms readiness. Recommend a specific posture (strict cutover vs indefinite dual-mode vs phased) with rationale grounded in the BACKLOG/LESSONS evidence on agent behavior under unfamiliar conventions. This is the CEO's open question — your ADR's job is to recommend, not just enumerate. **(7) Prototype Scope (the minimum-viable first executable):** Specify the smallest implementation that proves frontmatter works end-to-end against one of the four strikes. Name: which gate to migrate first (recommend strike 4 — `deposit_exists` on `_staging_*` — because it has the clearest empirical reproduction and the smallest blast radius), which functions get touched, which tests get added, what the canary plan that exercises the new code looks like. Cost estimate: production LOC, test LOC, agent-time, calendar time. This section is what the Planner will read to author the D2 executable next. **(8) Risks & Rejected Alternatives:** Name at least two alternatives considered and rejected. Candidates: (a) "scale up `_filter_transient_paths` to a full prose-classifier blacklist," (b) "deprecate prose `**Deposits:**` blocks entirely and require frontmatter from day one," (c) "introduce frontmatter but only for QA fields, keep `**Deposits:**` as the deposit source." For each, why rejected. Name the top three risks of the proposed change: e.g., agents missing the frontmatter altogether, frontmatter drift from prose body, YAML parse errors halting otherwise-correct plans. Mitigation per risk. **(9) Open Questions:** Anything the ADR cannot resolve and needs CEO input on. Be specific.
>
> **Constraints:** This is a research/architecture deposit only — do NOT modify `gates.py`, `verdict.py`, `bellows.py`, or any other source file in this step. Do NOT propose the implementation plan in this file — the prototype scope section is a sketch for the next plan to consume, not the plan itself. Do NOT generalize beyond the parser-fragility class. Recommend YAML frontmatter specifically — the CEO has already locked the format choice; do not re-litigate it. Strikes 1, 3, 4, 5 are the four in-scope strikes; do not silently include strike 2 or the auth-failure in coverage claims. Honesty about residual risk is required in the per-strike table — if a proposal cannot eliminate a strike fully, name what residual remains. The ADR is a peer-readable architectural document, not a plan; write it as a document that the Bellows Developer and the Planner will both reference when authoring downstream work.
>
> **Deposit:** Write the ADR to `bellows/knowledge/architecture/ADR-structured-plan-metadata-2026-05-20.md` using `Filesystem:write_file` (atomic — no staging step needed for ADRs since they are inert documents not consumed by Bellows). The file must end with the standard Output Receipt block.
>
> **Feedback:** If during authoring you encounter a question that the plan prompt did not anticipate (e.g., a gate behavior the source code reveals that contradicts the LESSONS narrative, a schema field that the Rule 22 verification loop would need but the plan didn't request), append an entry to `bellows/knowledge/research/agent-prompt-feedback.md` describing what the prompt should have asked you to investigate. Keep it short — 3-4 sentences.
>
> **STOP. Do NOT proceed to any further step. Wait for CEO confirmation before the Planner closes this plan.**

---

## Deposits

- `bellows/knowledge/architecture/ADR-structured-plan-metadata-2026-05-20.md`
