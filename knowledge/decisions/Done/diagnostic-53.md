# Bellows — Ledger-Apply Regression Root Cause (Slice 2) + in_progress-Strand Recovery Gap (#48)
**Date:** 2026-06-14 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## Context (Rule 27)
The PROJECT_STATUS-activation live canary (plan 52, diagnostic) exposed a regression: the agent emitted `### Ledger Updates` with `#### Project Status` (CANARY-PROJSTATUS-134417) and `#### Prompt Feedback` (CANARY-FEEDBACK2-134417), wrote NEITHER file, yet at teardown the daemon applied NEITHER ledger — no DB row, no PROJECT_STATUS.md append, AND no `_apply_ledger_updates failed` WARN. Established facts (do not re-derive, but VERIFY): (1) teardown SUCCEEDED for plan 52 — its deposit merged to main (commit 619a6f8) and the worktree is gone; (2) so `_apply_ledger_updates` was reached but produced/logged nothing — strongly implying `parsed.get("ledger_updates")` was EMPTY at apply time despite the agent emitting the blocks (confirmed present in the plan-52 step JSON raw output); (3) `prompt_feedback` contains ONLY plan 50's row — plans 51 AND 52 both failed to land feedback; (4) the WORKING control is plan 50 (feedback canary) on the plan-49 daemon: `ledger: recorded feedback to DB + regenerated .md + committed` (logs/terminal/bellows-2026-06-14.log:126). Plan 51 (Slice 2) modified bellows.py/lifecycle.py/gates.py but NOT parser.py. SECONDARY (folded per CEO): the #48 in_progress-strand recovery gap — a daemon restart during a running step leaves the plan `lifecycle_state='in_progress'` with no recovery path (`recover_half_claimed` only reclaims `'claimed'`), so it shows as a perpetual phantom in the dashboard's `closed_at IS NULL` in-flight view (live example: plan 48). Investigation ONLY — author no fix; the fix executable(s) follow CEO review. All DB/git/log access read-only.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Bellows Systems Analyst

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/BELLOWS_SYSTEMS_ANALYST.md` first. **Post a 1-line "Read X." per file and "Drafting Section N." per section.** Ground every claim at file:line, a log line, a DB row, or a reproduced parse.
>
> **Section 1 — Reproduce the empty-ledger.** Locate the plan-52 step JSON in `logs/` (the one whose raw output contains CANARY-PROJSTATUS-134417). Determine the EXACT object the daemon passes as `parsed` to `_apply_ledger_updates` at the teardown call sites (bellows.py:635/748/779) — trace where `parsed` comes from (the step result / `parse()` output) and at what line. Then REPRODUCE: call the same parse path on plan-52's raw output and inspect `parsed.get("ledger_updates")`. Is it empty/None or populated? If empty, drill into `parser.parse` (and any `### Ledger Updates` extraction helper) to find WHY it fails to extract for plan-52's output specifically — compare against plan-50's raw output (which parsed correctly). Note: `parse()` expects the event dict, not a bare string — use the same shape the daemon uses. NAME the exact discriminating difference (output shape, section heading match, both-subsections-present interaction, an exception swallowed inside parse, a key-name mismatch between what parser writes and what `_apply_ledger_updates` reads).
>
> **Section 2 — Is it Slice-2 code or pre-existing?** parser.py was NOT changed by plan 51 — so if the parse is empty, either (a) the input shape changed, or (b) `_apply_ledger_updates` reads a different/stale `parsed` than the one parsed from this step. Inspect what plan 51 (Slice 2) actually changed in the apply path and the call sites: `git show <plan-51 commit> -- bellows.py lifecycle.py gates.py` (find the SHA from `git log --grep '\[51\]'`). Determine at file:line whether any Slice-2 edit altered which `parsed` is passed, the `ledger_updates` key, the idempotency guard, or an exception path. State CONFIRMED root cause with the evidence that settles it (a reproduced empty parse, a diff hunk, or a swallowed exception).
>
> **Section 3 — Why did plan 51's own feedback not land?** Plan 51 (executable) ran on the plan-49 (working) daemon and its QA reported emitting feedback via channel — yet no plan-51 row in `prompt_feedback`. Determine why: did the QA agent actually write `agent-prompt-feedback.md` old-style (→ coexistence skip — check plan-51's files_changed / the daemon log for the skip line), or emit the channel but get dropped? This tells us whether the regression predates plan 52 or whether plan 51 used the old protocol. Pull the evidence (log line / files_changed / step JSON).
>
> **Section 4 — Blast radius + data-loss assessment.** State precisely what is lost and for which plans under the current (broken) daemon: feedback emitted via channel → dropped silently; PROJECT_STATUS via channel → dropped; deposits → SAFE (teardown merges them, proven by plan 52). Confirm no real (non-canary) plan lost data. State whether Slice 3 (FORWARD) must stay blocked.
>
> **Section 5 — #48 in_progress-strand recovery gap.** Read `recover_half_claimed` (lifecycle.py:190) and its callsite. Confirm it only selects `lifecycle_state='claimed'`. Specify the minimal safe extension to also recover restart-stranded `in_progress` plans WITHOUT touching a legitimately-running one — the discriminators available (no worktree on disk, no live runner process, `created_at`/age guard, the plan-22 mechanisms). State how plan 48 specifically would be resolved (set abandoned + closed_at) under the proposed rule.
>
> **Section 6 — Gap Assessment + `### Verification Blocks` (Rule 39).** Gap Assessment `| Gap | Current State (file:line) | Proposed State | Change Required |` for BOTH the ledger-apply fix AND the in_progress-recovery fix. Verification Blocks `(claim, query, expected_output)` for: the empty-ledger reproduction, the confirmed root-cause line, plan-51's non-landing reason, and the recover_half_claimed state filter. Close with **CEO decision forks** (recommendations): ledger fix shape; whether ledger-fix and recovery-fix ship as one executable or two; whether plan 48 is cleaned by the recovery fix (preferred) vs a one-time write; whether the broken activation needs a re-canary after the fix.
>
> **BEFORE FINISHING — explicitly `git add` your deposit file and `git commit` it.** Use `with open()` for the deposit; no heredocs. Standard prompt feedback → emit via the `### Ledger Updates > #### Prompt Feedback` channel per current protocol (it will be dropped on this broken daemon — that is itself a data point; note it in your findings). **Deposits:**
> - `bellows/knowledge/research/ledger-apply-regression-rootcause-2026-06-14.md`
