# Bellows — Diagnostic Phase 3: Reproduce the Bugs
**Date:** 2026-04-15 | **Type:** Read-only investigation with reproduction | **Source:** Phase 2 (`bellows-dispatch-path-2026-04-15.md`) identified two independent root causes — agent early termination on move-to-Done (stranding) and parser searching wrong data source ("Unknown" status) | **Single-step:** Yes (diagnostic-prefixed) | **Run mode:** Manual bootstrap in Claude Code (Bellows remains paused)

## Context

Phase 2 produced two specific hypotheses:

**Hypothesis A (stranding):** The agent subprocess completes its substantive work but does not execute the final `shutil.move`-to-Done. The strand check correctly catches this. Most likely cause is bootstrap prompt language causing the agent to interpret "report Complete when done" as the completion condition.

**Hypothesis B (Unknown status):** The parser at `parser.py:14-19` searches for `**Status:** Complete` in the agent's conversational `result_text` (last spoken message), but the Output Receipt is written into a deposited markdown file, not into the conversational output. Pattern never matches, status defaults to "Unknown".

This Phase 3 diagnostic reproduces both bugs to confirm the hypotheses before Phase 4 writes fixes. Reproduction for Hypothesis A is via direct `claude -p` subprocess (bypassing Bellows entirely). Reproduction for Hypothesis B is via inspection of an existing log file from a recent run.

Phase 3 is read-only and reproduction-only. No source code edits, no Bellows changes, no commits other than the findings deposit.

**Bellows remains paused.** Run this plan via manual Claude Code bootstrap.

## How to Run This Plan (manual bootstrap)

In a Claude Code session, paste:

```
Read the plan at /Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-reproduce-bugs-2026-04-15.md and execute Step 1. Claim the plan first by renaming it to in-progress-, then complete Step 1 and move it to Done.
```

---
---

## STEP 1 — Investigation Agent

---

> **FIRST — claim this plan:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/diagnostic-bellows-reproduce-bugs-2026-04-15.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-reproduce-bugs-2026-04-15.md")`. Skip specialist file and glossary reads — this is a reproduction investigation. Working directory is `/Users/marklehn/Desktop/GitHub/bellows`. **READ-ONLY: do not modify any source file, configuration file, or production database. Do not start, stop, or interact with the running Bellows process.** This investigation has six numbered questions (Q1–Q6). Answer each in the deposited findings file under its own section header. **Q1 — Inspect a recent log file to confirm Hypothesis B.** Find the most recent log file in `logs/` (likely organized by session or plan). Run `ls -lt logs/ | head -20` first to inventory. Then read the most recent JSON log file and report: (a) what fields exist in the JSON, (b) the literal value of the `result` field for the final step of a recent plan, (c) confirm whether `**Status:** Complete` appears in the `result` text. Quote the relevant sections verbatim. The expectation per Phase 2 hypothesis is that `**Status:** Complete` will be ABSENT from `result_text`, confirming the parser searches the wrong data source. Also report the value of `stop_reason` if present — Phase 4 may use this for the parser fix. **Q2 — Build a minimal reproduction plan for Hypothesis A.** Create a throwaway test plan at `/tmp/bellows-repro-test-2026-04-15.md` with the following content (use `with open(...) as f: f.write(...)` Python pattern, NOT a heredoc): a single-step diagnostic that asks the agent to (a) write a marker file to `/tmp/bellows-repro-marker.txt` containing the string "REPRO_OK", (b) move itself to a Done subdirectory at `/tmp/bellows-repro-done/`. Use the standard plan structure (header with `## STEP 1 — Investigation Agent`, claim the plan first via shutil.move to in-progress prefix, then do the work, then move to Done). The plan content should be a verbatim copy of a typical diagnostic plan so the agent's behavior matches production. Make sure `/tmp/bellows-repro-done/` exists before the test. **Q3 — Reproduce Hypothesis A via direct claude -p invocation.** Build the bootstrap prompt that Bellows would normally send for a single-step diagnostic. Per the Phase 2 finding, this is approximately: `"Read the plan at /tmp/bellows-repro-test-2026-04-15.md and execute Step 1. Execute it fully — this is a single-step investigation. Deposit your findings and report Complete when done."`. Run it via `claude -p --output-format json "<bootstrap_prompt>" 2>&1` and capture the full JSON output to `/tmp/bellows-repro-output.json`. **Do not use Bellows for this — direct claude -p only.** Report: (a) whether the marker file exists at `/tmp/bellows-repro-marker.txt` after the subprocess returns, (b) whether the test plan was moved to `/tmp/bellows-repro-done/`, (c) the value of `stop_reason` in the output JSON, (d) the final `result` text from the agent. The expectation per Phase 2 hypothesis is that the agent will write the marker but NOT execute the move-to-Done, reproducing the strand pattern outside of Bellows entirely. **Q4 — Try a more emphatic bootstrap to test prompt sensitivity.** Run a SECOND reproduction attempt using a more emphatic bootstrap that explicitly enumerates the move-to-Done as a critical step. Prompt: `"Read the plan at /tmp/bellows-repro-test-2026-04-15.md and execute Step 1 to completion. CRITICAL: the very last operation in the plan is to move the plan file to Done/. You MUST execute this move-to-Done as your final action. Do NOT report complete until the move is executed. Verify the file is in Done/ before reporting complete."`. Reset the test environment first: delete `/tmp/bellows-repro-marker.txt`, restore the test plan to its original location (out of Done/), and re-run claude -p with the new prompt. Report the same four observations as Q3 (marker file? plan moved? stop_reason? result text?). This question tests whether the strand can be eliminated via prompt language alone, or whether a Bellows-side fix is required regardless. **Q5 — Confirm both bugs are independent.** Based on Q1 (parser bug) and Q3 (strand reproduction): are these two bugs causally linked or genuinely independent? Q1 should show the parser bug exists regardless of whether the agent strands. Q3 should show the strand exists regardless of whether the parser correctly reads status. Confirm or refute Phase 2's claim that they are independent. **Q6 — Phase 4 fix recommendations.** Based on Q1-Q5, write specific recommendations for Phase 4. For the parser bug, name the specific fix (per Phase 2 Q7: "change plan template to instruct agents to include `**Status:** Complete` in their final spoken message" OR "have run_plan() read the deposited findings file after the subprocess returns" OR "infer status from stop_reason"). For the strand bug, name the specific fix (per Phase 2 Q7: "Bellows-side fallback that moves the plan to Done after verifying step completion" OR "change bootstrap prompt language" OR both). If Q4 showed the emphatic prompt eliminated the strand, that's strong evidence the prompt-only fix is sufficient. If Q4 still showed the strand, the Bellows-side fallback is required. **Cleanup at end:** Delete `/tmp/bellows-repro-test-2026-04-15.md`, `/tmp/bellows-repro-marker.txt`, `/tmp/bellows-repro-output.json`, and `/tmp/bellows-repro-done/`. **Step A — Findings deposit.** Write findings to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/bellows-reproduce-bugs-2026-04-15.md`. Use sections Q1–Q6. Include a standard Output Receipt with Status=Complete, Files Deposited, Files Created or Modified (Code) = `[]` (read-only), Decisions Made = the Q6 fix recommendations, Flags for CEO = the recommended Phase 4 fix scope (small/medium/large) for each bug. **Step B — Feedback append.** Append a feedback entry to `/Users/marklehn/Desktop/GitHub/bellows/knowledge/research/agent-prompt-feedback.md` per the standard prompt feedback protocol. **Step C — Commit.** `cd /Users/marklehn/Desktop/GitHub/bellows && git add knowledge/research/bellows-reproduce-bugs-2026-04-15.md knowledge/research/agent-prompt-feedback.md && git commit -m "docs: phase 3 diagnostic — bellows reproduce bugs"`. **Step D — Move-to-Done as the absolute last operation:** `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/in-progress-diagnostic-bellows-reproduce-bugs-2026-04-15.md", "/Users/marklehn/Desktop/GitHub/bellows/knowledge/decisions/Done/diagnostic-bellows-reproduce-bugs-2026-04-15.md")`. **Steps A → B → C → D in strict order. The move-to-Done must be the last thing done.**
