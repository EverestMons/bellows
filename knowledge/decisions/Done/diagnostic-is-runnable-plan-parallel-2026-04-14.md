# Bellows — Diagnostic: is_runnable_plan Parallel Prefix Bug
**Date:** 2026-04-14 | **Tier:** Small | **Test Scope:** n/a (read-only) | **Execution:** Step 1 (DEV)
**Priority:** 1

## How to Run This Plan

This plan is picked up and executed by Bellows automatically.

---
---

## STEP 1 — DEV (Investigation)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-is-runnable-plan-parallel-2026-04-14.md", "bellows/knowledge/decisions/in-progress-diagnostic-is-runnable-plan-parallel-2026-04-14.md")`. Skip specialist file and glossary reads — pure code-tracing task. Working directory is `/Users/marklehn/Desktop/GitHub/`. Investigate the parallel-prefix bug documented in the Phase 2B feedback log (`bellows/knowledge/research/agent-prompt-feedback.md`, "Phase 2B Step 1 (DEV)" entry). Do NOT fix anything — investigate and report only. **Read the following and produce findings:** (1) `bellows/bellows.py` — locate the `is_runnable_plan` function definition. Quote its exact current implementation verbatim. Report the file line range. (2) Test `is_runnable_plan` against these 6 filenames and report the return value for each: `"executable-foo-2026-04-14.md"`, `"diagnostic-bar-2026-04-14.md"`, `"parallel-1-executable-foo-2026-04-14.md"`, `"parallel-2-diagnostic-bar-2026-04-14.md"`, `"in-progress-executable-foo-2026-04-14.md"`, `"foo.md"`. Use `python3 -c "import sys; sys.path.insert(0, 'bellows'); from bellows import is_runnable_plan; [print(repr(f), '->', is_runnable_plan(f)) for f in ['executable-foo-2026-04-14.md','diagnostic-bar-2026-04-14.md','parallel-1-executable-foo-2026-04-14.md','parallel-2-diagnostic-bar-2026-04-14.md','in-progress-executable-foo-2026-04-14.md','foo.md']]"`. Report the literal output. (3) Grep `bellows/bellows.py` for every call site of `is_runnable_plan` using `grep -n "is_runnable_plan" bellows/bellows.py`. Report the line numbers and the 3 lines of context around each. (4) Locate `extract_parallel_group`, `collect_group`, and `handle_parallel_group` in `bellows/bellows.py`. For each, quote the first 5 lines and report whether they call `is_runnable_plan` internally. Specifically check: does `collect_group` call `is_runnable_plan` on the full `parallel-N-executable-*.md` filename, on the suffix after the group prefix, or neither? (5) Locate `PlanHandler._handle` in `bellows/bellows.py`. Quote the full method. Specifically identify the line where `is_runnable_plan(filename)` is called and report what `filename` refers to at that point (full name with parallel prefix, or stripped). (6) Check `bellows/tests/test_bellows.py` for any existing test of `is_runnable_plan` with parallel-prefixed input. Report whether such a test exists, and if so, what it asserts. (7) Based on findings (1)-(6), state whether the bug is real (parallel plans silently dropped) AND report the exact call chain that causes the drop: which function calls `is_runnable_plan` first, with what argument, and where control flow terminates when it returns False. Also state whether `collect_group` has a SECOND bug: if `_handle` returns early before `collect_group` is called, the group never gets collected at all — verify this. **Deposit findings to** `bellows/knowledge/research/is-runnable-plan-parallel-diagnostic-2026-04-14.md` using Python file I/O: `with open("bellows/knowledge/research/is-runnable-plan-parallel-diagnostic-2026-04-14.md", "w") as f: f.write(content)`. Structure the deposit with sections matching items (1)-(7) above. Commit: `docs: diagnostic — is_runnable_plan parallel prefix bug`. Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.
