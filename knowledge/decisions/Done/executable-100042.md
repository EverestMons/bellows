# bellows — executable: `cycle_check` runs the battery on its NORMAL path and a tool FAIL is a verdict input — thread 117 Ruling 2 in shape (a) (thread 189) — and the emitter stops writing `fold_check=N/A` for four different states (thread 190) — clone 3

**Date:** 2026-09-08 | **Project:** bellows | **Tier:** Small | **Dispatch Mode:** bellows | **cycle_tier:** T1 | **Test Scope:** targeted (`tests/test_cycle_check_battery.py`, a new sibling, + one ruled rewrite in `tests/test_cycle_check_manifest_provenance.py`) + full suite at QA | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** always | **known_failures:** 0 | **Priority:** 1 | **Discharges:** thread 117, thread 189, thread 190

**auto_close:** false

**Post-close:** ⚠️ **RESTART OWED** — `depositor.py:27` imports `cycle_check` in-process, so the running daemon carries the old `run_check` — and `bellows.py` imports `substrate_check`, which this plan also edits — until it is restarted (the thread-187 class, same day). Not a step; the CEO's act at close (f16).

**Depends on:** tuyere thread 189 (the ruling: shape (a), habituation answered by making the battery a verdict input) and `governance/knowledge/research/in-cycle-battery-2026-09-04.md` (the pricing). Clone origin: clone 2, `executable-bellows-cycle-check-battery-2.md` (BAR MET at walk 2; deposited as **#100041** and HALTED at STEP 1 on 2026-09-08 — its DEV commit `f95f604` reverted on `main` as `4219c99`, thread 194; its execution record is folded below as f54–f57), which cloned `executable-bellows-cycle-check-battery.md` (#100040, halted; thread 193; f49–f52), whose own origin is `Done/executable-100037.md` → `Done/executable-100033.md`, the plan whose MUST-PRESERVE clause this one overturns by ruling.

## What this changes

⛔ **One battery function, two callers, one new verdict input.**

1. **`run_battery(plan_path)`** — runs `plan_lint`, `fold_check` (against the baseline resolved the way `substrate_check` resolves it: manifest `fold_baseline:` first, beside the plan second) and `propagation_check`, and returns each tool's value in ONE vocabulary: `plan_lint=N_FAIL`; `fold_check=PASS | DRIFT | VACUOUS | NO_BASELINE | N/A`; `propagation_check=CLEAN | DIVERGENT:N | NOT_RUN | N/A`.
2. **`run_check`'s CONTINUE/BAR_MET exits run the battery** and print one `BATTERY:` line before the verdict. **A `plan_lint` FAIL or a `fold_check` DRIFT withholds the bar:** BAR_MET → CONTINUE with one WARN in the exact shape Item 4 pins — `WARN: BAR_MET downgraded to CONTINUE — battery: <tool>=<value> — <that tool's remedy>` (the thread-172 precedent: the downgrade SAYS WHY, and says what to do). `VACUOUS`, `NO_BASELINE` and `DIVERGENT:N` never downgrade. ESCALATE exits are untouched.
3. **`emit_manifest` calls the same function.** Keys and order unchanged (thread 117 Ruling 1). The only value that changes is the one that was wrong: `fold_check` rc≠0 was one bucket (`N/A`) and is now `DRIFT` / `VACUOUS` / `NO_BASELINE`. `PASS` stays `PASS`.

## Why this exists

Thread 117 Ruling 2: *"The battery should run in-cycle, not only at freeze … today the three subprocess calls live INSIDE `emit_manifest`, so a normal `cycle_check` invocation after each walk runs NONE of them. The battery is terminal, and 75% of fold-introduced findings occur between walks."* The cadence rule (DC §2, the auto-advance paragraph) already says *"run `cycle_check` and act on its verdict"* — so the verdict is the delivery surface that is actually read, and the ruling's habituation warning (*"what stops it becoming another ignored line?"*) is answered by putting the battery INSIDE the verdict rather than beside it. Thread 189 ruled shape (a); shape (b) (thread 81, the daemon stage) stays open and follows once this has run on live cycles.

Thread 190 is the defect found pinning this: the emitter's `PASS if rc == 0 else N/A` folds fold damage (DRIFT), the EXPECTED state at a judged stop (VACUOUS — the last walk folded nothing, so the artifact equals the re-saved baseline) and "no baseline at all" into one value. Plan 100039 closed with `fold_check=N/A` beside an armed baseline that says VACUOUS. 69 of the Done manifests that carry the key say `N/A`.

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | the ruling | thread 189: shape (a); FAIL is a verdict input; R1 untouched (four keys); 81 deferred not rejected | `threads show 189` |
| P2 | ⛔ the gap, stated precisely | `scripts/cycle_check.py` has FIVE `subprocess.run` sites: `:73` (`_find_git_root`) and `:349` (`git log` on the register) are git reads that run only when a register ref is declared (assert #2), `:909` `:922` `:935` are the three tools — ALL inside `emit_manifest`. `run_check` (`:424-629`) spawns no tool | `grep -n "subprocess.run" scripts/cycle_check.py` |
| P3 | the price, measured not recalled | `in-cycle-battery-2026-09-04.md` Q6: plan_lint ~52 ms, propagation_check + cycle_check ~65 ms, fold_check ~105 ms (C0) → ~240 ms per run, ~11 s per cycle; thread 117's own figure for the three cheap tools: ~168 ms. **Measured on THIS draft at walk 2 (median of 3): plan_lint 58 ms + fold_check 88 ms + propagation_check 161 ms** (f31) — propagation's cost scales with the plan's number count (this draft carries a pin table; the note's ~33 ms was per revision on a smaller plan) (f40) | read Q6 of the note; re-time at DEV |
| P4 | ⛔ `fold_check` exit codes | `scripts/fold_check.py:181-245`: **0** CLEAN, **1** DRIFT, **2** for VACUOUS (baseline sha == artifact sha), no baseline, or unreadable baseline. The emitter (`:929`) maps rc≠0 → `N/A` | read `main` in fold_check.py |
| P4a | ⛔ VACUOUS is the EXPECTED state at a judged stop | measured 2026-09-07 on `drafts/executable-tuyere-threads-retype-2.md` (plan 100039, closed BAR_MET at walk 2, baseline armed beside it): `fold_check` → `FOLD-CHECK VACUOUS … rc=2`; its manifest says `fold_check=N/A` | run fold_check on that draft |
| P4b | the conflation's footprint | Done manifests carrying the key: `N/A` 69 · `PASS` 31 · hand-typed `CLEAN` 2 / `CLEAN_` 7 / `NO_BASELINE` 1 (values the emitter never writes — the line is hand-edited despite DC:253) | `grep -rhoE 'fold_check=[A-Z_/]+' Done/*.md \| sort \| uniq -c` |
| P5 | `propagation_check` values | emitter `:941-950`: rc 0 `CLEAN`, rc 1 `DIVERGENT:N`, rc 2 `NOT_RUN`; thread 117's ceiling: ~25% of incomplete-propagation damage, 12/12 and 7/8 divergences classified false-positive on two plans → INFORMATIONAL, never a verdict input | read `:941-950`; thread 117 |
| P6 | ⛔ the stdout contract | `main` prints warnings first, verdict LAST (`:1019-1020`, "stdout contract (P8): verdict is always the LAST line"); two in-process callers read the tuple: `depositor.py:553` (`_rerun_validation`) and `tools/cycle_log_signal_census.py:304` | grep `run_check(` outside tests |
| P7 | the downgrade precedent | `:592-607`: BAR_MET → CONTINUE when the manifest key set is short, with `WARN: BAR_MET downgraded to CONTINUE — Cycle Manifest validation is missing required key(s): …` — thread 172's rule that a downgrade names its cause | read `:592-607` |
| P8 | ⛔ the clause this plan OVERTURNS, by ruling | `Done/executable-100033.md` MUST-PRESERVE: *"No new subprocess on the normal path. `cycle_check` runs constantly"* — pinned by `tests/test_cycle_check_manifest_provenance.py:241` `test_no_subprocess_spawned`, which counts launches against a baseline. Thread 189 overturns it for CONTINUE/BAR_MET exits; ESCALATE exits keep it | read `:239-270` of that test file |
| P9 | ⛔ baseline resolution to ADOPT, not re-derive | `scripts/substrate_check.py:102-112`: manifest `fold_baseline:` resolved by `cycle_check._resolve_register_ref(ref, plan_path)`, else beside the plan (`.<name>.foldcheck.json`), else missing. A deposited plan's baseline lives in `drafts/` and is reachable ONLY through the manifest field (v2.28) | read substrate_check leg 3 |
| P9a | a deposit preserves bytes | `Done/executable-100039.md` sha256 `dc977da1f011db58…` equals its draft's (`drafts/executable-tuyere-threads-retype-2.md`), so the deposited copy's `fold_check` through `fold_baseline:` reads what the draft's last run read (f38) | `shasum -a 256` both files |
| P10 | ⛔ blast radius, measured over the whole corpus | population: every `.md` under the shop's `knowledge/decisions/` trees (Done, drafts and the live lanes) = **2080 files**, run 2026-09-07 in 224 s. Normal-path verdicts: `ESCALATE:unparseable` 1717 · `claimed-close-unmet` 175 · `CONTINUE` 147 · **`BAR_MET` 24** · other ESCALATE 16. **plan_lint arm: 0 of the 24 BAR_MET plans carry a FAIL** (881 of 2080 do overall — none at the bar). **propagation arm, had it been one: 22 of 24 non-zero** (21 `DIVERGENT`, 1 `NOT_RUN`) — the measurement that keeps it informational. **DRIFT arm: 16 of 24 have no baseline beside them (Done plans) → `NO_BASELINE`; of the 8 drafts with one: 6 DRIFT, 1 CLEAN, 1 VACUOUS** — all six DRIFTs are shipped or superseded plans' drafts left on disk, and the one live-lane BAR_MET file is `halted-executable-100029.md` (no baseline). **Live blast radius: zero on both arms.** | the walk-0 probe (its script lived in a session scratchpad and is NOT durable — f48): for every `.md` under each project's `knowledge/decisions/` tree (and governance's `governance/knowledge/decisions/`), skipping `.bellows-worktrees`, run `cycle_check.py` (last stdout line = verdict), `plan_lint.py` (count lines starting `FAIL`) and `propagation_check.py` (exit code + `DIVERGENCES: N`); tabulate BAR_MET ∧ FAIL>0. ~4 min |
| P11 | tier precedent | `100033`, `100037` and four earlier `cycle_check` plans (`100022`, `100023`, `100025`, `100029`, per 100033's P10) all `cycle_tier: T1`; T-6 checked against the trigger AS QUOTED — no doctrine, template, gate or contract is written | grep the tier line in those plans |
| P12 | this plan's own class | run the assigner over the declared write set at execution and confirm `shop-infra`; it HOLDS by design and the CEO releases. Thread 190 is `cycle_check`'s one open defect; this plan's `Discharges:` names it, which is the deferral thread 110's gate would need if it existed (f21) | `_parse_plan` then `_assign_class` |
| P13 | in-flight | re-derive at execution | `sqlite3 lifecycle.db …` |

## What this does NOT do

- ⛔ **It does not edit doctrine.** The cadence sentence already binds the author to the verdict; a note naming the battery there is the CEO's optional direct amendment afterwards, on the v2.27/2.28 precedent. That is why T-6 does not fire (P11).
- ⛔ **It does not put `lens_order_check`, `substrate_check`, `walk_register_lint` or `mutation_check` in the in-cycle battery.** Substrate is checked at auto-close where it belongs; the register lint's coverage question is thread 135's; mutation_check is the 3.6 s tool Ruling 1 keeps out of every universal path.
- ⛔ **It does not change the emitter's KEY SET** (Ruling 1 — a versioned interface) nor the forms of `plan_lint=` and `propagation_check=`, nor `fold_check=PASS`. Only the rc≠0 bucket sharpens (thread 190).
- ⛔ **It does not add a flag to skip the battery.** Ruling 119: no optional gates. The cost is ~240–310 ms per run (P3) and the depositor pays it too.
- ⛔ **It does not ESCALATE on a FAIL.** The bar is withheld and the cause named; the author folds and re-runs. CONTINUE stays CONTINUE.
- **It does not compute thread 148's lens-4 signal**, though it lives on the same path — a separate plan (open fork), so this one stays one-conditional-class.
- **It does not build the C2 count-delta channel** — its false-positive rate is unmeasured (the 2026-09-04 note, Q4).

## MUST-PRESERVE

⛔ **Invariants that must still be TRUE afterwards.**

- ⛔ **The verdict is the LAST line of stdout** (P6). The `BATTERY:` line and any downgrade WARN print before it, through the same `warnings` channel the BASIS line uses. Proven by test 1.
- ⛔ **Every ESCALATE exit is byte-identical and spawns no tool** — the 100033 clause survives there. Proven by test 7.
- ⛔ **A clean plan at the bar still reaches BAR_MET** — `VACUOUS`, `NO_BASELINE`, `DIVERGENT:N` and `0_FAIL` all leave the verdict alone. Proven by tests 4, 5, 6.
- ⛔ **The emitter's `validation:` line keeps four keys in the same order and `PASS` for a clean fold check.** Proven by test 8 against a captured pre-change emission.
- ⛔ **One implementation.** `emit_manifest` and `run_check` obtain every tool value from `run_battery`; the value the emitter writes equals the value the normal path printed on the same plan. Proven by test 9 and the two-implementations mutant.
- ⛔ **The tools are launched by ABSOLUTE script path (`Path(__file__).resolve().parent / <tool>`) with `sys.executable`, never `-m`.** The depositor calls `run_check` in-process from the daemon, whose cwd is bellows; thread 187 (2026-09-07) is what a `-m` launch does there. Lifted verbatim from the emitter (`:909-937`), where it is already right (f11).
- ⛔ **The downgrade SAYS WHY** — the WARN names the tool and the value (P7). Proven by tests 2 and 3.
- ⛔ **The battery runs on every call — `warnings=None` changes where the lines go, never the verdict.** The depositor's call passes no list and must see the same verdict the author saw (f49). Proven by test 15 and its mutant.
- ⛔ **`--basis` and `--emit-manifest` argument handling unchanged.** The existing suites (`test_cycle_check.py` — 64 tests at walk 0, re-derived at execution — and the provenance sibling) pass with their ASSERTIONS byte-identical; the only edits are the ruled rewrite (P8) and the battery stubs / lint-clean fixtures in the five tests f50 names, and both are named in the commit.
- ⛔ **The depositor's re-validation is untouched** (`depositor.py:553`). It receives the battery-aware verdict for free; a stored `cycle_check=BAR_MET` with a fresh CONTINUE is the `validation_mismatch` hold it already has, and it fires where `plan_lint` would hold anyway or where `fold_check` reads DRIFT on the deposited copy — which it cannot when the author re-saved after the final edit, because a deposit preserves bytes (P9a) (f38). The census (`tools/cycle_log_signal_census.py:310`) filters `startswith("BASIS:")`, so a `BATTERY:` line cannot be misread as a BASIS line — verified at walk 1 (f20). ⚠️ The depositor passes no `warnings` list, so a CONTINUE caused by DRIFT (an author who skipped the final re-save) holds as `validation_mismatch` WITHOUT its cause — carrying the WARN into the hold reason is a depositor change, and the depositor has four open defects (110's rule): open fork 6 (f26).

## Drafting Cycle

**Tier:** T1 — T-3 fires (`cycle_check` runs on every machine that drafts). **T-6 does NOT fire**, checked against the trigger as QUOTED (*"Edits doctrine, the template, gates, or specialist contracts"*) and P11's six precedents. **T-8 does NOT fire**: the emitter already spawns these three tools on this plan's own machinery; moving the call is not novel. ⚠️ *"If unsure whether a trigger fires, it fires"* — not unsure: `writes:` names no doctrine file.
**Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-cycle-check-battery-3-2026-09-08.md
**Walks:** walk 0 pinned — clone 3: every origin pin re-measured after the revert `4219c99` (`scripts/cycle_check.py` and `scripts/substrate_check.py` sha256 equal to the walk-0 values of clones 1 and 2), plus #100041's execution record as four findings (f54–f57); `fold_check --save-baseline` ARMED on v0 before any fold (v2.27 §2.0 item 6), path recorded as `fold_baseline:` (v2.28). The origin records stay in `walk-register-cycle-check-battery-2026-09-07.md` (48 findings, 40 lens commits) and `walk-register-cycle-check-battery-2-2026-09-08.md` (5 findings, 10 lens commits). **Walks 1–2 complete.** Yields **4 → 1 → 0**; instruction-class **4 → 1 → 0**.
- Weak spots:          w1 1 folded — instruction 1 / record 0; w2 dry
- Destruction:         w1 dry; w2 dry
- Vulnerabilities:     w1 dry; w2 dry
- Integration-record:  w1 dry; w2 dry
- ACID:                w1 dry; w2 dry
**Walk 0 — 4 findings (instruction 4 / record 0); 0 fold-introduced** — #100041's execution record (f54 the mutation run as a Deposit, f55 selectors must collect, f56 the beside-only test, f57 Item 7 as four headings with P10 pasted).
**Walk 1 — 1 finding (instruction 1 / record 0); 1 of 1 fold-introduced.** f58: a post-condition that contradicted f50, carried through clone 2 unnoticed. Four lenses dry with bases stated. Instruction-class 1: the bar is NOT met; walk 2 owed.
**Walk 2 — DRY, all five lenses, bases stated in the register. Instruction 0: the walk MEETS THE BAR (§2) — a judged stop, with the classification shown.**

**Closing:** **BAR MET at walk 2** — instruction 0 / record 0 on a full five-lens pass; closing-record re-read run, dry; judged stop. FROZEN pending deposit authority (the CEO's act) — deposit through `ready-`, then the class release. Baseline re-saved after the emission and after this line.

## Cycle Manifest
tier: T1
target: scripts/cycle_check.py
class: shop-infra
reads: scripts/cycle_check.py, scripts/fold_check.py, scripts/propagation_check.py, scripts/plan_lint.py, scripts/substrate_check.py, depositor.py, tests/test_cycle_check_manifest_provenance.py, knowledge/decisions/Done/executable-100037.md, knowledge/decisions/Done/executable-100033.md, /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/in-cycle-battery-2026-09-04.md
writes: scripts/cycle_check.py, scripts/substrate_check.py, tests/test_cycle_check.py, tests/test_cycle_check_battery.py, tests/test_cycle_check_manifest_provenance.py, knowledge/mutants/cycle-check-battery.json, knowledge/mutants/cycle-check-battery.run.txt, knowledge/development/dev-log-cycle-check-battery-2026-09-07.md
open_forks: 1. thread 81 — the daemon stage (shape b), after this has run on live cycles and the downgrade is measured (folds produced vs overrides); 2. thread 148 — the lens-4 signal on this same path; 3. whether the depositor should compare all four validation values now that the vocabulary is honest (thread-worthy, not folded here); 4. a DC cadence note naming the battery — the CEO's direct amendment if wanted; 5. `tools/cycle_log_signal_census.py:304` calls `run_check` over the whole corpus and grows by ~2080 × 240 ms per run — a census-only bypass would be an optional gate (Ruling 119), so it stays slow until a thread decides; 6. the depositor's hold reason does not carry the battery WARN (f26) — thread-worthy, not folded here
walks: 2
yields: 1, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=N/A, propagation_check=DIVERGENT:49
coherence: 2/2 body walks named in the register (5 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-cycle-check-battery-3.md.foldcheck.json

---

## STEP 1 — DEV (one battery function, one new verdict input, one ruled test rewrite)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f scripts/cycle_check.py && echo TREE_OK` — HALT unless TREE_OK. Every command below is relative to the bellows root (LESSONS 2026-08-08, cd-first; its third form was measured on 2026-09-07 in this very repo's daemon).
>
> ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** — a dispatch worktree carries no `.venv` (measured on six 2026-09-04 step logs: every DEV step used the absolute path; `bellows.py` creates no venv or symlink). Prove `VENV_OK` with `-c 'import pytest'` before Item 1. The tools then run under that interpreter via `sys.executable`, from the WORKTREE's `scripts/` (`Path(__file__)`), which is the code under test (f13).
>
> **Scope:**
> - `scripts/cycle_check.py`
> - `scripts/substrate_check.py`
> - `tests/test_cycle_check.py` (the five named tests only — f50)
> - `tests/test_cycle_check_battery.py`
> - `tests/test_cycle_check_manifest_provenance.py`
> - `knowledge/mutants/cycle-check-battery.json`
> - `knowledge/mutants/cycle-check-battery.run.txt`
> - `knowledge/development/dev-log-cycle-check-battery-2026-09-07.md`
>
> **Item 1 — re-derive P1–P13 and HALT on mismatch.** ⛔ Re-derive every line number by grep — `cycle_check.py` moved on 2026-09-06 and 2026-09-07 and every pin into it has moved at least once. ⛔ Re-run P4a: `fold_check` on the 100039 draft must still say VACUOUS rc=2; if that draft is gone, substitute any Done plan's draft with a baseline beside it, name it, and say why. ⛔ Capture BEFORE any edit, into the dev-log: (a) `--emit-manifest` on a fixture plan — test 8 diffs against it; (b) the normal-path stdout of `Done/executable-579.md` (`ESCALATE:claimed-close-unmet` in the walk-0 probe) and `Done/qa-149.md` (`ESCALATE:unparseable`) — QA Item 4 diffs against these (f23).
>
> **Item 2 — write the failing tests FIRST**, in `tests/test_cycle_check_battery.py` (mock `subprocess.run` exactly as `test_cycle_check.py:127` does, dispatching on the launched script's BASENAME — `plan_lint.py`, `fold_check.py`, `propagation_check.py` — and passing every other argv, the git reads at `:73`/`:349`, to the real `subprocess.run`; register-less fixtures reach BAR_MET with assert #2 N/A, so most tests need no git at all (f8); never run the real tools in a unit test):
> 1. ⛔ **a CONTINUE plan prints one `BATTERY: plan_lint=… fold_check=… propagation_check=…` line BEFORE the verdict, and the verdict is the last line** — P6's contract; parametrized over a fixture reaching EACH of the four exits (f6)
> 2. ⛔ **BAR_MET plan + `plan_lint` FAIL>0 → CONTINUE, with a WARN that names `plan_lint=N_FAIL`** — the regression this plan exists for
> 3. ⛔ **BAR_MET plan + `fold_check` DRIFT (rc 1) → CONTINUE, WARN names `fold_check=DRIFT`**
> 4. ⛔ **BAR_MET + `fold_check` VACUOUS (rc 2, stdout carrying the real `FOLD-CHECK VACUOUS` line) → BAR_MET unaffected** — P4a, the dry-close state; a downgrade here would block every honest judged stop
> 5. **BAR_MET + no baseline resolvable → `NO_BASELINE`, BAR_MET unaffected** — baseline presence is `substrate_check`'s to enforce; firing here would double-report and train the reader to ignore both
> 6. **BAR_MET + `propagation_check` DIVERGENT:N → BAR_MET unaffected** — P5, informational
> 7. ⛔ **every ESCALATE exit spawns ZERO tool launches and its stdout is byte-identical to the pre-change capture** — the 100033 clause where it survives; every arm reachable by a register-less fixture, listed by grep at execution (`unparseable`, `plateau`, `yield-rising`, `restructuring-fold`, `claimed-close-unmet`, `assert-fail:1`, `assert-fail:3`); the two git-dependent arms (`assert-fail:2`, `uncommitted-walk`) use `test_cycle_check.py:108`'s `_find_git_root` monkeypatch (f9)
> 8. ⛔ **`--emit-manifest` on the fixture: the `validation:` line has the same four keys in the same order as the pre-change capture; `fold_check=PASS` when rc 0; `DRIFT`/`VACUOUS`/`NO_BASELINE` for the three rc≠0 states that used to read `N/A`** — thread 190
> 9. ⛔ **one implementation** — on the same plan, the value `emit_manifest` writes for each tool equals the value `run_check`'s `BATTERY:` line printed
> 10. ⛔ **baseline resolution order** — a plan whose baseline is reachable ONLY through the manifest's `fold_baseline:` field (the deposited-copy case, P9) resolves it; a plan with both resolves the manifest's first; a declared-but-unresolvable field → `NO_BASELINE` even when a beside-file exists (f15)
> 11. ⛔ **the ruled rewrite of `test_no_subprocess_spawned`** (P8): count launches by the launched script's BASENAME so the git reads cannot perturb it: on a CONTINUE/BAR_MET exit exactly the tools the battery has inputs for — `plan_lint.py`, `propagation_check.py`, and `fold_check.py` only when a baseline resolved (3 with, 2 without: a missing baseline is `NO_BASELINE` without a launch, the emitter's existing behaviour) — and on an ESCALATE exit zero (f28, f32). The docstring cites thread 189 as the authority that overturned 100033's clause
> 12. ⛔ **`--emit-manifest` launches each tool exactly once** — the `battery=` hand-off (f7); on a fixture WITH a baseline, so all three launch and "once" is checkable for each (f44)
> 13. ⛔ **one resolver** — `substrate_status` and `run_battery` name the same baseline path on a manifest-only fixture (f17)
> 14. ⛔ **a CONTINUE plan with a `plan_lint` FAIL prints its `BATTERY:` line and NO downgrade WARN** — the downgrade exists only at the bar; a WARN saying "downgraded" on a verdict that was never BAR_MET would be false (f41)
> 15. ⛔ **the verdict does not depend on the call shape** — `run_check(path)` with no `warnings` list returns the same verdict as `run_check(path, warnings=[])` and as the CLI, on a fixture carrying one `plan_lint` FAIL (all `CONTINUE`) (f49)
> 16. ⛔ **the beside-the-plan fallback discriminates** — a plan with NO `fold_baseline:` field and a baseline beside it resolves that file; a resolver that ignores the beside-file must fail this test (M7 SURVIVED on #100041 because test 10 covered manifest-only and both, never beside-only) (f56)
>
> ⛔ **Five EXISTING tests in `tests/test_cycle_check.py` reach the bar on lint-FAILing fixtures and the battery would downgrade them** — `test_cli_exit_codes`, `test_emit_manifest_well_formed`, `test_assert2_invalid_register_warns_verdict_unchanged`, `test_assert2_valid_register_no_warn`, `test_contract_last_stdout_line_is_verdict` (measured on #100040's DEV: exactly these five fail against the battery). **The remedy is on the TEST side and every assertion string stays byte-identical:** an in-process test monkeypatches `cycle_check.run_battery` to return a clean dict (`plan_lint=0_FAIL`, `fold_check=NO_BASELINE`, `propagation_check=CLEAN`) — it is not testing the battery; a test that runs the CLI in a subprocess (where a monkeypatch cannot reach) makes its OWN tmp_path fixture lint-clean — run `plan_lint` on the fixture, read the FAIL it names, add exactly that (a copied `Done/` plan would couple a unit test to the corpus and break the day that plan moves) — and reads the LAST stdout line, which is the P6 contract (f53). State per test which form was used. ⛔ **A loosened assertion anywhere in that file is a HALT** — #100040's DEV changed "verdict must be unchanged" to accept `CONTINUE` (Event 932), which is the watering-down Lens 2 exists to catch (f50).
>
> Run them and record the FAILURE output before implementing.
>
> **Item 3 — implement `run_battery(plan_path) -> dict`.** Lift the three calls out of `emit_manifest` verbatim (P2) into one function; resolve the baseline by ADOPTING `substrate_check`'s order (P9). ⛔ **The resolver is ONE function, `resolve_fold_baseline(plan_path, manifest=None) -> (Path | None, declared: bool)`, and it lives in `cycle_check`** — `substrate_check` imports `cycle_check` (`:29`), so the reverse import would be circular; `substrate_check`'s leg 3 (`:106-114`) is rewritten to call it and its 8 tests pass unchanged. Test 13: on a manifest-only fixture, `substrate_status` and the battery resolve the same path (f17); launch `fold_check.py <plan> --baseline <resolved path>` — the CLI takes `--baseline` (`fold_check.py:162`) and the default is beside-the-plan, which is wrong for a deposited copy (f29); the resolver returns ABSOLUTE paths (`git_root / ref`, `gov_root / ref` — both roots absolute; read at walk 4, f42), so the launch is cwd-independent — and only when a baseline resolved (f32); map `fold_check` rc 0 → `PASS`, rc 1 → `DRIFT`, rc 2 with a line starting `FOLD-CHECK VACUOUS` (the machine-readable prefix, same family as `FOLD-CHECK CLEAN`/`DRIFT`) → `VACUOUS`, rc 2 otherwise → `NO_BASELINE`, exception → `N/A` (P4). ⛔ A DECLARED `fold_baseline:` that does not resolve is `NO_BASELINE` — no silent fallback to a beside-file, because `substrate_check` names that state a defect and the two must agree (f15). `plan_lint` and `propagation_check` mappings unchanged. Each launch keeps the emitter's `timeout=30` (a hung tool costs the daemon at most 90 s per deposit — pre-existing in the emitter, f30).
>
> **Item 4 — wire it into `run_check`.** At every CONTINUE/BAR_MET exit — FOUR at walk 1, re-derived by grep for `return "CONTINUE"`, `return "BAR_MET"` and `return verdict` inside `run_check`: `:498` (BAR_MET proven through the register), `:513` and `:517` (CONTINUE, walk-less bodies) and the computed `:629` — run the battery, append the `BATTERY:` line to `warnings` — exact shape, pinned (f37): `BATTERY: plan_lint=<value> fold_check=<value> propagation_check=<value>`, space-separated, the same value spellings the manifest uses, so a register or dev-log can grep it; test 1 asserts the shape — and — only when the verdict is BAR_MET — if `plan_lint` FAIL>0 or `fold_check == DRIFT`, set CONTINUE and append one WARN PER downgrading tool — two lines when both fire (f43) — each in its exact shape (f24): `WARN: BAR_MET downgraded to CONTINUE — battery: plan_lint=N_FAIL — fix the FAIL(s) plan_lint names before the next walk` and `WARN: BAR_MET downgraded to CONTINUE — battery: fold_check=DRIFT — if the change is INTENDED, re-save the baseline and say so in the fold's record` (fold_check's own remedy, verbatim); tests 2 and 3 assert these substrings, and test 3 also covers the both-fire case: two WARN lines, plan_lint's first (f43). ⛔ **Do this at one exit point** — a helper the three exits call — so a fourth exit added later cannot forget it. ⛔ **ESCALATE arms untouched** (test 7).
> ⛔ **The battery runs on EVERY call.** `warnings=None` means only that the `BATTERY:`/WARN lines have nowhere to go — the VERDICT is battery-aware regardless; `depositor.py:553` passes no list. Test 15: on a fixture with one `plan_lint` FAIL, `run_check(path)`, `run_check(path, warnings=[])` and the CLI all return `CONTINUE`. (#100040's DEV made it conditional — `if warnings is None and battery is None: return v, 0`, Event 619 — and the depositor then saw `BAR_MET` where the CLI saw `CONTINUE`, measured before the halt) (f49).
> ⚠️ **A WORKFLOW CONSEQUENCE that must be stated, or it will read as a bug (f4; the precedent is 100037's own Item 3, the emit-then-confirm consequence).** Emitting the manifest at the bar EDITS the artifact after the last baseline save, so the first `cycle_check` after emitting reads `fold_check=DRIFT` and withholds the bar. That is correct — an intended change is acknowledged by re-saving the baseline, which is `fold_check`'s own instruction (*"If a change is INTENDED, re-save the baseline and say so in the fold's record"*) — so the DRIFT WARN carries that remedy verbatim. ⛔ And it is EVERY intended edit after the last save, not only the emission — the status lines written after the dry walk and the spliced manifest each move `plan_lint`'s signals (measured on THIS plan at walk 1's close: status lines + splice → DRIFT). The rule is fold_check's own, stated once: re-save after every intended edit and say so in the register; the bar is reached when the LAST `cycle_check` reads `fold_check=VACUOUS` (or `PASS`) (f25). Measured at walk 0: 6 of the 8 BAR_MET drafts with a baseline read DRIFT today for exactly this reason.
>
> **Item 5 — `emit_manifest` calls `run_battery`** and deletes its private copies of the three calls. ⛔ Keys, order and the `PASS` spelling stay byte-identical (test 8); ⛔ the `verdict, _ = run_check(plan_path)` call inside the emitter now runs the battery once ALREADY — do not run it twice: `run_check` gains a keyword `battery=None`: the emitter computes `b = run_battery(plan_path)` ONCE and passes `battery=b`; `None` means run it. ⛔ The tuple return shape `(verdict, code)` is untouched — `depositor.py:553` and the census read it (f7). Test 12: `--emit-manifest` launches each tool exactly once.
>
> **Item 6 — `knowledge/mutants/cycle-check-battery.json`**, one mutant per branch: drop the plan_lint downgrade arm → test 2 fails; apply the downgrade on CONTINUE too → test 14 fails; treat VACUOUS as DRIFT → test 4 fails; match a different token for VACUOUS → test 4 fails; skip the manifest `fold_baseline:` resolution → test 10 fails; run the battery on an ESCALATE arm → test 7 fails; map rc≠0 back to `N/A` in the emitter → test 8 fails; give the emitter its own battery copy → test 9 fails; emit one combined WARN when both tools fire → test 3's both-fire assertion fails (f45); skip the battery when `warnings is None` → test 15 fails (f49); ignore the beside-file in the resolver → test 16 fails (f56). ⛔ **The manifest is EXECUTABLE by `tools/mutation_check.py`:** each mutant carries exactly the runner's keys — `name`, `why`, `anchor`, `replacement`, `expect_fail` (commentary keys `_`-prefixed; the runner refuses any other key) — and `anchor` is a count-1 substring of the target. **Run it, REDIRECTED into a file that is a DEPOSIT:** `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/cycle-check-battery.json > knowledge/mutants/cycle-check-battery.run.txt 2>&1; echo "exit=$?"` — the file's last `MUTATION:` line must read `N killed, 0 survived, 0 error`; quote it in the dev-log. The run file is listed under Deposits and Scope, so `deposit_exists` FAILS the step when the run is skipped (#100041 skipped it and no gate noticed) (f54). ⛔ **Every `expect_fail` node id must COLLECT before the manifest is committed:** `pytest --collect-only -q <node id>` exits 0 and lists exactly one item, for each mutant — node ids are taken from the test file AS WRITTEN (`grep -oE '^def test_\w+' tests/test_cycle_check_battery.py`), never from this plan's test numbering (#100041 named 7 of 10 selectors after tests that did not exist; the runner's baseline exited 4) (f55). (#100040's DEV committed a description table with keys `id/description/killed_by/test_number`; the runner refused all nine and nothing was mutation-tested) (f51). ⚠️ **A survivor is a missing test, stated as Critical**; ⛔ **0 ERROR required — and a manifest the runner refuses IS an error count**.
>
> **Item 7 — dev-log**, with these FOUR literal headings, each filled — QA Item 7 greps for them (f57); #100041's dev-log carried none of the four:
> - `## P10 — blast radius (verbatim)` — paste EXACTLY this, unchanged: population: every `.md` under the shop's `knowledge/decisions/` trees (Done, drafts and the live lanes) = **2080 files**, run 2026-09-07 in 224 s. Normal-path verdicts: `ESCALATE:unparseable` 1717 · `claimed-close-unmet` 175 · `CONTINUE` 147 · **`BAR_MET` 24** · other ESCALATE 16. **plan_lint arm: 0 of the 24 BAR_MET plans carry a FAIL** (881 of 2080 do overall — none at the bar). **propagation arm, had it been one: 22 of 24 non-zero** (21 `DIVERGENT`, 1 `NOT_RUN`) — the measurement that keeps it informational. **DRIFT arm: 16 of 24 have no baseline beside them (Done plans) → `NO_BASELINE`; of the 8 drafts with one: 6 DRIFT, 1 CLEAN, 1 VACUOUS** — all six DRIFTs are shipped or superseded plans' drafts left on disk, and the one live-lane BAR_MET file is `halted-executable-100029.md` (no baseline). **Live blast radius: zero on both arms.**
> - `## Cost — before/after (median of 3)` — three timed runs of `cycle_check` on one BAR_MET plan before the change and three after, the medians and the delta; the ~240 ms prediction from P3 is superseded by the run.
> - `## P8 — the overturned clause` — that 100033's "no new subprocess on the normal path" was overturned by ruling 189 for CONTINUE/BAR_MET exits and kept on ESCALATE exits, not tidied away.
> - `## Mutation run` — the `MUTATION:` line quoted from `knowledge/mutants/cycle-check-battery.run.txt` (f54).
>
> **Item 8 — commit** (message tagged with the plan id, `-F` from a file — it contains backticks); record `numstat` — exactly 8 files.
>
> **Deposits:**
> - `knowledge/development/dev-log-cycle-check-battery-2026-09-07.md`
> - `knowledge/mutants/cycle-check-battery.run.txt` (f54)
>
> **Post-conditions:** all sixteen tests pass; `knowledge/mutants/cycle-check-battery.run.txt` ends `MUTATION: N killed, 0 survived, 0 error`; `test_cycle_check.py` changed ONLY in the five tests f50 names (stubs / lint-clean fixtures), every asserted verdict unchanged (count re-derived at execution, not hardcoded); the provenance sibling passes with exactly one test rewritten by ruling; the 100039 draft (P4a) returns BAR_MET with `fold_check=VACUOUS` on its `BATTERY:` line and the emitter prints `fold_check=VACUOUS` for it (stdout only — nothing written to that draft); a fixture with a `plan_lint` FAIL shows BAR_MET → CONTINUE with the WARN, shown as a before/after pair in one run; the runner's own mutants all killed, 0 error.

## STEP 2 — QA (full suite + the verdict input shown to discriminate)

> ⛔ **Re-establish the root first** — `cd "$(git rev-parse --show-toplevel)" && test -f scripts/cycle_check.py && echo TREE_OK` — HALT unless TREE_OK. STEP 2 is a separate dispatch and inherits no shell.
>
> ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE** — a dispatch worktree carries no `.venv` (measured on six 2026-09-04 step logs: every DEV step used the absolute path; `bellows.py` creates no venv or symlink). Prove `VENV_OK` with `-c 'import pytest'` before Item 1 of this step. The tools then run under that interpreter via `sys.executable`, from the WORKTREE's `scripts/` (`Path(__file__)`), which is the code under test (f13).
>
> **Item 1 — full suite, REDIRECTED not piped.** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/cycle-check-battery-suite-2026-09-07.txt 2>&1; echo "exit=$?"` → `exit=0`. The pre-plan count is a PREDICTION the run supersedes; the HALT condition is a non-zero FAILURE count.
> **Item 2 — the input discriminates, on real tools, no mocks.** The fixture is a COPY of `Done/executable-100037.md` (BAR_MET in the walk-0 probe, `plan_lint` 0 FAIL, no baseline beside the copy → `NO_BASELINE`) in a scratch dir under `knowledge/qa/evidence/` (never under any `knowledge/decisions/`): as copied → `BAR_MET` with `BATTERY: plan_lint=0_FAIL fold_check=NO_BASELINE …`; then delete its `PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.` line — measured at walk 6 on a scratch copy: `plan_lint` exit 1, `FAIL: (c) QA banner pair — missing: PASSED line` → `CONTINUE` with the WARN naming `plan_lint=1_FAIL` (f22, f47). Paste both outputs whole.
> **Item 3 — the dry-close state is not punished, and the emit→re-save sequence works live.** (a) `cycle_check` on the 100039 draft (P4a): verdict `BAR_MET`, `BATTERY:` shows `fold_check=VACUOUS`. (b) On a COPY of a BAR_MET fixture under `knowledge/qa/evidence/` (never the real draft): save a baseline, then make an edit that moves a `plan_lint` SIGNAL — set the manifest's `coherence:` value to exactly `<declare>` — check (f) WARNs on a whole-value placeholder, and `fold_check` then reads DRIFT with `APPEARED: plan_lint: (f) WARN: Cycle Manifest stanza contains <declare> placeholder(s)` (measured at walk 6 on a scratch copy of 100037; a `<declare>` merely PREFIXED to `open_forks:` reads CLEAN, as does a prose line — f39 named the wrong edit) (f46); run `cycle_check` → `CONTINUE` with the DRIFT WARN carrying the remedy; re-save; run again → `BAR_MET` with `fold_check=VACUOUS`. Paste all three outputs. If the 100039 draft is gone, name the substitute and why.
> **Item 4 — ESCALATE byte-identity.** Run `cycle_check` on `Done/executable-579.md` and `Done/qa-149.md` and show each stdout equals its pre-change capture from STEP 1 Item 1(b), byte for byte (f23).
> **Item 5 — cost, measured.** Median of three timed runs of `cycle_check` on one BAR_MET plan; state the delta against STEP 1's before-figure. The ~240 ms prediction is superseded by the run.
> **Item 6 — production writes, stated exactly.** None outside the two evidence files: no draft edited, no manifest emitted into a file, no baseline saved. State that the daemon restart is owed at close (f16).
> **Item 7 — receipt** `knowledge/qa/evidence/cycle-check-battery-qa-evidence-2026-09-07.md`: numstat vs the DEV commit — eight files; the dev-log's four Item-7 headings present (grep each, quote the result) (f57); toplevel; `git reflog -n 4` → 0 amends; Items 1–6 each on their own stated line; then the Rule 20 block inside a "Verification"-headed section.
> **Item 8 — commit**, pathspec-scoped: the two evidence files, exactly two.
>
> ⚠️ **Gate note:** pytest summary named at Item 1 — the gate parses it; no benign override pre-declared.
>
> **Deposits:**
> - `knowledge/qa/evidence/cycle-check-battery-suite-2026-09-07.txt`
> - `knowledge/qa/evidence/cycle-check-battery-qa-evidence-2026-09-07.md`
>
> **Scope:**
> - `knowledge/qa/evidence/cycle-check-battery-suite-2026-09-07.txt`
> - `knowledge/qa/evidence/cycle-check-battery-qa-evidence-2026-09-07.md`

Rule 20 banner (byte-exact, inside the QA receipt's VERIFICATION section):

```
============================================================
Rule 20 — QA Self-Check Results
============================================================
PASSED — SELF-CHECK PASSED — all evidence files present, no hedging keywords found.
```
