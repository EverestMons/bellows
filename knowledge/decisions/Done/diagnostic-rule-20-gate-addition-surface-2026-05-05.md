# Bellows — Rule 20 Gate Addition Surface Diagnostic
**Date:** 2026-05-05 | **Tier:** Small | **Test Scope:** none (read-only investigation) | **Execution:** Step 1 (DEV)

## How to Run This Plan

Paste the bootstrap prompt into Claude Code. The agent reads the full plan file and executes Step 1. After completing Step 1, the agent STOPS and waits for CEO confirmation. The Planner then performs Rule 22 verification on the deposited findings file and authorizes housekeeping.

**Bootstrap prompt:**
```
Read the plan at bellows/knowledge/decisions/diagnostic-rule-20-gate-addition-surface-2026-05-05.md. Execute Step 1 ONLY. After completing Step 1, STOP and wait for my confirmation. Do NOT proceed to any further step or move the plan to Done.
```

---
---

## STEP 1 — Bellows Developer

---

> **FIRST — before doing anything else, claim this plan:** `import shutil; shutil.move("bellows/knowledge/decisions/diagnostic-rule-20-gate-addition-surface-2026-05-05.md", "bellows/knowledge/decisions/in-progress-diagnostic-rule-20-gate-addition-surface-2026-05-05.md")`. **Skip glossary read — this is a code-tracing investigation. Read your specialist file at `bellows/agents/BELLOWS_DEVELOPER.md` first** for architectural context on the gate system. You are mapping the implementation surface for adding a new blocking gate to `gates.py` that detects Rule 20 self-check fabrication and omission in QA-deposited reports. The new gate's job (do NOT design or implement it in this diagnostic — only map the surface): when the step is a QA step (gate 6 `is_qa_step` returns True), read the deposited QA report file and verify the literal banner string `Rule 20 — QA Self-Check Results` is present AND a line starting with `PASSED — SELF-CHECK PASSED` follows it. Missing or banner-without-PASSED → gate fails (blocking). **Static analysis only — do NOT propose the new gate's code, do NOT edit gates.py, do NOT write tests.** **Investigation tasks (execute in order):** **(1) Identify the gate function pattern.** Read `bellows/gates.py` in full. For each of the 8 existing gates, identify and quote: (a) the function signature (name, parameters, return type), (b) the docstring or one-line purpose, (c) the failure shape — what is appended to `gate_result["failures"]` on failure (dict shape, expected keys), (d) whether it is blocking or informational. Build a table: gate # | function name | parameters | blocking? | failure shape. **(2) Identify the gate dispatcher.** Find the code that calls each gate in sequence and accumulates results. Identify: (a) the function name and file/line range that orchestrates gate evaluation, (b) the order gates are called, (c) how the dispatcher accesses inputs the new gate will need (the deposited QA report file path, the step's `is_qa` status), (d) where in the dispatch flow a new gate would be registered (mid-sequence vs. appended). Quote relevant code blocks verbatim. **(3) Identify how gate 5 `deposit_exists` resolves the deposit file path.** The new gate needs to read the QA report file. Gate 5 already locates this file. Trace exactly how it does so: which `**Deposits:**` block parser does it use, what path resolution strategy (absolute, project-relative, parent-relative), and which deposit it picks when multiple are declared. The new gate should reuse this resolution — it must NOT duplicate path-finding logic. Identify the exact function or helper that returns the resolved deposit path and confirm it is callable from a new gate. **(4) Identify how gate 6 `is_qa_step` is computed.** The new gate runs conditionally on `is_qa=True`. Identify: (a) where `is_qa` is computed in the dispatcher, (b) the exact heuristic (case sensitivity, header pattern), (c) whether the result is passed to subsequent gates or recomputed. The new gate must consume the existing `is_qa` value, not recompute it. **(5) Identify the test pattern.** Read `bellows/tests/test_gates.py` in full. Identify the pattern for testing a single gate: how is a fake `gate_result` set up, how are file inputs mocked or staged, how are pass/fail assertions structured. Build a list: existing test functions for each gate (function name, what scenario it tests). The new gate's tests will follow this pattern. **(6) Identify the verdict request emission for blocking gate failures.** When a blocking gate fails, the verdict request file gets a `## Gate Failures` section listing each failure. Trace: (a) which function emits the gate failure section, (b) the format used (the dict shape from each gate's `failures` append), (c) whether the new gate's failure dict needs any fields beyond `gate` and `evidence`. **(7) Identify any helpers for reading file content.** The new gate reads a QA report file. Are there existing helpers in `gates.py` or `bellows/` modules for safe file reads (with error handling for missing files, encoding issues)? If yes, the new gate should reuse them. Quote any relevant helpers. **(8) Synthesize findings into six answered questions.** Question A: what is the canonical gate function signature, return contract, and failure-dict shape that a new gate must follow? Question B: where in the dispatcher is a new gate registered, and what inputs does it have access to? Question C: how does the new gate access the deposited QA report file path without duplicating gate 5's resolution logic? Question D: how does the new gate consume the existing `is_qa` boolean from gate 6? Question E: what is the test pattern, and roughly how many test cases will the new gate require (positive case, banner missing, banner without PASSED, deposit file missing, non-QA step skip)? Question F: are there any architectural constraints or risks the DEV prompt should know about (e.g., circular imports, gate ordering dependencies, cache invalidation between gate runs)? **Deposit findings to `bellows/knowledge/research/rule-20-gate-addition-surface-findings-2026-05-05.md`** using the canonical Python file write pattern: `with open("bellows/knowledge/research/rule-20-gate-addition-surface-findings-2026-05-05.md", "w") as f: f.write(content)` where content is a triple-quoted string defined before the open call. The file structure: header (date, agent, plan reference), Investigation Section 1 (gate function pattern table with verbatim code), Investigation Section 2 (dispatcher mapping), Investigation Section 3 (deposit path resolution), Investigation Section 4 (`is_qa` computation), Investigation Section 5 (test pattern + existing test list), Investigation Section 6 (verdict failure emission), Investigation Section 7 (file-read helpers), Findings (the six answered questions A–F), Output Receipt (canonical format). **Standard prompt feedback protocol → `bellows/knowledge/research/agent-prompt-feedback.md`.** **Final commit:** `git add bellows/knowledge/research/rule-20-gate-addition-surface-findings-2026-05-05.md bellows/knowledge/research/agent-prompt-feedback.md && git --no-pager log -1 && git commit -m "diag: rule-20 gate addition surface findings"`. **STOP. Do NOT proceed beyond Step 1. Do NOT move the plan to Done. Wait for CEO confirmation.**
>
> **Deposits:**
> - `bellows/knowledge/research/rule-20-gate-addition-surface-findings-2026-05-05.md`
> - `bellows/knowledge/research/agent-prompt-feedback.md`
