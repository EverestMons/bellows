# bellows — THE AGENT'S ROOT IS COMPARED BY INODE: `_agent_owns_root` asks whether the launchd agent serves this checkout with `os.path.samefile`, as PLANNER_TEMPLATE Rule 97 requires, and reads a directory that no longer exists as not this checkout's — a case-variant spelling of the agent's own directory now matches (thread 324)

**Date:** 2026-09-14 | **Project:** bellows | **Tier:** Small (one helper in `bellows.py`, one test re-pinned and one added in its test file, one mutation manifest, one dev-log) | **Dispatch Mode:** bellows | **Test Scope:** full-suite (Rule 21 — `bellows.py` is imported by most of the suite; `pytest tests/ -q`) | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step | **known_failures:** 0 | **Discharges:** thread 324 | **cycle_tier:** T1

**auto_close:** false

**Post-close:** no daemon restart — the helper's one caller is the dashboard's `_spawn_child` (`dashboard.py:451`), and the dashboard imports `bellows` when it starts, so the change is live at each machine's next dashboard start after the merge; no daemon code path calls the helper. Two bound follow-ups, both the CEO's. (1) The doctrine, the CEO's apply act after the merge: the GLOSSARY's `own-root kickstart rule` entry (`:989–990`), which says the agent's working directory is "compared by `realpath` with `None` never matching", gains an addendum beneath it, the file being append-only: "Since bellows #<id> (thread 324): the agent's working directory and the dashboard's root are compared by inode (`os.path.samefile`, PLANNER_TEMPLATE Rule 97), so two spellings of one directory match; a directory that does not exist, and an empty answer from `launchctl`, never match, and `None` still never does." (2) T-3's check on the Air, where the Planner only reads — after the Air pulls, `cd ~/Developer/GitHub/bellows && .venv/bin/python -m pytest tests/test_daemon_agent.py -q` under its 3.9.6 venv, t13 `SKIPPED` there only if the Air's filesystem tells case apart; the Planner pastes the summary line into `tuyere.threads done 324 --commit bellows:<the DEV's first commit>`.

**Depends on:** thread 324 (filed 2026-09-13 13:26 from Gate 1 W=30 reader C's hazard note 2, opened by the CEO): `bellows.py:206` — `:207` at `65f713c` — decides whether the running agent owns this checkout by comparing two `os.path.realpath` strings although both paths exist, against Rule 97; its failure runs the safe way (a case-variant spelling reads as another checkout's, so the dashboard starts its own child instead of asking launchd). Clone origin by kind: #100087 (`knowledge/decisions/Done/executable-100087.md`, Done 2026-09-11), which wrote this helper and t9, the `patch.object` shape of t5 and t8–t12, the two-commit DEV with the manifest riding the first, the mutation run redirected, and the QA's read of the agent's run counter before and after the suite. Thread 342, filed at this plan's walk 0, carries the startup close's two string comparisons, which gate a removal and must move with its liveness match.

**Tier computed (§1):** **T1** — T-3 fires: the dashboard, the CEO's TUI, runs this helper on both machines — the mini's 3.12.14 venv and the Air's 3.9.6, the Air's filesystem's handling of case unmeasured; t13 skips where case is told apart. T-5 does not fire: the helper gates a kickstart without `-k` (#100087) and removes nothing; the comparisons that gate the startup close's removal are thread 342's. T-6 does not fire: the one doctrine text this change moves — the GLOSSARY's own-root kickstart rule — stays a bound Post-close apply act under §1's split rule; T-8 does not (the helper and its test are #100087's, one comparison changed); T-1, T-2, T-4 and T-7 do not.

## CEO Context

When the dashboard starts and finds no daemon running, it asks whether the launchd agent serves this checkout: if it does, the dashboard asks launchd to start the agent; if not, it starts a daemon of its own for this folder (#100087). The question compares two folder names as text. On a Mac's default disk, `~/Developer/bellows` and `~/Developer/Bellows` are one folder but two texts, so a differently spelled path reads as someone else's checkout and the dashboard runs the daemon as its own child rather than through the agent — the safe direction, since the agent is never kicked, but not the one the agent exists for. Thread 324 asks for the comparison the shop's Rule 97 requires: the same folder, as the disk itself decides. This plan changes that comparison, reads a folder that no longer exists — a stale agent setting after a move — as not ours instead of failing, and an empty answer as not ours too, and adds a test that proves the differently spelled folder now matches. Nothing restarts; each dashboard picks it up when it next starts. The class is shop-infra, so it holds for your release; after the merge a GLOSSARY addendum, quoted in Post-close, is your apply act, and on the Air, one test run is yours.

## What this changes

1. **`bellows.py` — `_agent_owns_root(root, label="com.eluvian.bellows-daemon")`** (:204–207, four lines, all by #100087, `98acd47`): `r = _agent_root(label)` as today; `if r is None: return False` FIRST — #100087's order, since `os.path.realpath("")` is the working directory, and now because `os.path.samefile(None, …)` raises `TypeError`; then `try: return os.path.samefile(r, str(root))`, `except OSError: return False` — a directory that no longer exists, or an empty answer (`os.stat("")` raises `FileNotFoundError`, where `os.path.realpath("")` was the working directory — the dashboard's own root), reads as not this checkout's, never an exception into `_spawn_child`; the docstring states the inode comparison and the False cases. `_agent_root` and every other line of the file unchanged.
2. **`tests/test_daemon_agent.py`** — **t9** re-pinned in place (:210–221): renamed `test_t9_agent_owns_root_compares_by_inode(tmp_path, monkeypatch)`, its docstring stating the inode comparison, its three assertions kept (the same directory True; the same basename under another parent False; no agent False) and two added — with `_agent_root` returning `str(tmp_path / "moved")`, a directory that does not exist, `_agent_owns_root(agent_dir)` is False; and, the working directory made `agent_dir` (`monkeypatch.chdir`) and `_agent_root` returning `""`, `_agent_owns_root(agent_dir)` is False — the case the string form answered yes, since `os.path.realpath("")` is the working directory; **t13** appended after t12, the file's last test: `test_t13_agent_owns_root_case_variant_spelling` — `tmp_path / "Bellows"` made and its spelling `tmp_path / "BELLOWS"`, `pytest.skip` when that spelling names nothing (a case-sensitive filesystem), an assertion that the two `os.path.realpath` strings differ, so the test cannot pass by string, and, with `_agent_root` returning the variant, `_agent_owns_root(agent_dir)` is True. t1–t8 and t10–t12 unchanged — t10's `_agent_root` names a directory that does not exist, `tmp_path / "elsewhere"`, and stays green through the `OSError` branch.
3. **`knowledge/mutants/agent-root-inode.json`** — `{"target": "bellows.py", "mutants": [ … ]}`, each mutant's keys `name`, `why`, `anchor`, `replacement` and `expect_fail`, three: (m1) `samefile-to-realpath-string` — the comparison thread 324 removes → t13; (m2) `missing-directory-raises` — `except OSError` narrowed to `except TypeError` → t9, its moved-directory case; (m3) `none-test-removed` → t9, its `None` case (`samefile(None, …)` raises, uncaught). An anchor that matches its target other than once is the run's `ERROR` (`tools/mutation_check.py:266–270`).
4. **The dev-log** `knowledge/development/dev-log-agent-root-inode-2026-09-14.md`.
5. **Not changed:** `_agent_root` (it never raises), `_kickstart`, `dashboard.py`, t1–t8 and t10–t12; `knowledge/mutants/dashboard-agent-root.json` — #100087's record, whose m5 anchors on the line this plan replaces and names t9's old node id, so it no longer runs as written, and nothing re-runs it; the startup close's comparisons (thread 342).

## Why this exists

Thread 324: Gate 1's reader C found `_agent_owns_root` comparing `os.path.realpath` strings for two paths that both exist, where Rule 97 requires `os.path.samefile`. Python's `realpath` keeps the spelling it is given, while the disk answers in its own case — measured on the mini: in a directory `Wt` entered as `WT`, `os.path.realpath` printed `WT`, and `os.getcwd()`, `lsof` and `git rev-parse --show-toplevel` printed `Wt` (P3). A launchd agent set up with one spelling of the checkout and a dashboard started with another therefore disagree about a folder they share, and the prototype's t13 shows it on today's code: red. This plan makes the helper ask the disk.

## What this does NOT do

- It does not change the startup close's identity guard or its liveness match (thread 342): fixed alone, the guard would admit a case-variant spelling while `lsof`'s paths, in the disk's case, no longer matched it — a close could then remove a worktree under a live runner — so the two belong to one plan, at T2.
- It does not change what the dashboard does with the answer, or which checkout the agent serves.
- It does not run `launchctl` in any test of this plan's file (MUST-PRESERVE, #100087's).
- It does not write the GLOSSARY addendum (the CEO's apply act, quoted in Post-close).

## MUST-PRESERVE

- ⛔ **No test in `tests/test_daemon_agent.py` runs `launchctl`.** Each patches `bellows._agent_root`, `_agent_loaded`, `_kickstart` or `bellows.subprocess.run`, and t7's installer runs a stub; the QA reads the agent's run counter before and after the suite, equal. *(Carried from #100087.)*
- ⛔ **`_agent_owns_root` never raises:** no agent → False, a directory that does not exist or an empty answer → False; the `None` test FIRST.
- ⛔ **`_agent_root` is unchanged** — it still never raises.
- ⛔ **Every existing test passes unedited but t9,** which this plan re-pins in place; t13 is appended after t12; t13 skips only where the variant spelling names nothing.
- ⛔ **Every `expect_fail` is a node id of this plan's tests** — t9's new id, t13's, or the test a survivor fix adds (Item 5) — and the mutation run is redirected into its deposit, never piped.
- ⛔ **Test code runs under pytest inside `tests/` or not at all** (bellows `CLAUDE.md`, *Test code*; thread 313).
- ⛔ **The thread-262 stop:** a QA step that must change production code STOPS and requests a verdict.

## Numbers discipline — measured 2026-09-14 by the Planner (bellows `65f713c`; governance `589b14e3`)

| # | pin | value | how to re-derive |
|---|---|---|---|
| P1 | ⛔ the helper | `bellows.py` :204 `def _agent_owns_root(root, label="com.eluvian.bellows-daemon"):`, :205 its one-line docstring, :206 `r = _agent_root(label)`, :207 `return r is not None and os.path.realpath(r) == os.path.realpath(str(root))` — four whole lines, all by #100087 (`98acd47`); `_agent_root` :180–202 (`launchctl print gui/<uid>/<label>`, the `working directory = ` line, else the root three `dirname`s above a `…/.venv/bin/python` program, else `None`; never raises); the one caller `dashboard.py:451`, in `_spawn_child` — a kickstart with `replace=False` when the helper says yes, else a child `bellows.py` under the dashboard's root; no other non-test caller | `sed -n '180,207p' bellows.py`; `git grep -n -F _agent_owns_root -- '*.py' ':!tests'` |
| P2 | ⛔ the rule | PLANNER_TEMPLATE Rule 97 (`:1399`): a guard deciding whether two paths are the same file tests with `os.path.samefile()`, never by comparing `realpath()` strings, and keeps the `realpath` comparison only for an output path that does not exist yet; the GLOSSARY's `own-root kickstart rule` (`:989–990`) describes this helper's comparison as `realpath`, `None` never matching | `sed -n '1399,1401p' PLANNER_TEMPLATE.md`; `grep -n -F 'own-root kickstart rule' GLOSSARY.md` |
| P3 | the defect, measured | in a scratch directory `Wt` entered as `WT` (the mini, 2026-09-14): `os.path.realpath` → `…/WT`; `os.getcwd()`, `lsof -a -p $$ -d cwd` and `git rev-parse --show-toplevel` → `…/Wt`; the prototype, a scratch archive of `65f713c`: t9 at its empty case and t13 red on the base, `2 failed, 11 passed` (`a36e051`) | the register's walk 0 |
| P4 | ⛔ the tests | `tests/test_daemon_agent.py`: 12 tests, 273 lines, a newline at its end; `import dashboard` before `import bellows` (:15–16); t5 (:84–97) patches `_agent_root` to return `str(tmp_path)`, with `create=True`; t9 :210–221 (its separator :210, its `def` :211) asserts the same directory True, the same basename under another parent False and no agent False, its docstring naming the realpath comparison; t10 (:224) patches `_agent_root` to return `str(tmp_path / "elsewhere")`, which does not exist, and asserts a child spawned and no kickstart; t11 asserts the kickstart with `replace=False` for the dashboard's own root; t12 :259–273, the file's last test | the file at those lines |
| P5 | the mutation tool and the manifest form | `tools/mutation_check.py` runs each mutant's `expect_fail` node alone (its docstring, :5), and an anchor matching other than once is `ERROR` (:266–270); the form `{"target", "mutants": [{"name", "why", "anchor", "replacement", "expect_fail"}]}`; #100087's `knowledge/mutants/dashboard-agent-root.json`, whose m5 anchors on :207's `realpath` line and names t9's old node id | the files |
| P6 | class; in flight; interpreters; the suite | the writes → **shop-infra** (bellows, the project floor), HOLDS at admission for the CEO's release; in flight at drafting: nothing running — thread 329's fix plan held for release (`tools/classify_deposit.py` and its tests), thread 341's plan held (`gates.py`, `verdict.py`, …), threads 333's and 322's plans in drafting, both writing `bellows.py` — the three run one after another, and this plan's anchors are the whole lines of one function neither touches; the bellows venv 3.12.14, `/usr/bin/python3` 3.9.6, the Air's venv 3.9.6; the full suite at `65f713c`, derived and not run in a canonical-shaped tree: 2377 passed, 2 skipped — `61794c4`'s 2364 with #100100's eight and #100101's five (thread 329's fix plan, its P5); behind the suite fence, over a scratch archive of the same base, `51 failed, 2325 passed, 3 skipped`, and over the change `51 failed, 2326 passed, 3 skipped`, the failure sets identical | `status.py`; `python -V`; the suite |

DEV re-derives P1 and P4 in Item 1 and HALTs on a mechanism mismatch; P2, P3, P5 and P6 are the drafting record.

## ACID, stated

- **Atomicity:** after the DEV's first commit (three files) the tree is green, and the change is live only for a dashboard started from this code; the second commit adds the run file and the dev-log; QA adds two evidence files. The doctrine half — the GLOSSARY addendum — lands at the CEO's apply act after the merge; until it does, the entry says `realpath` of a comparison made by inode (§1's split, its cost stated).
- **Consistency:** the helper answers by inode, as Rule 97 requires, and still never raises.
- **Isolation:** the dashboard asks the helper once per spawn, in its own process; in flight at drafting, none of these files is another plan's write but `bellows.py`, which threads 333's and 322's plans also write — they run one after another (P6). Should thread 333's commit binding merge and its daemon restart first, every commit here runs after its pre-check in one command, the shape it admits.
- **Durability:** the run file, the dev-log, the QA receipt with the counter lines, and the redirected suite file are the step's evidence.

## Drafting Cycle

**Tier:** **T1** — T-3 fires. **Walk register:** /Users/marklehn/Developer/eluvian-governance/governance/knowledge/research/walk-register-agent-root-inode-2026-09-14.md
**Walks:** walk 0 pinned (P1–P6 on bellows `65f713c`; the change prototyped in a scratch archive of `65f713c`; clone-diff against `Done/executable-100087.md`: FACTS, ARTEFACTS, STRUCTURE; thread 342 split off by the enumeration); `fold_check --save-baseline` ARMED on v0 before any fold, re-saved after every intended edit; every lens commit through `/Users/marklehn/Developer/bellows/scripts/lens_commit.py`, invoked directly with its output whole — no filter between it and the screen — its exit read; no lens or walk number in any `--desc`, and no plain commit subject carrying a walk and a lens number; `lens_order_check` after every lens commit.

- Weak spots:          w1 1 folded — instruction 1 / record 0; w2 dry
- Destruction:         w1 dry; w2 dry
- Vulnerabilities:     w1 1 folded — instruction 1 / record 0; w2 dry
- Integration-record:  w1 1 folded — instruction 1 / record 0; w2 dry
- ACID:                w1 1 folded — instruction 0 / record 1; w2 dry
**Closing:** WARM close after walk 2 — BAR MET (T1), thread 324's plan, on a dry walk: walk 2 dry on all five lenses. Findings by walk and class — walk 1: 4 (instruction 3 / record 1; 1 of 4 fold-introduced, w1-4 on w1-3's fold); walk 2: 0 — no residue, since walk 2 folded nothing. At walk 0 the enumeration of every path comparison split the startup close's two off as thread 342, whose guard and liveness match must move together. Deposit via `ready-`; HOLDS on class shop-infra, the CEO releases (`clear_plan --release-class-hold`). No restart; the close acts are the CEO's two in Post-close — the GLOSSARY addendum after the merge, and the test run on the Air, whose summary line the Planner pastes into `threads done 324`.

## Cycle Manifest
tier: T1
target: bellows.py
class: shop-infra
reads: bellows.py, dashboard.py, tests/test_daemon_agent.py, tools/mutation_check.py, tools/check_deposit.py, knowledge/decisions/Done/executable-100087.md, knowledge/mutants/dashboard-agent-root.json
writes: bellows.py, tests/test_daemon_agent.py, knowledge/mutants/agent-root-inode.json, knowledge/mutants/agent-root-inode.run.txt, knowledge/development/dev-log-agent-root-inode-2026-09-14.md, knowledge/qa/evidence/agent-root-inode-qa-receipt-2026-09-14.md, knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt
mutants: knowledge/mutants/agent-root-inode.json
open_forks: 1. the startup close's two `realpath`-string comparisons (`:1331–1332`, `:1337`) and its liveness match (`:1074–1077`) — thread 342, which moves them together; 2. #100087's manifest keeps m5's anchor on the line this plan replaces — its record, not re-run
walks: 2
yields: 3, 0
validation: cycle_check=BAR_MET, plan_lint=0_FAIL, fold_check=VACUOUS, propagation_check=DIVERGENT:35
coherence: 2/2 body walks named in the register (4 register rows; walk-token match, NOT row coverage)
fold_baseline: governance/knowledge/decisions/drafts/.executable-bellows-agent-root-inode.md.foldcheck.json

---

## STEP 1 — DEV (one helper by inode, t9 re-pinned and t13 added, the mutation run; two commits)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. ⛔ **The interpreter is `/Users/marklehn/Developer/bellows/.venv/bin/python`, ABSOLUTE.** ⛔ Never run the daemon, `run_plan`, a claim, `bellows.py stop|restart`, the dashboard, or `launchctl` in any verb — the helper's subject is the launchd agent, and P1 is re-derived by reading the file, not by asking launchd. ⛔ Test code runs under pytest inside `tests/` or not at all (bellows `CLAUDE.md`, *Test code*): no helper is called from `python -c`.
>
> **Scope:**
> - `bellows.py`
> - `tests/test_daemon_agent.py`
> - `knowledge/mutants/agent-root-inode.json`
> - `knowledge/mutants/agent-root-inode.run.txt`
> - `knowledge/development/dev-log-agent-root-inode-2026-09-14.md`
>
> **Item 1 — re-derive P1 and P4 and HALT on a mechanism mismatch** (a line-number drift is not one, and a count that moved is a record — paste it; `samefile` already in the helper, a `_spawn_child` that no longer calls it, or a t13 already in the file IS one). Paste each pin's re-derived line beside it.
> **Item 2 — the failing test FIRST:** t9 re-pinned and t13 appended as *What this changes* 2; run the file on the unedited helper and paste the red summary line — `2 failed, 11 passed` (t9 at its empty case: `os.path.realpath("")` is the working directory, `agent_dir`, so the string form answers yes; t13: the variant spelling reads as another checkout's).
> **Item 3 — the edit** as *What this changes* 1 and 3, each manifest anchor matching `bellows.py` once; the file green (`13 passed` — t13 passed, not skipped, on the mini's filesystem), then the FULL suite green: predicted `2378 passed, 2 skipped` on `65f713c` — P6's derived base and t13 — plus the tests of the plans merged before this one (the run supersedes the prediction; HALT only on a non-zero failure count). An existing test failing here is a HALT, never an edit.
> **Item 4 — first commit**, gated on the FULL suite AND the pre-check with the stage flag, path-scoped, run as ONE command — the verdict read checks each commit's place in the step's transcript, and a split chain or a suite piped into another command is a deviation it records: `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ -q && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 1 --wt "$(git rev-parse --show-toplevel)" --expect-missing knowledge/development/dev-log-agent-root-inode-2026-09-14.md knowledge/mutants/agent-root-inode.run.txt && git add bellows.py tests/test_daemon_agent.py knowledge/mutants/agent-root-inode.json && git commit -F <msg-file> -- bellows.py tests/test_daemon_agent.py knowledge/mutants/agent-root-inode.json` — three files (the manifest rides this commit so the mutation run's `HEAD:` archive contains it); the message tagged with the plan id and `thread 324`.
> **Item 5 — the mutation run, REDIRECTED into `knowledge/mutants/agent-root-inode.run.txt` (a Deposit):** `/Users/marklehn/Developer/bellows/.venv/bin/python tools/mutation_check.py knowledge/mutants/agent-root-inode.json > knowledge/mutants/agent-root-inode.run.txt 2>&1`; the last line must read `MUTATION: 3 killed, 0 survived, 0 error` — a survivor is a test to write, not a mutant to delete: the test, at the file's end, and the manifest, that mutant's `expect_fail` naming the new test — `tools/mutation_check.py` runs each mutant's `expect_fail` node alone, so a test the manifest does not name kills nothing — in ONE further commit, run as one command — `rm knowledge/mutants/agent-root-inode.run.txt` first (a run file recording a survivor fails the pre-check's `mutation_result` gate, and a present path cannot be named), then the FULL suite AND the pre-check with `--expect-missing knowledge/development/dev-log-agent-root-inode-2026-09-14.md knowledge/mutants/agent-root-inode.run.txt`, then `git add` and `git commit`, path-scoped to the test file and the manifest — then the run again, which writes the run file anew, its `HEAD:` naming that commit.
> **Item 6 — the dev-log** `knowledge/development/dev-log-agent-root-inode-2026-09-14.md` under the four headings declared below; `## Pins re-derived (P1, P4)` opens with P4's value cell pasted verbatim, whole, to its last character — ⛔ the cell ends with the words `the file's last test`; paste through those words, then stop. Under `## Failing-first (red, then green)` the red line and the two green lines (the file, the full suite); under `## The comparison observed (t9, t13)` the two PASSED lines of a `-v` run over those node ids — each test asserts the answer it names, and nothing in this step prints them otherwise; under `## Mutation run` the run file's last line. **The last commit** (the second, or the third after a survivor fix), gated on the FULL suite and the pre-check bare (`… tools/check_deposit.py … 1 --wt "$(git rev-parse --show-toplevel)"`, no stage flag — the run file and the dev-log now exist), run as ONE command, path-scoped to the run file and the dev-log.
> **Headings:** `## Pins re-derived (P1, P4)`; `## Failing-first (red, then green)`; `## The comparison observed (t9, t13)`; `## Mutation run`
> **Verbatim:** `## Pins re-derived (P1, P4)` ← P4
>
> **Deposits:**
> - `knowledge/mutants/agent-root-inode.json`
> - `knowledge/mutants/agent-root-inode.run.txt`
> - `knowledge/development/dev-log-agent-root-inode-2026-09-14.md`
>
> **Post-conditions:** t1–t13 green, t13 passed and not skipped; the FULL suite green with no `failed`; the mutation run's last line as Item 5 states it; two DEV commits, or three after a survivor fix — the first carrying the three files, the last the run file and the dev-log; the dev-log's four headings present as full lines with P4's cell whole; the `launchctl` verbs in `bellows.py` still exactly `print` and `kickstart` — `grep -oE 'launchctl", "[a-z]+' bellows.py | sort -u` prints two lines, as on `main`; `git status --porcelain` empty after the last commit.

## STEP 2 — QA (the agent's counter before and after the full suite; the comparison read through its tests; the receipt)

> ⛔ **Every item starts by re-establishing the root** — `cd "$(git rev-parse --show-toplevel)" && test -f gates.py && echo TREE_OK` — HALT unless TREE_OK. Interpreter ABSOLUTE; read-only beyond the two evidence files; `launchctl print` is the one `launchctl` verb this step runs, and it reads.
>
> **Item 1 — the counter before:** `launchctl print gui/$(id -u)/com.eluvian.bellows-daemon | grep -E '^\s*(runs|pid) ='` — paste both lines (`runs = N`, `pid = M`); and the daemon log's count, in one command that prints the file it reads — `f=/Users/marklehn/Developer/bellows/logs/terminal/bellows-$(date +%F).log; echo "$f"; grep -cF 'SIGTERM received' "$f"` — both lines pasted.
> **Item 2 — the full suite REDIRECTED not piped:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest tests/ --tb=short -q > knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt 2>&1`; in the receipt's Verification table the row quotes the count as `N passed` and names the skips as `two skips (test_gate_watcher live-DB; test_fallback_live_wal_window version gate)` — never pytest's own summary line, whose word the Rule 20 block scans for; a run that fails on a test outside this plan's files is re-run once, both summaries named in the receipt. Then **the counter after**, Item 1's command again: `runs` and `pid` EQUAL to Item 1's — the suite restarted no agent — and the log's count again, `grep -cF 'SIGTERM received'` over the path Item 1 printed, written out — `date` would name another day's file for a step that crosses midnight — equal. A moved counter would also have killed this very step — the suite runs under the daemon it would restart — so the two equal pairs are the proof that the branch stayed closed while this step lived; if the step is found halted with no receipt, the counter is the first thing the Planner reads (#100087's walk 1).
> **Item 3 — the comparison, read through its own tests:** `/Users/marklehn/Developer/bellows/.venv/bin/python -m pytest -v` over t5, t9, t10, t11 and t13 of `tests/test_daemon_agent.py`, and paste the five PASSED lines — t13 PASSED, not SKIPPED, on the mini's filesystem. No call into the module outside pytest.
> **Item 4 — production writes, stated exactly:** none outside the two evidence files; no lane file, no lifecycle row, no daemon act; Item 1's and Item 2's counter lines and log counts equal.
> **Item 5 — receipt** `knowledge/qa/evidence/agent-root-inode-qa-receipt-2026-09-14.md`: `numstat` over the DEV commits (`<base>..<dev>`, five files); `git diff <base>..<dev> -- bellows.py` pasted (the helper alone); the test file's edit, checked — `git diff -U0 <base>..<dev> -- tests/test_daemon_agent.py | grep -F '@@'` pasted beside `git show "<base>:tests/test_daemon_agent.py" | wc -l` (a base held in a shell variable is written braced, `"${BASE}:tests/…"` — zsh reads `$BASE:t` as a modifier): every hunk inside t9's lines (:210–221) or after the base file's last line — a hunk anywhere else is an edit to a test MUST-PRESERVE freezes, and the step STOPS; a `## Verification` table with one row per Item 1–4, each quoting the line it rests on — ⛔ the status cell holds exactly one token (`✅`) and no positive row's text carries a hedging keyword; then RUN the canonical Rule 20 block (`/Users/marklehn/Developer/eluvian-governance/RULE_20_SELF_CHECK_BLOCK.md`) with `plan_slug: agent-root-inode-2026-09-14`, `qa_report_path` and `evidence_dir` absolute under `$(git rev-parse --show-toplevel)/knowledge/qa/evidence/`, `required_evidence_files: ["agent-root-inode-suite-2026-09-14.txt"]`, and APPEND its stdout — the banner `Rule 20 — QA Self-Check Results` and the `PASSED — SELF-CHECK PASSED` line are the block's output, never hand-authored; a `FAILED` stdout goes into the receipt and the step STOPS (thread 262).
> **Item 6 — commit**, path-scoped and gated, run as ONE command: `grep -Eq '^[0-9]+ passed' knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt && ! grep -Eq '[0-9]+ (failed|error)' knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt && /Users/marklehn/Developer/bellows/.venv/bin/python tools/check_deposit.py /Users/marklehn/Developer/bellows/knowledge/decisions/in-progress-executable-<id>.md 2 --wt "$(git rev-parse --show-toplevel)" && git add knowledge/qa/evidence/agent-root-inode-qa-receipt-2026-09-14.md knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt && git commit -F <msg-file> -- knowledge/qa/evidence/agent-root-inode-qa-receipt-2026-09-14.md knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt`. ⛔ A QA step that must change production code STOPS and requests a verdict (thread 262).
>
> **Deposits:**
> - `knowledge/qa/evidence/agent-root-inode-qa-receipt-2026-09-14.md`
> - `knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt`
>
> **Scope:**
> - `knowledge/qa/evidence/agent-root-inode-qa-receipt-2026-09-14.md`
> - `knowledge/qa/evidence/agent-root-inode-suite-2026-09-14.txt`
>
> **Post-conditions:** the suite file's summary line is `N passed, 2 skipped` with N at least the DEV's count and no `failed`; Item 1's and Item 2's counter lines and `SIGTERM received` counts pasted, each pair equal; Item 3's five PASSED lines pasted; the test file's hunk headers pasted, every hunk inside t9's lines or after the base's last line; the receipt's `## Verification` table has four rows, and the receipt carries the block's stdout, its `PASSED — SELF-CHECK PASSED` line among it; one QA commit carrying the two evidence files.
