# bellows — Component 3: the in-bellows depositor + dashboard DEPOSITS panel
**Date:** 2026-08-20 | **Tier:** Medium | **Dispatch Mode:** bellows | **cycle_tier:** T2 | **Test Scope:** full | **Execution:** Step 1 (DEV) → Step 2 (QA) | **Priority:** 1

**auto_close:** false
**pause_for_verdict:** after_qa_step

## Context

Component 3 of the cycle-automation arc: build the **in-bellows depositor + dashboard DEPOSITS panel** from the finalized design in `bellows/knowledge/research/component3-depositor-design-2026-08-20.md` (diagnostic-478, T-7 — build from the spec + its 11-row Rule 27 gap table, do NOT re-derive). Completes the arc's substrate before the capstone (component 4).

**The single safety invariant (load-bearing):** *the depositor never mints, never dispatches — it stages and clears (a same-dir atomic `os.rename`); the daemon claims.* `depositor.py` imports NO dispatch function (`run_plan`, `handle_new_plan`, `lifecycle.mint_and_claim`). This is enforced by construction AND by a mechanical import test.

**⚠️ T2 — self-escalated (autonomous-dispatch surface).** Auto-deposit for `read-only` plans is a standing capability that can pull the dispatch trigger; the strict trigger list computes T1 (T-1 blast radius: bellows.py + depositor.py + dashboard.py + status.py; T-8 novel), but the safety surface self-escalates to **T2** ([[money-path-plans-self-escalate-cold-scout]] analog) → a full cold panel is owed at close.

**⚠️ Activation is a MANUAL post-merge step (walk-2 D2):** the running daemon won't have the depositor until it is RESTARTED (like the Claude-Code upgrade cadence in bellows CLAUDE.md). The DEV runs in a bellows worktree and the QA canary is scratch-only, so the LIVE daemon is untouched by the build; turning the depositor on is a deliberate CEO-run restart after merge, not part of DEV/QA.

**What it builds (from the gap table):** a new `bellows/depositor.py` (`Depositor` class) invoked by the daemon's existing `_handle` (`bellows.py:2063`) on `ready-`-prefixed files (already non-claimable, verified in the design) + a startup re-evaluation; the DEPOSITS dashboard panel (`dashboard.py` `assemble_state`:104 / `render_screen`:176 + `status.py` `render_depositor_status` mirroring :138/:163 + `COLOR_DEPOSITS`); and tests. Staging = `ready-` prefix; HOLD = `hold-` prefix + `.hold.json`; clear = atomic `os.rename`; class VERIFIED from `writes:` paths; collision set = in-flight (`status.query_in_flight`:188 over **lifecycle.db**) UNION other `ready-` siblings; fail-safe HOLD on any uncertainty; `_disk_preflight`/`_shutting_down` reused; freeze NOT NEEDED.

## Drafting Cycle
**Tier:** T2 (self-escalated) — triggers: T-1 (multi-subsystem), T-8 (novel); self-escalated for the autonomous-dispatch safety surface. Full cold panel owed at close (§2.6, likely full form — new machinery, no committed-builder clone). cycle_check dogfooded per walk. Real correctness guard is the QA **live canary** (actually stage files + observe AUTO/HOLD), scratch-only.
**Walk 0 (context pin) — real measurements:**
1. Newest same-class plan (clone-diff target): `executable-474` (2b tooling — new bellows script + tests + QA live-canary; same class + read-only-import discipline). Also diff vs `executable-476` (the most recent). No origin drift.
2. Edit sites (line-anchored, verified this session): `is_runnable_plan` (`bellows.py:2030`), `_handle` (`2057`) with the branch point `if not is_runnable_plan(filename):` (**2063**), `_shutting_down` checked at `2072`, `_disk_preflight` (`334`), startup/pending scans using `is_runnable_plan` (`2204/2330/2595/2675`); `status.query_in_flight` (`188`), `render_in_flight`/`render_awaiting_verdict` (`138/163`); `cycle_check.run_check` (`347`), `parse_manifest_stanza` (`419`), `emit_manifest` (`487`); `gates._extract_plan_required_deposits`/`_extract_plan_scope` (`463/520`); `dashboard.assemble_state` (`104`), `render_screen` (`176`). ⚠️ **The design flags bellows.py line numbers SHIFT — the DEV re-verifies every citation by CONTENT at edit time, never by the number.**
3. ⚠️ DB-identity: plans/id_sequence in **lifecycle.db**, not bellows.db (the collision query reads lifecycle.db). No `depositor.py` exists yet (deposit-once clear).
4. This is a bellows-in-tree plan (scripts/tests under bellows) → the location-dependent lint WARN class is benign.
5. cycle_check dogfooded per walk; T2 → cold panel at close.
**Direction verdict (after walk 1): PROCEED.** The angle — build the depositor + panel from the 478 gap table, safety-invariant-first, canary-guarded — is sound; the design is build-ready. No forcing finding.
**Walks:** 7 (walk 1–7). Instruction yield 6, 4, 4, 3, 2, 1, 0 → **6→4→4→3→2→1→0** (w2 corrected from a prose "5" — D2 is record-class, so instruction w2 = 4; grounded in cycle_check's computed counts, walk-7 I6). §2 BAR MET at walk 7. Cold panel owed (T2, full form).
- Weak spots:  w1 2 folded — instruction 2 / record 0; w2 1 folded — instruction 1 / record 0; w3 1 folded — instruction 1 / record 0; w4 1 folded — instruction 1 / record 0; w5 1 folded — instruction 1 / record 0; w6 1 folded — instruction 1 / record 0; w7 dry.
  - W1: the import-whitelist safety invariant (never import `run_plan`/`handle_new_plan`/`mint_and_claim`) is "closed by construction" (design R2) but needs a MECHANICAL test — folded a `tests/test_depositor.py` assertion that the depositor module's source imports none of the dispatch functions.
  - W2: R4 (concurrent `evaluate()` on the same `ready-` file, from create+modify events) is flagged MUST-CLOSE-BY-IMPLEMENTATION by the design — folded: the DEV implements a per-directory `threading.Lock` AND a present-before-rename recheck, with a test that two concurrent evaluations produce exactly one clear.
  - W3 (w2): `evaluate()` runs `cycle_check`+`plan_lint` SUBPROCESSES (seconds) — it must run on a WORKER thread, not block `_handle`'s watchdog observer (which would stall all filesystem events). Folded into the DEV spec.
  - W4 (w3): the QA canary's collision test needs a DETERMINISTIC in-flight set — seed a scratch `lifecycle.db` with a colliding in-flight plan and point `query_in_flight` at it. Folded into the QA canary.
  - W5 (w4, ⚠️ architecture): the Depositor must take `_disk_preflight`/`_shutting_down`/config as INJECTED deps — `import bellows` inside depositor.py would create a circular import (bellows↔depositor) AND pull dispatch symbols into the whitelist. Folded: dependency injection at construction.
  - W6 (w5): the additive DEPOSITS panel shifts row positions — existing dashboard/status/`_handle` render tests that assert exact output must be updated, or the full suite goes red on a benign layout shift. Folded into the DEV dashboard spec.
  - W7 (w6): the DEV Deposits list must include any EXISTING test file it modifies for W6 — an undeclared modified file trips `scope_check`/`file_change_audit`. Folded into the DEV Deposits.
- Destruction:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 0 / record 1; w3 dry; w4 dry; w5 dry; w6 dry; w7 dry.
  - D1: the `_handle` branch (2063) must be PURELY ADDITIVE — every currently-non-claimable file (`roadmap-`, `drafts/`, etc.) must still be skipped exactly as before; ONLY `ready-` gets the new path. Folded: the branch matches `filename.startswith("ready-")` narrowly, and a test asserts `_handle` on a `roadmap-`/non-ready file is unchanged (still skipped, no depositor call).
  - D2 (w2, record): activation requires a daemon RESTART (the running daemon won't have the depositor until restarted) — a manual post-merge CEO step, not part of DEV/QA. Folded a deploy note into Context.
- Vulnerabilities:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 1 / record 0; w3 1 folded — instruction 1 / record 0; w4 1 folded — instruction 1 / record 0; w5 dry; w6 dry; w7 dry.
  - V1 (⚠️ THE load-bearing safety constraint): the QA live canary MUST run SCRATCH-ONLY — a real `ready-` file in the LIVE `knowledge/decisions/` would be evaluated by the running daemon's new `_handle` branch → could auto-deposit → the live daemon CLAIMS it (the incident-mandate class). Folded: the canary builds a scratch decisions dir + a `Depositor` instance pointed at it, `cp`-copies real fixtures in, and NEVER touches the live tree; QA asserts the live `knowledge/decisions/` is byte-unchanged after the canary.
  - V2 (w2): an UNPARSEABLE sibling in the collision scan (half-written, malformed) has unknown `writes` → treat as a potential collision → HOLD the current plan (fail-safe extends to the sibling scan). Folded into the DEV spec (d).
  - V3 (w3): the re-run subprocesses must use ABSOLUTE script paths (`resolve_bellows_root()/scripts/…`), not cwd-relative — the daemon's cwd varies. Folded into the DEV spec (f).
  - V4 (w4): the collision intersection must cover file-vs-file, prefix-vs-file, and prefix-vs-prefix — a test each. Folded into the DEV tests.
- Integration-record:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 1 / record 0; w3 1 folded — instruction 1 / record 0; w4 dry; w5 1 folded — instruction 0 / record 1; w6 1 folded — instruction 0 / record 1; w7 1 folded — instruction 0 / record 1.
  - I1: verify every `bellows.py`/`status.py`/`dashboard.py` file:line citation by CONTENT at edit time (the design's own flag — lines shift). Folded into the DEV step + walk-0 pin 2.
  - I2 (w2): the DEPOSITS panel must CAP its rows (first N + "…M more") — a large held-set must not crowd the EVENT FEED off-screen. Folded into the dashboard spec.
  - I3 (w3): clarify that `read-only` is a STRICT ALLOWLIST (writes ⊆ {`knowledge/research/`, scratch}) — THE auto-vs-HOLD safety boundary; the register-vs-tooling label is secondary (both HOLD). Folded into the DEV spec (g).
  - I4 (w5, record-DECAY): the closing-record re-read found the Vulnerabilities line lagging w3/w4 and the ACID line lagging w4 (sub-bullets added, tallies unswept). Swept both to reconcile with the sub-bullet counts.
  - I5 (w6, record-DECAY): recording I4 itself left the Integration-record summary line lagging w5 — the recurring summary-line-vs-sub-bullet lag. Swept the Integration line (w5+w6). ⚠️ The recurrence (I4, I5) is MY record-keeping discipline, not a plan defect — the fold-sweep must update the summary tally in the SAME edit as the sub-bullet.
  - I6 (w7, record): the closing-record re-read found the prose yield series miscounted w2 as "5" — D2 is record-class, so instruction w2 = 4. Corrected to 6→4→4→3→2→1→0, GROUNDED in cycle_check's computed instruction counts ({1:6,2:4,3:4,4:3,5:2,6:1}), not asserted.
- ACID:  w1 1 folded — instruction 1 / record 0; w2 1 folded — instruction 1 / record 0; w3 1 folded — instruction 1 / record 0; w4 1 folded — instruction 1 / record 0; w5 1 folded — instruction 1 / record 0; w6 dry; w7 dry.
  - A1: durability + atomicity — the clear is a single `os.rename` (atomic, same-fs, D3, NOT `shutil.move`); the `.hold.json` survives restart; on restart the depositor re-evaluates `hold-` files but NEVER auto-clears them (A2). Folded into the DEV spec + tests (restart re-eval does not release a hold).
  - A2 (w2): TOCTOU — a concurrent daemon claim in the collision-check→clear window could introduce a collision the depositor didn't see. Folded: re-check collisions IMMEDIATELY before the rename to minimize the window; declare the residual (a global lock would serialize all execution), like the `reads:` residual.
  - A3 (w3): HOLD ordering — rename `ready-`→`hold-` FIRST (the durable non-claimable state), THEN best-effort write `.hold.json`; a crash between leaves a HELD (safe) plan, never a claimable one. Folded into the DEV spec (i).
  - A4 (w4): the sibling scan must span ALL watched directories — a plan staged in a different project dir that writes the same absolute path is invisible to a per-dir scan and would auto-clear. Folded into the DEV spec (d): the staged set is global.
  - A5 (w5): the evaluate lock must be GLOBAL, not per-directory — A4's global sibling scan requires global serialization; a per-dir lock leaves a cross-dir TOCTOU. Folded into the DEV spec (evaluate lock).
**Conflicts:** C1 — the evaluate LOCK GRANULARITY: W2 (w1) opened a per-directory lock; A4 (w4)'s global sibling scan requires global serialization. Joint-resolved at A5 (w5): ONE global evaluate lock. Settled once, no ping-pong.
**Walk 7 STATUS:** instruction 0 / record 1 (I6 yield-series correction) — **§2 BAR MET.** All five lenses instruction-dry over the whole artifact; the one fold is the closing-record re-read's own catch, no restructuring. Convergence: design gaps closed w1–4, last cross-lens conflict (C1) resolved w5, hygiene tail w6, dry w7.
**Closing-record re-read (§2.7, walk 7) — CAUGHT the yield miscount:** re-read the tallies against the sub-bullets AND against cycle_check's computed instruction counts — found the prose series' w2 was "5" while the instruction sum is 4 (D2 is record-class). Corrected (I6). All five lens lines now carry w1–w7 and reconcile with the per-walk sums; post-correction re-read dry.
**cycle_check (dogfood) per walk:** w1 `CONTINUE` · w2 `CONTINUE` · w3 `CONTINUE` · w4 `CONTINUE` · w5 `CONTINUE` · w6 `CONTINUE` · w7 `BAR_MET` (exit 0 — instruction-dry walk, earned close recognized).
**Warm close:** 7 walks (bar met walk 7, instruction-dry; yield 6→4→4→3→2→1→0); C1 (lock granularity) joint-resolved; closing-record re-read caught the w2 yield miscount.
**Cold panel (§2.6, FULL form — CEO-approved; T2 autonomous-dispatch surface). Meter opened at convene. Diagnostic-478 served as the design scout.**
- **Seat DISCOVERY (weak-spots + destruction; evidence-attack + clone-diff) — RUN, 94.5k tokens.** 7 findings (2 HIGH · 3 MED · 2 LOW), all author-verified against the live bellows + the 478 design, all pre-existing-v0 (the warm cycle reached bar-met without them — the panel's value). Folds taken: **DISC-1** (HIGH — `query_in_flight` is metadata-only; resolve each in-flight plan's file via `deposit_placeholder_name`/`plan_doc_ref` to get its `writes`), **DISC-4** (HIGH — a 10th wrong-dispatch path: EMPTY/undeterminable `writes` is vacuously ⊆ the allowlist → auto-clear; now HOLD per design Q2 l.145), **DISC-2** (periodic rescan must sweep `ready-`/`hold-` — the `is_runnable_plan` filter excludes them, no recovery net), **DISC-3** (dedup create+modify evaluations), **DISC-5** (positive `_handle`→`evaluate` wiring test, not just the negative), **DISC-6** (delete stale `.hold.json` on clear), **DISC-7** (QA grep the actual `depositor.py` path). Pins all clean (ran the live `is_runnable_plan` regex: `ready-`/`hold-` reject). Clone-diff vs 474: proven machinery carried; the one regression was DISC-5 (canary dropped from the wired path).
- **Seat EXECUTION (vulnerabilities + execution brief) — RUN, 94.8k tokens.** Ran the REAL bellows primitives the depositor depends on, against real inputs. **VERDICT: BUILDABLE and CORRECT AS FOLDED** — every safety-boundary primitive clean, every induced failure mode degraded to HOLD (the safe direction); 7 positive-evidence checks (claim gate, class allowlist incl. DISC-4 empty→HOLD, collision 3-cases, atomic-rename is_runnable flip, DISC-1 in-flight resolution via `plan_doc_ref`, no-circular-import injection, adversarial variants all HOLD). 4 spec-precision findings (0 HIGH · 2 MED · 2 LOW): **EXEC-1** (`run_check` needs a `pathlib.Path` — a str → `(None,2)` silent false-HOLD, verified), **EXEC-4** (parse plan_lint's FAIL COUNT + enumerate deposit-time-benign FAILs — exit-code alone is insufficient), **EXEC-2** (weight tests on Path B — 0 real stanzas in the corpus), **EXEC-3** (reads∩writes inert today — declared residual). All folded. The finding class is spec-precision, not design-break — the auto-CLEAR would silently degrade to always-HOLD (safe) without EXEC-1/EXEC-4.
- **Seat CAPSTONE (integration + ACID + system brief; runs last on the fold set) — RUN, 65.4k tokens.** Cross-checked the panel's OWN folds. 4 findings (0 HIGH · 3 MED · 1 LOW), all folded: **CAP-1** (⚠️ REFUTED my EXEC-4 premise — plan_lint exits 1 on ANY FAIL, `:728`, not exit-0-with-FAILs; the real gap is a staged plan legitimately exiting 1 on pre-QA-expected FAILs), **CAP-2** (the benign-FAIL list must use the [[benign-gate-failure-classes]] catalog, NOT mislabel the WARN-only location-dependent issue; blast radius bounded — class-check gates independently of plan_lint), **CAP-3** (DISC-3 dedup must be TRANSIENT not the process-lifetime `_seen`, else it defeats DISC-2's recovery net), **CAP-4** (DISC-1 unresolvable in-flight file → conservative HOLD). Verified-coherent: DISC-4×I3×EXEC-1 all converge on HOLD (no empty/None auto-clear); tallies reconcile exactly; ACID clean (C1 → one global lock); residue `BAR_MET`. **CAP-5 (found while author-verifying CAP-1): this plan itself lints (c) QA-banner + (d) Scope FAILs** — fixed (inlined the Rule 20 banner string; neutralized a prose `Scope` gate-token); plan_lint now **0 FAIL**.
**Cold panel COMPLETE (full form, 3 seats + the 478 design-scout). Meter: DISCOVERY 94.5k + EXECUTION 94.8k + CAPSTONE 65.4k ≈ 255k tokens.** The panel earned its cost decisively — it caught a 10th wrong-dispatch path (DISC-4 empty-writes), an unbuildable collision surface (DISC-1), a silent auto-clear→always-HOLD degrade (EXEC-1), a recovery-net-defeating fold interaction (CAP-3), and REFUTED one of its own folds (CAP-1) — none of which 7 warm walks surfaced. Every finding degrades to the SAFE (HOLD) direction; the safety invariant held throughout.
**Bar re-met post-panel:** all panel folds (DISC/EXEC/CAP) applied; the CAPSTONE read the fold set coherent (0 HIGH/MED unresolved), so the walk re-reaches the bar on the folded plan.
**Closing-record re-read (§2.7, final):** tallies + panel meter + fold IDs reconcile with the folds applied; plan_lint 0 FAIL; cycle_check BAR_MET; no closing prose over-claims. Dry.
**Closing:** 7 warm walks (bar met w7) + full cold panel (3 seats, ~255k tokens, 15 folds incl. a self-refuted premise and a 10th wrong-dispatch path); C1 joint-resolved; closing-record re-reads run (warm + post-panel), dry; deposit-shape clean (plan_lint 0 FAIL, cycle_check BAR_MET); cycle done — deposit exactly once. Class: governed-tooling → **HOLD-FOR-CEO** at deposit (not auto).

---
---

## STEP 1 — BELLOWS DEVELOPER

---

> **Identity:** You are building component 3 — the in-bellows depositor + dashboard DEPOSITS panel. Read `knowledge/research/component3-depositor-design-2026-08-20.md` (diagnostic-478) FIRST — its Q1–Q7 + gap table are the spec; implement them, do not re-derive. ⚠️ **Re-verify EVERY `bellows.py`/`status.py`/`dashboard.py`/`cycle_check.py`/`gates.py` citation by CONTENT (grep the function/line) before editing — the line numbers SHIFT (I1).**
>
> **⚠️ THE SAFETY INVARIANT (the whole point):** the depositor never mints, never dispatches — it stages and clears via a same-dir atomic `os.rename`; the daemon claims. `depositor.py` MUST NOT import or call `run_plan`, `handle_new_plan`, or `lifecycle.mint_and_claim`. Its only writes are `os.rename()` and `open(.hold.json,'w')`.
>
> **(1) `bellows/depositor.py` — the `Depositor` class.** Per the design Q1–Q5:
> - ⚠️ **The Depositor takes `_disk_preflight`, the `_shutting_down` flag, and the config as INJECTED dependencies from the orchestrator at construction (walk-4 W5) — do NOT `import bellows` inside `depositor.py`.** Importing bellows.py would create a circular import (bellows→depositor via the `_handle` branch, depositor→bellows for the helpers) AND pull dispatch symbols into the depositor's namespace, defeating the import-whitelist. Injection keeps the whitelist clean and makes the Depositor unit/canary-testable without the live daemon.
> - ⚠️ **`evaluate()` runs on a WORKER thread, never inline in the watchdog callback (walk-2 W3): it runs `cycle_check` + `plan_lint` SUBPROCESSES (seconds), and blocking `_handle`'s observer thread would stall every other filesystem event.** The `_handle` branch dispatches `evaluate` to a worker (reuse the daemon's plan-handling executor / a `threading.Thread`), the same way `handle_new_plan` does not block the observer.
> - `evaluate(self, path)` — the single entry point, run under a **GLOBAL `threading.Lock`** (W2/R4/A5 — one lock for ALL evaluates, not per-directory: A4's global sibling scan requires global serialization; a per-directory lock would leave a cross-directory TOCTOU where two evaluates in different dirs both scan-and-clear before seeing each other). Steps: (a) if `self._shutting_down_check()` → return (skip); (b) re-check the `ready-` file still exists (a prior event may have handled it) → else return; (c) parse the staged plan's `writes`/`reads`/`class` — `parse_manifest_stanza` (`cycle_check.py:419`) first, else legacy `_extract_plan_required_deposits`/`_extract_plan_scope` (`gates.py:463/520`); (d) scan ALL other `ready-*.md` siblings **across ALL watched directories, not just the current one (walk-4 A4 — a plan staged in a DIFFERENT project dir that writes the same absolute path is invisible to a per-dir scan and would auto-clear; the staged set is global)** + parse their `writes` (V2 sibling set) — ⚠️ **an UNPARSEABLE sibling (half-written, malformed) has unknown `writes`: treat it as a potential collision → HOLD the current plan (walk-2 V2, fail-safe extends to the sibling scan)**; (e) collision-check `writes∩writes` (HARD-HOLD) / `reads∩writes` (HOLD) against `status.query_in_flight` (`status.py:188`, **lifecycle.db**) UNION the sibling set — ⚠️ **`query_in_flight` returns METADATA only (no `writes` column, panel-DISC-1): for each in-flight plan, resolve its FILE via `deposit_placeholder_name`/`plan_doc_ref` (both in the `plans` schema) → read the `in-progress-*.md` text → parse its `writes` via `parse_manifest_stanza`/`_extract_plan_required_deposits`; ⚠️ an UNRESOLVABLE or empty-parse in-flight file (just closed / mid-rename) → treat as a POTENTIAL collision → HOLD, never silently no-collision (panel-CAP-4, mirror V2)** — with path-normalization (relative→target_project, absolute as-is, prefix-match for trailing-slash entries); (f) re-run `cycle_check.run_check(pathlib.Path(plan))` (`347`) — ⚠️ **pass a `pathlib.Path`, NOT a bare str: a str returns `(None, 2)` → silent false-HOLD that never auto-clears (panel-EXEC-1, verified)** — + `plan_lint` (subprocess — ⚠️ **ABSOLUTE paths, `resolve_bellows_root()/scripts/…`, never cwd-relative; the daemon's cwd varies, walk-3 V3**); non-`BAR_MET` → HOLD; ⚠️ **plan_lint EXITS 1 on ANY FAIL (`plan_lint.py:728`; exit-0-with-FAILs cannot occur — panel-CAP-1 corrects EXEC-4's inverted premise). The real gap: a STAGED pre-QA plan legitimately exits 1 on pre-execution-expected FAILs (e.g. `(c)` QA-banner, `(d)` Scope-parses-empty). The depositor distinguishes real FAILs from benign via the established [[benign-gate-failure-classes]] catalog — ⚠️ the location-dependent bellows-in-tree issue is WARN-only, NEVER a FAIL, so do NOT list it as a benign FAIL (panel-CAP-2). Blast radius is bounded: the class-check gates INDEPENDENTLY of plan_lint, so a mislabeled benign FAIL can only affect a read-only plan, never clear a governed one**; compare to the manifest's `validation:` if present (mismatch → HOLD) — ⚠️ **note the `reads∩writes` arm is inert on today's corpus (legacy plans have no `Scope` block → `reads` extracts empty; `writes∩writes` carries the load; declared residual, panel-EXEC-3)**; (g) ASSIGN class from `writes:` paths — ⚠️ **`read-only` is a STRICT ALLOWLIST: writes ⊆ {`knowledge/research/`, scratch} → `read-only`; ANY path outside the allowlist → NOT read-only → HOLD (walk-3 I3, THE safety boundary; the register-writing-vs-governed-tooling label is secondary, both HOLD, classified via a configurable governed-path list, default `governed-tooling` when unsure)** — ⚠️ **an EMPTY or undeterminable `writes` set is NOT `read-only` → HOLD (panel-DISC-4, "undeclared intent" per design Q2 l.145): an empty set is vacuously ⊆ the allowlist and would else AUTO-CLEAR a plan whose footprint the depositor never saw — a 10th wrong-dispatch path the warm cycle missed** — and if the assigned class ≠ declared `class:` → HOLD (D2); (h) `_disk_preflight` (reuse `bellows.py:334`) — low → HOLD; (i) DECIDE: `read-only` → ⚠️ **re-check the collision set IMMEDIATELY before the rename to minimize the check→clear TOCTOU window (walk-2 A2)**, then CLEAR (`os.rename` `ready-X.md`→`X.md`); else → HOLD — ⚠️ **rename `ready-`→`hold-X.md` FIRST (the durable non-claimable state), THEN best-effort write `hold-X.hold.json` (walk-3 A3): a crash between the two leaves a HELD (safe) plan, never a claimable one**. ⚠️ **FAIL-SAFE: ANY exception, unparseable input, missing class, EMPTY/undeterminable `writes` (panel-DISC-4), or uncertainty → HOLD, never clear.** On a successful clear of a previously-held plan, delete the stale `.hold.json` (panel-DISC-6). ⚠️ **Declared residual (walk-2 A2): a concurrent daemon claim in the narrow check→clear window could still introduce a collision the depositor didn't see; the window is minimized but not eliminated (the alternative — a global claim+deposit lock — serializes all execution). Documented like the `reads:` residual, not closed.**
> - `reevaluate_on_startup(self)` — re-evaluate all `ready-` files (full eval) and `hold-` files (re-run collision, UPDATE `.hold.json` reason, but NEVER auto-clear — a hold stays held until CEO renames it to `ready-`, A2).
> - Import whitelist (W1): only `os`, `json`, `subprocess`, `threading`, `pathlib`, and read-only helpers (`status.query_in_flight`, `cycle_check.run_check`/`parse_manifest_stanza`, `gates._extract_plan_required_deposits`/`_extract_plan_scope`). NO dispatch imports.
>
> **(2) `bellows.py` — wire the trigger (PURELY ADDITIVE, D1).** In `_handle` at the `if not is_runnable_plan(filename):` branch (~2063, verify by content): add `if filename.startswith("ready-"): self.orchestrator.depositor.evaluate(path); return` BEFORE the existing skip/return — so `ready-` files route to the depositor and EVERY other non-claimable file (`roadmap-`, etc.) is skipped exactly as before. Instantiate one `Depositor` on the orchestrator (respecting `_shutting_down`). Extend the startup rescan to call `depositor.reevaluate_on_startup()` once. ⚠️ **The PERIODIC rescan must ALSO re-scan `ready-`/`hold-` files (panel-DISC-2): every existing scan filters `is_runnable_plan`, which EXCLUDES those prefixes, so a missed watchdog `on_created` for a `ready-` file has NO recovery net until a restart — add a `ready-`/`hold-` sweep to the periodic rescan.** ⚠️ **Dedup evaluations (panel-DISC-3): `on_created`+`on_modified` both fire `_handle`; guard so a slow-written `ready-` file does not run the expensive `evaluate()` twice — ⚠️ the dedup MUST be TRANSIENT (a short-window guard), NOT the process-lifetime `bellows._seen` set (panel-CAP-3): a permanent `_seen` entry would make DISC-2's periodic recovery sweep skip a still-`ready-` file forever, defeating the recovery net.**
>
> **(3) Dashboard DEPOSITS panel (additive).** `dashboard.py`: `assemble_state` (:104) gains a `deposit_rows` key (enumerate `ready-*.md`/`hold-*.md` in each watched dir; read `.hold.json` reason for holds); `render_screen` (:176) gains a DEPOSITS section between AWAITING VERDICT and the EVENT FEED; add `COLOR_DEPOSITS = 5`. ⚠️ **The DEPOSITS section CAPS its rows (e.g. first N + "…(M more)"), matching how the other panels bound their height (walk-2 I2) — a large held-set must not crowd the EVENT FEED off-screen.** `status.py`: add `render_depositor_status(rows)` mirroring `render_in_flight`/`render_awaiting_verdict` (:138/:163). No change to existing panels. ⚠️ **Update any EXISTING dashboard/status/`_handle` tests that assert exact render output or row positions — the additive DEPOSITS panel shifts the rows below it (walk-5 W6); the full suite must stay green (a benign layout shift must not read as a regression).**
>
> **(4) Tests — `bellows/tests/test_depositor.py`** (+ a dashboard/status render test): class-assignment from `writes:` (all three classes); class-mismatch (declared read-only + register write) → HOLD; collision vs an in-flight plan (writes∩writes → HOLD, with the in-flight `writes` resolved via `deposit_placeholder_name`/`plan_doc_ref` per DISC-1) AND vs a `ready-` sibling (V2), with the intersection covering file-vs-file (exact), prefix-vs-file (a `knowledge/research/` prefix contains a file under it), and prefix-vs-prefix (overlapping) — a case each (walk-4 V4); ⚠️ **weight the matrix on Path B (legacy `_extract_*`): NO current plan carries a `## Cycle Manifest` stanza (panel-EXEC-2 — 474/476/478 all parse empty), so Path A + the manifest-`validation:`-compare is forward-looking — test Path A with a synthetic fixture, Path B against a real Done plan;** fail-safe (unparseable/missing class → HOLD); a `read-only` plan with no collision → CLEAR (renamed to claimable, `is_runnable_plan` True after); a HOLD writes `hold-X.md` + `.hold.json`, and `is_runnable_plan("hold-…")` is False; **the import-whitelist assertion (W1: the depositor module source contains none of `mint_and_claim`/`run_plan`/`handle_new_plan`)**; the `_handle` branch is additive (a `roadmap-` file is still skipped, no depositor call — D1) AND ⚠️ **a POSITIVE wiring test: `_handle` on a `ready-` file CALLS `depositor.evaluate` (panel-DISC-5 — the negative-only test lets a misplaced branch pass as a silent false-safe; 474's canary hit the real wired path)**; two concurrent `evaluate()` calls on one `ready-` file → exactly one clear (W2/R4); restart re-eval does NOT release a hold (A2).
>
> **DEV discipline + dogfood:** targeted runs only (`python3 -m pytest tests/test_depositor.py -q 2>&1 | cat`). Then dogfood `python3 scripts/cycle_check.py <this plan>` and record it. Commit `feat(bellows): in-bellows depositor + dashboard DEPOSITS panel (component 3) [<id>]`.
>
> **Deposits:**
> - `bellows/depositor.py`
> - `bellows/bellows.py`
> - `bellows/dashboard.py`
> - `bellows/status.py`
> - `bellows/tests/test_depositor.py`
> - ⚠️ **any EXISTING dashboard/status/`_handle` test file the DEV updates for W6 (e.g. `bellows/tests/test_dashboard.py` / `test_status.py` if they exist — verify at edit time and LIST each here, walk-6 W7): an undeclared modified file trips `scope_check`/`file_change_audit` at QA.**
> - `bellows/knowledge/development/component3-depositor-2026-08-20.md`
>
> End with an Output Receipt recording Status AND the DEV commit sha (QA reads it). Standard prompt-feedback protocol.

---
---

## STEP 2 — BELLOWS QA ANALYST

---

> **Identity:** You are QA for the component-3 depositor. Verify against the diagnostic-478 design + the safety invariant. Evidence is RAW command output, never summaries.
>
> **(1) Targeted + full suite (Rule 21).** `python3 -m pytest tests/test_depositor.py -v 2>&1 | cat` → `.../targeted.txt`; then `python3 -m pytest tests/ -q -rf 2>&1 | cat` → `.../full_suite.txt`. Extract FAILED node-ids; assert empty (the new depositor tests + all existing green).
>
> **(2) ⚠️ LIVE CANARY — SCRATCH-ONLY (the load-bearing guard, V1).** ⚠️ **NEVER stage a `ready-` file in the live `knowledge/decisions/` — the running daemon would evaluate + possibly auto-claim it (incident-mandate class).** Build a scratch decisions dir under `/tmp/depositor-canary/` and a `Depositor` pointed at it; `cp` real fixtures in. ⚠️ **Seed a scratch `lifecycle.db` with a known in-flight plan whose `writes` collide with a fixture, so the writes∩writes → HOLD path is DETERMINISTIC (walk-3 W4) — point the depositor's `query_in_flight` at the scratch db, never the live one.** Capture raw output to `.../live_canary.txt`:
> - A `read-only` plan (writes only `knowledge/research/x.md`), no collision → `evaluate` CLEARS it (file renamed `ready-…`→ claimable; `is_runnable_plan` True after). 
> - A `register-writing` plan (writes a governed register) → HELD (`hold-…` + `.hold.json` with the reason; `is_runnable_plan("hold-…")` False).
> - A **declared `read-only` that writes a governed register** → class-mismatch HOLD (the catastrophic path, D2).
> - Two colliding `ready-` siblings (same `writes`) → HOLD (V2).
> - **Assert the live `knowledge/decisions/` is byte-unchanged after the whole canary** (`git status --porcelain knowledge/decisions/` clean) — the read-only-on-live invariant.
> Any wrong AUTO/HOLD decision, any live-tree mutation, or any mint/dispatch call is a FAIL.
>
> **(3) Safety-invariant proof.** `grep -nE 'mint_and_claim|run_plan|handle_new_plan' depositor.py` (the module is `bellows/depositor.py` — target the ACTUAL path, not `scripts/depositor.py` which silently no-matches, panel-DISC-7) → assert EMPTY (the depositor imports/calls no dispatch function). Raw output to evidence.
>
> **(4) Scope.** `git diff --stat` shows only `bellows/` (depositor.py, bellows.py, dashboard.py, status.py, tests, knowledge/) — nothing outside.
>
> **(5) Rule 20 self-check** — run `/Users/marklehn/Developer/GitHub/RULE_20_SELF_CHECK_BLOCK.md` with `plan_slug: executable-component3-depositor-2026-08-20`, the qa report path, the evidence dir, and `required_evidence_files: ["targeted.txt", "live_canary.txt", "full_suite.txt"]`. Prints the banner `Rule 20 — QA Self-Check Results` and, on success, `PASSED — SELF-CHECK PASSED` (verbatim, em-dashes — the gate byte-matches; if `FAILED — SELF-CHECK FAILED`, halt). `qa_test_result` gate: name `full_suite.txt` + `targeted.txt` in Deposits.
>
> **Deposits:**
> - `bellows/knowledge/qa/2026-08-20-component3-depositor-qa.md`
> - `bellows/knowledge/qa/evidence/executable-component3-depositor-2026-08-20/`
> - `bellows/knowledge/qa/evidence/executable-component3-depositor-2026-08-20/targeted.txt`
> - `bellows/knowledge/qa/evidence/executable-component3-depositor-2026-08-20/live_canary.txt`
> - `bellows/knowledge/qa/evidence/executable-component3-depositor-2026-08-20/full_suite.txt`
>
> End with an Output Receipt (Status). Standard prompt-feedback protocol.
